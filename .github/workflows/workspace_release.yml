name: Full workspace release gate

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: workspace-release-${{ github.sha }}
  # A release-evidence run must finish even when another ref advances.
  cancel-in-progress: false

env:
  CARGO_INCREMENTAL: "0"
  CARGO_TERM_COLOR: always
  RUSTUP_TOOLCHAIN: 1.93.1

jobs:
  format:
    name: Workspace format
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ github.sha }}
      - name: Verify exact workflow source
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
        run: |
          set -euo pipefail
          source_sha="$(git rev-parse "${EXPECTED_SHA}^{commit}")"
          test "$(git rev-parse HEAD)" = "$source_sha"
          test "$WORKFLOW_SHA" = "$source_sha"
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: "false"
          components: rustfmt
          toolchain: 1.93.1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install pinned workflow-test dependency
        run: python3 -m pip install --disable-pip-version-check pytest==8.4.2
      - name: Verify release-workflow semantics
        run: python3 -m pytest -q pytests/scripts/workspace_release_gate_test.py
      - name: Verify locked metadata and workspace formatting
        run: |
          cargo metadata --locked --no-deps --format-version 1 > /dev/null
          cargo fmt --all -- --check

  build:
    name: Workspace build
    runs-on: [self-hosted, Linux, iroha2]
    container:
      image: hyperledger/iroha2-ci:nightly-2025-05-08
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ github.sha }}
      - name: Verify exact workflow source
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
        run: |
          set -euo pipefail
          source_sha="$(git rev-parse "${EXPECTED_SHA}^{commit}")"
          test "$(git rev-parse HEAD)" = "$source_sha"
          test "$WORKFLOW_SHA" = "$source_sha"
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: "false"
          toolchain: 1.93.1
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          shared-key: workspace-release-build-1.93.1
      - name: Build the full workspace
        run: cargo build --locked --workspace

  test:
    name: Workspace tests
    runs-on: [self-hosted, Linux, iroha2]
    container:
      image: hyperledger/iroha2-ci:nightly-2025-05-08
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ github.sha }}
      - name: Verify exact workflow source
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
        run: |
          set -euo pipefail
          source_sha="$(git rev-parse "${EXPECTED_SHA}^{commit}")"
          test "$(git rev-parse HEAD)" = "$source_sha"
          test "$WORKFLOW_SHA" = "$source_sha"
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: "false"
          toolchain: 1.93.1
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          shared-key: workspace-release-test-1.93.1
      - name: Test the full workspace
        run: cargo test --locked --workspace --no-fail-fast

  clippy:
    name: Strict workspace Clippy
    runs-on: [self-hosted, Linux, iroha2]
    container:
      image: hyperledger/iroha2-ci:nightly-2025-05-08
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ github.sha }}
      - name: Verify exact workflow source
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
        run: |
          set -euo pipefail
          source_sha="$(git rev-parse "${EXPECTED_SHA}^{commit}")"
          test "$(git rev-parse HEAD)" = "$source_sha"
          test "$WORKFLOW_SHA" = "$source_sha"
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: "false"
          components: clippy
          toolchain: 1.93.1
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: "true"
          shared-key: workspace-release-clippy-1.93.1
      - name: Lint every workspace target and feature
        run: >-
          cargo clippy --locked --workspace --all-targets --all-features
          -- -D warnings
