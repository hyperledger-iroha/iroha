verbose = "error"
no_progress = true
cache = true

timeout = 20
max_retries = 2

user_agent = "Mozilla/5.0 (X11; Linux x86_64; rv:10.0) Gecko/20100101 Firefox/10.0"

exclude_path = [
    "target"
]

exclude = [
    "^http[s]?://127.0.0.1",
    "^http[s]?://localhost",
    "^http[s]?://iroha"
]
