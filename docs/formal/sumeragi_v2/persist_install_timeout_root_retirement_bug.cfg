\* Leaving the Busy-deferred carrier untouched violates the atomic install
\* retirement boundary before the stale root can block the fresh window.
SPECIFICATION Spec

CONSTANT RetirementMode = "RetainDeferredCarrier"

INVARIANT TypeInvariant
INVARIANT InstallRetiresOlderOrEqualTimeoutRoots
