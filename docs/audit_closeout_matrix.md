<!-- SPDX-License-Identifier: Apache-2.0 -->

# Iroha security-audit closeout matrix

This matrix records the source-level disposition of the 21 security findings
approved for the first-release remediation. It identifies the enforcing code
and the regressions that make each disposition reviewable. It is not, by
itself, a release-qualification receipt: the final section separates evidence
that exists in the tree from validation that must be rerun on one settled
candidate.

The remediation deliberately uses fresh-genesis schema cuts. Contract
lifecycle state, moderation economics, ISO records and replay tombstones,
route-authentication metadata, and affected Norito fixtures are not inferred
or migrated from ambiguous legacy values.

## Implemented remediations

| Finding | Source-level disposition | Direct implementation and regression evidence |
| --- | --- | --- |
| `R-ISI-84` | **Implemented.** Every contract address retains immutable direct-or-Parliament deployment provenance, an account-or-Parliament lifecycle owner, two-party ownership offers, exact revision compare-and-set, revocable lifecycle-only Parliament delegation, and a bounded emergency hold. Raw activation cannot create an ownership binding. Expired holds remain auditable until an exact certified retrospective clears them. | Model and wire invariants: `crates/iroha_data_model/src/smart_contract.rs` (`lifecycle_control_enforces_revision_and_bounded_hold`) and `crates/iroha_data_model/src/isi/smart_contract_code.rs` (`lifecycle_activation_payloads_reject_missing_cas_revision`). Consensus transitions: `crates/iroha_core/src/smartcontracts/isi/world.rs` (`contract_binding_mutations_require_runtime_lifecycle_authority`, `contract_lifecycle_ownership_is_two_step_revocable_and_revision_guarded`, `parliament_due_certificate_completes_contract_ownership_transfer_to_account`, `parliament_due_certificate_completes_contract_ownership_transfer_from_account`, `parliament_due_certificate_applies_bounded_contract_emergency_hold`, `emergency_hold_retrospective_requires_exact_expired_hold_and_nonzero_finding`, `certified_retrospective_clears_exact_hold_and_allows_independent_successor_hold`, and `revoking_contract_parliament_delegation_supersedes_certified_lifecycle_effect`). `governance_certificate_v1_roundtrip_binds_body_and_ballot_roots` in `crates/iroha_data_model/src/governance/types.rs` proves the 333-of-500 rejection and 334-of-500 acceptance boundary for emergency holds. `unregister_account_rejects_contract_owner_and_pending_recipient_until_cleared` in `crates/iroha_core/src/smartcontracts/isi/domain.rs` protects both sides of an ownership offer. `contract_call_enforces_entrypoint_and_hold_before_argument_decode` in `crates/iroha_core/src/executor.rs` and `contract_call_trigger_enforces_entrypoint_and_hold_before_argument_decode` in `crates/iroha_core/src/state/tests.rs` prove that an active hold suspends direct and trigger-backed calls before argument decoding, effects, or completion events. `emergency_hold_blocks_only_its_exact_interval_and_remains_auditable_after_expiry` in `crates/iroha_core/src/smartcontracts/code.rs` pins the inclusive start, exclusive expiry, and retained retrospective state. `contract_lifecycle_survives_state_snapshot_and_preserves_canonical_root` in `crates/iroha_core/src/state/tests.rs` preserves the complete lifecycle record, rebuilds its derived subject index, and proves the canonical state root survives restart. `audit_projection_retains_complete_contract_lifecycle_state` and `audit_projection_represents_absent_lifecycle_fields_explicitly` in `crates/iroha_cli/src/gov/audit.rs` keep the complete query projection visible in CLI JSON and summarize owner, revision, and hold state in text output. `contract_owner_lifecycle_cas_and_transfer_converge_on_four_peers` in `integration_tests/tests/contracts.rs` carries the direct owner lifecycle, stale-CAS rejection, delegation/revocation, two-party transfer, former-owner denial, and new-owner activation through an exact four-validator NPoS/DA network and checks every peer's retained lifecycle projection. |
| `R-ISI-92` | **Implemented.** Registration checks every metadata value against the shared limit before Account, AssetDefinition, Domain, NFT, or RWA state is mutated. | `crates/iroha_core/src/smartcontracts/limits.rs` and the registration implementations under `crates/iroha_core/src/smartcontracts/isi/`; the five-path boundary regression is `registration_metadata_values_share_the_set_key_value_limit` in `crates/iroha_core/src/smartcontracts/isi/mod.rs`. |
| `R-ISI-111` | **Implemented.** `asset_transfer_controls` is a reserved account-metadata key. Only the dedicated transfer-control instructions may mutate it through Core, the initial executor, or the default executor. | Core guard: `crates/iroha_core/src/smartcontracts/isi/account.rs`. Executor guards and regressions: `initial_executor_cannot_bypass_native_asset_transfer_control_metadata` in `crates/iroha_core/src/executor.rs` and `default_executor_rejects_generic_transfer_control_set_and_remove` in `crates/iroha_executor/src/default/mod.rs`. The exact permission model is in `crates/iroha_executor_data_model/src/permission.rs`. |
| `R-ISI-121` | **Implemented.** Moderation challenges bind consensus-owned voting asset, bond, escrow/slash accounts, rejection slash, and resolution grace. Bond custody and accept/reject/expiry settlement are atomic, duplicate-safe, precision-aware, and idempotent. Sortition pins the first committed block whose timestamp is strictly greater than the registration deadline and keeps that draw stable across delayed QueuePlan submission. | Consensus model and schema: `crates/iroha_data_model/src/sorafs/moderation_ledger.rs` (`moderation_challenge_economics_are_consensus_fixed_and_digest_bound` and `appeal_sortition_anchor_schema_roundtrips_and_rejects_pre_cut_layout`). State transitions: `crates/iroha_core/src/smartcontracts/isi/sorafs_moderation.rs` (`challenge_submission_deadline_is_inclusive_and_one_tick_later_is_atomic`, `insufficient_challenge_bond_rejects_without_balances_records_or_counters`, `rejected_challenge_slash_floors_to_voting_asset_precision`, `accepted_challenge_blocks_reveal_and_closes_without_penalties`, `unresolved_challenge_expires_permissionlessly_and_fails_open`, `rejected_challenge_unblocks_reveals_and_tied_quorum_is_contested`, and `rejected_challenge_settlement_rolls_back_refund_when_slash_leg_fails`). Anchor regressions are `sortition_anchor_is_strictly_post_deadline_stable_and_delay_safe`, `restored_sortition_anchor_is_bound_to_exact_committed_history`, `soft_fork_replacement_repins_the_reverted_anchor_block`, and `same_deadline_anchor_schedule_is_canonical_bounded_and_pins_together` in `crates/iroha_core/src/smartcontracts/isi/sorafs/moderation_tail_tests.rs`; `sortition_maintenance_uses_the_pinned_anchor_not_the_latest_finalized_hash` and `post_registration_snapshot_without_a_pinned_anchor_fails_closed` in `crates/sorafs_node/src/moderation_orchestrator/operation_tests.rs`; and the public-path four-validator regressions `four_peer_moderation_sortition_anchor_is_post_deadline_and_queue_plan_stable` and `four_peer_moderation_challenge_settlements_converge_and_conserve_bonds` in `integration_tests/tests/sorafs_moderation_anchor.rs`. The settlement regression uses real challenge-bound PoP admission, verifies pending escrow, exact rejected refund/slash and accepted refund, total balance conservation, case counters, and byte-identical state across all four peers. `validate_persisted_moderation_schema_v1` is called by both ordinary state initialization and snapshot restoration; `startup_rejects_pre_cut_moderation_policy_layout`, `startup_rejects_pre_cut_moderation_appeal_layout`, and `startup_rejects_pre_cut_moderation_case_layout` prove that the first-release schema cut fails fast. The dedicated Rust client routes are signed with `TransactionAdmissionIntent::QueuePlanSynced`, with exact wire assertions in `client::tests::sorafs_moderation_signed_transaction_routes_use_exact_versioned_norito`. Orchestration remains in `crates/sorafs_node/src/moderation_orchestrator.rs`. |
| `R-ISI-122` | **Implemented.** Ordinary data triggers require one exact authority-owned subject. A bonded-citizen Parliament proposal grants or revokes the global-trigger permission for one exact account; the permission cannot be self-issued or widened. Per-authority and global registration cardinality, deterministic exact subject/family candidate selection, and per-transaction indexed-check, firing, and gas work have explicit consensus bounds. | Scope and registration caps: `crates/iroha_core/src/smartcontracts/isi/triggers/mod.rs` (`ordinary_data_triggers_require_an_exact_owned_subject`, `global_data_trigger_permission_is_bound_to_the_trigger_authority`, `data_trigger_authority_capacity_rejects_the_sixty_fifth_registration`, and `global_data_trigger_capacity_rejects_the_four_thousand_ninety_seventh_registration`). Governance admission, fingerprinting, and enactment use `ProposalKind::GlobalDataTriggerPermissionGovernance` in `crates/iroha_data_model/src/governance/types.rs` and `crates/iroha_core/src/smartcontracts/isi/world.rs`; `parliament_due_certificate_grants_and_revokes_exact_global_trigger_permission` proves exact direct state mutation, while the proposal codec/fingerprint regressions prevent target substitution. Permission encoding is covered by `global_data_trigger_permission_binds_one_exact_authority` in `crates/iroha_executor_data_model/src/permission.rs`. The deterministic index in `crates/iroha_core/src/smartcontracts/isi/triggers/set.rs` uses exact subject/family `BTreeMap<..., BTreeSet<TriggerId>>` postings, covered by `exact_account_and_domain_postings_bound_nested_event_candidates` and `asset_postings_deduplicate_multi_constraint_candidates`. `capture_data_events` in `crates/iroha_core/src/state.rs` incrementally charges each bounded candidate vector before canonical filter revalidation, avoiding a pre-charge aggregate allocation. `crates/iroha_core/src/state/tests.rs` covers atomic rollback, execution depth, the 256-firing ceiling, shared recheck/firing gas budget, and native-instruction metering with `data_trigger_fanout_rejects_the_two_hundred_fifty_seventh_firing`, `data_trigger_filter_recheck_and_firing_share_the_block_gas_budget`, and `native_trigger_instructions_are_not_an_unmetered_execution_path`. `fee_enabled_single_transfer_with_active_data_trigger_uses_durable_state_fallback` and `same_block_data_trigger_registration_is_atomic_with_rejected_transfer` in `crates/iroha_core/src/block/fee_admission_tests.rs` prove the candidate-wide trigger-ordering fence and atomic rollback of the later transfer and failing trigger effects. `four_peer_scoped_one_shot_data_triggers_roll_back_atomically` in `integration_tests/tests/triggers/data_trigger.rs` exercises deterministic four-validator execution and rollback of the originating transaction. |
| `R-TOR-08` | **Implemented.** Transaction ingress has a pre-authentication IP/API-token limiter and a separate authority limiter charged only after signature verification. Batch queue mutation starts only after decoding, signature precheck, and all ordered authority charges succeed. | `crates/iroha_torii/src/lib.rs` (`preauth_capacity_is_enforced_before_api_token_authentication`, `transaction_batch_rate_limit_collapses_same_authority_run`, and `transaction_batch_rate_limit_preserves_authority_ordering`); `crates/iroha_torii/src/tests/lib_runtime_handlers/part_1.rs` (`invalid_transaction_signature_does_not_charge_claimed_authority_bucket`); `part_2.rs` (`handler_post_transactions_batch_rejects_invalid_ed25519_precheck_without_partial_push`). |
| `R-TOR-34` | **Implemented.** Route metadata distinguishes optional canonical account authentication, exact dataspace visibility, operator identity, and authenticated protocol principals. Every descriptor and generated projection carries the explicit first-release route-auth metadata schema version; incompatible or pre-versioned metadata fails catalog validation. A route cannot claim an authentication boundary without the matching sealed mounting witness. | Catalog invariants: `crates/iroha_torii_shared/src/route_catalog/tests.rs` (`route_auth_metadata_schema_is_v1_in_every_projection_and_fails_closed`, `route_auth_metadata_labels_are_first_release_stable`, `dataspace_reads_advertise_their_real_authentication_boundary`, including both committed contract replay routes, `dataspace_admission_rejects_a_hollow_authentication_witness`, `canonical_catalog_satisfies_closed_security_axes`, and `protocol_handshake_is_reserved_for_protocol_principals`). `openapi_route_auth_metadata_matches_enabled_catalog_projection` in `crates/iroha_torii/src/openapi.rs` checks the descriptor-derived `x-iroha-route-auth` extension for every enabled operation; `every_catalog_tool_descriptor_publishes_exact_route_auth_metadata` in `crates/iroha_torii/src/mcp/catalog_and_policy_tests.rs` checks the equivalent MCP `tools/list` metadata. `scripts/tests/torii_lib_wrapper_macro_inventory_test.py` binds the schema version into its ordered mount/auth digest. Mounting invariants: `crates/iroha_torii/src/router/builder.rs` (`authentication_policy_mismatch_fails_before_mounting`, `arbitrary_middleware_cannot_claim_operator_authentication`, and `handler_and_unauthenticated_witnesses_match_only_their_declared_policy`). |
| `R-TOR-55` | **Implemented.** Torii carries an internal `ScopedEvent { event, scope }` envelope before subscriber filters and projections. A committed transaction is visible only when every coordinator/participant dataspace from every matching routing plan is visible; malformed, stale, or unknown scope fails closed to a global reader. Generic, contract, Explorer, and full-block streams retain their admitted principal/scope and terminate generically after account or permission revocation. The committed contract activity, event, and all eleven rollup replay routes apply dataspace visibility before replay, user filters, offsets, counts, stitching, candles, analytics, or entity projection. The native full-block stream requires `CanReadAllLedgerData`. | Scope extraction and `dataspace_visibility_requires_every_committed_route_leg`: `crates/iroha_torii/src/lib.rs` and `crates/iroha_torii/src/routing.rs`. Replay ordering is covered by `contract_activity_visibility_precedes_filters_offsets_and_counts`, `contract_event_visibility_precedes_filters_offsets_and_counts`, `uranai_rollup_visibility_precedes_replay_counts_and_projection`, `trader_rollup_visibility_precedes_filters_offsets_and_counts`, `swap_fill_stitching_omits_records_without_a_visible_committed_event`, and `empty_visible_swap_scope_omits_contract_and_asset_projection`. `contract_rollup_replays_are_dataspace_visible_optional_identity_reads` in `crates/iroha_torii_shared/src/route_catalog/tests.rs` binds all eleven route descriptors to the same policy. Dynamic revocation: `long_lived_dataspace_context_rechecks_permission_revocation`, `long_lived_visibility_retention_detects_every_revocation_shape`, `sse_authorization_revocation_is_generic_and_terminal`, `revoked_event_stream_uses_policy_violation_close`, `revoked_block_stream_uses_policy_violation_close`, and `block_delivery_rechecks_authorization_before_each_item`. The full-block handshake and ongoing permission check are in `handler_blocks_stream_ws`; the Rust client signs exact generic-event and full-block WebSocket upgrades after removing stale authentication headers, covered by `events_handler_canonically_authenticates_exact_stream_upgrade` and `blocks_handler_canonically_authenticates_exact_stream_upgrade` in `crates/iroha/src/client.rs`. |
| `R-TOR-56` | **Implemented.** Request identity and visible dataspace routes are centralized. Anonymous application reads see active public routes only; a valid account signature adds current UAID and exact-permission routes. Partial authentication returns `401` instead of becoming anonymous. Holder and Explorer surfaces use the same visibility object. Global aggregate Explorer metrics remain a signed `CanReadAllLedgerData` surface, while ledger-free Explorer health remains public. Available SDKs use an optional immutable default signer for dataspace-visible reads and otherwise preserve anonymous public access. | `crates/iroha_torii/src/application_account_auth.rs`, `crates/iroha_torii/src/lib.rs`, and `crates/iroha_torii/src/routing.rs`. `dataspace_reads_advertise_their_real_authentication_boundary` in `crates/iroha_torii_shared/src/route_catalog/tests.rs` inventories list, latest, stream, entity, snapshot, econometrics, QR, block, transaction, instruction, contract replay, and holder routes. Focused routed-read regressions in `crates/iroha_torii/src/tests/lib_routed_reads/part_1.rs` are `torii_partition_routes_by_visibility_counts_private_dataspaces_as_denied_for_unsigned_reads`, `torii_visibility_account_from_headers_rejects_unsigned_account_header`, `torii_visibility_account_from_headers_rejects_partial_signature_as_unauthorized`, `torii_visibility_account_from_headers_accepts_signed_caller`, and `torii_partition_routes_by_visibility_allows_bound_private_dataspaces_for_signed_caller`. Client evidence includes `ToriiBrowserClient signs every scoped Explorer and contract read over its final URL`, `ToriiBrowserClient leaves optional dataspace reads anonymous without a signer`, and `ToriiBrowserClient rejects partial, precomputed, and redirected canonical read auth` in `javascript/iroha_js/test/toriiBrowserClient.test.js`; `listExplorerNfts validates cursor pagination and encodes filters` plus `dataspace-visible Explorer reads stay anonymous without a default signer` in `javascript/iroha_js/test/toriiClient.test.js`; `testGetExplorerInstructionsEncodesQueryAndDecodesResponse`, `testDataspaceVisibleExplorerRequestRemainsAnonymousWithoutDefaultSigner`, `testStreamExplorerTransactionsAsync`, `testGenericEventStreamsSignExactFinalQueriesWithDefaultCanonicalAuth`, and `testGenericEventStreamRemainsAnonymousWithoutDefaultCanonicalAuth` in `IrohaSwift/Tests/IrohaSwiftTests/ToriiClientTests.swift`; `CanonicalCredentialsDoNotSignExplicitlyAnonymousPublicRoutes` and `CanonicalCredentialsSignVisibilitySensitiveReadRoutes` in `csharp/tests/Hyperledger.Iroha.Sdk.Tests/ToriiClientTests.cs`; `test_explorer_rwa_list_optionally_signs_exact_final_uri` and `test_live_event_stream_optionally_signs_exact_final_uri` in `python/iroha_python/tests/`; `test_get_explorer_account_qr_optionally_signs_exact_final_path` in `python/iroha_torii_client/tests/test_client.py`; `canonicalSigningBindsTheExactFinalStreamUri`, `contractStreamRemainsAnonymousWithoutSigningConfiguration`, and `rejectsPrecomputedOrPartialCanonicalHeadersBeforeDispatch` in `kotlin/core-jvm/src/test/kotlin/org/hyperledger/iroha/sdk/client/stream/ToriiEventStreamClientTest.kt`; and the mirrored `sseCanonicalSigningBindsTheExactFinalUri`, `contractSseRemainsAnonymousWithoutSigningConfiguration`, and `sseRejectsPrecomputedOrPartialCanonicalHeadersBeforeDispatch` in `java/iroha_android/src/test/java/org/hyperledger/iroha/android/client/stream/ToriiEventStreamClientTests.java`. |
| `R-TOR-58` | **Implemented.** Durable ISO replay tombstones retain message ID, payload hash, business message ID, and UETR for the full deduplication TTL. Replay admission is persisted before rich-record, signing, or lifecycle work; protected capacity returns retryable `503` without state mutation. Message, tombstone, and audit artifacts make a strict first-release V2 cut: malformed or conflicting durable identities stop startup instead of being silently discarded. The derived audit index is atomically written and bounded; a missing or schema-valid stale index is regenerated only after complete store validation. | `crates/iroha_torii/src/iso20022_bridge.rs`; regressions in `crates/iroha_torii/src/iso20022_bridge_tests.rs`: `unexpired_replay_tombstone_survives_detail_pruning_and_restart`, `protected_replay_capacity_fails_closed_without_mutation`, `detail_persistence_failure_keeps_the_precommitted_replay_tombstone`, `legacy_iso_record_store_fails_fast_with_schema_incompatibility`, `unversioned_or_malformed_iso_record_store_stops_startup`, `malformed_or_unversioned_iso_audit_index_stops_startup`, `digest_correct_malformed_iso_audit_entries_stop_startup`, `conflicting_v2_replay_and_transaction_identities_stop_startup`, and the missing/stale-index regeneration regressions. The Python notary and receipt verifiers in `scripts/iso_audit_notary_adapter.py` and `scripts/iso_operator_receipt_verify.py` require record/index V2, exact party metadata, and replay expiry without a V1 fallback. |
| `R-TOR-59` | **Implemented.** ISO records retain originator, counterparty, admitting participant/operator, pinned profile, and immutable replay identity. Either original party may read its records; separately configured audit administrators receive a global read-only view and cannot mutate bridge state. | Participant configuration is in `crates/iroha_config/src/parameters/`; enforcement is in `crates/iroha_torii/src/iso20022_bridge.rs` and route handlers in `crates/iroha_torii/src/lib.rs`. `participant_catalog_binds_initial_from_and_scopes_reads_to_both_parties` and `participant_catalog_rejects_legacy_unscoped_and_overlapping_admin_keys` in `crates/iroha_torii/src/iso20022_bridge_tests.rs` cover provenance, party reads, audit scope, and admin/mutation-key separation. |
| `R-TOR-62` | **Implemented.** ISO lifecycle updates authorize the authenticated participant against the original durable record, retain its profile and embedded-signature policy, require committed transaction evidence before a settlement status is accepted, and sign outbound XML over exact domain-separated bytes. | `crates/iroha_torii/src/iso20022_bridge.rs`; `lifecycle_roles_reject_cross_party_updates`, `settling_pacs002_requires_committed_transaction_evidence`, and `outbound_document_signature_covers_exact_xml_bytes` in `crates/iroha_torii/src/iso20022_bridge_tests.rs`; shared message fixtures under `fixtures/iso20022/`. |
| `R-TOR-64` | **Implemented.** Explorer collections use bounded keyset cursors. History cursors additionally bind the committed snapshot, route, filters, and visible-route-set digest; responses expose `has_more`, and blocking scans keep their query-admission permit. The shipped Taira edge now trusts only its exact loopback nginx hop, so the proxy-appended client address restores per-client pre-authentication, rate, and ban identities instead of collapsing public traffic into the loopback allowlist/bucket. | `crates/iroha_torii/src/explorer.rs` (`history_cursor_is_snapshot_filter_visibility_and_route_bound`, `explorer_cursor_is_canonical_collection_and_filter_bound`, `explorer_cursor_query_defaults_and_rejects_retired_page_fields`, and `sparse_cursor_scan_is_bounded_and_does_not_skip_first_unreturned_match`). Snapshot validation and admitted blocking handlers are in `crates/iroha_torii/src/routing.rs`; `explorer_heavy_history_routes_are_bound_to_cancellation_safe_worker` inventories all five HTTP/admitted/synchronous handler chains, while `cancelled_query_cannot_release_admission_before_blocking_worker_finishes` proves that aborting a caller cannot release the permit before the physical worker completes. Trusted-proxy chain behavior is covered in `crates/iroha_torii/src/limits.rs`. `configs/soranexus/taira/config.toml` pins `torii.transport.trusted_proxy_cidrs` to `127.0.0.1/32`, and `test_public_edge_is_the_only_trusted_torii_forwarding_hop` in `scripts/tests/render_taira_edge_nginx_conf_test.py` binds that runtime policy to the nginx forwarding contract. |
| `R-TOR-65` | **Implemented.** Recoverable blocking workers suppress the shutdown panic hook only on the worker thread, convert the unwind to a controlled error, and clear suppression before a pooled worker is reused. | `crates/iroha_core/src/panic_hook.rs` (`caught_panic_is_suppressed_only_for_the_recovery_boundary` and `blocking_worker_reuse_does_not_retain_suppression`) and `crates/iroha_torii/src/panic_recovery.rs` (`blocking_panic_is_controlled_and_suppression_clears`). |
| `R-TOR-66` | **Implemented.** Reviewed request, proof, provider, DA, and Halo2 boundaries use same-thread or joined-task recovery. Consensus and supervisor invariants remain fail-stop. A source guard rejects new bare recovery boundaries. | Recovery helpers and negative invariant test: `crates/iroha_torii/src/panic_recovery.rs` (`joined_async_panic_is_controlled` and `ordinary_invariant_panics_remain_unsuppressed`). The guarded inventory includes SoraFS gateway compliance, hedging/billing, orderbook and reserve signers, and drand high-water persistence in addition to request, proof, DA, and Halo2 workers. Static policy and its regression suite: `scripts/check_panic_recovery_boundaries.py` and `scripts/tests/panic_recovery_boundaries_test.py`. |
| `R-UNM-01-H24` | **Implemented.** Four local-only CID/site routes are anonymously mounted on every SoraFS-enabled node, independently of the optional app API. Client storage tokens are rejected. Canonical local-manifest validation, provider admission, compliance, trusted-proxy-aware rate/ban policy, concurrency, isolated paths/origins, and byte/range bounds run before readback; CID-origin redirects occur only after that same admission. | `crates/iroha_torii/src/sorafs/public_gateway.rs` (`every_public_gateway_entrypoint_rejects_client_storage_tokens`, `public_gateway_concurrency_limit_fails_fast_with_retry_after`, `no_app_api_gateway_primitives_keep_local_reads_canonical_isolated_and_bounded`, and `no_app_api_gateway_maps_governed_takedowns_and_provider_denials`) plus `public_site_and_every_cid_read_share_provider_admission_policy` in `crates/iroha_torii/src/sorafs/api.rs`. Catalog closure is `public_sorafs_gateways_are_anonymous_and_feature_independent` in `crates/iroha_torii_shared/src/route_catalog/tests.rs`; the router-level `public_sorafs_gateway_routes_are_mounted_without_app_api` regression proves that all four routes remain mounted when `app_api` is absent. |
| `R-UNM-53` | **Implemented.** Enabling relay incentive ingestion requires a canonical roster of 1–64 verifier accounts. Bounded decoding, roster membership, and `RelayBandwidthProof::verify_signature()` all complete before the performance accumulator is locked or changed. | Roster bounds and lookup: `tools/soranet-relay/src/incentive_log.rs`; startup regressions `enabled_incentive_ingestion_rejects_an_empty_verifier_roster` and `incentive_verifier_roster_accepts_sixty_four_and_rejects_sixty_five` in `tools/soranet-relay/src/config_tests.rs`; mutation-order regressions `bandwidth_proof_populates_accumulator` and `bandwidth_proof_rejects_tampering_and_untrusted_verifiers_without_mutation` in `tools/soranet-relay/src/runtime/tests.rs`. |

## Findings closed by retained regression evidence

These four findings required verification that the already-selected production
boundary remained closed rather than a new public behavior.

| Finding | Source-level disposition and direct evidence |
| --- | --- |
| `R-ISI-76` | **Regression retained.** Every oracle-economics field contributes to the consensus execution-policy digest. `execution_policy_digest_binds_every_oracle_economics_field` in `crates/iroha_config/src/parameters/actual/tests.rs` mutates the fields independently, and `durable_context_recovery_rejects_local_oracle_economics_drift` in `crates/iroha_core/src/sumeragi/v2_recovery_tests.rs` rejects a recovery mismatch. |
| `R-TOR-60` | **Regression retained.** `/v1/debug/witness` remains an operator-authenticated diagnostic and is served only under a developer telemetry profile. `debug_witness_requires_a_developer_telemetry_profile` and `debug_witness_operator_and_telemetry_profile_matrix` in `crates/iroha_torii/src/tests/lib_runtime_handlers/part_1.rs`, the route descriptor in `crates/iroha_torii_shared/src/route_catalog.rs`, and `specs/telemetry.md` define that boundary. The Rust client and `iroha audit witness` use an exact operator-signed request for both JSON and Norito representations; `debug_witness_json_uses_exact_operator_authentication_boundary` and `debug_witness_norito_uses_exact_operator_authentication_boundary` assert all four operator headers and the absence of account-signature headers. |
| `R-CON-28` | **Regression retained.** Adversarial nested `NativeAmx` and `LaneRelay` frames are rejected by global decode depth/allocation limits before unbounded recursion. See `native_amx_and_lane_relay_fall_back_to_canonical_global_decode_limits` in `crates/iroha_core/src/lib.rs` and `canonical_decode_limits_reject_recursive_native_amx_commitment_frames` in `crates/iroha_data_model/src/block/consensus_model_tests.rs`. |
| `R-UNM-06` | **Regression retained.** Release dependency-graph checks prove development test fixtures are absent from shipping artifacts. See `scripts/check_release_feature_graph.py` and `test_shipping_packages_exclude_test_fixtures` in `scripts/tests/release_feature_graph_test.py`. |

## Cross-surface closure addenda

- `R-ISI-84`: `strict_governed_contract_lifecycle_rejects_consensus_impossible_values`
  in `crates/iroha_cli/src/gov/audit.rs` keeps CLI audit acceptance in parity
  with consensus account, owner, Parliament-origin, delegation, and emergency-
  hold invariants.
- `R-TOR-34`: `openapi_standard_security_matches_enabled_catalog_authentication`,
  `protocol_specific_bootle_bearer_security_is_preserved`, and
  `static_authority_is_the_complete_catalog_projection_with_exact_effects`
  bind standard and protocol-specific OpenAPI security to the route catalog.
  MCP regressions `generic_xml_request_body_uses_exact_text_bytes_and_media_type`
  and `generated_iso_tool_advertises_raw_xml_as_its_default_media_type` prevent
  XML quoting or media-type drift. JavaScript and Python Sumeragi status-stream
  tests require a fresh operator signature and forbid redirects or automatic
  retries for each one-shot subscription.
- `R-TOR-56`: `ToriiClient uses its configured signer for optional account reads`
  and `ToriiClient permits explicit anonymous optional account reads` cover the
  JavaScript account surface. Python adds
  `test_dataspace_visible_account_reads_use_configured_canonical_signer` and
  `test_dataspace_visible_account_reads_remain_anonymous_without_signer`; the
  Swift account helpers use the same final-URI optional-signature path.
  The C# client rejects canonical signatures over non-anonymous routes when its
  injected `HttpClient` has no internal one-shot/no-redirect assurance, before
  any handler dispatch. `CanonicalCredentialsRejectUnverifiedInjectedTransportAtConstruction`
  covers four read families, and `CanonicalAccountReadRedirectIsSurfacedWithoutReplay`
  proves that the internally managed transport surfaces redirects after one
  original dispatch without replaying the signed nonce or signature.
- `R-TOR-66`: the guarded recovery inventory includes both Kagemusha
  accumulation and recursion adapters, which use the reviewed suppressed panic
  boundary and are pinned by `scripts/check_panic_recovery_boundaries.py`.

## Qualification boundary

The rows above mean that an enforcing source path and reviewable regression are
present for all 21 findings. They do not claim that the current shared working
tree, an arbitrary dirty build, or an earlier commit passed the final release
corridor.

Release qualification must be collected from one settled candidate and must
record the exact commands and results for formatting, focused crate checks and
tests, codec/source guards, generated OpenAPI and route-catalog equality, SDK
parity suites, relevant four-validator integration scenarios, strict
all-target Clippy, and the workspace test gate. A timed-out, blocked, or
deferred command is recorded as such and is not a pass. Documentation route and
translation parity must be validated in the sibling `iroha-docs` checkout.
This remediation does not author either Cargo lockfile. Release qualification
requires the repository owner to resolve any externally owned manifest/lock
delta first; an inconsistent or unexplained shared-checkout lockfile is a
blocker, not evidence to normalize or overwrite from this remediation lane.
