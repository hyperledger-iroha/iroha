---
lang: es
direction: ltr
source: docs/source/references/configuration.md
status: complete
generator: scripts/sync_docs_i18n.py
source_hash: 74af460760b65e41904f7b9b7fed03c60bb24155464cf00b76680146a777b493
source_last_modified: "2025-12-27T07:56:10.861922+00:00"
translation_last_reviewed: 2025-12-28
---

# Resumen de configuración

Esta página resume las secciones de nivel superior del archivo de configuración de Iroha (TOML) y lo que controla cada una. Para una plantilla concreta, vea `docs/source/references/peer.template.toml`.

Primero los valores por defecto: los valores de configuración están preparados para despliegues típicos de blockchain de Iroha. En la mayoría de los casos, puede ejecutar con los valores por defecto sin cambios. Ajuste los parámetros solo cuando tenga una necesidad operativa específica (p. ej., puertos personalizados, límites de tasa estrictos o restricciones de red conocidas).

- Las secciones específicas de Sora (`torii.sorafs`, `sorafs.*`, multi-lane `nexus.*`) requieren iniciar el daemon con `irohad --sora …` (o exportar `IROHA_SORA_PROFILE=1`). Sin la bandera, el cargador rechaza configuraciones que habiliten esas funciones.
- Cuando `nexus.enabled = false` (modo Iroha 2), las configuraciones siguen siendo de un solo carril: `/status` y `/v1/sumeragi/status` ocultan las secciones de carril/espacio de datos, `/metrics` omite por completo las etiquetas de carril/espacio de datos, y los tails de estado con alcance de carril devuelven `StatusSegmentNotFound`, por lo que los SDK deben ramificar según la bandera antes de enviar parámetros de carril. El análisis de configuración rechaza sobrescrituras de carril/espacio de datos/enrutamiento en este modo: elimine las secciones `nexus.*` a menos que habilite Nexus/Sora con `nexus.enabled=true` o `--sora`.
- `[common]`: Identidad de la cadena (`chain`), par de claves del nodo, peers de confianza, la etiqueta implícita de dominio de cuenta (`default_account_domain_label`) y el discriminante de cadena IH58 (`chain_discriminant`). Modifique esto cuando su dominio de génesis o prefijo de red difieran de los valores por defecto integrados para que las direcciones IH58/comprimidas sigan siendo canónicas.
- Admisión de validadores: `trusted_peers` debe listar claves de validador BLS-Normal, y las PoP viven en `[[trusted_peers_pop]] { public_key, pop_hex }`. El mapa de PoP debe cubrir todo el roster; PoP faltantes o inválidos se rechazan al parsear la configuración. El análisis de configuración ahora rechaza el mapeo alternativo `trusted_peers_bls` y cualquier clave de validador no BLS. La identidad de transporte sigue usando `public_key`/`private_key` para P2P/Torii, mientras que las firmas de consenso usan solo la clave BLS + PoP.
- Separación de identidades: el par `public_key`/`private_key` asegura el transporte (P2P, Torii) y puede permanecer en Ed25519/SECP/etc. independientemente de `allowed_signing`. El consenso usa la clave BLS-Normal de `trusted_peers` con su PoP. La admisión de transacciones es el único lugar donde se aplican `crypto.allowed_signing`/`allowed_curve_ids`; si quiere enviar transacciones firmadas con BLS, agregue explícitamente `bls_normal` a `allowed_signing` y el id de curva correspondiente a `crypto.curves.allowed_curve_ids`. El consenso seguirá funcionando con validadores BLS incluso cuando `allowed_signing` se deje con los valores por defecto Ed25519/secp.
- `[genesis]`: Ubicación del archivo de génesis firmado y la clave pública usada para verificarlo. El manifiesto referenciado lleva familias de parámetros como `transaction.max_signatures`, `transaction.max_instructions`, `transaction.ivm_bytecode_size`, `transaction.max_tx_bytes` y `transaction.max_decompressed_bytes`; actualice el manifiesto al endurecer límites de admisión. Los nodos que arrancan con almacenamiento vacío y sin un `genesis.file` local pueden obtener la génesis de peers de confianza opcionalmente fijando `expected_hash`, proporcionando un `bootstrap_allowlist` (por defecto `trusted_peers`) y acotando la transferencia con `bootstrap_max_bytes` y `bootstrap_response_throttle_ms`. Los intentos de bootstrap respetan `bootstrap_request_timeout_ms`, `bootstrap_retry_interval_ms`, `bootstrap_max_attempts` y `bootstrap_enabled`; los respondedores siempre hacen cumplir coincidencias de chain-id/clave pública/hash y rechazan cargas sobredimensionadas o peers fuera de la allowlist.
- `[network]`: Direcciones P2P, periodos/tamaños de gossip, refresco DNS, capacidades de colas acotadas y opciones de transporte (TLS/QUIC cuando están habilitadas).
- `[ivm]`: Ajustes del entorno VM. `ivm.memory_budget_profile` (por defecto: `compute.default_resource_profile`) selecciona qué perfil de recursos de cómputo suministra `max_stack_bytes` como límite de presupuesto de pila del invitado.
- `[tiered_state]`: Controles de WSV hot/cold (`enabled`, `hot_retained_keys`, `hot_retained_bytes`, `hot_retained_grace_snapshots`, `cold_store_root`, `da_store_root`, `max_snapshots`, `max_cold_bytes`). Si `cold_store_root` no está configurado, los snapshots se guardan bajo `da_store_root`; cambiar cualquiera de las raíces reinicia los metadatos del tiering.
- `[ivm.banner]` (valores por defecto: `show = true`, `beep = true`): Controla el banner de inicio de Norito/IVM y el jingle opcional. La bandera `beep` solo se respeta cuando el binario se construye con la feature `beep`. Esto reemplaza el shim de entorno `IROHA_BEEP` heredado: desactive el banner o el beep vía configuración en lugar de overrides por entorno.
- `[concurrency]`: Tamaño del pool de hilos y presupuestos de pila para trabajadores de scheduler/prover y el invitado IVM. `gas_to_stack_multiplier` (por defecto `4`) acota el espacio de pila del invitado como `min(guest_stack_bytes, gas_limit * multiplier, stack_budget)` con un piso de 64 KiB y un techo de 1 GiB; `stack_budget` viene de la selección `ivm.memory_budget_profile`. Las solicitudes fuera de esa ventana se recortan, se registran y se exportan vía `/status.stack` y las métricas `iroha_ivm_stack_*` (incluyendo contadores de fallback de pool/impacto de presupuesto). Reduzca el multiplicador para limitar la recursión en rutas de bajo gas o súbalo (dentro del límite de pila del invitado) para admitir marcos Kotodama más profundos.
- `network.peer_gossip_period_ms` (por defecto: `250`ms) también gobierna la cadencia de refresco de topología. El override de entorno `IROHA_P2P_TOPOLOGY_UPDATE_MS` ahora se ignora; ajuste el campo de configuración en su lugar. Los periodos de gossip (`block_gossip_period_ms`, `transaction_gossip_period_ms`, `peer_gossip_period_ms`) y `idle_timeout_ms` se limitan a un mínimo de 100 ms para evitar bucles apretados.
- `network.trust_gossip` (por defecto: `true`) anuncia soporte para tramas TrustGossip. Al establecerlo en `false`, se negocia la capacidad fuera en el handshake, descartando mensajes TrustGossip mientras se deja intacto el gossip de direcciones de peers. Los descartes se etiquetan vía `p2p_trust_gossip_skipped_total{direction,reason}` para que los operadores puedan detectar opt-outs o peers que rechazaron la capacidad.
- Las perillas de puntuación de confianza bajo `[network]` (`trust_decay_half_life_ms`, `trust_penalty_bad_gossip`, `trust_penalty_unknown_peer`, `trust_min_score`) se aplican en overlays permissioned: las tramas de confianza que referencian peers fuera de la topología activa incurren en la penalización de peer desconocido, incrementan `p2p_trust_penalties_total{reason="unknown_peer"}`, y los remitentes por debajo de `trust_min_score` se ignoran hasta que la decadencia se recupere. Los overlays públicos/NPoS omiten la penalización de peer desconocido para que el trust gossip permanezca abierto.
- Ajuste del gossip de transacciones: `network.transaction_gossip_drop_unknown_dataspace` descarta lotes para dataspaces que faltan en el catálogo de carriles; `network.transaction_gossip_restricted_target_cap` limita el fanout restringido (por defecto la topología completa de commit); `network.transaction_gossip_public_target_cap` limita el fanout del carril público (por defecto 16, establezca `null` para broadcast); `network.transaction_gossip_public_target_reshuffle_ms` y `network.transaction_gossip_restricted_target_reshuffle_ms` controlan cada cuánto se remezcla la selección de objetivos por plano (por defecto `transaction_gossip_period_ms`, limitado a ≥100 ms); `network.transaction_gossip_resend_ticks` define cuántos períodos de gossip pasan antes de reenviar las mismas transacciones (un valor mayor reduce duplicados); `network.transaction_gossip_restricted_fallback` (`drop` | `public_overlay`) controla si el gossip restringido cae a peers online cuando la topología de commit está vacía; y `network.transaction_gossip_restricted_public_payload` (`refuse` | `forward`, por defecto `refuse`) actualmente se niega a filtrar cargas restringidas cuando solo el overlay público está disponible (el reenvío se bloquea hasta que llegue transporte cifrado). La telemetría expone la política de drop/forward vía gauges y entradas `/status.tx_gossip` (caps, targets/peers, superficie de fallback, motivo/resultado) para que los operadores alerten sobre lotes bloqueados o redirigidos.
- `network.soranet_handshake.pow`: Configuración del desafío para admisión SoraNet. Campos: `required`, `difficulty`, `max_future_skew_secs`, `min_ticket_ttl_secs`, `ticket_ttl_secs`, `revocation_store_capacity`, `revocation_max_ttl_secs` y `revocation_store_path`. El revocation store controla cuántos tickets firmados se retienen en disco y cuánto tiempo permanecen válidos para detección de repetición.
- `network.soranet_handshake.pow.puzzle`: toggles de puerta de puzzle Argon2. Establezca `enabled = true` para requerir tickets de memoria dura y ajuste `memory_kib` (>=4096), `time_cost`, y `lanes` (1–16). Cuando está habilitado el relay sigue respetando `difficulty`, `max_future_skew_secs`, y `min_ticket_ttl_secs`; deshabilitar el bloque vuelve al PoW clásico hashcash.
- `network.soranet_handshake.kem_suite`: override textual opcional (`"mlkem512"`, `"mlkem768"`, o `"mlkem1024"`; también se aceptan alias `kyber*`) para el perfil ML-KEM usado durante el handshake SoraNet. Cuando se establece, sustituye `kem_id` y valida las longitudes de clave Kyber configuradas para el suite seleccionado.
- `[sumeragi]`: Rol de consenso (validador/observador) y ajustes de colector (K, redundant-send r), más opciones de holgura de temporización. La compuerta DA usa `da_enabled` para aplazar el commit hasta observar evidencia de disponibilidad (un `availability evidence` o un quorum RBC `READY`); el commit no espera entregas RBC locales. La contrapresión por cola se configura con `msg_channel_cap_votes`, `msg_channel_cap_block_payload`, `msg_channel_cap_rbc_chunks`, `msg_channel_cap_blocks`, y `control_msg_channel_cap`; el legado `msg_channel_cap` actúa como fallback solo cuando se omiten las capacidades por cola. `mode_flip_enabled` (por defecto `true`) actúa como interruptor de seguridad para cambios en vivo permissioned↔NPoS: cuando es `false`, el nodo anuncia el modo en preparación vía estado/telemetría pero se niega a cambiar hasta restaurar la bandera. Los fallos de persistencia retroceden según `kura_store_retry_interval_ms`/`kura_store_retry_max_attempts` para que los bloques permanezcan pendientes hasta que Kura los acepte o se agote el presupuesto de reintentos; `commit_inflight_timeout_ms` aborta trabajos de commit atascados para que las propuestas no se bloqueen indefinidamente. La búsqueda de bloques faltantes prefiere firmantes de commit certificate durante `missing_block_signer_fallback_attempts` intentos (por defecto 1; ponga 0 para omitir preferencia) antes de volver a la topología completa de commit. La deriva del hash de membresía se señala mediante `membership_mismatch_alert_threshold` (desajustes consecutivos antes de alertar) y `membership_mismatch_fail_closed` (descarta mensajes de consenso de pares que siguen discrepando). El intervalo de propuesta del pacemaker se deriva de `npos.block_time_ms`, `npos.timeouts.propose`, `pacemaker_rtt_floor_multiplier`, y está acotado por `pacemaker_max_backoff_ms`; aumente el piso de RTT o el tiempo de bloque para ralentizar propuestas en enlaces de alta latencia. Los stashes RBC pendientes se limitan entre sesiones por `sumeragi.rbc_pending_session_limit`. El quorum RBC READY/DELIVER siempre sigue la topología de commit (`Topology::min_votes_for_commit()`).
- `[torii]`: Ajustes del servidor de API pública como dirección de escucha, límites de tamaño de solicitud y parámetros del almacén de consultas.
- `torii.data_dir` (por defecto: `./storage/torii`): Directorio base para la persistencia de Torii (adjuntos, registro de webhooks, cachés DA). Reemplaza el shim de entorno `IROHA_TORII_DATA_DIR` heredado; los tests pueden anular temporalmente mediante `data_dir::OverrideGuard`.
- `torii.events_buffer_capacity` (por defecto: `10000`): Tamaño del canal de broadcast usado para `/v1/events/sse` y encolado de webhooks. Baje el valor para acotar memoria/contrapresión cuando los suscriptores se retrasan.
- `torii.ws_message_timeout_ms` (por defecto: `10000`): Timeout de mensajes WebSocket (lectura/escritura) para los streams de eventos/bloques de Torii; aumente para clientes lentos o enlaces con alta latencia.
- `torii.app_api.*`: Valores por defecto de paginación/contrapresión para endpoints JSON de conveniencia. `default_list_limit` inicializa `limit` cuando se omite, `max_list_limit` y `max_fetch_size` acotan tamaños de página/recogida, y `rate_limit_cost_per_row` controla cuánto cuesta cada fila solicitada bajo el limitador de tasa de la app API.
- `torii.webhook.*`: Ajuste de entrega/retroceso para el worker de webhook. `queue_capacity` limita las entregas pendientes en disco (por defecto: `10000`), `max_attempts` acota reintentos (por defecto: `12`), `backoff_initial_ms`/`backoff_max_ms` establecen la ventana de reintento exponencial (por defecto: `1000`/`60000`), y `{connect,write,read}_timeout_ms` configuran timeouts HTTP para intentos de entrega.
- `torii.push.*` (feature stub): toggles del puente de push notifications y límites de tasa. `enabled` habilita el puente; `rate_per_minute`/`burst` aplican un token bucket a las solicitudes salientes, `max_topics_per_device` limita suscripciones por token, y `connect_timeout_ms`/`request_timeout_ms` acotan intentos HTTP a FCM/APNS. Proporcione `fcm_api_key` o `apns_endpoint` + `apns_auth_token` para habilitar el proveedor respectivo.
- `torii.da_ingest.*`: caché de replay y configuración de spool de manifiestos para la canalización DA.
  - `telemetry_cluster_label` (opcional): establece la etiqueta `cluster` emitida en las métricas de ingest/viewer Taikai para que los tableros distingan entornos (p. ej., `region-a` vs `staging`).
    Establezca `governance_metadata_key_hex` en una clave ChaCha20-Poly1305 de 32 bytes cuando necesite que Torii selle metadatos solo de gobernanza; acompáñelo con `governance_metadata_key_label` (un identificador corto como `"primary"`) para que las entradas cifradas anuncien qué clave produjo el cifrado. Torii cifra automáticamente los metadatos de gobernanza en claro y rechaza manifiestos cuyos payloads pre-cifrados lleven etiquetas ausentes o no coincidentes. La tabla anidada `replication_policy` define la `RetentionPolicy` aplicada por clase de blob (ver `docs/source/da/replication_policy.md`). Sobrescriba `default_retention` para cambiar las ventanas base caliente/fría y añada entradas bajo `replication_policy.overrides` (p. ej., `class = "taikai_segment"`) para endurecer clases específicas; Torii reescribe manifiestos enviados por el llamador para igualar estos valores y rechaza envolturas no coincidentes.
  - `debug_match_filters` (por defecto: `false`) habilita trazas deterministas de depuración de filtros antes controladas por el toggle de entorno `TORII_DEBUG_MATCH`. Manténgalo apagado en producción.
- `[torii.iso_bridge.reference_data]`: Rutas a datasets ISO de referencia (`isin_crosswalk_path`, `bic_lei_path`, `mic_directory_path`), la cadencia de ingesta (`refresh_interval_secs`), y `cache_dir` opcional para persistir copias más metadatos de procedencia (versión/fuente/checksum) de cada snapshot cargado.
- `torii.strict_addresses` (por defecto: `true`): Cuando está habilitado (por defecto), Torii rechaza `alias@domain` alternativos/literales de clave pública crudos en cada endpoint orientado a app y devuelve `ERR_STRICT_ADDRESS_REQUIRED`. Solo establezca esto en `false` en clústeres dev/test mientras diagnostica regresiones, y vuelva a `true` una vez que la telemetría confirme la adopción canónica.
- `[torii.soranet_privacy_ingest]`: Guardas para `/v1/soranet/privacy/{event,share}`. `enabled` debe ser `true` para exponer los endpoints; `require_token` + `tokens` hacen cumplir el encabezado `X-SoraNet-Privacy-Token` (o `X-API-Token`), `allow_cidrs` restringe remitentes por CIDR (lista vacía deniega por defecto), y `rate_per_sec`/`burst` aplican un limitador tipo token-bucket. Los rechazos devuelven `401/403/429` con pistas de reintento y contadores de telemetría etiquetados para que los operadores alerten sobre abuso; vea `docs/source/sorafs_authz_runbook.md` para la lista de verificación de despliegue.
- `[torii.sorafs]`: Controles de caché de descubrimiento SoraFS expuestos en el lado Torii. Los valores se copian desde los bloques canónicos `[sorafs.*]` durante el análisis de configuración, así que configure las secciones raíz para la intención del operador; la vista Torii existe para snapshots de runtime/telemetría.
- `[sorafs.discovery]`: Toggle canónico de descubrimiento y allow-list de capacidades. `discovery_enabled` por defecto `false` (los perfiles Sora como `tests/fixtures/base.toml` lo habilitan explícitamente), `known_capabilities` se entrega con `["torii_gateway", "chunk_range_fetch", "vendor_reserved"]`, y la tabla opcional `[sorafs.discovery.admission]` apunta al directorio de sobres de gobernanza (`envelopes_dir`).
- `[sorafs.storage]`: Configuración del worker SoraFS embebido. Campos clave: `enabled`, `data_dir` (raíz de almacenamiento), `max_capacity_bytes`, `max_parallel_fetches`, `max_pins`, `por_sample_interval_secs`, `alias` opcional y (cuando quiera asentamientos en disco) `governance_dag_dir` apuntando al directorio del sistema de archivos donde deben materializarse artefactos de gobernanza. La tabla `[sorafs.storage.adverts]` expone metadatos del advert (`stake_pointer`, `availability`, `max_latency_ms`, `topics`); los snapshots de runtime reflejan la misma estructura bajo `torii.sorafs_storage`.
- `[sorafs.storage.pin]`: Autenticación/límites de tasa para `/v1/sorafs/storage/pin`. `require_token=true` impone un bearer token (vía `Authorization: Bearer ...` o `X-SoraFS-Pin-Token`), `tokens` provee la allow-list, `allow_cidrs` restringe clientes por CIDR, y `rate_limit` aplica un token-bucket con ban opcional. Torii rechaza intentos de pin antes de tocar el almacenamiento cuando fallan estas guardas, devolviendo `401/403/429` con `Retry-After` en respuestas limitadas.
- `[sorafs.repair]`: Ajustes del scheduler de reparación. Claves: `enabled` (por defecto `false`), `db_dsn` opcional, `db_pool_max_connections` (por defecto `8`), `claim_ttl_secs` (por defecto `900`), `heartbeat_interval_secs` (por defecto `60`), `max_attempts` (por defecto `3`), `worker_concurrency` (por defecto `4`), `backoff_initial_secs` (por defecto `5`) y `backoff_max_secs` (por defecto `60`).
- `[sorafs.gc]`: Ajustes del scheduler de GC. Claves: `enabled` (por defecto `false`), `db_dsn` opcional, `db_pool_max_connections` (por defecto `4`), `interval_secs` (por defecto `900`), `max_deletions_per_run` (por defecto `500`), `retention_grace_secs` (por defecto `86400`) y `pre_admission_sweep` (por defecto `true`).
- `[sorafs.quota]`: Ventanas de cuota de plano de control (declaración de capacidad, pin de almacenamiento, telemetría, telemetría de deals, disputa y envío PoR). La vista Torii bajo `torii.sorafs_quota` hereda estos valores tal cual.
- `[sorafs.alias_cache]`: Política de caché de alias-proof (`positive_ttl`, `refresh_window`, `hard_expiry`, `negative_ttl`, `revocation_ttl`, `rotation_max_age`). Los valores por defecto imponen ventanas de 10 min / 2 min / 15 min / 60 s / 5 min / 6 h; solo ajuste reduciendo valores. El snapshot de runtime los refleja bajo `torii.sorafs_alias_cache`.
- `sorafs.rollout_phase`: Selector de despliegue PQ de alto nivel (`canary`, `ramp`, `default`). Los clientes heredan la etapa de anonimato correspondiente a menos que `sorafs.anonymity_policy` se establezca explícitamente.
- `sorafs.anonymity_policy`: Flag de despliegue PQ por etapas de SoraNet (`anon-guard-pq`, `anon-majority-pq`, `anon-strict-pq`). Por defecto es la etapa A (`anon-guard-pq`); avance a la etapa B/C cuando la cobertura PQ sea suficiente. Sobrescriba solo cuando un componente deba divergir de la fase.
- `[nts]`: Cadencia de muestreo y suavizado del Network Time Service. `sample_interval_ms` se limita a >=100 ms; solo es consultivo, el consenso no depende de ello.
- `[kura]`: Ruta de almacenamiento de bloques y ventana de memoria; política de inicialización para snapshots/replay. `block_sync_roster_retention` acota cuántas instantáneas certificadas de roster de commit se guardan en disco para recuperación de block-sync (por defecto 7200), y `roster_sidecar_retention` limita cuántos sidecars de roster permanecen en `pipeline/roster_sidecars.*` tras la compactación (por defecto 512).
- `[logger]`: Nivel y formato de logs.
- `[queue]`: Límites de cola de transacciones y TTL.
- `[nexus]`: Catálogo de enrutamiento multi-lane, registro de manifiestos y módulos de gobernanza. Las entradas de lane aceptan `visibility`, `lane_type`, `governance`, `settlement`, `proof_scheme` (`merkle_sha256` por defecto; los lanes `kzg_bls12_381` reciben compromisos KZG deterministas derivados en ingesta), y `metadata = { key = "value" }` de forma libre. El bloque `registry` controla `manifest_directory`, `cache_directory`, y `poll_interval_ms` (por defecto 60 s). El bloque `governance` provee un `default_module` y un mapeo `modules`, donde cada módulo expone `module_type` más overrides de `params` clave/valor para hosts downstream.
  - La clave de metadatos de lane `da_manifest_policy` ajusta la aplicación de manifiestos DA por lane: `strict` (por defecto) bloquea manifiestos faltantes/ilegibles y desajustes de hash, mientras que `audit`/`audit_only`/`warn` permiten continuar con manifiestos faltantes con advertencias (los desajustes de hash siguen siendo fatales).
  - `nexus.staking.public_validator_mode` / `nexus.staking.restricted_validator_mode` seleccionan el ciclo de vida de validadores por lane: `stake_elected` (por defecto para lanes públicos) mantiene habilitada la vía de staking/NPoS, mientras que `admin_managed` (por defecto para lanes restringidos) omite staking y espera admisión de peers vía flujos de gobernanza/admin. Los lanes stake-elected requieren un peer registrado con clave de consenso viva presente en la topología de commit antes de la admisión y se activan en límites de época; los lanes admin-managed mantienen vivo el roster de peers de génesis para que los despliegues permissioned puedan avanzar sin staking.
  - `[nexus.axt]`: guardarraíles de temporización/expiración. `slot_length_ms` debe estar en `1..=600_000` (por defecto `1`); los hosts derivan `current_slot = block.creation_time_ms / slot_length_ms`. `max_clock_skew_ms` por defecto `0`, se limita a `60_000`, y no debe exceder `slot_length_ms`; las comprobaciones de expiración para proofs/handles incluyen esta tolerancia. `proof_cache_ttl_slots` (por defecto `1`, validado `1..=64`) limita cuántos slots quedan proofs aceptadas/rechazadas en caché del host para reutilización/rechazo de replay. `replay_retention_slots` (por defecto `128`, validado `1..=4_096`) controla cuánto tiempo se conservan los libros de uso/replay de handles a través de peers/reinicios; súbalo solo cuando los handles permanezcan válidos muchos slots y necesite que la protección contra replay cubra esas ventanas. Vea `crates/iroha_config/tests/fixtures/nexus_axt_full.toml` para un ejemplo con 1s/250ms/TTL=8.
- `[governance]`: Claves de verificación, parámetros de convicción, umbrales de aprobación, umbral de participación y tamaño/eligibilidad del consejo.
  - `debug_tie_break` / `debug_tx_eval` (por defecto: `false`): habilitan trazas de evaluación de overlay forense para depuración. Sustituyen los shims de entorno `IROHA_DEBUG_TIE_BREAK`/`IROHA_DEBUG_TX_EVAL` heredados; manténgalos apagados en configuraciones de producción.
- `[norito]`: Ajuste de serialización para Norito (tamaños/niveles de compresión y toggles de layout adaptativo). Determinista por diseño. `max_archive_len` por defecto es 512 MiB y se ajusta al menos a `sumeragi.rbc_store_max_bytes` y `network.max_frame_bytes` para mantener decodificables los payloads de consenso.
- `[streaming]`: Material criptográfico de plano de control para Norito streaming (HPKE + firmas de control).
  - `identity_public_key` / `identity_private_key` (opcional): cadenas multihash Ed25519 que sustituyen el par de claves principal del nodo para firmas de streaming; útil cuando la clave de validador es GOST u otro algoritmo no Ed25519.
  - `kyber_public_key` / `kyber_secret_key` (opcional): claves Kyber-768 codificadas en hex que habilitan HPKE; deben proporcionarse juntas.
  - `session_store_dir` (por defecto: `./storage/streaming`): Directorio para snapshots de sesión cifradas.
  - `feature_bits` (por defecto: `0b11`): máscara de capacidades anunciada durante la negociación de capacidades QUIC. El bit 0 habilita feedback hints, el bit 1 señala soporte del proveedor de privacy overlay, y los bits superiores están reservados. Los viewers que exijan bits no soportados son rechazados.
  - `soranet` (por defecto: habilitado): puentea automáticamente rutas de privacidad de manifiestos a SoraNet cuando las entradas de manifiesto omiten metadatos explícitos.
    - `enabled` (por defecto: `true`): habilita la lógica de aprovisionamiento automático; desactívela para mantener intactos los manifiestos existentes.
    - `exit_multiaddr` (por defecto: `/dns/torii/udp/9443/quic`): multi-dirección del relay de salida reenviada a Torii una vez que el circuito sale de SoraNet.
    - `padding_budget_ms` (por defecto: `25`): margen de padding de baja latencia en milisegundos aplicado al circuito; `null` desactiva padding extra.
    - `access_kind` (por defecto: `authenticated`): postura de salida aplicada al hacer el puente (`authenticated` o `read-only`).
    - `channel_salt` (por defecto: `iroha.soranet.channel.seed.v1`): cadena de dominio con hash junto con los identificadores de stream/ruta para derivar IDs de canal cegados.
    - `provision_window_segments` (por defecto: `4`): ventana de segmentos (inclusiva) usada al aprovisionar rutas de privacidad automaticamente; las ventanas se alinean a multiplos de este tamano.
    - `provision_queue_capacity` (por defecto: `256`): numero maximo de trabajos de aprovisionamiento de rutas de privacidad en cola antes de aplicar backpressure.
  - `soravpn`: ajustes del spool de aprovisionamiento local de SoraVPN para rutas de streaming.
    - `provision_spool_dir` (por defecto: `./storage/streaming/soravpn_routes`): directorio del spool usado para preparar actualizaciones de rutas SoraVPN para nodos VPN locales.
    - `provision_spool_max_bytes` (por defecto: `0`, ilimitado): tamaño máximo en disco del spool de aprovisionamiento SoraVPN.
  - `sync` (por defecto: observe-only): política de aplicación de sincronización de audio/vídeo para NSC-28b.
    - `enabled` (por defecto: `false`): active a `true` cuando el validador deba comenzar a evaluar informes de deriva.
    - `observe_only` (por defecto: `true`): cuando es `true`, las violaciones se registran y se exportan vía telemetría pero los segmentos se aceptan; ponga `false` para fallar en duro.
    - `min_window_ms` (por defecto: `5000`): ventana diagnóstica mínima requerida antes de aplicar.
    - `ewma_threshold_ms` (por defecto: `10`): umbral de deriva EWMA sostenida (ms) que dispara rechazos.
    - `hard_cap_ms` (por defecto: `12`): deriva máxima por muestra (ms) antes de fallo inmediato.
- `[relay]` (tools/soranet-relay): daemon de referencia para relays de entrada/salida SoraNet.
  - `mode` (`"Entry" | "Middle" | "Exit"`): Selecciona el rol de privacidad, etiquetas de telemetría y reglas de padding por defecto.
  - `listen` / `admin_listen`: Listener QUIC y socket admin opcional (métricas + control).
  - `padding`: Perfil de padding de stream. `cell_size` (bytes) y `max_idle_millis` (intervalo) describen el heartbeat por circuito. `global_rate_limit_bytes_per_sec` (por defecto: `0`, deshabilitado) habilita el token-bucket que limita el ancho de banda de padding agregado a través de todos los circuitos, y `burst_bytes` acota la capacidad del bucket compartido (por defecto `max(cell_size, rate)` cuando se omite) para que los relays absorban picos cortos sin violar el presupuesto en régimen.
- `exit_routing.norito_stream`: Puente opcional al adaptador de Norito streaming de Torii. Cuando se habilita, el relay escanea el spool de Torii en el sistema de archivos (`exit-<relay-id>/norito-stream/*.norito`) y reenvía rutas activas por WebSocket.
- `exit_routing.kaigi_stream`: Puente opcional a hubs de conferencias Kaigi. Cuando se habilita, el relay carga rutas desde `exit-<relay-id>/kaigi-stream/*.norito`, marca al `hub_ws_url` configurado y deriva identificadores de sala cegados desde los metadatos de canal. Los knobs de ajuste incluyen el timeout de conexión (`connect_timeout_millis`), categorías GAR públicas/autenticadas, override del spool del sistema de archivos y el intervalo `route_refresh_secs`.
    - `torii_ws_url`: endpoint `ws://` o `wss://` expuesto por Torii para tráfico Norito streaming.
    - `connect_timeout_millis` (por defecto: `5000`): los relays abortan intentos de conexión que excedan este límite.
    - `padding_target_millis` (por defecto: `35`): cadencia de padding inactivo aplicada cuando la entrada de spool omite `padding_budget_ms`. Establezca `0` para desactivar padding en segundo plano.
    - `gar_category_read_only` / `gar_category_authenticated`: etiquetas GAR registradas cuando viewers no autenticados o autenticados solicitan un stream Norito. Por defecto `stream.norito.read_only` / `stream.norito.authenticated`; personalice cuando su pipeline de telemetría espere otros buckets.
    - `spool_dir`: ruta raíz para el catálogo de sistema de archivos. Cada id de relay obtiene un subdirectorio `exit-<relay-id>/norito-stream` poblado por Torii (ver `streaming.soranet.provision_spool_dir`); omita para usar el valor por defecto de la herramienta (`./storage/streaming/soranet_routes`).
    - `route_refresh_secs` (por defecto: `5`): cadencia de refresco de caché al escanear archivos de spool. Bajar el valor fuerza lecturas de disco más frecuentes; subirlo favorece el rendimiento en estado estable cuando el spool es grande.
- `[crypto]`: Defaults de criptografía expuestos vía `iroha_config`.
  - `sm_intrinsics` (por defecto: `auto`): política de despacho de intrínsecos SM (`auto`, `force-enable`, o `force-disable`). Deje `auto` en producción; use `force-disable` para validar fallbacks escalares o `force-enable` para perfiles controlados.
  - `enable_sm_openssl_preview` (por defecto: `false`): Optar por la ruta de previsualización SM respaldada por OpenSSL (hashing SM3, verificación SM2, SM4-GCM AEAD). Requiere compilar con `--features "sm sm-ffi-openssl"` y desplegar OpenSSL/Tongsuo 3.0+ en cada nodo; cuando está habilitado, el nodo alterna `OpenSslProvider` al arrancar y expone entradas de telemetría/log para que los operadores verifiquen que el backend de previsualización está activo.
  - `default_hash` (por defecto: `blake2b-256`): algoritmo hash usado cuando los llamadores omiten uno. Si `allowed_signing` incluye `sm2`, cambie a `sm3-256` para que manifiestos y hashes ABI se alineen.
  - `allowed_signing` (por defecto: `["ed25519"]`): lista ordenada de algoritmos aceptados durante admisión de transacciones. Debe incluir siempre `ed25519`. Agregar `sm2` habilita syscalls de helper SM y admisión de firmas; eliminar `sm2` mantiene el nodo en modo solo-verificación. Los builds compilados sin la feature `sm` rechazarán `sm2` en esta lista. Las firmas de voto de consenso no se ven afectadas; los validadores siempre usan la clave BLS de `trusted_peers` + PoP aunque `allowed_signing` no mencione BLS.
    - `allowed_curve_ids` (por defecto: derivado de `allowed_signing`, típicamente `[1]` para Ed25519): identificadores de curva (ver [`address_curve_registry.md`](../references/address_curve_registry.md)) que pueden aparecer en controladores de cuentas. Sobrescriba esta lista para preparar despliegues ML-DSA/GOST/SM incluso cuando los binarios ya soportan los algoritmos; los manifiestos de génesis reflejan este campo vía `crypto.allowed_curve_ids`.
    - `sm2_distid_default` (por defecto: `1234567812345678`): identificador distintivo SM2 de respaldo aplicado cuando los firmantes omiten su propio `distid`. El nodo siembra `Sm2PublicKey::default_distid()` con este valor durante el arranque, por lo que los cambios surten efecto antes de ejecutar transacciones.
    - *Vista previa (feature `sm-ffi-openssl`)*: Compilar Iroha con `--features "sm sm-ffi-openssl"` expone el proveedor opcional OpenSSL/Tongsuo controlado por `crypto.enable_sm_openssl_preview`. Los operadores deben distribuir OpenSSL/Tongsuo 3.0+ junto con los binarios y dejar la bandera desactivada hasta que llegue el proveedor auditado.

  **Lista de verificación de despliegue SM.** El layering de `iroha_config` (`defaults` → `user` → `actual`) permite despliegues por etapas:
  1. **Validación de build.** Compile binarios con `--features sm` (y `sm-ffi-openssl` solo si el backend de previsualización es necesario). Sin la feature el nodo rechaza `sm2` en `allowed_signing`.
  2. **Propagación de configuración.**
     - Actualice `crypto.allowed_signing` para incluir `sm2` (mantenga la lista ordenada lexicográficamente) y establezca `crypto.default_hash = "sm3-256"`.
     - Sobrescriba `crypto.sm2_distid_default` con el identificador distintivo exigido por su política (p. ej., el valor ISO/TC 68 para su organización).
  3. **Alineación de génesis.** Regenerar manifiestos de génesis vía `kagami genesis generate --consensus-mode <mode>` (use `--allowed-signing sm2 --default-hash sm3-256` si necesita overrides) para que los bloques `parameters` y metadatos inyectados reflejen la instantánea de configuración; confírmelos en control de versiones junto con el TOML actualizado.
  4. **Pruebas de humo de telemetría y admisión.** Antes de activar tráfico, ejecute `cargo test -p iroha_crypto --features sm` y los scripts de la matriz de integración SM, luego verifique que `/status` expone `crypto.sm_helpers_available = true` y que Torii rechaza firmas SM2 cuando `sm2` está ausente en `allowed_signing`.
  5. **Plan de reversión.** Documente cómo volver a modo solo Ed25519 (eliminar `sm2` de `allowed_signing`, restaurar `default_hash = "blake2b-256"`) y confirme que los hosts descartan transacciones SM en vuelo de forma determinista.

- `[fraud_monitoring]`: guardarraíles de admisión para evaluaciones de fraude provistas por PSP.
  - `enabled` (por defecto: false): interruptor maestro; sin `required_minimum_band` el host registra una advertencia y omite la aplicación.
  - `service_endpoints` (por defecto: `[]`): lista ordenada de URLs de verificador (deduplicadas); reservadas para el próximo servicio remoto de fraude.
  - `connect_timeout_ms` / `request_timeout_ms` (por defecto: 500 / 1500): timeouts HTTP para el verificador; cero colapsa al valor por defecto.
  - `missing_assessment_grace_secs` (por defecto: 0): ventana de fallback determinista; valores no cero permiten paso temporal mientras se emite una advertencia.
  - `required_minimum_band` (por defecto: `null`): banda de severidad requerida (`low`, `medium`, `high`, o `critical`). Transacciones sin banda o por debajo del umbral se rechazan cuando `enabled` es `true`.
- `[zk]`: Ajustes de verificación de conocimiento cero (backend, curva, presupuestos). No críticos para consenso; guardarraíles aplicados.
  - `fastpq.execution_mode` (por defecto: `"auto"`): selecciona el backend de prover FASTPQ (`auto`, `cpu`, o `gpu`). Auto realiza detección en tiempo de ejecución con fallback CPU determinista; los modos explícitos fijan el prover al backend solicitado mientras registran si el acelerador no está disponible. Los builds de producción envían el backend Stage 6 por defecto vía `fastpq_prover::Prover::canonical`. La telemetría incrementa `fastpq_execution_mode_total{requested,resolved,backend,device_class,chip_family,gpu_kind}` para que los paneles de flota verifiquen que los nodos resuelven al conjunto de aceleradores esperado por perfil de hardware.【crates/fastpq_prover/src/proof.rs:126】【crates/iroha_telemetry/src/metrics.rs:8887】
  - `fastpq.device_class` (por defecto: sin definir ⇒ exportado como `device_class="unknown"`): etiqueta opcional que describe el perfil de hardware (p. ej., `apple-m4`, `xeon-rtx-sm80`, `neoverse-mi300`). Configúrelo vía TOML o `FASTPQ_DEVICE_CLASS`; las cadenas vacías se ignoran.【crates/iroha_config/src/parameters/user.rs:1224】
  - `fastpq.chip_family` (por defecto: sin definir ⇒ `chip_family="unknown"`): etiqueta opcional para la familia SoC/CPU (`m4`, `xeon-icelake`, etc.) usada para plantillas de dashboard y partición de alertas. Configure vía TOML o `FASTPQ_CHIP_FAMILY`.
  - `fastpq.gpu_kind` (por defecto: sin definir ⇒ `gpu_kind="unknown"`): etiqueta opcional que describe si el acelerador es `integrated`, `discrete`, u otro descriptor acordado por la flota. Configure vía TOML o `FASTPQ_GPU_KIND`.
  - `fastpq.metal_queue_fanout` / `fastpq.metal_queue_column_threshold` (por defecto: `None` ⇒ depender de heurísticas): overrides opcionales para la política de command-queue Metal. Se mapean directamente a `FASTPQ_METAL_QUEUE_FANOUT` y `FASTPQ_METAL_COLUMN_THRESHOLD` y se aplican al arranque vía `fastpq_prover::set_metal_queue_policy`, por lo que los operadores ya no necesitan variables de entorno ad-hoc para reproducir experimentos de colas. `fanout` acepta `1–4`, mientras que `column_threshold` debe ser mayor que cero; los valores inválidos abortan el análisis de configuración.【crates/iroha_config/src/parameters/user.rs:1549】【crates/fastpq_prover/src/lib.rs:60】【crates/fastpq_prover/src/metal.rs:2414】
  - `fastpq.metal_max_in_flight` / `fastpq.metal_threadgroup_width` / `fastpq.metal_trace` / `fastpq.metal_debug_enum` / `fastpq.metal_debug_fused` (por defecto: `None`, `None`, `false`, `false`, `false`): reemplazos controlados por configuración para los shims de entorno Metal. Los valores se aplican antes de inicializar el backend GPU vía `fastpq_prover::apply_metal_overrides`; los nodos de producción ignoran `FASTPQ_METAL_{MAX_IN_FLIGHT,THREADGROUP,TRACE}` / `FASTPQ_DEBUG_{METAL_ENUM,FUSED}` una vez que carga la configuración, reservando los shims de entorno para harnesses dev/test. Use estas perillas para capturas deterministas.【crates/iroha_config/src/parameters/user.rs:3040】【crates/iroha_core/src/fastpq/lane.rs:109】【crates/fastpq_prover/src/overrides.rs:11】
- Overrides de FASTPQ Metal/bring-up (variables de entorno – diagnóstico de desarrollador solamente):
  - `FASTPQ_GPU={auto,cpu,gpu}`: fuerza el modo de ejecución antes del análisis de configuración. El valor se registra, se exporta a Prometheus/OTEL y las cadenas no reconocidas vuelven a auto con una advertencia.【crates/fastpq_prover/src/backend.rs:214】【crates/fastpq_prover/src/backend.rs:349】
  - `FASTPQ_METAL_LIB`: ruta absoluta al `.metallib` producido durante el build. Si el archivo falta o el valor está vacío, el script de build limpia la variable y el planificador vuelve a la ruta CPU determinista.【crates/fastpq_prover/build.rs:29】【crates/fastpq_prover/build.rs:167】【crates/fastpq_prover/src/metal.rs:43】
  - `FASTPQ_SKIP_GPU_BUILD=1`: omite la compilación Metal/CUDA al cruzar sin toolchains; el backend permanece en CPU y el build emite una advertencia para notas de release.【crates/fastpq_prover/build.rs:42】【crates/fastpq_prover/src/backend.rs:195】
  - `FASTPQ_METAL_TRACE=1`: habilita logs por despacho de kernel (etiqueta de pipeline, ancho de threadgroup, grupos de lanzamiento, tiempo transcurrido) para que las corridas de perfilado puedan archivarse como evidencia de release. Prefiera `fastpq.metal_trace` para despliegues de producción; el shim de entorno ahora actúa como fallback dev/test.【crates/fastpq_prover/src/metal.rs:346】【crates/iroha_core/src/fastpq/lane.rs:109】
  - `FASTPQ_DEBUG_METAL_ENUM=1`: imprime la enumeración de dispositivos Metal realizada durante la detección (`MTLCreateSystemDefaultDevice` y `MTLCopyAllDevices`, tras un warm-up de CoreGraphics) para que shells macOS sin interfaz prueben por qué `FASTPQ_GPU=gpu` aún degradó a la ruta CPU. Prefiera `fastpq.metal_debug_enum`; el shim de entorno se mantiene para capturas de diagnóstico.【crates/fastpq_prover/src/backend.rs:665】【crates/fastpq_prover/src/overrides.rs:11】
  - `FASTPQ_METAL_THREADGROUP=<width>`, `FASTPQ_METAL_FFT_LANES=<lanes>`, `FASTPQ_METAL_FFT_TILE_STAGES=<1-16>`, `FASTPQ_METAL_LDE_TILE_STAGES=<1-32>`: sobrescriben las heurísticas automáticas de lane/tile (FFT y LDE comparten el mismo planificador de lanes, mientras que la variable específica de LDE limita cuántas etapas corren dentro del pase de memoria compartida antes de que se haga cargo el kernel post-tiling). Los valores se limitan a los límites de hardware, se pasan a los args del kernel Metal y se registran antes del dispatch para que los experimentos sean reproducibles sin reconstruir el metallib. Use `fastpq.metal_threadgroup_width` para overrides de producción; el shim de entorno sigue siendo un escape hatch dev/test.【crates/fastpq_prover/src/metal.rs:321】【crates/fastpq_prover/src/metal.rs:827】【crates/fastpq_prover/src/metal_config.rs:15】
  - `FASTPQ_METAL_POSEIDON_LANES=<lanes>` (potencias de dos 32–256) y `FASTPQ_METAL_POSEIDON_BATCH=<1-32>` controlan la forma de lanzamiento del kernel Poseidon2 Metal: cada threadgroup carga las constantes de ronda/filas MDS en memoria de threadgroup, desenrolla las rondas y procesa varios estados por lane de modo que cada dispatch alcance ≥4 096 hilos lógicos. Por defecto, el host inspecciona `MTLDevice::{is_low_power,is_headless,location}` una vez por arranque y sesga GPUs discretas a lanzamientos escalonados por VRAM (`256×24` en ≥48 GiB, `256×20` en 32 GiB, `256×16` en otro caso) mientras que los SoC integrados permanecen en `256×8` (hardware de 128/64 lanes baja a 8/6 estados por lane). Los overrides siguen disponibles para perfilado determinista, y el runtime limita valores no soportados, pasa el ajuste resuelto a través de `PoseidonArgs`, y lo registra dentro de los bloques `poseidon_microbench`/`poseidon_pipeline`/`kernel_profiles` emitidos por el bench Metal.【crates/fastpq_prover/src/metal_config.rs:78】【crates/fastpq_prover/src/metal.rs:1971】【crates/fastpq_prover/src/bin/fastpq_metal_bench.rs:1691】【crates/fastpq_prover/src/bin/fastpq_metal_bench.rs:1988】
  - `FASTPQ_POSEIDON_PIPE_COLUMNS=<16-256>` y `FASTPQ_POSEIDON_PIPE_DEPTH=<1-8>` (solo env) ajustan la canalización de streaming Poseidon de Stage 7 que solapa trabajo FFT/IFFT de CPU con hashing GPU. Las columnas se materializan en bloques de `PIPE_COLUMNS` (Metal discreto por defecto 128, CUDA por defecto 96, builds solo CPU vuelven a 64) y el canal acotado permite hasta `PIPE_DEPTH` lotes en cola antes de que la CPU pause. Los valores fuera de la ventana soportada se ignoran con una advertencia, y el ajuste resuelto se registra para que la evidencia de benchmarks pueda probar capturas deterministas.【crates/fastpq_prover/src/trace.rs:51】【crates/fastpq_prover/src/trace.rs:809】
  - `FASTPQ_METAL_FFT_COLUMNS=<1-32>`, `FASTPQ_METAL_LDE_COLUMNS=<1-32>`: fijan el tamaño de lote de columnas de FFT/IFFT o LDE en lugar de usar la rampa automática 32→16→8→4→2→1 derivada del ancho de threadgroup resuelto. Es útil para capturar telemetría reproducible entre hosts; los valores resueltos se muestran en `kernel_profiles.*.columns` dentro de cada reporte de benchmark.【crates/fastpq_prover/src/metal.rs:742】【crates/fastpq_prover/src/bin/fastpq_metal_bench.rs:697】
  - `FASTPQ_METAL_QUEUE_FANOUT=<1-4>`, `FASTPQ_METAL_COLUMN_THRESHOLD=<n>`: controlan la política de despacho multi-cola en macOS. Por defecto, las GPUs discretas ejecutan dos command queues Metal y solo hacen round-robin de lotes de columnas cuando la carga lleva al menos 16 columnas (escalado por el fan-out resuelto); estos overrides permiten fijar el número de colas y el mínimo total de columnas antes del fan-out para que perfiles y pruebas de paridad sean reproducibles entre hosts.【crates/fastpq_prover/src/metal.rs:620】【crates/fastpq_prover/src/metal.rs:900】
  - `FASTPQ_METAL_MAX_IN_FLIGHT=<n>`: acota la cantidad de command buffers Metal simultáneos (por defecto auto derivado del conteo de núcleos GPU detectado vía `system_profiler`, limitado al piso de fan-out con fallback de paralelismo del host cuando macOS oculta el dispositivo). Prefiera `fastpq.metal_max_in_flight` en producción; el shim de entorno ahora es un escape hatch dev/test. El semáforo registra métricas de profundidad/solapamiento de cola, y el bench Metal emite los bloques `metal_dispatch_queue` y `metal_heuristics` para que los artefactos de release prueben tanto el perfil de solapamiento como los límites resueltos del scheduler.【crates/fastpq_prover/src/metal.rs:2056】【crates/fastpq_prover/src/bin/fastpq_metal_bench.rs:2078】
- `[accel]`: Ajustes de aceleración de hardware para IVM y helpers.
  - `enable_simd` (por defecto: true): permite ejecución SIMD/NEON/AVX; cuando es false, fuerza fallbacks escalares para corridas de paridad determinista.
  - `enable_cuda` (por defecto: true): usa CUDA cuando está compilado y disponible.
  - `enable_metal` (por defecto: true): usa Metal en macOS cuando está disponible.
  - `max_gpus` (por defecto: 0): máximo de GPUs a inicializar; 0 significa auto/sin tope.
  - `merkle_min_leaves_gpu` (por defecto: 8192): mínimo de hojas para usar GPU en hashing de hojas Merkle. Reduzca solo si la GPU de su plataforma es inusualmente rápida en lotes pequeños.

Notas
- La aceleración es automática por defecto. Los backends ejecutan self-tests dorados en el primer uso y se deshabilitan en tiempo de ejecución si se detecta un desajuste; la VM vuelve a escalar/SIMD y mantiene bit-identical entre hardware.
- Prefiera editar archivos estilo `peer.template.toml` y extenderlos en despliegue. El comportamiento en runtime se configura vía `iroha_config`; los overrides por variables de entorno existen para tooling de desarrollo pero no deben dependerse en flujos operativos.
