---
lang: zh-hant
direction: ltr
source: docs/portal/docs/reference/torii-swagger.mdx
status: complete
generator: docs/portal/scripts/sync-i18n.mjs
---

import SwaggerUiPanel from '@site/src/components/SwaggerUiPanel.jsx';

# Torii API (Swagger UI)

This page embeds the Torii OpenAPI document using `swagger-ui-react`. Use it when
you want the familiar Swagger layout, request samples, and the ability to try
out requests directly within the browser.

## Usage tips

- Provide a bearer token in the panel to pre-authorise every request. Tokens are
  forwarded to the proxy using the `X-TryIt-Auth` header and are never persisted.
- Configure `TRYIT_PROXY_PUBLIC_URL` when serving the portal so the widget
  forwards requests through the bundled staging proxy. A warning banner appears
  if the proxy is disabled.
- Pick an OpenAPI snapshot from the dropdown to switch between the latest spec
  and archived versions generated by `npm run sync-openapi`.
- Try the proxy-backed “[Try it](/api/overview#try-it-sandbox-docs-3)” console
  for staged environments where CORS prevents direct Torii access.
- Keep the OpenAPI snapshot fresh with `cargo xtask openapi` (see
  `docs/portal/static/openapi/torii.json`).

<SwaggerUiPanel />
