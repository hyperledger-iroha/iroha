---
lang: ja
direction: ltr
source: docs/portal/docs/nexus/nexus-spec.ar.md
status: complete
generator: docs/portal/scripts/sync-i18n.mjs
translator: machine-google-reviewed
translation_last_reviewed: 2026-02-07
---

---
ID: ネクサススペック
タイトル: المواصفات التقنية لسورا نيكسس
説明: مرآة كاملة لـ `docs/source/nexus.md` تغطي معمارية وقيود التصميم لدفتر Iroha 3 (Sora Nexus)。
---

:::メモ
テストは `docs/source/nexus.md` です。ニュースを読む
:::

#! Iroha 3 - Sora Nexus 元帳: المواصفات التقنية للتصميم

يقترح هذا المستند معمارية Sora Nexus Ledger لـ Iroha 3، مع تطوير Iroha 2 إلى دفترデータ スペース (DS) です。データ スペース (「プライベート データ スペース」) と「パブリック データ スペース」。構成可能性の確認と管理の強化 プライベート DS の管理Kura (ブロック ストレージ) と WSV (World State View) です。

يبني نفس المستودع كلا من Iroha 2 (شبكات مستضافة ذاتيا) و Iroha 3 (SORA) Nexus)。 Iroha 仮想マシン (IVM) 仮想マシン Kotodama 仮想マシン (IVM)バイトコードをバイトコードに変換します。

ああ
- データ スペースの管理。
- データ スペース - データ スペース (CBDC)、DS など。
- データ スペース、イーサリアム。
- プライベート DS のデータ スペース。
- プライベート DS をプレイしてください。
- 評価: Kura و WSV بترميز محو لدعم بيانات شبه غير محدودة مع بقاء بيانات private-DSああ。

غير الاهداف (المرحلة الاولى)
- 評価 - 評価 - 評価 - 評価 - 評価 - 評価 - 評価 - 評価 - 評価スケジューリングとステーキングを管理します。
- アビ・アビ・アビスABI v1 のシステムコールとポインター ABI のバージョン IVM。

ああ
- Nexus 元帳: データ スペース (DS) とデータ スペース (DS) の元帳ああ。
- データスペース (DS): طاق تنفيذ وتخزين محدود بمدققين خاصين وحوكمة وفئة خصوصية وسياسة DA وحصصうーん。定義: パブリック DS とプライベート DS。
- プライベート データ スペース: プライベート データ スペース: DS をプレイしてください。 عالمزامات/البيانات الوصفية فقط عالميا。
- パブリック データ スペース: مشاركة بدون اذن؛重要な問題は、次のとおりです。
- データ スペース マニフェスト (DS マニフェスト): マニフェスト Norito يعلن معلمات DS (バリデータ/QC キー) فئة الخصوصية، سياسة ISI، معلمات DA، (英語)。ハッシュとマニフェストとの関係。 ML-DSA-87 (ダイリチウム 5) ポスト量子。
- スペース ディレクトリ: DS のマニフェスト DS のマニフェスト / アナリティクスのマニフェストやあ。
- DSID: データスペース。名前空間の変更。
- アンカー: التزام تشفيري من كتلة/راس DS يضم الى سلسلة nexus لربط تاريخ DS بالدفتر العالمي.
- 倉: تخزين كتل Iroha。ブロブをブロックする必要があります。
- WSV: Iroha ワールド ステート ビュー。重要な問題は、次のとおりです。
- IVM: Iroha 仮想マシン (Kotodama バイトコード `.to`)。
  - AIR: 代数中間表現。スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク・スターク。 وحدود。

データスペース
- 名前: `DataSpaceId (DSID)` يعرّف DS 名前空間。 DS のアクセス:
  - ドメイン DS: `ds::domain::<domain_name>` - ドメイン。
  - Asset-DS: `ds::asset::<domain_name>::<asset_name>` - テスト。
  ヤステルDSID を確認してください。
- マニフェスト: تسجيل إنشاء DS والتحديثات (تدوير المفاتيح، تغييرات السياسة) والإنهاء في Space Directory。 DS のスロット スロット ハッシュ マニフェスト。
- パブリック DS (مشاركة مفتوحة، DA عامة) とプライベート DS (مقيد، DA سرية)。マニフェストにフラグを立てます。
- 国際 DS: 国際規格 ISI، معلمات DA `(k,m)`، تشفير، احتفاظ، حصص (حصة tx الدنيا/العليا لكل كتلة)، سياسة اثبات ZK/تفاؤلي، رسوم。
- メッセージ: DS يحددان في قسم الحوكمة بالـ マニフェスト (مقترحات على السلسلة، multisig، او接続と認証)。UAID とマニフェスト
- データスペース: UAID データスペース (`UniversalAccountId` `crates/iroha_data_model/src/nexus/manifest.rs`) データスペース。マニフェスト (`AssetPermissionManifest`) UAID データスペースのマニフェストの許可/拒否`ManifestEntry` 、 `dataspace` 、 `program_id` 、 `method` 、 `asset` 、 AMX 。拒否する`ManifestVerdict::Denied` は、`Allowed` の手当を支給します。
- 手当: バケットを許可します `AllowanceWindow` (`PerSlot`、`PerMinute`、`PerDay`) と `max_amount` を許可します。ホストと SDK のサポート Norito は、SDK をサポートします。
- テレメトリ: スペース ディレクトリ `SpaceDirectoryEvent::{ManifestActivated, ManifestExpired, ManifestRevoked}` (`crates/iroha_data_model/src/events/data/space_directory.rs`) マニフェスト。 `SpaceDirectoryEventFilter` يمكّن مشتركين Torii/data-event مراقبة تحديثات UAID マニフェスト والابطال وقرارات拒否は勝ちです。

SDK マニフェストのマニフェストを確認するユニバーサル アカウント ガイド (`docs/source/universal_accounts_guide.md`)。 UAID を使用してください。

और देखें
1) チェーン (Nexus チェーン)
- データ スペース (DS) のデータ スペース (DS)。世界国家 الموحد (متجه ルーツ لكل DS)。
- 証明/QC の構成可能性 (DSID のルートと DSID の組み合わせ) قبل/بعد، التزامات DA، 証明 صلاحية لكل DS، وشهادة 定足数 للـ DS باستخدام ML-DSA-87)。すごいですね。
- 接続: BFT 接続パイプライン 22 接続 (3f+1 接続 f=7) VRF/ステーク接続 接続数~200k です。 1 秒のネクサスを接続します。

2) データスペース (パブリック/プライベート)
- テストの DS の証明 WSV のアーティファクトの証明 (証明の DS の証明) DA) تجمع في كتلة Nexus 1s.
- Private DS のゲームをプレイするDS と証明 PQ 。
- パブリック DS の証明 (DA 経由) と証明 PQ 。

3) データスペース (AMX)
- アクセス: DS (ドメイン DS および資産 DS) 。 Nexus واحدة او تلغى؛ああ、それは。
- Prepare-Commit 1s: DS المعنية بالتوازي على نفس スナップショット (roots DS スロット) 証明PQ は DS (FASTPQ-ISI) と DA です。セキュリティ コミテ ネクサス فقط اذا تحققت كل 証明 المطلوبة ووصلت شهادات DA في الوقت (هدف <=300 ms)؛スロットスロットをプレイしてください。
- الاتساق: مجموعات القراءة/الكتابة مصرح بها؛スロットを根付かせます。 DS يتجنب التعطيل بدون اقفال لكل DS يتجنب التعطيل العالمي؛ الذرية تفرض بقاعدة التزام nexus (الكل او لا شيء عبر DS)。
- 秘密: プライベート DS の証明/コミットメント、ルーツ قبل/بعد。すごいですね。

4) 評価 (DA) 評価
- クラ ソリューション、WSV ブロブ、セキュリティ。ブロブ العامة على نطاق واسع؛ BLOB とプライベート DS チャンクの両方。
- 世界のアーティファクト、DS、Nexus、サンプリング、評価كشف المحتوى الخاص。

هيكل الكتل والالتزام
- アーティファクト データ スペース (スロット 1 と DS)
  - 説明: DSID、スロット、pre_state_root、post_state_root、ds_tx_set_hash、kura_da_commitment、wsv_da_commitment、manifest_hash、ds_qc (ML-DSA-87)、ds_validity_proof (FASTPQ-ISI)。
  - プライベート DS アーティファクトの管理وpublic DS は باسترجاع الاجسام عبر DA です。

- Nexus ブロック (1 秒)
  - 説明: block_number、parent_hash、slot_time、tx_list (DS 間の DSID)、ds_artifacts[]、nexus_qc。
  - 保存: 保存されたアーティファクトの保存DS のルーツを確認してください。スケジュール管理
- اجماع Nexus Chain: BFT عالمي متسلسل (فئة Sumeragi) مجنة 22 عقدة (3f+1 مع f=7) تستهدف كتل 1秒 ونهائية 1秒。 VRF/ステーク 最大 ~200k の VRF/ステークログインしてください。
- データ スペース: DS BFT アーティファクト スロット (証明 DS QC)。レーンリレー インターフェイス `3f+1` インターフェイス `fault_tolerance` データスペース インターフェイス インターフェイスVRF シードは `(dataspace_id, lane_id)` です。プライベートDS Public DS は反シビルです。接続と接続。
- スケジュール設定: DSID および DSID を確認します。 DS スロットスロット1 秒間のアーティファクトと DA 時間 (<=300 ミリ秒)。
- アクセス: DS のメモリプールを確認します。 DS のクォータ、プライベート DS の先頭ブロック、プライベート DS の制限。

名前空間の設定
- DS 認定 ID: (ドメイン、アカウント、資産、役割) `dsid`。意味: `ds::<domain>::account`、`ds::<domain>::asset#precision`。
- グローバル参照: グローバル参照: タプル `(dsid, object_id, version_hint)` ويمكن وضعه オンチェーン في طبقة nexus و في وصاف AMX للاستخدامクロスDS。
- Norito シリアル化: クロス DS (AMX 証明) Norito コーデック。あなたのことを考えてください。

IVM
- 回答: 回答 `dsid` 回答 IVM。 Kotodama はデータ スペースです。
- プリミティブ DS:
  - `amx_begin()` / `amx_commit()` マルチ DS ホスト IVM 。
  - `amx_touch(dsid, key)` ルート スナップショット スロット。
  - `verify_space_proof(dsid, proof, statement)` -> ブール値
  - `use_asset_handle(handle, op, amount)` -> 結果 (مسموح فقط اذا سمحت السياسة وكان ハンドル صالحا)
- アセット ハンドル:
  - ISI/役割 للـ DS؛ DS のガスを入手してください。機能トークン (マルチ承認者、レート制限、ジオフェンシング) が必要になります。
- バージョン: システムコールとシステムコール、AMX および AMX のシステムコール。最高です。ポスト量子 (ISI معممة)
- FASTPQ-ISI (PQ の信頼できるセットアップ): ハッシュ ベースの転送 ISI セキュリティ メッセージとセキュリティ メッセージ20k GPU を搭載。
  - 重要:
    - 証明者証明者 `fastpq_prover::Prover::canonical` 証明バックエンド証明は、模擬を模擬します。 [crates/fastpq_prover/src/proof.rs:126]
    - `zk.fastpq.execution_mode` (構成) と `irohad --fastpq-execution-mode` ステータス CPU/GPU ステータス オブザーバー フック リクエスト/解決済み/バックエンドすごい。 [crates/iroha_config/src/parameters/user.rs:1357] [crates/irohad/src/main.rs:270] [crates/irohad/src/main.rs:2192] [crates/iroha_telemetry/src/metrics.rs:8887]
- アリメティゼーション:
  - KV アップデート AIR: WSV キーと値のポセイドン 2-SMT。 ISI の読み取り、チェック、書き込みの機能 (アカウント、資産、ロール、ドメイン、メタデータ、サプライ)。
  - オペコード: AIR セレクター ISI (保存、単調カウンター、権限、範囲チェック、メタデータ) ）。
  - ルックアップ引数: ビット単位の精度。
- 回答:
  - 集約された SMT 証明: テスト (事前/事後) テスト `old_root`/`new_root` テスト フロンティア テスト 兄弟重要です。
  - 不変条件: يتم فرض ثوابت عالمية (مثل اجمالي Supply لكل اصل) عبر مساواة multiset بين صفوف التأثير والعداداتああ。
- 回答:
  - 金曜日 (ディープ-金曜日) 金曜日 (8/16) 爆発 8 月 16 日ハッシュ ポセイドン2トランスクリプト Fiat-Shamir مع SHA-2/3。
  - 再帰: DS のマイクロバッチとスロットの再帰。
- 回答:
  - 操作: 転送、ミント、書き込み、アセット定義の登録/登録解除、精度の設定 (مقيد)、メタデータの設定。
  - 署名/削除: 署名者の作成/削除、キー/しきい値の設定、署名者の追加/削除 (署名者 فقط؛ فحص التواقيع يثبت من مدققي DS وليس داخل AIR)。
  - セキュリティ/セキュリティ (ISI): ロールと権限の付与/取り消し検索は単調にチェックします。
  - AMX/AMX: 開始/コミット AMX 機能のミント/取り消しありがとうございます。
- 航空便の運航状況:
  - التواقيع والتشفير الثقيل (مثل تواقيع ML-DSA للمستخدمين) يتحقق منها مدققو DS ويثبتونها في DS QC؛ログインしてください。 هذا يبقي 証明 PQ وسريعة。
- 性能 (CPU 32 個 + GPU 性能):
  - 20k ISI メッセージ (<=8 キー/ISI): ~0.4 ～ 0.9 秒、~150 ～ 450 KB、~5 ～ 15 ミリ秒。
  - ISI 処理: マイクロバッチ (10x2k) + 再帰処理 <1 秒スロット。
- DS マニフェスト:
  - `zk.policy = "fastpq_isi"`
  - `zk.hash = "poseidon2"`、`zk.fri = { blowup: 8|16, arity: 8|16 }`
  - `state.commitment = "smt_poseidon2"`
  - `zk.recursion = { none | local }`
  - `attestation.signatures_in_proof = false` (DS QC)
  - `attestation.qc_signature = "ml_dsa_87"` (فتراضي؛ البدائل يجب اعلانها صراحة)
- フォールバック:
  - ISI المعقدة/المخصصة يمكنها استخدام STARK عام (`zk.policy = "stark_fri_general"`) مع اثبات مؤجل ونهائية 1s عبر 認証 QC + スラッシュ認証済みです。
  - PQ (Plonk مع KZG) の信頼できるセットアップをビルドします。

AIR プライマー (Nexus)
- トレース التنفيذ: مصفوفة بعرض (اعمدة سجلات) وطول (خطوات)。 كل صف هو خطوة منطقية في معالجة ISI؛プレ/ポスト、セレクター、フラグが含まれています。
- 回答:
  - 計算: 計算結果 (post_balance = pre_balance - 金額 `sel_transfer = 1`)。
  - バージョン: パブリック I/O (old_root/new_root、カウンター) およびバージョン。
  - ルックアップ/並べ替え: (アクセス許可、資産パラメータ) ビットヘビーです。
- 回答:
  - 証明者 بالالتزام بالتراسات عبر ترميزات ハッシュ ويبني متعددات حدود منخفضة الدرجة صالحة اذاそうです。
  - 認証検証者 FRI (ハッシュベース、ポスト量子) 認証 Merkle 認証ありがとうございます。
- 転送 (転送): pre_balance、amount、post_balance、nonce、セレクター。マルチプルーフ SMT は、古い/新しいルートをサポートします。ABI とシステムコール (ABI v1)
- Syscalls لاضافتها (اسماء توضيحية):
  - `SYS_AMX_BEGIN`、`SYS_AMX_TOUCH`、`SYS_AMX_COMMIT`、`SYS_VERIFY_SPACE_PROOF`、`SYS_USE_ASSET_HANDLE`。
- ポインタ - ABI のアドレス:
  - `PointerType::DataSpaceId`、`PointerType::AmxDescriptor`、`PointerType::AssetHandle`、`PointerType::ProofBlob`。
- 回答:
  - ضافة الى `ivm::syscalls::abi_syscall_list()` (مع الحفاظ على الترتيب)، مع gating حسب السياسة.
  - `VMError::UnknownSyscall` ホスト。
  - システムコール リスト ゴールデン ABI ハッシュ ポインター タイプ ID ゴールデン。
  - ドキュメント: `crates/ivm/docs/syscalls.md`、`status.md`、`roadmap.md`。

ログインしてください。
- セキュリティ: セキュリティ セキュリティ WSV プライベート DS セキュリティ セキュリティああ、それは。
- ヘッダーと DA と証明と PQ を表示します。
- ادلة ZK اختيارية: يمكن لـ private DS انتاج ادلة ZK (مثل كفاية الرصيد او امتثال السياسة) لتمكين عمليات クロスDS دون كشف الحالة الداخلية。
- セキュリティ: セキュリティ セキュリティ ISI/ロール セキュリティ DS。能力トークンとは、能力トークンのことです。

QoS と品質
- DS をプレイしてください。
- クォータ、スケジューリング、DS ネクサス、アンカー、ヘッドオブライン ブロッキング。
- DS (コンピューティング/メモリ/IO) ホスト IVM。パブリック DS とプライベート DS があります。
- クロス DS とプライベート DS の組み合わせ。

ログイン して翻訳を追加する
1) いいえ
- リードソロモン (GF(2^16)) の文字列、クラ、WSV: `(k, m)` 文字`n = k + m` シャード。
- シャード (パブリック DS): `k=32, m=16` (n=48) シャード 16 個、最大 1.5 倍。プライベート DS: `k=16, m=8` (n=24) の情報。 DS マニフェスト。
- Blobs : シャード、DA/مدققين عديدة مع فحوص توفر بالعينات。 DA はヘッダーとライト クライアントをサポートします。
- BLOB : シャード プライベート DS (プライベート DS)。 السلسلة العالمية تحمل فقط التزامات DA (دون مواقع shards او المفاتيح)。

2) サンプリング
- ブロブ: マークル ルート シャード `*_da_commitment`。 PQ を使用してください。
- DA 認証者: 認証者 اقليميون يتم اختيارهم عبر VRF (مثلا 64 لكل منطقة) يصدرون شهادة ML-DSA-87 تؤكد サンプリング الناجح。認証時間 <=300 ミリ秒。シャードとのネクサスとの接続。

3) クラ
- マークルをブロックします。
- ヘッダーとブロブDA はパブリック DS とプライベート DS をサポートします。

4) WSV の作成
- スナップショット WSV: チェックポイント、DS スナップショット、ヘッダー。変更ログ。公共の場で、プライベートでの活動を行ってください。
- 証明携行アクセス: يمكن للعقود تقديم (او طلب) ادلة حالة (Merkle/Verkle) スナップショット。プライベート DS はゼロ知識を持っています。

5) 剪定
- 剪定を行うパブリック DS: クラ、WSV、DA (توسع افقي)。プライベート DS のテストを開始します。 Nexus は、DS のアーティファクトをブロックします。

और देखें
- アクセス: ネットワーク接続 Nexus ブロック、DS アーティファクト、DA パブリック DS。
- データ スペース: DS 版、Kura/WSV 版、DS 版。
- عقد DA (اختياري): ブロブ サンプリング。プライベート DS は、DA の評価を取得します。最高のパフォーマンスを見せてください。
- シーケンス/メモリプール: メモリプール DAG (イッカク) BFT 接続ネクサス スループットそうです。
- DS のセキュリティ: DS の先頭ブロック、プライベート DS のセキュリティ。
- DS (PQ): 定足数は DS の ML-DSA-87 (Dilithium5) です。ポスト量子スロット。EC スロット。QC スロット。 DS マニュアル ML-DSA-65/44 マニュアル EC マニュアル DS マニフェストパブリック DS は ML-DSA-87 です。
- DA 認証者: 公開 DS 認証者、VRF 認証、DA 認証者。ネクサスの評価とサンプリングの結果プライベート DS のテスト。
- 再帰: マイクロバッチ、DS スロット/エポック、スロット/エポックあなたのことを忘れないでください。
- レーン スケーリング (レーン スケーリング): レーン スケーリング (レーン スケーリング): レーン スケール (K レーン)やあ。 هذا يحافظ على ترتيب عالمي واحد مع توسع افقي.
- 説明: カーネル SIMD/CUDA 機能フラグ、ハッシュ/FFT フォールバック CPU 機能、および CPU 機能フラグ。
- レーン数 (اقتراح): 2 ～ 4 レーン (a) レーン数 1.2 秒、3 レーン(b) 評価 85% 評価 5 評価 (c) 評価 >1.2x重要です。レーン数と DSID のハッシュ数、ネクサスの数を確認します。

الرسوم والاقتصاد (افتراضات اولية)
- トークン: DS コンピューティング/IO トークンDS をプレイしてください。 DS をプレイしてください。
- ラウンドロビン DS 1 回、DS 1 回、SLO 1 秒DS にアクセスしてください。
- 重要: يمكن استكشاف سوق رسوم عالمي او سياسات تقلل MEV دون تغيير الذرية او تصميم ادلة PQ.

クロスデータスペース (مثال)
1) يرسل مستخدم معاملة AMX تمس public DS P و private DS S: نقل الاصل X من S الى المستفيد B الذي حسابه في P.
2) スロットスロット P と S のスナップショット。 تحقق S من التفويض والتوفر وتحدث حالتها الداخلية وتنتج اثبات صلاحية PQ والتزام DA (بدون تسريب ）。 يحضر P تحديث الحالة المقابل (مثل ミント/バーン/ロック في P حسب السياسة) واثباته。
3) تتحقق لجنة nexus من كلا اثباتي DS وشهادات DA؛スロットスロットスロット Nexus 1s وتحديث ルート DS في世界国家です。
4) اذا كان اي اثبات او شهادة DA مفقودا او غير صالح، تلغى المعاملة (بدون اثر) ويمكن للعميلスロットスロットをプレイしてください。は、S の الخاصة في اي خطوة です。

- ああ、
- バージョン: システムコール IVM バージョンDS は AMX コミットを実行します。
- ニュース: ISI プライベート DS のニュース。能力トークンは、DS 間で使用されます。
- 名前: プライベート DS シャードの詳細: プライベート DS シャードの詳細ZK اختيارية لاثباتات خارجية。
- DoS: メモリプール/コンセンサス/ストレージ、パブリック DS、プライベート DS。

テスト Iroha
- iroha_data_model: `DataSpaceId`、ID、DS、AMX 記述子 (مجموعات قراءة/كتابة) وانواع 証明/التزامات DA。 Norito です。
- ivm: システムコールとポインター ABI タイプ、AMX (`amx_begin`、`amx_commit`、`amx_touch`) および DA の証明ABI وفقا لسياسة v1.