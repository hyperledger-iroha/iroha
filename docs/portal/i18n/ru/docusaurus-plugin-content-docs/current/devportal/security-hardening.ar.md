---
lang: ru
direction: ltr
source: docs/portal/docs/devportal/security-hardening.ar.md
status: complete
generator: docs/portal/scripts/sync-i18n.mjs
translator: machine-google-reviewed
translation_last_reviewed: 2026-02-07
---

# تشديد الامن وقائمة فحص pen-test

## نظرة عامة

Используйте **DOCS-1b** для получения кода устройства OAuth, а также для получения кода устройства.
Он был убит в 1980-х годах в Нью-Йорке, где он провел время. المختبر. يشرح هذا
Включите программу Go-Live, чтобы начать работу в прямом эфире. يجب على مراجعات
البوابة تنفيذها.

- **النطاق:** وكيل Try it, ولوحات Swagger/RapiDoc المضمنة, ووحدة Try it المخصصة المعروضة عبر
  `docs/portal/src/components/TryItConsole.jsx`.
- **Проверка:** Torii в (отправлении Torii) SoraFS (в формате DOCS-3/7).

## نموذج التهديدات

| الاصل | خطر | تخفيف |
| --- | --- | --- |
| رموز Torii носитель | Наслаждайтесь песочницей в песочнице | Код устройства (`DOCS_OAUTH_*`) для проверки заголовков. Его сын Сэнсэз сказал ему, что он не может этого сделать. |
| وكيل Попробуйте | سوء الاستخدام كمرحّل مفتوح او تجاوز حدود Torii | `scripts/tryit-proxy*.mjs` используется для ограничения скорости, проверки работоспособности и проверки работоспособности `X-TryIt-Auth`. Он был в Сан-Франциско. |
| زمن تشغيل البوابة | XSS в разделе XSS | `docusaurus.config.js` включает в себя Content-Security-Policy, Trusted Types и Permissions-Policy; Встроенные сценарии выполнения во время выполнения Docusaurus. |
| بيانات المراقبة | غياب القياس عن بعد او العبث | `docs/portal/docs/devportal/observability.md` — датчики/панели мониторинга; `scripts/portal-probe.mjs` используется для CI в режиме онлайн. |

Он сказал, что в 1990-х годах в Нью-Йорке было 10 лет назад. Уиллоу Джонс Рэйвенс Миссисипи
Он был создан в 1980-х годах. В 2007 году он был выбран в качестве кандидата на пост президента США.
Дэн Сонсон Мэн.

## الضوابط المطلوبة1. **Вход по коду устройства OAuth**
   - اضبط `DOCS_OAUTH_DEVICE_CODE_URL` و`DOCS_OAUTH_TOKEN_URL` و`DOCS_OAUTH_CLIENT_ID`
     Он был отправлен в Нью-Йорк.
   - بطاقة Попробуйте تعرض عنصر تسجيل الدخول (`OAuthDeviceLogin.jsx`) Проверьте код устройства
     Конечная точка токена ويمسح الرموز تلقائيا عند انتهاء صلاحيتها. تبقى переопределяет
     اليدوية لـ Bearer متاحة كخيار طارئ.
   - Вы можете настроить OAuth и отключить TTL для резервного копирования.
     Время ожидания 300–900 с в формате DOCS-1b; اضبط `DOCS_OAUTH_ALLOW_INSECURE=1` فقط
     Будьте осторожны.
2. **Тахан Уилсон**
   - `scripts/tryit-proxy.mjs` — разрешенные источники и ограничения скорости.
     Тайм-ауты восходящего потока были установлены в `X-TryIt-Client` и отключены в данный момент.
   - `scripts/tryit-proxy-probe.mjs` или `docs/portal/docs/devportal/observability.md`
     Проверка активности датчика и приборная панель; Будет развертывание.
3. **CSP, доверенные типы, политика разрешений**
   - `docusaurus.config.js` в случае необходимости:
     `Content-Security-Policy` (default-src self, используйте Connect/img/script,
     (Доверенные типы) — `Permissions-Policy` и `Referrer-Policy: no-referrer`.
   - Подключение к CSP и конечным точкам, проверка кода устройства OAuth и токена, а также белый список.
     (HTTPS используется для входа в систему `DOCS_SECURITY_ALLOW_INSECURE=1`) Для входа в систему устройства
     Свою песочницу в Лос-Анджелесе.
   - Вы можете просмотреть текстовые сообщения в HTML-формате, чтобы получить доступ к веб-страницам.
     الى اعدادات اضافية. Встроенные сценарии и загрузочный файл Docusaurus.
4. **Runbooks, наблюдаемость, отслеживание**
   - `docs/portal/docs/devportal/observability.md` для датчиков и панелей мониторинга.
     В 2007 году он был избран президентом США в Нью-Йорке.
   - `docs/portal/docs/devportal/incident-runbooks.md` يغطي مسار التصعيد اذا تم
     اساءة استخدام песочница; اجمعه مع
     `scripts/tryit-proxy-rollback.mjs` отображает конечные точки.

## قائمة فحص pen-test والاصدار

В ответ на вопрос о том, как это сделать (ارفق النتائج بتذكرة اصدار):1. **Проверка OAuth**
   - `npm run start` позволяет экспортировать файлы `DOCS_OAUTH_*` в исходное состояние.
   - Если вы хотите получить код устройства, попробуйте попробовать.
     Он был убит в 2007 году в Нью-Йорке.
2. **Вечеринка**
   - شغل `npm run tryit-proxy` ضد Torii staging, ثم نفذ
     `npm run probe:tryit-proxy` — путь к образцу.
   - Для ограничения скорости используется `authSource=override`.
     Он сказал, что это не так.
3. **Обмен CSP/Доверенные типы**
   - `npm run build` или `build/index.html`. تاكد ان وسم `` Дополнительная информация
     В DevTools используется CSP для создания приложений.
   - `npm run probe:portal` (с завитком) HTML-кода зонд
     الان عندما تكون وسوم `Content-Security-Policy` и `Permissions-Policy` او
     `Referrer-Policy` создан для того, чтобы узнать больше о том, как это сделать.
     `docusaurus.config.js`, بحيث يمكن لمراجعي الحوكمة العتماد على exit
     код بدلا من فحص خرج curl يدويا.
4. **Великая наблюдаемость**
   - تحقق من ان لوحة Попробуйте использовать прокси-сервер (ограничения скорости, коэффициенты ошибок, проверка работоспособности).
   - В разделе `docs/portal/docs/devportal/incident-runbooks.md`.
     Это приложение (доступно Netlify/SoraFS).
5. **Вечеринка**
   - ارفق لقطات الشاشة/السجلات بتذكرة الاصدار.
   - Сэйл находит Кейла Стоуна.
     ([`docs/examples/pentest_remediation_report_template.md`](../../../examples/pentest_remediation_report_template.md))
     Владельцы Трэвиса и SLA провели повторное тестирование.
   - Контрольный список для составления дорожной карты DOCS-1b, подготовленной в рамках проекта DOCS-1b.

اذا فشل اي خطوة, اوقف الترقية, وافتح Issue مانعة, ودون خطة المعالجة في `status.md`.