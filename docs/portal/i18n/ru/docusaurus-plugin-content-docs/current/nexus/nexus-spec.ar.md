---
lang: ru
direction: ltr
source: docs/portal/docs/nexus/nexus-spec.ar.md
status: complete
generator: docs/portal/scripts/sync-i18n.mjs
translator: machine-google-reviewed
translation_last_reviewed: 2026-02-07
---

---
идентификатор: Nexus-спецификация
Название: المواصفات التقنية لسورا نيكسس
описание: مرآة كاملة لـ `docs/source/nexus.md` تغطي معمارية وقيود التصميم لدفتر Iroha 3 (Sora Nexus).
---

:::примечание
Создан на `docs/source/nexus.md`. Он был убит в фильме "Тренер" в Вашингтоне.
:::

#! Iroha 3 - Сора Nexus Книга учета: المواصفات التقنية للتصميم

Создан для Sora Nexus Ledger с Iroha 3, для Iroha 2 Он был создан в 2008 году в Data Spaces (DS). توفر Data Spaces نطاقات خصوصية قوية («частные пространства данных») ومشاركة مفتوحة («публичные пространства данных»). В разделе о возможности компонуемости вы можете получить информацию о том, как создать частный-DS, Он используется для создания приложений для Kura (блочное хранилище) и WSV (World State View).

Для этого необходимо установить Iroha 2 (протокол управления) и Iroha 3. (SORA Nexus). Загрузите виртуальную машину Iroha (IVM) и установите флажок Kotodama, Вы можете получить байт-код, который будет указан в таблице ниже.

الاهداف
- Создан журнал, посвященный областям данных и пространствам данных.
- Пространства данных создаются в CBDC (CBDC) в рамках DS الخاص.
- Пространства данных созданы на базе Ethereum.
- Вы можете создать пространство данных в области хранения данных в частном DS.
- عزل اداء بحيث لا تؤثر الحركة العامة على معاملات Private-DS الداخلية.
- Он сыграл в фильме "Кура" и WSV в составе команды "Бенсон Сноу". Воспользуйтесь услугами частного DS.

غير الاهداف (المرحلة الاولى)
- تعريف اقتصاديات التوكن او حوافز المدققين; Планирование ставок и ставки на предстоящие матчи.
- إدخال اصدار ABI جديد؛ Доступен ABI v1 для системных вызовов и указателя-ABI в формате IVM.المصطلحات
- Nexus Ledger: Зарегистрируйтесь в Data Space (DS) в Справочном центре. Это не так.
- Пространство данных (DS): Он был назначен DA. Типы: публичный DS и частный DS.
- Пространство частных данных: مدققون باذونات وتحكم وصول؛ Он был выбран в честь DS. Он был убит в 2008 году.
- Пространство общедоступных данных: مشاركة بدون اذن؛. Вы можете сделать это в любое время.
- Манифест пространства данных (DS Manifest): манифест Norito для DS (валидаторы/ключи контроля качества, файлы ISI, DA, الاحتفاظ, الحصصص, سياسة ZK, الرسوم). Это хеш-код манифеста, связанный со связью. Для создания кворума в DS, созданного для ML-DSA-87 (на Dilithium5) Постквантовое явление.
- Космический каталог: عقد دليل عالمي على السلسلة يتتبع манифестирует DS والنسخ واحداث الحوكمة/الدوران لاغراض الاستدلال والتدقيق.
- DSID: Доступ к пространству данных. Используйте пространство имен для проверки.
- Якорь: التزام تشفيري من كتلة/راس DS يضم الى سلسلة nexus لربط تاريخ DS بالدفتر العالمي.
- Кура: تخزين по Iroha. В фильме «Бобс Блобс» рассказывается о Нэнли.
- WSV: Iroha Представление состояния мира. Он был убит в 1980-х годах в Нью-Йорке.
- IVM: Виртуальная машина Iroha в исходном состоянии (байт-код Kotodama `.to`).
  - AIR: алгебраическое промежуточное представление. Тэхен Билли в фильме "Старк" и в фильме "Старк" в фильме "Старк" В 2007 году.

Пространства данных
- Код: `DataSpaceId (DSID)` для DS с пространством имен в открытом виде. В ответ на это DS сказал:
  - Домен-DS: `ds::domain::<domain_name>` - Доступ к домену.
  - Asset-DS: `ds::asset::<domain_name>::<asset_name>` - Вы можете получить доступ к данным в режиме реального времени.
  الشكلان يتعايشان؛ Нажмите кнопку DSID, чтобы получить доступ к DSID.
- Демонстрационный манифест: تسجيل إنشاء DS والتحديثات (تدوير المفاتيح, تغييرات السياسة) Доступно в Space Directory. В слоте DS отображается хэш-манифест.
- Доступные: общедоступные DS (от DA عامة) и частные DS (от DA سرية). Вы можете установить флаги манифеста.
- Сообщение для DS: ISI, DA `(k,m)`, تشفير, احتفاظ, حصص (حصة tx الدنيا/العليا لكل كتلة), سياسة اثبات ZK/تفاؤلي، رسوم.
- Сообщение: Уведомление о DS в манифесте DS . Мультиподпись, сертификаты Nexus и сертификаты).Манифесты القدرات и UAID
- Сообщение: Зарегистрируйтесь в UAID حتمي (`UniversalAccountId` или `crates/iroha_data_model/src/nexus/manifest.rs`). Пространства данных. Манифест манифеста (`AssetPermissionManifest`) в пространстве данных UAID. Для разрешения/запрета `ManifestEntry` используется `dataspace` и `program_id` и `method` и `method`. `asset` используется для AMX. Кэтрин отрицает, что Тэмми Дэйр Установите `ManifestVerdict::Denied` в приложении `Allowed`, чтобы получить допуск к работе.
- Припуски: в разрешенном ковше `AllowanceWindow` (`PerSlot`, `PerMinute`, `PerDay`) и `max_amount`. В качестве хостов используются SDK с кодом Norito, а также встроенные приложения и SDK.
- Телеметрия: Доступ к космическому каталогу `SpaceDirectoryEvent::{ManifestActivated, ManifestExpired, ManifestRevoked}` (`crates/iroha_data_model/src/events/data/space_directory.rs`) и доступ к манифесту. Создайте `SpaceDirectoryEventFilter` для файла Torii/data-event в манифесте UAID. Нападающий Дени-победил Дэниел Уинстон.

Для создания манифестов в SDK необходимо просмотреть манифесты. См. руководство по универсальной учетной записи (`docs/source/universal_accounts_guide.md`). Он был отправлен в UAID в рамках программы UAID.

المعمارية عالية المستوى
1) Установить блок управления (цепь Nexus)
- تحافظ على ترتيب قانوني لكتل Nexus на 1-м этапе создания программы. Открытие пространств данных (DS). Это мировое государство المعاملة تحدث World State (корни слова - DS).
- Обеспечивает проверку доказательств/контроля качества, проверку компонуемости и контроль качества (DSID) Корни DS قبل/بعد, DA, доказательства DS, кворум DS МЛ-ДСА-87). Он сказал: "Бэнтон".
- Результат: BFT запустит трубопровод на 22 день (3f+1 фут f=7) на VRF/количество в конце дня. Стоимость около 200 тыс. просмотров. لجنة nexus ترتب المعاملات وتؤكد الكتلة خلال 1s.

2) Пространство данных данных (публичное/частное)
- تنفذ مقاطع المعاملات لكل DS, WSV WSV المحلي, وتنتج артефакты صلاحية لكل كتلة (доказательства Для DS и DA) используется код Nexus в течение 1 с.
- Рядовой DS. Для проверки DS необходимо предоставить доказательства и предоставить доказательства PQ.
- Публичный DS تصدر اجسام البيانات الكاملة (через DA) и доказательства صلاحية PQ.

3) Создание пространств данных (AMX)
- Информация: используется для создания DS (домен DS и актив DS). Создан в коде Nexus в приложении. لا آثار جزئية.
- Подготовка-фиксация в течение 1 с: создание моментального снимка DS DS , созданного в режиме реального времени (корневой слот DS ). Получите доказательства PQ для DS (FASTPQ-ISI) и DA. Для этого необходимо создать связь с доказательствами, а также проверить DA на время (время <=300 мс). Вы можете открыть слот для слота.
- Сообщение: مجموعات القراءة/الكتابة مصرح بها؛ Он был создан в 2008 году, когда он укоренил слот بداية. В 2017 году он был отправлен в Лондон, где он был DS-специалистом. Используйте приложение Nexus (на английском и английском языках DS).
- الخصوصية: تصدر Private DS فقط доказательства/обязательства مرتبطة بـ roots قبل/بعد. LA تخرج بيانات خاصة خام.4) Сан-Франциско (DA)
- Он был назван Курой в WSV и BLOB-объектах в WSV. يتم توزيع blobs العامة على نطاق واسع؛ Большие двоичные объекты используются для создания частных DS и фрагментов.
- يتم تسجيل التزامات DA في artes الخاصة بـ DS وفي كتل Nexus, مما يمكّن من Выборка была сделана Дэном Кейном в 2007 году.

Хейл Уилсон
- Пространство данных артефакта (в слоте 1 для DS)
  - Доступные: dsid, slot, pre_state_root, post_state_root, ds_tx_set_hash, kura_da_commitment, wsv_da_commitment, Manifest_hash, ds_qc (ML-DSA-87), ds_validity_proof (FASTPQ-ISI).
  - تصدر Private-DS артефакты Публичный DS опубликовал сообщение DA.

- Блок Nexus (время 1 с)
  - Доступные: номер_блока, родительский_хэш, интервал_времени, tx_list (для перекрестных DS и DSID), ds_artifacts[], nexus_qc.
  - Создание: Создание артефактов, которые можно найти в музее. Его корни связаны с DS в сериале "Нидерланды".

Информирование и планирование
- Цепь Nexus: BFT с защитным кожухом (Sumeragi) на 22 дюйма (3f+1 дюйм f=7) Интервал на 1 с и на 1 с. Размер ставки на VRF/доля в размере около 200 000 фунтов стерлингов. Это было сделано для того, чтобы сделать это и сделать это.
- Пространство данных: доступ к DS BFT и поиск артефактов в слоте (доказательства, проверка DA, DS QC). Для этого необходимо настроить Lane-Relay `3f+1` и `fault_tolerance` для пространства данных, подключенного к Интернету. Он был создан в 2007 году в британском VRF семени `(dataspace_id, lane_id)`. Частный DS Public DS опубликовал сообщение о борьбе с Сивиллой. Используйте ссылку Nexus ثابتة.
- Планирование расписания: вы можете просмотреть список DSID и изменить его. القراءة/الكتابة. В слоте DS بالتوازي داخل; Используйте nexus для создания артефактов в 1 с. мс).
- Сообщение: мемпулы DS отключены. Квоты для DS можно установить в режиме DS, а затем отключить блокировку начала линии для частных DS.

Изменение пространства имен и пространства имен
- Идентификаторы, соответствующие требованиям DS: домены (домены, учетные записи, активы, роли) — `dsid`. Коды: `ds::<domain>::account`, `ds::<domain>::asset#precision`.
- Глобальные ссылки: المرجع العالمي عبارة عن tuple `(dsid, object_id, version_hint)` и on-chain для Nexus в AMX. للاستخدام кросс-DS.
- Norito Сериализация: добавлена ​​кросс-DS (проверка AMX), добавлены кодеки Norito. لا استخدام لـ serde في مسارات الانتاج.Дополнительная информация IVM
- Зарегистрировано: `dsid` или IVM. Проверьте Kotodama, чтобы открыть пространство данных.
- примитивный ذرية عبر DS:
  - `amx_begin()` / `amx_commit()` обеспечивает подключение multi-DS к хосту IVM.
  - `amx_touch(dsid, key)` позволяет установить корневой снимок экрана в слоте.
  - `verify_space_proof(dsid, proof, statement)` -> логическое значение
  - `use_asset_handle(handle, op, amount)` -> результат (مسموح فقط اذا سمحت السياسة وكان handle صالحا)
- Ручки активов
  - Сыграл в роли DS; Воспользуйтесь газовым баллоном DS. Для токенов возможностей. Поддержка нескольких утверждений (множественное утверждение, ограничения скорости, геозоны).
- Проверка: системные вызовы могут быть отключены при вызове системных вызовов. Используйте/подключайте AMX. لا تأثيرات خفية للوقت البيئة.اثباتات الصلاحية постквантовый (ISI معممة)
- FASTPQ-ISI (доверенная установка PQ): передача данных на основе хеш-функции для ISI и последующего восстановления. Обеспечивает производительность 20 000 пикселей на графическом процессоре.
  - В ответ:
    - Для проверки необходимо использовать `fastpq_prover::Prover::canonical` для проверки серверной части. تمت ازالة издевается над الحتمي. [crates/fastpq_prover/src/proof.rs:126]
    - `zk.fastpq.execution_mode` (конфигурация) и `irohad --fastpq-execution-mode` для настройки CPU/GPU, связанного с функцией наблюдателя. ثلاثيات запросил/разрешил/бэкэнд-файл. [crates/iroha_config/src/parameters/user.rs:1357] [crates/irohad/src/main.rs:270] [crates/irohad/src/main.rs:2192] [crates/iroha_telemetry/src/metrics.rs:8887]
- Аритметизация:
  - KV-Update AIR: добавление ключей и значений WSV для поддержки Poseidon2-SMT. В ISI используется система чтения-проверки-записи данных (учетные записи, активы, роли, домены, метаданные, ресурсы).
  - Добавлен код операции: AIR используется для выбора селектора, используется для ISI (сохранение, монотонные счетчики, разрешения, проверки диапазона, تحديثات метаданные محدودة).
  - Аргументы поиска: Задайте параметры поиска/точки и укажите параметры поиска. Побитовое الثقيلة.
- Информационные сообщения:
  - Совокупное доказательство SMT: جميع المفاتيح المتأثرة (до/после) تثبت مقابل `old_root`/`new_root` باستخدام frontier Его братья и сестры были убиты.
  - Инварианты: يتم فرض ثوابت عالمية (مثل اجمالي Supply لكل اصل) عبر مساواة multiset بين صفوف التأثير والعدادات المتعقبة.
- Ответ на вопрос:
  - التزامات متعدددة الحدود بأسلوب FRI (DEEP-FRI) с аритией (8/16) и взрывом 8-16; хэши Poseidon2; расшифровка Фиат-Шамир مع SHA-2/3.
  - Рекурсивный режим: обработка микропакетов в DS в зависимости от слота.
- Ответ на вопрос:
  - Управление: передача, создание, запись, регистрация/отмена регистрации определений активов, установка точности (مقيد), установка метаданных.
  - Создание/удаление: создание/удаление, установка ключа/порогового значения, добавление/удаление подписавших в эфире AIR).
  - Доступ/отмена (ISI): предоставление/отзыв ролей и разрешений; Для поиска и проверки используется монотонный поиск.
  - Настройки/AMX: запуск/фиксация AMX, возможность создания/отмены операции с ключом. Он сказал, что это не так.
- В режиме AIR в аэропорту:
  - Уилл Уилсон (в роли Тома ML-DSA в Лос-Анджелесе), которого зовут Миссисипи DS Написано в DS QC; Вы можете сделать это в любое время суток. هذا يبقي доказательства PQ وسريعة.
- Дополнительный процессор (32 процессора + процессор графического процессора):
  - Скорость передачи данных 20 000 ISI (<=8 ключей/ISI): ~0,4–0,9 с, время обработки ~150–450 КБ, ~5–15 мс تحقق.
  - ISI-запрос: микропакет (размером 10x2k) + рекурсивный интервал <1 с в слоте.
- Дополнительный манифест DS:
  - `zk.policy = "fastpq_isi"`
  - И18НИ00000091Х, И18НИ00000092Х
  - `state.commitment = "smt_poseidon2"`
  - `zk.recursion = { none | local }`
  - `attestation.signatures_in_proof = false` (проверка DS QC)
  - `attestation.qc_signature = "ml_dsa_87"` (на английском языке)
- Резервные варианты:- ISI ISI поддерживает/отключает STARK DA (`zk.policy = "stark_fri_general"`) на 1 сек. عبر аттестация QC + косая черта على ادلة غير صحيحة.
  - خيارات غير PQ (в Plonk от KZG) обеспечивает доверенную настройку для выполнения сборки в режиме реального времени.

AIR Primer (номер Nexus)
- Следите за событиями: مصفوفة بعرض (اعمدة سجلات) وطول (خطوات). В 2017 году в ISI Доступны значения до/после, селекторы и флаги.
- Ответ:
  - Выполнено: علاقات صف-الى-صف (مثلا post_balance = pre_balance - сумма, которую вы хотите получить `sel_transfer = 1`).
  - Обновление: Открытый ввод-вывод (old_root/new_root, счетчики).
  - Поиск/перестановки: поиск и настройка параметров доступа (разрешения, параметры активов). Дэн Дэниел немного тяжеловат.
- Ответ на вопрос:
  - проверяющий, который работает с хешем Сделайте это в ближайшее время.
  - Верификатор для проверки данных в FRI (на основе хеш-функции, постквантовый) для Меркла قليلة؛ Сделайте это в ближайшее время.
- مثال (Передача): تتضمن السجلات pre_balance, sum, post_balance, nonce, селекторы. تفرض القيود عدم السلبية/المدى, الحفظ, ورتابة nonce, BEAN تربط multi-proof SMT مجمعة الاوراق до/после, у Бэна корни старые/новые.

Использование ABI и системных вызовов (ABI v1)
- Системные вызовы
  - `SYS_AMX_BEGIN`, `SYS_AMX_TOUCH`, `SYS_AMX_COMMIT`, `SYS_VERIFY_SPACE_PROOF`, `SYS_USE_ASSET_HANDLE`.
- Указатель انواع-ABI لاضافتها:
  - И18НИ00000104Х, И18НИ00000105Х, И18НИ00000106Х, И18НИ00000107Х.
- Сообщение о:
  - Установите `ivm::syscalls::abi_syscall_list()` (встроенный блок управления) и затворите его.
  - Установите флажок `VMError::UnknownSyscall` на хостах.
  - Обнаружение: золотой список системных вызовов, хэш ABI, золотой идентификатор типа указателя, а также стандартный список.
  - Документы: `crates/ivm/docs/syscalls.md`, `status.md`, `roadmap.md`.

نموذج الخصوصية
- Доступ к WSV с помощью частного DS-сервера. تغادر مجموعة المدققين الخاصة.
- Проверка: заголовки и доказательства DA и доказательства PQ.
- Сообщение ZK: Доступ к частному DS в режиме ZK (отправка сообщений в режиме онлайн) Он перекрёстно DS Дэниел Уилсоном.
- Имя персонажа: В роли DS. маркеры возможностей

Качество обслуживания и QoS
- Он был создан в честь DS.
- Квоты при планировании в DS и nexus لتقييد زمن ادراج привязывают и блокируют начало линии.
- Для подключения к DS (вычисления/память/IO) используется хост IVM. Доступ к общедоступным DS или к частным DS.
- Кросс-DS позволяет получить доступ к подписке Private-DS.Он сказал, что это не так.
1) ترميز المحو
- Нападающий Рид-Соломон Джон (в роли GF(2^16)) в роли Блоба в фильме "BLOB" Ле Кура в WSV: От `(k, m)` к осколкам `n = k + m`.
- Открытый доступ (публичный DS): `k=32, m=16` (n=48) Доступно 16 осколков. Скорость ~1,5x. Частный DS: `k=16, m=8` (n=24) Откройте для себя DS Manifest.
- Blobs العامة: shards موزعة عبر عقد DA/مدققين عديدة مع فحوص توفر DA/عينات. Использование DA в заголовках для легких клиентов.
- Расширение Blobs: shards создано в рамках частного DS (на английском языке). Это было сделано в честь Д.А. (Дэми Мэн shards и المفاتيح).

2) Предварительный отбор проб
- Большой двоичный объект: корень Меркла в осколках и `*_da_commitment`. Он был назначен PQ в Вашингтоне.
- DA Attesters: подтверждающие лица, работающие в VRF (64 июля), в ML-DSA-87. تؤكد выборка الناجح. Продолжительность аттестации <= 300 мс. لجنة nexus تتحقق в الشهادات بدلا من سحب shards.

3) Сахи Кура
- В фильме «Капли Блобса» рассказывается о Меркле.
- заголовки и BLOB-объекты; Если вы хотите использовать DA для общедоступных DS, вы можете использовать частные DS.

4) Он WSV
- Снимки WSV: снимок экрана на контрольно-пропускном пункте DS и снимки экрана в режиме реального времени. В заголовках. Он добавил в журнал изменений. Публичный доступ к публичным данным и частным лицам может быть достигнут с помощью Миссисипи.
- Доступ для переноса доказательств: يمكن للعقود تقديم (او طلب) ادلة حالة (Merkle/Verkle) для создания снимка. В частном DS используется технология с нулевым разглашением данных, которую можно использовать для проверки данных.

5) обрезка и обрезка
- Выполнил обрезку публичного DS: У него есть имя Кура, WSV, DA (Тэхен). В частном DS вы можете связаться с нами, чтобы получить информацию о том, как это сделать. ЛЛОРДИ. Приложение nexus создано Nexus Блокирует артефакты обнаружения в DS.

الشبكات وادوار العقد
- Обновление: добавлено в Nexus, добавлено Nexus Blocks и DS артефакты. Создан в публичном DS.
- Пространство данных: Управление DS, Министерство здравоохранения, Университет Кура/WSV, Университет DA لـ. DS الخاصة بهم.
- DA (на английском языке): выборка BLOB-объектов и выборка. В частном DS, Д.А. Сэнсэй был убит в 2008 году в Нью-Йорке.تحسينات واعتبارات على مستوى النظام
- Для секвенирования/мемпула: создание мемпула DAG (в Нарвале) и BFT для обеспечения пропускной способности Nexus. Дэн Тэрри Уинстон.
- Сообщение DS: вызов DS в случае, если в телефоне есть блокировка начала линии, Кейн Кейн. Доступно в частном DS.
- Создание DS (PQ): создать кворум для DS, использующего ML-DSA-87 (на Dilithium5). Постквантовый анализ проводится в лондонском EC в Лос-Анджелесе, где QC находится в слоте. В комплект поставки DS входит ML-DSA-65/44 в каталоге EC в DS Manifest; Доступен общедоступный DS-файл ML-DSA-87.
- Подтверждающие DA: публичные DS, а также подтверждающие лица, работающие в VRF в офисе DA. لجنة nexus تتحقق в الشهادات بدلا من الخام؛ частный DS تحتفظ بشهاداتها الداخلية.
- Обновление рекурсии: создание микро-пакетов для DS и выбор слота/эпохи в зависимости от времени. Он сказал, что хочет, чтобы это произошло.
- Масштабирование полос движения (в режиме масштабирования): Вы можете изменить размер полосы движения, используя K-полосы движения. Это Дэйв Дэй. Он был убит Тэрри Уинстоном и Стивом.
- Обновление: поддержка ядер SIMD/CUDA, флаги функций, хеширование/FFT и резервный процессор, а также возможность резервного копирования. عبر العتاد.
- Запуск полос движения (в режиме): движение 2–4 полос движения (a) в режиме p95 с интервалом 1,2 с на 3 دقائق متتالية, او (b) تجاوز الكتلة 85% لاكثر من 5 دقائق، او (c) تطلب معدل Скорость вращения >1,2x при увеличении скорости вращения. В переулках можно использовать хеш-код DSID для связи с Nexus.

الرسوم والاقتصاد (افتراضات اولية)
- Проверка: токен используется для DS для вычислений/IO; Это сообщение было опубликовано в DS. Он был назначен Джоном DS в Нью-Йорке.
- Режим работы: круговая система DS в режиме DS в режиме DS и SLOs 1s; Он написал DS в журнале "Старый мир" в Вашингтоне.
- В фильме: Джон Сэнсэй Рэйсон Сейлз и Стокгольм в MEV Дэниел Стоун В PQ.

Межпространственное соединение данных (مثال)
1) Для доступа к AMX используйте общедоступный DS P и частный DS S: выберите X в S и выберите B. في П.
2) В слоте нажмите P и S для моментального снимка. Он был создан в 2017 году в рамках программы PQ. Найт Д.А. (Берег Сэнсэй Дэнс). يحضر P تحديث الحالة المقابل (مثل mint/burn/locking في P حسب السياسة) واثباته.
3) Запустите Nexus в штате DS в DA; Для того, чтобы получить доступ к слоту, выполните следующие действия: Nexus на 1s. История уходит корнями в DS из мирового государства в мире.
4). Воспользуйтесь слотом для слота. Он был в восторге от этого события.- اعتبارات الامان
- Добавлено: вызов системных вызовов для IVM. DS запускает AMX, совершает транзакцию в режиме реального времени и в режиме реального времени.
- Сообщение: Служба ISI в частном DS-центре в Нью-Йорке. المسموحة. маркеры возможностей могут использоваться для перекрестного DS.
- Сообщение: تشفير من الطرف للطرف لبيانات Private-DS, shards بترميز محو مخزنة فقط لدى Откройте для себя ZK اختيارية لاثباتات خارجية.
- В случае DoS: создание пула памяти/консенсуса/хранилища для публичного доступа к частному DS.

تغييرات على مكونات Iroha
- iroha_data_model: `DataSpaceId` и идентификаторы для DS и дескрипторы AMX (встроенные/дефолтные). доказательства/التزامات DA. Код Norito.
- ivm: системные вызовы и типы указателей ABI в AMX (`amx_begin`, `amx_commit`, `amx_touch`) и доказательства DA; Создан ABI / ABI لسياسة v1.