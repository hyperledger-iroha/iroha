---
lang: ru
direction: ltr
source: docs/portal/docs/nexus/nexus-operator-onboarding.ar.md
status: complete
generator: docs/portal/scripts/sync-i18n.mjs
translator: machine-google-reviewed
translation_last_reviewed: 2026-02-07
---

---
идентификатор: nexus-operator-onboarding
title: تاهيل مشغلي data-space في Sora Nexus
описание: Установленный `docs/source/sora_nexus_operator_onboarding.md` используется для установки Nexus.
---

:::примечание
Был установлен `docs/source/sora_nexus_operator_onboarding.md`. Он был убит в 2007 году в 1980-х годах.
:::

# تاهيل مشغلي Data-Space في Sora Nexus

Он был создан в рамках Data-space в Sora Nexus. الاعلان عن اصدار. В случае с Днем Рождения (`docs/source/release_dual_track_runbook.md`) необходимо установить блок управления (`docs/source/release_artifact_selection.md`) В 2017 году в 2007 году в 2017 году в 2017 году в 2017 году в 2017 году в 2017 году было объявлено о том, что в 2017 году в 2017 году в 2017 году было объявлено о том, что в 2017 году в 2017 году в 2017 году было зарегистрировано 100% случаев. В переулке есть место для отдыха.

## الجمهور والمتطلبات المسبقة
- تم اعتمادك من برنامج Nexus واستلمت تعيين data-space (فهرس Lane, ومعرف/alias للـ data-space, в Стокгольме).
- В разделе «Разработка выпуска» (tarballs, манифесты, манифесты) مفاتيح عامة).
- قمت بتوليد او استلام مواد مفاتيح الانتاج لدور validator/observer (هوية عقدة Ed25519, مفتاح) Используйте BLS + PoP для валидаторов, а также переключайте режимы проверки.
- Написано в Соре Nexus, созданном Сэнго-Бейнсом при начальной загрузке.

## الخطوة 1 - تاكيد ملف الاصدار
1. Добавьте псевдоним и идентификатор цепочки, чтобы узнать больше.
2. Установите `scripts/select_release_profile.py --network <alias>` (`--chain-id <id>`) и установите флажок. Для этого используйте `release/network_profiles.toml`. Он был создан Сорой Nexus и отправлен в США `iroha3`. Он выступил с проектом Release Engineering.
3. Установите флажок для подключения к Интернету (например, `iroha3-v3.2.0`); ستستخدمه لجلب القطع وملفات манифест.

## الخطوة 2 - جلب القطع والتحقق منها
1. Для подключения `iroha3` (`<profile>-<version>-<os>.tar.zst`) необходимо установить блок управления (`.sha256`, `.sig/.pub`, `<profile>-<version>-manifest.json`, и `<profile>-<version>-image.json` в штатном режиме).
2. Сообщение о том, что происходит в действительности:
   ```bash
   sha256sum -c iroha3-<version>-linux.tar.zst.sha256
   openssl dgst -sha256 -verify iroha3-<version>-linux.tar.zst.pub \
       -signature iroha3-<version>-linux.tar.zst.sig \
       iroha3-<version>-linux.tar.zst
   ```
   Установите `openssl`, чтобы получить информацию о системе KMS. بالاجهزة.
3. Создайте `PROFILE.toml` в tar-архиве с файлом JSON:
   - `profile = "iroha3"`
   - Установите `version` и `commit` и `built_at`.
   - نظام التشغيل/المعمارية تطابق هدف النشر.
4. Для получения хеш-кода `<profile>-<version>-<os>-image.tar` необходимо установить хеш-код. Найдите идентификатор изображения в формате `<profile>-<version>-image.json`.## 3 - تجهيز الاعداد من القوالب
1. Установите флажок `config/` для получения дополнительной информации.
2. Установите флажок `config/`:
   - `public_key`/`private_key` создан Ed25519. Вы можете получить информацию о том, как работает система HSM; Он был отправлен в HSM.
   - Запустите `trusted_peers` и `network.address` и `torii.address` и запустите загрузку одноранговых узлов.
   - حدث `client.toml` بنقطة نهاية Torii الدث (Джейсон) был убит в 1980-х годах в Лос-Анджелесе.
3. Идентификатор цепочки идентификатора, который можно найти в разделе "Управление" - "Lane Lane" Он был убит Селфи Уэнни в Нью-Йорке.
4. Установите флажок у Соры: `irohad --sora --config <path>`. Для этого необходимо установить SoraFS и многополосный режим в Калининградской области.

## الخطوة 4 - Доступ к пространству данных
1. Для `config/config.toml` необходимо создать `[nexus]` в пространстве данных Nexus. Совет:
   - Установите флажок `lane_count` для полос движения в режиме онлайн.
   - В программе `[[nexus.lane_catalog]]` и `[[nexus.dataspace_catalog]]` или `index`/`id`. Это псевдонимы المتفق عليها. لا تحذف الادخالات العالمية الحالية; Псевдонимы используются в пространствах данных.
   - Записано в пространстве данных `fault_tolerance (f)`; Установите реле полосы движения `3f+1`.
2. Установите `[[nexus.routing_policy.rules]]` для проверки работоспособности. القالب الافتراضي يوجه تعليمات الحوكمة الى переулок `1` ونشر العقود الى переулок `2`; اضف او عدل القواعد حتى يذهب المرور المخصص لـ data-space الخاص بك الى الـ Lane والـ alias صحيحين. В разделе «Разработка релизов» автор книги «Русский университет».
3. Установите `[nexus.da]` и `[nexus.da.audit]` и `[nexus.da.recovery]`. в 1980-х годах в Нью-Йорке; Он сказал, что это не так.
4. Сделай ставку на волшебную палочку. Runbook создан в соответствии с `config.toml` в программе (в разделе "Управление").

## الخطوة 5 - التحقق قبل التشغيل
1. Чтобы получить информацию о том, как сделать это, нажмите:
   ```bash
   ./bin/irohad --sora --config config/config.toml --trace-config
   ```
   В 2017 году он был выбран в качестве посредника в работе над фильмом "Главный мир". Он был создан в 1970-х годах в Бытии.
2. Он сказал, что в действительности он был убит в 2007 году. عبر `docker load -i <profile>-<version>-<os>-image.tar` (تذكر تضمين `--sora`).
3. Задайте место для заполнения дорожки/заполнителя пространства данных. اذا وجدت، ارجع الى الخطوة 4 - لا يجب ان تعتمد عمليات الانتاج على معرفات заполнитель المرفقة مع القوالب.
4. نفذ اجراء Smoke المحلي (مثلا ارسل استعلام `FindNetworkStatus` عبر `iroha_cli`, وتاكد من ان В качестве примера можно привести `nexus_lane_state_total`, который был создан в 2017 году. او استيرادها حسب الحاجة).## الخطوة 6 - التحويل والتسليم
1. Установите `manifest.json` для того, чтобы получить дополнительную информацию о состоянии окружающей среды. Он был убит в 2017 году.
2. Установите Nexus Operations для завершения операции; Ответ:
   - Проверка соединения (идентификатор узла, идентификатор узла Torii).
   - قيم كتالوجlane/data-space الفعلية وسياسات التوجيه.
   - Хэши للثنائيات/الصور التي تم التحقق منها.
3. نسق القبول النهائي للاقران (слуховые семена وتخصيص переулок) مع `@nexus-core`. لا تنضم للشبكة حتى تحصل على الموافقة; يفرض Sora Nexus اشغالا حتميا للـ переулки ويتطلب, манифест قبول محدث.
4. Создание резервных копий Runbook, которые переопределяются в соответствии с правилами, установленными в программе Runbook. Установите базовый уровень.

## قائمة تدقيق مرجعية
- [ ] تم التحقق من ملف الاصدار كـ `iroha3`.
- [ ] تم التحقق من хэши والتواقيع للحزمة/الصورة.
- [ ] تم تحديث المفاتيح وعناوين сверстников ونقاط نهاية Torii لقيم الانتاج.
- [ ] Доступ к полосе/пространству данных осуществляется с помощью Nexus, который находится в режиме ожидания.
- [ ] مدقق الاعداد (`irohad --sora --config ... --trace-config`) на сайте بدون تحذيرات.
- [ ] تم ارشفة Manifes/التواقيع في تذكرة التاهيل وتم اخطار Ops.

لمزيد من السياق حول مراحل هجرة Nexus وتوقعات التليمتري، راجع [Nexus примечания к переходу](./nexus-transition-notes).