# Retired pre-release example.
#
# scripts/release_sorafs_cli.sh deliberately does not accept configuration
# files or fixture credentials. Production release authentication requires
# explicit runtime paths for the canonical aggregate manifest, reviewed
# external Ed25519 signer, raw governed public key, trusted key fingerprint,
# and SHA256-pinned native release-manifest verifier.
#
# Run `scripts/release_sorafs_cli.sh --help` for the fail-closed interface.
