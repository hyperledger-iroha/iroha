# Kotodama V1 requalification record — 2026-09-08

This dated record preserves scoped local evidence and superseded qualification
prose. The [active ledger](../../../specs/kotodama_v1_redesign.md) owns current
acceptance. No historical pass, source comparison or prepared command establishes
release readiness.

## Current isolated checkpoint and limits

The current isolated source is
`adc1a627d5de8087d644087ab2c6a95430c3ec2f4ee1f3862b5ce1e8c98735a8`.
The QueuePlanSynced producer/SDK/global-ingress repair retains actual `3979c0d7…`
SDK 25, Torii 15, Python 231 executions/229 unique cases, pure JavaScript 18 and
C# manifest 21 results. The separate C# contract-call selection still has no
qualified validator assertions because its native prerequisite was unavailable.
The local miniature fixture correction passes the same 11 cases on `ff9a50a0…`.
That source also passes CLI 19, fresh offline project 12 commands/three tests and
all 61 golden outputs. The next harness compile failed before tests at a missing
`?` on a fallible builder. Its one-token fixture correction is applied; the
normal daemon and no-run harness builds pass on `adc1a627…`. Both subsequent
four-validator runs fail: companion detached verify never reaches Applied;
original submission rejects a future creation time. Both retained four-peer
cleanups exit zero, with exact source/index/lock/binary preservation. The
certificate/frontier census and held clock correction are recorded below.
Corrected Apple R7 attempt two remains active and workspace has not run.
Earlier records retain their named sources and failures; no release pass is inferred.
Compiler integration passes 78/78 on `2d270a80…`, including the three-case
exact-rejection-arity regression. Compiler/library 1,090, Koto/LSP 37 and
ABI/artifacts 73 passes retain their recorded affected-input continuity from
`f899fb47…`; these earlier selections were not rerun.
Core exits 101 with 135 passed, six failed and zero ignored out of 141 distinct
cases. All 107 original cases, both new corruption cases and the repaired
certified-body cases pass. Those overlapping groups are not additive.

| Remaining exact Core case | Attribution limit |
| --- | --- |
| `kura::tests::autonomous_first_attempt_uses_only_versioned_files_and_repairs_missing_pointers` | Same historical panic stage; temporary-path-only normalization. |
| `kura::tests::autonomous_payload_slot_is_bound_to_the_active_incarnation_marker` | The fixture changes the marker but leaves old input in the occupied slot; the strict reader rejects its inactive descriptor before replacement/publication preflight. Source-order attribution; no current HEAD baseline run. |
| `kura::tests::autonomous_startup_rejects_an_unretired_same_height_orphan_successor` | Same historical panic stage; temporary-path-only normalization. |
| `kura::tests::autonomous_view_recovery_preflights_named_and_atomic_temp_peak` | Same historical panic stage; temporary-path-only normalization. |
| `kura::tests::autonomous_view_writer_preflights_even_with_named_temp` | Same historical panic stage; temporary-path-only normalization. |
| `kura::tests::durable_autonomous_merge_source_requires_every_exact_component_and_survives_restart` | Same historical panic stage; temporary-path-only normalization. |

The five matching failures were actually executed on retained older-source
baseline binaries. Only temporary path names were normalized. This does not
execute the current HEAD or waive any of the six workspace/release failures.
The payload-slot fixture installs a new marker without replacing the old input
pair. `write_lane_block_execution_input_artifact` observes that occupied slot
through the strict reader, which rejects the stored old descriptor against the
new marker before incoming-input replacement or publication capacity preflight.
All nine relevant source files match `f899fb47…`; the strict observation helper
is absent from the compared `ff22bda4…` HEAD and retained older baseline.
This explains the changed failure stage by source order, without a new runtime
slot census or a current HEAD execution. C9 must identify and fix redesign
regressions while recording unrelated baseline blockers, not require a broader
Kura/formal redesign merely because these selections expose existing failures.

The completed ABI/VM/public-boundary queue passes 450/450 execution rows on
`9f7f713b…`: ABI values 172, VM values/scan/test driver 57, runtime/lists 50,
numeric/pointers 40, data-model contracts 58, Core contracts 61, Core header 1,
manifest metadata 6 and Torii state routes 5. All nine selections have zero
failures or ignored cases; all 18 source/index snapshots and actual raw-index
byte copies match. This closes that queue, not the separate Core 141 or the
remaining native/network/workspace gates. JavaScript 18 and C# 21 pass with affected
inputs unchanged from `f899fb47…`. Kotlin/Java runs all 17 cases: 15 pass and the
two `HttpClientTransportTest` contract-boundary cases fail while validating the
canonical account address, before the mocked request/payload assertions. The
current ABI-23 Rust address validator is unavailable; a fresh compatible JNI
artifact is required. The three pure Python wheels and freshly built normal-release
`iroha_native` wheel are installed through the ordinary package path. The native
wheel passes the official ABI/export probe; all 220 selected installed-consumer
cases pass on `122942d3…`, with zero failures or skips. Source, actual raw-index
bytes, HEAD, staged records, lock and dependency-cache checks pass. This is local
behavior qualification without a release seal; the existing dirty-source release
gate remains intact. The installed extension hash is
`c281206872058f77bb7c26a0127309fd5a801b781ee98739d339d9eaffaef218`.

The documentation comparison preserves all 2,123 authoring inputs, 39,028 Node
dependency files, 5,997 outputs and both pinned tools of the successful 12 GiB
build/link/locale run. The only changed recorded sentinel is Iroha's Cargo.lock,
which is not a docs input. No docs rebuild occurred; the separate historical
6 GiB heap failure remains retained. Current format and two read-only source
asset checks pass. Fresh CLI scaffold/debug tests pass 19/19. The offline first
project completes 12 CLI commands and three exact tests with IP networking
denied, including overwrite rejection. Official twice-rendered goldens and the
eight-output exporter match all 69 payload files. The reviewed golden owner seal
now covers 61 outputs; the current-Rust fixture was officially regenerated with
2,613 source inputs. Only that seal and JSON changed during artifact promotion.
Canonical checks, Rust admission 15/15 and browser structural checks 2/2 pass.
This replaces the stale 2,516-input build provenance and 60-output inventory;
unchanged payload bytes alone were never treated as proof of current provenance.

The two test-call-site migrations reached main with exact raw-index/staged/HEAD/
lock and unrelated-source preservation during that window. That delivery
readback found only unrelated status/import differences between main and the
isolated candidate; it is not a continuing claim about main. Later bindings must
include the actual final generated artifacts, ledgers and source. Current
effects and Rust include seals were already stale before the test migrations.
Both exact retired-reader negative seal tests pass on `9f7f713b…`, with no
failure or skip; no seal was refreshed, and these two results do not waive the
stale global seals or establish a full formal-checker pass or diagnostic total.

The no-run integration harness and normal-release daemon both compile on final
source `122942d3…`, with frozen Cargo-reported executables and preserved source,
actual raw-index bytes, staged rows, HEAD and lock. The prior 47 stale-client API
errors and subsequent nine dependency-name/type-inference errors are retained;
the final test migration uses immutable account preparation/signing and dedicated
read threads, with acceptance-only submission before partition healing.
Between `2d270a80…` and `e151fa7f…`, the sole source change was four module
qualifiers in that integration test, outside the compiler and official
2,613-input artifact closure.
The compiler integration and artifact receipts remain applicable to those inputs.
The updated acceptance matrix replaces the old integration-77 selection with
the actual integration-78 result. Its exact `2d270a80…` to `122942d3…` comparison
contains only `native_game_sessions.rs` and `sandbox.rs`, outside the selected
compiler and Rust SDK package inputs; no additional test execution is inferred.

The companion run `20260908T044213Z-network-companion-eb99bf96ca` closed with
native exit 101, zero passes and one failure after three startup attempts. Each
attempt used four validators; all 12 validator processes exited zero. Source,
lock, harness, daemon and observer ownership checks remain preserved. The final
error chain reports “blocking Iroha SDK call rejected inside a Tokio multi-thread
runtime.” The authoritative startup-height poll had been dispatched through
`tokio::task::spawn_blocking`, whose workers retain the runtime handle rejected
by synchronous SDK reads. No public call or restart stage executed.

The reviewed patch changes only `integration_tests/src/sandbox.rs`: the same
status poll runs through `iroha_test_network::read_on_dedicated_thread`, retaining
minimum height 1, error propagation and retry limits. Six focused cases pass on
`122942d3…`: dedicated-thread runtime exclusion/error/panic propagation, the
startup-height predicate, and four retry classifiers. The current harness and
normal-release daemon are successful builds, not network execution. The repair
is outside compiler and official artifact inputs, preserving those scoped results.
Both subsequent normal-release scenarios actually ran on `122942d3…`:

| Scenario | Actual outcome |
| --- | --- |
| Genesis-registered companion, `20260908T053221Z-network-companion-6641ebe4f1` | Deployment and the exact four-peer certified RBC join complete by source flow. The first `hajimari` prepare POST times out at `http_default.rs:374`, before response or call submission. Native exit 101: zero pass, one fail, zero ignored; 156.19 seconds. All four validators exit naturally with zero status. |
| Original upload/register/deploy, `20260908T054140Z-network-original-68f41ce576` | Upload/finalize, registration, deployment and the exact four-peer RBC join return before the same first `hajimari` prepare POST timeout. Native exit 101: zero pass, one fail, zero ignored; 288.56 seconds. All four validators exit naturally with zero status. |

Both runs preserve source, lock, frozen binaries and observer/ownership checks.
No response, call submission, public invocation result, later-progress or restart
success is established. The bounded retained logs do not identify an exact
server receipt or handler stage; the timeout alone is not an HTTP/auth rejection.

The separate public SDK real-socket reproduction exposes a concrete transport
lifetime defect. A blocking facade receives a keepalive 503 response; borrowing
the same asynchronous client on an external runtime then times out after two
seconds without a second server request. A fresh client responds in one
millisecond, and resuming the blocking facade responds in two milliseconds.
The current-thread owner only drives pooled I/O while inside `block_on`.
This controlled result proves the SDK defect, without supplying missing live
request/server traces for either network failure.

The approved production patch changes only `crates/iroha/src/blocking.rs` and the
SDK manifest's Tokio runtime feature. A multi-thread runtime with one worker keeps
owned I/O driven between facade calls; shared ownership, blocking-entry runtime
rejection and shutdown rules remain. The main application records source
`9189d58dd573990dd047ab946c5d93a341752088ffb40bc28fdfef9152cf721b`,
with exact two-path changes and preserved raw index, staged rows, HEAD and lock.
This source identity belongs to that application window, not ongoing main equality.
The following checkpoint records actual isolated SDK results and the subsequent
companion failure. The older live prepare timeouts above remain unchanged evidence.

The bounded input audit separates whole-source captures from compile closures.
Retained Cargo records include SDK in the CLI, harness, SDK 25 and Torii 5; the
JavaScript native host also directly depends on SDK. Those inputs need current
qualification after the fix. Compiler/Koto/ABI selections, the other 445 focused
rows and Core 141 exclude the changed SDK paths, preserving their actual earlier
source scopes and all six Core failures. The current-Rust 2,613-input closure and
exporter exclude them; the output seal remains a separate inventory of 61 outputs.
Fresh CLI and the official twice-rendered check are required, without an automatic
artifact write. The normal daemon and Python/Swift/Kotlin bridge build closures
also exclude SDK. Python's completed 2,109-entry native compiler-input inventory
contains no SDK path. The completed continuity audit compares all 2,108 present
compiler inputs, one intentionally absent path, 135 consumer inputs and exact
installed/frozen extension bytes through `b9164999…`; all match. The original
220 pass remains on `122942d3…`, with explicit continuity rather than relabeling. Existing whole-source owner guards still require explicit
rebinding or documented continuity. Remaining native behavior, the clean-source
release gate, both corrected network gates and final workspace remain open.

## SDK runtime and canonical response checkpoint

On `b9164999…`, normal `cargo check -p iroha --lib --locked --offline` exits zero
and emits production SDK metadata with Tokio's `rt-multi-thread` feature. The
inherited capture helper nevertheless records `qualified=false` because it
requires an executable from this metadata-only command. That original result
stays unchanged; a separate attribution verifies Cargo success, the normal
library artifact and all source/index/lock checks. This check executes no tests.
The six blocking-runtime tests and 25 contract/authentication tests then pass,
zero failed or ignored: 31 actual cases in two distinct selections.

The repeated public SDK socket probe uses the freshly emitted SDK library and
passes. After the first deliberate keepalive 503 response, the borrowed client
on an external runtime sends request two on the same connection and receives
the deliberate 503 response in the recorded zero-millisecond bucket. Fresh and
resumed-facade controls also respond. This is real loopback transport progress,
not Torii contract execution or a four-validator acceptance result.

Fresh daemon and no-run harness captures on `b9164999…` feed companion
`20260908T062242Z-network-companion-e42de202f7`. It closes native 101 with zero
passes, one failure and zero ignored cases after 85.76 seconds. All four owned
validators exit naturally with status zero; source, frozen binaries, lock and
observer guards pass. Deployment and the certified four-peer RBC join return
before the first `hajimari` request by the selected source control flow. This time
the SDK receives HTTP 200 and parses a JSON object, then reports exactly:
“contract call prepare response omitted required root field `transaction_ttl_ms`”.
No response body is captured. Completed draft validation, signing, call submission,
public invocation, later-progress and restart results remain unestablished.

On `b9164999…`, Torii's `ContractCallResponseDto` skips absent `transaction_ttl_ms` and
`pipeline_status`; `OperationReceiptDto` skips ten nullable fields. The Rust
prepare validator requires all 15 root keys and all 15 receipt keys, with explicit
nullable presence. The reviewed R2 repair removes only these 12 JSON omission
attributes, preserves binary layout, adds focused producer/codec regressions,
and updates `specs/torii/contract_lifecycle_app_api.md` plus
`specs/torii_contracts_api.md`. The strict Rust gate, authorization and invocation
assertions remain unchanged. Maintained managed parsers accept canonical explicit
nulls; their permissive missing-key handling is not omission-rejection coverage.
No fallback or duplicate API is introduced.

The exact three-path application records main source
`fe39108ae98089f18c2ff0a0bdef220bdef4ccb1ea2041744f17f31728df1a16`
and isolated source
`93e5aeb40d5bb9fd170ca350233931f41e1499768c3596dabf15c12f2fbdbe18`.
These are application-window bindings, not equality between checkouts. Two
capture-only unchanged-status criteria fail on expected owned clean-to-modified
and staged-modified-to-additionally-modified transitions. Both original results
are retained; neither patch is reapplied. The final status attribution verifies
unchanged staged columns and unrelated rows, exact postimages/modes, raw-index
bytes, HEAD and lock. The focused producer/state results below are actual tests;
no daemon, harness or network pass is inferred from application or static review.

The first focused producer capture,
`20260908T065103Z-torii-contract-response-96c3b8a16b`, exits Cargo 101 before any
test execution or executable is produced. Its sole E0433 diagnostic is the new
test fixture's unavailable `dm::Executable` namespace. The exact one-occurrence
fully qualified enum-path correction changes no production behavior or assertion.
Both applications preserve source scope, modes, actual index bytes, staged rows,
status, HEAD and lock. Their resulting sources are main
`27a58b35ed66e01d9dc28514e7bdffcd97ce3cd61cb28af3cefe2e56ae9b6e2f`
and isolated
`c0c93d26bc10a02253af57c3a9d184386ceddcacb68abb43c7524692b978a7fe`.
The original compile failure remains immutable. This is a compilation failure
with zero tests, not three failed tests; the actual rerun is recorded next.

The corrected producer capture `20260908T065614Z-torii-contract-response-b26dc07c2e`
passes all three selected cases. They exercise required null fields in prepared
responses, present submitted status with null unsigned-draft fields, and the
closed nullable receipt shape. Existing state capture
`20260908T070237Z-torii-contract-state-51edde73e6` passes all five selected nominal
identity/product/option/cursor cases. Both run on `c0c93d26…` with normal thread
stacks, exact test membership, zero failed or ignored cases, and the same frozen
Torii executable SHA-256 `a698559a65d81d37116975be6dad4f4d8efc55fd1e87c6f5c301cf95e8eafe2b`.
All recorded source, HEAD, staged rows, status, Cargo.lock, frozen-artifact and
actual raw-index-byte preservation checks pass. These are eight actual test
executions; the five state cases repeat coverage from the earlier 450-row queue,
so they are not eight new distinct acceptance cases.

The `c0c93d26…` queue subsequently closes: Torii's eight tests pass and both the
normal-release daemon and no-run harness build successfully. All eight source
and actual raw-index snapshots, staged rows, HEAD, status, lock and frozen
artifacts match. These builds execute no network test. Their full-source records
remain on `c0c93d26…` after the later SDK change.

The three response/spec paths are absent from Python's actual 2,109-entry native
compiler inventory. Retained conservative normal/build graphs exclude Torii for
the compiler, CLI, Python native package, Apple/JVM bridge and JavaScript host;
the daemon includes it. The two specs are also outside those native source roots.
No current-Rust/golden regeneration follows from this delta alone. JavaScript's
official owner still binds the whole tracked/untracked Git source, so fresh
binding and publication remain required despite native dependency exclusion.
Remaining native behavior, both four-validator gates and workspace qualification
stay open; prior failed captures and clean-source release prerequisites remain.

## Submitted SDK response and completed local consumers

The fresh `c0c93d26…` CLI capture builds the ordinary executable and passes all
10 scaffold and nine local-debug tests. The external project completes 12 CLI
commands and three exact tests with IP networking denied; overwrite rejection
preserves the nonempty destination and all eight authoring files. The official
golden check renders independently twice and matches all 61 owned outputs:
57 bytecode mappings and four compiler manifests. Its first attempt fails the
staged mode check under umask 0077. The normal-0022 retry passes with the original
mode guard intact; no generated source is promoted and no exporter or current-Rust
regeneration occurs. Source, actual index bytes, staged rows, HEAD, status and
lock remain unchanged throughout these completed captures.

The SDK's local submit path then exposes a separate canonical-shape mismatch:
after successful submission it removes `transaction_payload_b64` and
`signing_message_b64`, although both required root keys must remain explicit
nulls. The reviewed one-file patch changes only those two production mutations.
Strict draft validation, caller intent, account/key binding, signing and submitted
transaction bytes stay unchanged. The existing
`post_contract_call_authenticates_bound_account_and_rejects_foreign_authority`
test retains all assertions and adds exact 15-key root/receipt shapes, unchanged
unsigned response, signed hashes, queued status and null draft fields. The test
count stays 25. Static review is not a runtime pass.

The application binds main `f945bb42…` and isolated `b97acbcb…`. The first SDK
capture `20260908T072857Z-sdk-contract-auth-ce6432e604` exits Cargo 101 with E0382
because its expected-signature construction consumes the builder later used by
the new unsigned equality assertion. No tests or executable are produced. A
single `.clone()` before that test's `try_sign` preserves the builder and every
assertion; production code is unchanged. The guarded correction binds main
`99c5007b443fa5bf734b76b941761c2e85d6511adfd904364030dea75b225d27`
and isolated `4759aae1…`, with actual index bytes, HEAD, staged rows, status, lock
and all unrelated source preserved. The original failure remains immutable.

The corrected SDK capture `20260908T073547Z-sdk-contract-auth-1311dd90c2` passes
all 25 exact contract/authentication cases with zero failed or ignored cases.
The strengthened test passes; `post_contract_call_accepts_only_the_caller_trusted_draft_intent`
is a separate unchanged passing case. Its capture and actual index-byte guards
all pass. The warm normal daemon also builds on `4759aae1…`, retaining executable
SHA-256 `9c689c0850e9a21fe67b61dfdfdcfa3aea36583905c85d5312807e2daf9577e0`.
It executes no tests. The subsequent Torii and harness captures close as recorded
below; fresh CLI results remain pending. No active command supplies a pass or
network result.

The first current runtime-six attempt,
`20260908T073800Z-sdk-blocking-runtime-088370df41`, exits native 101 with five
passes, one failure and zero ignored tests. The keepalive case fails at
`TcpListener::bind` in `blocking.rs:827` with PermissionDenied under the inherited
all-IP-denying launcher, before HTTP. This is a launcher mismatch, not an
observed runtime-code assertion failure. The audit's single-line parser missed
the panic-interrupted outcome; a retained supplement binds all six starts, five
pass rows and the native failure. All source and actual index-byte checks pass.
Two proposed Seatbelt controls also fail before any additional runtime test:
the first rejects a numeric host at profile parsing; the second permits wildcard
bind and therefore fails its final assertion. Neither failed policy is treated
as an isolation pass.

The approved correction reuses the prior `b9164999…` direct launcher for exactly
these six tests. Capture `20260908T075127Z-sdk-blocking-runtime-c9879e5a71` passes
all six with zero failed or ignored cases on `4759aae1…`, using the same SDK
executable as the 25-case capture. Exact names, source, actual index bytes,
staged rows, HEAD, status, lock and frozen-artifact checks pass. This establishes
31 actual SDK passes in two selections; it makes no external-network-denial
claim and does not erase the earlier 5/1 result or two failed profile controls.
The real keepalive test reaches its HTTP behavior on the corrected launch;
these local SDK cases remain separate from four-validator qualification.

The same `4759aae1…` queue then passes the three producer and five existing
Torii state-route cases, zero failed or ignored. Both use frozen executable
SHA-256 `a698559a65d81d37116975be6dad4f4d8efc55fd1e87c6f5c301cf95e8eafe2b`,
unchanged from the earlier Torii selection. The final no-run integration harness
build also exits zero and freezes executable SHA-256
`b47ceb9fe039ac93a60195d0302b379fe48ce8fabf3a9aa2211f2fa076ed60cc`.
Together the closed queue contains 39 passing test executions: SDK 25, runtime
six, producer three and existing state five. The daemon and harness are build-only;
no network test is executed. All 12 source snapshots and actual raw-index copies,
HEAD, staged rows, status, lock and frozen artifacts remain preserved. The
consolidated receipt retains the initial compile, native and profile failures.

Current `cargo fmt --all --check` and `scripts/check_no_legacy_codec.sh` also exit
zero on `4759aae1…`, with all nine source/index/HEAD/stage/status/lock guards true.
CLI qualification has been released but is not yet a completed current result.
Both four-validator scenarios, remaining native consumers and final workspace
qualification remain pending; no network dry-run is counted as acceptance.

The bounded input supplement checks all 2,109 recorded Python compiler entries
(2,108 present and one intentionally absent), 135 consumer inputs, 63 manifest
controls and installed extension/wheel bytes; all match through `b97acbcb…`.
All 2,613 records in the retained official current-Rust generation attestation
also match, and `client.rs` is absent from both inventories. The later clone fix
changes only that same excluded test source. The original Python 220 execution
stays bound to `122942d3…`; this is input continuity, not a rerun or release seal.
The retained normal/build graphs exclude SDK from compiler/Koto, daemon and the
Python/Apple/JVM bridge. CLI and JavaScript native include it and need fresh
builds; JavaScript also requires fresh whole-Git provenance. The held official
native plan and four qualified cache copies are unchanged. Remaining native,
both four-validator scenarios, final workspace and clean-source release gates
remain open.

## QueuePlanSynced contract-call checkpoint

The latest four-validator companion on `4759aae1…` exits native 101 with
zero passed, one failed and zero ignored in 85.65 seconds; all four validators
exit zero. The client receives HTTP 409 with
`queue_plan_admission_intent_mismatch`: public transaction submission requires
signature-bound QueuePlanSynced admission. Source flow establishes that
deployment Applied, the full four-peer RBC join, strict draft validation and
local signing returned before that submission response. No accepted contract
call, `verify`, state readback or restart is established. The retained stage
audit does not invent a raw draft/certificate capture or global absence proof.

The reviewed 27-path correction is applied to MAIN `9f827a87…` and isolated
`3979c0d7…`. All 16 application checks preserve exact postimages/modes, actual
index bytes, staged entries, HEAD and lock. Torii prepares QueuePlanSynced intent
before quotation/hash/signature. Rust, JavaScript, Python, Kotlin, Swift and C#
contract-call validators enforce that exact intent while unrelated policies
retain their previous values. Python's local native builder already selects
QueuePlanSynced and is unchanged. No fallback, alias or wire layout is added.

Unsigned preparation no longer submits a transaction. A detached signed request
forwards its exact verified transaction, original headers and caller Accept to
the existing canonical global ingress owner. Only accepted ingress produces the
normal submitted response; rejected and ambiguous statuses, headers and bodies
remain intact. The companion `verify` fixture now prepares, validates and signs
the same payload locally before one authenticated detached contract-call POST.
Other selected calls retain the SDK local-sign path. The mini-integration fixture
also uses the current local-sign API. These changes are implementation facts;
the four-validator fixture has not run at this checkpoint. The miniature
selection compiles and executes 11 cases, with one pass, ten failures and zero
ignored in 20.24 seconds. All ten fail at `iroha_core/src/block.rs:15517` because
the unchecked synthetic block lacks the exact pre-staged sidecar required for
a QueuePlanSynced external entrypoint. The production gate correctly requires
autonomous lane ownership and a certified merge carrier. Source and actual
index bytes remain preserved. The later fixture correction removes the shared synthetic block helper and
keeps direct local execution inside this integration test. Its exact same
11-case selection passes on `ff9a50a0…`; no admission downgrade or Core-gate
relaxation is introduced.

On `3979c0d7…`, the Rust SDK contract/authentication selection passes 25/25 and
the exact Torii selection passes 15/15, zero failed or ignored. Torii includes
the eight retained response/state controls, three selector controls, three
canonical-ingress/response controls and one admission-domain source control.
Both captures preserve source, HEAD, staged rows, status, lock and actual index
bytes. The six blocking-runtime passes remain on `4759aae1…`; they are not
additional current executions. Current formatting and no-legacy-codec checks
both exit zero, with all seven source/index checks true.

The updated Python remote-client wheel is built from current source and installed
normally in a fresh validation venv. Its wheel SHA-256 is
`5c7c90bc33842a389328b5c9f6c9737bd087bd24afb60a8977b0b86ec7d6f6ad`;
the installed client matches current source SHA-256
`09f66ef3ed7472ea138964ad9eb4bb28f8abd599c6faeb0dad70404cc3fe4c36`.
All 2,109 retained native compiler-input records still match; the original
`122942d3…` ABI-23 extension loads through the ordinary installed package, with
the official export probe and runtime smoke retained from this setup. There is
no native rebuild, retired-module fallback or clean-source release-seal claim.

The first Python attempt stops before collection because the runner preloads
an installed module that the maintained conftest must authenticate itself.
The corrected import order collects 222 cases, then stops before execution
because pytest's automatic root abbreviates the two remote-client nodeids.
Both failures and all preservation guards remain retained. The final runner
pins the repository root and records a bijective path-prefix mapping for the
original 220 nodeids, without changing tests or weakening assertions.
It passes exactly 222/222: the old 220 plus two rehashed ordinary/unknown
admission negatives. The separate focused selection passes nine cases: five
positive payload variants, two existing substitutions and those same two
admission negatives. Thus 231 executions cover 229 unique cases, with zero
failures or skips. All 12 preservation guards pass and IP networking is denied.
The old installed environment, wheels, native bytes and dependency caches remain
unchanged; native-loaded is true and retired-native-loaded is false.

The current managed lane passes pure JavaScript 18/18 and freshly restores,
builds and passes the original C# manifest selection 21/21. The contract-call
wildcard separately produces 25 failed rows, all reaching the unavailable
ABI-23 AccountAddressNative prerequisite in class initialization before the
changed-validator assertions can qualify. One additional fatal xUnit metadata
error leaves the malformed MemberData theory without a row; the assembly reports
26 failures. Only 15 of the 16 required methods emit rows. This is not a complete
contract-call selection or a protocol assertion pass. Both managed phases
preserve all 16 source/index/HEAD/stage/status/lock checks, tools and caches.
The held native sequence must supply Apple five-target artifacts before the
complete C# contract selection (42 planned cases across all 16 methods), Kotlin 17, Swift 24 and JavaScript native 31.

The stronger six-failure Core crosswalk checks all 18,661 committed `ff22bda4…`
tree entries against the retained earlier source capture: regular-file bytes and
symlink targets match, with four 0600/0644 data-file mode differences and two
already-absent capture paths explicitly recorded. It also checks all 51 emitted
local packages and the historical `f744c583…` lock. The actual historical 139
selection remains 131 passed/eight failed, including every current failed case.
Five panic stages match after temporary-directory-only normalization; the
payload-slot fixture instead reaches an earlier current strict-reader failure.
The historical restart failure is not relabeled. This establishes failure lineage
before the current worktree changes through committed-source bytes. It is not a
fresh execution at HEAD, a pre-V1/pre-redesign baseline, or a passing suite.
All six current Core failures and the stale global formal seals remain unwaived.

The newer `ff9a50a0…` CLI recapture passes build, scaffold 10/debug nine on one
test executable, 12 fresh offline project commands and three exact tests.
The golden checker renders twice and matches all 57 bytecodes/four manifests.
All 14 aggregate source/index/artifact checks pass. The `4759aae1…` receipts
remain historical rather than being relabeled. Both corrected four-validator
scenarios, remaining native qualification, workspace validation and the separate
clean-Git release seal remain open. Workspace has not been attempted; prepared
commands and native cache copies supply no pass.

## Fixture repair and current executable checkpoint

The miniature repair changes only Torii's shared test utility and its local
contract integration test. The formerly failing selection is rerun unchanged:
11 passed, zero failed and zero ignored on `ff9a50a0…`. All source, index and
artifact checks pass. The original `3979c0d7…` result remains one pass/ten failures
at the required certified-carrier boundary, not a weakened production check.

The guarded `3979c0d7…` → `ff9a50a0…` application changes exactly two paths.
They are absent from Python's 2,109 native compiler inputs, both 135-file consumer
inventories, JavaScript's actual consumer inputs and the current-Rust generator's
2,613 inputs. The SDK 25 Cargo log emits 33 local packages, including the distinct
`iroha_torii_shared` crate, and excludes the changed `iroha_torii` crate. The C#
managed sources/resources are unchanged. These are input-continuity conclusions;
all older tests retain their actual execution sources. Torii 15 stays explicitly
at `3979c0d7…` because its crate changed, with no fresh Torii 15 rerun claim.

The next no-run integration harness exits Cargo 101 on `ff9a50a0…` before any
executable or tests are produced. `TransactionBuilder::from_payload` is fallible,
and the detached fixture omitted `?` before `encode_payload`. The exact
one-token correction changes only `integration_tests/tests/contracts.rs`, with
all 16 MAIN/PRIVATE application checks true, giving isolated `adc1a627…` and
MAIN `54a3e29f…`. The unchanged payload, signature and response assertions remain.
This final integration-only delta is outside the same retained consumer and
generator inputs; the `ff9a50a0…` CLI and miniature passes are not relabeled as
new executions. The later `adc1a627…` harness rebuild passes as recorded below.

The normal-release daemon recapture on `adc1a627…` exits zero. Cargo reports the
artifact fresh; its frozen SHA-256 is
`8e6401e0c421e18723f14aadede12f0f5f9f3481b42ddef60ca6c1fca41c2a9a`,
342,217,840 bytes and mode 0700. Source, copied index bytes, HEAD, staged rows,
status and lock remain unchanged. This is build-only evidence, not a network pass.

Apple R6's first attempt fails before Cargo: the official owner requires its
external output/build directories to exist. The failure and all preservation
records remain intact. R7 creates those exact empty owned directories and uses
fresh attempt-two evidence without cloning caches again. Its source/cache
binding passes on `adc1a627…`; the first target later builds, followed by the
strict packaging failure recorded below. No native consumer qualifies. The official owner deliberately
removes staging intermediates before publication, so post-build evidence will
read the three durable XCFramework libraries and canonical manifest, plus the
retained five-target C-link and unique host-runtime success logs. Four cross
targets are link-only. The approved C# host-record join remains strict.

The ordinary workspace owner still has its reviewed SHA-256
`9b8486e59d180b376db821d9d87640044df1d336b8f973db5d335c77256e470f`.
It accepts an explicit current source digest and invokes normal `cargo test
--workspace --locked --offline` with one build job, normal stacks, no selected
filters or feature/profile overrides, and child umask 0022. Evidence uses 0077;
actual source/index copies and Cargo outputs are checked without duplicate binary
copies. No timeout, signal or fail-fast override is added. Static parameter review
is complete, but the command has not run and may stop at a failing suite. The six
Core failures, stale formal seals and clean-Git release prerequisite are unwaived.

## adc1 four-validator outcomes and closed-store diagnosis

The current no-run harness succeeds, freezing `core_api` SHA-256
`e6ca4595fec028cccf0375c9c3c68d8a94a2d81a933cf77f8640a9169c5030a4`.
Both scenarios use it with the current normal-release daemon `8e6401e0…`.
The companion exits 101: zero passed, one failed, zero ignored in 207.46 seconds.
Hajimari reaches Applied by exact control flow; detached verify returns the
expected response after canonical certified ingress, then its Applied wait ends
with HTTP 404. Verify is `3532618e…`; no final readback/RBC/restart pass follows.
The original exits 101: zero passed, one failed, zero ignored in 219.81 seconds.
Its first local-sign call receives HTTP 400 `transaction_rejected`, specifically
`Transaction creation time is in the future`; no call-success marker is emitted.
Each scenario's four validators exit zero with empty stderr. All ten supplemental
preservation checks pass, including exact copied raw-index bytes; no prior
stat-only attribution is needed for these runs. Earlier failures remain retained.

Omitted status scope is global. Global reads intentionally suppress pending
queue/cache entries, so the observed 404 is not an admission-rejection or
certificate-absence proof. The earlier local/default-scope interpretation is
explicitly superseded by the source trace. The strict public decoder validates
the same verify certificate in all four closed stores against exact genesis,
network, signatures and ordered roster: certificate `30a3bfbb…`, authority 7,
proposal 8, lane 0/dataspace 0, durability 2 of 4. This is admission availability,
not the consensus Prepare/Commit threshold of 3 of 4. The first diagnostic
JSON-macro compile failure is retained; corrected compilation and decoding pass.

Read-only BlockStore index/body decoding finds identical canonical chains 1–8
in all four stores. Height 8, `bad5b6fe…`, contains that exact certificate but no
external entrypoints and no merge reference. Its header time 1788862518216 ms
precedes certificate enqueue time 1788862600058 ms by 81,842 ms. These are exact
decoded values, not a causal inference. No Kura replay, private commit marker,
finality sidecar or WSV is opened; journal presence alone does not prove committed
execution. All 25 closed inputs and source/index/library guards remain unchanged.

The original rejection has a source-proven clock-domain hazard: contract
preparation defaults to raw SystemTime while admission uses Core network time.
Its exact failed payload timestamp and numeric offset are absent from retained
logs, so the actual skew is not reconstructed. Reviewed candidate `4ba7ce14…`
changes only the omitted contract timestamp to the existing Core clock helper,
with explicit timestamp/signature preservation and strict envelope regressions.
At this diagnostic checkpoint it was unapplied and uncompiled. The subsequent
clock/gas application and Apple terminal failure are recorded below. No drift,
TTL, NTS policy or other API clock is weakened. Workspace has not run; both network
acceptance gates, six Core failures, formal seals and release prerequisites remain open.

## Applied clock and signed genesis gas corrections

The strict four-genesis census uses only public canonical block decoding and
typed instruction inspection. All four identical signed genesis files contain
10 instruction transactions, 104 instructions and seven SetParameter operations.
Their five custom parameter IDs omit `ivm_gas_limit_per_block`, and the default
custom map is empty. Core therefore starts with its 4,000,000 block-gas fallback.
All 12 source/index/library/input preservation checks pass. No mutable Kura,
private queue codec or WSV reconstruction is used.

The frozen fixture requests 5,000,000 for verify and 10,000 for hajimari. Its
validated draft and detached signature preserve the signed fee gas bound.
ContractCall proposal accounting reserves that entire bound, independently of
later VM usage. Each autonomous route share is at most the block budget, and
selection stops before a transaction whose accumulated gas exceeds its share.
Thus verify cannot be selected under the old 4,000,000 budget even by itself.
Combined with the exact certificate in all four indexed height-eight bodies,
this identifies a concrete barrier between registration and execution. Earlier
scheduler gates were not reconstructed, and exclusive clock causation is not
claimed. An admission certificate does not certify eventual schedulability.

The reviewed three-path application preserves all 16 MAIN/isolated source,
index, stage, HEAD and lock checks. It changes isolated source to
`bfddb2ff579b214c27bc0b9cf7bdbf8279cc479feaf26f18025285d64aaa7e14`
and MAIN to `b155f8d5347c841d339d16eb57018640cb56b49a34f39f9b926fb967ac704af3`.
Contract preparation now uses Core network time only for an omitted creation
timestamp. Explicit timestamps and detached signatures stay exact. The new
regression exercises the real envelope API at the future-drift boundary and TTL
expiry, without changing NTS policy. The original failed payload's timestamp and
actual offset remain unavailable; its numerical skew is not reconstructed.

The shared four-validator fixture sets the existing signed genesis custom gas
parameter to its unchanged 5,000,000 verify bound. Both scenarios require exactly
one full matching parameter and reject any uninspectable genesis executable.
A pure parameter/codec test is added. The 200-entry workload, four validators,
mandatory availability, deadlines, signatures and downstream assertions remain.
Neither Core defaults nor selection/admission gates are weakened. These changes
are applied but their fresh Torii, fixture, daemon/harness and network validation
remain pending at this checkpoint. Prior executions retain their actual sources.
The independent review's first receipt-only preimage assertion overlapped the
parent's authorized application; the exact postimages then matched on both roots.
That capture failure is retained and is not reported as a source-preservation pass.

Apple R7 attempt two closes with native owner exit one after 54 minutes 30.879
seconds. Its first aarch64-apple-darwin Cargo release build completes in 54 minutes
15 seconds, then the official PQClean normalizer rejects zero matching current
reference outputs. The sole copied build-script output still names the historical
cache. Both required current and historical archives have identical bytes, but
that does not satisfy the current reference provenance gate. There are zero C
links, zero host ABI/crypto executions and no later target or consumer execution.
All 11 source/index/prior-evidence/artifact checks pass; original failures remain.
This is a packaging/provenance failure, not a crypto test failure or native pass.
No checker bypass or cache-path rewriting is accepted. Apple five targets and
C# 42 planned cases, Kotlin 17, Swift 24 and JavaScript native 31 remain pending.
Workspace has not been attempted; six Core failures, stale global formal seals
and the separate clean-Git release prerequisite remain unwaived.

## Current clock/gas validation and remaining frontier gates

The following executions use isolated source
`bfddb2ff579b214c27bc0b9cf7bdbf8279cc479feaf26f18025285d64aaa7e14`.
Torii passes all 16 selected cases, including the real admission clock/drift/TTL
regression; the strict signed genesis gas fixture separately passes 1/1. Zero
cases fail or are ignored. The no-run harness build passes and freezes
`c9d902d0eb1119f048e55d34e2f299e6eadb28a397396f18208dfac7785bd731`.
The normal-release daemon build passes in 1,120.726 seconds and freezes
`cd499736dff5b847c5669b08d7ecd00c222ad0773935c2aa21eb8553f262183e`.
The build-only results do not count as runtime tests. Source, actual index bytes,
HEAD, stage, status, lock and captured executable identities remain preserved.
All earlier executions retain their recorded source or explicit input scope.

The new companion is 0/1, native 101, in 204.16 seconds. Both call helpers return;
hajimari Applied is inferred from the control flow reaching detached verify.
Verify `e5967fbc6f6c5900e73facc95e90222566878d29eb69d35347b7cf3fd18f15dd`
then times out waiting for Applied, with global status HTTP 404. Two validators
exit one after explicit fatal SigningGuard errors: “execution candidate validation
cache crossed a committed frontier change” and “durable merge candidate no longer
revalidates against its exact global round”. Both enter restart-required at
`v2_lane_work.rs:11388`; the authoritative runner fails closed. The other two
validators exit zero. No test-owned restart or final readback is reached.

The new original is 0/1, native 101, in 321.57 seconds. Its SDK verify hash is
`891969424c6a3b1529ccebf5069dd9f3a78b6fd88bc263b89041d6009f53de13`.
Both call helpers return, then verify's Applied wait times out with HTTP 404.
All four validators exit zero with empty stderr; the full retained stdout scan
finds no companion fatal/restart-required marker. It advances beyond the old
future-time rejection, without proving the old unrecorded numerical clock skew.
Both failed networks preserve all ten source/index/lock/artifact audit checks.
The terminal test wait fails at `contracts.rs:2559`; neither acceptance gate passes.

Read-only public decoding verifies the exact signed 5,000,000 block-gas parameter
in both scenarios. The companion's verify certificate
`fd291d35b8a60039c4e349a6930e166a82db0d71c3a5c26c6a03e0667248b2c1`
and exact autonomous anchor occur in all four identical indexed chains 1–9.
The original's certificate
`97a17ea7da02c5afe7fd36f4b2298f3356e7d39abafe7ed3a58fa16179e7390d`
is at height 14, and its exact autonomous anchor is at height 15 in all four
identical chains. Certificate durability threshold 2 remains distinct from
consensus quorum 3 of 4. Public checks cover canonical frames, network identity,
certificate signatures and exact proposal/envelope/entrypoint hash bindings. The
private autonomous producer-signature validator and lifecycle authority are not
recreated. No private queue/Kura recovery or WSV reconstruction is performed.
Indexed bodies do not certify execution, committed finality or Applied.
Omitted status scope already means global and suppresses pending records.

The original continues after its early readiness warning. Relative to verify's
11:31:29.879 UTC marker, certificate-body validation occurs at 10.682–14.232
seconds and autonomous-anchor validation at 40.918–44.431 seconds. Height-16
body receipt is at 58.612 seconds, with validation at 59.703–60.171 seconds.
That body is observed in logs, not independently decoded from a persisted index
as an execution carrier. A nearby candidate-scan event is not bound to a height
or subject. Logical header timestamps advance by eight seconds and must not be
substituted for these wall-clock observations. No explicit per-contract compile
or VM execution duration, batch failure or timeout-view event is recorded.

The fixture's signed cadence is eight seconds; its configured round budget is
80 seconds and the existing `da_commit_quorum_timeout()` yields 104 seconds.
The fixed 60-second Applied wait undercuts that round budget. Reviewed patch
`959e821d38789ee184eaec9be3356f6f96b4e1ddfc87c4118d67c2c81b495601`
changes only the deployment and looped invocation wait arguments to the existing
helper, with one explanatory comment. Workload, four peers, rejection/expiry
fail-fast and all readback/RBC/restart checks remain. It is unapplied and has no
new network execution. The frontier runtime correction is also still in
preparation, unapplied and uncompiled. The original is not attributed to the
companion's fatal guard, and timing evidence does not prove eventual success.

After the retained R7 Apple packaging failure, five approved package-clean
commands all exit zero. The cleanup owner nevertheless remains qualified=false:
its exact prospective-removal guard fails. Read-only attribution accounts for
all 1,854 removals within the eight approved packages. Cargo's preview omitted
111 direct dependency files (37 stems times .d/.rlib/.rmeta); each has an exact
untouched historical counterpart. They were not among the 1,579 files actually
copied by this recovery. Source/index and survivor/artifact checks remain true;
the original failed guard is not rewritten or called a qualified cleanup.
R8 attempt three is prepared only. No retry, cache clone, new source binding,
Apple build or downstream native consumer has run. Planned Apple five-target,
C# 42, Kotlin 17, Swift 24 and JavaScript native 31 counts remain prerequisites,
not passes. Ordinary workspace has not run; six Core failures, stale global
formal seals and the separate clean-Git release requirement remain unwaived.

The current MAIN source remains
`b155f8d5347c841d339d16eb57018640cb56b49a34f39f9b926fb967ac704af3`.
Its actual raw index is
`318d2bc6d03f44c1226c1c4c7231051394ab4d238c95151d6f110f89e2201600`.
The fresh comparison against the supplied three-path application snapshot finds
zero source-path or staged-row changes and the same actual index bytes, HEAD,
status and lock. That supplied snapshot already records this index, so no new
index drift is inferred from the earlier mistaken hash reference. The isolated
source and actual index remain unchanged. No index restoration is performed.

## Retained receipt inventory

Paths below identify local retained evidence under `target/kotodama-v1-qualification`
in the isolated checkout, except the marked main receipts. They are historical
records, not repository build prerequisites. Digests authenticate the report
bytes; the reports retain exact commands, actual test names and preservation
checks. A preparation or continuity report is not an executed test.

| Scope | Receipt | SHA-256 |
| --- | --- | --- |
| compiler1090 | isolated: `compiler-lib-current-audit-4h7vzeoa/REPORT.json` | `d6afd24c843fa97bf54589f3d8ccb07ee5c6382a6d76241abaacd58fde0837a4` |
| integration77 | isolated: `compiler-integration-current-audit-ia66ap55/REPORT.json` | `6eea35f1003eab2ac79962ec0468d540aef12ee649226144e813f916b77dfa0d` |
| koto37 | isolated: `koto-tests-current-audit-qsjoffo2/REPORT.json` | `9983eb9886db3e813f6cc6dd137e402cede871520b4d508866206ad4c6efc039` |
| artifact73 | isolated: `ivm-abi-artifacts-current-audit-24_f745t/REPORT.json` | `e79923440e224a3e237f624c8cac13f7f3ae9c71d319906c801c5fc87ff788e5` |
| input continuity | isolated: `f899-to-9f7f-input-continuity-jzycz3kl/REPORT.json` | `1ce121b6bd71f6689a063c01776fb17ce40a7e454287e43f1d50651bc6fda43d` |
| sdk25 | isolated: `20260908T021709Z-sdk-contract-auth-7b66c6de57/run.json` | `25929baa238e386daf7488565cb42c0fdad9be9bc200ea53afbf576adf4ed186` |
| core141 | isolated: `current-core141-outcomes-h1mjmft0/REPORT.json` | `b7074cc1a136a10d90444fc83c46f194bfa8dfa41c2d79e8b8e6bb5d6dd0984c` |
| core failure comparison | isolated: `current-core141-outcomes-h1mjmft0/failure-comparison.json` | `13da08becdeab4d5b399eaa22577c3616e40043292c941532085eea8e52b858c` |
| abi172 | isolated: `abi-values-current-audit-3koj8k8r/REPORT.json` | `3b883fe9919a1c5fd9934b73d8c3678863aee6174e74c06765e884e7f2accfb7` |
| driver57 | isolated: `ivm-values-driver-current-audit-9hbhl8re/REPORT.json` | `44465954133cec7036571790e3caf7829858516837742313a5d5eb52b4c4c056` |
| runtime50 | isolated: `ivm-runtime-lists-current-audit-42kewuz7/REPORT.json` | `4951963c633736500d0f431ead051829403e2cbfc46f7ba3bc0fdd8f14979f6c` |
| numeric40 | isolated: `ivm-numeric-pointers-current-audit-y4z6yocw/REPORT.json` | `0a2818db4588e6fdeb2b59c243fed4817a07a54caa24b0e54ce795d46278c164` |
| js18 | isolated: `current-pure-managed-sdk-g7f9q_8p/REPORT.json` | `54184ce3b9a0f3ebd8fafb5991b74c59b1e9c50de5344ccb81e223f68ab65f93` |
| csharp21 | isolated: `current-csharp-contract-consumers-t4zehnbs/REPORT.json` | `40dcfafd8a661e600a5433154f4c608af7b258bfd48c80f51d0076bca978e987` |
| kotlin17 | isolated: `current-kotlin-offline-a8lpncnb/REPORT.json` | `3c77e46ee21bc69971705b69ef9c3dbd70d2cd3177b031151f4edd7312042cd1` |
| kotlin native prerequisite | isolated: `current-kotlin-native-prerequisite-tkyzoqk2/REPORT.json` | `216be277d8dded829fcd5d76b4a29e4dd274fa7b9dbc617457bf302e7aad0a42` |
| python wheels | isolated: `current-python-pure-wheels-ux9lfeyt/REPORT.json` | `db9e2a63afc253beccd57684de4e9ff939c7efef1ad0d59100d4ff075127b94f` |
| source asset checks | isolated: `current-source-assets-readonly-kp1vsfsz/REPORT.json` | `a8341f588a85434e5dd2a07dd09d29251a80b5f5ad195a8a985d19cd98d505db` |
| format | isolated: `current-format-check-sbibv6my/REPORT.json` | `208b9814e6bcbe3123cf972a0c241329211610ac3af001fcd23658ba199dc13d` |
| formal impact | isolated: `current-core-fixture-constructor-audit-iaat2qny/guard-impact.json` | `a74b5741c78bed3c422ac4d7bc418f3b7e1867bc07c31f5a850793cbeed188bc` |
| main delivery | main: `current-two-test-migrations-main-sbyld42d/application.json` | `44963817cc78c79de8a1129b1400778536ef60ff4afe5cc2d20235f124b487ef` |
| docs continuity | main: `current-docs-continuity-__6xyxxo/REPORT.json` | `a79549a417b2227a043f6ce8f54c321035fb56b579a15ba23619095f2f5e5d61` |
| retired-reader negative seals 2 | isolated: `current-retired-reader-seals-zptqqlc9/REPORT.json` | `fddb8407f2b7a65755ad74c7ccb74613978c945b2faf22c998a58336f669bb84` |
| model58 | isolated: `model-contracts-current-audit-19m_iyc5/REPORT.json` | `0fa9b5e60919efb998cd891075eef36e1372b3e71e1301ee38a9b9a6d99a7ed8` |
| core contracts61 | isolated: `core-contracts-current-audit-jhz5kgg_/REPORT.json` | `43aefc7a0b409887af8198eef49515b8d609ff5580a764a07703521f62df42a3` |
| core header1 | isolated: `core-header-current-audit-_ogqxg0d/REPORT.json` | `69b1a5595d1f792417e83799a7748b627a2896d64b83353854a987bab5b3c9b4` |
| completed queue450 | isolated: `current-focused-values-queue-eiopumf3/completed-queue.json` | `527f50a4abd03da3678bc7717d05a94d914767b965ff7278a7af875155da553d` |
| payload-slot source order | main: `current-payload-slot-stage-audit-n7qmq_3x/REPORT.json` | `915b1940ff07e71d9a1dafa3bba8893192ce23e2aebf9a0f88f11a1ba510b194` |
| final artifact/tooling | isolated: `current-artifact-onboarding-preparation-erb9u4h8/FINAL_ARTIFACT_RECEIPT.json` | `06da7123fa79928cc53e58d75828042d01c48fbcff29040874eef23284833766` |
| integration78 | isolated: `20260908T043502Z-compiler-integration-10196c223c/run.json` | `e3058b86d7a06f715338aa1deb403c4d0cb3f292442c8f328cfd0d3e6d4f1ec8` |
| final harness build | isolated: `20260908T043953Z-integration-harness-46fae05c36/run.json` | `bfd016302a813a12193b45cc8d3eb18ca0667aad99e680e4e0ab22fe51721636` |
| final daemon build | isolated: `20260908T044100Z-release-daemon-533e3ec7c9/run.json` | `3be6311cf915783a74b2b1578c5072c623872806070d3da1971782bca5f05bdc` |
| closed companion failure | isolated: `20260908T044213Z-network-companion-eb99bf96ca/run.json` | `91393b33f83cecf1fe2c90785823841b5d5ee9c20af9d85459b4ef5cdb9e91a7` |
| startup read application | main: `startup-status-dedicated-read-application-rrvt04t0/application.json` | `8c0cbe391aff8431cd8be04ca50a6bf7ce9f9ae469b0986ee6c89b1d25a64b91` |
| startup focused6 | isolated: `startup-status-checks-vkteopd9/completed.json` | `2f5154afe5014599c91af37c8b6c2f8f4a80fac2b940c6a33bf0d012fc8edd71` |
| repaired harness build | isolated: `20260908T050845Z-integration-harness-b6dc5011fd/run.json` | `83da068dffa6ca30c46185ef940f3532d82ed9e1e63b6457a950970cf7cbec03` |
| current daemon build | isolated: `20260908T045631Z-release-daemon-293bbbfec4/run.json` | `46c4645a88e19848f5c08b312ac040cdb58be8583d57dfef9f00955db08173c7` |
| acceptance matrix78 | isolated: `current-acceptance-matrix-78-nekqfydn/matrix.json` | `cff5f73da667daa2bc27b547f57da2d892e35c6d37b82eaa08f0f339ebc9cb09` |
| 2d270-to-122942 inputs | isolated: `current-acceptance-matrix-78-nekqfydn/input-continuity.json` | `08bb63082fdb67053e4331255f68cde6b125d340eca881bab6ff972fac1d38e2` |
| companion prepare timeout | isolated: `20260908T053221Z-network-companion-6641ebe4f1/run.json` | `611a92f0ce043129e8688ca189d85fa92aee7172d2fe0385e33484c50668cbb7` |
| original prepare timeout | isolated: `20260908T054140Z-network-original-68f41ce576/run.json` | `07cf0b92587cfbe2bc8c00d313dce6a73ac8f23c953de44bdb1005e7e6841bef` |
| companion request-stage limits | main: `companion-first-prepare-timeout-9_99_4ha/REPORT.json` | `d83e3789d26e5df3f5965ac9c5d4e8ee8b15fd2629f3da8bc4d4493b22eca89c` |
| original request-stage limits | main: `original-first-prepare-timeout-l34pbnsp/REPORT.json` | `8820e7fea7d160629ccbd58d65a00e4a614510564a5909713abd07472000bd05` |
| public SDK socket reproduction | isolated: `sdk-cross-runtime-probe-wzlrld29/REPORT.json` | `b3d477f6b82a286ee018cea1433eebbc674aeb7872f1e440a0e50a8cfb1ab7ad` |
| SDK runtime main application | main: `sdk-blocking-runtime-main-application-g1vzwmj1/application.json` | `e7de464aca9fa11de2814023920f311160aec0900694d963456282c7e0578daf` |
| SDK runtime input impact | main: `sdk-runtime-input-impact-lvfr3d8_/REPORT.json` | `552db0e2fafebaf9c89abe6e01c5f5c1623bce928c946eb0b7ed5dbd59d09942` |
| Python native installed220 | isolated: `python-native-local-xhh2w0xy/REPORT.json` | `30582f70978eff7867b2de5acf1877c1a0d5da8ae441f81358c29083b1a99b0b` |
| SDK normal check (helper false retained) | isolated: `20260908T061218Z-sdk-production-check-7d10438828/run.json` | `3d618171e47028d22b8c5947d730de3916371bf11b2f01b5e8dec727c9703f4a` |
| SDK normal check attribution | isolated: `20260908T061218Z-sdk-production-check-7d10438828/production-check-attribution.json` | `6dd2d6218a076388c2c5ed265580079d8cc735089251c9051679c77e0ebbd693` |
| SDK runtime six | isolated: `20260908T061542Z-sdk-blocking-runtime-da3f604db8/run.json` | `d52310c7cfa56d085c7400b9054f710f2fd82816f2c1f9f155770622f5d6cc1a` |
| SDK contract boundary 25 on b916 | isolated: `20260908T061716Z-sdk-contract-auth-53b9768e11/run.json` | `ba1f0ae72b6d465ebb59707a89e8c790547fcc4c12b7cfb74c6167e5b1a32019` |
| public SDK socket progress | isolated: `sdk-cross-runtime-green-probe-eezscxz6/REPORT.json` | `9d2a3a5529f9a99da903bdaceb8557647a1d4f85fcdf8f717b40fd94ee9593b8` |
| Python continuity to b916 | isolated: `python-native-sdk-delta-continuity-qjim6ctm/REPORT.json` | `067e2da948a2797cc3165118d29e7337e7d77a2b6f547a9c319ece3339190810` |
| b916 companion missing TTL | isolated: `20260908T062242Z-network-companion-e42de202f7/run.json` | `faac655ba693009a50c45ac61b78187fbb9af3efc600d103a1026087971f102d` |
| b916 companion response-stage attribution | isolated: `companion-response-schema-stage-ndjwrdxa/REPORT.json` | `994d7e31e79d20a0354c84674d30345927aa944c04297f8d43c508127eff5de6` |
| canonical-null consumer/input audit | isolated: `contract-call-null-consumer-impact-yy99r81b/REPORT.json` | `aee3d943f4bdf93f80500f88f08ac2e52ba27b99b63167a6414654b631024e54` |
| response producer application and exact status attribution | main: `contract-call-response-schema-application-zjj7qa24/application.completed.json` | `cc08322f94363afd686c29589630b30361f293a06993b62c45502d8e598a6de1` |
| producer first compile-only failure | isolated: `20260908T065103Z-torii-contract-response-96c3b8a16b/run.json` | `d7af847ba9d5dfb4209b5fb004e45bf47d60a91528fa7bc75c87c988dd9dde06` |
| producer fixture namespace correction | main: `contract-call-response-fixture-path-tu1pwob8/application.json` | `cc69c5475f7a152ddc2d43113defefc37425e84f3c6b6324319eb8ddcd827af0` |
| current Torii response/state eight | isolated: `contract-call-response-capture-sxngoh0j/torii-eight.completed.json` | `dc52c4a405c0a8efab60550633c986b2590e45bf109849d49b789e9c816b410c` |
| c0 CLI, project and goldens | isolated: `contract-response-cli-recapture-5manv5e9/REPORT.json` | `2a40ae477cfbc583f4e6ede1143657e3e7c92ee4e60aa569515ecbd76f889872` |
| c0 Torii and daemon/harness queue | isolated: `contract-call-response-capture-sxngoh0j/completed-queue.json` | `61e5ce75fdc92963a8180110e038319f352e2513358a582ed9b9d1b97d3a47cd` |
| SDK submitted-null patch | main: `sdk-submitted-response-shape-vmevnqpr/candidate.patch` | `13d331f837b1bb196010b7de2f4400f2013a398ebc46d521fed422b69b971d21` |
| SDK submitted-null application | main: `sdk-submitted-response-shape-vmevnqpr/application-t6zdj6u6/application.json` | `60c9006fb6901209e12c6667c13214319eda464f65cc26402435c781d1b8d2bd` |
| SDK submitted-null independent review | main: `sdk-submitted-response-shape-vmevnqpr/independent-review.json` | `9df4e67281cfa3a0732b469725c5ec14e7bb8047b473155d9771e16878bb3a4b` |
| SDK client input continuity | isolated: `sdk-submitted-shape-input-continuity-_exxwmw6/REPORT.json` | `b7198924b583b41d318c597d904bd898a0207b26811e1cb1974a40980a384eb1` |
| SDK first compile E0382, zero tests | isolated: `20260908T072857Z-sdk-contract-auth-ce6432e604/run.json` | `d2c400683b72945402834ad63a96085af58c45876a62872748d30e582f129fa4` |
| SDK fixture clone patch | main: `sdk-submitted-response-fixture-clone-t7xw8ahq/candidate.patch` | `7b71da6d28f97df3aefd097e7f3969900489b52fb824dfe607fb9b07da1603bd` |
| SDK fixture clone application | main: `sdk-submitted-response-fixture-clone-t7xw8ahq/application-ev9np3j9/application.json` | `dd88ae1087936593731a112c981515881e1305b6858fc2f4e1c49022db2fa70b` |
| current SDK contract/authentication 25 | isolated: `sdk-submitted-shape-capture-5z3efkwv/20260908T073547Z-sdk-contract-auth-1311dd90c2.audit.json` | `23a344baf5c22e73488dba6350f1c37aa44fb9bf6c80a2f16fb2b8545e7d46e9` |
| current warm daemon build only | isolated: `sdk-submitted-shape-capture-5z3efkwv/20260908T073547Z-release-daemon-bf74c13184.audit.json` | `34540f30e80ad6ba300d014ea6a52af453bacfbe11496a574061b424856026ed` |
| runtime six initial bind-denial result | isolated: `20260908T073800Z-sdk-blocking-runtime-088370df41/run.json` | `3a681803453d5dc56ba1b976d5734cbffbead5c33e207e63a6a58c8b7ee91a90` |
| runtime six exact failed-attempt supplement | isolated: `sdk-runtime-loopback-launcher-3g0is8zj/failed-attempt.supplement.json` | `c2c4ef30937b4a783b97a990ee35696eec2b43d88d8aca226ca845959e52ef52` |
| first failed Seatbelt profile control | isolated: `sdk-runtime-loopback-launcher-3g0is8zj/controls.json` | `643942a3736ce4576fc4b4a7926953a19c85fe5068f72f6cb84d6262c2dd13d8` |
| second failed Seatbelt profile control | isolated: `sdk-runtime-loopback-launcher-3g0is8zj/revision-2/controls.json` | `9eee8738870887e44a759ebf48d98d63bd2068b40935916fde0dfab4796237d5` |
| current runtime six, approved direct launcher | isolated: `sdk-submitted-shape-capture-5z3efkwv/20260908T075127Z-sdk-blocking-runtime-c9879e5a71.audit.json` | `7fd1d099fa8c251230c4b492aacf8207d8e8deba6e4115c9fd77fa85784d2756` |
| current SDK/Torii 39 and daemon/harness builds | isolated: `sdk-submitted-shape-capture-5z3efkwv/completed.after-fixture-clone.json` | `5e482947d1306a229efaef7dd13e709acf94536820e44ef3f1a6aeef1b7f22c5` |
| current formatting and no-legacy-codec checks | isolated: `final-format-codec-mrbe7tim/REPORT.json` | `7d01d2ef92abba9e0a4dffdd4785cde3b26f06b383f1199d151cedd4956507b5` |
| combined QueuePlanSynced application | main: `contract-queue-plan-combined-g2slmqkr/application-wl7lcny_/REPORT.json` | `b4ebf873c53b03dbb3fb0ba32b0497ed3f24eb130d42b74456fad98bbed90ec2` |
| 3979 formatting and codec checks | isolated: `contract-queue-format-codec-0eku9frz/REPORT.json` | `f550df8dd3007e622eff3172c20aa11cd9c3bb304c80457fdbc968b8d10fdbcf` |
| 3979 Rust SDK contract/authentication 25 | isolated: `20260908T085631Z-sdk-contract-auth-ada970497e/run.json` | `fca1797dbe1a5eb182b4ad9bba9872663d813dc3b97c874f345d8ec91d431c94` |
| 3979 Torii certified preparation 15 | isolated: `20260908T085719Z-torii-contract-certified-preparation-8960c4e72f/run.json` | `6e21a17b8e343f105f8443d339800dbc0dd6d6e644a03ee6ed3f3406cef491e7` |
| 3979 installed Python 222 plus focused nine | isolated: `python-contract-admission-installed-3agb824s/REPORT.json` | `1bd87891e9fa6291d9c99b7be9c4dff72811bcaef24542639d3a9a28a8792c83` |
| Python first collection import-order failure | isolated: `python-contract-admission-installed-i5kkbf9_/REPORT.json` | `18ca97becce070524e9a30e252675a5c9420ee8f63c99b56b1351ff89871ebe6` |
| Python collected-nodeid identity failure | isolated: `python-contract-admission-installed-yijqv8_o/REPORT.json` | `d829ffe6b510521fdd117e05c240527c3f1dd38b41a4747df46b5b3af9ae83a2` |
| 3979 pure JavaScript and C# outcomes | isolated: `contract-queue-managed-recapture-xra348pb/execution-3979-r2/OUTCOMES.json` | `66727b9a105bffb54c73c66421746e28fe21c937a95369d4d3a1863fd05f4fc3` |
| 4759 companion admission rejection | isolated: `20260908T081649Z-network-companion-69439517dc/run.json` | `f00b70c8b76f2e0562e29228cda5a913e31d573054fab26c676d946febfc7305` |
| 4759 companion exact stage audit | isolated: `companion-queueplan-admission-stage-f9liqm0g/REPORT.json` | `8735e7397e10457cb68438dfa666dbd54c024fcfba61814122b2aaa859affc57` |
| 4759 CLI, project and golden recapture | isolated: `sdk-submitted-cli-recapture-2jcvz9wb/REPORT.json` | `bbd710ec01ad71dc6629026702b18eb44662cfd2df2201240cc310a5abec9da6` |
| six Core failures committed-source byte lineage | main: `current-six-kura-baseline-inputs-f2mf77p5/DECISIVE-CROSSWALK.json` | `234ef050313531016759975f22f90017bf1dfe7b7172a0259ec153d7278e2d78` |
| 3979 mini-integration fixture failure | isolated: `20260908T090436Z-torii-contract-miniature-5039546267/run.json` | `23d6c4684b471d9b715509f0e9ea7785583fa18bf7234f878f2583eea9221e18` |
| miniature local execution fixture application | main: `contract-mini-direct-application-u4mvahbq/application-5554_mt2/REPORT.json` | `9af586f6f1127582c2870a2102c20c85bd73986d0ae5672c982040b9b4673329` |
| ff9a miniature eleven actual pass | isolated: `20260908T092544Z-torii-contract-miniature-9cb6d5d32c/run.json` | `201c5664e648ab2c0a6259c8f778817385fafd7f466c9996a90038e992e40afc` |
| miniature identical eleven-case outcome audit | isolated: `20260908T092544Z-torii-contract-miniature-9cb6d5d32c/mini11-audit.json` | `e543ae43cf789b61e03364f2cb0cd5133e055fa18f8f2671219f1046bd3d8a6a` |
| ff9a CLI nineteen and offline project/goldens | isolated: `queueplan-cli-recapture-wn_xwc6d/execution.completed.json` | `f64669e3dc0309a0d2a110174b930d88ec75abbe5e689880d696a749312d6f58` |
| ff9a formatting and codec checks | isolated: `contract-mini-format-codec-_k70dcxi/REPORT.json` | `7a369e3ddbbce8da352d8d573cf876989705b013d73b19a31c0d35e19d28565e` |
| 3979 to ff9a exact input continuity | main: `mini-fixture-input-continuity-81srw0km/REPORT.json` | `5545eb93930bbee397366d74b888246d5b2db9f94f7e22a0676cdb641450edb9` |
| ff9a detached fixture compile failure | isolated: `20260908T093154Z-integration-harness-8dd59504f7/run.json` | `f657fd7561840e9ed86ba2d4406bae2f41b1d90e82fb0ff6513d71c22caf14bf` |
| fallible detached builder application | main: `contract-detached-fallible-builder-vvyyym17/application-2zizmdl7/REPORT.json` | `1167af33855d9f611ca6875d8259a89efdd13a612203e5ad9d01be3f51f7c9ac` |
| ff9a to adc1 integration-only input continuity | main: `current-c9-queueplan-ledger-adc1-r2/input-continuity.json` | `1f894daeff7808335e9ab08c47ae9688f7f12f491844a8451c6eb93fdb065b28` |
| adc1 normal-release daemon recapture | isolated: `20260908T100105Z-release-daemon-987b0bf142/run.json` | `e8bf7019e460909cc443ed21f4d43153d66c32c154c9028260a3a9056b005586` |
| adc1 normal-release terminal audit | isolated: `20260908T100105Z-release-daemon-987b0bf142/terminal-audit.json` | `6b5f05685fe7fc44462fd0ae1de8db9250991ae3d0579653eb2541e5b12f5102` |
| native first attempt pre-Cargo setup failure | external: `evidence/apple/qualification.json` | `a16888b2c025ecc7820d76a29a0943e1e784a8bacbdf3f9a1340a4118bdabdad` |
| adc1 native attempt two source/cache binding | external: `evidence/native-attempt-2/prebuild-binding.json` | `671fd88f24fff4076102d2dfdd5cb180435498f6af6bb9bd8e95149f61184b3c` |
| ordinary workspace owner current parameter review | main: `current-c9-queueplan-ledger-adc1-r2/workspace-readiness.json` | `2661f95c1ac85f74e9f41cbf41338e4a54f100553696dd028eec9dbf67ce4b00` |
| adc1 integration harness no-run pass | isolated: `20260908T100110Z-integration-harness-0f41b902ae/run.json` | `10bce13bd949322cad82daba6a99dd86c92c6f2118dda96985bd1c28b23f4054` |
| adc1 integration harness exact binding audit | isolated: `20260908T100110Z-integration-harness-0f41b902ae/root-harness-audit.json` | `d23d01db1d324fe018da44091f7dd38d27afc83a5074a1b359cff688d57d0ed2` |
| adc1 companion actual four-validator failure | isolated: `20260908T101407Z-network-companion-9b9c68fa8b/run.json` | `0e822fefad3fe8cbed76a492e5935e11b803e51640b479b804fc4d099e1eab2f` |
| adc1 companion terminal stage and preservation audit | isolated: `20260908T101407Z-network-companion-9b9c68fa8b/terminal-audit.json` | `d5baad06403956cc5ad6c5bb84dd170d20a3d80bb71f96f5c6070e6268e8d566` |
| adc1 original actual four-validator failure | isolated: `20260908T101904Z-network-original-64d8e88d67/run.json` | `49a810f442d8513a9b10cd2181eef5a25bf572b7c80ca45c21307ea69c5bb60b` |
| adc1 original terminal stage and preservation audit | isolated: `20260908T101904Z-network-original-64d8e88d67/terminal-audit.json` | `3a0de455b5b518eb80364c3faf9fc1d1097df82db8dfd79f3d301b71c2d0a04a` |
| global status default and pending semantics correction | isolated: `detached-verify-closed-diagnosis-y96ndu34/status-scope-and-admission-trace.json` | `df6ed8e75d8aa309fee4d2b1ebd0b8a995dc3fd06739693c0ae9d7e1b9026e49` |
| strict all-four detached certificate census | isolated: `detached-verify-certificate-decoder/SUMMARY.json` | `ed2e3f32b2232d06811937c6c107403cd5e0194256876d68ee793fc169dadd2e` |
| canonical indexed block frontier census | isolated: `detached-verify-certificate-decoder/FRONTIER-SUMMARY.json` | `8b0e28738378a7b9b53a17ce40eb3e0f57db2a5491bc2c0959ff03bff588c651` |
| held contract network-time candidate R2 | main: `contract-network-time-preparation-j6xa5urn/revision-2-public-accepted-api/REPORT.json` | `3e7dfa9495e1a015c01b4a9789d9b8b48356651abd0d0ab71dfdda918d4e6736` |
| applied contract clock and signed genesis gas budget | main: `contract-clock-gas-combined-jkfsdcxb/application-_ddsgt6l/REPORT.json` | `cbe8dbdd72b40aed1b931d200a99912e1516a142a028676f9bbe9e502c3b7f59` |
| strict four-genesis gas parameter census | isolated: `detached-verify-certificate-decoder/revision-4-genesis-gas/execution-1/REPORT.json` | `abde36cc0d0b71b37c7ece02ff6395609179a822707a6a8ba502a0dec5a73cce` |
| admission-to-selection gas barrier conclusion | isolated: `detached-verify-certificate-decoder/GAS-SELECTION-CONCLUSION.json` | `c6c32fbbf952239787a67f25390fbb860f8638d2a80e9ee4b1dae7a0f4ccac2b` |
| clock and strict genesis gas independent review | main: `contract-clock-gas-independent-review-y4492y00/REPORT.json` | `e80b1dfc9317e14c9137edf712371b8a449de59426cfa2482dd1b45223f96c8b` |
| strict signed genesis gas fixture candidate | main: `contract-probe-genesis-gas-preparation-urk2469y/revision-2-strict-genesis/REPORT.json` | `7feafe2c45b4192c8b4aa3375b6c2a7c35e81659275dd5136db3d6d2d053d60e` |
| adc1 Apple attempt two terminal packaging failure | external: `evidence/native-attempt-2/apple/qualification.json` | `6fc0e0f636e4f178dbde889c73235dd0e3efbe7f23319eab5838551ba4641d7e` |
| adc1 Apple first-target process result | external: `evidence/native-attempt-2/apple/apple-build/result.json` | `455c1c45925ace3a887ae26a6040d9c4c845d5d80cd13da486a3967994705152` |
| Apple relocated reference provenance diagnosis | isolated: `current-native-final-run-t4dabqp7/revision-7-apple-owned-attempt/revision-2-source-transition/normalization-failure-diagnosis/REPORT.json` | `c9103702877fed5b0980260aff451caf84ac349baab02d22be4c9bc9b5f1f86f` |
| bfdd Torii sixteen exact passes | isolated: `20260908T110050Z-torii-contract-certified-preparation-7874bce7c3/run.json` | `f5521a13c3a5614a1b0254292e5ea09fd01918eed97ba2510bb6727f9f2f1d0f` |
| bfdd integration harness build only | isolated: `20260908T111216Z-integration-harness-87ae436e46/run.json` | `f3d18e9344759a88b4b4a176f1eb93fff8f9bf1a3e023db0562732191cee2cb6` |
| bfdd signed genesis gas fixture one pass | isolated: `clock-gas-fixture-execution-_oop87i6/execution-fyy6plkx/REPORT.json` | `3ea49eaddf7b7f60ca765c89df8497663ef269c533cc8ee905e73acea27e98d4` |
| bfdd normal release daemon build | isolated: `20260908T110103Z-release-daemon-c4955aa20d/run.json` | `68fa826fc8ef0e4862159baf5f37781e9e1c37b6b227658349c2cbc614297914` |
| bfdd normal release identity and preservation | isolated: `20260908T110103Z-release-daemon-c4955aa20d/terminal-audit.json` | `8350b1e09900208500f1ebcbaf80ae7b685202c520b984510104234e4a320d5c` |
| bfdd companion actual network failure | isolated: `20260908T112133Z-network-companion-c59128c913/run.json` | `2c46b25d15bdd694bbf7553910e67a5a31c1e709f0041f04d455d5c5f3fa8cca` |
| bfdd companion terminal stage and preservation | isolated: `20260908T112133Z-network-companion-c59128c913/terminal-audit.json` | `a5c5b2c758961139650b39b72af7fc0e862d2fe4218ebbdefb3501bff9658298` |
| bfdd original actual network failure | isolated: `20260908T112703Z-network-original-aed3c2f649/run.json` | `9fae653ae1b343a3a18a022ed03bfa03ce69a9812c3e9e96cf933d9b47bf8c69` |
| bfdd original terminal stage and preservation | isolated: `20260908T112703Z-network-original-aed3c2f649/terminal-audit.json` | `8708fabf47748fb2fdb5d1292c9a93b7d524e007f7b258421188b070944ce1bb` |
| bfdd closed public certificates and indexed frontiers | isolated: `bfdd-closed-public-frontier-21lnyd0n/SUMMARY.json` | `a69ffc773a6128156c1d81b86bf8bcb6870cc913eac63dbf3cb9e05e3e450fc3` |
| bfdd original wall stage crosswalk | isolated: `bfdd-closed-public-frontier-21lnyd0n/ORIGINAL-STAGE-TIMING.json` | `0af3b0a53bf4929782b7e960283e152a31f563fc75c70ee4a1a94a742ba538ee` |
| bfdd configured cadence and Applied wait comparison | isolated: `bfdd-closed-public-frontier-21lnyd0n/CADENCE-COMPARISON.json` | `43cdc0884e570d888759920804a1a8d1328ff668c9282ab95b898a1880dbe02b` |
| held two-call cadence-derived Applied wait patch | main: `contract-cadence-applied-wait-yfnm8njm/REPORT.json` | `859a933e9651b6aabeae2f25ea7adf113cf4af0aacecc24585a0862c258e898b` |
| MAIN current source and actual index comparison | main: `main-post-clock-source-audit-_yajs97q/REPORT.json` | `09252663faf89a5024166ea15231db3deb9a110323042979640ebb21bb128bbc` |
| native package cleanup original guard failure | isolated: `current-native-final-run-t4dabqp7/package-cache-recovery-preparation/recovery-execution/REPORT.json` | `89f81cc1918b5ece4ad7debedd911b0c289e938680159893031cc323c74d52aa` |
| native actual package removal attribution | isolated: `current-native-final-run-t4dabqp7/package-cache-recovery-preparation/actual-cleanup-attribution.json` | `49e616e346b25513fa7d4bc574f85ec31b066a63a76bb9b706065077367949e4` |
| held native R8 attempt three preparation | isolated: `current-native-final-run-t4dabqp7/revision-8-package-cache-recovery/REPORT.json` | `e62ba800f72b2be89426deaf6cdcaf9d06a8adecd602654399db97c82bcdf123` |

## Preserved superseded qualification prose

The following two regions are exact UTF-8 bytes removed from the active spec.
Their dates, counts, source identities, “latest” wording and unavailable paths
are historical claims only. The active bounded live scan contract stays in the
spec and is not part of either archived region. Existing 2026-09-07 and 2026-09-06
archives remain untouched.

Region 1 SHA-256: `7d28e2262ff8627bd333167c0eef370337cd3cb699574cf44126e215a53b1c39`.

<!-- preserved-region-1-start -->
## Latest compiler qualification

All 1,090 compiler library tests pass on the current candidate at
`target/kotodama-v1-qualification/20260907T083242Z-compiler-lib-3d7f16def1/`.
There are no failures or skips; source, Cargo.lock, HEAD and staged contents are
unchanged during the run. The new nominal-identity test permutes package, module
and import ordering eight ways and compares complete error catalogs, separate
variant schemas and embedded public return schemas. The DEX payout vectors and
both-tag Result if-let execution pass in the fresh 50-test VM capture below;
the cursor authorization case passes in the Core qualification captures.

Compiler integration now passes 77/77 in
`20260907T083348Z-compiler-integration-73e361ac61`. Its preceding 74/77 run
exposed one stale Json::parse label fixture and two zero-cost if-let regressions.
The fixture now accepts only the positional constructor and retains exact
unknown/duplicate-label, arity, direct-literal and source-span checks. Shared
statement/expression lowering branches directly on canonical Option/Result tags,
swapping branch targets for tag zero, while nominal errors retain exact code
comparison. Initializers still run once and payloads bind only in the selected
branch. The original exact IR and executable-byte comparisons pass unchanged.
The fresh compiler library run includes this production correction.

Koto/LSP passes 37/37 in `20260907T083423Z-koto-tests-6520640c56`, including
exact Japanese notes, help and related labels with UTF-16 locations. The editor
acceptance mapping binds its fifteen requirements to 27 compiler and 22 Koto
cases actually executed in these fresh suites. Local debug initially passes 8/9
in `20260907T083512Z-cli-debug-c0ab7fdeba`: one fixture incorrectly expected
Unicode normalization before duplicate detection. Its test-only correction
checks canonical NFC key/byte readback, rejects decomposed and mixed spellings
with the exact StatePath error, and checks identical raw keys fail with Norito
DuplicateField at the JSON stage. Production remains strict. The corrected
nine-test selection passes 9/9 in `20260907T084730Z-cli-debug-1dcfefb112`,
with exact membership, zero ignored cases and unchanged source, index and lock.

## Offline first-project qualification

Fresh normal CLI and Koto builds pass in
`20260907T084954Z-build-cli-3a5776b482`; the fixture exporter build also passes
in `20260907T090404Z-build-fixture-export-34aedeb808`. Both captures preserve
source, index, HEAD and Cargo.lock and retain their exact executables.

The complete generated-project workflow passes in
`20260907T090456Z-first-project-39f2ad4f13`, with IP networking denied. The
published project has exactly eight authoring files, including both manifests,
editor settings, the branded stateful contract and standalone tests. Actual
check, build, locked build, Koto project check, format and schema commands pass.
All three discovered standalone cases execute successfully: `adds_to_total`,
`rejects_zero_amount` and `requires_invocation_permission`. Reusing the nonempty
destination fails with the expected reason and preserves its complete inventory.
No runtime credentials or network side effects are generated. The complete
workflow, frozen-binary binding and unchanged-source checks are retained in its
qualification and acceptance-facts records. Native SDK, final artifact admission,
four-validator execution/restart and workspace qualification remain open.

## Latest runtime qualification — 2026-09-07

The Core completion/recovery selection now passes **107/107**, with zero ignored
cases, in `20260907T071604Z-core-retirement-qualification-c171433b75`. Both
actual cold-restart fixtures complete their exact queued worker through the
production coordinator publication and acknowledgement before teardown. Their
fixture cleanup retains the validated carrier, exact completion ordinal, empty
queue census and open output guard; it does not forge a signing result. All five
new recovery regressions, the 21 retained boundary cases and the cursor
permission revoke/restore test pass. This supersedes the earlier 105/107 result
below without erasing its two fixture-teardown failures.

Additional fresh selections pass with no ignored tests:

| Selection | Passed | Capture |
| --- | ---: | --- |
| VM runtime, lists and fused arithmetic after the if-let correction | 50 | `20260907T084222Z-ivm-runtime-lists-8a6d2f203b` |
| ABI policy and contract artifact acceptance | 72 | `20260907T082416Z-ivm-abi-artifacts-d1bd447fdb` |
| VM values, bounded state scan and exact test driver | 57 | `20260907T072722Z-ivm-values-driver-0ec105bba7` |
| Numeric syscalls, shared fixtures and pointer policy | 40 | `20260907T073728Z-ivm-numeric-pointers-73fe8d7b9c` |
| Generated syscall documentation | 1 | `20260907T074228Z-ivm-syscall-docs-7671aab125` |
| Data-model contract values and exact executor rejection | 57 | `20260907T074312Z-model-contracts-4880d59fc1` |
| Core execution header binding | 1 | `20260907T075254Z-core-header-ce420c0aa0` |
| Core manifest ABI acceptance/rejection | 6 | `20260907T075832Z-core-manifest-d0d963af07` |
| Torii public nominal state, argument products and cursors | 5 | `20260907T080014Z-torii-contract-state-8490f9c6b3` |

These receipts preserve the complete source inventory, HEAD, staged contents and
Cargo.lock during each command. The ABI value selection separately passes
172/172 as recorded below. Their scopes do not replace final native, generated
artifact, four-validator or workspace execution.

The VM runtime/list selection initially passes 49/50; its added DEX vector
fails before execution because a hand-formatted JSON record is not in canonical
key order. It now uses `Json::try_new(norito::json!(...))`, retaining all input
precision, payout, runtime and constant-folding assertions. The ABI/artifact
selection initially passes 70/72; two stale `range` metadata fixtures now use
`page`, check both `page` and `take`, and explicitly reject otherwise-valid
retired `range` metadata. The existing LSP diagnostic test additionally asserts
exact Japanese note, help and related-location messages alongside UTF-16 ranges.
The three reviewed test-only patches are applied in
`20260907T081805Z-three-reviewed-fixture-patches-217f8fe7`. Runtime/list tests
subsequently pass 50/50 both before and after the if-let correction; artifact
tests pass 72/72 and Koto tests 37/37. No production alias or compatibility path
is introduced.

The full Sumeragi proof-ledger checker exits 1 after 2,925 seconds with 276
diagnostic lines in `seal-refresh-owner-20260907T070618Z-4uryw_kx/` under the
qualification root. It does not report the eight reviewed kernel seals or the
actual-index provider manifest. Completed static attribution establishes 242
failed predicates on authenticated HEAD inputs. This includes all 35 diagnostics
on the four adapter/effects/ledger/scheduler roots changed by this work. The
remaining 34 have identical complete source, owner, helper and expected-hash
inputs since the earliest retained resumed snapshot at 03:34:38. Those 34 are
not established as HEAD failures or as predating the original user task. No
diagnostic remains unclassified relative to these two distinct baselines.
This is static attribution, not a passing gate or a full HEAD checker execution.
The exact comparisons are retained in
`full-proof-diagnostic-attribution-20260907T081458Z-lpzpt413/retry/completed-static-attribution.json`.
No unrelated source seals are refreshed to suppress these diagnostics.

The twelve selected source guards have passing execution evidence: eleven
unchanged cases from the initial selection and the corrected Prepare-cache test
with all six original mutation predicates and expected diagnostics. Its stale
physical-provider references are replaced with the actual included leaves,
which match HEAD; the final single-test capture passes in 43.62 seconds with
zero failures or skips. The receipt is
`prepare-cache-remaining-paths-20260907T082151Z-m4sus8iw/test-20260907T082259Z-qtts16bk`.
This scoped success does not turn the broader failed checker into a pass.

The sibling documentation default production command also fails with a 6 GiB
V8 heap on immutable HEAD `de24208350f23c15ca5474ccb525ca9f1dee873a`, confirming
a pre-existing build-resource failure. Its source/dependency snapshot, denied
IP-network probe, actual exit and both checkouts' preservation are captured in
`docs-HEAD-default-build-20260907T071301Z-k1zsk2vx`. The updated tutorials pass
the 12 GiB production build and link checks for all 21 locales. Iroha's own
build remains independent of that optional checkout.

<!-- preserved-region-1-end -->

Region 2 SHA-256: `2527a007b87d76232ae803f384b2acd0fd62c8b89216e479ed5b067de716b412`.

<!-- preserved-region-2-start -->
## Resumed qualification checkpoint — 2026-09-07

The temporary `/tmp/kotodama-*` and `/private/tmp/kotodama-*` records cited
below are unavailable after resumption, including the acceptance crosswalk,
Apple publication receipts, guarded network tools and final release daemon.
Recovered integration-harness and JavaScript-native bytes match their recorded
prior hashes, but that does not authenticate them against subsequent source edits.
The repository-local Apple package advertises ABI 21 and lacks the required
universal macOS slice; current Swift requires ABI 23. It cannot qualify the
current candidate. Existing native outputs must remain preserved while their
official owners produce fresh source-bound artifacts.

Source corrections retain receiver facts from the exact locked graph in incomplete
editor buffers, execute declared standalone tests before scaffold publication,
and reject empty test declarations or suites. Scaffold publication now uses one
rename without deleting an existing empty destination first. Ten Unix tests cover
validation failures, exact-rejection mismatches, injected final-publication failure,
late nonempty destination creation and successful empty-directory publication.
All ten pass in the fresh CLI run at
`target/kotodama-v1-qualification/20260907T050848Z-cli-scaffold-516f13a5d2/`,
with no skips and unchanged source, lock, HEAD and staged contents. The same
build compiles the production Core cancellation path; its runtime tests remain pending.
Swift fixtures now select the configured authenticated external artifact and use
the canonical contract address. A new Core regression selection exercises
certified Fetch/Store completion when a timeout supersedes its reducer owner;
it reproduces obsolete completion rejection but does not establish the cause of
all earlier network timeouts. Fresh compiler library tests pass 1,089/1,089, including all sixteen
editor tests and cursor positions after partially typed member names. The run at
`target/kotodama-v1-qualification/20260907T034223Z-compiler-lib-d664c52c85/`
records exit zero, unchanged source/lock/staged contents during execution and the
frozen Cargo-reported test executable. Completion retirement and native
SDK corrections still require fresh focused tests. The complete generated-project
command workflow still requires fresh normal CLI and koto executables. Retired-codec, diff and history/current-view
guards also pass; the history verifier checks 64,736 records and 67,311 occurrences.

Offline Python manifest/nominal-schema tests pass 220/220 under the existing
private Python 3.12.14 environment, with 139 selected inputs unchanged. Evidence
is `target/kotodama-current-python-manifest/qualification.json`. This is
source-mode schema evidence; the imported cached crypto extension is identified
in the report and is not a freshly qualified native build. JavaScript's five
selected schema/artifact test files pass 18/18, with 573 inputs unchanged and no
native-binding acquisition calls; evidence is
`target/kotodama-current-js-manifest/qualification.json`. Both runs deny network
access. The editor client tests pass 3/3 and all eighteen generated syntax files
remain current.

Focused Kotlin-owned JVM and Java-source consumers pass 17/17 without skips:
fourteen manifest tests, one Java nominal-schema test and two argument-record/
nested-Option transport cases. Gradle 9.3.0 runs offline on Temurin 21.0.11 with
one worker; JDK 8 compilation compatibility remains enforced. Test execution is
fresh, compiler tasks reuse verified up-to-date outputs, and source/lock/staged
hashes remain unchanged. Evidence is
`target/kotodama-v1-qualification/20260907T041025Z-kotlin-contract-consumers-f2b0ca1b7d/`.
No JNI build is claimed by this selection.

C# passes all 21 focused manifest/nominal-schema cases with no skips under its
pinned .NET SDK 8.0.419. The SDK is installed privately after checking Microsoft's
published archive SHA-512; global SDK settings and `global.json` remain unchanged.
All 193 selected source/cache inputs are unchanged. Tests use cached restore
assets with IP networking denied and local Unix-domain IPC permitted for the
test runner. Evidence is `target/kotodama-current-csharp-manifest-sdk419/`;
`target/kotodama-dotnet-sdk-8.0.419/installation.json` retains SDK provenance.
The earlier missing-SDK and blocked local-IPC attempts are resolved harness
prerequisites, not product test failures or native qualification.

The new Core supersession regression compiles, but all four cases stop in the
shared Prepare-QC fixture setup before exercising the target transition. The
captured command exits 101, with source/lock/staged contents unchanged, at
`target/kotodama-v1-qualification/20260907T034424Z-core-fetch-supersession-e8e413528c/`.
This is a test-fixture failure, not a demonstrated runtime reproduction or fix.
The second run, at
`target/kotodama-v1-qualification/20260907T040156Z-core-fetch-supersession-257170e540/`,
also exits 101 with unchanged source/lock/staged contents. Selecting the frozen
view leader fixes Fetch creation, but the older hand-built owner helper rejects
the authentic manifest-less Fetch before the target transition. The fixture is
being changed to use empty-owner construction and normal selected-response
admission, which binds the authenticated response manifest separately. Production
consensus behavior remains unchanged.

The third run, at
`target/kotodama-v1-qualification/20260907T041534Z-core-fetch-supersession-d8fac686ac/`,
compiles and executes four tests with unchanged source/lock/staged contents.
Both Fetch cases now pass real ingress admission, fsynced Ready publication,
the Busy fence and authenticated timeout/view installation, then fail at the
target retry with `Err(DispatchProjection)`. They reproduce the target Fetch
failure, including the protected-body case. Both Store cases still fail before
timeout while publishing the initial Fetch successor: the test's empty owner
lacks the paired live lifecycle ordinal authority. The fixture is being bound
to the normal shared runtime/coordinator authority before Fetch creation. The
command exits 101; no production fix or passing network result is claimed.

The fourth capture, at
`target/kotodama-v1-qualification/20260907T042933Z-core-fetch-supersession-f312dea852/`,
compiles but the newly added test logger fails before fixture execution because
the test has no Tokio reactor. The logger is removed. This is a fixture regression,
not evidence against the production transition.

The fifth capture, at
`target/kotodama-v1-qualification/20260907T043849Z-core-fetch-supersession-42c26242c6/`,
reaches the intended completion retry in all four cases. Fetch and Store both
reject with `Err(DispatchProjection)` after the Busy fence, authenticated timeout
and view installation; protected and unprotected cases reproduce. There are no
setup failures, no ignored tests and no production consensus changes in this
capture. Its command exits 101 with unchanged source/HEAD/lock/staged contents.
The exact frozen Core libtest SHA-256 is
`18c08b8d721eca110ba92315e6294df9454573fe770d778d4ff7d99c392cd936`.
The runtime repair now stages exact obsolete-work cancellation before carrier
retirement. Its sealed registry/reducer join and immutable marker comparison
preserve the durable body and current owner. Seven adapter negative/identity tests
and expanded ordinary periodic/FIFO progress and publication-failure tests await
execution. No passing repair result is claimed. All four reproduced cases remain
required.

That same freshly built Core executable passes all sixty contract-focused cases:
thirteen nested-call cases, three dispatched-call gas/rollback cases, twenty
return codecs, eight bounded merge cases, one production schema-bound scan, four
nested-state rollback cases and eleven metadata/rejection cases. The ten exact
selections each execute their expected nonzero count with no failures or skips.
Evidence is
`target/kotodama-v1-qualification/20260907T044628Z-core-contract-frozen-427e85d586/`;
source/HEAD/lock/staged contents and the frozen executable remain unchanged.
These passing contract paths do not close the reproduced completion bug.

The editor and native-JavaScript setup commands are corrected in English and all
twenty tutorial translations. Current content, locale/provenance checks and 56
focused validator tests pass. The package's normal `pnpm run build` exhausts its
fixed 6 GiB heap and aborts; that attempt is preserved in
`target/kotodama-current-docs-after-setup/`. The same maintained VitePress builder
passes with an explicit 12 GiB process heap in 97.4 seconds. Fresh built-link and
all twenty-one locale-root checks pass; all twenty-one built tutorial pages contain
the corrected setup. Source/dependency inputs remain unchanged during execution.
Evidence is `target/kotodama-current-docs-build-12g/qualification.json`. This is a
successful production documentation build with a larger heap, not a passing
unmodified package command or an established baseline for its memory failure.
Iroha's build remains independent of the sibling checkout.

The first full cancellation candidate compiles and executes 78 selected Core
cases at
`target/kotodama-v1-qualification/20260907T052636Z-core-retirement-qualification-8f4e7f247f/`:
68 pass, ten fail, none are skipped, and source/lock/index/HEAD are unchanged.
All seven adapter authentication/supersession cases, four closed-output or
failed-publication regressions, and the new 90-assertion source case pass.
Seven success regressions pass exact durable cancellation, body preservation and
current-owner/marker invariants, then misuse the dispatcher on an empty census;
the production caller first classifies this state as no work. Their fixture must
follow that precondition before current-work and complete recovery assertions
can qualify. Two source checks need review of the new opaque registry import
group; another existing source check rejects its cold leader-wire lock-round
anchor. These failures do not yet qualify the cancellation candidate.
The empty-census fixture now uses the production no-work classifier. The registry
guard pins the exact new two-item opaque import group. The lock-round assertion
also fails in the preserved earlier Core executable; its adapter delegation and
factory source are HEAD-identical. Evidence is
`target/kotodama-v1-qualification/retirement-guard-baseline-20260907T053918Z-ptadz8jh/`.
Its refreshed guard follows the actual factory, retaining the lock checks and
adding exact context, owner and WAL-frontier checks. All five compaction tests
pass with the 2,161-line reduction floor intact. A new production nested-call test
checks the same valid map cursor under denied, granted, revoked and restored
entrypoint permission; that regression passes in the next 81-test Core capture.


The next current-source Core capture (`20260907T055132Z-core-retirement-qualification-47788ca071`)
passes 76/81 tests, with no ignored cases. The corrected empty-census checks,
ordinary cancellation cases, failure-publication checks and cursor authorization
regression pass. Five failures remain: two restart cases, one coalesced Store
progress assertion, and an EnterView source guard plus its aggregate runner. The
periodic timer legitimately emits both the protected PrepareQC and Store; a test-only
Store counter now checks that actual batch without changing the production effect
classification. Startup diagnostics now distinguish authenticated adapter replay
failure from census preparation. These edits and the remaining source guard require
fresh Rust execution. The restart fixture used generation one although ordinary
view installation and the production cold opener use `Generation::INITIAL`; its
helper now follows that production seed without changing persisted tags or replay
validation. The next run must establish whether this resolves the restart failure.


The following capture (`20260907T061551Z-core-retirement-qualification-892a435c29`)
passes 78/81, with every source guard passing and no ignored tests. The real
ordinary restart still rejects BodyAvailable: WAL recovery restores the protected
old-round lock but does not seed its missing body work. This is a runtime recovery
issue. The two Store cases reach a test-only assertion that incorrectly expects a
single broadcast; the real periodic batch contains the protected PrepareQC, its
TimeoutCertificate and one Store. The corrected observation now checks all three
and the exact PrepareQC. A fourteenth fixture case adds a real strict same-view TC
upgrade before restart to test persisted generation recovery. These latest fixture
changes and the prospective runtime correction are not yet qualified.


The next fixture-only baseline (`20260907T062856Z-core-fetch-supersession-b2a583871e`)
passes 11/14. Both genuine restart cases now report exactly “BodyAvailable replay
has no matching work”; the corrected mixed-broadcast/current-progress case passes.
The attempted fourteenth case fails its own strict-upgrade precondition: its
already-known PrepareQC cannot strictly advance the retained highest QC. That
invalid fixture extension is removed. Five independent reducer tests now cover
exact lock-body recovery, bounded lock/high versus Decision ownership, unchanged
no-timeout seeds, live/WAL generation parity with successively higher certificates,
and ordered malformed/foreign WAL rejection. Their baseline runs before any
production recovery change; prior failing fixtures remain recorded as such.


The production-unchanged 18-test baseline
`20260907T064552Z-core-cold-recovery-baseline-8fa058b68d` passes 13/18: both real
cold restarts and two exact lock-body unit tests fail on missing body work; the
live/WAL parity test confirms generation zero versus live generation one after a
valid strict upgrade. No-timeout seeds and exact malformed-WAL rejections pass.
The reviewed repair now seeds only the exact undecided lock through the same
non-overwriting insertion used by live Fetch, without a new startup effect or
historical Prepare authority. Recovery reconstructs generation in the same ordered
WAL pass, using the live checked transition and preserving durable validation error
precedence. The six-file production/guard patch is applied; scoped format and diff
checks pass. The expanded runtime selection adds 21 existing boundary, FIFO,
Decision and authenticated-WAL checks. Its capture
`20260907T065745Z-core-retirement-qualification-3f325b77a0` passes 105/107,
including all five new recovery tests and all 21 existing boundary cases. Both
cold fixtures now reach `ValidateQueued`, then fail the unchanged receiver
teardown guard because the fixture leaves that work queued. A test-only cleanup
must finish and acknowledge the exact worker completion before teardown; the
production recovery assertions pass, but these two complete tests remain open.

Fresh ABI value tests pass 172/172 with no failures or ignored tests in
`20260907T070644Z-abi-values-8ec2bc7dc3`. Both captures preserve source,
Cargo.lock, HEAD and the complete current index during execution.

Fresh workspace formatting (`cargo fmt --all -- --check`), retired-codec checks
and the exact 2026-09-06 history archive verification all pass without changing
source or index. Their current-source receipts are retained under the qualification
root; full workspace compilation, lint and execution remain pending.


The three new include providers are now staged through standard Git for the
repository's actual-index admission check. The controlled staging receipt
`owned-provider-staging-20260907T064513Z-78227f8e` verifies all 18,617 original
index rows unchanged, exactly three new stage-zero blobs, and unchanged worktree,
HEAD and Cargo.lock. No commit was created; no unrelated entry was staged.

The semantic-helper source guard is refreshed for the approved V1 test bodies,
error-value traversal, exact rejection selector, removed `StateKeys` builtin and
diagnostic identities, including the new fifteen-test labels/patterns leaf. Its
existing line cap, mutation checks, required regression names and effect-walker
invariants remain; all twenty-six selected source/syntax/IR tests pass after the
refresh. The previous guard also rejects isolated HEAD sources at its
builtin-set check; this source-guard baseline is distinct from Rust runtime
evidence. Review inputs and the exact patch are retained in
`target/kotodama-v1-qualification/semantic-guard-review/`.

New command evidence is retained under the ignored
`target/kotodama-v1-qualification/` directory with exact source/lock/staged-content
inventories, actual exit status and frozen Cargo-reported binaries. An active
capture is not a pass. C9 stays open until current-candidate compiler/CLI/Core,
native SDK, final artifacts, normal-release four-validator execution/restart and
workspace validation have authoritative evidence. Preserve separately established
baseline failures without claiming that historical results prove current success.

<!-- preserved-region-2-end -->


## Frontier deferral implementation checkpoint — 2026-09-08

The current candidate implements typed merge-frontier deferral. A changing
committed State generation or authenticated Kura publication ahead of State stops
new merge continuation; stable invalid candidates, missing or wrong durable
parents and storage failures retain their error paths. Completed durable signing
authority is preserved. Providers complete their operation guard when deferring,
and producer/retransmission continuations wait without changing proposal shape.
The Apply tests pause an otherwise successful application before and after Kura
publication and release the worker during assertion unwinding. These tests and
the updated formal owner checks have not yet qualified the applied candidate.

Both selected four-validator fixtures now use the existing cadence-derived
Applied wait (104 seconds at their signed eight-second cadence). The previous
60-second failures remain recorded; extending this fixture bound is not a pass.
The pending source changes intersect three actual CLI, Python-native and Apple
host compiler inputs. Those outputs require rebuilding. The separately captured
Koto/compiler, Rust SDK25 and pure JavaScript inputs do not intersect; all 69
retained artifact payload hashes remain unchanged. Whole-tree build identities
still require a fresh binding. Network, native and workspace gates remain open.


## Core merge-frontier execution checkpoint — 2026-09-08

The exact 26-case selection closes on isolated source
`a0beef73fa7577dc2546c0a8a5efe648180be1707389094fa2c392ee661d06ac`:
26 pass, zero failed or ignored, native exit 0. It uses the ordinary test profile,
existing `iroha-core-tests,bls` features, one Cargo job and no `RUST_MIN_STACK`
override. All nine capture checks and 18 independent terminal-audit checks pass;
source, HEAD, staged contents, status, Cargo.lock and actual index bytes are
preserved. The frozen Core executable is `169bba15e0483961de5ee44aef23e4cdbdfca37110d4a4029e85a2f0588a64a0`.
The test summary reports 5,799.85 seconds; command wall time is 8,714.589869
seconds, while Cargo reports 7 minutes 15 seconds of build time. These are
separate recorded measures.

The retained run is `20260908T122958Z-core-merge-frontier-26-788fc6f413/run.json`
(SHA-256 `38e2f7fcac46f37b68d75d1123f0ec19793140d525628714436c974823917576`).
The independent audit is
`core26-terminal-audit-preparation-au0go6fk/revision-2-terminal-child-guard/execution-4s34khmu/REPORT.json`
(SHA-256 `bb0c1ac8ed3c781c716e3093a2efbf60b18e5fe9064bdba20a6f65e50380889c`),
both under the isolated qualification root.

All nine real Apply cases pass, including the original SuccessfulApply case.
The prepared completion-aware rendezvous is therefore a robustness improvement,
not a repair of an observed failure. Its two Rust test paths and the formal-test
anchor correction remain unapplied at this checkpoint. The retained input census
(`completion-test-input-impact-il12gkgo/REPORT.json`, SHA-256
`573ec5804045e8996f8c9a33dc51bb6f99867715b13e09c6126574dd32a9b876`)
finds none of those three paths in eight production/consumer inventories. Both
Rust files are actual Core test compiler inputs. This scoped exclusion does not
requalify old CLI or native outputs after the earlier production frontier change.

The normal-release daemon capture is still active. Both bfdd four-validator
failures remain recorded; no new network pass is claimed. C9, affected CLI/native
consumers, the six separately recorded Core failures, unwaived formal seals and
ordinary workspace validation remain open. Pending test-only changes and any
subsequent build or test results require their own source-bound receipts.


## Applied test-only changes and final-source checks — 2026-09-09

The three-path completion-aware rendezvous and formal-anchor change is now
`APPLIED_VALIDATED` in both trees. The application captured MAIN as
`a16ac8ada050d40f5c8adc44014e3e5432a53baa2a4bc08e45b5b00af1846f63`; isolated
source is `961db6d836ea3c53e911b84c0b09ae6fed9d7a731ebc962d7f903ba11bcbeb9d`.
All eight application checks per tree pass, preserving HEAD, actual index bytes,
staged contents and Cargo.lock, with exactly the reviewed three paths changed.
The receipt is MAIN qualification
`merge-frontier-completion-combined-jpbt86nt/application-1aiycp8d/REPORT.json`
(SHA-256 `3c1cc9e1dae96b4c2491f0df42fd95cf9a5ad44108901abddd06b97f53f67852`).
The preceding Core26 checkpoint remains an actual a0 execution, not a 961 rerun.
Core5 and two pytest nodes including 29 mutation checks are still pending.

The normal a0 daemon build closes native 0, with all nine capture checks and
32 independent checks passing. Cargo reports 268 minutes 54 seconds; command
wall time is 18,591.81332 seconds. These clocks are separate, not CPU measures.
Its run is `20260908T132210Z-release-daemon-c38060b70c/run.json` (SHA-256
`d57f6e8e5b70774c0fc48222a7dbe2b125c10e724d25f5ae56cbb7e3b76f74ef`);
the adjacent `independent-terminal-audit.json` is
`7cd4c79d2d73a63dbe452730e5eb476d521c09debed65af4a82ae147a46f054b`.
Its completed 2,410-input dep-info excludes the three test/formal paths.

A new ordinary release capture on actual 961 also closes native 0. Cargo reports
`fresh=true` and 1.12 seconds; command wall time is 1.156731 seconds. All nine
capture and 35 independent checks pass. The frozen daemon has SHA-256
`96a884e6f424248c6e77a97a9ec6963db3a6a917239c8e88a7d06d26165c2467`,
343,282,304 bytes, mode 0700, exactly matching the a0 daemon bytes. This is an
actual cached build on 961, not a rewritten a0 result or runtime/network pass.
The run is `20260908T183851Z-release-daemon-ee19d8fe7a/run.json` (SHA-256
`3b7f56110c2b5dc047d88377f0a7c152fb151082513704a9c9a1aa1be91eb27a`);
its adjacent independent audit is
`b805ff4e20a0470f24f38e9a2ea8bd1f0a5e99d1adacbb412b93918d1a835c56`.
Both daemon records are under the isolated qualification root.

On the same actual 961 source, `cargo fmt --all --check` and
`bash scripts/check_no_legacy_codec.sh` each return native 0. The codec guard
finds no retired dependencies, Native AMX V1, lane payload handoff or PK2
compatibility paths. All ten preservation checks pass; the actual 2,554,928-byte
index copies both hash to
`8d7a1856ee263c47c9d7eae9e9381d74e5dbcd954637ef2f9e888167ca7caf65`.
The isolated receipt `contract-mini-format-codec-_348rrsn/REPORT.json` is
`a38fba016e025338578c223acc9df589a7673421d146eb432c7d74694f5b04b1`.
MAIN retains its separate `318d2bc6…` raw index; neither result asserts tree equality.

The final held workspace route is
`current-workspace-program-bound-e_athvwm/run.py` (SHA-256
`29878a2e2cac9ca80d0bdc9689ddefa89ba5936da6d36aa32f2d864ae806d150`),
replacing the retained 9b8486e5/d6011389 owners for final execution. It requires
three actual source-bound programs: normal daemon, a separate message-control
daemon captured by `message-control.capture.py` (SHA-256
`fe68e3ed2ff87c77a45e9e420b36c09bb0945b7542e845facc2943956b1565eb`),
and fresh CLI artifacts from the held CLI binder. The ordinary workspace command
and default real four-validator message-control case remain included; the
feature-gated Parliament network case is excluded. The CLI artifact-binding
report is metadata evidence, not proof of the later external command matrix.
No workspace, message-control build or new CLI execution is claimed here.

Both bfdd verify-Applied failures, the six earlier Core failures and older formal
seal failures remain unwaived. The current harness, both four-validator gates,
CLI/project/goldens, rebuilt Python installed consumers, native R8 and its
consumers, ordinary workspace and separate clean-source release evidence remain
pending. C9 is open; this checkpoint makes no readiness claim.


## Core5 and focused formal completion — 2026-09-09

On isolated source
`961db6d836ea3c53e911b84c0b09ae6fed9d7a731ebc962d7f903ba11bcbeb9d`,
the exact Core5 selection closes with five passed, zero failed or ignored, native
exit 0. It covers four completion-aware rendezvous helper cases and the real
SuccessfulApply case. All nine capture and 20 independent audit checks pass,
including source, HEAD, staging, status, Cargo.lock and actual index bytes. The
ordinary test profile, existing `iroha-core-tests,bls` features and native stack
policy remain. The test summary reports 83.46 seconds, Cargo reports 33 minutes
34 seconds, and command wall time is 3,043.429402 seconds; these are separate
recorded measures. Core26 remains a scoped 26/26 execution on `a0beef73…`.

The same 961 source passes both selected formal pytest nodes:
`test_merge_frontier_deferral_current_owner_contract` and
`test_merge_frontier_deferral_semantics_survive_digest_refresh`. The latter
completes all 29 semantic mutations, requiring the intended diagnostic after
refreshing each source digest. Native pytest exits 0 with two passed, zero
failures, errors or skips; all nine private source/index preservation checks pass.
The test summary reports 1,985.26 seconds. The retained older full-helper baseline
failure was not rerun or waived.

All three receipts below are under the isolated qualification root:

| Receipt | Path | SHA-256 |
| --- | --- | --- |
| Core5 native capture | `20260908T183851Z-core-merge-frontier-rendezvous-5-6dc028608f/run.json` | `a31432f5eff862523f6781d23f2dd7c340f9e6b21edab04c18aaaf5da00f6210` |
| Core5 independent terminal audit | `core5-terminal-audit-preparation-hgr24a55/revision-2-failed-capture-log/execution-0hv92wom/REPORT.json` | `889dc39296831b3fef18b6bd07faca5eda9917edc33ac0cf186e9916a680c59b` |
| Formal two-node/29-mutation capture | `merge-frontier-formal-completion-rerun-jf7yvscu/REPORT.json` | `a8239f9bccf44bda5b7dbadcf324adeeab32425153ca6a204fc76622ed745be2` |

The current 961 harness build is running with no result yet. The already recorded
961 normal daemon and format/codec passes remain scoped as above. Current CLI
evidence is metadata only, not a fresh 19-test or external-project/golden result.
R8 current source binding closes with native exit 0 and all 16 guards passing;
the Apple build is active in session 27619, with native build and consumer
qualification pending. The Python source-only binding also passes all 17 guards;
no Python native build, install, load or test has started. Both bindings record
actual isolated `961db6d8…` source and preserve actual index bytes.

| Closed source binding | Receipt | SHA-256 |
| --- | --- | --- |
| R8 attempt three | External `kotodama-v1-current-native-final-20260908/evidence/native-attempt-3/prebuild/qualification.json` | `289e26620b371a9652951c2a5c48b8f4ec1491a43f8c93731ef255a9144f48f9` |
| Python source only | MAIN `target/kotodama-v1-qualification/frontier-python-executable-16qczqh5/binding-rcjia97q/binding.json` | `d649be5d8b567fb934d9d480e0e1574583edba2af1f8f1a89f27aa2b6b928ebd` |

Both bfdd four-validator failures, the six earlier Core failures and older
formal-seal failures remain unwaived. Both current network gates, affected
CLI/Python/native consumers, ordinary workspace and separate clean-source
release evidence remain open. C9 is not complete.


## Current harness and CLI closure — 2026-09-09

On isolated source
`961db6d836ea3c53e911b84c0b09ae6fed9d7a731ebc962d7f903ba11bcbeb9d`,
the normal integration-harness no-run build returns native 0. All nine capture
and 32 independent audit checks pass, including actual index bytes and the
source-bound frozen executable. Both companion and original network selectors
return 0 in dry-run admission against the qualified normal daemon/harness pair.
These preflights launch no validators and execute no scenario tests; both actual
four-validator readback/RBC/restart gates remain pending.

The current CLI/Koto normal build returns native 0, followed by 10/10 scaffold
and 9/9 debug cases with no failures or ignored cases. Exact-name audits bind
every result to the current source and actual index bytes. The supplied frozen
executables then complete the offline project's 12 command checks and all three
exact tests: `adds_to_total`, `rejects_zero_amount` and
`requires_invocation_permission`. The separate network-denial control passes.
Overwrite refusal returns the expected 1 and preserves the whole project.
The official golden check renders twice and matches all 57 bytecodes and four
manifests in bytes and modes, with zero changes. Its owner-manifest digest is
`ad9970d81855f70209c59bafbe3422595722aaa806e49d3eeaa7de34c4a15add`.
All 39 consolidated controls pass. The artifact-binding receipt supplies the
workspace's qualified CLI prerequisite; the completed external receipt separately
proves project/golden execution. No source promotion or release seal is implied.

Paths below are relative to the isolated qualification root unless marked
External; those paths are relative to the sibling development directory.

| Closed receipt | Path | SHA-256 |
| --- | --- | --- |
| Harness no-run capture | `20260908T193406Z-integration-harness-6db6df37a3/run.json` | `0f0a4549daeb982bc1ab5af569e5a34ef707ed970a3d0dd367ecfbf4b7e7dd48` |
| Harness independent audit | `20260908T193406Z-integration-harness-6db6df37a3/independent-terminal-audit.json` | `83c3934d6321eaa96c7ec51c853e5dc0e2539374ab346567e6491a738cad6b06` |
| Companion preflight | `20260908T193406Z-integration-harness-6db6df37a3/independent-terminal-readback-c7v90lk0/companion.dry-run.stdout.json` | `193d92018ead49ec3157f6e2794318fcf6a4beea90f08f35b83df29ce01a55ad` |
| Original preflight | `20260908T193406Z-integration-harness-6db6df37a3/independent-terminal-readback-c7v90lk0/original.dry-run.stdout.json` | `69abfad844b86b8f6b4d371fdf105802e80aff218c7c334a7f2de60f27cd33c5` |
| CLI consolidated actual audit | `frontier-cli-binding-preparation-mm0pgqol/completed-961-1cj3yxxp/REPORT.json` | `b04f3700053ab3bb735c3124a605ebe4572034cd4ae25ea9fa06ccf271610a5a` |
| CLI normal build | `20260908T201130Z-build-cli-e97746e1e3/run.json` | `bcf03e925871b761ce79d9f7a33015a888d9d3291c7fad8cdba9c11436e56b39` |
| CLI scaffold 10 | `20260908T203042Z-cli-scaffold-9e2e06206a/run.json` | `a60083379edfb17c6e102da88337866dff150465f37bd2101e6fdfa64588eeba` |
| CLI debug nine | `20260908T210501Z-cli-debug-29edc87ef7/run.json` | `9e055c0d78fb946bb90d1b9be84f31791778f0d8d92f0e015429160165d6836c` |
| CLI artifact binding for workspace | `frontier-cli-binding-preparation-mm0pgqol/external-ceqgn6t_/REPORT.json` | `629630c8caf50be10659c33e640b9ca733ba3f69bf2d4fce130df1a112f3c371` |
| Completed external execution | External `kotodama-v1-cli-frontier-final-20260908/evidence/batch-2026-09-08T210835.031541_0000/REPORT.json` | `541d03305ac8f72eaf9e7211e138a8dfbd0caf2ad89d5cb7390ffbe328e4fdc0` |
| Offline project | External `kotodama-v1-cli-frontier-final-20260908/first-project/qualification.json` | `3c87c57d9336b79eb2f64f8ef76fb7049f91ff0d08482f9dd3b5b108f2e819d8` |
| Golden check stdout | External `kotodama-v1-cli-frontier-final-20260908/evidence/batch-2026-09-08T210835.031541_0000/goldens-repository-check/stdout.log` | `78229241a7f309ef70e8882e367c4d0eeb1f593468eb7053e1b9982ad1cd9b3e` |

The held workspace successor is
`workspace-message-control-shared-target-u_4nqd0n/run.py` (SHA-256
`7349dc9df5df50d593a04d5785387d5d099e0cfc15120db28f937175c89ac01c`),
with `message-control.capture.py` (SHA-256
`efb533c244a63c9a075cbe31124edfb8c398e9d1ac81e33b120844a4b76c7de9`).
It supersedes the retained `29878a2e…`/`fe68e3ed…` pair, reusing the existing
Cargo target for the ordinary dev build with `test-network-message-control`.
The feature-specific frozen daemon remains a separate required artifact from
the normal-release daemon. Message-control build and actual workspace execution
remain pending; no normal-daemon substitution is implied.

The scoped a0 Core26, final 961 Core5 and formal two-node/29-mutation results
remain as recorded above. Both bfdd network failures, the six earlier Core
failures and older full-helper/formal-seal failures remain unwaived. R8 and
Python source-only bindings do not qualify native builds or installed consumers.
Actual networks, Python/native consumers, the separate message-control build,
ordinary workspace and clean-source release evidence remain pending. C9 is open.


## Implementation and native publication scope — 2026-09-09

A bounded read-only audit of the actual artifact owners corrects the earlier
C9 wording that grouped clean-source release evidence with implementation
completion. The [native publication contract](../../norito_bridge_release.md)
explicitly supports fingerprint-bound local integration artifacts and separately
requires clean source for release artifacts. Canonical Kotodama golden and
currentRust generation authenticate source/output bytes without requiring a
clean Git tree. Clean-Git native publication certification remains unsatisfied;
this nonpublishing implementation task does not require a commit or that
publication certification. All prior history, actual passes, failures and source
scopes above remain unchanged. The SDK, Python, four-validator and workspace
acceptance requirements remain intact.

After Apple artifacts pass the existing five-target and official readback
checks, review the generated external projection and apply only the three
fallback hashes in `IrohaSwift/Sources/IrohaSwift/NativeBridge.swift` through the
guarded MAIN/PRIVATE source workflow. Then run the official pin check and native
provenance readback against PRIVATE, proving unchanged normalized source closure
and frozen artifact bytes. The native seal normalizes exactly those literals;
a pin-only change therefore needs no Apple rebuild. Preserve every actual `961`
receipt and explicitly bind the resulting source to the remaining consumers,
Python, networks and workspace. Complete the pin edit before Swift/JavaScript
consumer execution, whose compiled input or whole-source provenance changes.
No Apple pass, pin update or subsequent source binding is claimed by this note.

MAIN HEAD `b46c286ee62f64a81f829ae2206e81bf51918774` and PRIVATE HEAD
`ff22bda4c42219ccc055a69d7b5ce86e25b299e8` remain distinct. Delivering generated
pin literals to MAIN does not convert a PRIVATE build into MAIN-HEAD evidence.
The existing R8 source and Apple-receipt joins require an explicit reviewed
transition; the old records must not be relabeled.

The retained MAIN audit is
`target/kotodama-v1-qualification/c9-clean-release-completion-scope-n8gpa6dr/AUDIT.md`,
SHA-256 `bca053e60a74b60e046778a8033209fbce48e0ec336c22f4a921e08a6dbac78f`.
It cites the pin owner, normalized source seal, provenance checker and canonical
generators. This correction follows the prior four-document application
`harness-cli-final961-ledger-hmh8dduc/revision-2-shared-workspace/application-383088p_/REPORT.json`,
SHA-256 `db6776edb6377cb71c503ae16e26cdb89984944e767a73fd7521d10d14c287b2`.
