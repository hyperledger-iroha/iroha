# Settlement work before the two completed happy-day observations

These three source-specific status entries are preserved verbatim from the September 15 working status page (original complete-file SHA256 `3fb9ed7c28eee5bdfb25482f92a11feaab61d866edee909d04c1723da14b9e20`). Their failures, unstarted controls and outstanding work describe their recorded source epochs. The later completed payments do not retroactively change those outcomes.

The SORA Nexus Ready Fetch handoff now retains its exact body-pipeline guard until the sole coordinator completion publishes Store or durable cancellation retires it. Cold recovery restores authenticated current-tag guards before timers; malformed or overlapping carriers remain errors. The source-matched September 15 diagnostic failed before payment when a certified Fetch projection was inactive; the repaired interleaving has five new regression definitions plus sixteen existing cancellation/supersession cases selected for focused qualification. The expanded qualification preserves all prior 596 obligations and adds these 21, for 617 total. Actual validation outcomes and failed attempts are retained in the BCK26 evidence workspace; this source note makes no successful settlement or release claim.

The September 15 current-source qualification stopped with 579 passes, one failed Kagami rollout-binding check and 16 unstarted checks; all children closed naturally. A prior dependency change updated Cargo.lock without refreshing the privacy rollout or OpenAPI lock pins. The lock digest, rollout bytes, bootstrap anchor and compiled rollout pin are now synchronized, and the rollout regression explicitly rejects a substituted lock digest. The first focused rerun passed the repaired check and thirteen other controls, then detected a stale NEVO golden genesis missing optional null asset metadata fields. The repository fixture generator refreshes exactly the current base plus one unchanged overlay, preserving public identities and permissions. Fresh full qualification and actual SORA Nexus settlement remain required.

The exact recorded source `8412521a…` passed all 581 native checks (580 ordinary controls plus explicit CPU correctness). The subsequent sixteen-validator N=3 diagnostic completed all three concurrent proofs with mandatory self-verification, availability certification and restricted upload, then failed pending-receipt polling with HTTP 404 before Prepare or financial finality; all owned network processes closed naturally. Public SORA Nexus receipt polling now reads committed WSV terminal records independently of local encrypted sidecars. Its strict pending response contains only `bundle_id` and makes no acceptance, storage or lifecycle claim; the separate local bundle-status projection accepts valid sparse manifest ordinals while retaining exact durable bindings. Rust, JavaScript, Swift and Kotlin consumers use the canonical per-variant identity fields, including `manifest.bundle_id` for finalized receipts, and reject removed fields. Native qualification and matching real-network settlement remain pending on this changed source; earlier passes do not qualify it. Both pre-genesis test constructors now derive the configured lane and dataspace baselines together, retaining custom dataspace geometry under the committed runtime-catalog model. The N=3 client uses bounded three-leg certification and upload phases, with every started worker joined before advancing; endpoint order, certificate validation and financial assertions remain enforced. Two constructor regressions and two phase-ownership controls extend the next focused suite to 596 checks. These corrections and the incoming runtime-catalog changes await current-source native qualification; no measured concurrency benefit is claimed.

