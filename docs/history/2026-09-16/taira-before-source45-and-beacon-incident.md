# Taira deployment observations before source45

This paragraph is retained verbatim from the prior working status. Its old deployment and pending statements are historical, not current readiness evidence.

Original paragraph SHA-256: `119b004c02fa1ef3908b2089cbd03407f593097c980c5b781f8e6d41c0242225`.

Public Taira still runs native117 (`d0e1ee61b7c69acb6aa6ffb516a470fa568c44e0`) with retained history; native118 rollout and physical DPN/paid namespace activation remain pending. The validator host is MacStadium in Dublin (`test.inori.co.il`), with four distinct authenticated HTTPS roots on `taira.sora.org:8443` through `:8446`; ingress installation and TLS checks are complete. Company validators do not use AWS. The [September 13 basic acceptance](docs/history/2026-09-14/taira-native117-basic-acceptance.md) is historical, and API22/FE17 recovery remains coordinated with its application owner. The candidate binds SNS, pipeline and account-alias absence to exact request selectors, preserves native alias diagnostics and shares one SDK transaction-wait deadline across dispatch, sleeps and outcome admission. Focused13 passed 29 selected native unit regressions, plus eight deadline/fixed-failure regressions separately on its matching SDK artifact, but rejected the paid alias transaction: an obsolete activation branch routed the universal fee transfer into DPN. Alias registry routing is now unconditional; the activation parameter, validator and codec identity are retired. Focused14 passed seven routing regressions, four configuration checks, 220 FSM checks and six lifecycle checks. Its clean-client journal advanced all three phases and authenticated carriers at heights 4, 7 and 10, but the unchanged 180-second operation deadline expired during final completion verification; no completion receipt was produced. The last printed Queued report was stale. The source correction runs independent validator reads concurrently with one authenticated successor-chain and journal owner. Apply now waits through the retained phases in one invocation, uses one deadline for HTTP and proof admission, and retries only explicit proof-sync or validated peer progress. Both status scopes validate before any pending interpretation; fixed proof or identity errors cannot hide behind another peer's pending state. Completion requires fresh challenges, exact carrier bytes and all four validator states, including after a previous completion receipt. Duplicate certificate QC verification is removed after strict storage readers, preserving their validation and durability checks; no runtime speedup is yet claimed. The current helper/guard checks pass 261 Python tests and 588 subtests. Build progress now distinguishes requested and observed Cargo targets and fresh versus rebuilt artifacts while preserving both production and test feature graphs. Focused15 passed all 23 selected native unit regressions, four configuration checks, 220 FSM checks and six lifecycle checks. Its single-apply clean-client case advanced the three retained phases to Applied at heights 4, 7 and 10 but failed initial four-peer finality verification: validator 4 returned a generic attestation 404. No finality cache or completion receipt was published. Sumeragi status reports the committed decision height, which may lead applied state. The candidate exposes exact request-bound HTTP409 tip progress separately from fixed proof failures; the SDK and CLI preserve that distinction within the original 180-second workflow budget. Focused16 passed test metadata and code generation, four configuration tests and all five selected SDK regressions, then stopped because the release gate named an existing OpenAPI test incorrectly; its remaining unit tests and clean-client case did not run. The corrected gate is paired with an ordinary closed-prefix drain that avoids repeating broad lane recovery per retired occurrence while preserving sealed Completion work, fresh lifecycle readiness and the final durable proof audit. Focused17 passed all 24 selected native unit diagnostics and four configuration tests, then built the shipping binaries. Its clean-client case reached catalog and bootstrap Applied but exhausted the unchanged 180-second operation deadline while waiting for aliases Applied; it produced no completion receipt and did not reach finality, repeated-apply or idle acceptance. A concurrent checkout fast-forward changed two selected Core state files during compilation, so this run is diagnostic evidence only; no unchanged-source pass or latency gain is claimed. Build progress continues while the compiler emits no new artifacts. Authenticated preparation now verifies its six live bootstrap sources and loads the separately authenticated native gate only from its fixed source capture; development checks import the mutable gate explicitly. The signed candidate also carries the maintained environment-resume tests that match its existing preparation behavior. Signed source39 immutable qualification stopped on six alias-routing regressions before native network acceptance or ARM release compilation and remains unqualified. The public faucet policy endpoint, route catalog, OpenAPI, SDK inventory and read-only MCP discovery are implemented, with seven policy regressions added to both development gate scopes. A separate native117-based policy successor preserves deployed runtime semantics while its immutable qualification and ledger-preserving rollout remain pending. Public policy discovery does not supply an independent faucet authority pin. Clean-client acceptance, full native release qualification and live deployment remain pending. These first-release fixes introduce no compatibility branches or empty blocks. Runtime credentials remain owner-private and outside Git.
