The [Sumeragi liveness redesign](specs/sumeragi_liveness_redesign_goals.md) is active; all six goals remain open. A four-validator outage reproduces a silent initial lane author: the surviving peers finalize the transaction admission and continue global timeout certificates, but the lane cannot start a replacement proposal without an existing payload. The shared-reducer replacement now has transactional frozen lane contexts, canonical first-admission ordering and global-finality proof retention. Its canonical model and physical WAL foundation passes all 110 selected Core tests and 66 model tests, with no ignored tests. A disk-backed three-of-four pre-payload timeout survives restart and carries a lagging fourth reducer into the next view. The signed availability-manifest binding correction also passes all ten affected Core and nine model tests. The review-fix candidate’s Core and Torii test binaries compile and report 487/489 Core and 61/61 Torii passes. Actual 800 KiB admission batching within a 2 MiB carrier, signed 160 KiB gossip within 256 KiB, deferred custody, and cold-state recovery pass. The shared decoder keeps the 1 MiB input cap. The 17 scoped formal ledger/publication checks pass. The combined library-test build and all 16 admission model tests pass, including exact-cap decoding and stricter caller-budget rejection; Core/Torii artifacts match the tested source exactly. The separate completed-publication repair correction now passes its combined build, all 133 affected Core tests and 61 Torii tests, including full economic recovery and second-cycle replay. All eight new repair controls pass. Live certified archival was an invalid positive fixture; refusal and actual completed-secondary archival are being tested separately. The latest combined build passes: 22 model and 61 Torii tests pass, with 187/189 selected Core tests passing. All-route input preparation, exact native RS16 and replay-complete WAL witnesses pass. The successor Core build and all 46 affected checks pass, including both corrected fixtures and actual completed-secondary cold reopening. Thin durable native input storage rejects corrupt, foreign and rebound paths and remains fenced after publication/readback failure; native effect projection preserves exact signed proposal bytes. The 22 selected inventory and admission-binding checks pass after reviewing the actual include additions; the full structural gate still reports 148 semantic-binding errors. These are boundary tests, not full Core/workspace qualification. The latest Core build and all 55 affected tests pass, including the process-lived pre-payload owner under output saturation/restart/global advancement and exact all-route Decision joining. Production construction, body workers, economic consumption and old-signer retirement remain open. These checks do not qualify a live replacement driver or four-/seven-validator network fault and restart behavior. See the [scoped foundation record](docs/history/2026-09-16/sumeragi-lane-context-foundation.md) and [preserved prior evidence](docs/history/2026-09-16/sumeragi-liveness-status-before-lane-context.md).
