The [Sumeragi liveness redesign](specs/sumeragi_liveness_redesign_goals.md) is active; all six goals remain open. The runner derives permissions from actual retained work instead of remembered completion history. The Store handoff fix passes 11 selected controls; the broader ownership run passes 1,249 tests with two stale source assertions subsequently corrected and passing, and one intentionally ignored incident inspector. An earlier candidate separately passed 416 runtime/protocol tests and 50 owner/Serve source controls. An actual serialized-runtime/worker regression reproduces lost Fetch authority after a signed timeout; preserving existing disk-persistence custody makes both protected and unprotected cases pass, alongside exact-capacity and direct reconstruction-race controls. The combined Decision cancellation/permanent-retry candidate passes 11 focused controls on 1,609 unchanged local inputs, including actual launched Validate admission gates. Its 17 scoped adverse mutations are rejected; the native cancellation proof, broader source obligations and full launched qualification remain open. The combined 1,679-test Core run has 1,675 passes, three source-contract failures, and one intentional incident-inspector ignore; its inputs remained unchanged. After correcting the stale timer and persistence-cleanup clauses, all 14 selected behavioral/source checks pass on a rebuilt binary with 1,609 unchanged local inputs. The fifth unchanged four-validator attempt passed canonical block-query controls but failed during the outage: height five committed the transaction’s QueuePlan admission, then no execution proposal appeared before its 600-second finality deadline. All three survivors continued certifying timeouts through seven views. All four peers exited normally; no restart or network liveness pass is claimed. Native BLS/PoP-authenticated lane frontiers identify the stopped peer as the next lane author; the current NewView path requires a payload and cannot replace a silent initial author. Two native counterexample/control tests reproduce that gap. The shared-reducer replacement is under implementation; its explicit external-finality anchor passes all 224 reducer unit tests, while native adapter integration and frozen-context recovery remain open. The harness corrections and structured SDK errors pass 33 query tests; seven-validator and seed-matrix execution remain pending. The next runtime candidate replaces its cached owner list with a fresh validated borrow and passes four focused census controls plus all 418 runtime/protocol tests on its retained binary. Runtime consolidation, complete ownership/resource coverage and unchanged-candidate network/release qualification remain outstanding.
