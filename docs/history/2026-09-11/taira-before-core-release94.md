# Taira status before core release94 reconciliation

These exact former root paragraphs are retained as incident history. Their pending/NO_GO statements describe preceding attempts and do not report current deployment status.

## Former status paragraph

The immediate public Taira rollout is scoped to basic BPNG connectivity: four validators, public Torii/MCP, onboarding/faucet, exact Applied transaction proof, restart persistence and application access. Preparation59 at source `388266f83f568c0a9c94daa16d05396999476854` passed all 301 basic checks, including real four-validator transactions, a signed-snapshot restart and exact postrestart Applied proof; its Linux release build also passed. Actual release90 reached native apply step 7, then failed readiness because the CLI sent `Accept: application/json` to the text/plain `/readyz` route, which correctly returned HTTP 406. All four validators rolled back and the public edge was untouched. Preparation60 at source `b6d24dc38ed8c980fc6a9db287d37dcf0e8d7b35` passed the corrected readiness middleware regression and all 301 basic checks, including the real four-peer signed-snapshot restart and postrestart Applied proof; its warm Linux build passed in 65.33 seconds. Actual release91 then failed its command-line bound check while observing validator 3 startup after `/proc/PID/exe` identified the daemon. The retained error did not distinguish empty, oversized or unterminated input; the missing empty-command-line startup state is consistent with Linux exposing the new executable before publishing command-line bounds during exec. Service logs showed no preceding crash, OOM or panic. All four validators rolled back cleanly and the public edge was untouched. Preparation61 at source `8644f86197eba4d8443116112cbffb4c704fe3b6` passed all 301 basic checks, including the real four-peer signed-snapshot restart, and completed the warm Linux build in 80.21 seconds. Release92 passed all four restart-and-write waves after read-only recovery of a transient launcher observation. It reached public edge verification, where the doctor rejected HTTP 200 responses from time and MCP tools/list; all four validators and the edge rolled back without failures. The next correction accepts both canonical MCP tool namespaces, separates basic connectivity from advanced product and synchronized-time qualification, and shares the doctor check definition with its deployment consumer. The signed core scope is being reduced to one restart with existing postrestart onboarding, faucet and Applied write checks. A maintained unit renderer now emits Type=exec, preventing systemd from announcing startup before executing the custody launcher; its disposable FD-custody tests pass. Preparation62 passed the new doctor producer/consumer and restart-plan regressions and every other independent check except an older core argv fixture that still requested four waves. That fixture now preserves 27 basic and 54 full-scope valid cases and explicitly rejects the 27 extra basic-wave cases. Preparation63 at source `f5b5a658daf67bc0ff3891300a7679d2b0ea6b77` passed all 310 basic regressions, the actual four-peer Applied/snapshot-restart test and its warm Linux build in 71.96 seconds. Release93 passed initial canaries/convergence and its single restart plus postrestart canaries, then failed EdgeVerify on MCP tools/list despite HTTP 200; time qualification passed. All four validators and the edge rolled back with zero rollback failures. Source review found count-only MCP catalogue pagination can exceed the HTTP response byte bound; the discarded diagnostic prevents identifying the exact release93 JSON-RPC error. The correction bounds pages by their decorated wire size and preserves bounded public error codes. Preparation65 passed the native build and CLI checks, then the new actual-catalogue transport regression reproduced a serialization failure before Linux compilation or deployment. Expanded schemas exceed Norito’s 32-container bound after adding the MCP envelope, including the curated Musubi release-publish descriptor. Advertised schemas are being factored into self-contained local definitions while exact expanded argument validation remains unchanged. The basic network gate now runs the actual same-revision doctor against the daemon with the verified deployed curated Writer profile before release compilation. This next correction still requires its regression run and rollout. Public Taira and BPNG/test.inori remain NO_GO. Native preparation now explicitly distinguishes the default basic deployment census from optional full regression qualification; both use the same warm Cargo graph, and the selected scope is bound to preparation and resume records. Advanced history/fault and proof-production matrices are deferred from this first basic rollout. Preparation58 passed all 298 independent basic checks, then failed fresh four-peer startup before genesis in 19.3 seconds because the lane-evidence repair fence still equated empty replay with open admission. The repair fence now requires the explicit closed startup phase; the pending-Kura handoff also clears completed reconciliation before entering its successor. Both directly affected runner checks joined the basic census and passed in preparation59. Release87 exposed missing ordinary lane frontier publication: ordinary slots 1/2 were Applied, but the next certified autonomous slot 3 remained unselectable. Both ordinary execution paths now stage and verify the exact shared frontier before the execution witness and global vote. Preparation46 passed 427 selected regressions, failed one new fixture in setup, and did not run the remaining four-peer test; its earlier 428-pass count was incorrect. The corrected preparation47 passed all 429 selected regressions, including three consecutive transactions with local/global Applied proof on four validators, and completed its Linux release build. Release88 passed initial convergence and pre-edge canaries, then failed its first restart: durable merge-ledger reconstruction expected autonomous slot 1 even though ordinary execution had already consumed slots 1/2. Validator 1 could not restore its height-6 snapshot, and Kura correctly required that snapshot after recovery-floor compaction. Cold reconstruction now uses an explicit authority bound to the signed merge entry and its verified global carrier, with descriptor-shape, replay/regression and adjacent predecessor-hash checks; live execution still requires the exact shared WSV frontier. The mixed-history regression now restores the actual snapshot, applies a real successor, evicts the earlier merge body and restores again using retained finality. Three independent source reviews pass. Preparation48 stopped at the enum-layout lint before native regression execution; both validation authority variants now borrow their inputs. Preparation49 passed all 439 regressions and its Linux build. Release89 passed the pre-edge writes and restored the height-6 snapshot, then failed the next startup step: snapshot decoding replaced prior dataspace/staking policy with defaults, producing a false live-stake ownership change. Release89 is fully rolled back on all four validators, without rollback failures or edge cutover. The next source candidate retains the canonical prior owner policy in the required snapshot layout and freezes manifest/compliance inputs before authentication and replay; custody guards remain active. Its mandatory four-peer test now includes an actual signed-snapshot process restart between Applied transactions. Startup now checks consensus-key policy against canonical state before starting Kura instead of overwriting it after replay. Preparation50 caught a missing explicit Norito enum layout before tests. Preparation51 compiled the complete native harness graph, then reported four failures in two new fixture setups: an autoscale range exceeding the unconditional cap, and omitted process-local execution policy after snapshot decoding. The preparation52 early diagnostic passed the corrected runtime-policy fixture and exposed the shared staking helper's invalid zero activation height. The snapshot fixture now supplies a positive activation height and validates both numeric-ledger contracts before serialization. The maintained runner now executes both short startup groups after configuration/CLI checks and before expensive proof/consensus cases, retaining the complete census and exact-checkpoint contract. Preparation53 passed every independent regression, including all 18 early startup checks. Its four validators applied two transactions through global height 7, and the restarted validator loaded the signed height-7 State snapshot. It then rejected completed historical bundle recovery because repair incorrectly re-admitted an old certificate against the latest live frontier. Completed slots now consume only exact readback components; only the current unfinished publication may be reconstructed. The adjacent source audit found that startup checked historical cross-pair joins before completing interrupted pruning. Recovery now distinguishes the authenticated discarded prefix from retained history, validates existing pair rewrites before promotion, and admits all outstanding publication capacity before finishing appends and fresh compaction. All three strict startup/restoration entry points share this order. Later State repair omits obsolete singleton work, and canonical empty sidecar rewrites recover without recreating discarded input. Preparation55 compiled the combined native harness graph in 213.6 seconds, then failed eight early cold-storage regressions before production compilation or network execution. Six new fixtures omitted signed lifecycle custody, one incorrectly expected no terminal reservation, and startup discarded authenticated append-preimage rows before rereading them through the strict live-pair path. Preparation56 passed all independent native checks, including all 38 early startup checks and the 18 interrupted rewrite combinations. All four validators applied two transactions through height 7; the restarted validator loaded its signed snapshot and completed Kura recovery. It then accepted a new QueuePlan before empty-journal startup reconciliation finished, invalidating the runner's exact ownership cut. Empty replay now retains the admission gate until State/Kura completion, and readiness reports the same gate before restart canaries. Preparation59 qualified this correction in the basic native network and Linux build; the release90 HTTP readiness mismatch passed its correction in preparation60; release92 public doctor contract remains the current public-rollout blocker. Iroha does not create empty blocks: idle height is expected, including after restart; actual transaction gates require exact Applied proof. The disposable devnet restart accepts unchanged committed height while preserving process and workload continuity checks. Public health and BPNG/test.inori application connectivity remain unqualified.

## Former roadmap paragraph

The immediate Taira outcome is a basic testnet for BPNG. Preparation59 source `388266f83f568c0a9c94daa16d05396999476854` passed all 301 basic checks, the real four-validator signed-snapshot restart with postrestart Applied proof, and its Linux build. Actual release90 native apply step 7 then failed because readiness requested JSON from text/plain `/readyz`, receiving HTTP 406; all four validators rolled back and the public edge was untouched. Preparation60 source `b6d24dc38ed8c980fc6a9db287d37dcf0e8d7b35` passed the readiness correction, all 301 basic checks including the actual four-peer snapshot restart and postrestart Applied proof, and its Linux build in 65.33 seconds. Release91 then failed validator 3 startup observation at its command-line bound check; the retained error did not identify which predicate failed. Audit found an unhandled Linux exec window where the daemon executable is visible before command-line bounds are published. No preceding service crash, OOM or panic was observed; all four validators rolled back cleanly and the public edge was untouched. Treat an empty command line as pending within the original startup deadline while retaining full process attestation, verify that correction, then deploy and check the basic public/application path. No public GO is established. Validate the reviewed shared ordinary/autonomous lane frontier through snapshot restoration and cold durable merge-ledger reconstruction, including canonical historical carrier authority, ordinary/autonomous interleaving, and subsequent execution after restart. The mixed-history fix passed all 439 preparation49 regressions and restored the actual release89 height-6 snapshot. Release89 then exposed missing prior Nexus policy during startup hydration. Qualify the canonical owner-policy snapshot, typed frozen AMX/execution-policy inputs, read-only canonical key-policy check and real four-peer process-restart regression before the next rollout. Preparation53 passed the corrected fixtures and all independent checks, then exposed historical certified-bundle re-admission after successfully loading the four-peer height-7 snapshot. Qualify complete-history readback, current-only partial repair and authenticated interrupted-pruning recovery with cold-storage negative controls in the early gate. Preparation56 passed the corrected storage gates and restored the actual height-7 snapshot, then exposed premature admission during empty-journal Queue startup. The closed-until-completion gate and strict durable-claim retries passed preparation59; preserve that behavior and the qualified HTTP readiness correction while completing process-startup observation. Use fresh first-release state; do not patch retained snapshots or fabricate predecessor evidence. Complete four-validator convergence, onboarding, faucet and exact Applied writes, restart persistence, public Torii/MCP access and BPNG/test.inori connectivity under the `core_testnet` scope. Preserve the qualified genesis/network identity and private runtime custody. Advanced Inrou workloads remain separate. Preparation58 proved the Queue/readiness unit changes but exposed the remaining fresh-start repair-fence mismatch. The corrected fence and completed pending-Kura phase handoff passed preparation59, including the actual network restart; complete the fresh rollout after the process-startup observation correction. Use the explicit basic native check scope for this sunny-day rollout; full history/fault and advanced proof qualification remain separate. Reuse the warm build lane and exact-request checkpoints. Idle block heights are expected, including after restart; use pending-work and exact transaction evidence to judge progress. Release92 passed all four restart-and-write waves, then fully rolled back validators and edge after a public doctor contract failure. Preparation63 passed the corrected MCP namespaces/shared doctor contract, Type=exec startup and one core restart wave. Release93 proved initial and postrestart transactions/convergence, then rolled back at the MCP tools-list doctor check. Qualify catalogue pagination against the actual decorated HTTP response byte limit and compact advertised schema references against the JSON nesting limit, preserving exact argument validation. Keep bounded semantic diagnostics and run the real CLI doctor against the four-peer fixture with the deployed curated Writer profile before release compilation. Complete the basic public cutover and separate application qualification.

## Former current-table rollout rows

These exact rows predate the release94 reconciliation and are retained only as historical evidence.

| Taira reset and startup | Cold-start snapshot handling now treats an owned root with no CURRENT pointer as a fresh chain; all three regressions pass. Validator convergence and restart wait for HTTP readiness inside the existing authorization deadline; all 55 focused release checks pass. Cargo cache retirement preserves the opposite profile family; four focused checks pass. The same approved VM is now 80 GB, with 55.5 GB guest and 41.2 GB host space available after closed public-payload cleanup. | Public Taira remains unavailable pending deployment of these corrections. Release75 passed preseed and process startup, then rolled back completely after an early doctor check; the actual cold-start snapshot error is fixed in source. The complete capacity plan includes all four writable Inrou runtimes and passes 18 helper tests. Finality, signed canaries and test.inori.co.il connectivity remain pending; no release GO is claimed. |

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Complete the approved public reset with same-source Linux binaries, candidate convergence/canaries/restarts before cutover, preserved public Host routing, and test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Replace per-release adapters with shared native read-only input/progress checks and immutable preparation reuse; run composed FD, inventory, shared-deadline transport and stage-consumer gates before heavy builds, retaining submitted ambiguity in one native apply recovery journal. Qualify the complete onboarding/faucet/final-canary transport lifecycle before rollout. Measure executable-only revision changes in the release build lane without invalidating shared libraries. |
