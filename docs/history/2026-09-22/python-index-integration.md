# Python original-index integration checkpoint

Work remains in `/Users/takemiyamakoto/devstuff/iroha`, branch `optimizations`.
Observed HEAD is `7249f17f0b3ac05552e025bd938f99e1ae5d23d2`; no immutable release
candidate was sealed by this work. The implementation keeps one V1, without
compatibility shims or an HSM prerequisite. All fourteen overall release goals
remain open.

## Implemented joins

The [original-index verifier](../../../specs/sorafs/python_index_adapter_v1.md)
consumes the actual held SDK package, execution archive and all twenty original
input roles. Independently supplied runtime/dependency pins, a clean matching
trusted source, native extension and ABI manifest, complete installed content,
nine fixed commands and the full child output frame must agree. The verifier
returns scoped immutable observations; it grants no producer or release authority.

The live producer and captured replay share the report-origin relation for
Python, pytest, both installed packages, loaded modules, source dependencies and
retained metadata. Content results never become physical file owners. The child
uses one pure input parser followed by its existing live path checks; replay
does not reopen historical paths. Producer and verifier both require the
explicit POSIX `_crypto.abi3.so` profile, independent of verifier-host suffixes.

The fresh virtual environment has exactly eight bootstrap files. All three
interpreter copies join the selected original bytes, and the configuration
matches the fixed operation. Dependency console names cannot occupy bootstrap
paths, including case aliases. The producer retains identical bootstrap bytes
through installation, execution and final publication. Activation scripts remain
bounded unexecuted generated output. Complete environment ownership is checked
by both live and captured consumers.

Review found and fixed three further joins: Boolean/integer equality in derived
dependency rows, the need to repeat the package-source census before return, and
a missing bound on reported wheel seal strings. A source added after the initial
census and a late replacement of an original indexed file now reject before
observations return. No original indexed input can be ignored or reused as a
second role.

## Validation

The combined source-observed run passes **2,017 tests**, with zero failures,
errors, skips or source drift. Driver elapsed time is **91.88 seconds**. Exact
commands, JUnit output and source hashes are retained in
`target/first-release-python-index-integrated-20260922/`; its source-observation
SHA-256 is `0c26562ef61cb2640303db8f0e9a529b08ef714149a5dd3153b407a7c70c9886`.
This includes the unchanged native/SDK wheel fixture comparisons, existing
producer/child/custody controls, new pure input and bootstrap controls, shared
report origins and the complete six-consumer index adapter controls.

Earlier focused results are retained: 307 origin/bootstrap/input controls pass
in 4.70 seconds and 32 top-level adapter controls pass in 6.04 seconds. Two initial
fixture/assertion failures are preserved: a profile fixture hardcoded the wrong
dist-info version, and an original-file replacement was correctly rejected by
the held-descriptor check earlier than the test's expected diagnostic. Both were
fixed without weakening production checks. Logs are under
`target/first-release-python-input-profile-root.log`,
`target/first-release-python-index-adapter-root-1.log` and their subsequent runs.

The adapter positive fixture is deliberately synthetic: its native/interpreter
bytes are inert, its execution transcript is fabricated and candidate selection
is explicitly mocked. It exercises actual original file custody, archive,
source, installed-byte and report relations. It is not evidence of an executed
native SDK, an approved producer or release readiness. Existing actual fresh-venv
and nested-process controls remain separate. No final native package was built
or executed in this checkpoint.

Release automation validation, changed shell syntax and `git diff --check` pass.
The historical archive verification passes for 64,736 records and 67,311
occurrences. All new suites/helpers are registered in release CI. `status.md` remains at
300 lines and `roadmap.md` at 146. The source-budget guard still reports **276
findings**, with no new Python verifier finding and no raised limit; details are
in `target/first-release-source-budget-after-python-index-20260922.json`.

## Remaining outcomes

TODO: implement the four remaining concrete consumer adapters; connect selected
runtime/dependency pins and authenticated producer/operator approvals to the
signed aggregate, ReleaseManifest custody/completion and SF11 derivation in the
production cutover. Execute the fixed native-backed cases, full supported SDK
and platform matrix, and final matching-candidate qualification. Preserve the
separate privacy, consensus/resource, SoraFS service/topology/soak, independent
audit and signed promotion requirements in the integrated goal ledger. Neither
these script tests nor historical Rust results close those gates.
