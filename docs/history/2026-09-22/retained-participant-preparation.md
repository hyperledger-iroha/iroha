# Retained participant preparation custody

Cell, Storage and transaction membership now retain their original journals,
partial native acquisitions, installation admissions and actual release events
in caller-owned slots. Construction is inert; preparation borrows the installed
slot. Ordinary refusal recovers the exact original allocations and identities.
Caught unwind and terminal release grant cleanup authority only.

Membership retains the real advisory release before admission and the final
writer before predecessor comparison. Its convenience adapter returns refused
cleanup to the enclosing carrier; abort and publication carry the same preflight
notification. Storage keeps the original scoped Prepaid admission boundary.

## Captured component evidence

`dist/sumeragi-main-work/generation156-core/retained-membership-union-verification.json`
joins the unchanged component candidate:

- 643 Core controls pass, preserving all previous 636 and adding seven membership
  tests. Both ordinary and replacement journals use the default test stack.
- All 355 MV controls pass: 234 unit tests and 121 integration tests. Seven new
  unit controls exercise exact recovery, admission unwind, partial acquisition,
  late identity refusal, native poison and the original Prepaid scope.
- The Core and MV builds have zero compiler diagnostics and identical 7,532
  Rust/build inputs and Git metadata. Core executable SHA-256 is
  `c7784de1ba80d49ce7b89bf5443f63e5b02998b07853ee5316ed4da993e670f9`.
- The canonical structural gate passes on unchanged 12,269 inputs in
  `generation156-formal/canonical3`, including the same Rust source hashes.
- All 153 membership structural controls pass on 1,385 copied inputs, preserving
  the 130 prior selectors. The lower focused run passes 99 controls on 1,692
  copied inputs: its baseline, 60 prior controls and 38 new mutations. All 851
  prior native-preparation selectors remain in the 889-case inventory.
- The scoped Prepaid compile-fail doctest passed on the preceding component
  capture. The later public cleanup transfer must be checked again with its
  aggregate integration candidate.

The first Core compile retained three stale test tuple consumers; they were
fixed before the successful second build. Earlier formal failures exposed two
non-unique authorization anchors and a new mutation that changed only a panic
message; the defining bindings and executable mutation now pass. These failed
runs remain in their original artifact directories. Codec guards pass. The
workspace format check found vendor edition formatting, which is corrected in
the following aggregate candidate rather than changing this captured source.

## Integration boundary

World, runtime, TriggerSet and the complete carrier are the next integration
candidate. Their component preparation must borrow one enclosing owner from
before the first probe through the final handoff. All real writers and
State/Queue/Kura fences must release before payload destruction or callbacks.
Component pass counts do not qualify those subsequent source edits.

Complete resource admission, the production Validate-to-Apply cutover and the
unchanged four-/seven-validator loss, restart and final-transaction qualification
remain open. No release-readiness claim follows from structural bindings. All
L1–L6 remain open.
