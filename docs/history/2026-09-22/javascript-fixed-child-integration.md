# Fixed JavaScript child and runtime review checkpoint

Work remains in `/Users/takemiyamakoto/devstuff/iroha` on `optimizations`, with
initial HEAD `bdadfae6175a89e5fdb56292a5a64b93b44a231f`. This agent created no
branch, worktree, commit or release candidate. All fourteen integrated release goals remain
open; this record establishes component implementation and scoped validation.

## Integrated child and CI changes

The fixed Node 24 child now retains the original inherited input descriptor,
native cache/snapshot observations and prepared session through the six original
test registrations, final hook and actual structured test-stream EOF. The input
has one closed schema, the original 191-file core and a separate eight-file tool
inventory. Installed public/private subjects are selected explicitly. The
original 55 cases and 172 assertion expressions are unchanged. Source, ABI,
cleanup and module-loading observations refuse incomplete execution; cleanup
must finish before the child emits its observation frame.

The CI workflow runs dependency-free child controls immediately after Node 24
setup. TypeScript-dependent source contracts and the original suite-structure
controls run after the existing locked npm installation, before the existing
native build. The Python ABI-policy contract is in the actual release pytest
batch. Mutation controls protect the triggers, executable registrations and
dependency ordering. No runtime matrix, native implementation or source seal
was substituted.

The twelve-file child patch is `0be7b22695880093c7e405365c062c2def647a91dabbed1c5356cdbec5fa4827`;
the seven-file CI patch is `4d2f4ea9918f22053f2ab27bb9a9bd2ab9d64f47172613591b826828f3f36bdd`.
The application receipts in `target/first-release-javascript-child-application-20260922.json`
and `target/first-release-javascript-child-ci-application-20260922.json` bind their
exact pre/postimages. Existing unrelated working-tree changes were preserved.

## Validation and limits

The root independently reviewed the five production modules and ran 59 new
Node controls, 16 unchanged suite-structure controls and six Python ABI controls:
81 passed, with 426 observed inputs unchanged. After integration, the two full
workflow Python suites plus ABI controls passed **1,401 tests**. The combined
Node event/file/cache/profile/source/child suite passed **363 tests**. There were
zero failures, errors, skips, cancellations or TODO tests, and all 873 observed
source inputs and both selected executables were unchanged during the run.

`target/first-release-child-integrated-validation-20260922/identity.json`
has SHA-256 `022c785abe752774720dc1b32f50d77d268757218a7d02b28b7cebcacf668426`.
It retains exact commands, executable identities, counts, logs and the source
observation digest. The automation validator, both changed shell syntax checks
and `git diff --check` also passed. These counts describe overlapping component
work; the independent 81 are not additional release evidence on top of 1,764.

These tests execute inert filesystem/loader/test-runner mechanics, failure
paths and static contracts. They did **not** execute SDK/native assertion bodies
or load a rebuilt candidate addon. Loader hooks cannot independently attest
final compiled bytes: a later chained hook can observe originals and transform
them afterwards. The retained negative control makes this limitation explicit.

The source-owned parent process, complete stdout/stderr and exit-status join,
original-index adapter, authenticated runtime/native inputs and actual supported
platform execution remain required. The [child contract](../../../specs/sorafs/javascript_child_bootstrap_v1.md)
defines these boundaries. No child frame currently closes SDK or release gates.

## Runtime proposals remain outside canonical source

The configuration/Kura pool, typed restore, native retained-poison prerequisite
and membership replacement compose into 74 target files without merge conflicts
or canonical input drift. The draft patch is `0b32ae875085ed9be9518e3907ac99a8a2ad0bae4e8a8af56665bcc840bc2163`
under `target/first-release-history-combined-draft-20260922-2`. It is not applied
or compiled. Typed restore revision 2 preserves original resource refusals into
daemon startup policy instead of authorizing empty-State recovery from a local
capacity failure; the independent call-site review found no additional blocker
within that bounded scope.

Root review rejected native poison revision 1 because a repeated cleanup
handoff erased a recorded true poison verdict. Revision 2 preserves it and its
unchanged regression fails against revision 1. The author's copied component
passed 444 unit, eight integration and four documentation controls with exact
package identities from the proposed lock; this is not root graph validation.

A required preintegration issue remains in the membership replacement: current
native reads release their active-reader notification synchronously. A wake can
therefore reenter an enclosing State or snapshot fence. Logical preparation,
physical publication and retirement now separate their owners, but general read
scopes need the same explicit notification lifetime. Existing analogous
block-hash debt does not justify introducing that membership deadlock. The
correction must retain the actual notification beyond all enclosing fences and
preserve wakes, poison and refusal ownership. Full State funding, ordinary
execution retention and Native production cutover remain open.

## Subsequent concurrent source advance

Other work advanced the same `optimizations` branch during review, through
`4aaa84eeb8f8d9055b105b826e384f8f9c6e0fc8` to observed
`1b8e5f92b6dadb1dcdd16f945286560e31b347ff`, incorporating the JavaScript changes
and adding independent membership/Merkle-map/Norito and other work. The pending
74-file runtime proposal now has a changed canonical preimage at
`crates/iroha_core/src/state/storage_transactions.rs`; it must be reconciled
with the new membership-record/root owners before application.

`target/first-release-external-source-advance-20260922.json` records the read-only
comparison (`aa11d8bf0bc847b9e3da3213bc1d7f1e6b0eb472a0642a8da9b2f9bdae38a5ec`).
Three of the earlier 873 observed inputs changed after validation: the two
Taira release scripts from concurrent work, and the qualification-source spec
updated here to describe the implemented child. The historical test interval
still had zero drift; its result is not a test of every subsequent source.

The later source-budget diagnostic reports 276 findings across 12,687 files,
with no limit or baseline change. Its Norito finding differs from the prior
diagnostic because concurrent source reduced that file further; this is not an
immutable-candidate pass. Historical archive verification passed for 64,736
records and 67,311 occurrences. Root status/roadmap remain within 300 lines.
