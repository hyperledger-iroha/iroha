# Historical native117 basic acceptance

The following September 13 status paragraph is retained exactly. It is historical evidence, not current release readiness.

Public Taira runs `d0e1ee61b7c69acb6aa6ffb516a470fa568c44e0` on all four validators. The candidate passed all 439 basic native regressions, including three real four-peer Applied transactions with an authenticated snapshot restart before the third. The warm Linux build completed in 806.572 seconds and the state-preserving rollout in 274.245 seconds. All four validators restored authenticated snapshots, remained Ready with their original startup processes and zero restarts, and agreed on advancing committed frontiers. A fresh public transaction, `a520f2cd3c75f55982be7c82bc2c429abd0982e0f6799ca7f796ed52eb05244b`, is state-resolved Applied at height 1774 on all four validators, public HTTPS and the public MCP endpoint. The public basic doctor passed all ten checks. Fresh DPN contract checks accept its own authority and reject a foreign authority. API21 and FE14 release `20260913T044726Z-785c3f145260` are deployed to `test.inori.co.il`. Current-release verification, no-funds bootstrap, and normal authenticated login/wallet/balance reads passed; all three authenticated routes returned HTTP 200 and all 18 identity, MFA and permission checks passed. Independent public verification passed all 173 checks, covering the 166 reviewed frontend assets, API readiness and network identity, live Torii connectivity and denied secret-file paths. A fresh browser reload rendered the company sign-in page. Runtime signing inputs remain outside Git with restrictive owner permissions. After a temporary one-block follower lag, the final bounded observation confirmed all four validators Ready at height 1819 without restarts or intervention. This qualifies the deployed basic Taira and application path; advanced Inrou and full-workspace qualification remain open. Iroha does not produce empty blocks, so an idle height alone is not a failure.
