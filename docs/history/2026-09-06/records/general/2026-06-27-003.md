# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-1bcd4495b154fcde8f1eb61d5b0375ed13aa0cd3d2adc503255cbea50d5b12ba"></a>

<!-- Original context: Status / 2026-06-27 Nexus lane-geometry failure ordering -->
- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib relabel_kura_conflict -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib retire_kura_preflight_failure_preserves -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib retire_tiered_preflight_failure_preserves -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib preflight_lane_geometry -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib tiered_preflight_failure_preserves -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_kura_preflight_failure_does_not_create_tiered_lane_storage -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_kura_preflight_failure_does_not_create_tiered_lane_storage -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_aborts_on_storage_error -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_tiered_failure_does_not_create_kura_lane_storage -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus -- --nocapture`
    (`58` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_kura_preflight_failure_does_not_publish_staged_da_or_tiered_state -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_tiered_preflight_failure_does_not_publish_staged_da_or_kura_state -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_scale_in_kura_preflight_failure_does_not_publish_staged_da_or_tiered_state -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_scale_in_tiered_preflight_failure_does_not_publish_staged_da_or_kura_state -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit -- --nocapture`
    (`5` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_transition -- --nocapture`
    (`40` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_scale_out_height_mismatch_does_not_publish_storage_or_da -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_scale_in_height_mismatch_does_not_publish_block_local_cleanup -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_scale_in_height_mismatch_does_not_publish_block_local_world_cleanup -- --nocapture`
    (`1` passed)
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_transition_prunes_public_lane_economic_state_for_retired_lane -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_scale_in_height_mismatch_does_not_publish_block_local_world_cleanup -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_scale_in_kura_preflight_failure_does_not_publish_staged_da_or_tiered_state -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_scale_in_tiered_preflight_failure_does_not_publish_staged_da_or_kura_state -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_prunes_public_lane_economic_state_for_reset_lanes -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_exits_public_validators_for_removed_lanes -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_prunes_lane_state_when_lane_dataspace_changes -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_prunes_lane_state_when_lane_dataspace_changes -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib validator_lane_ids_for_peers_ignore_inactive_public_lane_records -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib stake_snapshot_from_roster_preserves_order_and_max_stake -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib active_topology_for_npos_prefers_active_validators -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib public_lane_validator_records_by_peer_ignore_mismatched_storage_key_rows -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib locate_validator_in_roster_prefers_matching_peer -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib soracloud_active_validator_authority_rejects_mismatched_public_lane_validator_rows -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib hf_active_validator_stakes_ignore_mismatched_public_lane_validator_rows -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib register_ignores_mismatched_public_lane_validator_rows_for_capacity_and_peer_binding -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib rebind_ignores_mismatched_public_lane_validator_rows_for_peer_binding -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib state_block_due_activation_ignores_mismatched_public_lane_validator_rows -- --nocapture`
  - pending after public-lane validator key/record consistency hardening,
    because existing cargo session `90379` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib finalize_pending_activations_ignores_mismatched_public_lane_validator_rows -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_same_shard_dataspace_rebind_persists_da_cursor_reset -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_recreated_lane_persists_da_cursor_reset -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_same_plan_recreated_lane_resets_da_cursors -- --nocapture`
  - pending after restarted recreated-lane operator-status addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_addition_resets_rehydrated_merge_history_for_recreated_lane -- --nocapture`
  - pending after restarted recreated-lane operator-status addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_addition_resets_rehydrated_merge_history_for_recreated_lane -- --nocapture`
  - pending after status-lock poisoning reset addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib reset_public_lane_staking_lanes_recovers_poisoned_status_lock -- --nocapture`
  - pending after reset-selector coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib lanes_requiring_state_reset_tracks_additions_retirements_and_dataspace_rebinds -- --nocapture`
  - pending after adversarial validator reset ownership addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib public_lane_validator_reset_updates_treat_key_or_record_lane_as_reset_owner -- --nocapture`
  - pending after path-level adversarial validator reset addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_exits_public_validators_with_embedded_reset_lane -- --nocapture`
  - pending after manual lifecycle path-level adversarial validator reset
    addendum, because existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_exits_public_validators_with_embedded_reset_lane -- --nocapture`
  - pending after autoscale path-level adversarial validator reset addendum,
    because existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_transition_exits_public_validators_with_embedded_reset_lane -- --nocapture`
  - pending after adversarial economic reset ownership addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib public_lane_economic_cleanup_keys_treat_key_or_record_lane_as_reset_owner -- --nocapture`
  - pending after path-level adversarial economic reset addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_prunes_public_lane_economic_records_with_embedded_reset_lane -- --nocapture`
  - pending after manual lifecycle path-level adversarial economic reset
    addendum, because existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_prunes_public_lane_economic_records_with_embedded_reset_lane -- --nocapture`
  - pending after autoscale path-level adversarial economic reset addendum,
    because existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_transition_prunes_public_lane_economic_records_with_embedded_reset_lane -- --nocapture`
  - pending after adversarial verified-relay contract-state selector
    contract-map and malformed exact-key addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib verified_lane_relay_contract_state_keys_drop_ref_or_envelope_lane_matches -- --nocapture`
  - pending after verified-relay hydration exact-key addendum, because existing
    cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib merge_candidates_ignore_verified_lane_relay_record_under_uppercase_digest_key -- --nocapture`
  - pending after `set_nexus` verified-relay recreated-lane cleanup addendum,
    because existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_recreated_lane_prunes_verified_relay_contract_state -- --nocapture`
  - pending after AXT replay selector addendum, because existing cargo session
    `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib axt_replay_ledger_keys_for_lanes_selects_target_lane_only -- --nocapture`
  - pending after routing default-dataspace validation addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_rejects_missing_default_routing_dataspace -- --nocapture`
  - pending after routing rule-dataspace validation addendum, because existing
    cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_rejects_missing_explicit_rule_dataspace -- --nocapture`
  - pending after default-lane autoscale-ownership precheck addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_runtime_elastic_range_rejects_default_lane_claiming_autoscale_ownership -- --nocapture`
  - pending after default-lane autoscale-ownership config-swap addendum,
    because existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_rejects_default_lane_claiming_autoscale_ownership -- --nocapture`
  - pending after shared cleanup fixture contract-map addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_scale_in_tiered_preflight_failure_does_not_publish_staged_da_or_kura_state -- --nocapture`
  - pending after shared cleanup fixture contract-map addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_retire_kura_preflight_failure_preserves_catalog_and_tiered_storage -- --nocapture`
  - pending after shared cleanup fixture contract-map addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib set_nexus_retire_tiered_preflight_failure_preserves_catalog_and_kura_storage -- --nocapture`
  - pending after shared cleanup fixture contract-map addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_retire_kura_preflight_failure_preserves_catalog_and_tiered_storage -- --nocapture`
  - pending after shared cleanup fixture contract-map addendum, because
    existing cargo session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_retire_tiered_preflight_failure_preserves_catalog_and_kura_storage -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib retire_kura_preflight_failure_preserves -- --nocapture`
  - pending after operator-status coverage addendum, because existing cargo
    session `97083` was still active:
    `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib retire_tiered_preflight_failure_preserves -- --nocapture`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib lane_segment_reconciliation -- --nocapture`
    (`3` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_tiered_failure_does_not_create_kura_lane_storage -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib tiered_failure_does_not_create_kura_lane_storage -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle_aborts_on_storage_error -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle -- --nocapture`
    (`49` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_failure_does_not_publish_staged_da_indexes -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`
    (`passed`)



<a id="record-77a1606b741ee913363e9a6ca6ef8d6c6df43737ee9163664d65abcd0f3ff0c5"></a>

<!-- Original context: Status / 2026-06-27 Nexus autoscale commit-staging hardening -->
## 2026-06-27 Nexus autoscale commit-staging hardening



<a id="record-736228f0a06b757e520c869cddbcb0b55dcaad2f4c7a6de15cdde6419abfecfe"></a>

- Ordered commit-time staging so the fallible autoscale lifecycle publish runs
  before DA commitment and pin-intent indexes are materialized; a storage
  failure while reconciling elastic-lane geometry now aborts before DA runtime
  or query indexes can leak from the uncommitted block.


<a id="record-b392dbdf8cbf3ae849b696d050c215f336a6988ce2aaeb8bbef0440869a4d45e"></a>

- Added adversarial coverage for an autoscale scale-out staged during block
  application followed by a `HeightMismatch` commit failure; committed Nexus
  state and transaction height remain unchanged.


<a id="record-003756c52cbe4eccea61c92210d00c0236e6ed46ec8c5c717540e2fb0355ba47"></a>

- Added adversarial DA coverage for a commitment and pin-intent bundle staged
  during block application followed by a `HeightMismatch` commit failure;
  committed DA stores, receipt cursors, and pin-intent aliases remain unchanged.


<a id="record-741d105ffa1f460317593b517264b066694227138e74fec5ff772a1a2d364af9"></a>

- Added adversarial mixed autoscale/DA coverage for a valid-height block that
  stages both an elastic-lane scale-out and DA bundle, then fails commit-time
  storage reconciliation; committed Nexus state, transaction height, DA stores,
  receipt cursors, and pin-intent aliases remain unchanged.


<a id="record-60e27e49c8575d69616daa64822fe0fa9dd5a1386430c637008a31db0b5fb63e"></a>

- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib storage_transactions -- --nocapture`
    (`12` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib apply_lane_lifecycle -- --nocapture`
    (`44` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib da_bundle_indexes_are_not_committed_when_block_height_mismatches -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib da_pin_intents -- --nocapture`
    (`4` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib da_shard_cursor -- --nocapture`
    (`9` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_commit_failure_does_not_publish_staged_da_indexes -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo test -p iroha_core --lib autoscale_transition -- --nocapture`
    (`40` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-da-staging cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-3fa1658ba959edcc5d2a7d04c48f2b3fc5109079f019b30ca1b957dc60d53b37"></a>

<!-- Original context: Status / 2026-06-27 Nexus public-lane economic reset cleanup -->
## 2026-06-27 Nexus public-lane economic reset cleanup



<a id="record-e536d1cffc0cdac4e84fef338102267ff6b8f0f103e7e6157cbf45fae2c7f79f"></a>

- Lane reset paths now prune public-lane stake shares, reward records, and
  reward-claim cursors keyed by retired or rebound lanes, including external
  `set_nexus`, manual lifecycle, and block-local autoscale lifecycle updates.


<a id="record-86071213b120e80b0ddaf52a4983643c02d83b18b04fd84ead222755f0e41847"></a>

- Public validator records remain retained for lifecycle/audit history and are
  still terminalized as `Exited`; live economic indices are deleted so a
  recreated lane id cannot inherit stale reward epochs or claim cursors.


<a id="record-053b4d8dabcb4dfe2c6df6cfb7cfb367c7c1e07aa09540b7abcbe099d892bb7a"></a>

- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib reset_public_lane_staking_lanes -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib public_lane_economic_state -- --nocapture`
    (`3` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib autoscale_transition -- --nocapture`
    (`39` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib set_nexus_prunes -- --nocapture`
    (`10` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib apply_lane_lifecycle -- --nocapture`
    (`44` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-401d3aa0c8cd708a774e7fc622448134560c3fba9358ae7c80ee519d394e56ec"></a>

<!-- Original context: Status / 2026-06-27 Nexus AXT replay lane-reset cleanup -->
## 2026-06-27 Nexus AXT replay lane-reset cleanup



<a id="record-d953db84bef83819d7077f9b4cb5c0cd0d83099389de8ac679d0a7209d80feb8"></a>

- Autoscale replay cleanup preserves cross-lane AXT replay guards whose handles
  target surviving lanes, so retiring the aggregate envelope lane cannot erase
  replay protection for a still-live handle target lane.


<a id="record-84ec95a3662a43955cc21d7d4d995068727cd7129ab14aca66887f4ead52d7dc"></a>

- Tightened AXT replay fixtures that use explicit lane catalogs so enabled
  Nexus and routing defaults match the tested lane/dataspace geometry.


<a id="record-5a1e223ad1ba8adbe3dba0d1b79077bcba606859ac8e25e106faadb89b228ee4"></a>

- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib autoscale_transition_prunes_axt_replay_ledger_for_retired_target_lane -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib autoscale_transition_preserves_cross_lane_axt_replay_for_surviving_target_lane -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib set_nexus_prunes_axt_replay_entries_for_reset_target_lanes -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib axt_replay_ledger -- --nocapture`
    (`11` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo test -p iroha_core --lib autoscale_transition -- --nocapture`
    (`38` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-recreated-verified-relay cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-21743629e42796e259e912e4c12306de16c2745cf5373983ed66e7f1db1b0590"></a>

<!-- Original context: Status / 2026-06-27 Nexus autoscale scale-in runtime cache cleanup -->
## 2026-06-27 Nexus autoscale scale-in runtime cache cleanup



<a id="record-1ad42a6d9afd5da5a5eb71752fcdc12608d1d262d3819d378c4b17b21c6f9431"></a>

- The regression verifies automatic lane destruction prunes the retired
  managed lane's scoped caches in the same committed transition while retaining
  unrelated default-lane relay state.


<a id="record-11bad08347d4a5916bbf90bbc3d307123b5b38286cf7674293182b49ac6e348f"></a>

- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-runtime-caches cargo test -p iroha_core --lib autoscale_transition_prunes_retired_managed_lane_runtime_caches -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-runtime-caches cargo test -p iroha_core --lib autoscale_transition -- --nocapture`
    (`35` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-runtime-caches cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-94f12ac712f22e943662cd9b5f1a22e41ce8500d5eb0fa7fcc92b12df384bb21"></a>

<!-- Original context: Status / 2026-06-27 Nexus queue autoscale routing-plan refresh -->
## 2026-06-27 Nexus queue autoscale routing-plan refresh



<a id="record-a232a5203f37773247864280bb03d7570e853fee7cf9ed84f38b6a894787812d"></a>

- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib proposal_queue_reconfigures_pending_default_route -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib queue_plan_journal_tombstones -- --nocapture`
    (`6` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-a214fff18fc2d1a2c3ea92e214ada29734a3425be080f52619067c488cb6a359"></a>

<!-- Original context: Status / 2026-06-27 Nexus autoscale failed-lifecycle cooldown guard -->
## 2026-06-27 Nexus autoscale failed-lifecycle cooldown guard



<a id="record-ad92a4b484cd499efaaaa0d4095f29922a4b98fbb8dd9e82c92aeb2883dc7909"></a>

- Added an adversarial autoscale regression for corrupted runtime dataspace
  state where hot/cold windows reach the internal lifecycle helper but the
  helper rejects the generated add/retire plan.


<a id="record-6404aacbc2cac4e7a80c2dbafe09a5b8a06a3c527321d551d3ee84d49cf7592e"></a>

- The regression proves failed internal scale-out and scale-in lifecycle
  attempts leave the lane catalog unchanged and do not advance
  `last_transition_height`, so a rejected plan cannot pin autoscale cooldown or
  suppress later legitimate scaling.


<a id="record-a4968ef6893f086fd23dbeb23fdec8401c599be5953b5d5e59fcc955e9d22a38"></a>

- Updated the Nexus lane docs and transition notes with the failed-lifecycle
  cooldown invariant.


<a id="record-1f053c5db91037cb7a21434c9b74d202dab08c3a787f2ac300a1c9a026f79ef7"></a>

- Validation status:
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib autoscale_transition_failed_internal_lifecycle_does_not_record_cooldown -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib autoscale_transition -- --nocapture`
    (`34` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-65efd97eb83c8fd32eda211c00636809778efae9a95076c699bdfc0ccdbd07e5"></a>

<!-- Original context: Status / 2026-06-27 Nexus autoscale fragment-history validation -->
## 2026-06-27 Nexus autoscale fragment-history validation



<a id="record-e6ec8e25f40492d0353a250c06071ee664721f5458fd4525c1d7dbffd060c1ea"></a>

- Added a block-validation regression proving legacy/forged zero
  `committed_fragment_count` metadata is normalized to the locally executed
  fragment count before accepted blocks expose load history to downstream
  autoscale sampling.


<a id="record-08703b20aba961bc1903d1b7e925152e2d2944eeaa301a52fa8588dfdd0fd310"></a>

- Rechecked the forged non-zero rejection and autoscale committed-fragment load
  consumers, including persisted historical samples read from Kura.


<a id="record-ae3d6b0a1d5f29fc00837f8402a5ee1394bfac8a21d935184e459ae1ac753200"></a>

- Validation status:
  - `cargo fmt --all`
  - initial two-filter cargo invocation was rejected by Cargo because only one
    test name filter is supported.
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib committed_fragment_count -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib committed_fragments -- --nocapture`
    (`3` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo test -p iroha_core --lib fragment_counts -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-autoscale-fragment-history cargo clippy -p iroha_core --lib --tests --no-deps -- -D warnings`



<a id="record-f775d1ccc87873f83632ad6e8659d6268e02e3bafa7bedf830a59cf0cf839a21"></a>

<!-- Original context: Status / 2026-06-27 - ISO Archived Text Normalization Hardening -->
## 2026-06-27 - ISO Archived Text Normalization Hardening


