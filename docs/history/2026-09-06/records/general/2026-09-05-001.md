# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-f1927194d516a3e45f47c04209000bfd4a0caff904a7c1ccefb3b813bf6678ba"></a>

<!-- Original context: Status / 2026-09-05 Atomic settlement historical authority candidate -->
## 2026-09-05 Atomic settlement historical authority candidate



<a id="record-17d3909887004e813178833c125d532223266534fcaff45b525f35404f154043"></a>

- The reviewed profile/lifecycle selection passes three tests. The resumed
  Orchard module passes 18, PQ fast checks pass 35 with one ignored full proof,
  and retained native preflights pass two. Their executable was built across
  the concurrent incentives extraction, so unchanged execution-time hashes
  do not establish frozen compilation provenance. The full profile diagnostic
  has 24 passes and the separate incomplete ZK-X509 failure.


<a id="record-2e8277f37231fa4aaf9b8b66111f66017052911cbd5fdfb46ade707554b47be9"></a>

- Model merge/private selections pass 44 tests; five event tests exposed a
  shared all-zero BLS seed before their filter assertions. The fixture now
  uses four distinct nonzero seeds with unchanged committee sorting, catalog
  construction and assertions. The combined Core/model/configuration run
  passes 232 tests (175 Core, 56 model and one configuration), including all
  five repaired event cases; one full proof remains ignored. Ten concurrent
  configuration, route-catalog and Norito source changes prevent that run
  from qualifying an unchanged-source full-PQ preflight.


<a id="record-a845c463169b195fea45a9c879acbea2b022d3f713ae647ba62bb183de45364d"></a>

- The next daemon attempt observed an intermediate schema-generator status
  migration. Current source imports `Status` and `Uptime` from
  `iroha_torii_shared::status`; the focused schema-generator build and all
  four registry completeness/overlap tests pass. The subsequent optimized
  daemon build passes, and current integration preflights pass 28 tests with
  five real-network entrypoints ignored. Both receipts retain source drift
  during compilation; they qualify development behavior, not frozen-source
  proof, real-network or release evidence. The next combined Core/model/profile
  validation stopped before compilation because the concurrent service-model
  extraction required a lockfile update. After that update appeared externally,
  locked offline metadata resolution passes with unchanged captured manifests
  and lockfile. The combined retry must bind unchanged source before the full
  PQ proof can run; this task has not edited Cargo.lock.



<a id="record-2958ceda9524a8cd3104dcc45f4afa6a67feab72f6b5a9363e6bda5696424b1c"></a>

- The shared source walker distinguishes effective untracked ignore rules from
  policies beneath directories already excluded by tracked rules. Cache policies
  cannot hide tracked inputs, and filename-only exclusions, local Git excludes
  and self-hiding untracked policies remain rejected. All 82 source-manifest
  tests pass, including eight new boundary cases. This removes a false refusal
  of existing ignored caches without accepting a dirty release candidate.
  The combined smoke/release-runner consumer selection passes all 70 tests
  with nine unchanged captured source hashes.



<a id="record-a72eca7a29dc81e84d1867c85c519fead7e7d2bec60af8f5237728435611102f"></a>

- Requalification now runs only in the original `optimizations` checkout. Its
  FastPQ, smoke-tool and release-runner suites pass 34, 27 and 37 tests,
  respectively; workspace formatting, whitespace and codec checks pass. The
  first Core feature build exposed two duplicate catalog initializers where
  explicit fields met shorthand fields during transfer. Those duplicates are
  removed; the empty-lane fixture branch is preserved and retirement catalog
  selection uses the same immutable view as lane/incarnation selection.
  The retry builds successfully. Native fixture preflights pass two tests,
  canonical-genesis fixtures pass two, PQ verifier independence passes one,
  and the PQ suite passes 35 tests with one full-domain proof ignored.
  The current private-settlement suite passes 93 tests while excluding the
  separately running native proof. Six immutable-authority/capacity tests and
  all six required historical-sidecar regressions pass at unchanged source.
  A further carrier-header fixture fails because its persisted parent chain
  lacks complete-wire signed finality; the fixture now supplies exact signed
  parent QCs. The rebuilt parent/sidecar selection passes all ten tests,
  including permissioned/NPoS chain verification, deterministic carrier headers
  and the formerly failing header-substitution negative.


<a id="record-029b0d830fb129b955b9f6d14fcef5c23d88e66e951c5e0f836c8a78450810db"></a>

- Current profile checks exposed stale expected digests. Five added GOST
  algorithm variants change IVM/Orchard reserve-account bootstrap schemas;
  canonical v1 wire KATs change the three PQ commitments. Seven expectation
  strings are corrected with unchanged mutation negatives. Separate ZK-X509
  checks retain the intentionally incomplete wide-MAIN proof geometry and
  correctly refuse activation; its activation/certificate test failures remain
  open. No proof cap or independent certificate pin has been relaxed.


<a id="record-0765f92b65f6eef3868578739486bc36c04ba35d6f7f8ce33313056d18160ac8"></a>

- The earlier optimized PQ-MASP full-domain run reached its final adversarial
  assertion and failed because its re-signed outer proof reused an inconsistent
  network/genesis pair. The facade correctly rejected that pair before inner
  verification. The corrected test preserves that negative, preflights a
  separate matching alternate network/genesis pair, independently verifies
  its fresh outer authorization, and still requires inner-STARK rejection.
  A fast inert-proof regression exercises that preflight boundary and passes
  in the current 35-test PQ suite. The corrected real full-domain proof remains
  pending. The failed full proof is retained under its prior binary/profile.


<a id="record-57d509c483fa06e9873e36a374efe5008c31479ee67ed8419b51700cae83b821"></a>

- All Iroha work now uses `/Users/takemiyamakoto/dev/iroha` on `optimizations`,
  as explicitly directed by the author. Task changes are reconciled with the
  existing staged work; the index and unrelated changes remain preserved.
  Earlier validation belongs to its recorded source and binaries and does not
  qualify this reconciled checkout. Academic manuscript and evidence material
  remain exclusively in the author's external BCK26 workspace.


<a id="record-24b9fe760a25c0c832c9c5a0ec5c436bdc1b6c0644d9210a64dff007e438a92d"></a>

- The real-process harness source contract now follows the current
  `TopologyShape::process_count()` function for all-process atomicity coverage;
  its focused Python suite passes 21/21. This corrects a stale test assertion,
  not a network execution result. The complete evidence-tool run passed 146 of
  147 tests; its sole stale-assertion failure is retained, and the corrected
  focused harness suite subsequently passed 21/21.


<a id="record-7d954765d55ab4091e7d6f2342492d592717adf2df4efd12893a6a8a38eeff73"></a>

- Execution-prefix selection measures the full catalog-bearing candidate.
  Prospective geometry admission reserves a 12-MiB execution batch, a 1-MiB
  bounded BLS QC, and canonical catalog/framing bytes without assuming roster
  overlap. Model tests accept 190 lanes with 127-member committees, reject 191,
  and admit 1,024 four-member lanes; these are size bounds, not network capacity.


<a id="record-0ebac454ae871bd3a763ac3f2393e82dd1afacd842cbe4f95969ef7b75655248"></a>

- A further smoke-evidence review identified missing intrinsic skip rejection,
  retained per-run state/finality artifacts, continuous finalization observation
  and a ten-fresh-run prerequisite. The Rust additions pass 28/28 module
  regressions with five real-process entrypoints ignored; they have not passed
  a real network run. The ten-run orchestrator passes 27 synthetic checks.
  It binds signed source, fresh builds, all 80 per-run artifacts, continuous
  response/phase chains and exact canonical hash markers; Boolean indices and
  embedded requests and unsigned Cargo configuration symlinks are rejected.
  Release execution now requires the completed signed-source smoke campaign
  before any job and revalidates it before completing the output fragment;
  its full Python suite passes 34/34, including source and retained-record
  substitution rejection and refusal to start work without the prerequisite. A
  checkpoint race in the shared observer's retained-response path was corrected:
  stale polls now change neither the accepted count/hash chain nor retained
  bytes. The pure stale-epoch/capacity regression passes. Two compile attempts
  exposed incorrect Norito macro paths and unsupported macro expression/borrow
  forms; explicit typed evidence records now compile and the failures remain
  retained.


<a id="record-4e050ec56595e684179cbb3174ca19c067477223a5f02fa5705a6f2337ebf0be"></a>

- Proof profiling found 128-bit division inside the portable Goldilocks digest
  reducer. Proven single corrections replace its unbounded subtraction loops;
  ARM64 and x86-64 optimized code-generation diagnostics contain neither
  division helper. Fixed digest vectors, 65,536 full-width modulo samples and
  boundary products pass. The adjacent exact qROM calculator now rejects
  numerator shifts that discard high bits, including collision-only overflow;
  frozen parameter selection is unchanged. The complete FastPQ crate passes
  34/34 and focused strict Clippy passes after explicit limb conversions and
  divisibility checks. The initial overflow reproduction and lint failures are
  retained. These checks do not replace full-proof or independent qualification;
  the three original live Core proof images still use the preceding reducer
  and parameters. A separate current optimized Core test build succeeded.


<a id="record-4f178b3122837030346dde223bf628669e4e2e849c6ef25917b82a4c0f7d54db"></a>

- Canonical evidence hash checks now use the standard-library CRC implementation
  with the same Norito polynomial, initialization and exact ASCII framing.
  Independent bit-loop vectors and malformed literals pass. Current smoke and
  release-runner suites pass 64/64 together (27 plus 37), with stable source
  hashes. This supersedes the narrower runner count above; counts overlap.


<a id="record-c5e1644e1c79aa4444521985aeb4155c0d9fc9712fbc11c031511c8eb90652e6"></a>

- Ten consecutive fresh sixteen-process N=3 settlements, the full fault/leakage/
  benchmark matrix, clean-source formal and release gates, independent audit,
  reproducible builders, and a DOI deposit remain outstanding. The feature
  remains disabled by default; no latency, privacy-campaign, or production
  readiness claim is made.


