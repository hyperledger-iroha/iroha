# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-7d5bb47be55f559d89a7fa9ee9670df225c1280920e3775dd68d239c36e10dd5"></a>

<!-- Original context: Status / 2026-09-05 Atomic settlement historical authority candidate -->
- The current daemon build exposed a missing direct `iroha_torii_shared`
  dependency after `NetworkAcl` moved to its canonical configuration module.
  The daemon manifest now declares that existing workspace crate. Locked
  resolution required its matching lockfile entry, which has now appeared
  externally in the shared checkout. The one-edge update changes no package
  versions, and locked offline metadata resolution now passes. The failed
  daemon build and initial locked-resolution refusal remain retained.


<a id="record-5c87dccc6284f1f469eaeeada8262ddc62c54977540b55bdbe337295508c35c9"></a>

- The smoke source seal now reuses the canonical workspace release-source
  checker instead of a duplicate blob-only inventory. Exact commit signature,
  tracked-byte checks, Cargo configuration restrictions and before/after
  identity equality remain mandatory. Canonical gitlinks are empty directories
  pinned by the signed parent tree; populated documentation checkouts remain
  separate unsealed source and are not changed by the driver. The focused
  smoke suite passes 33 tests, including actual byte/directory verification
  under synthetic Git metadata. Release-runner consumer revalidation passes
  37 tests at unchanged captured source. These development checks do not
  qualify signed source or a network run.


