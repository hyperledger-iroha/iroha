# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-07c3da9980ffca42867013438b793721e9aac9f41aca3ec47781f268fa741d92"></a>

<!-- Original context: Status / 2026-08-23 Workspace all-target compile and warning cleanup -->
## 2026-08-23 Workspace all-target compile and warning cleanup



<a id="record-62433abc36c6b3fdb3c432f684cf7369ccb8df6e4c2a62c7d4f70895f1aef7ea"></a>

- Repaired the reported compile failures and Rust warning backlog across
  configuration, data-model, Sumeragi, Torii, CLI, `irohad`, JavaScript host,
  Halo2, SoraNet relay, and VPN targets. The cleanup corrects test-module
  visibility and trait scope, removes stale imports, helpers, variants, and
  macro rules, avoids unnecessary `Debug` requirements in negative tests, and
  explicitly retains the unfinished Inrou launcher seams tracked by the
  first-release roadmap.


<a id="record-48197f366be0f740ddc3c0bd55ef69aa1fa71a20f68e254b9c6b36c179619181"></a>

- `cargo fmt --all -- --check`, `git diff --check`, focused package checks and
  regressions, and the workspace all-target check pass without Rust compiler
  errors or warnings. The two CUDA build scripts still emit their intentional
  host-compiler notices. The focused 50-case proof-ledger source-contract suite
  passes. An exploratory broad Inrou filter still encounters 14 unchanged
  fixture or host-environment failures; every modified diagnostic assertion
  reached by its fixture passes. The complete formal checker still reports 233
  broader baseline contract and seal drifts, so no full-formal closure is
  claimed. `Cargo.lock` remains unchanged.



<a id="record-bf8f73aceed32039529ce6bf50d1cc7f4bf5a275a3b0e426c92210f4b4ae99c1"></a>

<!-- Original context: Status / 2026-08-23 Taira canonical XOR precision and operator-contract hardening -->
- The repository and installed `sora-taira-testnet` skills now use the public
  UUID `fc56984b-2be7-431d-840e-21514d1883f0`, canonical Base58 asset IDs,
  curated live tools, strict routed-read completeness, `body_base64`, an
  explicit locally derived transaction hash, and matching terminal `Applied`
  receipts. Both skill directories pass the official skill validator.


<a id="record-53221074020dc0987336f8eea9f644c638353d18c43356cedc100323778482a8"></a>

<!-- Original context: Status / 2026-08-23 Optimizations merge reconciliation -->
- Workspace formatting, conflict-marker scanning, and staged/unstaged whitespace
  checks pass. The tail-publication ledger equations use explicit `u64` values,
  and the incomplete incoming Sumeragi call sites have one typed first-release
  implementation. A normal exact-profile build now compiles through ZK, Core,
  Torii, Irohad, and Kagami; its two follow-on CLI API-drift errors and one dead
  Torii binding were corrected before the final validation rerun.


<a id="record-9460bb720f480ef4ba2c8ca8035e0df3d10eac77002adbc41afe813a88cd4a05"></a>

<!-- Original context: Status / 2026-08-23 Taira disposable deployment hard cut -->
- The disposable four-validator workflow builds and verifies the exact
  four-binary qualification toolchain: `kagami`, `iroha3d_taira`, `iroha`, and
  `sorafs-node`.
  Process custody accepts only the dedicated `iroha3d_taira` daemon, runtime
  signer files remain owner-held, and every generated peer gets the canonical
  Taira storage overlay before config validation and startup.

