# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-9fd65e90d5f30764901c1763eec7ecd0fe5ea59995cb0bf53ce94418467ad281"></a>

<!-- Original context: Status / 2026-08-19 local Cargo iteration throughput -->
- Local runnable builds now have an explicit `local-release` profile: O1, 64
  codegen units, incremental compilation, no LTO, and no debug info. The
  production `release` and `deploy` profiles and the reviewed data-model
  optimizer ceiling remain unchanged.


<a id="record-aeb31fb810e7d4d9cde0dfd776479d04c93199a28da2e2b87a827e89467b0045"></a>

- Shared build support suppresses Git-ref filesystem invalidation only for the
  wrapper's `local-fast-build` `VERGEN_GIT_SHA` sentinel. Exact release source
  identity and the Sumeragi `IROHA_GIT_COMMIT_HASH` fingerprint remain
  untouched. The canonical `build_line.sh` also resolves and schedules the
  daemon and CLI binaries in one Cargo invocation instead of two sequential
  traversals.


<a id="record-67a0fccbc0d700e5e6d0f5a24d5ce71a9258cdc09e8b256a99c5c7b8c6bf1eee"></a>

- Focused wrapper, build-support, target-inventory, shell-syntax, profile, and
  unit-graph validation is green. Full-workspace validation remains
  outstanding.


