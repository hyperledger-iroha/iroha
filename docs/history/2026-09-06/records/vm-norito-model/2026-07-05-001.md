# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-2cd338d4a06d58ade765a6402d8feff966c4c74ec38c93111db5cb060de64168"></a>

<!-- Original context: Status / 2026-07-05 Nexus lane block vote templates -->
- Lane payload planning now derives full-committee prepare and commit vote
  templates for every standalone lane-block proposal. The signable vote digest
  binds proposal hash, descriptor hash, lane coordinates, subject, DA/RBC
  ownership, accepted transaction hashes, validator-set hash, quorum, and QC
  mode tag while excluding signer-local transport fields so validators sign the
  same aggregateable message.


<a id="record-b11815caad516dbe63efca3f3f4dd8172dc37e5c0fb9c1e9a5ef3c833ff5e56f"></a>

<!-- Original context: Status / 2026-07-05 Nexus lane payload ownership execution-context binding -->
- Validation: `cargo test -p iroha_data_model block_execution_context_bundle -- --nocapture`;
  `cargo test -p iroha_core lane_payload_ownership -- --nocapture`;
  `cargo test -p iroha_core --features sumeragi-main-loop-tests assemble_proposal_publishes_final_lane_payload_ownership_status -- --nocapture`;
  `cargo test -p iroha_core --features sumeragi-main-loop-tests proposal_multilane_ -- --nocapture`;
  `cargo fmt --all --check`; targeted `git diff --check`; `git diff --exit-code -- Cargo.lock`.


