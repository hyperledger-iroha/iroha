# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-24bfd97e97c79d30ef7de9247510bf192875613c0a523a62c3c7b1724fed9c8f"></a>

<!-- Original context: Status / 2026-08-17 signed-genesis owner bootstrap boundary -->
- Taira genesis grants now preserve Norito's typed permission payload shapes:
  field-bearing permissions carry their required objects while unit
  permissions remain `null`. This closes staged-genesis rejection of the
  domain-scoped `CanRegisterAccount` grant.


<a id="record-c5d36ee9dea5c4d8386fb4fa1c8b8a009089dba8962241a940412a151e36aac7"></a>

<!-- Original context: Status / 2026-08-17 explicit Taira receipt signers and lifecycle capture -->
- Deployment revalidates each rendered key pair and rederives the supervisor
  runtime and lifecycle bindings from the exact binary stat seal, config digest,
  restart generation, validator slug, and receipt-signer node ID. The former
  unconditional pre-deploy lifecycle node-ID refusal is therefore closed. The
  prerequisite audit now reports nine other authority stubs; deployment
  issuance and the public-soak observation authority remain fail-closed.


<a id="record-48871fab1f45af96c5f0916f15b88dd4af8afd295c1134cc2b5bb520568c99e0"></a>

- The path-only public-soak prerequisite producer now derives the candidate and
  publication handoffs from replayed admission bytes, the exact seven-file
  signed OCI publication, and the current installed `macos-publish` controller
  attestation. Two sealed authority operations inject that attestation, close
  root-owned handoff directories, and the publication workflow uploads both
  handoffs. The producer/publisher suite is green at 112 tests and the combined
  controller/workflow contract at 158 tests. A separate path-only deploy-handoff
  producer now closes the applied reset report, reset manifest, four independent
  native-receipt identities, candidate/publication handoffs, and replayed
  `macos-deploy` controller attestation into the exact public-soak checker
  schema. Its focused suite passes 12 tests, including direct checker admission.
  The deploy-native authority refusal remains ahead of caller path I/O, and the
  producer is not yet a controller/workflow operation. A separate protected-
  runner module now closes the exact 432,000-slot monotonic dispatch, process-
  bound native launch receipt, held lease, streamed capture inventories, and
  native backend-shutdown receipt behind an injected backend seam. Its public
  entry point remains an unconditional source barrier before caller path or
  network I/O because no genuine runtime signer/native verifier, replay broker,
  or durable admission authority is installed. The cross-slice runner/lease/
  handoff/checker/collector corridor passes 192 focused tests; no live 24-hour
  public-soak receipt is claimed.


