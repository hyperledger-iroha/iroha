# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-6096fa715ec086d517b95cce1e2f0ef36381e5e2e4d1e0bccad18913e66e58ec"></a>

<!-- Original context: Status / 2026-08-01 ABI-21 SDK/native gate closure in progress -->
## 2026-08-01 ABI-21 SDK/native gate closure in progress



<a id="record-d62a9fa7811106123b0bbaaaa6f72c42d50ebaad7146b512095ae12d7aff1b0a"></a>

- The hermetic C/JNI release gate now honors the explicit
  `NORITO_MOBILE_JAVA_HOME` JDK 21 pin on macOS before falling back to
  `/usr/libexec/java_home`. It still requires an absolute non-symlinked JDK
  directory, canonicalizes the resolved path, verifies Java major version 21,
  and hashes the selected tool. A local tool-resolution probe selected the
  pinned Python 3.12, Rust 1.93.1 Cargo/rustc, Xcode `llvm-nm`, Android SDK, and
  configured Temurin JDK without consulting ambient `JAVA_HOME` or `PATH`.


<a id="record-fea799bb56fc8495b7735b36a3df27efbc7a80324e2d5ca54162def38e954995"></a>

- The Python ABI-21 lane can now retain its verified payload-free artifact
  manifest after the native suite and zero-skip audit. Retention is opt-in,
  re-verifies the artifact and clean source, creates one fresh external
  non-symlinked `0700` directory, and writes only a single-link `0600`
  `python-native-abi21.json`; the SDK workflow uploads that exact file. Local
  runs still delete temporary evidence by default. Thirty-nine strict helper
  tests, 36 fixture-workflow contract tests, and the shell retention contract
  pass;
  this does not replace the pending clean five-target rebuild.


<a id="record-11d021b8ca07403f3de44e3b7dc4112b3fbadcfcf413da8fdff87ee01640a9df"></a>

- C# managed CancelAssetLock and reference-validator coverage is green with no
  skips: the exact 85-byte payload, all eight mandatory shared files, and the
  positive/negative appeal-finance outcomes passed. The complete native class
  remains correctly fail-closed because no authenticated ABI-21 C# bridge is
  installed; the checked `dist` manifest is still an obsolete ABI-19 artifact
  and was not substituted.


<a id="record-3e9c9caf75c89d7114b9dc4045600a7c00ef4f0f07cafcbe2a9a3c4cd1990ad7"></a>

- Two guarded JavaScript native rebuilds compiled successfully but were
  rejected by the source-seal check because other L0 source changed during each
  build. No rejected library was published and the existing `.node` artifact
  remains unchanged. A final authenticated ABI-21 rebuild and native profile
  run remain pending the L0 source freeze.


<a id="record-41240c824547e9cb827977aa5c674338652a43693687fbde99bef6902ad3054b"></a>

- The strict non-native CancelAssetLock hard cut is green on the current tree:
  JavaScript passed all eight codec/type tests and pinned every mandatory
  appeal-finance file; pinned CPython 3.12 passed 85 focused codec, exact-byte,
  builder, alias, UTF, quantity, and trailing-byte tests. The offline signed
  inventory checker accepts exactly 82 payloads, 32 `ValidationOutcomeV1`
  goldens, and 38 negative vectors. Seven static hard-cut tests, 36
  fixture-workflow trigger tests, and 53 native-artifact contract cases also
  pass across the ABI-21, Swift, JVM, C#, and Python release surfaces. Managed
  Kotlin/JVM, mirrored Java
  Android, and C# parity is green; native qualification remains separate.
  The JavaScript and Python bare decoders now reject archives outside the exact
  85–148-byte canonical range before CRC work, bounding hostile input cost;
  maximum positive signed-512-bit quantity roundtrips and 149-byte rejection
  regressions pass in the refreshed 10-test JavaScript and 85-test Python
  slices.
  Seven focused Rust `EscrowId` hard-cut regressions are green: lowercase
  checksum parsing, four canonical JSON/Norito/schema cases, noncanonical query
  rejection, and typed public-selector roundtrip. Native qualification remains
  separate.


<a id="record-14f18c8413d9a3fcd3445c974b762907cc5a0e9f432e822723cf4580a35fe8fa"></a>

- Swift managed CancelAssetLock parity passed all 10 focused cases twice,
  including the shared positive and negative profiles. The native lane remains
  correctly fail-closed because the currently linked bridge reports ABI 19;
  no fallback or capability skip was counted.


<a id="record-0ff2416c6f0cee7c78848bc2930e86edd00377b63d29d70819af18602cca1278"></a>

- A fresh isolated ABI-21 C/JNI bridge passed the complete JVM parity slice
  under pinned JDK 21 with Kotlin's JDK 8 API guard: 37 Kotlin/Java tests, zero
  skips, failures, or errors. Both SDKs actively decode and re-encode the exact
  85-byte positive archive, validate the positive JSON, reject all six shared
  negative payloads plus alias/checksum substitutions, and pass the native
  appeal-finance profiles. Their mirrored moderation expectation now uses the
  canonical checked-in CID. This proves the current source behavior only; the
  authenticated artifact manifest correctly refused the dirty merge tree, so
  a final clean-source rebuild and provenance record remain pending.


<a id="record-b1156431ab06d2cd8fa1a98d8733c519139091a567828b53a34e14548c03db8d"></a>

- The immutable finalized reputation source query now exposes typed const
  accessors for its exact source identity and optional finalized cursor.
  `sorafs_node`, `irohad`, and ledger execution consume those accessors rather
  than private query representation. Its exact focused data-model regression
  passes, and the current `sorafs_node` library check is green.


<a id="record-7eed849f446cf554a2d9b0817f2d1a07f9f14fa3b5a53af426981c43aa7690e7"></a>

- All 127 OpenAPI tooling tests pass, including deterministic replay,
  detached-signature, safe-file, source-input, and version-map negatives. The
  default sync path now enables the `xtask` binary's required `dev-tools`
  feature, and the independent canonical-spec job installs and caches its own
  pinned Node dependency graph. Cargo's tracked `.cargo/config.toml` is now
  included in the exact Rust/JavaScript generator-input inventory and workflow
  triggers; its changed cross-language digest vector is pinned. Detached replay
  now imports the tracked provisioner, verifies and copies the caller's exact
  installed dependency graph into each isolated worktree, and guards empty
  cleanup under Bash 3.2 `set -u`. The release contract passes 3/3, eight
  Python fail-closed/order tests pass, and a real detached-worktree module-load
  smoke stayed Git-clean. The checked-in
  mutable root/current specs remain byte-identical to each other but their
  manifests and version map still pin an older size and digest, so both version
  and signature verification fail closed. Final source freeze, clean double
  generation, synchronized manifests/maps, generated-client policy, and
  external manifest signing remain required; no moving-tree metadata was
  accepted as release evidence.


<a id="record-b1fce876a72845002dfc069f2ad20e94eb998ac68ce164d783d62b1da15bc838"></a>

- Release-automation and reference-SDK supply-chain contract suites pass all
  225 focused cases, and the complete SoraFS rollout source contract passes all
  384 cases. These validate fail-closed SBOM, vulnerability, cosign, lane,
  topology, and evidence plumbing; they are not substitutes for final binaries,
  genuine scans/provenance, or deployment evidence.


<a id="record-a1d65cd849aa825270a823da91cb2d9f499bf75828d6e3496b5f644cb7de7633"></a>

- The repository codec guard is green: no retired codec dependency, Native AMX
  consensus codec, lane executable handoff codec, or PK2 multilane
  compatibility path was found.


<a id="record-9345e47eba19db41b6b96b977754cd261c206639e339fade36e74129c65a016b"></a>

- The pre-deployment L1 topology, signed-qualification, and resilience contract
  suites are green (`69 passed`). They do not count as live evidence: genuine
  four-validator, multi-provider, dual-gateway execution and all 17 lane
  summaries remain open.


<a id="record-b6e4908e3b56018eb2f789f6f15178030e0b3ab7c9c01006e0da5f3d05542f24"></a>

- The complete pinned-Python-3.12 promotion/readiness logic slice is green
  (`592 passed` in 39m02s): aggregate acceptance and deterministic replay,
  foundational prerequisite construction, and the archived missing,
  duplicate, stale, predecessor, signature, mapping, substitution, and tamper
  negatives all pass with warnings treated as errors. The fixture harness now
  closes private topology directories and unloads dynamic lane modules on both
  success and failure, with idempotent and injected-failure regressions. This
  validates synthetic payload-free decision machinery only; the production
  aggregate remains blocked with zero supplied/recognized summaries and no
  authenticated external software Ed25519 signature.


<a id="record-98bbe7ae0bf4f891bc9d4e2b0b6a1f937d17ec1928b22c62731aa0100837b26a"></a>

- Honest configuration boundaries removed 150 strict dead-code diagnostics
  from the zk-X509 DER/SHA tranche without lint allowances: `der_stark.rs` is
  clean in default and release-evidence library builds (48/7 diagnostics
  removed), `der_air.rs` is clean in both (47/6 removed), and the SHA call-bus
  slice is clean in both (55/7 removed). The always-on verifier evaluators,
  fixed grammar, and transcript surfaces remain compiled; only prover,
  differential-oracle, and test-only construction helpers are gated. The
  complete workspace warning inventory and test-profile validation remain in
  progress.



<a id="record-af4cd2ac1abb1d5903e8d9c962ed05527558f23146d9ba763aa056b8b7da351e"></a>

<!-- Original context: Status / 2026-08-01 Genesis authentication and bootstrap authority -->
- Executor upgrades enforce the exact `CanUpgradeExecutor` capability both at
  native instruction dispatch and inside `Executor::migrate`; only the
  authenticated initial-genesis context bypasses the capability. Direct-call
  regressions cover both mutation boundaries.


<a id="record-34bf5b87f5c9f0a008e45ef85b50b09f923bb8025e496150b4be6c1734ad452b"></a>

<!-- Original context: Status / 2026-08-01 Standalone IPA parameter ownership -->
- The complete derived parameter fingerprint remains bound into the polynomial
  opening transcript. Retired inline-generator encodings are rejected as
  non-canonical, so `/v1/zk/verify-batch` has no caller-controlled basis surface.


<a id="record-8b9f5f68f6caf2be692d504b74f78dc1200bb220e43bde80a489cb517b296b17"></a>

<!-- Original context: Status / 2026-08-01 NPoS immediate-successor entropy binding -->
- Boundary context construction no longer derives the immediate successor seed
  from an older finalized record or a deterministic fallback. It requires and
  fully reauthenticates the current epoch's non-finalized pre-boundary record,
  including its frozen seed/roster/schedule, canonical signer order,
  signatures, VRF proofs, and observation heights, then mixes only ordered
  in-window reveals. Missing or inconsistent state fails closed before roster
  selection; late and boundary-height observations cannot change the seed.


<a id="record-cf7ff800dec50e6fbbb374f4e8035bbe5aecc3aed1e29df807d8fd50cc054517"></a>

<!-- Original context: Status / 2026-08-01 Storage-budget, RAM-LFE secret, and pre-auth ban hardening -->
- Kept filesystem-derived Nexus budgets runtime-only and added end-to-end
  coverage proving startup reconciliation leaves a read-only key-bearing TOML
  file's bytes, mode, and inode unchanged. Restart-stability, canonical alias
  deduplication, and descendant mount-identity regressions cover the remaining
  filesystem trust boundary.


<a id="record-f207ab1cc42c7e7c5d92413ecc628391cfb36ba6b18ea1e16eed93bc33a30133"></a>

<!-- Original context: Status / 2026-08-01 Build-performance root-cause refactor -->
- Replaced `NoritoSerialize`'s generic writer method with one object-safe,
  concrete `Encoder` boundary. Buffer and byte-sink writes retain specialized
  direct paths, arbitrary writers remain supported, and canonical bytes,
  digests, signatures, decode limits, and error propagation are unchanged.
  Under the same feature set, the measured Norito/data-model artifact fell
  from 586,879,728 to 397,686,216 rlib bytes (32.24%), its rmeta fell from
  242,036,189 to 100,604,837 bytes (58.43%), and defined serializer symbols
  fell from 10,912 to 4,517 (58.61%).


<a id="record-f6d86ee0d93c0dff7e40753609957ff511afb7c5e3e9f312d71aa2c9c6fb97dc"></a>

- Removed the data-model derive's three-way default/export/import item
  expansion. One generated item now carries the FFI export attribute only when
  requested; default and FFI-export consumers remain covered without retaining
  the undeclared import branch.

