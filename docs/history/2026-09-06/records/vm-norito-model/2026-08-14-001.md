# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-a0e5802126143d28c0a2e36982ebd2ea9d1a9e75652b838bd61e76ced578aa55"></a>

<!-- Original context: Status / 2026-08-14 Taira and Minamoto topology classification correction -->
- The Taira release gate now fails closed unless it can derive one
  manifest-ready validator-account roster projection for each physical
  dataspace cohort, rejects conflicting projections from roster-bearing lanes
  in the same dataspace, and requires the five derived rosters to be
  pairwise-distinct. It does not require every lane to carry a roster. Account
  identities are the strongest current public telemetry; direct `PeerId`/Torii
  endpoint and machine-boundary proof remains tracked in the roadmap together
  with the dataspace-level manifest migration. A catalog declaration alone is
  not deployment evidence.


<a id="record-6df0c0456beb7bbf986025bd384791c78d02facf8bc5dca975ce8d2d9fef0b8f"></a>

- Regenerated the complete signed Kagami Taira bundle twice from the corrected
  generator and confirmed the two output trees are byte-identical. The checked-in
  bundle now carries the exact seven-lane/five-dataspace catalog, its 19 typed
  routing rules, a freshly bound consensus fingerprint, canonical signed Norito
  genesis, and one consistent expected genesis hash across every peer config.

