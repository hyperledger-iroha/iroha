# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-dba9c4b8ebda7c92c75dc921b0f5005a7b1319f15e96b6e9f6af1d6854403eae"></a>

<!-- Original context: Status / 2026-07-20 Mixed native-ISI and contract-call batches -->
- Focused data-model wire-tag/order tests pass 2/2, and the canonical fixture
  verifier now passes all 27 entries. The current owner refresh intentionally
  changed 62 of its 65 registered outputs to encode mandatory
  `admission_intent` and explicit nullable `gas_limit`; three outputs remained
  byte-identical. Focused Java, Kotlin, JavaScript, and Python fallback checks
  pass; Swift parsing and the C# build also pass. An earlier `iroha_core`
  library check passed before the final fee/cap refinements; the final Core
  `--tests` check is blocked before test execution by unrelated dirty-tree
  Sumeragi reply-route/API compile errors. Full workspace and native-bridge
  platform suites remain release evidence.


<a id="record-07081a26a0f2d4142bd8bd6326828183b2789fda831117f44a7821471dc07e64"></a>

- Review hardening now resolves deployed trigger calls while holding the IVM
  cache and releases that guard before guest-emitted ISIs are applied, carries
  the deterministic NFT sequence across calls in one trigger batch, retains VM
  gas on failures, and accumulates successful and rejected live-batch gas in the
  parent block. Mixed native/contract routing also falls through to the complete
  target collector so native and contract dataspaces cannot be collapsed onto a
  single lane. Kotlin/Java payload authoring rejects empty batches and missing
  executable gas limits, while Kotlin, Java, Swift, C#, and browser JavaScript
  validate exact lowercase V1 Bech32m contract addresses before signing.

