# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-536fd9481b438ad1e967b8979c2eef56f7cfafc2b51248a86e8cd7f3c75c4394"></a>

<!-- Original context: Status / 2026-06-09 DA gate finalize telemetry de-duplication -->
## 2026-06-09 DA gate finalize telemetry de-duplication



<a id="record-e06c55b62a63366ccc72a5f1d2f5c80bd93445090fc1a23eb55e34f2eb403ad4"></a>

- Removed the duplicate DA-gate telemetry recording after
  `finalize_pending_block` refreshes the gate. The refresh path already records
  the `MissingLocalData` block event, so the finalize path now reports one
  missing-local-data gate block instead of double-counting the same defer.


<a id="record-1c7e113dfd216feb2d59227371dc6900e90ae19b4287f396bd8384ffa47b3173"></a>

- Extended `finalize_pending_block_defers_until_da_payload_available` to assert
  the `sumeragi_da_gate_block_total{reason="missing_local_data"}` counter
  increments exactly once when finalization is held by missing local DA payload
  bytes.


<a id="record-94d85825e51b574493f283e7276ad3ae74ff8dfe48a9d861f9f3c8600257feb9"></a>

- Validation:
  - `cargo fmt --all`
  - `cargo test -p iroha_core finalize_pending_block_defers_until_da_payload_available --lib --features telemetry -- --nocapture`
    (`1` test passed; emitted the pre-existing unrelated `soracloud.rs`
    warnings)
  - `env CARGO_TARGET_DIR=/tmp/iroha-codex-da-gate-test cargo test -p iroha_core finalize_pending_block_defers_until_da_payload_available --lib --features telemetry -- --nocapture`
    (`1` test passed; same pre-existing unrelated `soracloud.rs` warnings)
  - `cargo test -p iroha_core record_da_gate_telemetry --lib --features telemetry -- --nocapture`
    (`2` tests passed; same pre-existing unrelated `soracloud.rs` warnings)
  - `git diff --check`
  - `git diff -- Cargo.lock`



<a id="record-b7074a05ac2d66a4523fd870651f0f6d80ee45f72a79d90e76ee1e8a28960bae"></a>

<!-- Original context: Status / 2026-06-09 Taira edge nginx rendered dry-run validation -->
## 2026-06-09 Taira edge nginx rendered dry-run validation



<a id="record-0a13d78d674fe4af3bae12ee6d292dff779acae5ddbe031a8abe5452562900ca"></a>

- `install_taira_edge_nginx_conf.sh` now validates the rendered `$OUTPUT`
  through a temporary nginx config before dry-run success or install copying,
  so default runs cannot pass by only testing the host's existing include tree.
  Install mode still runs live `nginx -t` after copying and before reload.


<a id="record-a3be669b05c88d6679e824b30dc9f79113f263e2acc51b54ee940d4b91399285"></a>

- Mock coverage now asserts the temporary config includes the rendered output
  and that an invalid rendered snippet fails in dry-run mode.


<a id="record-99a1ab354d4a13179640a398d420a10cc2c1dfec77e59713705f6d2827ca5c20"></a>

- Validation:
  - `bash -n configs/soranexus/taira/install_taira_edge_nginx_conf.sh configs/soranexus/taira/install_taira_edge_nginx_conf_mock_test.sh`
  - `bash configs/soranexus/taira/install_taira_edge_nginx_conf_mock_test.sh`
  - `git diff --check -- configs/soranexus/taira/install_taira_edge_nginx_conf.sh configs/soranexus/taira/install_taira_edge_nginx_conf_mock_test.sh configs/soranexus/taira/README.md status.md`



<a id="record-9f2bbc7774f7262ccc638a32885da3efdfb4ea4e6b7556cd84c0b7968d63f55a"></a>

<!-- Original context: Status / 2026-06-09 ISO CLI and XSD identifier guards -->
- Operator evidence verification now rejects canary summaries whose `config_path`
  still points at checked-in `fixtures/iso20022/operator_canary/` runbook
  templates, and production-readiness replay reports
  `evidence.repository_canary_config` if a forged aggregate summary reintroduces
  that compact path. The canary runner itself now rejects non-plan
  config/stage/explicit verifier receipt paths under `fixtures/iso20022/`,
  while preserving checked-in template usability for `--plan-only` validation.
  Evidence replay mirrors that rule for executed canary child-command
  `--inbox-dir`, `--message`, `--export-dir`, `--receipt-dir`, and `--receipt`
  values so forged archived commands cannot reintroduce repository fixtures.
  Raw evidence verification now also rejects `--canary-summary` and
  `--trust-summary` inputs under checked-in `fixtures/iso20022/` artifacts, and
  production-readiness replay blocks forged compact XSD/evidence/canary/trust
  summary paths that point back to those artifacts.


<a id="record-58a7be0f5dcd26c0dc8e31071a50d371bb7d50dbdde54aad79b42bdaad55b1d8"></a>

- Operator evidence verification now preserves trust-bundle source paths in
  compact trust profiles and rejects checked-in
  `fixtures/iso20022/trust_bundles/` template paths; production-readiness replay
  reports `trust.repository_trust_bundle` if a forged aggregate summary
  reintroduces that compact path.


<a id="record-c05adbf06a1419d12eb522020e4c67fb5461ace107c2e6080931b45932bf1eb5"></a>

- The production-readiness gate remains blocked on external operator and
  standards evidence: official trust/revocation bundles, official MDR/XSD
  lifecycle fixtures, and live canary evidence still need to be supplied.


<a id="record-b2bf876e554c1b84af1e99a830a069200375dcb909a9a89c345cd4486ab1a219"></a>

<!-- Original context: Status / 2026-06-09 ISO receipt source-replay secret-value parity -->
- Hardened `scripts/iso_operator_receipt_verify.py` so direct archived receipt
  verification mirrors the audit-notary publisher's secret-material preflight
  for embedded audit-index identifiers and persisted record-source strings.


<a id="record-4660bbefe168d39ab52dc2c77e6e81f0a2d08a939d3df3e90250a2cefd40da97"></a>

<!-- Original context: Status / 2026-06-09 DA gate exact-condition recovery telemetry -->
## 2026-06-09 DA gate exact-condition recovery telemetry



<a id="record-548dcbcfa9a6d5b16874c100953da0ec314d6129d19002e5239014daaf0c1fdf"></a>

- Tightened DA gate satisfaction tracking so recovery is reported when an exact
  manifest guard changes to a different manifest lane/sequence/kind, even if
  another DA gate remains active. This preserves fail-closed commit behavior
  while making partial DA/manifest recovery visible instead of hiding progress
  behind broad manifest-guard class matching.


<a id="record-41b96cbffb7eb74b1c2b23e3b2e06cc0e97b4e60fbbc863026c25a91789a4f74"></a>

- Added negative/transition coverage proving unchanged manifest gates do not
  synthesize recovery progress, while changed manifest failures and
  missing-data-to-manifest transitions surface the recovered prior condition.


<a id="record-5fa74724ce7c53972e4c61bb4a00b3b2fae7e746671ac3328f117ebabbef8058"></a>

- Validation:
  - `cargo test -p iroha_core --lib sumeragi::da --features telemetry -- --nocapture`
    (`4` tests passed)
  - `cargo test -p iroha_core --lib manifest_gate --features telemetry -- --nocapture`
    (`7` tests passed)


