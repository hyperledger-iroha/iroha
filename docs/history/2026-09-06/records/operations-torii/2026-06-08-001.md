# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-db6781d1279f884539170c154db8875d27b1e99a424c18df4e89ae2db344cb37"></a>

<!-- Original context: Status / 2026-06-08 ISO canary runbook path control-character preflight -->
- Hardened the operator canary runbook path scanner so artifact paths reject
  non-whitespace control characters before plan-only summaries or child command
  arguments are built.


<a id="record-84f795d66463b1d9604253fb223e1848b051dd3b6923caee27b058d6564b3269"></a>

<!-- Original context: Status / 2026-06-08 ISO canary child-output control-character guard -->
- Hardened the operator canary summary emitter so child stdout/stderr previews
  reject unsafe control characters, while still allowing ordinary line breaks
  needed for verifier JSON output.


<a id="record-c29303374f25ed8b66f8b9b61af3624f72e660a9d0bf58acd0862367c5d7a81d"></a>

<!-- Original context: Status / 2026-06-08 ISO canary runbook receipt selector overlap preflight -->
- Hardened `iso_operator_canary.py` planning so direct `verify.receipts` entries
  already covered by explicit or generated `verify.receipt_dirs` are rejected
  before child command construction. Direct receipt files also cannot replace an
  omitted non-dry-run generated stage receipt directory when
  `include_stage_receipts=false`. This aligns runbook planning with the stricter
  archived-evidence replay rule for non-overlapping verify selectors.


<a id="record-caa877344e0b5edd045b394d187c1dcf5eacdc3febbd73032c5d57cded3e200e"></a>

<!-- Original context: Status / 2026-06-08 ISO receipt source-file override binding -->
- Hardened operator evidence replay so `--allow-receipt-source-missing` is
  rejected unless at least one verified receipt summary records
  `require_source_files=false`. The local diagnostic policy can no longer be
  archived as an unused top-level override.


<a id="record-41b90b902a6972c784e6885cc09ad79cfc090ce9bbb270bc3fd3848b03d24e21"></a>

<!-- Original context: Status / 2026-06-08 ISO trust-summary hidden policy guard -->
- Hardened operator evidence replay so each archived trust summary must bind its
  own diagnostic policy flags to its own bundle material. A global
  `--allow-record-only-trust` or `--allow-insecure-http` override justified by
  another trust summary can no longer mask a summary that records
  `record-only` policy or an `http://` or local/private source URL while its
  own `allow_record_only` or `allow_insecure_source_url` flag is false.


<a id="record-1f90ba7e5312cd1eeb8e89ba814c375b0058db1905fd8d106257fa024455f9db"></a>

<!-- Original context: Status / 2026-06-08 Background dispatch runtime gate -->
- Added telemetry-enabled runtime parity coverage for
  `SumeragiBackgroundDispatchGate.tla` in
  `crates/iroha_core/src/sumeragi/main_loop/tests.rs`, covering all six
  `BackgroundRequest` variants across ready, full, no-worker, and disconnected
  channel states.


<a id="record-4ee3f792fdb72be762851d9f5c33677326b83b854d9e79dd86d8c2137495bac6"></a>

<!-- Original context: Status / 2026-06-08 Torii direct-Serde removal -->
## 2026-06-08 Torii direct-Serde removal



<a id="record-4f49d79b76dcdae28f6efd56ad63b1bd83ebefc6d649cc9aaf7e753df83c7463"></a>

- Removed Torii's direct `serde`/`serde_json` dependencies and stopped enabling
  the SCCP compatibility `serde` feature from `crates/iroha_torii`.


<a id="record-fdd8ddfa7ab47a8a9a39833366425a2455abf9023c06ee42007bbb2efcec5ae3"></a>

- Replaced tx-history JWT claim decoding and push-provider JWT claim signing
  with explicit Norito JSON payload handling while keeping `jsonwebtoken`'s
  cryptographic `sign`/`verify` implementation for the actual signatures.


<a id="record-0271c5a88c9790a84a20441b553ba76d7b856480904b984f973871e0ea471675"></a>

- Removed `crates/iroha_torii` from `scripts/serde_allowlist.txt`; the
  remaining direct-Serde allowlist is now limited to SCCP compatibility
  derives/adapters.


<a id="record-5cc9db8b0ba938b6121f625403b026a2d4b29e116738d5e10857c7c2a61931bf"></a>

- Validation:
  - `make guards`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-torii-serde CARGO_INCREMENTAL=0 cargo check -j 1 -p iroha_torii --lib`
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-torii-serde CARGO_INCREMENTAL=0 cargo test -j 1 -p iroha_torii tx_history_jwt_claims --lib -- --nocapture`
    (`2` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-torii-serde CARGO_INCREMENTAL=0 cargo test -j 1 -p iroha_torii tx_history_viewer_from_headers_rejects_bare_subject_aliases --lib -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-torii-serde CARGO_INCREMENTAL=0 cargo test -j 1 -p iroha_torii sccp_evm_destination_query_decodes_norito_json_defaults --lib -- --nocapture`
    (`1` passed)
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-torii-serde CARGO_INCREMENTAL=0 cargo test -j 1 -p iroha_torii encode_jwt_claims_builds_verifiable_compact_token --lib -- --nocapture`
    (`1` passed)
  - `cargo fmt --all -- --check`
  - `bash scripts/check_no_direct_serde.sh`
  - `git diff --check -- crates/iroha_torii/Cargo.toml crates/iroha_torii/src/lib.rs crates/iroha_torii/src/routing.rs crates/iroha_torii/src/push.rs scripts/serde_allowlist.txt status.md roadmap.md`
  - `git diff --name-only -- Cargo.lock` produced no output.



<a id="record-ae6a46a8bf15a468889f3a8b5dcda2e9b74d55eadee1fd7be20e922da1bd6cdd"></a>

<!-- Original context: Status / 2026-06-08 ISO notary exact schema gate -->
- Hardened the ISO operator canary runner so any executed rail, notary, or
  receipt-verify stage with truncated stdout/stderr previews or non-empty
  successful stderr now marks the canary summary failed even when the child
  process exits with status `0`. This prevents incomplete or warning-bearing
  child output, including receipt-verifier output, from becoming successful
  production evidence.


<a id="record-1ab4cfa708928f1e7865c0fdfa53cdc01229f86ecfa7578a0134985dcf5fe9dd"></a>

- Hardened Torii durable-store reload so digest-correct local persisted record
  JSON must retain the complete record, context, metadata, and status-history
  schema, including nullable fields and a non-empty status history, before the
  record can rebuild runtime status or regenerated audit indexes.


<a id="record-8ac5013e8c3e6c834aa3c952ae222de92e52ae3b3d29e6283f0dceb904a2e9c3"></a>

- Bound Torii durable-store reload to the digest-addressed record filename, so
  a digest-correct record whose on-disk filename no longer matches its embedded
  `message_id` is ignored and excluded from regenerated audit indexes.


<a id="record-4ac6a4215f8e358eec9cb59fe0ef5beb92637ef2817ac929d4e6f6f9c6098ae2"></a>

- Hardened Torii durable-store reload to require the `messages` directory and
  each loaded record entry to be non-symlinked regular filesystem objects, so
  symlinked record paths or symlinked record directories are not followed while
  rebuilding runtime status.


<a id="record-c90485dd59a41342fc5909d9da1face7adb024b8c691c54e4a1401a1f11c8b50"></a>

- Hardened Torii durable-store writes to require real final directories for
  `messages`, local audit indexes, external audit export roots, and
  digest-addressed external anchor directories, preventing record, index, or
  notary preimage writes through symlinked durable-output directories.


<a id="record-21e1735e11f644be9504c1606d41cb5ce156ea520fed88b4a2740346941def08"></a>

- Added Torii adversarial coverage for digest-correct persisted records with
  missing top-level fields, missing nested context/metadata/history fields,
  extra fields, empty status history, malformed change-reason arrays, embedded
  `message_id` drift, digest-addressed filename drift, and symlinked record
  paths, symlinked `messages` directories, plus oversized digest-correct local
  records, dirty persisted strings, and symlinked durable-output directories on
  the persist/export side.

