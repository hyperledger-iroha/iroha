# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-e11e35c58ff6daaaaa678c9f197b8a64290aa1abcb919cd9a8f06e9b05de0ff5"></a>

<!-- Original context: Status / 2026-09-05 Atomic settlement historical authority candidate -->
- The current merge-ledger V3 layout binds the complete deduplicated historical
  lane-authority catalog into entry hashes and merge QCs. Focused data-model
  tests pass 16/16, including canonical Norito/JSON roundtrips, rejection of
  prior layouts, roster geometry/hash/order and catalog reference/deduplication
  negatives and aggregate maximum-envelope bounds. The codec dependency guard
  passes. Core finalized access, publication-generation fencing, prefix
  selection, concurrent lifecycle, bounded QC fields, and full recovery through
  candidate application/restart passed on the capacity build. Configuration and
  Core startup/governance/autoscale capacity guards also passed. Broader QC
  regressions passed 7/7 and isolated replay passed 1/1 after repairing fixture
  authority/finality bindings; failed attempts remain retained.


<a id="record-cf6f494cb03ae327642ac804a339d683c2219c0cb14c0b1e00be8f07532637e0"></a>

- The N=3 smoke now checks all sixteen financial-state projections, exactly-once
  finalization and replay silence, the same signed RS16 block/context under the
  configured global committee, and bounded rolling restart of every process.
  Its optimized module regressions pass 25/25; the five release-only process
  entrypoints remain ignored and the expanded smoke has not yet executed. Development
  formal runs passed N=3, N=255 and expiry, with all five negative controls failing
  as required. All five selected committee-indexed fault configurations also
  passed. These split development runs do not qualify the clean-source release
  gate. Optimized daemon and integration builds passed. The current private
  suite excluding the separately running native proof passes 93/93. A stale
  atomic-application fixture was repaired to use canonical PeerId order,
  Committee-role keys and registered Prepare locks; its 20-test global-state
  suite passes, including byte-clean wrong-purpose authority rejection.
  Production validation is unchanged. The earlier broad invocation retains
  its fixture failure and still-running native proof; no full-suite pass is
  claimed.

