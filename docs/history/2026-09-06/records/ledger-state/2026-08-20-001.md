# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-9c2749c2b0a257351ec366ff1c93428013a952a61598939c771c9e723577678a"></a>

<!-- Original context: Status / 2026-08-20 AXT persistent replay, ratchets, and cumulative handle-family budgets -->
- The policy/counter ratchet, unexpired exact-use replay ledger, and cumulative
  family ledger, together with the exact asset-incarnation map, are required,
  non-skipped World fields authenticated by canonical snapshot/WSV hashes.
  Every replay record carries the complete normalized signed family key and
  must reference matching budget evidence; Kura skips an already-applied charge
  only when that stored family exactly matches the authenticated handle. This
  is a first-release hard cut: pre-cut snapshots and checkpoints are
  incompatible and must not be interpreted as empty stores, while pre-cut
  block results without the required transitioned-dataspace set must not be
  defaulted to an empty set. Deployments must re-genesis or rebuild at a
  boundary that invalidates all pre-cut handles and families and regenerate
  affected block fixtures.

