# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-e3e430c4e6f6fbcbe16b38870a0b8318483f7a88861d3fa3c3bb183133b18ef0"></a>

<!-- Original context: Status / 2026-08-28 Mochi first-release simplification and hardening -->
- Explorer blocks and assets use bounded exact response schemas. Asset rows are
  parsed as canonical `AssetId`, `AssetDefinitionId`, `AccountId`, and
  `Quantity` values, block timestamps and transaction counts are validated,
  and response totals, exact page lengths, and effective default pagination
  must match the request. Dashboard asset fetches retain signer order while
  using bounded concurrency and copy only the non-secret signer identity needed
  by each request. The desktop relay view follows the current compact finality
  authority model instead of the retired in-envelope QC field.


<a id="record-c53719acff5327035665c92a78307a816c4edea078a9e4f0657ef89735c9840a"></a>

- The clean locked suites pass: `mochi-core` reports 433 passed across its
  library and external fixture target, with one registered fixture-owner test
  ignored; the GUI binary reports 179 passed; and `mochi-integration` reports
  22 passed with one registered fixture-owner test ignored. Strict
  no-dependency Clippy with `-D warnings` passes for core,
  GUI, and integration targets; only pre-existing warnings emitted while
  compiling unrelated workspace dependencies remain. Rust formatting,
  whitespace validation, locked metadata, and the retired-codec guard pass.
  This work did not modify `Cargo.lock`.


