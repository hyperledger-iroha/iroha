# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-1c49c6f086b6dd838dac2c542b802f7cb7fac0c811602dd5951f03678e20ace5"></a>

<!-- Original context: Status / 2026-04-23 Torii space-directory mutation coverage follow-up -->
- `crates/iroha_torii/tests/space_directory_manifests.rs` now inspects the queued transaction payloads for the Space Directory mutation endpoints instead of only checking `queued_len()`.


<a id="record-9c2ef6b72e10f4138f350ae59bacd1963928e5fecf8595e182c3c60c0d09fdab"></a>

<!-- Original context: Status / 2026-04-23 Torii space-directory handler coverage follow-up -->
- The bindings multi-dataspace test seeds a minimal manifest set alongside the bindings because `State::new_for_testing(...)` prunes stale standalone `uaid_dataspaces` entries during storage migration when no manifest sets exist.

