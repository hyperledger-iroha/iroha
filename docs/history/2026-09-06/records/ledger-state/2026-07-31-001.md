# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-0fff3de1685c7c86e3eba3348b482860ed0efa27a2a72c897aed757ace93e38b"></a>

<!-- Original context: Status / 2026-07-31 Taira snapshot restart canonicality and replay-floor closure -->
- Empty-state fallback now performs a read-only configured-primary geometry
  preflight before constructing `World` or `State`. It authenticates an existing
  durable primary binding even when no transition/checkpoint exists. If a
  durable snapshot checkpoint already compacted the genesis replay cursor,
  startup preserves the snapshot failure and reports the required checkpoint
  height instead of partially mutating geometry and failing later.

