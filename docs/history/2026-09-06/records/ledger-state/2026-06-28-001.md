# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-e965d90d434ee66498e57ff0d5d69032ee8066b74a8c5eb5787ab66e1e64245b"></a>

<!-- Original context: Status / 2026-06-28 - Nexus Fee Sponsor Policies -->
- Added sponsor-owned Nexus fee sponsor policies as WSV records with
  `UpsertFeeSponsorPolicy`/`RemoveFeeSponsorPolicy`, `CanManageFeeSponsorPolicy`,
  and `CanUseFeeSponsor { sponsor, policy }` grants.


<a id="record-748c201f59f3efc23c67f61b49584f21e6203960c33bf252f7adfd925d7147a3"></a>

<!-- Original context: Status / 2026-06-28 Nexus autoscale elastic-lane route authority -->
- Added state regressions covering the managed-elastic inheritance path and the
  malformed-lane fail-closed path, and documented the route-authority rule in
  `specs/nexus_lanes.md`.


<a id="record-7f9330e28fc814640b8b4dd93b817b9d373073dcbffda8303925d245a171699c"></a>

<!-- Original context: Status / 2026-06-28 Nexus manual lifecycle commit serialization -->
- Added regression coverage that prebuilds a `StateBlock`, publishes a manual
  lifecycle lane update, and only then releases the block commit, proving
  world-backed lifecycle cleanup does not retain the commit-serialization lock
  while it waits for the prebuilt block overlay to drain.


<a id="record-ed274725357652e56e5168d588022d04a4a88475797fd23e9655e8e541311c8f"></a>

<!-- Original context: Status / 2026-06-28 - Localnet Transaction Queue Retained-Byte Guard -->
## 2026-06-28 - Localnet Transaction Queue Retained-Byte Guard



<a id="record-b14a0f4be34b2dc83292ca72d0890f4b14d61fd7fec2f186fb1b0c65f8277c92"></a>

- Added `queue.max_retained_bytes` to user, actual, and client configuration,
  with a default of 128 MiB. Transaction queue admission and requeue now charge
  each retained transaction by canonical payload length plus a 128 KiB retained
  heap estimate, then reject/backpressure once the byte budget is exhausted
  instead of relying on transaction count alone.


<a id="record-eec5402efa4cac9671e7a562e24041cfa9a7fb09f33ed572667e835277867839"></a>

- Torii transaction ingress now sheds single and batch submissions before
  signature admission, route resolution, or batch decode when the queue is
  already count/byte saturated or when the incoming batch's minimum retained
  byte charge cannot fit. Full-queue rejection logs on Torii, queue admission,
  and gossip ingress are debug-level to avoid memory-amplifying warning floods
  during intentional backpressure.


<a id="record-e059d379705e248040019c87b78afedbfaa03fa4135ecb199829cc6f87bbdcf9"></a>

- Updated `scripts/tx_load.py` to request the correct Torii content types,
  parse nested Sumeragi queue-byte status fields, and wait while the node
  reports queue saturation when explicit queue gating is requested. Queue-full
  `429` / `PRTRY:QUEUE_FULL` responses are now treated as expected
  backpressure for bounded burst repros instead of terminal harness failures.
  `scripts/run_localnet_memory_guard.py` now derives the shifted API port from
  the generated localnet client config before driving load, runs the child load
  driver unbuffered, defaults to node-side backpressure instead of client-side
  queue gating, records `load`/`post_load` RSS sample phases plus last-sample
  RSS in the JSON report, and stops the localnet in a `finally` cleanup path.


<a id="record-440469cd868710355edcd5106bfc7f601c8d5bc2759589c1ef01d74b8b5a5543"></a>

- The pre-cap guarded repro on the 100,000 transaction localnet crossed the
  8 GiB total RSS guard (`8,785,068,032` bytes total, `2,439,512,064` bytes
  peak peer), confirming that count-only queue gating was insufficient under
  burst load. The first 512 MiB retained-byte cap pass with a 32 KiB
  per-transaction charge still crossed the guard at `8,686,190,592` bytes total
  and `2,326,151,168` bytes peak peer, so the per-transaction retained-heap
  estimate was raised to 128 KiB. A second 512 MiB pass with the 128 KiB charge
  still crossed the guard at `8,629,911,552` bytes total and `2,324,463,616`
  bytes peak peer while the queue byte cap was actively rejecting new ingress.
  The 256 MiB default cap then crossed the guard at `8,616,493,056` bytes total
  and `2,361,802,752` bytes peak peer; after demoting full-queue rejection logs
  it still crossed at `8,627,994,624` bytes total and `2,456,403,968` bytes
  peak peer, pointing to pre-admission burst churn rather than retained queue
  bytes or log volume alone. The Torii early-shed guard reduced the failure to
  a narrow threshold miss at `8,595,636,224` bytes total and `2,487,386,112`
  bytes peak peer, so the default retained-byte budget is now 128 MiB to leave
  real headroom under the 8 GiB localnet guard. With the 128 MiB default,
  node-side queue-full backpressure, and no client-side queue hiding, the
  guarded 100,000 transaction attempted burst completed with `tx_returncode=0`
  at `/tmp/iroha-oom-repro-20260628-100k-post-128m-burst`, peaking at
  `6,289,670,144` bytes total RSS and `1,621,311,488` bytes for the largest
  peer across four peers.


<a id="record-c6b99e1c5a7b3842fb7f2263bd76293129d19d068d7f10db7632e6a8a8dcf457"></a>

- A follow-up guarded 100,000 transaction attempted burst with the default
  30-second post-load sampling window also completed with `tx_returncode=0` at
  `/tmp/iroha-oom-repro-20260628-100k-post-128m-postload`. It peaked at
  `6,344,884,224` bytes total RSS and `1,640,251,392` bytes for the largest
  peer across four peers, then stayed below the 8 GiB guard throughout
  `28` post-load samples. Total RSS rose only `1,572,864` bytes between the
  first and last post-load samples and flattened at `6,344,884,224` bytes for
  the final samples. A stronger 120-second post-load run at
  `/tmp/iroha-oom-repro-20260628-100k-post-128m-postload120` also completed
  with `tx_returncode=0`, peaked at `6,180,683,776` bytes total RSS and
  `1,563,443,200` bytes for the largest peer, then dropped by
  `2,172,125,184` bytes total RSS during post-load sampling and flattened at
  `3,639,918,592` bytes across the final samples.


<a id="record-7641c7e037007d4fd9869de08fef8eaee5dae212ca6fc96c77a8ba10b201b9b4"></a>

- Validation passed:
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_core retained_byte_cost_floor_scales_with_incoming_count -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_core retained_byte_budget_rejects_before_count_capacity_and_releases_on_remove -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo check -p iroha_core`
  - `CARGO_BUILD_JOBS=1 cargo check -p iroha_torii`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_torii --lib transaction_ingress_precheck_rejects_incoming_batch_over_retained_byte_budget -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_config queue_defaults_allow_two_times_legacy_soak_capacity -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_config config_update_with_handshake_roundtrip -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_telemetry status_json_roundtrip -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_telemetry serialize_status_json -- --nocapture`
  - `CARGO_BUILD_JOBS=1 cargo test -p iroha_torii --lib status_snapshot_json_includes_tx_queue_pressure_reasons -- --nocapture`
  - `python3 -m py_compile scripts/tx_load.py scripts/run_localnet_memory_guard.py`
  - `pytest -q scripts/tests/tx_load_test.py scripts/tests/run_localnet_memory_guard_test.py`
  - `python3 scripts/run_localnet_memory_guard.py --out-dir /tmp/iroha-oom-repro-20260628-100k-post-128m-burst --memory-limit-gb 8`
  - `python3 scripts/run_localnet_memory_guard.py --out-dir /tmp/iroha-oom-repro-20260628-100k-post-128m-postload --memory-limit-gb 8`
  - `python3 scripts/run_localnet_memory_guard.py --out-dir /tmp/iroha-oom-repro-20260628-100k-post-128m-postload120 --memory-limit-gb 8 --post-load-sample-seconds 120`
  - `cargo fmt --all -- --check`
  - `git diff --check`
  - `rg -n "^(<<<<<<<|>>>>>>>|=======$)"` returned no matches
  - `git diff --name-only -- Cargo.lock` returned no paths



<a id="record-06152e3a741259e3273c15a0b627d135f84a4b20156ccdf74d3bfc5754335198"></a>

<!-- Original context: Status / 2026-06-28 Nexus replay validation stack refresh -->
- The replay WSV checkpoint mismatch regressions now run through the existing
  16 MiB replay validation stack helper, matching the other deep replay tests
  in the module. The follow-up broad `iroha_core --lib` run no longer hit the
  replay stack overflow; it completed the suite and exposed 11 remaining
  failures instead.


<a id="record-9e315d68e428fd0a838d38670c8c3b08ada241244da83c6ed8c4af36bde72d8d"></a>

<!-- Original context: Status / 2026-06-28 Nexus public-lane invariant validation refresh -->
- Re-ran the pending public-lane key/record invariant filters for core and
  Torii app API exposure. The refreshed evidence covers mismatched validator
  rows across staking mutations, due activation, Soracloud authority,
  host-finance stake accounting, peer/account cleanup, roster penalty lookup,
  multisig rekey cleanup, and public Torii validator/stake/reward reads.


<a id="record-73a1b22f3d0bede1153c87031163fc26d5a0be42ade3c55d7206de2cc222554c"></a>

<!-- Original context: Status / 2026-06-28 iroha_core review compile fixes -->
## 2026-06-28 iroha_core review compile fixes



<a id="record-fd4f35cb29def03183186e0b180ea93750ee79c0c301f24b61401b5c9d3fdfc0"></a>

- Fixed the reviewed `iroha_core` staking/state compile regressions: the slash
  closure now has an explicit instruction error type, the mismatched reward-row
  test keeps its fixture lanes inside the 64-lane catalog and borrows the
  invariant message, and the AXT replay fixture converts `LaneId::as_u32()` to
  `u64` before constructing a `DataSpaceId`.


<a id="record-da70e8f7707fa4941b2d3f9cffe4aa3f4cd86939a78f4e003439b62463f97bc4"></a>

- Validation status:
  - no processes were killed, signaled, interrupted, or restarted.
  - an existing default-target `cargo test -p iroha_core --lib stale_geometry_da
    -- --nocapture` job was running, so validation used
    `CARGO_TARGET_DIR=/tmp/iroha-codex-review-fix`.
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-review-fix cargo check -p iroha_core`
    passed.
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-review-fix cargo test -p iroha_core --lib record_rewards_rejects_mismatched_public_lane_validator_row -- --nocapture`
    passed.
  - `CARGO_TARGET_DIR=/tmp/iroha-codex-review-fix cargo test -p iroha_core --lib axt_replay_ledger_keys_for_lanes_selects_target_lane_only -- --nocapture`
    passed.


