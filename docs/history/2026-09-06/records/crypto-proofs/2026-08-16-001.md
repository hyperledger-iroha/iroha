# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-6c664ed6124c780be9f5124b2f25e977c5f67115a9d5fb07f604f27c9e4d78bf"></a>

<!-- Original context: Status / 2026-08-16 public Taira 24-hour evidence boundary -->
- The public-soak authority and replay namespace are separate from privacy
  rollout observation. Public entry points refuse before caller path I/O until
  an independent Ed25519 verifier, pinned native evidence verifier, and atomic
  consume-once replay broker are provisioned. Fresh admission is distinct from
  its durable signed admission receipt; environment flags, marker files,
  workflow identity, local keys, and self-issued evidence remain forbidden.
  The 126 focused structural and authority tests pass. A separate process-held
  controller lease now binds the exact source, candidate/publication/deploy
  handoffs, public-network tip, controller, and native verifier for one fixed
  24-hour attempt. It uses atomic owner-private state, holds one non-reacquirable
  lock for the whole process lifetime, rejects clock divergence and premature or
  late capture, and cannot resume an abandoned or pathname-replaced attempt.
  Its native-verified soak-anchor and producer-launch identities define the
  slot-zero clock epoch; process ownership cannot cross a fork, and quiescent
  inspection distinguishes a released lock from a live owner. Its 31 focused
  filesystem, timing, transition, and tamper tests pass. The
  lease is deliberately not exposed as a controller operation. The structural
  workload/capture runner now owns it through one exact 432,000-slot attempt and
  publishes native launch and shutdown receipts, but its public authority gate
  remains source-disabled until the genuine protected backend and independent
  admission broker are installed; no live public-soak receipt is claimed.

