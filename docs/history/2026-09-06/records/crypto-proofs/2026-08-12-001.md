# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-0572253a548e3255e56fa96ed6d11379fb227e529c26484bafc8e717057fc61b"></a>

<!-- Original context: Status / 2026-08-12 Workspace review-failure closure checkpoint -->
- The review-reported Norito, crypto, data-model, genesis, Core query,
  SoraFS, and Sumeragi compile failures were repaired without changing
  `Cargo.lock`. Focused validation passed for Norito (`394 passed, 1 ignored`),
  `iroha_crypto --all-targets`, `iroha_data_model --all-targets`,
  `iroha_genesis --all-targets`, and `iroha_zkp_halo2 --all-targets`; the last
  stable ZKP snapshot emitted no Rust warnings. Both `iroha_core --lib` and
  `iroha_core --all-targets` passed with zero Core errors or warnings.


<a id="record-456ed2d13d6e23cdb4b644fe6094a40ff5eb0e9412f37cee18fb72d1728e9df0"></a>

<!-- Original context: Status / 2026-08-12 Merge resolution, closure, and build-efficiency checkpoint -->
- The permanently unavailable first-release MKHE implementation was reduced to
  a 334-line fail-closed governance facade after proving that 254 retired
  low-level exports had no external tracked consumers. The 44-field manifest,
  12-field readiness record, validation order, and exact manifest, readiness,
  and candidate-profile digests remain pinned. An isolated offline build and
  four focused facade/readiness tests passed; this retirement removes 87,146
  physical Rust lines including the earlier private Q-PCS prototype.

