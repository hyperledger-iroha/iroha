# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-7539ef8369960fd3d835cfaca11c9d5a824e30271f6949755ccc3c302b92d7ed"></a>

<!-- Original context: Status / 2026-08-21 MKHE and Figure 9 completion tranche -->
## 2026-08-21 MKHE and Figure 9 completion tranche



<a id="record-8fd88b9b01af57b14ca5e8837b554ae272eb71b8384ed122a7bfd1469cf594d3"></a>

- The restored native MKHE implementation is now protected by an explicit
  safety branch while completion proceeds on the reconciled first-release
  history. The release boundary remains fail closed; no readiness bit was
  flipped to compensate for missing cryptographic or physical evidence.


<a id="record-75d562e6792e549a55f131d994440c3a8e2e016747256ab90cc48f3a0f6931cc"></a>

- MKHE wire, measured-resource, and release-KAT gates now consume canonical,
  profile-bound evidence records instead of literal booleans or a merely
  nonzero digest. The frozen records deliberately carry zero artifact pins.
  A separate corrected RNS-native candidate fixes the Phase-II/III capacity
  inconsistency with 40 ordered NTT limbs, 2,400 modulus bits, 2,287 residual
  bits, 112 bits of centered headroom, and a 258-byte signed response. Its
  digest-bound proof topology fixes the 43-record family order, two RNS RLWE
  equations, five cross-field points, 160 qPCS queries, 18 FRI rounds,
  base-2^15 digits, 103-bit quotients, and 29 lookup/sumcheck rounds. It is not
  release authority until estimator, proof/KAT, theorem-review, and measured
  resource artifacts are installed and authenticated together.


<a id="record-28bdd652a87dcb8cc3472a0f70c4cddf9c83784e35ce39f95fd6c96a97bc010c"></a>

- The corrected RNS-native candidate now has a Core-backed confidential-spool
  source contract, an exact four-section/40 MiB envelope, and one consuming
  Fiat-Shamir transcript. The transcript fixes all 43 source/Hyrax commitment
  pairs, 20 qPCS roots, and 28 ordered challenge domains. An atomic composite
  boundary validates the source, envelope, transcript, and all four typed
  sections together and never exposes a caller-supplied verifier or partial
  receipt. The final move-only transcript retains every initial statement,
  source, roster, ciphertext, receipt, and operation identity, so rebuilding a
  context-A envelope around context-B sections fails before any stage. Its
  production qPCS adapter now performs the first real cryptographic stage: it
  derives 160 unique unbiased query pairs, decodes the fixed 40-limb/Fq2 leaf
  geometry canonically, authenticates all 320 leaves against the transcript's
  initial Merkle root, and binds every ordered leaf-pair digest. Its next
  consuming prefix binds all 400 ordered relation evaluations and both RLWE
  equation commitments through an explicit aggregation identity, authenticates
  the quotient, FRI-0, and FRI-1 trees at the same queries, checks every opening
  and ten-row batch equation, and verifies fold 0 including upper-half query
  selection. A final consuming stage now authenticates FRI layers 2--17 under
  the exact aggregate leaf, path, and byte caps, verifies folds 1--16 including
  upper-member selection, and derives fold 17 from all four authenticated final
  leaves to enforce the terminal-degree equation without trusting an
  uncommitted producer value. Its move-only result retains the parameter,
  transcript, query, section, schedule, and residual identities needed by the
  next verifier. Separate move-only prerequisites now implement the private
  RLWE/source-statement preflight, exact source-to-terminal/cross-field mapping,
  the complete 1,536-row terminal cross-basis representation-equality kernel,
  and authenticated zero-padding commitments. Those prerequisites deliberately
  grant no authority and are not yet all consumed by the atomic production
  composite. It still returns explicit `StageUnavailable` until the complete
  source mapping, terminal materialization, and packing seals; the 40-limb
  cross-field/global-lookup proof; and source/global-lookup/terminal ownership
  of the authenticated padding lanes are linked in one verifier. No composite
  candidate receipt or readiness result is emitted before that boundary, and
  the external proof/KAT, theorem-review, and measured-resource evidence is
  still absent.


<a id="record-aa4f71f9ebbb41c0bfc30b26a6d7f3f711e7009cf84a27a9fcafd32338a900df"></a>

- The first-party Microsoft Vega-MC verifier can now accept an exact governed
  Figure 9 verifier key through a bounded install-once API, reject replacement
  or noncanonical keys, run every crate-owned verification equation, and bind
  all 14 relation outputs plus four context-digest limbs. The Microsoft proving
  key now has a bounded canonical codec, and PK/VK pair installation is atomic,
  idempotent, nonreplaceable, and checks every component plus the embedded VK
  digest. Proving revalidates those artifacts and the canonical eight-step /
  18-core public schedule before touching caller randomness. A dependency-free
  split adapter now pins the exact two birth and six issuer SHA steps, both SHA
  row exclusions, the 183,382 retained relation rows plus 18 inputization rows,
  the 262,144-row split-core domain, and the 524,288-element shared witness. A
  dependency-free replay also reconstructs the historical Bellpepper SHA
  allocation and value order for all eight steps and streams every governed
  step/core witness equation before prover randomness. The governed application
  preparation then exact-matches the derived 2,048-column commitment key,
  replays the pinned `StdRng`/ChaCha12 stream, samples exactly 2,560 row
  blindings, commits the shared assignment once, streams all padded private
  sections, and validates every split-instance transcript. All RNG, reconstructed
  rest-witness, and blinding owners wipe on success, error, and unwind. The
  first-party pipeline now continues through the exact 47-round semantic
  NIFS/sumcheck engine, random-Nova tail, relaxed-Spartan proof, and final
  streamed Hyrax/linear-IPA opening. It publishes bytes only after canonical
  decode/re-encode and crate-owned verification under the installed key. The
  repository still lacks the governed full-shape PK/VK artifacts and an
  independent full-shape Figure 9 proof vector, so compact mutable-tree fixtures
  are not release evidence and production proving or verification without the
  installed governed key remains fail closed.


<a id="record-d514c5a9078d61111edb3d98066fe1466cb88527889ab3ca4030310dc7979ab7"></a>

- The brittle MKHE source-contract baseline was repaired without weakening its
  source budgets: 47 focused contract checks and four exact regression guards
  pass. Recorded focused MKHE results include 42/42 consolidated RNS-native,
  7/7 qPCS-initial, 8/8 qPCS-prefix, 10/10 composite-verifier, 24/24
  production-surface, 21/21 combined qPCS/FRI, 7/7
  source-to-terminal/cross-field, 3/3 terminal cross-basis (110.56 s), and the
  post-edit 10/10 composite verifier. The zero-padding cases have a prior 5/6
  run plus a green rerun of the repaired exact scanner; a complete post-edit
  zero-padding filter is not recorded here. The locked ZKP library check is
  green after the linkage changes.


<a id="record-0cec69dc1bea36c117b5d61d3242f6a774878aed623971a16eb6e0d871eedf14"></a>

- Figure 9 focused results include 8/8 reachability/cross-conformance, 6/6
  governed RNG, 6/6 application preparation, both split-adapter guards and its
  optimized release gate, 6/6 semantic engine, 3/3 random Nova, 1/1 non-power
  R1CS, 5/5 Spartan, 3/3 relaxed Spartan, 4/4 final opening, and 7/7 canonical
  boundary tests. These exercise the first-party path but are not the missing
  governed full-shape vector.

