# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-ed56356e987937e4bd9ae03ea47ca5fb756c51e4087388c9ca21b0af6ba3f5c5"></a>

<!-- Original context: Status / 2026-08-14 Bounded canonical app authentication and routed-read admission -->
- JavaScript, Kotlin, Java, Swift, Python, C#, and Rust canonical-request helpers share the V1
  limits and form-canonicalization rules. They emit canonical accounts in the
  transport-portable ASCII `0x` account-address spelling while preserving I105
  in signed data models and request bodies. SoraFS reputation reads now require
  the exact genesis-derived `NetworkId` and bind it into the same bounded
  canonical request transcript used by Torii. Every signer helper rejects
  non-token methods, inexact paths, structurally malformed lowercase-ASCII
  alias headers, and over-limit freshness or signature material before signing.
  Torii remains authoritative for alias UTS-46 validation, active-catalog
  resolution, and resolved-controller verification.


<a id="record-8b0cc08e7dcd2663ce10140c73b1f13b3adb97aef7bddcc639d313076ba05aac"></a>

- Scoped diff, legacy-codec, C#, Kotlin, Java, JavaScript, Python, and Swift
  parser/test checks pass for this slice. The reviewed source budget is
  ratcheted to the candidate (`checked=7,995`, `exceptions=195`,
  `rust_lines=5,012,058`, `findings=0`) without changing its 5,067,263-line
  provenance baseline or 5,014,603-line aggregate ratchet ceiling. The
  ten-percent objective remains 4,560,536 lines, leaving a 451,522-line gap.
  The integration merge is
  committed at `c3a0c7a7c99833722770eac84c7946a365179d56`, and locked offline
  dependency resolution succeeds without changing `Cargo.lock`. The locked,
  offline workspace all-target check passes warning-free; all 53
  app-auth tests and the exact Connect-network/SID and authenticated governance
  MCP regressions pass. The protected MKHE verifier's locked/offline library
  test build and all 13 focused semantic-verifier tests pass. Of the nine
  protected MKHE findings from the last strict all-target Clippy attempt, four
  are fixed while five remain structurally unresolved behind local lint allows.
  The formerly cited protected import groups now pass direct `rustfmt --check`,
  although repository-wide formatting reconciliation remains open. The honest
  static JavaScript graph also remains above its release ceilings: Torii is
  1,070,886/1,006,592 bytes, transaction codec is 304,228/304,128 bytes, and
  the public browser aggregate is 563,467/480,256 bytes. Full-workspace tests,
  repository-wide formatting reconciliation, a tracked freeze, and a signed
  candidate commit remain pending.


