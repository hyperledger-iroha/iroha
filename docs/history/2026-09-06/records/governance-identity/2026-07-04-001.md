# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-a60497fa151332e7df3075476350a3da2783cbfa7f6e24a1320bd73160534c5c"></a>

<!-- Original context: Status / 2026-07-04 ISO 20022 schema-backed first-release profile scope -->
- Removed the pre-release XMLDSig trust-pin alias fields
  `trusted_public_key_sha256` and `trusted_certificate_sha256` from the ISO
  bridge profile model, Torii config conversion, trust-bundle summaries, XSD
  fixture preflight, and operator evidence validation. The first-release
  surface accepts only `signature_public_key_sha256_pins` and
  `x509_trust_anchor_sha256_pins`; stale alias fields are now malformed input,
  and current public-key, trust-anchor, and revoked-certificate pin roles are
  checked for overlap.

