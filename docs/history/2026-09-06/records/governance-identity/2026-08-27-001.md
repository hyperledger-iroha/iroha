# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-7713baf8a20d942ce18e23bf5fab4a8fe6b9bf611826869d3df62f425302c710"></a>

<!-- Original context: Status / 2026-08-27 Trigger authorization and callback isolation hardening -->
- Added regression coverage includes alias-domain authority confusion, malformed
  permission-cache hydration, stale permission transfer after ID reuse,
  failed-callback rollback with a healthy sibling, witness-overlay rollback,
  staking-status rollback and reset ordering, time/pipeline/data same-ID sibling
  replacement, same-transaction by-call replacement, and bounded verifier-cache
  warming. Scoped Rust formatting and whitespace validation pass. An
  isolated-target Core library check and fourteen focused trigger exploit and
  legitimate-control regressions also pass; exact filters and the verification
  snapshot are recorded in the remediation report.



<a id="record-4e489b1b85a42b134e00552d32952609e247f20a7fd536998630dff5165fe721"></a>

<!-- Original context: Status / 2026-08-27 cross-SDK digital-signature and Android attestation hardening -->
- The Java harness and both shell entry points now require roots, alias,
  challenge, and expected leaf-SPKI digest from a separately trusted inventory;
  bundle-local identity or roots are never authoritative, and missing or empty
  bundle trees fail the release gate. They accept only the canonical snapshot
  artifact and trusted commitment. The capture helper derives the
  artifact from the exact Google response bytes and headers, includes reviewed
  TBS hashes, publishes it owner-only, and records its commitment in the
  capture receipt. The device-lab validator rechecks snapshot freshness for
  every chain at the same instant used for certificate validity and enforces
  both committed deny lists across every certificate. Mutation tests keep the
  commitment fixed while changing every security field.


<a id="record-1e5521545e582635dc3a3afd0f617ff3829cc3680309286346643007ba9f2b3e"></a>

- A sealed Codex Security diff scan found the revocation-snapshot and alias-key
  binding gaps as two medium-severity findings; both are fixed. Follow-up review
  also closed bundle-controlled trust anchors and identity expectations,
  zero-evidence release success, replayed challenge semantics, fail-open backend
  errors, and non-canonical Swift signature scalars. Browser Connect passes
  27/27 focused tests. Swift source type-checking, independent differential
  point-admission checks, and a direct scalar-admission runtime probe pass; the
  full Swift package remains blocked by the absent
  `dist/NoritoBridge.xcframework` artifact.


<a id="record-c0367c654349a64bfe445a69e6259ea2cae6a250d355caf7af47b4ce624a09fa"></a>

<!-- Original context: Status / 2026-08-27 Kotodama first-release simplification -->
- SSA copy/Phi coalescing now batches aliases through deterministic value equivalence, tracks Phi
  class counts incrementally, and rewrites uses once. Dead-value removal is one fail-closed
  mark/sweep from effects, traps, and terminators, so dead dependency chains and unrooted Phi cycles
  no longer require repeated whole-function scans. Duplicate pre/post optimization verification was
  removed while retaining verification before mutation and after every transformed round.

