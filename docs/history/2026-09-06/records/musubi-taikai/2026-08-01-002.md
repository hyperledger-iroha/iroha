# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-e08581552e7ec319c5d82835ee11eaaa28f28f5f1126a4a9946269f395aeac3a"></a>

<!-- Original context: Status / 2026-08-01 Musubi first-release V1 registry and Core reset -->
- The publication client/service core now has a secret-free resumable client
  journal, platform-file-only authenticated HTTPS clients, compiler-checked
  staged CAR, archive/pin registration, finalized three-provider replication,
  distinct two-provider readback, Native AMX submission, and exact final-index
  checking. Readback deterministically scans the finalized sorted provider set
  until two distinct valid responses succeed, so a failed or substituted early
  provider cannot strand a healthy later quorum. The exact persisted floor may
  be any strictly ordered two-provider subset of the selected location;
  exhaustion preserves the first deterministic permanent or retryable backend
  class/code, or reports substituted evidence when no backend failed, without
  persisting a partial floor or reaching Native AMX. Its transport-independent
  private three-route service implements
  canonical authorization/evidence checks and bounded-journal retry, dead-letter,
  and expired-receipt handling, now under focused validation. The exported Unix
  `DurableMusubiPublicationServiceJournalV1` provides the concrete bounded
  restart journal: it binds chain/genesis/broker/provider identity and fixed
  capacities, reserves worst-case terminal response space before backend work,
  holds an effective-UID-owned empty lifetime lock, and atomically commits a
  digest-bound canonical Norito snapshot with exact rereads and durability
  barriers. Startup durably converts interrupted fresh attempts to aborted
  tombstones and interrupted receipt refreshes to their retained prior response.
  Candidate writes and reopen enforce the same operation, route, digest,
  authorization-index, response, and count invariants. The in-memory
  implementation remains process-local for tests and development. The private
  seed request now authenticates the complete archive commitment plus the
  domain-separated digest and exact length of a canonical multi-file plan
  witness. Its closed versioned body envelope carries fixed magic/version and
  exact plan/CAR lengths followed by the canonical Norito witness and CAR; the
  retired raw-CAR media type is rejected with no compatibility path. The
  publisher durably installs `<operation>.plan.norito` as an immutable,
  no-follow sidecar beside the staged CAR, reopens it under bounded canonical
  decode/re-encode and commitment checks, and keeps its bytes and path out of
  the journal. Fresh publication persists that secret-free journal recovery
  anchor before installing either sidecar, so a crash cannot leave an
  undiscoverable CAR. Before installation and again on reopen, the publisher
  reproduces the canonical CAR from the complete plan and checks its root,
  statistics, payload digest, and PoR commitment; a different file inventory
  with identical scalar/chunk fields therefore fails before immutable state is
  poisoned. Package production and ingress also share the full portable
  path-set validator, including NFC, reserved/bidirectional names, exact and
  Unicode case-fold aliases, and case-folded file/directory prefix conflicts.
  The shared provider-grade
  `sorafs_car::musubi::MusubiBundleVerifierV1` checks the canonical CAR and
  plan, commitment-bound chunker, chunk-plan, and PoR, then verifies the nested
  semantic-release, artifact-descriptor, normalized source-tree,
  verification-lock, and bundle commitments. It returns typed verified
  evidence and exposes only closed, redacted integrity-failure surfaces. Its
  provider-oriented fresh-reader entry point makes three commitment-bound
  passes without materializing the CAR: canonical CAR reconstruction into a
  sink, chunk-plan/PoR validation, and semantic-bundle verification. Every pass
  requires exact EOF; focused adversarial coverage includes open/read failures,
  pass-to-pass mutation, truncation, trailing bytes, and substitution
  error-surface parity with the complete-CAR verifier. It accepts only an
  authenticated payload extraction or admitted chunk source; raw provider CARs
  still require the complete canonical-container boundary. The generic
  provider path caps its PoR store at 32 MiB and drops it before metadata
  parsing in both entry points. The shared exact semantic-release and
  verification-lock decoders cap cumulative allocation accounting at 48 MiB,
  including nested field charges and whole-input realignment. Those controls do
  not prove whole-process RSS below 64 MiB, so supported-target qualification
  remains open; the generic 512 MiB chunk-store ceiling is separate. The
  embedded `sorafs_node::NodeHandle` now supplies the matching digest-only,
  callback-scoped lifecycle lease. It blocks concurrent eviction, exposes no
  paths, permits exactly three byte-zero readers, schedules and accounts every
  verified chunk, and retains only one verified chunk per reader. Canonical-key
  lookup avoids a full manifest scan, public acquisition failures are redacted,
  later-chunk failures cannot erase an earlier successful short read, and eviction releases
  global state while waiting for a lifecycle writer. A transient exclusive
  retirement intent rejects new leases while existing readers drain, and a
  failed verified chunk retains its complete admitted byte charge rather than
  refunding corrupt-read retries. Higher-ranked callback
  lifetimes prevent leases or readers from escaping. Exact-byte, three-reader,
  pass-bound, missing-manifest, later-chunk-failure, and deterministic concurrent
  eviction fixtures are present. The provider-attestation pre-completion
  projection prerequisite is implemented: replication orders explicitly marked
  for Musubi atomically install the exact registered `ArchiveId` and complete
  immutable archive commitment, while generic SoraFS orders remain unbound.
  Consensus advances that record from pre-location to active and then to a
  permanent retired tombstone retaining the exact historical provider set;
  execution and snapshot validation enforce bidirectional marker/binding parity,
  canonical order/pin/archive/commitment identity, and exact completed-provider
  sets for active and retired records. The durable provider-indexed finalized
  archive retains the binding together with chain/genesis identity, the exact
  finalized anchor, and current completion state. Its runtime-created opaque
  pre-completion claim binds chain/genesis/provider/cursor and the exact
  order/archive/commitment, and only its validated fetch projection crosses the
  versioned private broker. Native storage revalidates the stored manifest and
  plan, then runs the shared semantic bundle verifier under the callback-scoped
  lifecycle lease. The V5 outbox checkpoint durably binds each Musubi job to a
  bounded chain/genesis/archive authorization context that participates in the
  job ID and immutable replay binding. Generic local rows require no receipt;
  Musubi local rows require the exact bounded verified-bundle receipt, and the
  same pairing survives finalized terminal state. The public verified-bundle
  receipt is deliberately nonserializable and nondecodable; only a
  crate-private persisted DTO is admitted by the sealed checkpoint, and the
  retired V4 discriminator is rejected. Every other checkpoint discriminator
  fails closed. Retained admission cursors are monotonic floors:
  later finalized claims and receipts remain valid, equal heights require the
  exact block hash, and lower or forked observations are rejected. Empty source
  sets for valid local-only orders now release their live claim into a
  `SourceUnavailable` retry, and all other source-request construction failures
  persist `SourceRejected` before returning, so neither path strands a
  `SourceClaimed` job until lease expiry.
  The finalized reader now also seals a distinct opaque completed-row claim only
  for this provider's exact finalized completion. The only downstream-visible
  approval-request minting API is the doc-hidden
  `NodeHandle::verify_provider_ingest_completed_musubi_capture_bundle`. It
  rejects an unbound or foreign process-local marker before entering the
  callback-scoped admitted-payload lifecycle lease, whose crate-private verifier
  binds the retained authorization, completed claim, exact plan, admitted
  manifest/payload digests, and all three freshly opened verifier readers;
  `NodeHandle` rechecks the marker on the returned nonserializable request. The
  raw verifier-evidence constructor is crate-private, so a pre-completion
  verifier result cannot be retained and combined with a later claim outside
  `sorafs_node`. The request binds the
  unsigned attestation payload, stable completed-row digest, separate covering
  cursor, and governed signer policy; the digest excludes only the observation
  cursor, so a same-row request at a later finalized head reuses the retained
  identity while lower or equal-height-fork observations fail.
  The effect-free completed-Musubi scanner's signed reader receives neither a
  claim factory nor an opaque claim. The daemon reader lazily owns an ephemeral
  Ed25519 session and returns a private-field, non-codec projection only inside
  an untrusted envelope whose bounded canonical transcript commits the exact
  request and every projected field. A one-response generation cache makes
  cancellation and response loss byte-exact even if the archive head advances;
  different same-generation, old, or skipped requests fail. The scanner pins
  the session, verifies the signature before privately sealing and revalidating
  paired authorization/claim candidates, and advances its generation only
  after complete validation. In-progress cursor state, monotonic finalized
  high-water, and last-completed cursor are separate. During one scanner
  lifetime, each subsequent fresh scan at the same head performs one bounded
  fully validated first-page probe, suppresses candidates, and still advances
  the generation; a later head resumes paging. Restart creates a new scanner
  and rescans, so this is not a durable no-work watermark. A crate-private
  one-page reconciler now rederives each request under the admitted-payload
  lifecycle lease and idempotently enqueues it only while the key remains
  retained in the bounded journal; delivered entries may be capacity-pruned.
  It restores all scanner progress on failure. The scanner and reconciler
  remain crate-private and no daemon child uses them. A private clone-shared
  `Arc` marker is created only for a `NodeHandle` with both storage and the
  provider-ingest outbox. It identifies that process-local handle incarnation
  through completed claims, candidates, and approval requests; clones share
  it, while another handle or restart receives a fresh non-equal marker.
  Generic claims remain unbound. Reconciliation rejects a foreign scanner before
  a signed page request, while a foreign or unbound claim fails before the
  lifecycle lease or payload-reader I/O. The marker has no codec or stable bytes
  and does not enter completion digests or approval IDs, but it also does not
  authenticate Kura, State, the signed reader, or provider slots. The marker
  now carries a non-resetting atomic coordinator take guard shared across handle
  clones. The prepared archive owns one movable concrete signed reader with no
  cloneable accessor; a private daemon composer consumes it into a doc-hidden,
  non-generic coordinator retained on `Iroha`. Acquisition performs no reader
  call, lazy binding retries the same reader/session after height-zero startup,
  and the coordinator exposes no public operational method. The reconciler now
  samples qualified inventory before and after one exact slot-59 `get` under a
  shared timeout: an exact request payload suppresses admission, absence
  proceeds to CAS-checked enqueue, and conflict, rejection, drift, or timeout
  fails closed. A drop guard restores the exact scanner generation and cursors
  on cancellation as well as every returned failure, so an unpersisted suffix
  cannot be skipped. Qualified effect-driver ownership and supervision remain gates
  before the shared daemon startup rejection can be lifted.
  The inert provider-attestation foundation now includes an approval-only,
  replay-stable timed signer contract with an independent non-zero deployment-
  adapter revision/policy digest, exact owner/controller-policy, and pre/post
  qualification and eligibility checks, plus a bounded content-addressed CAS
  journal. A private daemon signer wrapper now pins that exact configured
  adapter plus chain/genesis/local-provider context and the finalized
  `State::provider_owners()` value before and after approval; it rejects
  ownership, adapter, request, or result substitution. The sealed-clock signing
  driver and wrapper remain uninstantiated and crate-private.
  The journal retains a UNIX-time floor, exact owner/generation/lease fencing,
  fixed-footprint capacity reservations, exclusive sequence/ID pages for
  restart-ready approval, handoff, and dead-letter discovery, and
  generation-fenced dead-letter requeue/acknowledgement with handoff evidence
  retained. Its inventory contracts are canonical and idempotent; only the
  journal constructs the non-Norito opaque acknowledgement, whose checkpoint
  form is a private DTO. The inventory handoff requires a runtime supertrait
  with a production handle, non-zero adapter revision/policy digest, and a
  bounded non-mutating readiness probe. A private daemon wrapper now fences the
  exact configured metadata around readiness and every fallible inventory call,
  restricts put/get to the local provider, and validates exact configured
  chain/genesis plus requested archive/order scope and returned
  item/key/revision/inventory. It intentionally does not read provider-owner
  state; that authorization belongs to the signer. The wrapper and handoff
  driver remain uninstantiated and crate-private. The inert public file-store
  adapter now binds the exact chain/genesis/provider identity to a root-fenced
  two-slot CAS with a fixed 128 MiB checkpoint/payload ceiling on Linux/macOS,
  with nonblocking normal-operation locks, durable lineage validation, and a
  fixed five-second cross-process initialization deadline. The sealed wrapper
  reuses the exact initialization-lock identity committed in the immutable
  two-slot headers as a nonblocking process/cross-process composite lease over
  external authority and local reconciliation. Cancellation drops that lease;
  exact retry can repair the local cache, and unlink/recreate substitution
  cannot admit a second accepted lock identity. Other targets fail closed.
  Raw checkpoint snapshots, CAS outcomes/errors, checkpoint-head orchestration,
  the abstract store, sealed wrapper, transition engine, and runtime constructor
  are crate-private.

  Slot 57 is now a combined durability contract with separate authenticated
  small-record namespaces for the monotonic UNIX-time floor and checkpoint
  head, plus immutable content-addressed checkpoint blobs. Canonical
  domain-separated Norito hashes bind the checkpoint scope to the exact chain,
  genesis, provider, and retained journal-policy digest. Explicit initialization
  proves the local cache empty and installs generation-one empty `H0`; ordinary
  open rejects a missing external head and never promotes local bytes. Mutation
  order is exact blob put/readback, predecessor-linked head CAS/readback, then
  local two-slot CAS. The external head/blob is authoritative, and only an
  authenticated exact direct predecessor can be repaired forward from its
  retained record/blob; deeper rollback, ahead/fork, missing or substituted
  state fails closed. Every head time is bounded by the separate sealed clock
  floor. Exact successor replay returns success after cancellation or response
  loss, so privileged restoration of only the local cache cannot become
  authoritative. Signer and inventory drivers seal a second post-effect sample
  before persisting the journal floor; raw timestamp-taking and delivery
  transitions remain crate-private. A nested default-off policy projects
  independent 1--4,096 entry and 4--128 MiB checkpoint caps. It projects the
  combined durability provider, signer, and inventory as all-or-none
  credential-free runtime slots 57--59. Registry resolution validates exact
  configured metadata and stable pre/post qualification snapshots, but the
  stock broker implements none of the roles and resolution invokes no readiness
  or effects. The policy remains only an activation request. The stock launcher
  fails during pre-Tokio provider resolution because its broker does not support
  slots 57--59; if an injected registry resolves them, the shared
  `start_with_runtime_deps` gate still rejects `enabled = true` before supervisor
  startup. No scanner, store, signer, or inventory effect is activated. A
  retained take-once daemon coordinator extended to own the concrete combined
  durability/signer/inventory effect drivers and broker support,
  bounded readiness and supervision, singleton rooted session ownership per
  exact external provider scope across machines or provider-side session
  fencing, and production crash/cancellation/corruption/concurrency/platform
  chaos remain release gates; neither the pre-completion claim nor ordinary
  storage completion authorizes an attestation by itself. The
  verification lock rejects the root inside its node
  set, unreachable nodes,
  and propagated development edges; the semantic manifest must bind every
  direct dependency to the matching normal root edge by alias, package, and
  requirement. Semantic manifests, exact proof nodes and roots, resolver rows,
  and consumer-lock roots also reject duplicate parent-local aliases; parallel
  versions remain valid under distinct aliases. Resolver rows additionally
  reject an availability projection newer than their own revision. Core
  publication-proof validation revalidates each row and binds its row/storage
  revisions, storage height and equal-height block hash, and yank height to the
  claimed snapshot before accepting it as fresh-selectable. Portable-plan
  failures are classified at the source-tree
  surface rather than as an undifferentiated archive failure. Seed
  ingress resolves the registered chunker and invokes this shared verifier
  before operation reservation or admitted backend work. A maximal chunk
  witness is at least 720,896 bytes before file records and framing, which is
  why it is carried in the bounded envelope rather than the 64 KiB metadata
  header. The receipt path exposes an external signing-provider trait: the
  service constructs the exact payload and expiry, accepts only approvals,
  verifies the assembled
  receipt, and binds signer-broker plus seed-backend provider identity during
  construction. The runtime client and publisher engine enforce the same fixed
  30-second service-clock lead: a future-issued receipt inside that bound is
  persisted but leaves archive registration pending without a journal write
  until its issue time, while a receipt beyond the bound fails before
  persistence and only an actually expired receipt is restaged. It owns a
  fail-closed, non-regressing clock boundary with no request-supplied time,
  resamples after staging and signing, and refuses to cache a receipt expired
  by signer latency. The new Unix durable clock separates
  one-time initialization from restart open, holds a lifetime owner lock, and
  atomically persists a bounded, digest-bound canonical Norito high-water mark
  with private file and identity checks. Missing, corrupt, substituted, or
  regressing state fails with stable path-free codes instead of resetting the
  floor; non-Unix use remains unsupported until equivalent primitives are
  qualified. Both adapters require deployment qualification for a trusted,
  non-replaceable ancestor, rollback-resistant storage or an external sealed
  monotonic head, selected journal limits/peak memory, and the crash/cross-process
  fault matrix. The service also rejects broker thresholds
  that cannot fit the bounded approval set. The in-process `KeyPair` signer is
  explicitly a software test/development adapter; a concrete qualified
  deployment provider remains a deployment gate. Standalone and combined
  custom launcher APIs now accept a one-shot deployment-owned factory. After trusted
  replay and SoraFS construction, the factory receives the exact chain/genesis
  identity and live state, queue, and SoraFS handles; the resulting opaque
  runner joins the real daemon supervisor. Factory failures are redacted and
  occur before a child is installed, an early runner exit is fatal, and the
  runner shares the node shutdown signal. Stock `irohad` passes no factory,
  exposes no listener, and has no retired Torii upload fallback. Focused Cargo
  validation remains pending for the factory and fresh-reader verifier; the new
  `ReplicationOrderRecord` marker layout/roundtrip; retired-provider bounds;
  Core issue, renewal, marker deletion/injection, Active/Retired completion-set,
  and snapshot-substitution regressions; finalized-provider archive
  reconstruction, shared-order binding-conflict, and transition tests; runtime
  marker/opaque-claim mismatch tests; and compilation of every affected Rust
  literal and generated/SDK fixture surface. Unrelated Cargo/rustc jobs still
  occupy the shared build lanes. Targeted direct `rustfmt --check`, scoped
  whitespace validation, and the repository-wide missing-marker literal scan
  pass for this slice.
  The publisher-owned operation journal now requires bounded exact canonical
  Norito, rejects trailing or legacy bare frames, and holds a private
  operation-scoped OS lock across create and compare-and-set transitions so two
  resume processes cannot both overwrite one revision. Journal, staged-CAR,
  plan, lock, immutable-readback, and directory-sync opens now add qualified
  nonblocking flags to their no-follow policy; deterministic FIFO-substitution
  regressions prove a raced special file is rejected without hanging.


<a id="record-e7d570a089b30d1e24ad3897d56f92043075e811e711f57ae041fe3d510b2b84"></a>

- `publish --recover OPERATION_ID` now provides bounded pre-ingress recovery
  for a pristine revision-one Validation journal. It derives the exact package
  and verification graph from the journal without reading or rewriting the
  workspace lock or loading mutation credentials, rebuilds and compiler-checks
  the clean package, proves exact publication and archive-commitment equality,
  then locks and rereads the journal before idempotently staging the immutable
  plan first and the CAR second. Validation also checks the complete plan before
  any CAR/backend action. An exact-existing immutable-file retry now fsyncs the
  parent directory, then reopens the same inode and verifies its exact bounded
  bytes before returning `AlreadyPresent`, and finally revalidates the bounded
  directory snapshots. Descriptor-relative qualification and exact cleanup of
  an operation-owned temporary hard-link residue that can leave link count two
  after interruption remain open release gates; no broad cleanup scan was
  added. Unix command-level recovery fixtures now inject private state/cache
  roots without mutating process-global home variables and cover malformed
  signer/runtime configuration, a read-only invalid consumer lock under
  `--locked --offline`, authenticated dependency cache hit, cache miss before
  transport construction, selector/version recovery rejection before sidecar
  creation, and unreachable-graph admission rejection before any journal or
  sidecar exists. Their focused execution remains part of the pending
  Musubi library rerun below.


<a id="record-2dfc2beee2e5d082988acc9d792198d75de90d326411a8ddfbc078e5f0a54d5d"></a>

- Exact locked cache-miss fetches now bind finalized archive-location evidence
  to the lock's exact genesis-derived `NetworkId` and non-regressing snapshot before any
  provider transport. Wrong deployments, lower heights or index revisions, and
  conflicting equal-height snapshots fail closed; exact and later
  non-regressing snapshots remain valid, while verified global cache hits stay
  content-addressed rather than deployment-scoped. The production SoraFS fetch
  loader now parses `client.toml` as a TOML document under the same current
  parser semantics as the signer-free registry reader. Online resolution,
  recovery, and cache maintenance derive both registry and fetch configuration
  from one anchored, bounded, singly linked image read through an
  architecture-qualified nonblocking/no-follow stable descriptor. The parsed
  fetch boundary retains no raw image and defers token reads, DNS pinning, and
  HTTP-client construction until an immutable-cache miss. Fresh publication
  carries only nonserializable, Debug-redacted path/digest provenance across
  clean-package validation, then requires an exact secure reread before signer
  parsing; its registry reader, signer, and publication services consequently
  share that matching image. Resume constructs the same three boundaries from
  one current secure read, allowing legitimate credential rotation without
  putting a config-derived digest in the secret-free journal.


<a id="record-cad88021c0fab39fb0eabba825b2444f3b1c017662f937fd8bb93553cc090a77"></a>

- Archive registration now has bounded append-only intent and
  authoritative-record checkpoints inside phase three. The journal retains up
  to eight exact seed receipts, canonical registration-instruction digests,
  fee-quoted signed transactions, hashes, and terminal evidence. Pending,
  absent, or transport-unknown attempts are never replaced. Finalized
  `RetainUnknown` archive absence permits a fresh receipt and transaction only
  with authoritative `Expired` status or a consensus-committed finalized block
  time strictly beyond the exact transaction/receipt deadline; local and
  cache-only expiry never authorize rotation, and generic rejection is
  permanent. Applied status carries a nonzero block height and the
  authoritative archive page must
  cover it before `PublicationRegisteredArchiveV1` is persisted. The private
  coordinator request additionally binds the verification-lock digest and
  rejects fewer than three parsed-bundle attestations. The bounded retention
  response and Rust/Kotlin/Java/Swift decoders now carry that finalized block
  time. Focused state-machine and HTTP fixtures cover restart before and after
  submission, expiry,
  generation bounds, unknown/pending preservation, applied/rejected/absent and
  wrong-hash status, exact prebuild/hash checks, and authoritative-record
  conflict; real fee-quote and blocking-submit transports remain unexecuted.
  Core accepts an identical active archive-location Add as a validated no-op
  before the consumed CAS revision and verifies its structural, immutable
  attestation, and reverse-reference invariants without requiring later
  provider health. An archive registrant may bootstrap storage only while the
  archive is unpublished; once any release references it, every location and
  proof mutation requires the current archive-location capability across all
  referencing packages. Recovery or member removal therefore revokes the
  former registrant, while an identical proof replay by a successor manager is
  a no-op that preserves the original registrant audit. Explicit location or
  pin/order invalidation of a protected active or yanked archive must leave at
  least three distinct current providers across its remaining locations;
  ordinary provider loss may still degrade the row below fresh-selection
  quorum while retaining locked fetches. The publisher now obtains the current location CAS revision
  from a complete finalized page and reconciles a post-submit page with exact
  transaction status, closing the unrelated-location race. A separate bounded
  append-only journal retains at most eight location generations, each with its
  complete preparation page, exact signed CAS, applied finality, and terminal
  page. Generation-specific private-service idempotency keys make crash replay
  exact; prior stable IDs cannot be returned or resurrected. Finalized
  rejection/rebase, authoritative expiry, applied-then-retired, and later
  retirement evidence can append a terminal and coordinate a new ID, while
  unknown, pending, lagging, or coordinator-only claims cannot rotate. The
  workflow rechecks active finalized state before readback and release
  submission and after a typed release rejection, clearing derived checkpoints
  before replacement. Replication now journals the complete validated finalized
  directory page as its monotonic floor, and accepted terminal history retains
  that floor across durable reopen. Lagging healthy or retirement pages remain
  retryable and cannot overwrite a later renewal; equal finalized anchors,
  snapshot hashes, and archive revisions must reproduce the exact directory.
  The non-consensus-legal embedded provider-proof aggregate has been removed.
  Each complete signed parsed-bundle proof is now an immutable separately
  registered record with a 1 MiB canonical ceiling, while location ISIs,
  events, pages, and journals carry only the bounded sorted provider list and
  archive/order-bound aggregate proof-set digest. Core exact-loads every record,
  verifies its current finalized SoraFS completion binding, and recomputes the
  aggregate before location admission. The publisher persists a compact set
  descriptor plus no-replace exact signed-registration sidecars before the
  compact location Add. The main operation journal now retains a compact
  append-only hash anchor for the set sidecar and every signed-transaction
  sidecar. The production runtime installs or revalidates one immutable
  sidecar, returns the new anchor for a separate durable journal advance, and
  only submits from a previously anchored sidecar. Missing, substituted,
  reordered, or partially reproduced anchored sidecars fail permanently on
  restart. Provider-registration rejection can append another of at most eight
  attempts only when its non-zero rejection height is covered by a complete
  finalized page whose location revision strictly advanced; exact proof
  recovery accepts the immutable original registrar audit when another current
  package manager performs the replay.
  A finalized same-ID
  renewal resumes with its current pin,
  replication order, epochs, and exact provider bundle attestations instead of
  freezing mutable storage fields to the first coordinator response. Storage
  coordination now sends a finalized immutable registration projection
  (archive id, commitment, original receipt, registrant, and registration
  height) bound to the exact `NetworkId`, transaction, and snapshot, while the
  coordinator response carries its current mutable archive record for location
  CAS. Any later exact archive page can reproduce that projection, so
  production no longer requires a historical mutable WSV. The daemon now has a
  read-only authoritative reader that uses one coherent State view, Core's
  snapshot-history validator, a cryptographically verified registered-height
  V2-finality artifact, an exact result-bearing Kura block-wire commitment, one
  unique successful sole-native registration transaction, and exact current
  projection equality. Only locally ahead height/revision evidence is
  retryable. Production remains blocked on the effectful storage coordinator,
  production SoraFS pin/replication mutation backend, and deployment wiring;
  the reader does not activate the stock service.


<a id="record-aa2ddd056ae00e0001e241f177b2aa9a235d2035126f125949084f538c80b2f8"></a>

- Release submission now has a bounded exact Phase-A/Phase-B journal and
  production-backend path. A compact envelope losslessly reconstructs the sole
  V1 publish instruction and signature-bound creation time, lifetime, nonce, fee
  intent, primary signature, and optional canonical multisig proof. Each
  append-only intent retains the exact replication/readback floor, payload
  transaction hash, and a separate authorization-inclusive fixed-V1 wire
  digest. A successor requires finalized exact-version absence at one resolver
  and retention snapshot plus a terminal expiry, consensus-time deadline, or
  rejection outcome; rejection additionally requires a higher finalized
  same-location revision or finalized retirement and replacement. Derived
  canonical budgets reserve independent non-release, eight-attempt, and final
  evidence corridors and fail closed before accepting an intent that cannot be
  completed inside the 16 MiB frame. The engine persists the intent in one
  advance before any send. The registry backend reconstructs it and queries
  authoritative status for the payload transaction hash before each send or
  replay; the authorization-inclusive wire digest is a local durable replay
  binding, not committed-wire evidence. An absent transaction is submitted or
  replayed only when a fresh finalized directory reproduces the selected
  location byte-for-byte from the signed readback floor; stale, retired, or
  renewed locations leave the live intent unsent. Pending and lost-response
  recovery never re-sign. A matching same-publisher visible release may replay
  the journaled bytes through Core's idempotent no-op path but never fabricates
  application evidence without `Applied` payload status; publisher or
  commitment conflicts fail permanently. Finalized consensus time can
  terminalize a stale `Pending` status after the signed lifetime. Production
  Torii-body tests compare the reconstructed envelope through the real client
  payload encoder. Final verification now validates the complete paired
  home/universal response, then persists only a compact append-only checkpoint
  containing the request-derived operation id, chain/genesis identity, covering
  snapshot, release/archive identity, the complete immutable release-manifest
  digest, canonical digests of both exact projections, and a self-digest. The
  operation id binds the publication nonce and complete immutable request. The
  full mutable records no longer consume journal capacity. A near-8-KiB legal governance
  account regression proves that full query evidence grows while the compact
  completion size remains constant. Later paired yank, governed takedown, or
  below-quorum storage revisions can verify the already-applied immutable
  release without claiming fresh selectability. The self-digest is a local
  commitment, not an authenticated finalized-query receipt, so completed resume
  still relies on the documented trusted private-journal boundary.


<a id="record-fa52249b60cb0243082b36ab49e2ce530d5b3be91c67fe1cf0919d88952d631b"></a>

- The final publication checkpoint validates chain and genesis identities
  plus the nonce-derived operation id against the immutable operation request.
  Native AMX evidence also retains authoritative applied height and the final
  snapshot must cover it. A cross-operation, cross-chain, different-genesis, or
  pre-application page cannot complete publication; an unchanged exact row may
  legitimately have an older row revision than a later unrelated global
  snapshot revision.

