# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-f94f1e4f636273e7699a1ba8506eac5d724aadbc0095af80a485a8a38d4c2408"></a>

<!-- Original context: Status / 2026-07-22 Cross-SDK and input-boundary bug-hunt repairs -->
- Repaired two more advertised shell entry points: the SoraFS preview packager
  now passes its JSON config to Node through a valid here-document and avoids
  Bash-4-only `readarray`, while the iOS demo key generator closes its argument
  `case` correctly. The preview wrapper also reports a stable usage error when
  `--config` has no path. Syntax, help-path, option-error, and stubbed
  preview-submission tests pass.

