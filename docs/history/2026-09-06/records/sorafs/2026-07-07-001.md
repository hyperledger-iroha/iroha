# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-afa013e8328386861e5b7d55180fa2b8a116321ff0e1e667769cf5ada94bc8df"></a>

<!-- Original context: Status / 2026-07-07 SoraFS aggregate anchor-matrix coverage hardening -->
## 2026-07-07 SoraFS aggregate anchor-matrix coverage hardening



<a id="record-403c3073e5a13abe71a91763a75060532666c3c298db513143fa090c14e27c2c"></a>

- Hardened `scripts/tests/check_sorafs_production_readiness_test.py` so the
  aggregate production-readiness adversarial matrices for hex-list metadata and
  hex tuple-binding metadata must match their configured source-kind maps before
  they run owner-fingerprint mismatch checks. Future `valid_*` metadata fields
  can no longer skip aggregate mismatch coverage by missing the manual matrix.


<a id="record-47b34d083167bfbe4c0e4a29cb58073922a964daad747a27f4c23a8a661427b5"></a>

- Changed object-list metadata fingerprint checks to derive every tested
  digest field from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_FINGERPRINT_HEX_BINDINGS`, so multi-field
  object summaries such as billing cycles, evidence-viewer digest sets, and
  provider bakes now exercise every configured digest tether instead of a
  representative field.


<a id="record-0c4c9786cad9f57049f73b117d59dfd5739e8fddb362acbd1f1a3efc3692f47c"></a>

- Changed object-list required-artifact count checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_SOURCE_KINDS` and
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_REQUIRED_KIND_COUNTS`, so future
  object-list metadata surfaces cannot miss count-mismatch coverage.


<a id="record-e011a571c524e9dff658f39408f441e6f76de8e66cfb6f5f8e76162ceadf9704"></a>

- Changed object-list domain-identity duplicate checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_DOMAIN_IDENTITY_FIELDS`, schema metadata,
  and each configured object-list source kind, so future detail lists cannot
  skip duplicate-domain adversarial coverage.


<a id="record-5f34e77501bea55d2a4e0796732dfd8a6f72206d185fe9ab7e818d6b0b49e22b"></a>

- Changed object-list string identity policy checks to derive forbidden-marker
  and pattern-mismatch cases from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_STRING_FIELD_POLICIES`, so future
  lane-owned identity labels automatically get no-echo policy replay coverage.


<a id="record-11e44d54a19cde73f79b4147410b963fb328bcc497921941425104ad9f28a736"></a>

- Changed required/allowed string-list and companion count-binding checks to
  derive from `PAYLOAD_FREE_SUMMARY_REQUIRED_STRING_LIST_VALUES`,
  `PAYLOAD_FREE_SUMMARY_ALLOWED_STRING_LIST_VALUES`, and
  `PAYLOAD_FREE_SUMMARY_STRING_LIST_COUNT_BINDINGS`, so new metrics or closed
  string-list policies automatically receive missing-value, unknown-value, and
  count-drift adversarial coverage.


<a id="record-b2d9c2bfae31051105ca5a5cfd6ca35baa9208e0c13b2788d2889ab45537dbb6"></a>

- Changed ordered list uniqueness/sort checks to derive from
  `PAYLOAD_FREE_SUMMARY_ORDERED_LIST_METADATA_FIELDS` across every gate/field
  pair, so future digest, binding, integer, string, or metric lists cannot skip
  duplicate-entry or canonical-order adversarial coverage.


<a id="record-1e14eb4a2f73e334153d93437eb596b455fbe7666742e263b7956eccade301b7"></a>

- Changed required lane metadata presence and list-shape checks to derive from
  `GATE_METADATA_FIELDS` and `PAYLOAD_FREE_SUMMARY_LIST_METADATA_FIELDS`, so
  every configured gate/field pair now gets missing-field, non-list, empty-list,
  and no-echo shape coverage.


<a id="record-733604a6335d4c2582b4688515181d50a8323cbbc92211d9ad68be158b8f126d"></a>

- Changed cross-lane metadata rejection checks to derive every payload-free
  metadata field from `PAYLOAD_FREE_SUMMARY_METADATA_FIELDS` and exercise it
  against a gate that does not allow that field, so future lane metadata cannot
  bypass disallowed-field or no-echo coverage through a hand-picked sample.


<a id="record-292c2ae02faea35c76caaec7c0d113bbfbe1f60d7a166cede92986c81794e7fe"></a>

- Changed final-production deployment-context negative checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_METADATA_FIELDS` and
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_METADATA_FIELDS`, so every configured
  top-level or object-list metadata surface carrying `deployment_id` and
  `environment` now rejects non-production IDs and environments without echoing
  the forged values.


<a id="record-3e409ac447dfe00000a5d290169f00a770eb29dbddc5400d929212ff56824546"></a>

- Changed object-list metadata schema validation checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_METADATA_FIELDS`, so every configured
  string, positive-integer, hex, non-object, sensitive extra-key, and ordered
  integer-pair rule now gets fail-closed/no-echo adversarial coverage.


<a id="record-e4a1d58362a28c587b297f83b09d3df1c0742d9856ef852a8263c70d5d0e3488"></a>

- Changed object-list duplicate-entry checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_METADATA_FIELDS`, so every configured
  object-list metadata schema now rejects exact duplicate entries without
  echoing deployment IDs, digest values, or lane labels.


<a id="record-374edab09a6bd19c787a4fb5a0187950157705be03e2a4ba7a6ec97b6c9512fa"></a>

- Changed object-list owner-kind tether missing/drift checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_LIST_SOURCE_KINDS`, so every configured
  object-list metadata field now proves fail-closed behavior when its source
  artifact tether is removed or pointed at the wrong required artifact kind.


<a id="record-974ad4524e7333bddfb71a2061b36ce3c25e927b0e9c6e7fd0c600c4b4ba839d"></a>

- Changed non-object-list owner-kind tether missing checks to derive from every
  configured scalar, list, string-array, positive-integer, and binding
  source-kind map, so removing any payload-free metadata-to-artifact tether now
  fails closed under adversarial coverage instead of a single sampled field.


<a id="record-3a5146ac621a14180f129c4f04af6cc9a90dc1b3ef177cf9f7d147f1f1966c53"></a>

- Changed hex-list metadata entry-shape checks to derive from
  `PAYLOAD_FREE_SUMMARY_HEX_LIST_METADATA_FIELDS`, so every configured digest
  or CID list now rejects malformed, uppercase, and object-shaped entries
  without echoing raw values or sensitive-looking keys.


<a id="record-030f1e3fa5ea6944e2fcf1914607b3b709f857d473c36f9fcfa63ca42d7333a1"></a>

- Changed hex-binding metadata entry-shape checks to derive from
  `PAYLOAD_FREE_SUMMARY_HEX_BINDING_METADATA_FIELDS`, so every configured
  binding field/key now rejects uppercase values, missing keys, non-object
  entries, and sensitive extra keys without echoing attacker-controlled values.


<a id="record-19174c9dde45af80a80d813ba07582071034bcc43a0830ed7bf4313b775d68b2"></a>

- Changed positive-integer list entry-shape checks to derive from
  `PAYLOAD_FREE_SUMMARY_POSITIVE_INT_LIST_METADATA_FIELDS`, so every configured
  count-list metadata field rejects zero, negative, boolean, string, and object
  entries without echoing raw values or sensitive-looking keys.


<a id="record-e2403f012eade690fe448db1aa9b5bf436cadff3bd3d0689418058555a6e7160"></a>

- Changed string-list and string-array entry-shape checks to derive from
  `PAYLOAD_FREE_SUMMARY_FINGERPRINT_STRING_LIST_SOURCE_KINDS` and
  `PAYLOAD_FREE_SUMMARY_FINGERPRINT_STRING_ARRAY_LIST_SOURCE_KINDS`, so every
  configured string metadata list rejects malformed strings, non-strings, and
  sensitive object entries without echoing raw values.


<a id="record-f30be61abc69c33e2304c2af349e361bf69d168d884bf8c656707e62dc00fefb"></a>

- Changed scalar hex metadata entry-shape checks to derive from
  `PAYLOAD_FREE_SUMMARY_STRING_METADATA_FIELDS` and
  `PAYLOAD_FREE_SUMMARY_HEX_METADATA_LENGTHS`, so every configured scalar hex
  field rejects malformed strings, uppercase values, and object-shaped values
  without echoing raw values or sensitive-looking keys.


<a id="record-bd106e7e2a6dda8fae2850a6b2064840e18ce54d24dcb70c7c348b660d22505c"></a>

- Changed top-level object metadata entry-shape checks to derive from
  `PAYLOAD_FREE_SUMMARY_OBJECT_METADATA_FIELDS` and `GATE_METADATA_FIELDS`, so
  every configured object metadata surface rejects non-object values,
  noncanonical keys, missing or invalid required strings, and sensitive extra
  keys without echoing raw values or sensitive-looking keys.


<a id="record-77c78688b80fadc91065cef4d1b3531918058cfce461e771241b3a480c997eda"></a>

- Changed deployment-context metadata/artifact consistency checks to derive
  from every configured top-level object or object-list surface carrying
  `deployment_id` and `environment`, so any future deployment-context metadata
  field must prove fail-closed mismatch diagnostics without echoing forged
  deployment IDs.


<a id="record-923d4a9aaeca92b89f537181aa8b721d1fd26e5277045993be083bc8a0e193f4"></a>

- Changed artifact fingerprint deployment-context production checks to derive
  from every configured required artifact kind across the aggregate SoraFS gate,
  so non-production deployment IDs and environments are rejected for every lane
  artifact surface without echoing forged runtime labels.


<a id="record-3336e00ec87729abbbccfce44cedd2ee93b444da06836b9dab0bd180ba9965cc"></a>

- Changed required-row and artifact schema binding checks to derive from every
  configured required artifact kind in `GATE_REQUIRED_KIND_SCHEMAS`, so row and
  artifact schema drift fails closed across all lanes without echoing forged
  schema labels.


<a id="record-78068d433fc5b00890525fca13c0e9d6be7e9073e0187e9d560bcc5ba7cdc7fa"></a>

- Changed required and recognized artifact digest-shape checks to derive from
  every configured required artifact kind, so uppercase or malformed SHA-256
  identities fail closed across all SoraFS aggregate lanes without echoing
  forged digest values.


<a id="record-18e209080ae6882b177d1325d660fb0fe2723e7b7bb821de3c3dfa8fa266c8b4"></a>

- Added malformed artifact digest-shape checks derived from every configured
  required artifact kind, so missing, boolean, or object-shaped SHA-256
  identities fail closed across required and recognized artifact rows without
  echoing hostile digest payloads.


<a id="record-1016a8af5ea944546198840632692558d1107d0d6645664b55c2f2a015b4f05f"></a>

- Added required-kind and required-row inventory checks derived from every
  configured gate, so scalar, noncanonical, narrowed, duplicate, unknown, or
  extra required inventory labels fail closed without echoing hostile labels.


<a id="record-0d005ef26c86bf8d4d0e7a6107c77152064f729d5732a899e992ce918a357129"></a>

- Added recognized-artifact inventory shape and count checks derived from every
  configured gate, so missing, scalar, empty, or count-mismatched top-level
  recognized inventories fail closed without echoing malformed payload values.


<a id="record-de483013dadc12ea90b4764703817b43437967fbf6f6b2d5cb3887a9fe7838e2"></a>

- Added top-level count invariant checks derived from every configured gate, so
  malformed count fields, evidence-count overflows, recognized path-count
  drift, and required-row total drift all fail closed without echoing forged
  count payloads.


<a id="record-43be959fd2c674f6dceab8745102e6a8db0f251383e6af2f3ba381d4de858dd8"></a>

- Added aggregate output row contract checks derived from every configured gate,
  so ready and invalid aggregate rows reject drifted contract counts, expected
  kind lists, timestamps, paths, hashes, environments, and sensitive fields
  without echoing hostile payload values.


<a id="record-a073b0eead655e4ccd81b6da2f4b9e43c2041b2a85c2435dcd9a81250a2c621a"></a>

- Added aggregate summary envelope contract checks derived from every
  configured gate, so required-gate inventory drift, count overflows,
  malformed counts, deployment drift, row deployment drift, and sensitive
  summary fields fail closed without echoing hostile payload values.


<a id="record-4fd4a089ef35f6dc184652ead3c26bdb231d430d31489236e3925e3c81a2fcf6"></a>

- Added duplicate and explicit-unrequired summary input diagnostics derived
  from every configured gate, so duplicate summary rows and explicit summaries
  for unrequested gates fail closed without echoing duplicate paths or
  unrequired schemas.


<a id="record-e3735052b7116f71553249745c3b3ec208a9e03452ea81b00238eb9b5c226841"></a>

- Added unknown-schema summary input diagnostics derived from every configured
  gate, so stray SoraFS summary schemas fail closed without echoing forged
  schema labels or file paths.


<a id="record-f4ec3b50ee42bfa2e950edc437218c8873571aeaa503f5c90df930bf33518489"></a>

- Added missing-summary row diagnostics derived from every configured gate, so
  absent required summaries produce deterministic schema-closed blocked rows
  without path or payload material.


<a id="record-887be47645111c8e55c51db8504b0b84d93a5be9350bdbb5ba594d7f1d1f25f3"></a>

- Added evidence-source conflict diagnostics derived from every configured gate,
  so duplicate explicit inputs, explicit/directory overlap, and overlapping
  evidence directories fail closed without echoing operator paths.


<a id="record-d6c62bd2c74f7d1a660b51dadb093d3c1d004ab9baa324e4f94bab53a09805d5"></a>

- Added lane status and load-error checks derived from every configured gate,
  so blocked lane summaries and non-empty or malformed `load_errors` fail
  closed without echoing lane-provided diagnostic payloads.


<a id="record-55e97bb05392ac140bde3238cca7bb093615a2c36a7d4f1fe878a91dcb0b2882"></a>

- Added lane threshold shape checks derived from every configured gate, so
  missing, scalar, empty, noncanonical, sensitive, or non-integer threshold
  fields fail closed without echoing hostile threshold payloads.


<a id="record-4a55801c7dfee76e022db7a956d302951455ce82e58696ed32fc6dcad5a2aa3c"></a>

- Added payload-free lane summary envelope checks derived from every configured
  gate, so extra top-level fields and sensitive canonical or noncanonical keys
  fail closed without echoing hostile summary payloads.


<a id="record-f137e2424942509d8d2df04c4adc0666f2394f8bcd04283df6690e5707213905"></a>

- Added artifact freshness timestamp checks derived from every configured
  required artifact kind, so stale or future fingerprint timestamps fail closed
  across all gates through required and recognized rows without raw timestamp
  field diagnostics.


<a id="record-b542ba13c0ad25460455dafd94ada3798688ecdcbc0a063490b75c3a0aef16c5"></a>

- Added artifact generated-at timestamp shape checks derived from every
  configured required artifact kind, so missing, boolean, non-positive, string,
  or object-shaped fingerprint timestamps fail closed across required and
  recognized artifact rows without echoing hostile timestamp payloads.


<a id="record-c5a55bb88a38916739240ba14cbed50de58d7dd6e1072c4a232b7fe4a5f8da67"></a>

- Added artifact fingerprint shape and sensitive-key checks derived from every
  configured required artifact kind, so non-object fingerprints and sensitive
  fingerprint metadata fail closed across required and recognized rows without
  echoing hostile key material.


<a id="record-a71b7fc97e8653329775f87ad20a1cb19264d1e544e1fd7db494f610851d7a49"></a>

- Added artifact fingerprint deployment-context shape checks derived from every
  configured required artifact kind, so noncanonical deployment ids,
  noncanonical environments, and unreviewed markers fail closed across required
  and recognized rows without echoing malformed context payloads.


<a id="record-d517c31f9200c87c0d29ed70a9fc08d28e52a12d1ab9bb25938f029586d08d97"></a>

- Added artifact deployment-context required-field checks derived from every
  configured required artifact kind, so missing fingerprint deployment ids or
  environments fail closed across required and recognized artifact rows.


<a id="record-5f78f0aaedaf853562cdaf880ac89c71bea22bc052ad0abdd20a1bfd978f347f"></a>

- Added artifact deployment-context reviewed-marker shape checks derived from
  every configured required artifact kind, so missing, string, or object-shaped
  review markers fail closed across required and recognized artifact rows
  without echoing hostile review-marker payloads.


<a id="record-3f6d54bd9df33de24a05401bf33c6a626c2b377a7b1691faccc87b80319f04d0"></a>

- Added artifact optional label canonicality checks derived from every
  configured required artifact kind, so malformed `schema` or `status` labels
  fail closed across required and recognized artifact rows without echoing
  hostile label payloads.


<a id="record-5012dc63919809e86924fa3d5b1137e7b324d1e3e469ee88b2aea49fd7b68fcd"></a>

- Added artifact schema-shape checks derived from every configured required
  artifact kind, so missing or object-shaped schema labels fail closed across
  required and recognized artifact rows without echoing hostile schema
  payloads.


<a id="record-7605bf68776cf564005798e3e5c8420fd21a43c10f17164d53ac8d98ead74aa1"></a>

- Added artifact status-shape checks derived from every configured required
  artifact kind, so missing or object-shaped status labels fail closed across
  required and recognized artifact rows without echoing hostile status
  payloads.


<a id="record-0ea7b7e9f83e08d2f879d53fd3ca5b32bd3173f5fc4cd51c6f4c7aefb0f2e153"></a>

- Added recognized-artifact per-kind count-binding checks derived from every
  configured required artifact kind, so retagging one recognized artifact to a
  different valid kind fails closed without dumping the required-artifact
  structure into diagnostics.


<a id="record-6fec6bcb07a1fced901e925bed9bec55a058d6d87d783fdd88874609ed692d11"></a>

- Added recognized-artifact canonical identity-drift checks derived from every
  configured required artifact kind, so a valid but wrong `(kind, path,
  sha256)` identity fails closed across all SoraFS aggregate lanes without
  echoing the forged digest.


<a id="record-dcc0749260ae0386e8949da818dfb09777d92da8a3658aa2010b2ae121d17988"></a>

- Added required-artifact duplicate path checks derived from every configured
  required artifact kind, so two required artifacts cannot reuse a path with
  different digests without failing closed across all aggregate lanes.


<a id="record-43eb42ad2599e1b5aa495ca12efc8229c2ffebbd3c63ee50684814328c2950d3"></a>

- Added recognized-artifact duplicate path checks derived from every configured
  required artifact kind, so any recognized row that reuses another artifact
  path fails closed across all aggregate lanes without echoing the forged path.


<a id="record-0105cba17a073c52a55a1fb14a5c9cebb0624e8fe7ec302b4d03c591ffcf3654"></a>

- Added required-artifact duplicate identity checks derived from every
  configured required artifact kind, so repeated `(kind, path, sha256)` rows
  fail closed across all aggregate lanes with explicit duplicate-path and
  duplicate-identity diagnostics.


<a id="record-b0a3e5d18244560d957c0af84c92347158a3bfb5c86df35976e0dbc2d2a16978"></a>

- Added required-row state checks derived from every configured required
  artifact kind, so forged `present`, `valid`, or `artifact_count` row values
  fail closed across all aggregate lanes without echoing forged labels.


<a id="record-d8c75c0beed187ba0c3bc74ac335fb1048fd98a5476dc6aa0b3e234869761ada"></a>

- Added required-row presence and shape checks derived from every configured
  required artifact kind, so missing rows or non-object row payloads fail closed
  across all aggregate lanes without echoing forged payload labels.


<a id="record-894a3f67c914b75c5e56c7b823c87b6549c40ca778421359bb29a9f2651c4a3a"></a>

- Added required-row schema canonicality checks derived from every configured
  required artifact kind, so padded, control-character, or object-shaped row
  schema payloads fail closed without echoing hostile schema material.


<a id="record-a69ccf69d12e570cf6363433e8b750dc1f92fcd39e68b96c3dc72a9d615324a4"></a>

- Added required-artifact inventory shape/count checks derived from every
  configured required artifact kind, so missing, scalar, empty, non-object, or
  count-drifted required artifact inventories fail closed across all aggregate
  lanes without echoing hostile inventory payloads.

