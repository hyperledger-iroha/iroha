# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-cdb6d0b18e692cba3e340fd1bfcae51bab57c3e58cc9ac1188d4de70aea43731"></a>

<!-- Original context: Status / 2026-08-02 SoraFS V1 closure-claim audit -->
## 2026-08-02 SoraFS V1 closure-claim audit



<a id="record-215dc3f5b80475ff3f1c60a450531c7ddd5556ed0a5edf644148c8c52a26d9e2"></a>

- Rechecked the closure ledger, reference-SDK plan, current roadmap, fixture
  READMEs, and the repository documentation split. The signed offline inventory
  remains exactly 82 payload artifacts, 32 `ValidationOutcomeV1` goldens, and
  38 negative vectors; all eight appeal-finance cancellation payload files are
  mandatory. This is read-only verification of the checked-in bytes, not the
  Cargo-owned true double regeneration, which remains pending on the final
  clean tree. No active SoraFS document points at the retired `docs/source`
  fixture tree, and no in-repository localized SoraFS mirror remains to carry
  stale source metadata.


<a id="record-14c74c8d190482f3c0cf74d9dfbd0584f6fd39ed4d1d889482608b87d7c6f8c4"></a>

- Hard-removed the two former moderation viewer aggregate-audit POST routes
  from the route catalog, Torii router, handlers, and OpenAPI snapshots. Added
  negative router/OpenAPI regressions for their absence while retaining
  `/v1/evidence/audit` and `/v1/evidence/status` as the canonical signed receipt
  projections. Cargo validation of this hard cut remains pending.


<a id="record-d3824b1ac5f140e6d108675667cd8a741458c5b97c7e33c6b23ffa5adfed9955"></a>

- Closed the moderation terminal-compaction source gap without claiming a
  deployment. The finalized orchestrator now seals a canonical archive prefix
  spanning delivered notifications, resolved notification dead letters,
  terminal native operations, resolved durable dead letters, and completed
  handoffs; obtains a checkpoint-authority source attestation; requires exact
  immutable install/readback and an unchanged authoritative checkpoint before
  prune; and persists predecessor-linked Ed25519 heads with explicit signer
  rotation. Authenticated head publication/readback, restart replay,
  incremental audit, and operator-triggered bounded full-history audit fail
  closed. Focused Cargo validation and exercise against genuine immutable
  storage/checkpoint/publication providers across replicas remain open.


<a id="record-6f43fb01f774b4405daa0bfdc54582576f524f91d6babc9b359850db614c04e7"></a>

- Closed the remaining Governance DAG sizing-clone source gap. Canonical node,
  block, head, CID, and signature construction uses borrowed Norito views that
  are wire-equivalent to the owned forms under every layout; source payloads
  are measured with the allocation-free counting encoder, and checked envelope
  bounds reject oversized node/block shapes before allocating or cloning dummy
  frames. The exact-size, one-byte-over, wire-parity, and digest-parity tests are
  present. The complete serial `governance::tests` filter is green (`142`
  passed; `0` failed; `0` ignored), closing local execution of the current
  producer/filesystem hardening only. External allocation-flood qualification
  and deployment evidence remain open.


<a id="record-385eca4bac096af3766f5133c69b5353de40259f46ddc2282f85f7636ab33b1c"></a>

- The generic source-kind transparency write remains removed. Appeal-finance
  report and weekly-rollup publication use only their exact canonical-account-
  signed routes, require `sorafs_appeal_finance_publisher`, and bind
  authenticated provenance; both GET readbacks remain. The supervised
  finalized-view/external-software-signer producer and clean exact-candidate
  regeneration of the five stale OpenAPI artifacts remain open.


<a id="record-a86db04a9aeeffe67916210fd9cd49c23892e2079b44eb2855c8246cc79ec93b"></a>

- Gateway-compliance catalog-approval and regional-acknowledgement rosters are
  now administratively disjoint by both signer identifier and Ed25519 public
  key. Cross-role reuse fails during trust-policy validation before feed
  provider qualification or checkpoint-store access; the exact startup
  regression passes with one selected test and 3,756 filtered. Genuine
  feed/ACME adapters, independently administered gateways, failover, probes,
  and L1/L2 evidence remain open.


<a id="record-33d3bf7ed526af54a53800febbe9992b381188909ec46b871db1c1e2f4f87ec1"></a>

- The reference-validator release packager no longer accepts the pre-release
  `--skip-smoke` selector. Every package now runs both committed advert and
  closed-bundle smokes, requires each output to be a strict successful
  `ValidationOutcomeV1` with its exact code, timestamp, required tags, and
  bounded canonical fields, includes both outputs in the deterministic
  archive, and records their exact digests with `smoke_checks=true`; the old
  selector is an unknown-argument negative. All 33 packager tests, all six
  strict capture tests, and all 154 release-automation tests pass. Five-target
  binary builds and live release evidence remain open.


<a id="record-f4c1076ece38b31f0bea73be6a7c667b80a82bb5e24f7b8a21a610c03d7b9fe5"></a>

- PoP projection reads now preauthorize, drain finalized successors under the
  V1 collection ceiling, requalify the runtime providers, and reauthorize the
  final projection. Added catch-up/restart, cached-reader failure, exhaustion,
  and unauthorized-no-work regressions; focused Cargo execution remains
  pending.


<a id="record-abb564f365f75af322de5f6929f10e0fb36689a399d0773823f8164d4f11c33d"></a>

- Updated native-artifact wording for the current Swift hard cut: package
  admission requires an embedded `NoritoBridge.artifacts.json` declaring exact
  ABI 21, and runtime loading rejects missing or stale manifest ABI metadata
  before hash acceptance. Ignored or locally rebuilt artifacts are not
  qualified regardless of their reported ABI; no checked-in, clean-source,
  five-target ABI-21 inventory or authenticated native execution record exists.
  Clean five-target builds, unskipped parity, and authenticated provenance
  remain open.


<a id="record-d92bf57193d9bf0e2b740bc13d118421a8c28c243a8684840dffef518f10e111"></a>

- Provider ingest still has the supervised standard-daemon worker, exact
  finalized assignment/policy revalidation, and daemon-owned provider-indexed
  archive. Deployment-owned authenticated child transports, the governed
  authenticated external signer backend, sealed-CAS retention backend,
  remaining focused/workspace validation, and genuine four-validator evidence
  remain open.


<a id="record-66252a59f62e2ba262bfe1bf30419bc63c5a3c7688c9fa1eaf8eb0730a54344a"></a>

- Closed two Governance DAG repository-source/package gaps without claiming a
  deployment: the standalone validated service view now has a public exact
  broker-catalog projection containing only slots 8 and 10 for IPNS or slots
  8, 9, and 10 for signed HTTP, never producer signer slot 7. The canonical
  build script, deterministic release bundle including its Windows executable
  mapping, and generic OCI image inventory now include
  `sorafs_governance_dag`. The fixed broker endpoint accepts a canonical,
  non-empty client subset only when every requested binding matches the
  server-qualified catalog byte-for-byte; live qualification and subsequent
  operations are confined to that authenticated subset. Focused tests cover
  proof-only subset confinement and the standalone Governance service view.
  The sanitized catalog can now cross the deployment boundary as one
  explicitly versioned canonical Norito artifact without exposing
  `actual::Config`: export/load enforce a 256 KiB ceiling, exact canonical
  encoding, chain identity, strict ordering and slot multiplicity, and retain
  only public handles, identities, keys, revisions, bounds, and policy
  digests. Six codec negatives, an eight-family production-shaped
  configuration matrix, and the external-crate API test pass; every matrix
  case double-exports and load/re-exports byte-identically. The shared
  `RuntimeProviderBrokerExecutableV1` process shell now adds the exact
  catalog-only CLI, secure absolute-path bounded loader, redacted diagnostics,
  readiness/lifecycle integration, and SIGINT/SIGTERM shutdown. It accepts no
  credential, private-key, plugin, test-provider, or endpoint override, and
  unsupported platforms fail before catalog access. Focused execution of this
  newest shell remains pending.
  The same handoff now rejects relationship or configured-bound drift:
  appeal signer/checkpoint overlap, PoTR identity reuse, incomplete fenced
  privacy pairs, provider-ingest source/resolver/signer/checkpoint mismatch,
  moderation/evidence checkpoint/archive aliasing, and Governance IPFS/head
  body-bound mismatch all fail before export and after load; export also proves
  exact reconstruction from the public bindings. The complete 13-test catalog
  suite passes.
  A concrete deployment-owned registry and vendor-linked broker executable
  with genuine production backends, broker and service supervision,
  clean-source five-target artifacts and smokes, and all L1/L2 deployment
  evidence remain open; source/package inventory is not deployment
  qualification. V1 currently supports a statically linked registry binary;
  no versioned authenticated plugin/IPC ABI exists for a generic executable.


<a id="record-474b12f99f436d730cf3f347a9622bed8cdad747705c476086733589ca772069"></a>

- One shared canonical WebAuthn policy now governs configuration, the runtime
  registry/catalog/broker, and evidence-viewer verification: RP IDs must be
  lowercase multi-label ASCII DNS names, and origins must be exact HTTPS
  origins bound to that RP ID, without credentials, path, query, fragment,
  IP/localhost aliases, or noncanonical default ports. Five focused
  configuration tests pass; the 13-test catalog suite above includes the
  corresponding catalog-projection rejection coverage.


<a id="record-373d75117c88ccb48bc41c09574f0611627b2b8bef71ce83de217ce44d43271b"></a>

- Provider ingest now performs its source-pool, resolver, signer, and sealed
  checkpoint qualification as a state-free startup preflight before creating
  the supervisor, archive, Sumeragi, node handle, or completion outbox. The
  opaque qualified token retains the exact checkpoint provider and revalidates
  every adapter before runtime assembly; checkpoint preflight never loads or
  mutates checkpoint state. Four focused preflight regressions and the exact
  opaque-preflight consumption/revalidation and main daemon startup-order
  regressions are green (each `1 passed`, `556 filtered`).


<a id="record-dfaa7d578bc5491a869ea8dfa90b915dab3a83c6637d9537bce197e0a8999c22"></a>

- The checked aggregate remains blocked with zero supplied and zero recognized
  lane summaries (`0/17`). No L1 deployment qualification, 17-lane evidence set,
  authenticated external software Ed25519 prerequisite signature, or L2 ready
  decision is claimed.


<a id="record-0a882e39f641d3bcbb85f72d9f7eea0e754d811ee26d79ad65471f1b7782e8b2"></a>

- The latest complete aggregate production-readiness regression run passed all
  465 current tests; earlier complete runs covered the then-current 459-test
  suite. A second complete 465-test final-tree replay remains required. The
  current run includes positive construction/replay and the
  archived missing, duplicate, stale, predecessor, signature, mapping,
  substitution, path-safety, and tamper negatives. This validates the local
  checker contract only; it does not replace the absent 17 genuine summaries.


<a id="record-bcf53b1fdbb17f3c97a8b6a81e91d062527029aa387cdab24794748d51dfb4c8"></a>

- The read-only final promotion control now conjunctively revalidates the two
  byte-identical ready aggregate executions and their exact 22-input replay
  manifest, the local-only fixed six-case negative archive, and a fresh
  operator-pinned external-software Ed25519 receipt binding its cosign/OIDC
  provenance, ordered receipts, runner/checker/toolchain/runtime, and positive
  hashes. Its 36 focused synthetic tests pass, including local-only, archive
  order/count/tamper, input/replay/cosign substitution, stale/future receipt,
  signer-trust, duplicate-key, symlink, and hardlink negatives. No genuine
  provenance was created, and the current `0/17` evidence remains
  non-promotable.


<a id="record-da7a17f2fbb9159a9ed4cfbcc04b5888a3c141f61d47a4a91e38d2c6e31c8a6e"></a>

- A fresh pinned-Python-3.12 release/SDK matrix passes all 268 fixture-workflow,
  ABI-21 artifact, strict-capture, packager, and release-automation tests. The
  offline inventory checker separately accepts 82 payloads, 32 exact outcomes,
  and 38 negative vectors; this remains static/managed validation, not native
  artifact or deployment qualification.


<a id="record-f36b5845962efcf62a4265ed5ae0dcb1cf7d00c69980eba0b50cb5e9c1030f58"></a>

- The `sorafs_node` PoR CLI fixture now signs its canonical proof payload with
  a deterministic test-only Ed25519 key instead of inert placeholder bytes;
  the expected proof digest is
  `e725de3d9e31f4d5150cb9f26122f7e4ca1c21b177c1c27a3e7047ae7832a9da`.
  The locked/offline CLI integration suite passes all six cases. This is
  fixture validation, not provider, HSM, or deployment evidence.



<a id="record-18ef65d43d0da19265114a95c1dc388448563051eeced5098228240216dac451"></a>

<!-- Original context: Status / 2026-08-02 Iroha CLI regression repair -->
- Updated the generated SoraFS gateway configuration and its direct-mode
  documentation fixture to use the current `[sorafs.gateway]` hierarchy and
  Norito-compatible `{ secs, nanos }` duration values. Both generated and
  checked-in snippets now have schema-backed regression coverage.


<a id="record-26ddad4ed5110108759dec2b5d52e193315631633b6cda289e5742e5dc4c5418"></a>

<!-- Original context: Status / 2026-08-02 Confidential unshield proof-output binding -->
- Canonical exporter, renderer, and SoraFS replay substeps complete; Android
  metadata parity, exact six-field assertions, the documentation hash-tree test
  (1/1), and scoped diff checks pass. A locked/offline Core/Torii check cleared
  the H22 data-model, configuration, state, and world paths before stopping in
  unrelated Jindo compilation. Focused Core, IVM, bridge, fixture, and SDK test
  execution therefore remains open.


