# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-b0fd4e6e2522fd842377a4cdbe6e36bca548e024a4e2ab46ae274c7a6ba0f7ad"></a>

<!-- Original context: Status / 2026-08-12 Sumeragi V1 consuming lifecycle-storage startup owner -->
- The durable Ready-Fetch recovery cut now owns the exact opened
  `LifecycleLedgerStoreV1` as well as its decoded LedgerV1 frame, verified
  height context, non-clone body store, and complete authenticated Fetch
  census. Startup cannot substitute a same-context path or byte-equal copied
  frame; the retained store is reloaded immediately before coordinator open.


<a id="record-a286e8287933d632882695ecd0b72b1edf89c32df5f5d6e3a425da4142805f06"></a>

- One consuming transaction joins that cut with terminal Validate outcomes and
  the authenticated Certified-Serve payload store/cut, installs every recovered
  Fetch carrier into a fresh concrete registry, proves full
  registry/coordinator equality before durable publication, and returns one
  opaque owner retaining the verified context, coordinator, registry, payload
  store, and body store. The final ledger publication reloads and compares the
  authenticated predecessor frame, so same-store drift after preparation fails
  without overwriting it. Unsupported live classes fail closed and no recovery
  parts or compatibility path are exposed.


<a id="record-cfe581dca6de4c28a31e115ce8bd5d4d677c6e4130394c4bd5c6ffef57a78096"></a>

- Focused BLS fixtures cover empty and two-Fetch opens, terminal Validate with
  live Serve/Producer coexistence, foreign stores, corrupt QC/source/census,
  a retained ledger changed after cut mint, prepare-to-commit same-store drift,
  unsupported live work, and byte-for-byte no-additional-write failures. Direct
  edition-2024 formatting and diff checks pass; Rust compilation remains
  pending while the active Isabelle process owns the repository validation
  lane.



<a id="record-b7cd92f9d41ccb8d6ffa363c1b5c39e1c9307bc8dbdb6f4b965120696b747e0d"></a>

<!-- Original context: Status / 2026-08-12 Merge resolution, closure, and build-efficiency checkpoint -->
- Post-merge Sumeragi integration initially exposed 94 `iroha_core` compile
  errors plus one hidden borrow conflict. The bounded visibility, delegation,
  module-path, concrete-runtime, and borrow repairs now compile through
  `iroha_core`, `iroha_torii`, and `irohad`; the exact locked/offline
  provider-ingest quarantine/restart regression passes (`1` passed, `682`
  filtered). `Cargo.lock` remains byte-identical at SHA-256
  `0ddb3f3938cf32035371317100674cd1601c3cb41232237f7a7d28b3aeab6222`.


<a id="record-df016c083cc4bc30c3ae9a005693673ce7e27532a04cc2459c25a817509d011d"></a>

- The multilane ledger now distinguishes the implemented `ML-LIFE-01` through
  `ML-LIFE-05` lane lifecycle from the staged generic lifecycle-coordinator
  replacement. The latter remains intentionally unwired and outside this
  finish scope; the classification advances no proof or release gate. The
  first-release structural action partition itself has all 27 named source
  seams and an empty open-action tuple, while formal operational
  correspondence and immutable-candidate evidence remain open.

