# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-0f064d665ea6399fa69c2990688c9409bd0e1bb561084be4675dd98931a8fa2e"></a>

<!-- Original context: Status / 2026-09-05 Atomic settlement historical authority candidate -->
- Additional private-note relation, AIR, wallet, and consensus-binding checks
  pass 38/38, with two full-proof gates ignored by that invocation. Stale
  network fixtures now use canonical genesis bytes, and authenticated mutations
  of every note-material byte must fail both recipient commitment and auditor
  opening validation. The initial three assertion failures remain retained;
  production validation is unchanged. Workspace formatting passed. Aggregate
  STARK checks pass 26/26, transparent STARK checks pass 20/20, X25519 checks
  pass 3/3, and prover-randomness checks pass 14/14. The proof-managed note
  selection completed with 15 passes and two failures. Current degree-capacity
  fixtures now account for the 975-degree mask and four quotient chunks.
  Invalid degrees still fail closed, with the existing 2..4 range checked before
  mask geometry so the error is correctly classified as an invalid profile.
  The focused selection passes 15/15 after rebuilding; the separate malformed
  trace/entropy negative also passes, with matching source/binary hashes before
  and after execution. Both optimized private-note full-proof
  gates remain running against the preceding driver source, explicitly bound
  to their original binary; they do not qualify the changed complete source.


<a id="record-9f43e63291b302f221c60cc59829bb9778cbafae2b56036079acf5ecb88d47bd"></a>

- Two PQ-MASP consensus fixtures now use canonical marked genesis hash bytes,
  matching the shared statement fixtures. A direct regression also requires
  `NetworkGenesisMismatch` for unmarked bytes. The optimized Core image builds
  successfully. Its PQ-MASP suite records 32 passes, two known-answer wire
  mismatches and one ignored proof gate. Both mismatches also occur with the
  preceding arithmetic; an inherited v0-to-v1 protocol-domain change omitted
  the corresponding known-answer profile commitments. The correction binds
  the current v1 wire and changes the governed PQ-MASP profile identity.
  Separate Orchard and runtime verifier tests reproduce genesis-binding
  failures; reviewed fixes bind the exact fixture network there and in all
  four retained native release-stage call sites. Post-transfer compilation
  and focused validation remain required.

