# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-4e680aa5853c68f06e75afa0c7d2f1c7f3fa71c5daa7795428408e6fab2ff996"></a>

<!-- Original context: Status / 2026-07-17 Sumeragi v2 successor activation and exact recovery refinement -->
## 2026-07-17 Sumeragi v2 successor activation and exact recovery refinement



<a id="record-cf573ae3f3133a03ef5b7862aa2468dde13bd96136ebc4f8c83282adc2829014"></a>

- Closed the TimeoutVote/recovery admission collision found during the
  successor audit. The roster-aware outer ingress now requires exactly
  `4 * roster_len + 1` count ownership: one ordinary, one non-timeout Progress,
  one distinct TimeoutVote corridor, and one shared TransportCompletion owner
  for `PayloadChunk` or `CertifiedBodyResponse` per validator, plus the
  untrusted lane.
  Certified-body and Commit-certificate requests are Progress traffic, while
  TimeoutVote retains its own signer-bounded byte and count owner. The
  Busy-deferred Progress lane independently reserves `3 * roster_len + 3`
  entries for locked Commit votes, exact current-view locked-reproposal
  Prepare votes, TimeoutVote messages, PrepareQC, CommitQC, and TC. The three
  vote partitions are now structurally signer-injective:
  exact duplicates coalesce, while a distinct same-owner item cannot displace
  admitted progress and retries after fair service. Current-view TimeoutVote
  keys also bypass a saturated ordinary semantic-history budget through one
  bounded slot per signer, so the deferred reservation is reachable at the
  production reducer boundary. Pinned adapter regressions realize the full
  `1024 + 4 * roster_len` live semantic bound and prove that view retirement,
  same-owner rollback, service, retry, and exact duplicate coalescing do not
  poison delivery state. The complete authoritative ingress module is green at
  30/30. The release inventory first reached 166 tests after adding two exact
  composite replay-FIFO regressions and two source-linked replay-refinement
  regressions. The two effective-lock acquisition regressions now raise the
  release inventory to 168 and the worker leg from 17 to 19. The two
  same-runtime-step Decision reconciliation regressions raise the current
  canonical inventory to 170 and the effects leg from 37 to 39. The exact
  local-timeout-versus-locked-Commit classifier regression raises the current
  canonical inventory to 171 without adding an owning module. Eleven Kura
  progress-witness durability regressions, two lane-geometry durability
  regressions, and the lane-work alternate-certificate retirement regression
  raised the inventory to 185 tests across 16 owning modules. Six exact
  lane-retirement recovery/substitution regressions and the lane-work plus
  runner platform-role gates raised the inventory to 193. The isolated-runner
  view-zero deadline/view-one observation contract raised it to 194. Six
  bounded effect-dispatch/preemption regressions, one source-faithful serialized
  runtime trace, and the canonical watchdog lane regression raised it to 202.
  Two exact post-decision timeout/TC quiescence regressions raise the current
  inventory to 204. The preceding Cargo discovery found all 166 then-required
  names among 6,748 tests
  with none missing or ignored; focused `locked_candidate` execution then ran
  all nine matching tests, including both new non-ignored cases, among 6,750
  library tests. The preceding mutable-source discovery enumerated 6,750
  library tests and found all 168 then-required names exactly once, with none
  missing or ignored; direct execution was green at 168/168. The preceding
  discovery on the merged mutable tree enumerated 6,756 library tests and found
  all 170 then-required names exactly once; four unrelated tests were ignored
  and none belonged to that release inventory. The added classifier is green
  in its exact 1/1 regression, the watchdog module at 21/21, and the complete
  status module at 29/29. Fresh full discovery and the 17-module serial corridor
  for all 204 names remain pending. The preceding 14 owning modules were green
  under the serial corridor, with exactly one passing row for every then-required
  name. The adapter module is also green under default parallel execution at
  59/59. The six effective-lock
  acquisition/reconstruction seam tests remain separately green at 6/6;
  focused refinement witnesses are green at 2/2, the reducer source-link module
  at 11/11, and the isolated reducer library at 96/96. The checkout remains
  mutable, and HEAD is unsigned, so this diagnostic does not replace the
  committed, detached, source-sealed release corridor, which remains pending.
  A fresh exact isolated-network rerun after the status-registry isolation
  change also passed the authoritative four-validator genesis case 1/1 in
  266.01 seconds. Networking was required, startup attempts were fixed at one,
  and the emitted seed matched
  `authoritative_v2_genesis_commits_on_every_validator`; the test observed
  genesis on every voter and the common awaiting-proposal successor height.
  This second mutable-tree result is likewise diagnostic rather than sealed
  release evidence.


<a id="record-f8967b7f3120682806e1ed69ba3fcbe0ff12e8b59b0a711582a85fbc0aad4d12"></a>

- Replaced the formal product's atomic application-to-join shortcut with an
  explicit per-node lifecycle. Physical `Idle`/`Queued`/`Running`/`Complete`
  state is separate from process-visible predecessor `Published`/`Absent`
  ownership. Applied and Recovered tokens bind the whole canonical successor
  context, and the model orders adapter, runtime, services, startup, clocks,
  marker, ingress, and publication. Fail-closed startup clears volatile state
  but retains durable authority. Recovered publication requires exact durable
  complete-tip evidence, never fabricates predecessor `Complete`, and only the
  two kind-specific publication actions may join the successor.


<a id="record-e2dc88ed66aef0f691ad83e9138b95f1487662b189d53fa483eaa25af6f424cb"></a>

- Replaced the standalone historical catch-up receipt/stage machine with one
  exact per-context Async recovery target. A current responsive voter serves an
  authenticated canonical CommitQC; Core imports that envelope into ordinary
  certificate delivery, and the real reducer performs decision persistence,
  body recovery, store, validation, and application. Nonterminal application
  queues successor startup without joining. Terminal `RecordKnownApplication`
  neither advances height nor creates successor work.


<a id="record-7d8eae43dea2faacf69e51aad961449a99a1a089af9e0019a99d8fd50f6974cd"></a>

- Added source-fidelity and semantic-mutation contracts for full-context token
  binding, startup ordering, fail-closed recovery, distinct Applied/Recovered
  publication, stage skipping, terminal exclusion, voter-only exact recovery,
  and
  fictitious terminal activation. The production-refinement seam and the new
  minimal `0..19` successor-activation starvation rank for each responsive
  validator, with a one-shot pre-failure `+9` offset, are explicitly
  `specified_unproved` and block genesis and height-liveness promotion. The
  formal fairness and all six release properties intentionally exclude an
  honest validator outside `Responsive`: such a validator may retain work
  queued before GST without contradicting the conditional liveness target.
  Safety state and nondeterministic activation steps remain defined for every
  validator.
  The 46-entry ledger now has 24 `tlaps_proved`, 15 `specified_unproved`, 6
  `trusted_contract`, and 1 `out_of_scope` entries, and retains
  `machine_checked_completion: false`; the proof-ledger and source-linked
  Verus evidence suites are green at 425/425, and the complete release
  proof-fidelity selection including TLC trace normalization was green at
  440/440 at that checkpoint. The current selection supersedes that historical
  count at 462/462. This includes
  fail-closed mutations for the effective-lock acquisition operators, terminal
  theorem, TLC configuration, and mutation-runner exit/marker contracts. Nine
  widening mutations demonstrate that the six activation
  properties, chain progress premise, weak-fairness clause, and join bridge all
  fail closed if `Responsive` is replaced by `ValidatorIds`. The checker also
  pins the exact four category-specific
  outer frames for all 18 weak-fairness actions, their raw quantified fairness
  inventory, the matching `AsyncFairActionAt` union, the typed
  `AsyncFairActionsRefineAsyncNext` source claim, and the exact 40-theorem
  dedicated proof module. A prior release-exact `--strict --nofp --threads 4`
  submodule run proved 1,143/1,143 obligations and promoted only
  `async-fair-action-refinement`. The checker rejects action deletion,
  quantifier drift, frame misclassification, claim or theorem weakening,
  unreviewed helper theorems, a local theorem in the executable model, and any
  TLC-only duplicate variable or fairness relation. Keeping the complete Core
  `Next` disjunction out of each `WF` target removes undefined-prime and
  conflicting-branch diagnostics while preserving one canonical public/finite
  spec. The restart contracts now additionally pin replay-scoped
  pre-GST I/O-worker fairness, an exact locked-Commit source guard before
  replay completion, the FIFO carrier invariant which establishes that guard,
  and the four-field recovery projection. Those separate liveness proof bodies
  are SANY-valid but remain backend-unverified until a fresh strict TLAPS run
  succeeds.


<a id="record-9da5018acdde803da2d1f4b4a64ebe7a3f91ad7bab5e48999d97475d2128a55f"></a>

- Narrowed the remaining application proof debt to the substantive
  per-responsive-validator completion pipeline. The ledger now pins
  `application-liveness` to the proofless
  `ApplicationCompletionProgressObligation`; the aggregate
  `ApplicationLivenessObligation` has a source proof which composes durable
  application monotonicity, the frozen responsive-voter set, and finite
  induction over validator prefixes. This removes an aggregate bookkeeping
  gap without assuming a global apply barrier. The per-validator recovery,
  validation, and application leadsto remains `specified_unproved`, no ledger
  status is promoted, and `machine_checked_completion` remains false until the
  complete pinned strict TLAPS run succeeds.


<a id="record-2e4c9f46310fd2f17b163ed7158969c2a720a9207465fa7c34825936ee33aa70"></a>

- Added an executable effective-lock acquisition owner and separated its
  deductive model proof from the ordinary Rust worker/runtime refinement debt.
  The owner keeps physical ID/subject immutable across same-lock rebinds,
  defers higher different-subject replacement until active local work
  terminates, rejects stale/future/wrong/duplicate completions, waits for the
  exact certified durable store, and retries before delivering to the latest
  consumer. Exhaustive TLC is green at 15,472 generated / 5,910 distinct states
  to depth 15. Negative models reproduce reload-per-view, missing retry, and
  future-completion admission failures. These bounded checks do not promote
  either obligation. Focused Rust coverage is green at 9/9, including new
  regressions for unissued future completion and rebind-while-waiting recovery.
  The repository-pinned Verus 0.2026.05.31.5dd6d83 toolchain previously
  verified 1,690 dependency conditions and 107 root conditions with zero
  errors under `--no-cheating`. The evidence parser's stale 105-root contract
  was raised to the observed 107 and gained an explicit negative transcript
  test rejecting the obsolete count. The focused evidence suite was green at
  21/21, and the combined proof-ledger/evidence regression suite was green at
  425/425. A 2026-07-20 audit repaired the new causal-filter induction bridge,
  split the largest EnterView query without increasing the pinned resource
  limit, and now verifies 1,690 dependency plus 126 root conditions. The
  evidence contract is pinned to 126 and its focused suite is green at 24/24.
  The effective-lock production-refinement entry now has four exact shared
  Rust/Verus trace kernels, with four claim projections enforced at five live
  production call sites. Fail-closed mutations cover vacuous ensures,
  disconnected proofs, altered projections, constant kernels, missing calls,
  and tautological TLA bridges.
  Its focused strict TLA bridge slice is green at 2/2. The ledger entry remains
  `specified_unproved`: these direct runs are diagnostic while production and
  TLA sources are changing, and the source-manifest-bound evidence wrapper
  still must rerun on stable sources before promotion.


<a id="record-a8de04ad8a809fbb6adc92653db112d64629903510d30b1215b5f1741ec881aa"></a>

- Bound the successor/exact-recovery abstraction to the production runner, status
  registry, adapter, runtime clock fence, certified-body effect pipeline, and
  historical block-sync source order. Four mutations remove live-ingress-first
  publication, fabricate recovered predecessor completion, skip certified
  signer validation, or unpin exact recovery; all fail closed. The full
  block-sync module is green at 11/11. This source mapping does not by itself
  discharge the still-unproved deductive refinement obligation.


<a id="record-550ff28c5a28781dd42006c74eada60686a25aa24430f589f2173b7c7d181450"></a>

- Repaired the bounded ChainEpoch harness so inherited Core variables are
  initialized by `Init`, included in the subscript, type-checked, and frozen
  while receipt state advances. A deterministic run checked 2,343,864 states
  with no invariant error. The wrapper correctly failed closed on TLC's
  multi-minute footer shape; its parser now accepts an exact multi-unit duration,
  requires at least one progress marker, and still requires one run, one
  initial-state marker, zero TLC errors, and status zero. A source-final wrapper
  rerun remains pending. The TLC-only receipt relation no longer changes the
  deductive transition; its refinement back to `ChainEpochNext` passed a focused
  strict TLAPS slice at 5/5 obligations, and a post-separation one-trace TLC
  smoke checked 23,556 states without an invariant error.


<a id="record-43d53152385b1f01599b83b1f0a81b291f21d9638f56f25ac196c45ed9ecd40e"></a>

- Ran the exact authoritative four-validator genesis regression
  `sumeragi_v2_runner::authoritative_v2_genesis_commits_on_every_validator`
  against the current working tree with networking required, network-start
  retries disabled,
  and its exact deterministic seed. The single permitted startup attempt passed
  1/1. Live diagnostics crossed the original reset boundary through view 10
  while retaining the exact view-9 locked Commit intent; the test then observed
  the genesis block applied on every validator and one common awaiting-proposal
  successor height. All four peer processes exited with status 0. Libtest
  completed in 1192.57 seconds, including cold network binary preparation. The
  temporary retained log is
  `/tmp/iroha-root-genesis.1T17yY/run.log`, and the localnet is
  `/tmp/iroha-root-genesis.1T17yY/irohad_test_network_FbEl6A`. This is mutable
  current-working-tree diagnostic evidence, not a clean signed or
  checkout-manifest-bound source-attested release receipt. It does not close the
  four-seed PR corridor, 32-seed release matrix, strict proof debt, clean
  source-attested chaos rerun, or 24-hour Taira-profile soak.


<a id="record-48e58bb36de9c47bcb187f7eb652f3a931ae39998ad5e22144cb9effa4143b18"></a>

- Reordered snapshot-free startup so the configured lane-manifest source set is
  frozen and installed before atomic Kura replay, then rebound from that same
  immutable snapshot after replay instead of rescanning mutable files. Replay
  rejection now reports the first failed transaction. Lane-certificate recovery
  also treats valid signer subsets for one exact proposal as proof variants:
  the first durable certificate bytes remain authoritative, an interrupted
  application receipt is repaired from that retained certificate, and a
  globally applied body without a certificate remains recoverable. Different
  bodies at the occupied lane height still fail closed. The three focused lane
  recovery regressions passed 3/3, and the strict replay regression is green.


<a id="record-83d9f39bcf083c41f09119203d77e4a2d641dc8386d1448c938f986ff2e44681"></a>

- The exact four-validator restart scenario
  `authoritative_v2_finalizes_through_validator_restart` passed 1/1 in 165.05
  seconds; the restarted validator replayed heights 1 through 4 and rejoined
  without a certificate conflict, ownership mismatch, or invalid decided body.
  Its retained network is
  `/tmp/iroha-restart-retained-final-20260718/irohad_test_network_rBpU3P`.
  The exact timeout-rotation scenario
  `taira_npos_leader_timeout_commits_within_rotation_bound` also passed 1/1 in
  90.00 seconds, with retained network
  `/tmp/iroha-taira-timeout-retained-20260718/irohad_test_network_VU2gxW`.
  The historical distinct-subject PrepareQC scenario did not pass: after
  1105.30 seconds its one
  permitted attempt produced the intended 2/2 split across QC views 1 and 0,
  but both groups certified the same block subject, so the distinct-subject
  precondition timed out. The retained diagnostic network is
  `/tmp/iroha-divergent-prepareqc-retained-20260718/irohad_test_network_M4CsaG`;
  no retry has been counted as success. The causal audit found that the old
  harness delivered the view-zero QC broadly enough to lock all validators, so
  the safe-value rule correctly forced the same subject in view one. Its
  replacement now isolates A at the actual first leader, advances the other
  three using no-high-QC timeout evidence, forms B at two receivers, and then
  exposes stale A at a fourth receiver for the exact 2+2 highest-QC split.
  Formatting and script syntax checks pass; compilation and real-network
  execution remain pending.


