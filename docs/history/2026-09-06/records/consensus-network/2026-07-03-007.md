# Historical project evidence

This page is historical evidence, not current release readiness.
See [the archive index](../../index.md) for provenance and reconstruction.


<a id="record-b6d051765e5f90a3082875f456e6e65607a272dc6970e2fc515d655796de3f57"></a>

<!-- Original context: Status / 2026-07-03 Sumeragi consensus core model-check runner repair -->
- Refreshed decomposed reconfiguration, recovery, election, timing, ingress,
  Kura, frontier, and proposal-edge evidence. `reconfig-fast` and
  `recovery-fast` passed Apalache with `NoError` to length 7,
  `view-change-fast` passed to length 6, and every other selected mode passed
  to length 1. The matching TLC runs completed with no errors:
  - `reconfig-fast`: `12` generated states, `11` distinct states, depth `10`.
  - `recovery-fast`: `17` generated states, `13` distinct states, depth `5`.
  - `view-change-fast`: `3,665` generated states, `264` distinct states, depth
    `5`.
  - `npos-vrf-fast`: `994` generated states, `32` distinct states, depth `2`.
  - `validator-election-fast`: `2` generated states, `1` distinct state, depth
    `1`.
  - `stake-snapshot-fast`: `2` generated states, `1` distinct state, depth `1`.
  - `timing-monitor-fast`: `2` generated states, `1` distinct state, depth `1`.
  - `counter-backpressure-cooldown-fast`: `2` generated states, `1` distinct
    state, depth `1`.
  - `round-liveness-fast`: `2` generated states, `1` distinct state, depth `1`.
  - `fast-finality-inline-validation-fast`: `22` generated states, `11`
    distinct states, depth `1`.
  - `observer-signature-recovery-fast`: `2` generated states, `1` distinct
    state, depth `1`.
  - `ingress-dedup-cache-fast`: `2` generated states, `1` distinct state, depth
    `1`.
  - `kura-store-status-fast`: `25` generated states, `25` distinct states,
    depth `25`.
  - `proposal-parent-resolution-fast`: `35` generated states, `18` distinct
    states, depth `2`.
  - `proposal-stale-vote-fast`: `2` generated states, `1` distinct state, depth
    `1`.
  - `proposal-sccp-root-fast`: `2` generated states, `1` distinct state, depth
    `1`.
  - `frontier-gap-realign-fast`: `962` generated states, `32` distinct states,
    depth `2`.


<a id="record-f9a841715ecbff8aa678a44133952779c82e343b4fcc08f87e885074210c0cd1"></a>

- Added central Sumeragi direct-commit proof corridors to the top-level model:
  `TlcDirectCommitNoFaultCorridor`, `DirectCommitNext`,
  `DirectDeliveredFirstCommitNext`, and matching fairness/spec helpers. These do
  not change the full `Next`/`Spec`; they provide narrower proof surfaces for
  tuning the resource-bound central bundle. Apalache typechecking for
  `Sumeragi.tla` still passes, and the formal coverage guard remains
  consistent. Split probes now characterize the remaining central bottleneck:
  the aggregate `SumeragiConsensusCoreStateMatchesEnvelope`, all `93`
  top-level state invariants without temporal properties, the
  `consensus-finality` state-invariant group, and a single direct-commit
  `EventuallyCommit` property all still exhausted a `16 GiB` Apalache heap at
  length `10`; top-level TLC probes over the full, direct-commit, and
  delivered-first action relations remained silent in local smoke windows and
  were interrupted. Treat these as tuning evidence, not proof completions.


<a id="record-d8ee810f8efdeff71e3745ef65b54b6f3478827834353453a7554e9d0f505dc3"></a>

- Added `SumeragiDirectDeliveredFirstCorridorGate.tla` and the
  `direct-delivered-first-corridor-fast` runner mode as a finite abstract gate
  for the no-fault delivered-first path: proposal, RBC chunks, READY delivery,
  prepare quorum, and commit-vote finality. The mode is wired through the
  Apalache/TLC runners, PR formal check, and README length table. Validation
  passed with Apalache `NoError` to length `1`, and TLC completed the graph
  with `29` generated states, `15` distinct states, and depth `2`.


<a id="record-5205f1c5bfea8b92bc479c1be6a7b0f058ced0f88759a9a8eb4697ec30a8c8c5"></a>

- Added expected-failure coverage for the delivered-first gate's `12` seeded
  artifact mutations: committed-phase, delivered-RBC state, chunk count, READY
  quorum count, prepare quorum count, commit-vote count, stake, finality latch,
  commit-evidence vote/stake counts, header seeding, and digest seeding. The
  `direct-delivered-first-corridor-bug-*` modes are wired through Apalache,
  TLC, expected-failure CI, and the formal README. Validation passed with all
  `12` Apalache modes rejected as invariant failures (`exit 12`) and all `12`
  TLC modes reporting the expected invariant violation.


<a id="record-003ac6d39b87d511891eb798602965f003e7acc7a972fce00e017ecbff4e8e29"></a>

- Added the TLC-only `direct-delivered-first-corridor-progress` temporal mode
  over an ordered delivered-first path inside the finite corridor model. It
  now proves eventual installation of the delivered-first finality stack over
  the complete `15`-state path graph under weak fairness for the path-advance
  action. The paired `direct-delivered-first-corridor-progress-bug-*` modes
  confirm that missing any finality-stack component is rejected by the
  progress-mode safety/progress surface. The clean and mutation progress CFGs
  now also check
  `DirectDeliveredFirstProgressSafetyEnvelope`, tying the temporal claim to
  `TypeInvariant` and `DirectDeliveredFirstCorridorExactness`; the formal
  coverage guard pins both the aggregate definition and every ordered
  delivered-first progress CFG check.


<a id="record-3cd89fe49c91e34866b213ad122453748c0a02caf25e243ebd8d58ae5f50f612"></a>

- Added `SumeragiDirectVoteFirstCorridorGate.tla` and the
  `direct-vote-first-corridor-fast` runner mode for the complementary no-fault
  path where prepare and commit votes reach quorum before RBC delivery. The
  gate proves that buffered live vote/stake quorum cannot publish finality or
  commit-certificate artifacts before delivery, and that later RBC delivery
  installs finality from the buffered quorum plus delivered payload evidence.
  Validation passed with Apalache `NoError` to length `1`, and TLC completed
  the graph with `29` generated states, `15` distinct states, and depth `2`.


<a id="record-c9227326c05f2b7743d6f2942cf8b02ba6de1cfafc034e3f85aa4c4868006721"></a>

- Added expected-failure coverage for the vote-first gate's `14` seeded
  mutations: premature committed phase/finality, missing delivered state,
  missing chunks or READY quorum on delivery, under-counted prepare/commit
  votes or stake, missing finality latch, premature certificate evidence,
  missing final certificate vote/stake evidence, and missing header/digest
  seeding. The `direct-vote-first-corridor-bug-*` modes are wired through
  Apalache, TLC, expected-failure CI, and the formal README. Validation passed
  with all `14` Apalache modes rejected as invariant failures (`exit 12`) and
  all `14` TLC modes reporting the expected invariant violation.


<a id="record-bd6d4e0e23f78a81378abd867f0284539234bfae953dfe3863bf0fbc73a12002"></a>

- Added the TLC-only `direct-vote-first-corridor-progress` temporal mode over an
  ordered vote-first path inside the finite corridor model. It now proves
  eventual installation of the vote-first finality stack over the complete
  `15`-state path graph under weak fairness for the path-advance action. The
  paired `direct-vote-first-corridor-progress-bug-*` modes confirm that missing
  any finality-stack component is rejected by the progress-mode
  safety/progress surface.
  The clean and mutation progress CFGs now also check
  `DirectVoteFirstProgressSafetyEnvelope`, tying the temporal claim to
  `TypeInvariant` and `DirectVoteFirstCorridorExactness`; the formal coverage
  guard pins both the aggregate definition and every ordered vote-first
  progress CFG check. The ordered path progress modes are documented as
  TLC-only because Apalache currently ignores fairness clauses.


<a id="record-2e2f21ed106c295af3da1f0db84ed15c5b16bef5cb438a302675cc0dcd3328c3"></a>

- Added `SumeragiDirectCommitInterleavingGate.tla` and the
  `direct-commit-interleaving-fast` runner mode as a finite no-fault
  transition-system gate that allows prepare votes, commit votes, RBC chunks,
  READY votes, and delivery to interleave freely. It subsumes the
  delivered-first and vote-first orderings in one graph and proves RBC evidence
  shape, vote/stake handoff, pre-delivery buffered-quorum certificate absence,
  and finality installation by either the last delivered commit vote or later
  RBC delivery. Validation passed with Apalache `NoError` to length `14`, and
  TLC completed the graph with `87` generated states, `50` distinct states, and
  depth `14`.


<a id="record-5d7c2275ce1d7ab506bc7bc9c5c662476d2a7aba79bc11dd20a79dd09206ba0e"></a>

- Added expected-failure coverage for the interleaving gate's `15` seeded
  mutations: skipped RBC proposal seeding, missing header/digest evidence,
  under-counted prepare/commit quorum, missing stake, missing second chunk,
  under-counted READY quorum, skipped delivery state, premature finality or
  commit-certificate evidence before delivery, committed-phase/finality latch
  mismatches, and missing final certificate vote/stake evidence. The
  `direct-commit-interleaving-bug-*` modes are wired through Apalache, TLC,
  expected-failure CI, and the formal README. Validation passed with all `15`
  Apalache modes rejected as invariant failures (`exit 12`) and all `15` TLC
  modes reporting the expected invariant violation.


<a id="record-1e6e0875c36c49b57f439c0208c799b168df93bc9498da07d7de0801b15c0f95"></a>

- Added `SumeragiByzantineCommitInterleavingGate.tla` and the
  `byzantine-commit-interleaving-fast` runner mode as a finite
  Byzantine-tolerant transition-system gate with `F = 1`. The gate allows
  honest commit votes, one Byzantine commit vote, RBC chunks, READY votes, and
  delivery to interleave freely while proving weighted stake accounting, the
  `CommitQuorum - F` honest-support threshold, Byzantine vote budget, absence
  of pre-delivery certificate artifacts, and exact final certificate evidence.
  The formal coverage guard now pins the direct delivered-first, vote-first,
  and interleaving exactness aggregate bodies, plus their correctness and
  progress-safety envelopes, requires every source safety mutation CFG to keep
  the same `TypeInvariant`, model exactness, and correctness-envelope surface
  as its clean fast CFG, and requires
  `ByzantineCommitInterleavingExactness` to extend the direct interleaving core
  with `ProposedRoundInitializesRbc`. Validation passed with Apalache `NoError`
  to length `14`, and TLC completed the graph with `160` generated states, `78`
  distinct states, and depth `15`.


<a id="record-7c227e0e007ad5ceddbe5ef7eb0086ead12307532be8543cd9e8c40e8754e046"></a>

- Added expected-failure coverage for the Byzantine interleaving gate's `17`
  seeded mutations: skipped RBC proposal seeding, missing header/digest
  evidence, under-counted prepare quorum, missing honest stake, over-counted
  Byzantine stake, over-budget Byzantine votes, missing second chunk,
  under-counted READY quorum, skipped delivery state, premature finality,
  finality without honest support, premature commit-certificate evidence,
  committed-phase/finality latch mismatches, and missing final certificate
  vote/stake evidence. The `byzantine-commit-interleaving-bug-*` modes are
  wired through Apalache, TLC, expected-failure CI, and the formal README.
  Validation passed with all `17` Apalache modes rejected as invariant failures
  and all `17` TLC modes reporting the expected invariant violation.


<a id="record-6ba34f2001806fe55a5fe05d1bf2104c57ed6eb9a8e5e9664c7c4bdce365c35a"></a>

- Added `ByzantineCommitProgressSafetyEnvelope` to the source Byzantine
  interleaving model and required it in the clean
  `byzantine-commit-interleaving-progress` CFG plus every source
  `byzantine-commit-interleaving-progress-bug-*` CFG. This ties the TLC-only
  eventual-finality proof to `TypeInvariant` and
  `ByzantineCommitInterleavingExactness` across the fair source graph, before
  projection. The formal coverage guard now pins the aggregate's direct
  conjuncts and the clean/mutation progress CFG check surface. It now also
  independently pins every source progress safety envelope to `TypeInvariant`
  plus its model-specific exactness predicate, so the temporal proofs cannot
  drift from their safety baselines by changing the contract map and TLA body
  together. It also requires the Byzantine interleaving exactness aggregate to
  retain the direct no-fault RBC/vote/certificate core plus the
  Byzantine-only proposal/RBC initialization obligation. Validation passed with
  TLC on the clean progress mode (`160` generated, `78` distinct, depth `15`)
  and all `11` source Byzantine progress mutations rejected under the
  safety/progress surface.


<a id="record-0d9d8b2a2a9016eab5c4f9b5c853d4c87545bce9385825f73253de00c14a4e87"></a>

- Added a top-level delivered-first Byzantine direct-commit corridor to
  `Sumeragi.tla`: `ByzantineDeliveredFirstCommitNext`,
  `ByzantineDeliveredFirstCommitSpec`,
  `ByzantineDeliveredFirstTopExactness`, and
  `ByzantineDeliveredFirstTopCorrectnessEnvelope`. The new
  `byzantine-delivered-first-top-fast` mode is wired through Apalache PR CI as
  an explicit typecheck-only top-level bridge from the standalone
  Byzantine/interleaving gates back to the central model. Validation passed
  with Apalache typechecking the updated `Sumeragi.tla` in `14` seconds, and
  the formal coverage guard now treats this mode as Apalache-only. Local TLC
  probes over the top-level delivered-first corridor did not reach
  initial-state completion in bounded smoke windows, so TLC-backed state-space
  evidence remains in the standalone `byzantine-commit-interleaving-fast` gate.


<a id="record-0d4d85b05189ae40879369c4f2a24fda4f66a4fd21a1c10cb662f6a56d6e0c70"></a>

- Added the complementary top-level vote-first Byzantine direct-commit corridor
  to `Sumeragi.tla`: `DirectVoteFirstCommitNext`,
  `ByzantineVoteFirstCommitNext`, `ByzantineVoteFirstCommitSpec`,
  `ByzantineVoteFirstTopExactness`, and
  `ByzantineVoteFirstTopCorrectnessEnvelope`. The
  `byzantine-vote-first-top-fast` mode is wired through Apalache PR CI and
  the formal coverage guard as an explicit typecheck-only bridge for the path
  where prepare/commit votes buffer before delivered RBC evidence installs
  finality. Validation passed with Apalache typechecking the updated
  `Sumeragi.tla`, and the formal coverage guard now reports `512` PR modes.


<a id="record-b7f5d0e97275a96f43158da48a297c13c35c38d78e901798320d97e52ead1b86"></a>

- Added the combined top-level Byzantine direct-commit corridor to
  `Sumeragi.tla`: `ByzantineDirectCommitNext`,
  `ByzantineDirectCommitSpec`, `ByzantineDirectTopExactness`, and
  `ByzantineDirectTopCorrectnessEnvelope`, plus
  `ByzantineDirectTopCoversOrderedTopCorridors` to keep the combined direct
  exactness envelope tied to the delivered-first and vote-first top envelopes.
  The `byzantine-direct-top-fast` mode is wired through Apalache PR CI as the
  central action-surface bridge for mixed RBC/commit-vote interleavings covered
  by the standalone `byzantine-commit-interleaving-fast` state-space gate.
  Validation passed with Apalache typechecking the updated `Sumeragi.tla`, and
  the formal coverage guard now reports `513` PR modes. The formal coverage
  guard now also pins the three top-level Byzantine CFG check surfaces and the
  direct conjunct/implication contracts for the delivered-first, vote-first,
  and combined top-level aggregate bridge operators.


<a id="record-04bae1a7e197abd229791e90ccd41e1c953aa27f82fa8b5c7fbb29f994e2f8af"></a>

- Added `SumeragiByzantineCommitProjectionGate.tla` and the
  `byzantine-commit-projection-fast` runner mode as a checked bridge between
  the finite Byzantine interleaving state space and the top-level
  `ByzantineDeliveredFirstTopExactness`, `ByzantineVoteFirstTopExactness`, and
  `ByzantineDirectTopExactness` evidence obligations. The projection fixes
  `view`, `commitView`, `newViewVotes`, and `viewEvidenceVotes` to the direct
  commit corridor values while checking the finality stack, Byzantine
  commit-vote gate, RBC-deliver gate, vote/stake accounting, and
  delivered-without-finality wait predicates. The bridge now also carries the
  full source interleaving exactness aggregate, tying projected top obligations
  back to RBC evidence shape, proposal/RBC initialization, vote handoff,
  commit-certificate shape, and buffered-vote delivery semantics. Validation
  passed with Apalache `NoError` to length `14`; TLC completed the graph with
  `160` generated states, `78` distinct states, and depth `15`; the `17`
  `byzantine-commit-projection-bug-*` mutations now match the source
  Byzantine interleaving mutation set, covering finality/accounting faults plus
  proposal/RBC seeding, prepare-quorum, honest-stake, chunk/READY, and delivery
  state faults under the same full bridge before expected rejection. All `8`
  newly added projection safety mutations were rejected by Apalache and TLC.
  The formal coverage guard now pins the `TypeInvariant`, projected direct-top
  exactness/correctness, ordered-top bridge, and interleaving bridge invariants
  in every projection mutation CFG,
  and pins the projection gate aggregate conjunct contracts, including bridge
  implication antecedents. It also compares the
  source/projection Byzantine mutation suffix families: projection safety must
  match source Byzantine interleaving safety, projection progress must match
  source Byzantine interleaving progress, and progress modes may omit only the
  documented safety-only Byzantine faults. The guard also pins the direct
  safety/progress mutation suffix families: delivered-first progress must match
  delivered-first safety, vote-first progress may omit only the documented
  pre-delivery safety faults, and direct interleaving progress may omit only
  the documented direct safety-only quorum/stake/pre-delivery faults. The guard
  also pins the clean projection fast/progress CFG check surfaces and now
  reports `514` PR modes, `9966` expected-failure modes, `10481` documented
  modes, `509` TLC fast modes, and `10021` TLC mutation modes. Focused guard
  pytest passed for the
  Byzantine mutation-family alignment, projection bridge CFG, and projection
  progress CFG checks; Python compilation and the full Sumeragi formal coverage
  guard also passed. The guard now pins the internal top-corridor family split:
  `ByzantineDeliveredFirstTopExactness` remains the common direct-commit core,
  while `ByzantineVoteFirstTopExactness` and `ByzantineDirectTopExactness`
  remain that common core plus the delivered-without-finality wait obligations.
  The guard now additionally requires that the
  top/projection Byzantine direct-commit contracts stay aligned, so projected
  delivered-first, vote-first, combined top-corridor, and ordered-bridge
  implication obligations cannot drift from their central `Sumeragi.tla`
  counterparts. The guard now also requires
  `ProjectionBridgeMatchesInterleavingCore` to mirror
  `ByzantineCommitInterleavingExactness`, keeping the projected bridge core
  tied to the source Byzantine interleaving proof surface. The guard also
  requires that the projection bridge interleaving exactness composes projected
  direct-top and source core obligations, deriving the full bridge surface from
  `ProjectedByzantineDirectTopExactness` plus
  `ProjectionBridgeMatchesInterleavingCore` instead of trusting a separate
  manual list.


<a id="record-d6bb3670cd7113368ec73ffb0d973b0f7a5f3962b380af76e6befb2f515c518f"></a>

- Added the TLC-only `byzantine-commit-projection-progress` temporal mode over
  the same projection bridge. It now proves eventual installation of the
  projected finality stack over the complete `78`-state Byzantine direct-commit
  projection graph under weak fairness for proposal, prepare, honest/Byzantine
  commit-vote, RBC chunk, RBC READY, and RBC deliver actions. The clean progress
  config also checks `ProjectedCommitProgressSafetyEnvelope`, tying temporal
  eventual finality to the ordered top-corridor bridge and full
  source/projection exactness envelope. The paired
  `byzantine-commit-projection-progress-bug-finality-not-latched`,
  `byzantine-commit-projection-progress-bug-phase-not-committed`,
  `byzantine-commit-projection-progress-bug-commit-evidence-votes-missing`,
  `byzantine-commit-projection-progress-bug-commit-evidence-stake-missing`,
  and `byzantine-commit-projection-progress-bug-commit-without-honest-support`
  modes reject missing projected finality-stack commit components, including
  honest support, under the safety/progress surface. The
  additional `byzantine-commit-projection-progress-bug-propose-skips-rbc`,
  `byzantine-commit-projection-progress-bug-header-not-seeded`,
  `byzantine-commit-projection-progress-bug-digest-not-seeded`,
  `byzantine-commit-projection-progress-bug-drop-second-chunk`,
  `byzantine-commit-projection-progress-bug-ready-quorum-under-counted`, and
  `byzantine-commit-projection-progress-bug-skip-deliver-state` modes reject
  missing RBC initialization, header/digest seed, chunk completion, READY
  quorum, or delivered-state latch under that same surface. Every projection
  progress mutation CFG now also carries `TypeInvariant` and
  `ProjectedCommitProgressSafetyEnvelope`, and the formal coverage guard pins
  that type/safety/property surface. The guard also pins
  `ProjectedCommitProgressSafetyEnvelope` itself to
  `ProjectionBridgeCoversOrderedTopCorridors` plus
  `ProjectionBridgeMatchesInterleavingExactnessCorrectnessEnvelope`, so the
  temporal progress run cannot drift away from the bridge composition it
  depends on. The guard also pins that the projection progress spec composes
  the named fairness aggregate, keeps direct `[][Next]_vars` transition
  closure, and uses exactly the proposal, prepare, honest/Byzantine commit
  vote, RBC chunk, RBC READY, and RBC deliver fairness actions.
  The guard also requires that source progress specs compose their named
  fairness aggregates and keep their family-specific transition closure and
  fair action sets, so the source temporal proof specs cannot silently fall
  back to stale raw `WF_vars(...)` lists.
  The guard also requires that top-level Sumeragi specs compose their named
  fairness aggregates and keep the documented top-level commit, direct-commit,
  delivered-first, and vote-first transition closures and fair action sets.
  This progress check is documented as TLC-only because Apalache
  currently ignores the fairness clauses; the coverage guard now counts `10021`
  TLC mutation modes.


<a id="record-efd8d00a4620e7fecb847302ea306147ad984934b5d17cb72159aa889333997f"></a>

- Added the TLC-only `byzantine-commit-interleaving-progress` temporal mode over
  the source Byzantine direct-commit interleaving graph. It now proves eventual
  installation of the Byzantine finality stack over the complete `78`-state
  graph under weak fairness for proposal, prepare, honest/Byzantine
  commit-vote, RBC chunk, RBC READY, and RBC deliver actions: committed phase,
  delivered RBC state, quorum vote/stake counters, `CommitQuorum - F` honest
  support, and commit-certificate evidence matching the vote counters. The
  paired `byzantine-commit-interleaving-progress-bug-finality-not-latched`,
  `byzantine-commit-interleaving-progress-bug-phase-not-committed`,
  `byzantine-commit-interleaving-progress-bug-commit-evidence-votes-missing`,
  `byzantine-commit-interleaving-progress-bug-commit-evidence-stake-missing`,
  and `byzantine-commit-interleaving-progress-bug-commit-without-honest-support`
  modes confirm that missing any Byzantine finality-stack commit component is
  rejected as a temporal eventual-commit violation before the projection layer.
  The additional
  `byzantine-commit-interleaving-progress-bug-propose-skips-rbc`,
  `byzantine-commit-interleaving-progress-bug-header-not-seeded`,
  `byzantine-commit-interleaving-progress-bug-digest-not-seeded`,
  `byzantine-commit-interleaving-progress-bug-drop-second-chunk`,
  `byzantine-commit-interleaving-progress-bug-ready-quorum-under-counted`, and
  `byzantine-commit-interleaving-progress-bug-skip-deliver-state` modes reject
  missing source RBC initialization, header/digest seed, chunk completion,
  READY quorum, or delivered-state latch before projection. This progress check
  is documented as TLC-only because Apalache currently ignores fairness clauses;
  the coverage guard now counts `10013` TLC mutation modes.


<a id="record-e60b8156f5eec5c344a8f56dd49d32eb1a0729f0140a631a92ef942621d897cd"></a>

- Added the TLC-only `direct-commit-interleaving-progress` temporal mode over
  the no-fault direct-commit interleaving graph. It now proves eventual
  installation of the full finality stack over the complete `50`-state graph
  under weak fairness for proposal, prepare, commit-vote, RBC chunk, RBC READY,
  and RBC deliver actions: committed phase, delivered RBC state, READY quorum,
  complete chunks, header/digest seed, quorum vote/stake counters, and
  commit-certificate vote/stake evidence. The clean progress CFG now also
  checks `DirectCommitProgressSafetyEnvelope`, tying temporal eventual finality
  to `TypeInvariant` and `DirectCommitInterleavingExactness` before the
  Byzantine extension. The formal coverage guard now pins the aggregate's
  direct conjuncts and the clean/mutation progress CFG check surface, and now
  independently pins every source progress safety envelope to `TypeInvariant`
  plus its model-specific exactness predicate. TLC validation passed on the
  clean progress mode (`87` generated, `50` distinct, depth `14`) and all `10`
  direct progress mutations rejected under the safety/progress surface. The
  paired
  `direct-commit-interleaving-progress-bug-finality-not-latched`,
  `direct-commit-interleaving-progress-bug-phase-not-committed`,
  `direct-commit-interleaving-progress-bug-commit-evidence-votes-missing`, and
  `direct-commit-interleaving-progress-bug-commit-evidence-stake-missing` modes
  confirm that missing any finality-stack commit component is rejected as a
  temporal eventual-commit violation before the Byzantine extension. The
  additional `direct-commit-interleaving-progress-bug-propose-skips-rbc`,
  `direct-commit-interleaving-progress-bug-header-not-seeded`,
  `direct-commit-interleaving-progress-bug-digest-not-seeded`,
  `direct-commit-interleaving-progress-bug-drop-second-chunk`,
  `direct-commit-interleaving-progress-bug-ready-quorum-under-counted`, and
  `direct-commit-interleaving-progress-bug-skip-deliver-state` modes reject
  missing direct RBC initialization, header/digest seed, chunk completion,
  READY quorum, or delivered-state latch before the Byzantine extension. This
  progress check is documented as TLC-only because Apalache currently ignores
  fairness clauses; the coverage guard now counts `10021` TLC mutation modes.


<a id="record-6c820b648e306da7849a9db400567078f11272fa7c3aca0c2fd13641f6def5b1"></a>

- Follow-up: split or otherwise tune the central Sumeragi correctness bundle so
  the direct `deep` Apalache run and top-level TLC fast cross-check can complete
  again under documented resource bounds.


