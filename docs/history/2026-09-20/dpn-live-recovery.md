# DPN and Taira recovery, September 20

## Current observations and boundaries

Work remains in the canonical `iroha` checkout on `optimizations`; another operator advanced HEAD to `a07a9cf3f8cc391d183b2c76f19a1babeddf9887`. An external cleanup removed the entire prior `target` tree, including historical logs, compressed executables and both warm lanes. Historical pass counts below remain historical prose, not retained executable evidence. The same fixed Linux lane was recreated; no new random lane or Cargo cleanup was used by this task.

The saved source42 deployment record and its public host-key pins validate. Its approved MacStadium Mac route works. The host initially had only113MiB available; nginx recorded ENOSPC and the Linux guest closed SSH before its protocol banner. PostgreSQL accounted for about240GiB, primarily indexes belonging to another hosted application; no database data or indexes were changed.

The maintained nginx guard rejected generation4 under a public four-archive policy. Actual macOS `newsyslog` retains positive native generations inclusively, so public count4 now emits native count3. Public counts2–10 are supported; the strict four-archive guard remains. Real private offline rotations exercise saturation. Existing excess archives were preserved outside the active namespace. Successful live rotation retained the same nginx master and closed its old descriptors. Thirty tests and12subtests pass, including ENOSPC health-publication failures. The deployed script SHA256 is `7230bc67c76df5f542d167c444277afbfdcd6ee0f5d68eee4cd5d77e6179a647`; it records bounded private last-attempt/last-success metadata, verifies rotation during installation and exposes read-only health with separate launchd exit state.

Lossless gzip of33 closed historical Taira logs recovered1,243,430,905bytes. Routine log retention and an off-host verified copy of the unused Tart installer brought backing availability to2,157,178,880bytes before restart. No ledger files were deleted or replaced. The existing VM stopped successfully in0.335seconds and its installed supervisor restarted it with identical disk inode/size and configuration hash. SSH recovered and the guest reported13GiB free. All four real `iroha3d-taira-validator-*` units start from the existing source42 binary.

Validator1 serves faucet policy200 on its recorded local port8080. Validators2–4 authenticate3,598 Kura finality artifacts and the matching snapshot, then stop because the active threshold-beacon key session is absent. Primary public status and funding policy still return502 through the existing8081 upstream. No public healthy-network or funding-write claim is made. No ledger replacement or empty production block was performed.

The focused runner now executes only its selected pending-Kura controls immediately after configuration, stops later work on their failure, and never duplicates them. Independent review and197tests/1,697subtests pass. Linux75 passes224 FSM controls, six lifecycle source controls, test metadata in299.5seconds and native Core/config codegen in458.2seconds. It then refuses a group-writable Cargo profile lock created under the guest's ambient umask0002; the six native tests have not yet executed. Owner-private child creation and exact existing-artifact repair are being validated without relaxing custody checks.

Fresh operational evidence is under `target/dpn-devex/`, including `macstadium-same-disk-restart-20260920.json`, `guest-validator-recovery-details-20260920.json`, `nginx-logrotate-deploy-20260920.json` and `linux75-pending-kura.log`. These are mutable development/incident observations, not immutable shipping qualification. Full Linux selection, shipping binaries, four-validator recovery and the paid clean-client DPN workflow remain open.


## Completed bounded follow-up

Cargo and native test subprocesses now explicitly use owner-private creation permissions. Existing custody rejection remains unchanged. The existing Linux lane required only seven exact metadata corrections under its lane/profile/cache locks; content digests were preserved. Linux76 passes all six mandatory pending-Kura selectors in17.40seconds. Linux77 records190passes and23 temporary-directory custody failures across213unique remaining selectors in185.23seconds. After fixing the same umask cause at the native test child boundary, Linux78 passes those23selectors in33.54seconds. These runs cover all219current basic Core selectors successfully with one unchanged Core executable SHA256 `699d36fab1c8e3119beefaf8227fbaa6f4030f0227c1d7cb5faebcff03f55dab`. Both Core74 catalog fixtures and the deferred-beacon/no-empty-proposal/readiness controls pass. Per-run source observations and the Rust source remain unchanged. This combined development census does not replace immutable release qualification.

The final native-runner Python suite passes200tests on macOS and198with two platform skips on Linux. The Cargo/release creation suite passes293on macOS and291with two platform skips on Linux. Routine updater capacity admission and explicit local backing-record authoring pass147tests/265subtests and independent review. The real source42 deployment now has a fresh owner-private record binding its unchanged guest route to the explicitly pinned Mac route and actual VM directory. No host was contacted by the authoring command. A separate live metadata-only probe then correctly refuses the4,362,076,160-byte minimum while3,006,984,192bytes are available; this is a lower-bound observation, not full artifact admission.

Live logrotation reports healthy, with a successful scheduled run and exit0. Validator1's public-format status confirms its stale height1,969, queue1 and one observed peer; its local policy200 does not establish network health. Validators2–4 completed11 startup attempts with the same missing-beacon-session failure. Their restart loops were stopped; all three now have inactive units, identical3,598-entry public indexes/hashes, and unchanged digests across the stop. This limits churn while the retained history is inspected. No replacement genesis, new signing material or state injection was performed.

Current deferred activation removes premature idle-start failure; it does not reconstruct missing committed public key-session state. At a mandatory pulse height, candidate assembly and independent block validation require the active session from committed prestate. A newly installed session activates one height later. The bootstrap-ingress test proves admission only. Retained public history must therefore distinguish an actual restoration loss from initialization that never committed before its required boundary. Existing runtime Kagami binaries lack the bounded history reader; a focused current diagnostic build is in progress and will inspect only a verified copy of public store files.

## Superseded root status and P9, preserved verbatim

Public Taira runs source42 (`d085418a382e831875731144436c174d8621cdd6`) on the retained ledger. The public faucet policy route was deployed and returned HTTP 200, but public ingress is currently degraded: a fresh September 15 15:28 UTC check returned 502 on the primary origin and direct validator roots 2–4; validator 1 still served the policy. Validators 2–4 first failed at 15:06:31 UTC after committing height 3598, because the mandatory NPoS beacon at height 3599 has no active public key session. Valid snapshots preserve height 3598, and automatic restarts repeat the same failure. Validator 1 remains in its earlier finalized-lane recovery stall. Source44 does not change the beacon requirement; a binary-only update has not been shown to recover this incident. No ledger replacement or consensus-validation bypass has been performed.

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Native initialization derives typed inputs from authenticated genesis and the exact four-peer profile; deployment keeps independently authenticated completion and resumable once-only submission. Earlier CLI qualification passed all 85 deployment controls and all 379 macOS CLI controls; maintained serial batching reduced the latter from 254.091 to 75.611 seconds. The doctor validates canonical public faucet policy in both scopes without adopting signing trust. Core73 rebuilt the repaired Core harness in 421.725 seconds and executed all 222 selected macOS tests in 337.816 seconds: 220 passed, including all 13 previously failing Linux diagnostic selectors, six retained-Kura controls and three adjacent snapshot/genesis checks. The two new failures were direct fixtures publishing protected World catalog data without matching canonical runtime metadata. Core74 corrects those fixtures while preserving exact negative, positive and no-mutation assertions; unchanged-source test metadata passes in 55.919 seconds with zero errors, but their native rerun remains pending. Python contract edits during Core73 compilation/execution changed the whole-checkout source observation; these are development results, not immutable release or Linux qualification. Both previously red source acceptance checks now pass: include inventory has 57 acceptance/mutation passes, owner projection has 21 passes, and four ordinary-ingress cases cover 32 negative controls. Historical item review supports the narrow source-item and test-inventory pins; no broad trusted-release seal was refreshed. Four completed obsolete Mac test executables and the earlier separate Linux diagnostic are retained in verified compressed archives; warm Cargo caches remain intact. Physical free space is about 3.7 GiB after the builds, and additional capacity is needed for native rebuilding. Linux57 remains active in GNU linking and has not been signaled; future maintained Linux development builds select verified LLVM18, while macOS retains Apple ld. Public status and funding policy most recently returned502; approved MacStadium operator access remains unavailable. Shipping binaries, the real four-validator restart/paid clean-client corridor and actual DPN deployment remain unfinished. N0 original Validate-to-Apply custody, aggregate admission and consuming-publisher ownership remain separate open outcomes; raw State publication is not a blanket refusal. Generic bundle/OCI qualification still needs its broader preexisting source-seal review. No live write, validator restart, empty production block or ledger replacement occurred. Detailed evidence and historical scopes are retained in the [dated recovery record](docs/history/2026-09-19/dpn-recovery-geometry.md).

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Preserve release94's completed public reset, same-source Linux preparation, convergence/canaries/restarts and public Host routing; finish test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Qualify the maintained native public-input/profile export, typed genesis-derived manifest generation and `dataspace-deploy init/plan/apply/status` path for physical DPN and paid `dpn`/`admin@dpn`, including bounded fees, once-only submission, independently anchored inclusion and all-four StateApplied. Require selector-bound typed absence and preserved alias diagnostics through Torii, native SDK/MCP readers and OpenAPI, plus one inherited wait deadline with no zero-budget dispatch or late finality and exact typed terminal-failure evidence. Retain the merged-source Linux metadata and separate runtime diagnostics; retain Core73 macOS native220/222, including all13 prior failures now passing; natively rerun the two reviewed and metadata-checked Core74 catalog fixtures, then complete the current Linux Cargo batch, shipping binaries and production four-validator supervisor restart before the unchanged full release gate; retain composed FD, inventory, shared-deadline transport and stage-consumer checks and submitted ambiguity in one native apply recovery journal. Retain the qualified core onboarding/faucet/final-canary transport lifecycle in future rollouts. Recover the source42 first-epoch beacon incident without weakening finalized history or pulse validation; supply a production native key-session and per-seat signer bootstrap, prove its committed activation before the first required pulse, and make missing beacon readiness fail deployment qualification early. Retain source46’s passing four-peer recovery fixture; qualify bounded finality-read HTTP 429 retries, production beacon custody/bootstrap/readiness, authenticated height observation and maintained controller integration before DPN activation. Retain the six passing pending-Kura recovery controls and qualify their original-body four-stage restart in the unchanged four-validator Linux corridor. Run all six mandatory recovery controls first and stop before other startup/release work on failure. Complete N0’s original Validate-to-Apply custody, aggregate resource admission and consuming publisher lifecycle/participant ownership before claiming one original execution through publication. Resolve retained-height-3598 recovery separately; no ledger replacement is authorized. Public faucet policy discovery is deployed and both doctor scopes now validate its exact response; carry the 19 passing doctor/consumer controls into fresh ingress and chain-health verification, require enabled funding for deployment qualification and keep funding authority independently pinned. Retain the 85 current-source deployment regressions, including signed-prefix retry and disk-tamper controls; qualify them in the real four-peer completion corridor. Retain the exact serial CLI batch and its 379 passing macOS controls; execute the complete Linux selection in the maintained candidate, preserving configuration/startup ordering, closed result counts, failure aggregation and progress output. Qualify the default Linux development LLVM workflow through its first Cargo fingerprint rebuild; treat the 12.13-second separate Core link only as linker timing. Preserve macOS Apple ld, fixed lsof preflight and authenticated release boundaries. Preserve early beacon-root admission, observation-only build deferral and bounded source-content revalidation; retain the repaired source-contract and inventory acceptance with their negative controls, and complete the broader trusted release-source review before claiming generic bundle/OCI qualification. Ordinary deployment of existing binaries must remain independent of Cargo/release gates. Remove the client build dependency on full validator Core only through shared exact trust/manifest checks and an explicit operator-command boundary, without duplicate or weakened validation. Measure executable-only revision changes in the release build lane without invalidating shared libraries. |

## Physical backing and diagnostic completion

The existing weekly guest TRIM service had previously succeeded, and the root ext4 filesystem advertised discard with zero recorded filesystem errors. A root-only supported `/usr/sbin/fstrim --verbose /` completed in10.748seconds, accepting16,760,815,616bytes of unused extents. This is not the physical recovery figure. The same raw disk inode149088402 shrank from62,694,666,240 to59,358,625,792 allocated bytes; Mac availability rose from2,997,821,440 to6,355,697,664bytes (about3.1GiB recovered). Disk logical size, VM configuration SHA and the three validator2 public-ledger file hashes stayed unchanged. The maintained4,362,076,160-byte minimum probe then passed with6,355,021,824bytes available; full release-artifact admission remains separate. Evidence: `vm-fstrim-host-before-20260920.json`, `vm-fstrim-execution-20260920.json`, `vm-fstrim-host-after-20260920.json`, and `mac-backing-minimum-after-trim-20260920.json` under `target/dpn-devex/`. No database, cache or ledger cleanup was performed by this operation.

The stopped validator2 selected snapshot at height3598 already has empty current and undo maps for all five canonical beacon stores. Its1,104,543-byte payload SHA256 `87846456b0e23334e8ee1fd3c8fa6cd6165f4301eab8268a5c51b31a4409520c` matches both the current pointer and digest file; pinned identity stayed stable. This rules out the current restart decoder dropping a session present in that payload. The Python projection did not independently verify the snapshot signature and cannot prove historical absence. Exact bounded script and metadata-only evidence are retained in `target/dpn-devex/beacon-snapshot-audit-20260920/`.

Current Kagami's nine native beacon-history tests pass. The package build produced executable SHA256 `6b2a8055696d488c8c403c48f613f8a5c49281fdaf9ef98a3c6198a2f7d47cb2` (470,795,640bytes). An initial whole-tree source refusal and a later Cargo-alias refusal were preserved. Ordinary Cargo binaries legitimately use a published path and a normalized hashed-deps hardlink. The new shared Cargo-only importer pins that exact two-name closure, its directories and descriptor/content identity before/after capture under maintained locks. Unknown/third links remain rejected; generic artifacts and immutable captures retain single-link custody. The helper joins signed bootstrap/source/module-origin admission. Independent review caught and closed the hyphen-to-underscore target case. Three Python suites pass303tests/1,827subtests on macOS and301tests with two platform skips on Linux. Linux's retained initial failed suite used `/tmp`; the successful run uses the existing private temporary-root/umask fixture prerequisites. No Cargo cache aliases were removed or changed.

The existing diagnostic executable was captured and run only against the verified local public-store copy; no Rust rebuild or live execution was needed. Beacon-history stopped at retained block1. A fresh unchanged-source ordinary print observation exposed the exact cause: `Norito (de)serialization issue: length mismatch`; all copied store hashes remained unchanged. Consequently there is no projected history, authenticated replay or absence result. The source comparison identifies deliberate first-release changes to required BlockHeader and BlockResult layouts, with an explicit retired thirteen-field header rejection test; it does not instrument the exact failing nested field. The scanner now retains the decoder's Display message instead of discarding it, a diagnostic-only edit checked by rustfmt. A directory-scoped gitignore exception makes new Python test sources visible; two preexisting hidden test files were exposed and preserved, not authored or staged by this work. Evidence is in `target/dpn-devex/beacon-live-20260920/`, especially `decode-observation/` and `wire-comparison/`.

The approved guest already has active Linux `nginx.service`, managed edge roots and historical native reset inventories. The Mac ingress is not a Linux target; complete historical edge/topology binding remains to be checked. Fresh reset preparation requires qualified signed artifacts, new signed genesis/public inputs, exact old/new topology and occupied-state intent, actual configurations and runtime credential custody, native beacon inputs and supervisor intent. No fresh genesis, key, authorization or reset was generated. Native rollback retains old state by same-filesystem rename before deployment proof; this is not an off-host backup, and post-proof rollback is forbidden. Any reset approval must identify the actual old/new genesis and state replacement.

### Superseded current status and P9 before this follow-up, preserved verbatim

Public Taira still runs source42 (`d085418a382e831875731144436c174d8621cdd6`) on the retained ledger. September20 recovery restored MacStadium guest SSH after backing-disk exhaustion, repaired the nginx archive-retention defect and restarted the existing VM with unchanged disk identity and configuration. Validator1 serves faucet policy200 locally; validators2–4 authenticate the retained3,598-block history, then stop because their active threshold-beacon key session is absent. Primary public status and funding policy remain502. Host backing headroom is still limited. No ledger replacement, database deletion or empty production block occurred. The [incident record](docs/history/2026-09-20/dpn-live-recovery.md) distinguishes live findings from historical qualification.

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Typed initialization, resumable once-only deployment and independently authenticated completion remain implemented. All219current basic Core selectors now pass across maintained focused Linux runs using one unchanged executable, including both Core74 catalog fixtures, six pending-Kura controls and the beacon/no-empty-proposal controls. Owner-private Cargo and native child creation fixes the permissive guest umask without weakening custody checks; exact warm-artifact metadata repair preserves content hashes. Native-runner Python tests pass200on macOS and198with two Linux skips; Cargo/release creation tests pass293on macOS and291with two Linux skips. Focused recovery ordering preserves the explicit census and stops before later work on recovery failure. The deployed nginx retention/health correction passes30tests/12subtests and its scheduled health is green. Explicit Mac backing-record authoring and host/guest capacity admission pass147tests/265subtests and independent review; the real route binding is complete, while current physical capacity still refuses a full update. Historical target outputs were externally deleted; fresh evidence is retained in the same restored fixed Linux lane. Shipping binaries, full native qualification, four-validator retained-history recovery and actual paid clean-client DPN deployment remain open. Broader trusted-source seal review and N0 original Validate-to-Apply custody, aggregate admission and consuming publication remain separate outcomes. See the [current incident record](docs/history/2026-09-20/dpn-live-recovery.md) and [historical recovery evidence](docs/history/2026-09-19/dpn-recovery-geometry.md).

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Preserve release94's completed public reset, same-source Linux preparation, convergence/canaries/restarts and public Host routing; finish test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Qualify the maintained native public-input/profile export, typed genesis-derived manifest generation and `dataspace-deploy init/plan/apply/status` path for physical DPN and paid `dpn`/`admin@dpn`, including bounded fees, once-only submission, independently anchored inclusion and all-four StateApplied. Require selector-bound typed absence and preserved alias diagnostics through Torii, native SDK/MCP readers and OpenAPI, plus one inherited wait deadline with no zero-budget dispatch or late finality and exact typed terminal-failure evidence. Retain the merged-source Linux metadata and separate runtime diagnostics; retain Core73 macOS native220/222, including all13 prior failures now passing; retain the219current basic Core Linux passes, including both Core74 catalog fixtures; complete the remaining native selection, shipping binaries and production four-validator supervisor restart before the unchanged full release gate; retain composed FD, inventory, shared-deadline transport and stage-consumer checks and submitted ambiguity in one native apply recovery journal. Retain the qualified core onboarding/faucet/final-canary transport lifecycle in future rollouts. Recover the source42 first-epoch beacon incident without weakening finalized history or pulse validation; supply a production native key-session and per-seat signer bootstrap, prove its committed activation before the first required pulse, and make missing beacon readiness fail deployment qualification early. Retain source46’s passing four-peer recovery fixture; qualify bounded finality-read HTTP 429 retries, production beacon custody/bootstrap/readiness, authenticated height observation and maintained controller integration before DPN activation. Retain the six passing pending-Kura recovery controls and qualify their original-body four-stage restart in the unchanged four-validator Linux corridor. Run all six mandatory recovery controls first and stop before other startup/release work on failure. Complete N0’s original Validate-to-Apply custody, aggregate resource admission and consuming publisher lifecycle/participant ownership before claiming one original execution through publication. Resolve retained-height-3598 recovery separately; no ledger replacement is authorized. Public faucet policy discovery is deployed and both doctor scopes now validate its exact response; carry the 19 passing doctor/consumer controls into fresh ingress and chain-health verification, require enabled funding for deployment qualification and keep funding authority independently pinned. Retain the 85 current-source deployment regressions, including signed-prefix retry and disk-tamper controls; qualify them in the real four-peer completion corridor. Retain the exact serial CLI batch and its 379 passing macOS controls; execute the complete Linux selection in the maintained candidate, preserving configuration/startup ordering, closed result counts, failure aggregation and progress output. The restored Linux LLVM lane passes metadata, Core/config codegen and all219basic Core selectors; carry owner-private build/test creation and unchanged custody guards into immutable qualification. Preserve reviewed explicit Mac backing and guest capacity admission, provide sufficient physical headroom, and retain the deployed nginx retention/health repair; treat the 12.13-second separate Core link only as linker timing. Preserve macOS Apple ld, fixed lsof preflight and authenticated release boundaries. Preserve early beacon-root admission, observation-only build deferral and bounded source-content revalidation; retain the repaired source-contract and inventory acceptance with their negative controls, and complete the broader trusted release-source review before claiming generic bundle/OCI qualification. Ordinary deployment of existing binaries must remain independent of Cargo/release gates. Remove the client build dependency on full validator Core only through shared exact trust/manifest checks and an explicit operator-command boundary, without duplicate or weakened validation. Measure executable-only revision changes in the release build lane without invalidating shared libraries. |

## Signed candidate preparation

The continued goal authorizes release preparation and reserves explicit approval for a concrete ledger replacement. Applicable repository instructions require signed commits; no current instruction prohibits committing. The previous no-commit boundary was carried forward from assistant context rather than an applicable user or repository restriction. The extra commit confirmation was withdrawn. The candidate contains only the19 reviewed paths; two preexisting newly visible test files remain outside its scope. Configured public signing fingerprint is `9D1C8BFA5A0C1FEF5A8B1E5F552C2D0FD7C40BEB`. No ledger replacement is authorized by signing the source candidate.

### Superseded DevEx status before signed preparation, preserved verbatim

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. All 219 selected Linux Core controls pass, including the final catalog fixtures, pending-Kura recovery and deferred-beacon/no-empty-work controls. Cargo and native subprocesses create private files; the gate runs selected pending-Kura failures first. The shared Cargo importer now accepts only the exact owner-held published/hashed-deps alias pair, including normalized binary names, while generic artifacts and captured outputs retain single-link custody. Independent review passes; its three Python suites pass 303 tests on macOS and 301 with two skips on Linux. Nine native history-reader controls passed before the diagnostic-only decoder-error-context edit, which passes formatting. Explicit Mac/guest capacity binding has 147 tests/265 subtests; deployed nginx retention/health has 30 tests/12 subtests. These are development observations, not immutable release qualification. The current HEAD is unsigned and reviewed controller fixes are uncommitted; signed preparation, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and actual paid DPN deployment remain outstanding. Existing Linux edge metadata is present on the approved guest; its complete reset binding still needs validation. No root staging, commit or push occurred.

## Signed Linux preparation follow-up

The reviewed nineteen-file candidate was committed as `10ee1b1d619887fc68bd9a8d5b1d0bf023d039ac` on `optimizations`, with full signer fingerprint `9D1C8BFA5A0C1FEF5A8B1E5F552C2D0FD7C40BEB`. Both macOS and the existing Colima Linux guest verify its signature and exact signer. The shared checkout had a macOS-only absolute `gpg.program`; selecting the portable `gpg` command and importing only the public verification key into the guest fixes Linux verification. The inherited guest home and all private signing material remain unchanged. Evidence: ignored `target/dpn-devex/{signed-candidate-result-20260920,release-signer-verification-20260920}.json`. No push or ledger replacement occurred.

Mac preparation runs native macOS fixtures before cross-compiling Linux artifacts; it does not exercise Linux-only supervision and restart behavior. Current preparation therefore targets the existing Linux guest. The authenticated runner previously omitted the already-tested development LLVM selection and would return to the slow GNU linker. Native compiler/linker selection must be explicit and bound into the preparation request and every revalidation, while shipping Zig keeps its separate environment.

The native-linker correction is independently reviewed. Its final combined Cargo importer/release/gate Python suite ran312tests on Linux:310passed and two platform skips, with unchanged hashes for all four edited modules. macOS preparation tests and the earlier combined310-test suite passed; the final13standalone tests also passed. Evidence is retained in `target/dpn-devex/prepare-native-linker-{linux-tests-20260920,final-tests,standalone-tests}.log`. Official portable Zig0.16.0 and cargo-zigbuild0.22.1 archives were checksum-verified, safely extracted and version-tested on Linux; exact executable identities are in `target/dpn-devex/linux-release-tools/linux-verified.json`. These do not constitute release or deployment qualification.

### Superseded status and P9 before pinned Linux preparation, preserved verbatim

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. All 219 selected Linux Core controls pass, including the final catalog fixtures, pending-Kura recovery and deferred-beacon/no-empty-work controls. Cargo and native subprocesses create private files; the gate runs selected pending-Kura failures first. The shared Cargo importer now accepts only the exact owner-held published/hashed-deps alias pair, including normalized binary names, while generic artifacts and captured outputs retain single-link custody. Independent review passes; its three Python suites pass 303 tests on macOS and 301 with two skips on Linux. Nine native history-reader controls passed before the diagnostic-only decoder-error-context edit, which passes formatting. Explicit Mac/guest capacity binding has 147 tests/265 subtests; deployed nginx retention/health has 30 tests/12 subtests. These are development observations, not immutable release qualification. Signed preparation, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and actual paid DPN deployment remain outstanding. Existing Linux edge metadata is present on the approved guest; its complete reset binding still needs validation.

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Preserve release94's completed public reset, same-source Linux preparation, convergence/canaries/restarts and public Host routing; finish test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Qualify the maintained native public-input/profile export, typed genesis-derived manifest generation and `dataspace-deploy init/plan/apply/status` path for physical DPN and paid `dpn`/`admin@dpn`, including bounded fees, once-only submission, independently anchored inclusion and all-four StateApplied. Require selector-bound typed absence and preserved alias diagnostics through Torii, native SDK/MCP readers and OpenAPI, plus one inherited wait deadline with no zero-budget dispatch or late finality and exact typed terminal-failure evidence. Retain the merged-source Linux metadata and separate runtime diagnostics; retain Core73 macOS native220/222, including all13 prior failures now passing; retain the219current basic Core Linux passes, including both Core74 catalog fixtures; complete the remaining native selection, shipping binaries and production four-validator supervisor restart before the unchanged full release gate; retain composed FD, inventory, shared-deadline transport and stage-consumer checks and submitted ambiguity in one native apply recovery journal. Retain the qualified core onboarding/faucet/final-canary transport lifecycle in future rollouts. Recover the source42 first-epoch beacon incident without weakening finalized history or pulse validation; supply a production native key-session and per-seat signer bootstrap, prove its committed activation before the first required pulse, and make missing beacon readiness fail deployment qualification early. Retain source46’s passing four-peer recovery fixture; qualify bounded finality-read HTTP 429 retries, production beacon custody/bootstrap/readiness, authenticated height observation and maintained controller integration before DPN activation. Retain the six passing pending-Kura recovery controls and qualify their original-body four-stage restart in the unchanged four-validator Linux corridor. Run all six mandatory recovery controls first and stop before other startup/release work on failure. Complete N0’s original Validate-to-Apply custody, aggregate resource admission and consuming publisher lifecycle/participant ownership before claiming one original execution through publication. Preserve the retained height-3598 ledger and source42 evidence. Current required header/result layouts reject the retained format; no compatibility decoder or unauthenticated session injection is allowed. Prepare qualified fresh-genesis and existing Linux-edge inputs, then obtain explicit old/new-ledger replacement approval before applying a reset. Public faucet policy discovery is deployed and both doctor scopes now validate its exact response; carry the 19 passing doctor/consumer controls into fresh ingress and chain-health verification, require enabled funding for deployment qualification and keep funding authority independently pinned. Retain the 85 current-source deployment regressions, including signed-prefix retry and disk-tamper controls; qualify them in the real four-peer completion corridor. Retain the exact serial CLI batch and its 379 passing macOS controls; execute the complete Linux selection in the maintained candidate, preserving configuration/startup ordering, closed result counts, failure aggregation and progress output. The restored Linux LLVM lane passes metadata, Core/config codegen and all219basic Core selectors; carry owner-private build/test creation and unchanged custody guards into immutable qualification. Preserve reviewed explicit Mac backing and guest capacity admission; retain measured TRIM recovery and deployed nginx retention/health repair, rechecking actual artifact capacity. Carry the exact Cargo alias importer and 303-test Python validation into signed release preparation; treat the 12.13-second separate Core link only as linker timing. Preserve macOS Apple ld, fixed lsof preflight and authenticated release boundaries. Preserve early beacon-root admission, observation-only build deferral and bounded source-content revalidation; retain the repaired source-contract and inventory acceptance with their negative controls, and complete the broader trusted release-source review before claiming generic bundle/OCI qualification. Ordinary deployment of existing binaries must remain independent of Cargo/release gates. Remove the client build dependency on full validator Core only through shared exact trust/manifest checks and an explicit operator-command boundary, without duplicate or weakened validation. Measure executable-only revision changes in the release build lane without invalidating shared libraries. |

## First signed Linux launch: directory creation failure

Signed linker candidate `f38ca7731e9b86a678bbd4ecd94cf292c1570bfe` passed signature verification and source-scoped tests. Its real Linux `prepare` stopped before source capture or compilation: `Path.mkdir(mode=0o700, parents=True)` created intermediate `target/taira-release-sources` as0775 under the guest default umask0002. The leaf was0700, which hid this failure in tests using private ambient permissions. Child-process umask handling did not cover Python parent creation. The failed tree contained only a keyed directory and empty session lock; after obtaining that exact lock and checking inode/contents, it was renamed intact beneath the owner-private incident directory. No Cargo cache, captured source or ledger was removed or modified. Evidence: `target/dpn-devex/linux-preparation-f38ca7731e9b86a678bbd4ecd94cf292c1570bfe.log` and `failed-source-lane-retention-20260920.json`. The maintained correction must create every absent directory privately while rejecting existing unsafe owners; changing the launch shell umask is insufficient.

The shared descriptor-based private-directory helper now covers all three affected owners: source-lane setup, nested Git capture and host/cross-target fingerprint retirement. It leaves existing unsafe paths unchanged, checks custody on opened descriptors and does not change the process umask. Review passes. macOS focused validation passes142tests/121subtests; the actual Cargo relocation case was separately exercised in the final Linux suite, which ran322tests with320passes and two platform skips in7.034seconds. Six changed source/test hashes remained unchanged throughout that Linux run. The initial aggregate Linux test invocation lacked pytest and the managed Cargo PATH; its setup failures are retained, while the successful corrected unittest run and macOS pytest coverage are recorded separately. No failed invocation is counted as a pass.

## Authenticated compilation and predecessor runtime correction

Signed candidate `48041b83eaca550392086719d1043c3817190c14` passed private source setup under the unmodified Linux umask0002, source audits and standalone controls. Native test codegen stopped after763.3seconds and684rebuilt units because selected Torii test sources still called the removed six-argument `BlockHeader::new`. No qualification result or shipping artifact was published. The complete repository migration removes57obsolete fourth `None` arguments across33files, preserving every remaining argument; no constructor overload or compatibility decoder is added. Linux `cargo check -p iroha_torii --tests --locked --offline` passes in the persistent development lane (5m22s). The outside-Torii lexical census confirms all1,599calls have five arguments and no other semantic changes. Evidence: ignored `target/dpn-devex/torii-header-api-check-20260920.log` and `block-header-arity-audit-20260920/verification.json`. This metadata result is not native execution or release qualification.

The occupied predecessor schema incorrectly required all eight candidate artifacts, including per-validator build tools absent from the actual prior runtime. It now requires exactly the five ordered runtime roles: daemon, configuration, genesis, genesis hash and validator unit. Candidate publication retains all eight roles. A prior epoch supervisor independently binds its retained CLI and Kagami in its signed plan; those roots remain protected during cleanup discovery, durable replay and the final rename. Malformed prior plans fail closed. The old eight-role predecessor input is rejected rather than interpreted as a compatibility format. Fresh read-only observations bind all actual prior runtime files; the existing retained Kagami manifest association is not claimed as cryptographically reverified provenance. No remote artifact relocation, service change or ledger mutation occurred.

Independent review passes. The gate Python suite runs203tests on Linux, with201passing and two platform skips; macOS passes203tests and1721subtests. Four new native selectors are registered exactly once in both scopes, but their native execution is pending. Evidence: `target/dpn-devex/predecessor-gate-python-linux-20260920.log`. The next signed preparation reuses the existing authenticated source/target lane and must run the current selection before shipping or deployment claims.

### Superseded status and P9 before constructor and predecessor corrections, preserved verbatim

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `10ee1b1d619887fc68bd9a8d5b1d0bf023d039ac` verifies on macOS and Linux. All 219 selected Linux Core controls pass in development. Cargo/native children create private files, pending-Kura failures run first, and exact Cargo alias-pair capture retains custody. Authenticated preparation now binds the native compiler/linker choice and tool identities, defaults to LLVM18 on Linux and Apple ld on Mac, and propagates those flags to Cargo and direct rustc separately from shipping Zig. Independent review passes; the final combined Python suite runs 312 tests on Linux with 310 passing and two platform skips. Pinned portable Linux Zig and cargo-zigbuild are verified. These remain development/setup observations: immutable preparation, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and actual paid DPN deployment remain outstanding. The approved guest contains the existing Linux edge; current occupied bindings still require fresh assembly. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Preserve release94's completed public reset, same-source Linux preparation, convergence/canaries/restarts and public Host routing; finish test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Qualify the maintained native public-input/profile export, typed genesis-derived manifest generation and `dataspace-deploy init/plan/apply/status` path for physical DPN and paid `dpn`/`admin@dpn`, including bounded fees, once-only submission, independently anchored inclusion and all-four StateApplied. Require selector-bound typed absence and preserved alias diagnostics through Torii, native SDK/MCP readers and OpenAPI, plus one inherited wait deadline with no zero-budget dispatch or late finality and exact typed terminal-failure evidence. Retain the merged-source Linux metadata and separate runtime diagnostics; retain Core73 macOS native220/222, including all13 prior failures now passing; retain the219current basic Core Linux passes, including both Core74 catalog fixtures; complete the remaining native selection, shipping binaries and production four-validator supervisor restart before the unchanged full release gate; retain composed FD, inventory, shared-deadline transport and stage-consumer checks and submitted ambiguity in one native apply recovery journal. Retain the qualified core onboarding/faucet/final-canary transport lifecycle in future rollouts. Recover the source42 first-epoch beacon incident without weakening finalized history or pulse validation; supply a production native key-session and per-seat signer bootstrap, prove its committed activation before the first required pulse, and make missing beacon readiness fail deployment qualification early. Retain source46’s passing four-peer recovery fixture; qualify bounded finality-read HTTP 429 retries, production beacon custody/bootstrap/readiness, authenticated height observation and maintained controller integration before DPN activation. Retain the six passing pending-Kura recovery controls and qualify their original-body four-stage restart in the unchanged four-validator Linux corridor. Run all six mandatory recovery controls first and stop before other startup/release work on failure. Complete N0’s original Validate-to-Apply custody, aggregate resource admission and consuming publisher lifecycle/participant ownership before claiming one original execution through publication. Preserve the retained height-3598 ledger and source42 evidence. Current required header/result layouts reject the retained format; no compatibility decoder or unauthenticated session injection is allowed. Prepare qualified fresh-genesis and existing Linux-edge inputs, then obtain explicit old/new-ledger replacement approval before applying a reset. Public faucet policy discovery is deployed and both doctor scopes now validate its exact response; carry the 19 passing doctor/consumer controls into fresh ingress and chain-health verification, require enabled funding for deployment qualification and keep funding authority independently pinned. Retain the 85 current-source deployment regressions, including signed-prefix retry and disk-tamper controls; qualify them in the real four-peer completion corridor. Retain the exact serial CLI batch and its 379 passing macOS controls; execute the complete Linux selection in the maintained candidate, preserving configuration/startup ordering, closed result counts, failure aggregation and progress output. The restored Linux LLVM lane passes metadata, Core/config codegen and all219basic Core selectors; carry owner-private build/test creation and unchanged custody guards into immutable qualification. Preserve reviewed explicit Mac backing and guest capacity admission; retain measured TRIM recovery and deployed nginx retention/health repair, rechecking actual artifact capacity. Carry the exact Cargo alias importer and 303-test Python validation into signed release preparation; treat the 12.13-second separate Core link only as linker timing. Preserve macOS Apple ld, fixed lsof preflight and authenticated release boundaries. Preserve early beacon-root admission, observation-only build deferral and bounded source-content revalidation; retain the repaired source-contract and inventory acceptance with their negative controls, and complete the broader trusted release-source review before claiming generic bundle/OCI qualification. Ordinary deployment of existing binaries must remain independent of Cargo/release gates. Remove the client build dependency on full validator Core only through shared exact trust/manifest checks and an explicit operator-command boundary, without duplicate or weakened validation. Measure executable-only revision changes in the release build lane without invalidating shared libraries. Complete signed Linux preparation with request-bound native LLVM18 compiler/linker identities, exact resume revalidation and separate pinned Zig shipping tools; retain Apple ld on macOS and do not count Mac fixtures as Linux restart qualification. |

## Current native fixture and funding-admission follow-up

Signed candidate `cc1f58f66a48d17296242f3de329ebcc45b4d339` compiled all23requested native targets in42.3seconds, retaining679fresh Cargo units and rebuilding eight. The native stage ended after314.231seconds with15selected startup failures, so CLI completion, the four-peer corridor and shipping compilation did not run. Four Ordinary ingress tests shared a fixture whose SCCP proposal setter correctly invalidated previously attached results; the fixture now reattaches the same declared outputs to the final proposal before its genuine signature and CommitQC. Four routed-read/onboarding tests shared pre-genesis physical-catalog setup through the ordinary runtime setter; four setup helpers now bind configured/effective catalogs identically and call the existing startup boundary. All69helper consumers were reviewed as fresh height-zero fixtures. No runtime catalog guard changes.

Seven beacon-broker cases failed before their protocol assertion because a Mac-shared directory could not apply Linux Unix-socket permissions. Both real and fake broker fixtures now use a unique mode0700 short directory under canonical native `/tmp`; the existing socket permissions, inode/peer credentials and corruption checks remain intact. Disconnected readiness reports the actual broker thread result. The next preparation uses the guest's private native runtime temporary directory, preserving inherited HOME; a socket bind/chmod probe passes. The source and Cargo lanes remain unchanged. Evidence: `target/dpn-devex/broker-socket-fixture-20260920.json`, `native-linux-temp-root-20260920.json` and the complete `linux-preparation-cc1f58f66a48d17296242f3de329ebcc45b4d339.log`. Independent fixture reviews pass; native reruns remain pending.

A separate deployment defect admitted candidate configs whose faucet was disabled or whose authority, asset or amount differed from signed intent. One shared typed public-policy validator now runs during native context derivation and again against held pinned configs before forward remote dispatch. It preserves native Quantity/default semantics, rejects unbound inheritance and redacts malformed inputs without opening faucet signer files. The two new controls cover coherent configurations, every mismatch and the actual four-validator pinned-input seam. Both scopes register them exactly once (Mac1031/1209; Linux1036/1214). The gate Python suite passes203tests/1729subtests on Mac; native execution is pending.

The full workspace formatting check was attempted and reports preexisting drift in three unrelated files (`canonical_runtime_tests.rs`, `production_beacon_prepare.rs`, `external_software_signer.rs`); scoped changed Rust files are formatted. This is not a full workspace formatting pass. Fresh public probes still return502 for status and faucet policy. No live reset, ledger replacement or empty production block occurred.

### Superseded status and P9 before current native fixture corrections, preserved verbatim

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `48041b83eaca550392086719d1043c3817190c14` reached authenticated Linux native compilation with pinned LLVM18 and private source creation under the guest default umask. Compilation stopped on stale six-argument BlockHeader constructors. All 57 remaining calls now use the required five-argument API without a compatibility shim; every Torii test target passes Linux metadata checking. The reviewed occupied-reset model now requires exactly five prior runtime artifacts, retains eight candidate artifacts, and protects independently signed prior supervisor tools through cleanup. Its Linux Python gate suite passes 201 tests with two platform skips; native regressions remain pending. All 219 selected Linux Core development controls and earlier custody/linker tests remain retained evidence. Complete the next signed immutable preparation, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment. Public Taira remains unhealthy. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Preserve release94's completed public reset, same-source Linux preparation, convergence/canaries/restarts and public Host routing; finish test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Qualify the maintained native public-input/profile export, typed genesis-derived manifest generation and `dataspace-deploy init/plan/apply/status` path for physical DPN and paid `dpn`/`admin@dpn`, including bounded fees, once-only submission, independently anchored inclusion and all-four StateApplied. Require selector-bound typed absence and preserved alias diagnostics through Torii, native SDK/MCP readers and OpenAPI, plus one inherited wait deadline with no zero-budget dispatch or late finality and exact typed terminal-failure evidence. Retain the merged-source Linux metadata and separate runtime diagnostics; retain Core73 macOS native220/222, including all13 prior failures now passing; retain the219current basic Core Linux passes, including both Core74 catalog fixtures; complete the remaining native selection, shipping binaries and production four-validator supervisor restart before the unchanged full release gate; retain composed FD, inventory, shared-deadline transport and stage-consumer checks and submitted ambiguity in one native apply recovery journal. Retain the qualified core onboarding/faucet/final-canary transport lifecycle in future rollouts. Recover the source42 first-epoch beacon incident without weakening finalized history or pulse validation; supply a production native key-session and per-seat signer bootstrap, prove its committed activation before the first required pulse, and make missing beacon readiness fail deployment qualification early. Retain source46’s passing four-peer recovery fixture; qualify bounded finality-read HTTP 429 retries, production beacon custody/bootstrap/readiness, authenticated height observation and maintained controller integration before DPN activation. Retain the six passing pending-Kura recovery controls and qualify their original-body four-stage restart in the unchanged four-validator Linux corridor. Run all six mandatory recovery controls first and stop before other startup/release work on failure. Complete N0’s original Validate-to-Apply custody, aggregate resource admission and consuming publisher lifecycle/participant ownership before claiming one original execution through publication. Preserve the retained height-3598 ledger and source42 evidence. Current required header/result layouts reject the retained format; no compatibility decoder or unauthenticated session injection is allowed. Prepare qualified fresh-genesis and existing Linux-edge inputs, then obtain explicit old/new-ledger replacement approval before applying a reset. Public faucet policy discovery is deployed and both doctor scopes now validate its exact response; carry the 19 passing doctor/consumer controls into fresh ingress and chain-health verification, require enabled funding for deployment qualification and keep funding authority independently pinned. Retain the 85 current-source deployment regressions, including signed-prefix retry and disk-tamper controls; qualify them in the real four-peer completion corridor. Retain the exact serial CLI batch and its 379 passing macOS controls; execute the complete Linux selection in the maintained candidate, preserving configuration/startup ordering, closed result counts, failure aggregation and progress output. The restored Linux LLVM lane passes metadata, Core/config codegen and all219basic Core selectors; carry owner-private build/test creation and unchanged custody guards into immutable qualification. Preserve reviewed explicit Mac backing and guest capacity admission; retain measured TRIM recovery and deployed nginx retention/health repair, rechecking actual artifact capacity. Carry the exact Cargo alias importer and 303-test Python validation into signed release preparation; treat the 12.13-second separate Core link only as linker timing. Preserve macOS Apple ld, fixed lsof preflight and authenticated release boundaries. Preserve early beacon-root admission, observation-only build deferral and bounded source-content revalidation; retain the repaired source-contract and inventory acceptance with their negative controls, and complete the broader trusted release-source review before claiming generic bundle/OCI qualification. Ordinary deployment of existing binaries must remain independent of Cargo/release gates. Remove the client build dependency on full validator Core only through shared exact trust/manifest checks and an explicit operator-command boundary, without duplicate or weakened validation. Measure executable-only revision changes in the release build lane without invalidating shared libraries. Complete signed Linux preparation with request-bound native LLVM18 compiler/linker identities, exact resume revalidation and separate pinned Zig shipping tools; retain Apple ld on macOS and do not count Mac fixtures as Linux restart qualification. Retain the complete five-argument BlockHeader migration and all-Torii Linux metadata pass. Qualify the exact five-role prior runtime contract and separately signed predecessor supervisor cleanup protection; candidate publication still requires all eight roles. |

## Native catalog construction and content-custody follow-up

The focused Linux Torii run completed with all four SCCP ordinary-ingress controls passing. The four catalog controls still failed because even blank State construction had already authenticated the default H0 catalog; applying configured catalogs afterward correctly rejected a different original anchor. The correction supplies the intended catalog to the shared pre-genesis constructor before State and Kura are initialized. Production anchor validation remains unchanged. Evidence: `target/dpn-devex/torii-current-api-fixtures-20260920.log`.

An exact serial diagnostic of the retained signed cc1 CLI harness ran all388 Linux selectors:386 passed and two content-custody controls failed. Metadata alone missed same-size changes when timestamps collided. Journal revalidation now compares retained bytes in bounded offset-preserving reads; original epoch seeds retain their admitted32bytes in zeroizing memory and compare them on every custody revalidation. Two deterministic timestamp-collision regressions join the existing controls. Both gate scopes select them and the two faucet-policy controls exactly once (Mac1033/1211; Linux1038/1216). The gate Python suite passes203tests/1737subtests on macOS. These are diagnostic and source-review results; new native execution remains pending. Evidence: `target/dpn-devex/cc1-cli-batch-diagnostic/` and `target/dpn-devex/cc1-cli-custody-fix/`.

The maintained source-transfer owner was also exercised against the actual signed cc1 repository, not only miniature fixtures. The initial full-tree import exposed legitimate zero-byte tracked source files being rejected by a binary-artifact reader. The source-only correction must retain exact signed bytes and custody while leaving binary nonemptiness mandatory. No guest import or activation was performed.

### Superseded status and P9 before signed-source transfer qualification, preserved verbatim

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `cc1f58f66a48d17296242f3de329ebcc45b4d339` compiles all selected Linux native harnesses in42.3seconds with679fresh/eight rebuilt Cargo units. Its native gate then stops with15failures: four SCCP fixtures attach results before final proposal mutation, four catalog fixtures use a runtime setter for initial physical catalogs, and seven broker fixtures place Unix sockets on the shared Mac filesystem. Corrections preserve proposal/result authentication, pre-genesis catalog admission and socket custody. Candidate funding validation now requires enabled authority/asset/quantity matching signed intent at both assembly and pinned-config forward admission; native execution is pending. The updated gate Python suite passes203tests/1729subtests on Mac. Prior constructor, predecessor-runtime and Linux Core evidence remains retained. Complete focused tests, the next immutable preparation, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment. Public status and funding policy remain502. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

| P9 | Persistent Taira joining and disposable networks | CLI/daemon and authorized network operators | One signed-bundle `iroha taira join` path initializes an owner-private permissionless observer, validates expiry/network/genesis and supports staking activation. Disposable four-validator `taira_devnet.py up` proves typed Applied, advancing convergence, MCP and guest workload; canonical source/artifact identity, no startup-only success or unmanaged cleanup. Preserve release94's completed public reset, same-source Linux preparation, convergence/canaries/restarts and public Host routing; finish test.inori.co.il application verification. Verify stopped-owner cgroup/firewall cleanup on the deployed native dispatcher. Qualify the maintained native public-input/profile export, typed genesis-derived manifest generation and `dataspace-deploy init/plan/apply/status` path for physical DPN and paid `dpn`/`admin@dpn`, including bounded fees, once-only submission, independently anchored inclusion and all-four StateApplied. Require selector-bound typed absence and preserved alias diagnostics through Torii, native SDK/MCP readers and OpenAPI, plus one inherited wait deadline with no zero-budget dispatch or late finality and exact typed terminal-failure evidence. Retain the merged-source Linux metadata and separate runtime diagnostics; retain Core73 macOS native220/222, including all13 prior failures now passing; retain the219current basic Core Linux passes, including both Core74 catalog fixtures; complete the remaining native selection, shipping binaries and production four-validator supervisor restart before the unchanged full release gate; retain composed FD, inventory, shared-deadline transport and stage-consumer checks and submitted ambiguity in one native apply recovery journal. Retain the qualified core onboarding/faucet/final-canary transport lifecycle in future rollouts. Recover the source42 first-epoch beacon incident without weakening finalized history or pulse validation; supply a production native key-session and per-seat signer bootstrap, prove its committed activation before the first required pulse, and make missing beacon readiness fail deployment qualification early. Retain source46’s passing four-peer recovery fixture; qualify bounded finality-read HTTP 429 retries, production beacon custody/bootstrap/readiness, authenticated height observation and maintained controller integration before DPN activation. Retain the six passing pending-Kura recovery controls and qualify their original-body four-stage restart in the unchanged four-validator Linux corridor. Run all six mandatory recovery controls first and stop before other startup/release work on failure. Complete N0’s original Validate-to-Apply custody, aggregate resource admission and consuming publisher lifecycle/participant ownership before claiming one original execution through publication. Preserve the retained height-3598 ledger and source42 evidence. Current required header/result layouts reject the retained format; no compatibility decoder or unauthenticated session injection is allowed. Prepare qualified fresh-genesis and existing Linux-edge inputs, then obtain explicit old/new-ledger replacement approval before applying a reset. Public faucet policy discovery is deployed and both doctor scopes now validate its exact response; carry the 19 passing doctor/consumer controls into fresh ingress and chain-health verification, require enabled funding for deployment qualification and keep funding authority independently pinned. Retain the 85 current-source deployment regressions, including signed-prefix retry and disk-tamper controls; qualify them in the real four-peer completion corridor. Retain the exact serial CLI batch and its 379 passing macOS controls; execute the complete Linux selection in the maintained candidate, preserving configuration/startup ordering, closed result counts, failure aggregation and progress output. The restored Linux LLVM lane passes metadata, Core/config codegen and all219basic Core selectors; carry owner-private build/test creation and unchanged custody guards into immutable qualification. Preserve reviewed explicit Mac backing and guest capacity admission; retain measured TRIM recovery and deployed nginx retention/health repair, rechecking actual artifact capacity. Carry the exact Cargo alias importer and 303-test Python validation into signed release preparation; treat the 12.13-second separate Core link only as linker timing. Preserve macOS Apple ld, fixed lsof preflight and authenticated release boundaries. Preserve early beacon-root admission, observation-only build deferral and bounded source-content revalidation; retain the repaired source-contract and inventory acceptance with their negative controls, and complete the broader trusted release-source review before claiming generic bundle/OCI qualification. Ordinary deployment of existing binaries must remain independent of Cargo/release gates. Remove the client build dependency on full validator Core only through shared exact trust/manifest checks and an explicit operator-command boundary, without duplicate or weakened validation. Measure executable-only revision changes in the release build lane without invalidating shared libraries. Complete signed Linux preparation with request-bound native LLVM18 compiler/linker identities, exact resume revalidation and separate pinned Zig shipping tools; retain Apple ld on macOS and do not count Mac fixtures as Linux restart qualification. Retain the complete five-argument BlockHeader migration and all-Torii Linux metadata pass. Qualify the exact five-role prior runtime contract and separately signed predecessor supervisor cleanup protection; candidate publication still requires all eight roles. Qualify current SCCP output attachment, pre-genesis catalog fixtures and native-filesystem broker fixtures. Require candidate enabled funding policy to match signed intent at assembly and pinned-config forward admission before remote mutation. |

## Completed source import and current merged qualification candidate

The corrected maintained source owner exported and independently imported the actual signed cc1 source tree on macOS in59.47seconds:20,908files,23,269objects and517,663,675expanded bytes. Linux imported and reauthenticated the same public pack in121.96seconds, including exact signed empty blobs, the absent symlink referent and empty gitlink. The source suite passes15real Git/GPG tests on both platforms. Evidence: `target/dpn-devex/signed-source-cc1-real-diagnostic-20260920-02.json`, `source-cc1-linux-import-20260920.log` and `source-linux-empty-blob-tests-20260920.log`. These source-transfer observations are not binary qualification or live activation.

The transfer suite passes all19tests on Linux after fixture inputs were given explicit safe modes rather than inheriting the guest umask0002. Production admission remains strict. Genuine signed-source/controller verification, bounded streaming, exact completed reuse, corrupt/partial input rejection and receipt/retirement mode alignment are covered. Evidence: `target/dpn-devex/transfer-linux-integration-tests-20260920-02.log`.

Current signed optimizations merge `de8954ea3b1da3519c9fdff0c36ebf5cbe2eb736` includes the completed69-call catalog construction migration and concurrent Core/Torii work. Qualification must use this current source, not infer validity from the older cc1 harness. The existing configured-catalog regression is now registered as a startup control in both scopes (Mac1034/1212; Linux1039/1217); it proves explicit chain/network identities, exact State/Kura ownership, preserved runtime defaults and rejection of a different authenticated original catalog. Full native preparation remains required.

The current pre-build audit passes212Python tests (204gate and8lifecycle registration) with1743subtests, all six source-asset checks and all four shipping registrations. The audit originally caught stale pins for a committed pending-Apply startup contract strengthening; length, full digest and the affected case count/digest now match the reviewed canonical asset. Grammar, inventory and mutation controls remain unchanged. Evidence: `target/dpn-devex/current-gate-smoke-de8954-20260920/verified-results.json`.

### Superseded current status before resumed immutable qualification, preserved verbatim

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. The signed cc1 candidate compiles every selected Linux native harness; its startup gate identified SCCP proposal/output order, initial catalog construction and shared-filesystem socket fixture defects. All four corrected SCCP controls pass. Catalog fixtures now need their intended geometry installed during construction; broker sockets use native temporary storage. An exact retained CLI diagnostic passes386/388 controls, exposing timestamp-collision content checks now corrected for journals and original epoch seeds. Enabled faucet policy is bound to signed intent before deployment dispatch. Python gate tests pass203tests/1737subtests; source transfer has real signed-tree coverage, including all20,908files. New native qualification, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. Public Taira remains unhealthy. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).


### Immutable qualification 6c7bb2 and refreshed capacity

Signed candidate `6c7bb2f7fd8070ba5e85cb86efa00cb4df35c448` compiled all 23 Linux native targets in 642.2 seconds (638 fresh Cargo units, 49 rebuilt). Its 392 exact CLI regressions all passed in 37.4 seconds, including both funding-policy controls and both metadata-collision content-custody controls. Qualification failed 31 other regressions: 29 Torii tests with stale initial genesis/catalog construction, one Kagami key fixture writing into immutable source, and one invalid-profile fixture replaying a successful output claim under deliberately invalid configuration. Native guards remain intact. These findings do not qualify shipping binaries or the public network. Evidence: `target/dpn-devex/current-native-failures-6c7bb2-20260920.json`.

Fresh strict-route observations find about 2.09 GB free on the Mac backing volume after supported guest TRIM, below deployment reserve requirements. Guest free space is about 13.44 GB. Independent public topology observation confirms loopback P2P 1337–1340 and Torii 0.0.0.0:8080–8083; credentials are not emitted. A bounded, offhost archive and exact retirement of inactive historical public binaries is being implemented and reviewed. No retained binary has yet been removed by this new path. Source trees, ledger, receipts and current release bindings remain protected.

Previous current-status paragraphs, preserved verbatim:

Public Taira remains on source42 (`d085418a382e831875731144436c174d8621cdd6`); primary status and funding policy remain HTTP 502. Approved MacStadium access is restored and the nginx rotation/health repair is deployed. Validator1 is stale at height 1969; validators2–4 repeatedly loaded height 3598 and failed for an absent beacon session, so their loops are stopped. The selected snapshot already contains empty beacon stores. Current Kagami rejects retained genesis with a decoding length mismatch; current source deliberately changes required block-header/result layouts. No authenticated beacon-history projection or absence conclusion is available. Supported guest TRIM recovered about 3.1 GiB of physical backing space; the minimum capacity probe now passes, but actual artifact admission remains required. No ledger replacement, compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. The current merged source includes all69 catalog-fixture migrations, SCCP proposal/output ordering and native-filesystem broker fixtures, exact funding-policy admission, and content-bound journal/seed custody. The early audit passes212Python tests/1743subtests, six semantic-asset checks and all four shipping registrations. Real signed-source transfer passes on Mac and Linux with all20,908files; all19 transfer tests pass on Linux. These remain source and diagnostic observations. Current immutable native qualification, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. Public status and funding policy still return502. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).


The next native materializer requires an explicit canonical `--torii-bind-address IP:PORT`, preserving the generated port and independent P2P listeners. This closes the split-bind input gap: Taira keeps P2P on127.0.0.1 while the API binds0.0.0.0. Three positive/negative/parser controls are selected in both scopes; native execution remains pending. The four-peer fixture explicitly retains its existing loopback API listener. The updated gate-runner suite passes212tests/1759subtests (Mac1039/1217; Linux1044/1222). No live configuration has been changed.


### Selected native diagnostic after source updates

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `6c7bb2` compiled all 23 Linux native targets and passed all 392 CLI regressions, including funding-policy admission and content-bound journal/seed custody. Qualification failed 31 other tests because stale fixtures bypass current genesis/catalog admission, write into immutable source, or reuse successful execution results under an intentionally invalid profile. Corrections are being checked without weakening native guards. Gate-runner tests now pass 212 tests and 1,747 subtests, including two existing private-key custody controls newly selected in both scopes. Real signed-source transfer and all 19 Linux transfer tests pass. Immutable native qualification, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. Public status and funding policy still return 502. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `6c7bb2` compiled all 23 Linux native targets and passed all 392 CLI regressions; its independent gate failed 31 stale fixture cases. A subsequent mutable diagnostic passed eight selected cases, including immutable-source key custody and resultless negative-genesis validation, but failed 25 Torii cases. Concurrent source changes prevent treating that diagnostic as release evidence. Corrections use native signed genesis without an ordinary lane context, canonical catalog baselines, and typed lifecycle error propagation. Gate-runner tests pass 212 tests and 1,759 subtests; the next candidate includes three explicit split API/P2P listener controls. The signed archive owner has 35 passing macOS and Linux tests and a fresh live-reference observation for 36 inactive public binaries. Immutable native qualification, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. Public status and funding policy still return 502. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

The diagnostic log records eight passing and 25 failing selected tests. The separate eleven-case contract sweep fails at the same shared genesis-context setup. Neither mutable run is release qualification. Signed controller `4cc740c73d1f5e8b78e12cd0a3126c24e6abd72f` owns the reviewed 36-file inactive-public-binary archive/retirement workflow; the off-host archive has started, with no retirement yet. Existing ledger, runtime configuration and current executable remain protected.


### Native execution-publication fixture repair

Previous current-status paragraph, preserved verbatim:

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `6c7bb2` compiled all 23 Linux native targets and passed all 392 CLI regressions; its independent gate failed 31 stale fixture cases. A subsequent mutable diagnostic passed eight selected cases, including immutable-source key custody and resultless negative-genesis validation, but failed 25 Torii cases. Concurrent source changes prevent treating that diagnostic as release evidence. Corrections use native signed genesis without an ordinary lane context, canonical catalog baselines, and typed lifecycle error propagation. Gate-runner tests pass 212 tests and 1,759 subtests; the next candidate includes three explicit split API/P2P listener controls. The signed archive owner has 35 passing macOS and Linux tests and a fresh live-reference observation for 36 inactive public binaries. Immutable native qualification, shipping artifacts, four-validator qualification, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. Public status and funding policy still return 502. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

Signed candidate `170f827a633f95cd5657bdf879dd21ad046f4a38` failed its independent native gate with 23 cases: 22 shared Torii publication fixtures and one split-listener assertion that omitted the already-required PoW revocation-store relocation. The three earlier lifecycle failures pass. Compilation took 599.4 seconds; the independent native phase completed in 1098.230 seconds. Shipping builds and the four-validator corridor did not run. Exact results are retained in `target/dpn-devex/current-native-fixture-results-170f827a-20260920.json`.

The correction adds a non-shipping Core publication owner for genuinely executed fixture blocks, with exact sealed wire and State ownership, actual captured witness and manifests, three valid votes from four deterministic BLS validators, durable native finality and witness sidecars, verified publication, then promotion. Missing witnesses, wrong State, altered wire and a different signed genesis roster are rejected; mint top-ups and epoch rotation remain outside this narrow helper. The five added controls are selected in basic/full release scopes. Independent source review and 44 Python gate controls pass; native execution is still pending. No production publication guard, compatibility decoder or empty production block was introduced.


### Focused publication validation and guest I/O interruption

Previous current-status paragraphs, preserved verbatim:

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `170f827a` compiled all 23 Linux native targets and passed 394 of 395 CLI regressions. Its 23 failures comprise one overly broad fixture assertion and 22 Torii fixtures that bypassed the native execution-output publication owner; all three previously failing lifecycle cases now pass. The fixture correction retains real captured witnesses, exact output wire, verified three-of-four finality, durable sidecars and the existing publication guards. Five new positive/negative controls are mandatory in both release scopes; 44 focused gate-runner tests pass, and a 28-case native diagnostic is running. This is not release qualification. The inactive-binary archive is still in progress; public status and funding policy still return 502. Immutable qualification, shipping artifacts, four-validator operation, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

Public Taira remains on source42 (`d085418a382e831875731144436c174d8621cdd6`); primary status and funding policy remain HTTP 502. Approved MacStadium access is restored and the nginx rotation/health repair is deployed. Validator1 is stale at height 1969; validators2–4 repeatedly loaded height 3598 and failed for an absent beacon session, so their loops are stopped. The selected snapshot already contains empty beacon stores. Current Kagami rejects retained genesis with a decoding length mismatch; current source deliberately changes required block-header/result layouts. No authenticated beacon-history projection or absence conclusion is available. Supported guest TRIM previously recovered about 3.1 GiB, but fresh inspection now finds only about 2.09 GB free on the Mac backing volume. Deployment capacity is insufficient; exact inactive public-binary archival and retirement remains under review. No ledger replacement, compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

The mutable Linux diagnostic passed all 28 exact selected regressions plus mandatory configuration in 1131.881 seconds. All five new Core controls and all 22 Torii fixtures passed. Evidence is `target/dpn-devex/execution-publication-focus-20260920.log`; immutable preparation remains required.

The read-only archive of 36 inactive public binaries failed with guest `pread` EIO at the final Kagami object: 35 complete objects and 110,100,480 of 132,083,440 bytes remain locally; no completion receipt exists and no binary was removed. Mac free space had declined to 118 MB, while known VM disk allocation moved only about 0.5 MB and swap stayed unchanged. Guest SSH subsequently closes before authentication and all four API ports refuse connections; Tart reports running. These observations do not establish filesystem corruption or its cause.

The independent eight-file frozen-log backup remained complete and pinned. Exact source logs were retired after fresh full hashes, ancestry/inode checks and root lsof observations. Their compressed backups remain held off host; observed Mac free space increased from 120,176,640 to 138,481,664 bytes during cleanup. The first helper attempt failed before any remote mutation due to a bootstrap NameError; the corrected second attempt completed all eight files. No validator ledger or current runtime files were modified.


### Verified archive continuation and bounded cleanup controllers

The retained public binary owner now resumes only a verified contiguous archive prefix, preserving completed objects and copying only missing bytes after fresh source identity/hash and partial-prefix validation. An explicit signed execution-controller commit is authenticated separately from the immutable same-format archive controller. Original archive metadata and payloads remain held across capacity probes and deletion. Binary cleanup charges its exact bounded metadata plus separate 32 MiB guest and Mac operating reserves; actual rounded metadata above 32 MiB is refused. Deployment reserves remain unchanged. All 56 binary custody/resume controls pass on Mac and native Linux.

The separate source owner admits one explicitly pinned inactive source85–93 through current native terminal records and canonical signed Git proof, validates the complete public payload and Git metadata closure, archives it off host, then quarantines and retires only admitted members through resumable batches. It retains separate 256 MiB guest and Mac reserves. All 31 source controls pass on Mac and Linux against the final common controller. Source85/86 local proofs and plan inputs are ready; no source archive or deletion has run. Neither controller permits ledger replacement or accepts an older schema. Evidence is `target/dpn-devex/retained-release-final56-tests-20260920.json` and `target/dpn-devex/source-retirement-85-86-20260920/source-common-final-integration.json`.


### Existing VM stopped for backing-volume recovery

Previous current-status paragraphs, preserved verbatim:

Public Taira remains on source42 (`d085418a382e831875731144436c174d8621cdd6`); primary status and funding policy remain HTTP 502. Its retained ledger cannot be recovered through current native genesis/beacon admission without new qualified inputs and an explicit replacement decision. During a read-only inactive-binary archive, the guest returned EIO and then began closing SSH before authentication; all four guest API ports refuse connections while the Mac and Tart process remain reachable. Mac free space declined outside the known VM allocation and unchanged swap. Eight fully backed-up frozen historical logs were retired with exact identity/hash and root reference checks, leaving about 138 MB free. No validator data, current configuration, compatibility decoder or empty production block was introduced. Recovery and measured capacity admission remain open. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `170f827a` compiled all 23 Linux native targets but failed 23 regressions. The reviewed correction now passes all 28 focused native cases: five genuine execution-publication controls, all 22 previously failing Torii fixtures and the split-listener correction, plus mandatory configuration. The helper retains actual witnesses, exact sealed output wire, verified three-of-four finality and durable publication; production guards remain intact. Forty-four gate-runner tests pass. This diagnostic does not qualify a release. The inactive-binary backup failed on its final 22 MB with a guest I/O error; its verified prefix is preserved. Immutable qualification, shipping artifacts, four-validator operation, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

The first existing-VM stop completed in 0.552 seconds; restarting its unchanged `user/501/org.sora.taira.retained-linux` service recovered SSH briefly. The guest then reported vda write EIO and ext4 unwritten-extent failures, and SSH failed again. The verified archive continuation exited before completion with BrokenPipe; no inactive binary was deleted. A second stop completed in 0.335 seconds and confirmed the VM stopped with its disk inode, configuration hash and launchd definition preserved. It remains stopped until Mac backing capacity can support recovery. No ledger replacement was attempted. Evidence: `target/dpn-devex/mac-existing-vm-recovery-20260920.log`, `guest-after-existing-vm-recovery-20260920.json`, and `mac-existing-vm-stop-for-capacity-20260920.log`.

Read-only census found no Tart cache or temporary images and only the named VM. The backing data volume `/dev/disk3s5` reports no APFS snapshots; no snapshot or VM image was removed. Five old upload fragments were proven redundant against a retained complete compressed binary, with exact retirement still pending verified backup and fresh root-reference checks. These observations do not establish that validator ledger bytes were damaged.

Signed candidate `818948b63b16a3f6252d3724804e5fee5e79f874` compiled its 23 native test targets in 561.1 seconds and passed all 1,030 independent selected tests across 18 selections. Its later independent shipping-codegen phase failed after 305.8 seconds with E0599: the unconditional Torii test utility calls Core's non-shipping execution-publication helper. The native check phase took 1401.380 seconds overall; preparation exited 1. The production-only fixture boundary is being corrected without enabling test authority in shipping Core. The failed candidate has no qualified shipping or four-validator result.


Previous separate current-status storage-controller paragraph, preserved verbatim before consolidating the current view:

The retained-public-binary owner supports verified prefix continuation and exact archive custody under a separate signed execution controller. Its bounded cleanup budget removes the circular free-space prerequisite without changing deployment reserves; 56 controls pass on Mac and Linux. A separate exact-source archive/quarantine owner passes 31 controls on both platforms and retains 256 MiB reserves. Neither owner authorizes deployment or ledger replacement. Live continuation and physical reclamation remain pending; see the [controller evidence](docs/history/2026-09-20/dpn-live-recovery.md).


### Production feature isolation and early shipping admission

Previous current-status paragraph, preserved verbatim:

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `818948b6` compiled all 23 Linux native targets and passed its independent selection of 1,030 tests. Its subsequent separate shipping build failed because Torii exposed a fixture helper whose Core implementation is correctly limited to tests. The production/test boundary correction is under focused validation; the candidate remains unqualified. Earlier focused validation passed all 28 publication/listener cases plus mandatory configuration, retaining real witnesses, exact sealed output wire, verified three-of-four finality and durable publication. The inactive-binary backup preserves its verified prefix with 22 MB remaining; a resume attempt stopped when guest SSH failed again. The signed binary continuation and exact source-retirement owners pass 56 and 31 controls respectively on Mac and Linux; their deployment reserves remain unchanged and physical reclamation is pending. Shipping artifacts, four-validator operation, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

Torii now exposes its execution-publication fixtures only under `cfg(test)` or its explicit `test-fixtures` feature, which forwards Core test support. A self dev-dependency enables the exact integration/benchmark consumers; default production features remain unchanged. The standalone Linux production library check passed in 255.45 seconds, and actual Cargo artifacts confirm both `iroha-core-tests` and Torii `test-fixtures` absent. A separate native focused invocation passed the faucet-policy discovery case and all four mandatory configuration cases. These are diagnostic checks, not immutable release qualification. Evidence: `target/dpn-devex/torii-fixture-feature-boundary-review-20260920.json` and `torii-fixture-feature-faucet-focus-20260920.log`.

The gate now checks the authoritative shipping binaries in a separate production Cargo metadata graph after mandatory configuration, ownership, recovery and startup controls but before CLI and long independent tests. It rejects Core or Torii fixture features even if accidentally enabled through a normal dependency; checkpoint reuse does not skip this check. All 213 Mac gate tests and 45 focused Linux controls pass. Actual shipping codegen, immutable artifact validation and four-validator execution remain required. Evidence: `target/dpn-devex/early-shipping-feature-guard-validation-20260920.json`.

Five redundant Mac upload objects were backed up privately and fully reread; their four-part stream exactly matches the retained complete compressed binary. Two guarded quarantine attempts failed before any namespace mutation. The second captured errno 28 (ENOSPC), despite about 120 MB reported free. An explicitly bounded recovery then removed only the first 22,000,000-byte redundant chunk after held backup/source hashes, original inode/ancestry, full-ready redundancy, root-reference observations and durable local intent. The remaining four sources and complete binary remain intact. Measured Mac free bytes rose from 125,153,280 to 147,132,416. No ledger or current Linux configuration changed. Evidence: `target/dpn-devex/public-upload-single-recovery-final-run-20260920.log`.


Previous current-host status, preserved verbatim:

Public Taira remains on source42 (`d085418a382e831875731144436c174d8621cdd6`); primary status and funding policy remain HTTP 502. Its retained ledger cannot pass current native genesis/beacon admission without qualified inputs and an explicit replacement decision. After an inactive-binary archive hit guest EIO, restarting the exact existing VM briefly restored SSH but exposed vda write failures and ext4 errors. The VM is now stopped under its existing launchd owner, with disk, configuration and service definition preserved. The Mac backing volume has about 122 MB free and no APFS snapshots or Tart cache to reclaim. Eight fully backed-up frozen logs were retired; further exact public-artifact recovery is pending. No ledger replacement, compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

Workspace formatting check reports pre-existing drift in `canonical_runtime_tests.rs`, `production_beacon_prepare.rs` and `external_software_signer.rs`; those sources were not changed in this feature-boundary checkpoint. The changed Torii library passes scoped rustfmt, and `git diff --check` passes. No full-workspace format pass is claimed.


### Restored host capacity and exact historical retirement

Previous current-status paragraphs, preserved verbatim:

Public Taira remains on source42 (`d085418a382e831875731144436c174d8621cdd6`); primary status and funding policy remain HTTP 502. Its retained ledger cannot pass current native genesis/beacon admission without qualified inputs and an explicit replacement decision. After an inactive-binary archive hit guest EIO, restarting the exact existing VM briefly restored SSH but exposed vda write failures and ext4 errors. The VM is now stopped under its existing launchd owner, with disk, configuration and service definition preserved. The Mac backing volume has about 147 MB free and no APFS snapshots or Tart cache to reclaim. Eight fully backed-up frozen logs and one verified redundant upload fragment were retired; broader public-artifact recovery is pending after APFS refused allocation with ENOSPC. No ledger replacement, compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `818948b6` passed 1,030 independent native tests, then failed standalone shipping codegen because Torii exposed a test-only Core helper. Torii now exports that fixture authority only to tests; the independent production check confirms both test features absent, and the native faucet-policy regression plus four configuration controls pass. A separate production metadata check now follows mandatory startup checks and precedes long tests, rejects either fixture feature, and reruns on checkpoint reuse; 213 Mac and 45 focused Linux gate controls pass. Actual shipping codegen and four-validator qualification remain mandatory. Binary continuation and exact source-retirement owners pass 56 and 31 controls on both platforms. The interrupted archive still lacks its final 22 MB. Qualified shipping artifacts, four-validator operation, concrete fresh-ledger inputs/approval and paid DPN deployment remain outstanding. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

All seven selected public upload pieces are backed up off-host and retired. The six-file completion binds both exact backup sets and the earlier single-chunk receipt. APFS reclamation was measured after descriptor close, including deferred recovery; no estimated bytes were credited. Eight obsolete USER LaunchAgent definitions were independently backed up, verified inactive and decommissioned. Their receipt is `4fee6d9e47c1874aaa08c98e4957c0a1c2ac759b15cca56fde743ada62156e79`. The unrelated old SYSTEM definitions were only read; their literal selector closure and absent supervisor were rechecked. The exact inactive r62 executable was then retired from a full verified backup, producing receipt `9a3b54316c99325ce95aca4d533846e5f00e697807aa318021ac5af79ac95d89` and measured Mac free space 645,103,616 bytes. The current Tart/nginx/Linux configuration and ledger were excluded.

The restart helper now compares the loaded launchd program, complete argument vector, working directory and TART_HOME against its pinned saved definition before kickstart. Four focused controls pass. The existing VM restarted with 641,880,064 measured free bytes, retaining its disk/configuration identity. SSH returned after 17.44 seconds; the four exact old validator units were stopped without changing their files. Both immediate and post-cleanup observations show writable root ext4, empty kernel error output and all four units inactive with MainPID zero and no queued jobs. Evidence: `target/dpn-devex/mac-existing-vm-capacity-restart-20260920-01.log`, `guest-recovery-quiesce-20260920-01.log` and `guest-after-binary-reclamation-20260920.json`. This does not claim every retained ledger byte has been verified.

The signed `9dd3ab818372b317e2540b6d480ffbdcbb6c32ed` binary owner resumed the exact missing 21,982,960 bytes and sealed all 36 files under original plan/admission authority. Archive completion is `e0ec0f9e859e629af8aeaa7a3a41e51ae71004ea14db63c33d0fdca96a6c3f3b`. Exact retirement completed with receipt `e0950ba3903b6d3d525432bc14793b1f2ba9fcbae352914eae919c050b899df6`; sync and TRIM both succeeded. Independent observations report 18,149,703,680 guest bytes and 5,405,270,016 Mac bytes available. Current source94/d085, original historical receipts and all private backup payloads remain preserved.

The subsequent source85 archive failed during read-only admission before creating an archive directory or mutating the guest. `taira_retained_source.inspect` incorrectly applied the new shipping producer’s delta-free `_validate_pack` grammar to an existing native-receipt-pinned Git pack. The exact source85 pack is 124,603,620 bytes with SHA256 `0878ad2668a488e4ec680dfdc353a62e922a68f3eda825e3adca9185cbb27d38`. The retirement-boundary correction must retain exact pack custody, closed metadata and canonical signed object/tree/content checks; it must not change new shipping import grammar or repack existing sources.

Immutable candidate `1d9f88c29ea76a241247185badbb75c5fd102571` compiled its 23 native test targets in 706.1 seconds and passed the earlier separate production metadata check in 301.8 seconds. The separate production shipping-codegen build then passed in 340.5 seconds. Its separate message-control fixture build and four-validator qualification remain open. No deployment qualification is claimed. A fresh public funding-policy probe still returned HTTP 502 after host recovery.

Independent read-only observation verified that the exact source85 pack contains 22,049 objects and 971 real Git deltas, with maximum depth six; the first delta uses REF_DELTA type 7. Git verification passed, and the held path/identity/full SHA256 stayed unchanged. This establishes a retirement-boundary mismatch rather than an inferred decompressor-offset defect. Evidence: `target/dpn-devex/source-retirement-85-86-20260920/source85-pack-delta-readonly-observation.json`.

The corrected retirement owner verifies the exact bounded retained pack SHA256, PACK v2 object count and SHA1 trailer, then retains all existing strict index/reverse-index, Git verification, canonical signed object/type/size/SHA256/tree, worktree and closed-metadata checks. New shipping source capture/import remains byte-identical to signed `9dd3` and still rejects deltas. All 39 source-custody controls pass on Mac (55.456 seconds) and native Colima Linux (6.879 seconds); all 15 unchanged shipping-source controls pass on Mac (16.717 seconds). The genuine Git delta fixture reproduces the prior shipping-validator refusal and now completes retained inspection, off-host archive verification and retirement. Transport/trailer/count/foreign-object/canonical-metadata/index corruption controls remain strict. Evidence: `target/dpn-devex/source-retirement-85-86-20260920/retained-pack-boundary-validation-20260920.json`. Live source retry still requires a newly signed controller and fresh private outputs.


### Immutable native handoff failure and verified source retirement

Previous current-status paragraphs, preserved verbatim:

Public Taira remains bound to source42 (`d085418a382e831875731144436c174d8621cdd6`); funding policy still returns HTTP 502. The existing MacStadium VM has recovered after exact backed-up public-artifact cleanup. It is running with its original disk/configuration, all four old validators are quiesced with unchanged units, and fresh root-filesystem observations show writable ext4 and no kernel errors. Binary retirement and successful sync/TRIM leave about 5.4 GB measured Mac space and 18.1 GB guest space. Current ledger and runtime data remain preserved. Further inactive-source cleanup, qualified fresh inputs and an explicit shared-ledger replacement decision remain outstanding. No compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `1d9f88c2` isolates Torii fixture authority from production and adds an earlier separate production metadata gate; that immutable Linux check and the separate shipping-codegen build now pass, while four-validator qualification remains open. The gate controls pass 213 Mac and 45 focused Linux cases. The interrupted 36-binary archive is now complete, verified and used for exact inactive-file retirement; its controller passes 56 controls on both platforms. The first source85 archive refused before any remote mutation because the retirement owner reused the shipping producer’s delta-free Git pack validator. The retirement boundary now verifies the exact existing pack and signed object closure; 39 source-custody tests pass on Mac and Linux, and all 15 unchanged shipping-capture controls pass. Live source-archive retry remains pending; shipping import policy remains unchanged. Qualified artifacts, fresh-ledger inputs/approval and paid DPN deployment remain outstanding. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

Immutable candidate `1d9f88c29ea76a241247185badbb75c5fd102571` passed all 1,030 independent native selections, separate production metadata validation and shipping code generation. The message-control fixture binary also built successfully. The four-validator production epoch fixture then failed after 79.7 seconds: onboarding and faucet submission succeeded, but final-canary submission returned HTTP 503 `queue_plan_admission_capacity_unavailable` during peer0’s height-three successor admission handoff. Public peer logs show no earlier actor exit or panic; shutdown follows test failure and is cleanup. This is not qualified release evidence. The diagnostic record is `target/dpn-devex/native-admission-handoff-failure-1d9f88c2-20260920.json`.

Signed controller `34e2847b930cc39bf8a15e0f019d38b50589a9c2` completed source85 off-host archive and exact inactive-source retirement. Archive receipt `41a43658af8b91b7d1e799d211bf9aae5a6ea1db3ec94ff3d1608269ac797e1a` binds 621,832,051 payload bytes. Retirement receipt `3cc101d6f858a21ba44724f4a4aad63254074f72fecc99d7328f20ea2b70d3ad` records 681,910,272 allocated bytes removed and successful sync/TRIM; its final observations show 18,823,954,432 guest bytes and 4,073,074,688 Mac bytes available. No physical reclamation is inferred from logical deletion. Source86 archive also passed independent full verification: receipt `7b86ad88505d81b319dacee345a11a5a1a372d712faeb3ee3e050ebeeb9be2c9` binds 621,892,034 payload bytes; retirement is still running.

Fresh predecessor observations confirm the four old validators remain stopped with unchanged state-directory identities, public daemon/genesis/unit pins, SSH guards and active nginx. Private configuration was inspected by metadata only. The host-global lease/progress joins the sealed current historical terminal, but two older live-name journals remain; no blanket ownership absence is asserted. About 2 GB of Mac capacity loss outside the observed VM allocation remains unexplained after bounded checks exclude VM growth, unchanged swap, selected logs, core files, crash reports and APFS snapshots. Evidence: `target/dpn-devex/fresh-predecessor-20260920/summary.json` and `target/dpn-devex/mac-capacity-loss-diagnosis-summary-20260920.json`.

Source86 exact retirement then completed with receipt `521a0f28f8ba51c80af92ba91e31eabcacf31e2f88ff3b0d2f1029dbc438fd8a`. It removed 681,979,904 allocated bytes from 22,473 admitted entries; sync/TRIM succeeded, with measured final free space 19,498,496,000 guest bytes and 4,740,829,184 Mac bytes. Signed read-only inspections also admitted inactive source87 and source88 with combined 1,251,353,009 payload bytes and 1,372,655,616 allocated bytes; neither inspection claims reclamation. Source87 off-host archiving is in progress. The source85/86 retirement paths preserve current source94/d085, original receipts, private runtime configuration and ledger state.

Previous host-status paragraph, preserved verbatim:

Public Taira remains bound to source42 (`d085418a382e831875731144436c174d8621cdd6`); funding policy still returns HTTP 502. The existing MacStadium VM is running with its original disk/configuration, four old validators stopped under unchanged units, writable ext4 and no observed kernel errors. Binary and source85 retirement preserved all current runtime/ledger data and verified off-host backups. Fresh observations show about 4.07 GB Mac and 18.82 GB guest space; a later capacity loss outside the VM allocation is still unexplained. Source86 retirement, qualified fresh inputs and an explicit shared-ledger replacement decision remain outstanding. No compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).


Source87 exact off-host archiving and retirement completed under signed controller `34e2847b930cc39bf8a15e0f019d38b50589a9c2`. Archive receipt `1c4efd54db1f0b6a99d156bf662fbcfe169fb62d5ace1e1611e4ffe54d5d026a` binds 625,646,545 payload bytes with SHA256 `11529f110b55ffdd1268b69f93da49c96d1ae64f371f1db47b77e0b651e46902`; maintained and independent verification passed. Retirement receipt `7a46ccd67ac256591add2c09e19facb287b180e0a48ce924a6be90746401a48e` records 686,297,088 allocated bytes removed, successful sync/TRIM, and measured free space of 20,177,170,432 guest bytes and 5,404,672,000 Mac bytes. Source88 archive also completed and passed maintained verification: receipt `babfd873bace6c64df0fbbac1f4182888ef2724f3d540c4f11aeddd27b24080f` binds the exact admission, signed proof and payload SHA256 `8812fe11f26b0d2b15fc211d1f88ad9857d83fb43ae636ecd5dbf70e7987b1be`. Its retirement is still running; no reclamation is claimed yet.

The admission-handoff runner now selects 18 exact helper/runtime/regression cases early in both basic and full scopes. Broader gate controls exposed stale synthetic libtest executables that lacked canonical serial-batch output, a checkpoint fixture tied to a brittle snapshot-call count, and Linux temporary directories whose inherited group-write mode correctly failed custody checks. The fixtures now model real libtest output and the actual collection lifecycle, explicitly protect their own disposable directories, and use a private HOME-descendant temporary root for Linux custody tests. The production ancestry and custody guards are unchanged. The paired 244-case suite passes on Mac (no skips) and Linux (two Darwin-only skips), with unchanged source hashes: `target/dpn-devex/admission-handoff-gate-mac-custody-20260920.json` and `target/dpn-devex/admission-handoff-gate-linux-20260920-04.json`.

The first focused native admission-handoff run passed all four wait-helper cases, seven existing runtime regressions and mandatory configuration, but all seven new higher-level cases failed while constructing a one-validator fixture (`RosterTooSmall`). The corrected fixtures use four actual BLS/PoP authorities and two distinct post-quorum receipts, preserving production roster requirements. Their focused native rerun remains pending; passing source-contract controls do not establish runtime or release qualification.


Previous current-status paragraphs, preserved verbatim:

Public Taira remains bound to source42 (`d085418a382e831875731144436c174d8621cdd6`); funding policy still returns HTTP 502. The existing MacStadium VM is running with its original disk/configuration, four old validators stopped under unchanged units, writable ext4 and no observed kernel errors. Binary and source85/86 retirement preserved all current runtime/ledger data and verified off-host backups. Fresh observations show about 4.74 GB Mac and 19.50 GB guest space; a later capacity loss outside the VM allocation is still unexplained. Further admitted inactive-source cleanup, qualified fresh inputs and an explicit shared-ledger replacement decision remain outstanding. No compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).

Current DPN DevEx work uses only `/Users/takemiyamakoto/dev/iroha` on `optimizations`. Signed candidate `1d9f88c2` passed 1,030 independent native tests, the separate production metadata check and shipping/fixture code generation. Its four-validator epoch fixture failed when final-canary admission encountered a closed successor owner after onboarding and funding; the bounded production admission path is being corrected without bypassing ownership or renewing requests. The gate controls pass 213 Mac and 45 focused Linux cases. The retained-source owner passes 39 custody controls on both platforms, with all 15 unchanged shipping-source controls passing; source85 retirement is complete and source86 backup is verified. Qualified artifacts, fresh-ledger inputs/approval and paid DPN deployment remain outstanding. See the [incident and validation record](docs/history/2026-09-20/dpn-live-recovery.md).

Source88 retirement completed with receipt `1efb0d1526d187099fc4f4414173781230a91ea00827daeefdc32e742311a802`. It records 686,358,528 allocated bytes removed from 22,718 admitted entries, successful sync/TRIM, and measured free space of 20,855,869,440 guest bytes and 6,094,348,288 Mac bytes. Current ledger/configuration/history remain preserved. Inactive source89 metadata and canonical signed proof are prepared read-only as a capacity contingency; no source89 archive or retirement has run. Actual qualified transfer capacity remains an independent admission.


### Native controller transition and admission qualification candidate

The completed source review covers a bounded Torii owner wait, exact transaction uncertainty across original-clock/wire expiry and bounded-attempt construction, and a reversible native dispatcher transition with a native plan producer. The transition preserves the sealed historical lease/progress and independently bound current runtime while replacing exactly the dispatcher and five guards; staged publication, crash continuation, rollback, inode aliases and changed predecessor refusal have dedicated controls. Its current-runtime input preserves the update118 source42 daemon/unit binding separately from the sealed94 configuration/controller identity. No old launcher or historical inventory decoder was added. Native runtime validation is pending; twenty admission and twenty-one dispatcher controls are now mandatory in both maintained release scopes.

The second focused admission attempt passed four corrected higher-level cases but failed three test assumptions: two direct invocations lacked the caller-owned memory reservation their assertions expected, and one expected an internal deadline reason that the public envelope intentionally omits. The fixtures now supply a real pre-admitted reservation and assert public uncertainty code and exact transaction identity. Two added regressions retain a real prior journal claim across initial deadline and post-wait aggregation refusal, with zero dispatch. Final source-contract validation passes all 62 semantic/mutation controls and six source-asset grammar controls. These results do not substitute for native execution.

Prepared transfer now carries all four public qualification records in the same closed import as the four executables and signed source. Exact `preparation/result.json`, `request.json`, `checks.json` and completed-attempt `capture.json` bytes are bounded, hash-pinned, read-only, rechecked on publication/reuse and included in capacity accounting. The native transition derives its proof pins directly from that completed import, eliminating a separate manual proof-copy step. All twenty transfer tests pass on Mac and Linux, including genuine signed Git import/reuse, proof corruption and incomplete six-row rejection. Paired runner suites pass 244 cases on each platform (Linux skips two Darwin-only clone cases); their source hashes stayed unchanged. Evidence is `target/dpn-devex/transfer-preparation-proof-independent-evidence-20260920.json` and `admission-handoff-gate-{mac,linux}-final-20260920-05.json`.

Shared checkout commits advanced during final preparation. External changes and the index were preserved; no process was interrupted. Required native validation will use an explicitly signed Git-object capture, which excludes later HEAD or working-tree changes. The mutable combined diagnostic did not launch because its observed HEAD pin changed before invocation. The new transition modules satisfy source-size limits without exceptions; the repository-wide budget report still contains pre-existing oversized-file findings and is not claimed as a passing gate.

Source89 off-host archive completed and passed maintained verification with receipt `50d322a92f979e57266bac60f6aba93b9b99a13b15a1fe899eab50293ae45a49`, binding 625,723,212 payload bytes under the same signed retirement controller. Exact retirement is running; no source89 reclamation is claimed yet. Public funding remains HTTP 502, and no dispatcher transition, shared-ledger replacement or paid DPN deployment has run.


Signed candidate `5c635dd28e21e4d7f6eea696444252fcb7ac3afa` stopped before compilation when the cached old source lacked newly tracked `crates/iroha_cli/DISPATCHER_TRANSITION.md`. The different-commit comparison caught only the domain error, while a newly added path raises `FileNotFoundError` and a file/directory replacement may raise `NotADirectoryError`. The correction classifies only those structural mismatches for refresh, then fully authenticates the previous recorded snapshot before creating or replacing anything. Same-commit corruption, permission errors and I/O failures remain fatal. All 108 preparation tests pass on Mac and Linux; three new cases cover eleven path-evolution/corruption subcases, timestamp and warm-artifact retention. The fixture stubs the signature decision and does not claim new cryptographic qualification. Independent review is clear; evidence is `target/dpn-devex/taira-source-refresh-review-20260920.json`. No Cargo ran and no source capture was manually removed.

Source89 retirement completed with receipt `4fcf79781fdc00921ef51376fd80a64400c823c769455f3c08acec8060c1c518`, removing 686,379,008 allocated bytes from 22,718 admitted entries with successful sync/TRIM. The guest reports 21,526,200,320 free bytes; the Mac reports 4,551,950,336 after another roughly 2.2 GB capacity decrease before retirement. Fresh observations confirm unchanged swap and a smaller VM disk allocation; these inode observations alone do not attribute APFS physical extent use. Bounded metadata-only totals identify about 258.17 GB under `/opt/homebrew/var/postgresql@16`, without reading database/configuration/credential contents or modifying other services. This is a size observation, not proof of the loss's cause. Evidence is `target/dpn-devex/mac-capacity-loss-after-source89-20260920.json` and `mac-capacity-service-totals-20260920-01.json`.

Previous host-status paragraph, preserved verbatim:

Public Taira remains bound to source42 (`d085418a382e831875731144436c174d8621cdd6`); a fresh funding-policy probe returns HTTP 502. The existing MacStadium VM is running with its original disk/configuration, four old validators stopped under unchanged units, writable ext4 and no observed kernel errors. Binary and source85–88 retirement preserved all current runtime/ledger data and verified off-host backups. Source88 completion measured 6.09 GB Mac and 20.86 GB guest space. An earlier capacity loss outside the VM allocation remains unexplained; transfer still requires its exact artifact budget. Qualified fresh inputs and an explicit shared-ledger replacement decision remain outstanding. No compatibility decoder or empty production block was introduced. See the [incident record](docs/history/2026-09-20/dpn-live-recovery.md).
