# Roadmap (Open Work Only)

This roadmap enumerates the outstanding efforts required to ship the optional
NPoS Sumeragi mode and keep the broader Nexus transition on track. For every task listed here we are preparing the first public release, so teams can design and implement with a clean slate. Completed
items continue to live in `status.md`; only tasks that still need engineering
work appear here.
SDK parity note: Swift offline receipt vectors are now synced to the canonical artifacts; no new SDK open items were added.
Latest sync (sandbox serialized-network drop flake hardening, 2026-02-28): stabilized `integration_tests/src/sandbox.rs` test `serialized_network_drop_completes_on_current_thread_runtime` by adding an explicit peer startup timeout override (`with_peer_startup_timeout(Duration::from_secs(480))`). This test targets `SerializedNetwork` drop behavior under a current-thread runtime; on loaded hosts startup can exceed the generic 240s floor and fail before reaching the drop assertion. Validation: `cargo fmt --all` (ok), `cargo test -p integration_tests serialized_network_drop_completes_on_current_thread_runtime -- --nocapture` (ok; target test passed, observed runtime `862.22s`). Remaining open for this tranche: none.
Latest sync (Sumeragi adversarial RBC flake hardening, 2026-02-27): stabilized `integration_tests/tests/sumeragi_adversarial.rs` checks for `sumeragi_adversarial_chunk_drop` and `sumeragi_adversarial_locked_qc_gate_rejects_conflicting_proposal` by switching from single-snapshot `delivered` gating to post-wait, height-wide session delivery evaluation plus committed-height confirmation. Added helper regression coverage (`delivered_height_check_scans_all_sessions_for_the_height`). Validation: `cargo fmt --all` (ok), `cargo test -p integration_tests --test sumeragi_adversarial -- --nocapture` (ok; `12 passed`, `0 failed`, `623.31s`). Remaining open for this tranche: none.
Latest sync (Nexus autoscale repeated-cycle strict retry hardening, 2026-02-27): stabilized `integration_tests/tests/nexus/autoscale_localnet.rs` repeated-cycle localnet coverage by adding bounded per-cycle retries (`AUTOSCALE_MULTI_CYCLE_RETRY_LIMIT=2`) with attempt-scaled strict load (`96 -> 192`) in `nexus_autoscale_repeats_expand_contract_cycles_in_localnet`. Strict deterministic transition assertions remain enabled on each attempt; retries only absorb first-attempt strict expansion misses observed under localnet startup contention. Validation: `cargo fmt --all` (ok), `cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok; `28 passed`), `cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture --test-threads=1` (ok; `1 passed`, `224.57s`). Remaining open for this tranche: monitor nightly soak/repeated-cycle stability; no additional code changes queued.
Latest sync (Nexus cross-dataspace startup retry stability, 2026-02-27): fixed stale startup-run cleanup in `crates/iroha_test_network/src/lib.rs` by changing `Network::shutdown()` to invoke `shutdown_if_started()` for all peers (not only peers with `is_running=true`). This closes a retry panic where a peer could exit during startup, flip `is_running=false`, keep a stale `run` handle, and then panic on next startup attempt with `already running`. Added regression test `network_shutdown_clears_stale_peer_runs_even_when_not_marked_running` and revalidated the original failing scenario: `cargo test -p iroha_test_network network_shutdown_clears_stale_peer_runs_even_when_not_marked_running -- --nocapture` (ok) and `cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (ok, `296.26s`). Remaining open for this tranche: none.
Latest sync (SORA parliament JIT/no-council completion + VRF epoch-seed syscall, 2026-02-26): completed the planned parliament hardening in `iroha_core` by binding approvals to proposal-time multibody sortition snapshots (selection epoch, beacon, roster commitment) in parliament mode and removing council-state dependency for those approvals; stage quorum checks now use proposal-bound epochs when snapshots exist. Added bounded bond-tier ticket weighting in governance citizen draws and maintained hostile/Sybil coverage. Implemented ABI syscall `VRF_EPOCH_SEED (0x7E)` with shared Norito envelopes (`ivm::vrf::{VrfEpochSeedRequest,VrfEpochSeedResponse}`), world-snapshot hydration in CoreHost setup paths, host tests for found/missing/latest-fallback behavior, and synced ABI/docs artifacts (`ivm` spec/docs/generated table + golden syscall list). Validation: `cargo fmt --all`, `cargo run -p ivm --bin gen_syscalls_doc -- --write`, `cargo test -p ivm --test abi_syscall_list_golden --test syscalls_doc_sync`, `cargo test -p iroha_core --lib vrf_epoch_seed_syscall_`, and targeted governance suites (`gov_council_gate`, `gov_pipeline_sla`, `gov_propose_validation`, `gov_enact_deploy`, `gov_parliament_adversarial_sybil`) all passed. Remaining open for this tranche: none.
Latest sync (Sumeragi DA Kura-eviction test flake fix, 2026-02-26): stabilized `integration_tests/tests/sumeragi_da.rs` scenario `sumeragi_da_kura_eviction_rehydrates_from_da_store` by replacing per-iteration network-wide height synchronization with a timeout-bounded wait on the inspected peer (`peer.once_block(expected_height)`). This aligns synchronization with the scenario's verification target (single-peer Kura eviction + DA-store rehydration) and prevents unrelated peer lag from triggering false `Network overall height did not pass given predicate` failures. Validation: `cargo fmt --all` (ok), `CARGO_TARGET_DIR=/tmp/iroha-codex-fix-target IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test sumeragi_da sumeragi_da_kura_eviction_rehydrates_from_da_store -- --nocapture --test-threads=1` (ok), `CARGO_TARGET_DIR=/tmp/iroha-codex-fix-target IROHA_TEST_NETWORK_PARALLELISM=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test sumeragi_da sumeragi_da_ -- --nocapture --test-threads=1` (ok). Remaining open for this tranche: none.
Latest sync (Offline allowance/App Attest gap closure, 2026-02-27): closed the remaining follow-up gaps in `crates/iroha_core/src/smartcontracts/isi/offline.rs` by adding dual App Attest nonce-extension decoding compatibility (Apple production DER wrapper + legacy plain OCTET STRING), adding fixed DER fixture decoding and legacy-encoding verification tests, introducing registration-time reallocation logic so certificate reprovision/reissue can proceed without mandatory balance change when sufficient allowance is already reserved on-ledger (shortfall-only reserve remains enforced), and adding deterministic newest-first reallocation regression coverage to lock consensus behavior. Validation now includes integration-level offline admission checks. Updated `docs/source/offline_allowance.md` to document nonce-format compatibility. Validation: `cargo fmt --all` (ok), `cargo test -p iroha_core attestation::tests:: -- --nocapture` (ok; `18 passed`, `0 failed`), `cargo test -p iroha_core register_allowance_reissue_ -- --nocapture` (ok; `2 passed`, `0 failed`), `cargo test -p iroha_core register_allowance_ -- --nocapture` (ok; `11 passed`, `0 failed`), `cargo test -p integration_tests --test offline_allowance_security -- --nocapture` (ok; `3 passed`, `0 failed`). Remaining open for this tranche: none.
Latest sync (Offline lineage/build-claim fixture gap closure, 2026-02-27): completed follow-up fixture/test alignment for enforced certificate lineage and build claims across core and Torii by updating register/reissue/renew test certificates to carry valid lineage metadata chains, isolating future-issued rejection coverage from lineage collisions, and updating Torii offline app/certificate API fixtures to include valid lineage drafts plus signed Apple build claims with required iOS metadata bindings. Validation: `cargo fmt --all` (ok), `cargo test -p iroha_core register_allowance_ -- --nocapture` (ok; `11 passed`, `0 failed`), `cargo test -p iroha_core submit_transfer_ -- --nocapture` (ok; `8 passed`, `0 failed`), `cargo test -p iroha_torii --test offline_certificates_app_api -- --nocapture` (ok; `5 passed`, `0 failed`), `cargo test -p iroha_torii offline_ -- --nocapture` (ok). Remaining open for this tranche: none.
Latest sync (2026-02-26 post-policy reruns complete): reran unchanged-shape 1200s/3600s gates after the near-quorum backlog-threshold and aggregate-mismatch retry refinements. Artifacts: pilot `/tmp/izanami_pilot_1200_gate_20260226T191033Z.log` + `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_8QiS6c` (`successes=2971 failures=1`, strict `630`, forced-VC `1`, fallback-anchor `19`); acceptance `/tmp/izanami_acceptance_3600_gate_20260226T191033Z.log` + `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_PYVD6w` (`successes=7473 failures=4`, strict `1535`, forced-VC `1` with one 6s-class event, fallback-anchor `48`). No progress-timeout failure occurred. Remaining open: recover the observed throughput/height regression versus the earlier 2026-02-26 recovery baselines while reducing fallback-anchor churn under the same fixed `--tps 5 --max-inflight 8` envelope.
Latest sync (2026-02-26 bottleneck-policy follow-up implemented): completed the internal liveness refinements for the top stall/churn bottlenecks in `iroha_core` without FSM/public-config changes: near-quorum reduced-timeout gates now use thresholded queue backlog semantics (`main_loop.rs`, `reschedule.rs`, `propose.rs`), forced-VC pending-stall path now uses soft-limit RBC backlog gating for near-quorum reduction, QC missing-payload fast recovery prioritizes fetch/range-pull before vote churn (`main_loop/qc.rs`), and block-sync `AggregateMismatch` QC failures now enter retryable quarantine/replay instead of immediate final-drop (`main_loop/block_sync.rs`). Focused validation is green (`cargo fmt --all` plus targeted `iroha_core` tests for forced-VC near-quorum gating, reschedule suppression semantics, QC fast-recovery cooldown, cached-slot behavior, and aggregate-mismatch retry classification). Remaining open in this roadmap area: rerun unchanged-shape 1200s/3600s gates and verify that these policy shifts reduce payload-missing/fallback churn while preserving the previously improved 6s-class timeout behavior.
Latest sync (gate A/B reruns executed, 2026-02-26): completed the planned unchanged-shape reruns with canonical envelope (`--allow-net --nexus --peers 4 --faulty 0 --duration 1200s/3600s --target-blocks 1200/3600 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable`). Pilot artifacts: `/tmp/izanami_pilot_1200_gate_20260226T145420Z.log` + `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_L8nEAl` (`successes=3073 failures=1 ingress_failover_total=0 ingress_endpoint_unhealthy_total=0`, strict persisted height `651`). Acceptance artifacts: `/tmp/izanami_acceptance_3600_gate_20260226T162742Z.log` + `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_KJGDpZ` (`successes=7453 failures=3 ingress_failover_total=8 ingress_endpoint_unhealthy_total=1`, strict persisted height `1537`). Compared to the latest 2026-02-26 recovery baseline (`3101/7554` successes, strict `652/1555`), throughput dipped modestly while safety/liveness guardrails held: no progress-timeout failures, no `no proposal observed before cutoff` increase, forced-VC warnings decreased (`pilot 2->1`, `acceptance 2->0`), and 6s-class forced-VC events reduced (`acceptance 2->0`). Remaining open: reduce payload-missing/fallback churn (`stake quorum observed but block payload missing` and fallback-anchor share warnings both rose slightly) while recovering the lost `-1` pilot / `-18` acceptance strict-height gap versus the latest baseline.
Latest sync (2026-02-26 policy-only liveness refinement complete): implemented the DA/commit-path stall-reduction changes in `crates/iroha_core/src/sumeragi/main_loop/{qc.rs,propose.rs,reschedule.rs,tests.rs}`, `crates/iroha_core/src/sumeragi/main_loop.rs`, `crates/iroha_core/src/sumeragi/main_loop/mode.rs`, and `crates/iroha_core/src/block_sync.rs` (plus shared-host profile alignment already maintained in `crates/izanami/src/chaos.rs`) without public CLI/config/FSM changes. Focused validation is green (`cargo test -p iroha_core main_loop -- --nocapture`, `cargo test -p iroha_core --lib should_share_unknown_prev_hash_tracks_peer_and_height -- --nocapture`, `cargo test -p izanami shared_host_recovery_profile_ -- --nocapture`, `cargo test -p izanami shared_host_stable_soak_profile_ -- --nocapture`, `cargo fmt --all`). Remaining open for this roadmap area: run unchanged-shape 1200s/3600s gate A/B reruns against the 2026-02-26 baselines and confirm reduced 6s timeout-bound stalls/fallback-anchor churn with no progress-timeout or view-change-storm regression.
Latest sync (hotfix reruns executed, 2026-02-26): reverted the regression caused by the previous consensus timeout patch by (1) restoring shared-host DA multipliers in `crates/izanami/src/chaos.rs` (`quorum_timeout_multiplier=4`, `availability_timeout_multiplier=3`) and (2) narrowing cached-slot timeout forcing in `crates/iroha_core/src/sumeragi/main_loop/propose.rs` so slots with observed precommit votes no longer trigger forced view-change. Validation: `cargo fmt --all` (ok), `cargo test -p izanami shared_host_recovery_profile_ -- --nocapture` (ok), `cargo test -p izanami shared_host_stable_soak_profile_ -- --nocapture` (ok), and focused `iroha_core` pacemaker/reschedule/QC regressions (all ok). Gate reruns with unchanged shape (`--tps 5 --max-inflight 8 --workload-profile stable`) show recovery: pilot `/tmp/izanami_pilot_1200_gate_20260226T105101Z.log` + `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_tLZ5Yo` reached persisted `651/651/652/652` with summary `successes=3101 failures=1 ingress_failover_total=0 ingress_endpoint_unhealthy_total=0`; acceptance `/tmp/izanami_acceptance_3600_gate_20260226T111739Z.log` + `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_4pyRKc` recovered from the prior 600s progress-timeout failure and finished at persisted `1554/1554/1554/1554` with summary `successes=7554 failures=12 ingress_failover_total=8 ingress_endpoint_unhealthy_total=2`. Compared to the bad run (`295` pilot quorum candidate, `508` acceptance), liveness/throughput is restored; compared to baseline (`633` pilot, `1554` acceptance), acceptance pace is effectively back to baseline and pilot is slightly improved.
Latest sync (post-patch pilot + acceptance gate reruns executed, 2026-02-26): reran both gates unchanged (`--tps 5 --max-inflight 8 --workload-profile stable`) after the consensus stall-timeout patch and observed severe regression. Pilot artifacts: `/tmp/izanami_pilot_1200_gate_20260226T082333Z.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_riNQf3`; outcome: `target blocks not reached`, summary `successes=954 failures=66 ingress_failover_total=238 ingress_endpoint_unhealthy_total=100`, with persisted per-peer state showing split (`295/296/77/296`, strict candidate `77`, quorum candidate `295`). Acceptance artifacts: `/tmp/izanami_acceptance_3600_gate_20260226T085029Z.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_ajnB2Y`; outcome: `no block height progress for 600s (quorum min 508, strict min 508, target 3600)`, summary `successes=2387 failures=30 ingress_failover_total=160 ingress_endpoint_unhealthy_total=121`. Compared to the 2026-02-26 baseline gates (`633` pilot, `1554` acceptance), throughput regressed materially (`pilot quorum candidate 295`, `acceptance 508`), and churn shifted toward proposal starvation (`no proposal observed before cutoff` and cached-slot stalls increased sharply). The only positive signal was fewer 6s timeout-boundary age events; this did not translate to liveness or pace uplift. Remaining open: roll back or narrow the near-quorum timeout behavior and re-run A/B with identical gate shape before further tuning.
Latest sync (consensus stall-budget removal patch finalized, 2026-02-26): implemented the internal-only throughput/convergence tuning in `crates/iroha_core/src/sumeragi/main_loop/{qc.rs,reschedule.rs,propose.rs,votes.rs}` and `crates/izanami/src/chaos.rs` with no public CLI/config schema changes. The patch adds fast cooldown-gated payload-missing QC recovery, near-quorum reduced timeout gating (`clamp(2 * rebroadcast_cooldown, 400ms..2000ms)`) for stale reschedule and cached-slot stall paths (suppressed under backlog or legitimate RBC in-flight progress), and tunes balanced shared-host DA multipliers to `2/2` (quorum/availability) while keeping `tps=5`, `max_inflight=8`, and pipeline floor unchanged. Validation: `cargo fmt --all` (ok), `cargo test -p iroha_core main_loop -- --nocapture` (ok; `1289 passed`, `0 failed`), `cargo test -p izanami shared_host_recovery_profile_ -- --nocapture` (ok), `cargo test -p izanami shared_host_stable_soak_profile_ -- --nocapture` (ok), `cargo test -p iroha_core --lib should_share_unknown_prev_hash_tracks_peer_and_height -- --nocapture` (ok). Remaining open for this roadmap item: rerun the unchanged 1200s/3600s gates and compare against the 2026-02-26 baseline artifacts to confirm reduced timeout-bound churn and improved sustained block pace.
Latest sync (pilot + acceptance gate reruns executed, 2026-02-26): reran both gates unchanged (`--tps 5 --max-inflight 8 --workload-profile stable`). Pilot artifacts: `/tmp/izanami_pilot_1200_gate_20260226T040832Z.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_XKwAsf`; outcome: `target blocks not reached`, final height `633/1200`, strict/quorum max divergence `2`, summary `successes=3023 failures=4 ingress_failover_total=0 ingress_endpoint_unhealthy_total=0` (includes `repeat_trigger_0`-not-found submission failures). Acceptance artifacts: `/tmp/izanami_acceptance_3600_gate_20260226T043457Z.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_a4a4B0`; outcome: `target blocks not reached`, final height `1554/3600`, strict/quorum max divergence `1`, summary `successes=7592 failures=1 ingress_failover_total=0 ingress_endpoint_unhealthy_total=0`. Versus the prior 2026-02-25 scopefix baseline, pilot regressed by `-17` blocks while acceptance improved by `+8` blocks; acceptance progress windows averaged `4.33` blocks/10s (required `10`), so the open blocker remains throughput (not consensus split), with sustained underproduction at `43.17%` of target block pace.
Latest sync (plan implementation finalized, 2026-02-25): completed the scoped `crates/izanami/src/chaos.rs` patch with no public CLI/config changes: pilot+soak shared-host recovery selection now keys off `is_shared_host_stable_recovery_run(...)` (`nexus`, stable profile, `faulty_peers=0`, `peer_count>=4`, `duration>=1200s`), load-shape pinning remains soak-only (`duration>=3600s`), submit-permit scope is restricted to submit RPC/retry only, and deterministic submit backlog is bounded by `max_inflight * IZANAMI_SUBMISSION_BACKLOG_MULTIPLIER`. Shared-host baseline constants were kept unchanged (`tps=5`, `max_inflight=8`, `pipeline_time_ms=900`), and config-layer regression coverage was added (`shared_host_recovery_profile_config_layer_writes_tuned_da_timeouts`). Validation: `cargo fmt --all` (ok), `cargo test -p izanami shared_host_stable_soak_profile_ -- --nocapture` (ok), `cargo test -p izanami shared_host_recovery_profile_ -- --nocapture` (ok), `cargo test -p izanami submission_ -- --nocapture` (ok), `cargo test -p iroha_core --lib should_share_unknown_prev_hash_tracks_peer_and_height -- --nocapture` (ok); formal check remains environment-blocked (`bash ci/check_sumeragi_formal.sh`: `apalache-mc` missing, Docker daemon unavailable). Remaining open for this area is unchanged: 3600s acceptance throughput is still under target and needs a one-variable-at-a-time consensus-throughput tuning follow-up with fresh artifacts.
Latest sync (SORA hostile-governance adversarial coverage + 4-peer localnet closure, 2026-02-26): completed the hostile-governance tranche across core + integration and closed all remaining items. Added six adversarial Sybil/sortition tests in `crates/iroha_core/tests/gov_parliament_adversarial_sybil.rs` and hostile blocked/captured governance scenarios in `integration_tests/tests/sora_parliament_lifecycle_smoke.rs`; captured-hostile path now models full committee capture under multibody sortition quorum, with explicit quorum-derived guard checks (`HOSTILE_PARLIAMENT_QUORUM_BPS`, `hostile_required_approvals()`) and dead-code warning suppression for the shared runtime helper import. Validation: `cargo fmt --all` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_sybil_core cargo test -p iroha_core --test gov_parliament_adversarial_sybil --no-run` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_sybil_core cargo test -p iroha_core --test gov_parliament_adversarial_sybil` (ok; `6 passed`), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_sybil_integration_live cargo test -p integration_tests --test sora_parliament_lifecycle_smoke --no-run` (ok), targeted captured-hostile run (ok; `1 passed`, `270.02s`), and full integration file run `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_sybil_integration_live cargo test -p integration_tests --test sora_parliament_lifecycle_smoke -- --nocapture --test-threads=1` (ok; `3 passed`, `609.02s`). Remaining open for this tranche: none.
Latest sync (Time-trigger scenario stabilization, 2026-02-26): hardened `integration_tests/tests/triggers/time_trigger.rs` to reduce `triggers::time_trigger::time_trigger_scenarios` flakes under transient view-change/commit lag. The `mint_nft_for_every_user_every_1_sec` path now drives sample transactions without strict per-submit height waits, uses unique metadata payloads for each driving transaction, and waits for expected NFT counts via bounded polling across all accounts. Added helper predicate coverage (`counts_meet_expected_requires_each_account_to_reach_target`). Validation: `CARGO_TARGET_DIR=/tmp/iroha-codex-fix-target cargo test -p integration_tests --test mod triggers::time_trigger::timeout_tests -- --nocapture` (ok), `CARGO_TARGET_DIR=/tmp/iroha-codex-fix-target cargo test -p integration_tests --test mod triggers::time_trigger::time_trigger_scenarios -- --nocapture` (ok), `cargo fmt --all` (ok). Remaining open for this tranche: none.
Latest sync (SORA runtime-governance helper extraction + overlap-policy regression, 2026-02-25): extracted shared runtime-governance fixture/round logic into `integration_tests/tests/common/sora_runtime_governance.rs` and wired both `integration_tests/tests/sora_runtime_upgrade_resilience.rs` and `integration_tests/tests/sora_parliament_lifecycle_smoke.rs` to consume it (shared governance builder in smoke + shared runtime round execution in both files). Runtime chained-round scheduling now derives from on-chain runtime-upgrade state (`max end_height + 1`) instead of local fixture-only state. Added negative regression `sora_runtime_upgrade_resilience_rejects_overlapping_runtime_window_proposal` that enacts a baseline runtime upgrade and asserts a second overlapping proposal is rejected at submission. Resilience lint hygiene was tightened by removing broad file-level allow usage and pruning duplicated local helper scaffolding. Validation: `rustfmt --edition 2024 integration_tests/tests/common/sora_runtime_governance.rs integration_tests/tests/sora_runtime_upgrade_resilience.rs integration_tests/tests/sora_parliament_lifecycle_smoke.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo test -p integration_tests --test sora_runtime_upgrade_resilience --no-run` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo test -p integration_tests --test sora_parliament_lifecycle_smoke --no-run` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_runtime_resilience/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo test -p integration_tests --test sora_runtime_upgrade_resilience -- --nocapture` (ok; `2 passed`, `1 ignored`, `486.68s`), and `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_runtime_resilience/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo test -p integration_tests --test sora_parliament_lifecycle_smoke -- --nocapture` (ok; `1 passed`, `266.01s`). Remaining open for this tranche: none.
Latest sync (Cross-lane hardening gap closure + CI filter wiring, 2026-02-25): completed the remaining optional cross-lane hardening gaps in `crates/iroha/src/nexus.rs` and `crates/iroha/src/client.rs`, and wired a dedicated PR CI filter. Added tuple-permutation dedup-key regression coverage (`batch_verification_duplicate_detection_depends_on_full_tuple_permutations`) so duplicate detection is only triggered by exact `(lane_id, dataspace_id, block_height)` matches. Added malformed JSON status-wire decode rejection tests (`get_sumeragi_status_wire_rejects_malformed_json_payload`, `get_sumeragi_status_wire_rejects_json_payload_missing_required_fields`). Added `ci/check_nexus_cross_lane_proofs.sh` and new `pr.yml` job `nexus_cross_lane_proofs` to run focused filters (`batch_verification_`, `get_sumeragi_status_wire_rejects_`, `get_cross_lane_transfer_proofs_`) with `NORITO_SKIP_BINDINGS_SYNC=1` for deterministic test-only execution in environments without local JDK parity tooling. Validation: `rustfmt --edition 2024 crates/iroha/src/nexus.rs crates/iroha/src/client.rs` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_cross_lane_gapfill NORITO_SKIP_BINDINGS_SYNC=1 bash ci/check_nexus_cross_lane_proofs.sh` (ok; `6 passed`, `3 passed`, `9 passed`). Remaining open for this tranche: none.
Latest sync (SORA runtime-upgrade resilience e2e gap fill, 2026-02-25): completed `integration_tests/tests/sora_runtime_upgrade_resilience.rs` implementation (the file had been left truncated at `#[tokio::test]`) by adding full runtime-governance resilience coverage. New scenarios include peer-restart fault injection during multi-body approvals (`sora_runtime_upgrade_resilience_peer_restart_during_multibody_sortition`) and an ignored chained-upgrade soak (`sora_runtime_upgrade_resilience_chained_rounds_soak`). Runtime round orchestration now gates referendum polling on successful `ProposeRuntimeUpgradeProposal` application and reuses the proven governance permission prerequisites (`CanProposeContractDeployment`, `CanEnactGovernance`) to avoid admission flakes. Follow-up hardening for chained rounds added non-overlap scheduling (`next_runtime_window_start`) and reduced round window duration (`RUNTIME_WINDOW_DURATION_BLOCKS=40`) so second-round activation is observable within deterministic tick budgets. Validation: `rustfmt --edition 2024 integration_tests/tests/sora_runtime_upgrade_resilience.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo check -p integration_tests --test sora_runtime_upgrade_resilience` (ok), peer-restart run `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_runtime_resilience/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo test -p integration_tests --test sora_runtime_upgrade_resilience sora_runtime_upgrade_resilience_peer_restart_during_multibody_sortition -- --nocapture --test-threads=1` (ok, `1 passed`, `238.81s`), and ignored chained-soak baseline `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_runtime_resilience/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami CARGO_TARGET_DIR=/tmp/iroha_target_runtime_resilience cargo test -p integration_tests --test sora_runtime_upgrade_resilience sora_runtime_upgrade_resilience_chained_rounds_soak -- --ignored --nocapture --test-threads=1` (ok, `1 passed`, `493.29s`). Remaining open for this tranche: none.
Latest sync (Cross-lane remaining-gap test closure, 2026-02-25): completed the remaining test-only gaps in `crates/iroha/src/nexus.rs` and `crates/iroha/src/client.rs`. Added SDK helper coverage for empty relay batches (`batch_verification_accepts_empty_envelopes`), and client coverage for JSON fallback decode path (`get_cross_lane_transfer_proofs_accepts_json_status_payload`), empty relay payloads (`get_cross_lane_transfer_proofs_returns_empty_for_empty_envelopes`), and proof-order preservation (`get_cross_lane_transfer_proofs_preserves_envelope_order`). Validation: `rustfmt --edition 2024 crates/iroha/src/nexus.rs crates/iroha/src/client.rs` (ok), `cargo test -p iroha --lib batch_verification_ -- --nocapture` (ok; `5 passed`, `0 failed`), `cargo test -p iroha --lib get_cross_lane_transfer_proofs_ -- --nocapture` (ok; `9 passed`, `0 failed`). Remaining open for this tranche: none.
Latest sync (Cross-lane duplicate-key boundary + error-precedence coverage extension, 2026-02-25): expanded cross-lane proof regression coverage across integration, SDK helper, and client paths. Added explicit acceptance tests for same dataspace+height with distinct lane IDs (`verify_lane_relay_envelopes_allows_distinct_lanes_on_same_dataspace_and_height`, `batch_verification_allows_distinct_lanes_on_same_dataspace_and_height`, `get_cross_lane_transfer_proofs_accepts_distinct_lanes_on_same_dataspace_and_height`) to lock duplicate-key semantics. Added precedence checks to ensure invalid relay envelopes fail before duplicate tuple detection (`verify_lane_relay_envelopes_reports_relay_error_before_duplicate_tuple_check`, `batch_verification_reports_relay_error_before_duplicate_key_check`, `get_cross_lane_transfer_proofs_reports_invalid_relay_before_duplicate_error`). Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/cross_lane.rs crates/iroha/src/nexus.rs crates/iroha/src/client.rs` (ok), `cargo test -p iroha --lib batch_verification_ -- --nocapture` (ok; `4 passed`, `0 failed`), `cargo test -p iroha --lib get_cross_lane_transfer_proofs_ -- --nocapture` (ok; `6 passed`, `0 failed`), `cargo test -p integration_tests --test mod cross_lane -- --nocapture` (ok; `23 passed`, `0 failed`). Remaining open for this tranche: none.
Latest sync (Cross-lane client proof retrieval coverage extension, 2026-02-25): extended `crates/iroha/src/client.rs` cross-lane client tests so `get_cross_lane_transfer_proofs()` explicitly accepts valid multi-envelope responses where dedup keys differ only by dataspace (`get_cross_lane_transfer_proofs_accepts_distinct_dataspaces_on_same_lane_and_height`) or only by height (`get_cross_lane_transfer_proofs_accepts_same_lane_dataspace_across_heights`). Added a focused relay-construction helper for status fixtures (`make_lane_relay_for_status(...)`) in the test module. Validation: `rustfmt --edition 2024 crates/iroha/src/client.rs` (ok) and `cargo test -p iroha --lib get_cross_lane_transfer_proofs_ -- --nocapture` (ok; `4 passed`, `0 failed`). Remaining open for this tranche: none.
Latest sync (Cross-lane SDK helper coverage extension, 2026-02-25): extended `crates/iroha/src/nexus.rs` with additional cross-lane proof unit coverage for dedup-key semantics and constructor guardrails: distinct-dataspace acceptance on same lane+height (`batch_verification_allows_distinct_dataspaces_on_same_lane_and_height`), settlement/header mismatch rejection (`builder_rejects_settlement_height_mismatch`), and DA commitment mismatch rejection (`builder_rejects_da_commitment_hash_mismatch`). Validation: `rustfmt --edition 2024 crates/iroha/src/nexus.rs` (ok) and `cargo test -p iroha --lib nexus::tests:: -- --nocapture` (ok; `9 passed`, `0 failed`). Remaining open for this tranche: none.
Latest sync (Testus quorum-first membership lag hardening + staged soak matrix completion, 2026-02-25): completed the quorum-first membership lag policy in `integration_tests/tests/testus_public_localnet.rs` using `MembershipCycleOutcome { hard_lagged, warning_lagged }`, warning-only joiner catch-up stall classification, hard-lag-only membership backoff, and new summary/JSON field `membership_churn_warning_cycles`; documentation semantics were updated in `integrated_test_framework.md`. During soak validation, applied an additional leave-cycle hardening change (`stop_joiner()` before unregister propagation wait) to prevent active-connection linger from being counted as unregister propagation hard lag. Validation: compile gate `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_testus_soak cargo test -p integration_tests --test testus_public_localnet --no-run` (ok), focused classification/scheduling tests passed, Stage A rerun (`testus-matrix-a|b|c`) passed 3/3 with aggregate `committed_tps avg/min/max=5.1648/5.1434/5.2022`, `tx_submit_errors_total=0`, `view_changes_delta_total=0`, `max_height_skew_observed_max=1`, `membership_churn_lagged_cycles_total=0`, `membership_churn_warning_cycles_total=11`; Stage B extension (`testus-matrix-d|e`) also passed. Per-seed matrix metrics: `a` (`committed_tps=5.2022`, `view_changes_delta=0`, `max_height_skew_observed=1`, `tx_submit_errors=0`, `membership_churn_hard/warn=0/5`), `b` (`5.1434`, `0`, `1`, `0`, `0/3`), `c` (`5.1489`, `0`, `1`, `0`, `0/3`), `d` (`5.1575`, `0`, `1`, `0`, `0/3`), `e` (`5.1509`, `0`, `1`, `0`, `0/3`). Final 5-seed aggregate (`a..e`): `committed_tps avg/min/max=5.1606/5.1434/5.2022`, `tx_sent_total=79288`, `tx_submit_errors_total=0`, `view_changes_delta_total=0`, `max_height_skew_observed_max=1`, `membership_churn_lagged_cycles_total=0`, `membership_churn_warning_cycles_total=17`. Cargo command-shape confirmation run was also executed with the exact wrapper form for `testus-matrix-e` (`... cargo test -p integration_tests --test testus_public_localnet -- --ignored --nocapture --test-threads=1 testus_public_localnet_5tps_churn_stability`): pass (`3601.47s`), artifact `target/testus-localnet/testus-simulationFAW1RY/localnet/testus_simulation_summary.json`, metrics `committed_tps=5.1379`, `view_changes_delta=0`, `max_height_skew_observed=1`, `tx_submit_errors=0`, `membership_churn_hard/warn=0/3`. Remaining open for this tranche: none.
Latest sync (Nexus cross-lane transaction proof coverage follow-up, 2026-02-25): extended `integration_tests/tests/nexus/cross_lane.rs` with additional constructor/quorum/dedup-key edge cases: settlement/header mismatch rejection at build time (`cross_lane_builder_rejects_settlement_height_mismatch_at_construction`), DA commitment mismatch rejection at build time (`cross_lane_builder_rejects_da_hash_mismatch_at_construction`), exact-min-quorum acceptance (`lane_relay_quorum_accepts_exact_min_quorum`), and acceptance of distinct dataspaces for the same lane+height (`verify_lane_relay_envelopes_allows_distinct_dataspaces_on_same_lane_and_height`). Validation completed: `rustfmt --edition 2024 integration_tests/tests/nexus/cross_lane.rs` (ok) and `cargo test -p integration_tests --test mod cross_lane -- --nocapture` (ok; `21 passed`, `0 failed`). Remaining open for this tranche: none.
Latest sync (Nexus cross-lane transaction proof coverage, 2026-02-25): extended `integration_tests/tests/nexus/cross_lane.rs` with three additional edge-case tests: signer bitmap length mismatch under quorum validation (`lane_relay_quorum_rejects_signer_bitmap_length_mismatch`), non-duplicate acceptance for distinct lane/dataspace tuples at identical block height (`verify_lane_relay_envelopes_allows_distinct_lanes_on_same_height`), and non-duplicate acceptance for identical lane/dataspace across different heights (`verify_lane_relay_envelopes_allows_same_lane_across_heights`). Validation completed: `rustfmt --edition 2024 integration_tests/tests/nexus/cross_lane.rs` (ok), `cargo test -p integration_tests --test mod cross_lane -- --nocapture` (ok; `17 passed`, `0 failed`), and focused rerun `cargo test -p integration_tests --test mod lane_relay_quorum_rejects_signer_bitmap_length_mismatch -- --nocapture` (ok; `1 passed`). Remaining open for this tranche: none.
Latest sync (Nexus autoscale gap-closure validation follow-up, 2026-02-25): completed targeted cargo validation for the new strict-contraction baseline and soak reporter paths. Verified strict post-expansion scale-in quorum tests (`nexus::autoscale_localnet::tests::strict_scale_in_quorum_*`: `2 passed`) and reporter/serialization coverage (`nexus::autoscale_localnet::tests::soak_*`: `4 passed`) on warmed target caches. During validation, hit and fixed an unrelated pre-existing compile break in `crates/iroha_core/src/state.rs` by guarding `inc_runtime_upgrade_event("activated")` behind `if let Some(telemetry) = wtx.telemetry` so `Option` telemetry is handled correctly. Completed strict repeated-cycle confirmation with skip-build binary overrides (`.../mod-8c7afc1a90b9898d --exact nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet --nocapture --test-threads=1`: `1 passed`, `91.39s`) and completed matrix smoke ingestion with forced fail cycle (`scripts/nexus/run_autoscale_soak_matrix.sh --runs 1 --force-fail-cycle 1 ... --output-dir /tmp/iroha_autoscale_soak_matrix_smoke2`, expected run failure `exit_code=101`) producing canonical artifacts + aggregates from files (summary/events/runs CSV/aggregate JSON+CSV). Added deterministic multi-seed ingestion check with `--runs 2 --continue-on-failure --force-fail-cycle 1` (`/tmp/iroha_autoscale_soak_matrix_smoke3`), confirming seeded artifact aggregation (`autoscale-seed-1`, `autoscale-seed-2`) is deterministic (`runs_total=2`, `fail_count=2`, both run summaries persisted). Also smoke-validated the nightly workflow command shape with forced-fail env override (`cargo test ... nexus_autoscale_soak_expand_contract_cycles_in_localnet -- --ignored --nocapture --test-threads=1`) and confirmed artifact flush under `/tmp/nexus_autoscale_soak_ci_smoke`. Remaining open for this tranche: none beyond routine nightly monitoring.
Latest sync (Testus quorum-first membership lag hardening + staged soak gate, 2026-02-24): implemented quorum-first membership lag classification in `integration_tests/tests/testus_public_localnet.rs` by replacing join/leave `Result<bool>` lag flags with `MembershipCycleOutcome { hard_lagged, warning_lagged }`, keeping only submit/propagation/quorum failures as hard lag, and making joiner catch-up stall threshold crossings warning-only. `run_testus_simulation(...)` now tracks `membership_churn_warning_cycles`, applies membership churn backoff only on hard lag, and writes the new summary JSON field without removing existing fields. Added focused tests for warning-vs-hard classification, hard-only backoff behavior, and summary JSON field presence; updated `integrated_test_framework.md` semantics accordingly. Validation: compile gate `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_testus_soak cargo test -p integration_tests --test testus_public_localnet --no-run` passed; focused tests passed via direct test binary execution (`stalled_joiner_catchup_marks_warning_without_hard_lag`, `propagation_and_quorum_failures_mark_hard_lag`, `membership_backoff_triggers_only_on_hard_lag`, `simulation_summary_json_includes_membership_warning_cycles`, `joiner_stall_warning_threshold_matches_policy`, plus existing membership deadline tests). Stage A 3-seed gate run with preserved artifacts (`IROHA_TESTUS_KEEP_LOCALNET=1`, seeds `testus-matrix-a|b|c`) produced: `a` pass (`3602.28s`, summary `target/testus-localnet/testus-simulationyIq7Ec/localnet/testus_simulation_summary.json`, hard/warn `0/3`), `b` fail (`3605.63s`, `target/testus-localnet/testus-simulationSzZOXi`, failure `membership churn lagged cycles exceeded threshold: lagged=5, total=10, allowed=4`), `c` pass (`3604.89s`, summary `target/testus-localnet/testus-simulationZK5Vl4/localnet/testus_simulation_summary.json`, hard/warn `0/3`). Gate result at this checkpoint was `2/3` passing and Stage B blocked. Remaining open for this historical checkpoint: closed by the 2026-02-25 staged matrix completion.
Latest sync (SORA parliament runtime-upgrade e2e extension, 2026-02-25): `integration_tests/tests/sora_parliament_lifecycle_smoke.rs` now exercises the runtime-governance lifecycle end-to-end in addition to deploy/reject flows: submit `ProposeRuntimeUpgradeProposal`, collect approvals from all seven parliament bodies, open/vote/finalize/enact the referendum, and assert runtime-upgrade activation through `/v1/runtime/upgrades` (`ActivatedAt` at the scheduled start height). Added deterministic runtime-governance proposal-id helper + runtime-upgrade status polling helper in the smoke test. Follow-up live-run fixes aligned runtime-body approvals to council-quorum submissions and corrected the numeric mint literal type in the smoke path. Conclusive validation now completed: `rustfmt --edition 2024 integration_tests/tests/sora_parliament_lifecycle_smoke.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_BUILD_JOBS=8 CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/iroha_target_runtime_e2e_clean cargo check -p integration_tests --test sora_parliament_lifecycle_smoke` (ok), and `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_BUILD_JOBS=8 CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/iroha_target_runtime_e2e_clean cargo test -p integration_tests --test sora_parliament_lifecycle_smoke -- --nocapture` (ok, `1 passed`, `495.55s`, runtime-upgrade governance path verified). Repeatability confirmation also passed in two additional runs: warm-cache rerun in the same target dir (`1 passed`, `616.59s`) and cold-build rerun in `CARGO_TARGET_DIR=/tmp/iroha_target_runtime_e2e_fresh` (`1 passed`, `787.95s`). Additional direct-binary flake sweeps on the warmed test artifact (`/tmp/iroha_target_runtime_e2e_clean/debug/deps/sora_parliament_lifecycle_smoke-b6ec3e2d4ead714f`) also passed: initial `2/2` run sweep (`/tmp/sora_runtime_e2e_flake_20260225T105801`) and extended `3/3` sweep (`/tmp/sora_runtime_e2e_flake3_20260225T111642`, durations `420.87s`, `425.76s`, `427.76s`, aggregate `avg/min/max=424.80/420.87/427.76s`, status `all_passed`). Remaining open for this tranche: none.
Latest sync (Nexus autoscale localnet gap closure plan execution, 2026-02-24): completed the requested hardening scope across strict contraction semantics, soak observability artifacts, deterministic matrix tooling, and nightly CI gating. `integration_tests/tests/nexus/autoscale_localnet.rs` now requires strict scale-in quorum against post-expansion transition baselines (cycle-start deltas retained for diagnostics only), emits structured soak artifacts (`autoscale_soak_summary.json`, `autoscale_soak_events.jsonl`) through internal reporter structs, supports deterministic seed control via `IROHA_AUTOSCALE_SOAK_SEED`, supports artifact output override via `IROHA_AUTOSCALE_SOAK_ARTIFACT_DIR`, and includes a test-only forced-failure hook (`IROHA_AUTOSCALE_SOAK_FORCE_FAIL_CYCLE`) so failure runs still flush artifacts. Added `scripts/nexus/run_autoscale_soak_matrix.sh` for multi-seed ignored-soak matrix execution + artifact-native aggregation (CSV/JSON, pass/fail counts, retry/max-attempt/cycle distributions, expansion/contraction p50/p95), and added `.github/workflows/nightly_nexus_autoscale_soak.yml` for scheduled/manual ignored-soak gating with always-on artifact upload. Validation completed so far: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` and `bash -n scripts/nexus/run_autoscale_soak_matrix.sh` (both ok). The earlier “queued cargo validations still pending” note from this entry is superseded by the completed follow-up validations listed above.
Latest sync (Nexus autoscale localnet soak resilience hardening, 2026-02-24): closed the remaining autoscale soak reliability gaps in `integration_tests/tests/nexus/autoscale_localnet.rs` by adding attempt-based strict-cycle load escalation (`96 -> 192 -> 288 -> 384`), running cooldown-clearance prechecks only on the first attempt of each cycle (so retries are not blocked on no-load non-empty-height movement), and making expansion top-up pressure load-aware during strict waits (periodic/reinforce/post-storage top-ups now scale with per-attempt load). Added helper/unit coverage for retry load profile, cooldown-clearance gating policy, and load-scaled top-up behavior. Validation: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_transition cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (`21 passed`), `... cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture` (pass, `1 passed`, `228.52s`), and full ignored soak `... cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_soak_expand_contract_cycles_in_localnet -- --ignored --nocapture --test-threads=1` (pass, `1 passed`, `2040.85s`, `cycles completed: 72`; one late strict timeout at cycle 72 attempt 1 recovered on attempt 2).
Latest sync (Testus public localnet 3600s multi-seed soak matrix, 2026-02-24): executed a 3-seed churn/load matrix for `integration_tests/tests/testus_public_localnet.rs::testus_public_localnet_5tps_churn_stability` by adding deterministic seed override support (`IROHA_TESTUS_SIM_SEED`, default `testus-public-sim`) and running with `IROHA_TESTUS_KEEP_LOCALNET=1` across seeds `testus-matrix-a|b|c`. Validation command shape: `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_SEED=<seed> CARGO_TARGET_DIR=/tmp/iroha_target_testus_soak cargo test -p integration_tests --test testus_public_localnet -- --ignored --nocapture --test-threads=1 testus_public_localnet_5tps_churn_stability` (all 3 runs passed; durations `3912.20s`, `3779.54s`, `3789.56s`). Aggregated preserved summaries (`target/testus-localnet/testus-simulationaGGTX0`, `...njNZ7A`, `...xy128G`): `committed_tps avg/min/max 5.1786/5.1683/5.1883`, `tx_sent_total=48401`, `tx_submit_errors_total=0`, `view_changes_delta_total=0`, `max_height_skew_max=1`, `process_churn_lagged_cycles_total=0`, `membership_churn_lagged_cycles_total=7`. Remaining open for this historical checkpoint: closed by the 2026-02-25 quorum-first hardening + staged matrix completion (`membership_churn_lagged_cycles_total=0` in 5-seed matrix).
Latest sync (Confidential localnet restart/invalid-proof gap closure, 2026-02-24): completed the remaining confidential localnet reliability/testing gaps in `integration_tests/tests/zk_confidential_localnet.rs` by adding dual-restart stress (`confidential_dual_restart_stress_mid_flow_localnet`), combined downtime+timeout pressure (`confidential_combined_peer_downtime_and_timeout_pressure_localnet`), and malformed-proof rejection coverage (`confidential_unshield_rejects_corrupted_proof_bytes_localnet`, `confidential_unshield_rejects_corrupted_vk_bytes_localnet`, `confidential_unshield_rejects_wrong_statement_hint_localnet`), plus helper/unit coverage for pressure submitter timeouts and corrupted attachment builders. Hardened peer restart handling with idempotent shutdown (`shutdown_if_started`) and stabilized policy-sensitive balance checks using quorum polling (`wait_for_numeric_balance_quorum`). Also aligned ZkNative transparent-transfer behavior with current policy by replacing `confidential_zknative_transparent_transfer_after_mint_localnet` with rejection coverage `confidential_zknative_transparent_transfer_after_mint_rejected_localnet`. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_gapfill2 NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_zk_conf_gapfill/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami cargo test -p integration_tests --test zk_confidential_localnet confidential_zknative_transparent_transfer_after_mint_rejected_localnet -- --nocapture --test-threads=1` (ok), `... confidential_unshield_rejected_when_disabled -- --nocapture --test-threads=1` (ok), and full file `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (`34 passed`, `0 failed`, `544.14s`).
Latest sync (Nexus matrix runner five-gap hardening closure, 2026-02-24): completed the remaining reliability gaps in `scripts/nexus/run_cross_runtime_matrix.sh` by adding strict option-value guards, timeout-marker-based `timed_out` attribution, fail-fast summary finalization via `trap finalize_outputs EXIT`, process-group timeout teardown (`SIGTERM`/`SIGKILL`) to prevent orphan children, and exact/anchored test-id matching for `--prefer-test-binary` resolution. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), missing-value checks (`--cross-runs` and `--cross-runs --runtime-runs 1`) now fail with explicit `Missing value for --cross-runs`, non-timeout `124` classification smoke (`--runtime-timeout-s 0` with fake target) records `timed_out=0` in `/tmp/iroha_matrix_non_timeout_124/runtime_registration_matrix.csv`, wrapper-timeout smoke (`--runtime-timeout-s 1`) records `timed_out=1` in `/tmp/iroha_matrix_wrapper_timeout/runtime_registration_matrix.csv`, fail-fast run without `--continue-on-failure` still emits `/tmp/iroha_matrix_failfast_summary/{matrix_summary.txt,matrix_summary.json}`, process-tree kill smoke confirms spawned child pid is gone post-timeout, and exact-match smoke with similar/correct fake binaries selects `mod-correct` and records exit `42` in `/tmp/iroha_matrix_exact_match/runtime_registration_matrix.csv`.
Latest sync (deterministic missing-QC + shared-host soak hardening, 2026-02-25): completed the requested deterministic recovery tranche in Sumeragi (`crates/iroha_core/src/sumeragi/main_loop/{block_sync.rs,proposal_handlers.rs,qc.rs,votes.rs}`, `crates/iroha_core/src/sumeragi/main_loop.rs`), validated targeted regressions, and revalidated formal checks (`bash ci/check_sumeragi_formal.sh` now passes locally with Apalache). Added shared-host soak profile hardening in `crates/izanami/src/chaos.rs` (long-soak profile clamp for `max_inflight`/`progress_timeout` while preserving canonical `tps=5`, expanded DA/recovery windows, tuned NPoS timing defaults, shared-host profile tests, release build verification).
Remaining open: close the final acceptance gate for shared-host long soak (`--duration 3600s`) so all 4 peers finish at height `>= 3600` without progress-timeout churn. Current probe evidence (`--duration 3600s --target-blocks 180`) in both debug/release Izanami still trends at ~`0.6-0.7 blocks/s` strict height on this host (`/tmp/izanami_probe*_20260225*.log`, `/tmp/izanami_release_probe_3600dur_180target_20260225T114144.log`), so additional shared-host throughput tuning is still required.
Latest sync (pilot/acceptance gate reruns executed, 2026-02-25): ran the unchanged gates with fixed external interface (`--tps 5 --max-inflight 8 --workload-profile stable`) after the recovery-selector + submit-scope/backlog patch. Pilot artifacts: `/tmp/izanami_pilot_1200_scopefix_20260225T205417.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_h3D6yj`; outcome: `target blocks not reached`, final height `650/1200`, strict/quorum max divergence `1`, summary `successes=3089 failures=1 ingress_failover_total=0 ingress_endpoint_unhealthy_total=0`. Acceptance artifacts: `/tmp/izanami_acceptance_3600_scopefix_20260225T212106.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_jPd2Rd`; outcome: `target blocks not reached`, final height `1546/3600`, strict/quorum max divergence `1`, summary `successes=7525 failures=6 ingress_failover_total=10 ingress_endpoint_unhealthy_total=4` (includes `Repeated instruction` submit rejections). Remaining open for this item: consensus convergence is now stable enough (no sustained strict/quorum split), but throughput remains materially below required gate slope; next patch must tune one internal throughput lever at a time (first candidate remains `IZANAMI_SHARED_HOST_SOAK_PIPELINE_TIME_MS`) with fresh A/B artifacts.
Latest sync (pilot recovery selector + submit-path throughput uncap, 2026-02-25): implemented the planned `crates/izanami/src/chaos.rs` changes while keeping the external gate interface fixed at `--tps 5 --max-inflight 8`: (1) `recovery_profile_for(...)` now uses new `is_shared_host_stable_recovery_run(...)` (`nexus`, stable, `faulty_peers=0`, `peer_count>=4`, `duration>=1200s`) so pilot-scale runs get shared-host recovery tuning; (2) shared-host load-shape pinning remains soak-only (`duration>=3600s`); (3) submit permits were moved from supervisor pre-plan scope into `submit_plan(...)` immediately before submit RPC/retry and released right after submit completion; (4) deterministic submit-backlog bound added with `IZANAMI_SUBMISSION_BACKLOG_MULTIPLIER=8` and enforced queue gating at `max_inflight * multiplier`; (5) regression coverage added for pilot recovery selection and backlog/permit invariants.
Validation for this patch: `cargo fmt --all` (ok); `cargo test -p izanami shared_host_stable_soak_profile_ -- --nocapture` (ok); `cargo test -p izanami shared_host_recovery_profile_ -- --nocapture` (ok); `cargo test -p izanami submission_ -- --nocapture` (ok); `cargo test -p iroha_core --lib should_share_unknown_prev_hash_tracks_peer_and_height -- --nocapture` (ok); `bash ci/check_sumeragi_formal.sh` (environment failure: `apalache-mc` missing, Docker daemon unavailable).
Remaining open for this item: maintain the fixed interface and carry the next single-variable throughput experiment with fresh pilot + acceptance reruns and artifact-backed comparison against this `650/1200` and `1546/3600` baseline.
Latest sync (lagging-peer recovery storm suppression + inflight A/B lock, 2026-02-25): completed deterministic recovery-loop suppression in `crates/iroha_core/src/block_sync.rs`, `crates/iroha_core/src/sumeragi/main_loop.rs`, and `crates/iroha_core/src/sumeragi/main_loop/propose.rs` (per-peer unknown-prev fallback suppression; one active highest-QC-missing defer marker/reacquire per `(height, view, highest_qc_hash)` key with stale-marker cleanup), and pinned the shared-host baseline in `crates/izanami/src/chaos.rs` (`tps=5`, `max_inflight=8`) with profile regression coverage. A/B pilot evidence from 1200s runs (`/tmp/izanami_pilot_1200_inflight8_20260225T162303.log`, `/tmp/izanami_pilot_1200_inflight6_20260225T165243.log`, and paired sample JSONL artifacts) showed `max_inflight=8` materially reduces divergence and missing-fetch churn versus `max_inflight=6`, so the inflight decision is complete and fixed at `8` for acceptance soaks.
Remaining open: only the 3600s shared-host acceptance gate remains (`--duration 3600s --target-blocks 3600 --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable`), with pass criteria requiring all four peers at height `>= 3600` without progress-timeout and without prolonged stale missing-QC/proposal-missing loops.
Latest sync (canonical long Izanami soak + formal rerun, 2026-02-24): reran local reproducible formal verification (`bash ci/check_sumeragi_formal.sh`) and both Apalache checks passed with `NoError` (`/tmp/sumeragi_formal_check_after_fix_20260224T023428.log`). Reran the full canonical Izanami soak command unchanged (`RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable`) with artifacts at `/tmp/izanami_canonical_long_rerun_20260224T023548.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_yZLSlT`. Outcome changed from prior synchronized hard-stall: run no longer tripped `no block height progress for 300s`, but still failed (`exit 1`) at duration end with `izanami run stopped before target blocks reached` (`crates/izanami/src/chaos.rs:1455`). Terminal summary: `successes=3708 failures=222 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=665 izanami_ingress_endpoint_unhealthy_total=462`; final committed heights `1298/1298/1298/278`.
Remaining open: close the deterministic single-peer divergence/stall path (lagger pinned near `height: 280` with repeated `missing_qc`/`missing_payload` + range-pull hard-cap churn while responders return unknown-prev fallback anchors `1216+`) so non-faulty replicas reconverge and canonical soak reaches `target-blocks=2000` within the 3600s window.
Latest sync (canonical long Izanami soak rerun, 2026-02-23): reran the full canonical command (`RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable`) and reproduced a deterministic liveness failure: `no block height progress for 300s (quorum min height 516, strict min 516, target 2000, tolerated_failures 1)` at `crates/izanami/src/chaos.rs:1540` (exit code `1`). Terminal summary: `successes=2328 failures=23 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=111 izanami_ingress_endpoint_unhealthy_total=110`. Evidence: `/tmp/izanami_canonical_monitor_20260223T2015.log` shows a synchronized plateau at `515` from `20:17:58` through `20:23:00`; peer logs under `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_eq3wuB` show repeated roster-sidecar mismatch/fail-closed quarantine around height `516`.
Remaining open: resolve the height-515/516 consensus stall path (roster sidecar mismatch + quarantine loop under sustained load) and rerun the same canonical soak until it reaches duration/target without progress-timeout failure.
Latest sync (consensus missing-block liveness fallback hardening, 2026-02-23): patched `crates/iroha_core/src/sumeragi/main_loop/votes.rs` so missing-block fetch does not stall when commit/QC rosters are empty; both highest-QC recovery and unresolved-roster vote paths now deterministically fall back to `trusted_topology()` before declaring no targets. Added deterministic regressions in `crates/iroha_core/src/sumeragi/main_loop/tests.rs`: `new_view_highest_qc_fetch_uses_trusted_topology_when_all_rosters_empty` and `handle_vote_seeds_missing_block_fetch_with_trusted_topology_fallback`. Validation: `cargo test -p iroha_core --lib handle_vote_seeds_missing_block_fetch_with_trusted_topology_fallback -- --nocapture` (ok), `cargo test -p iroha_core --lib new_view_highest_qc_fetch_uses_trusted_topology_when_all_rosters_empty -- --nocapture` (ok), `cargo test -p iroha_core --lib new_view_highest_qc_fetch_uses_ -- --nocapture` (ok, 5 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_sumeragi_model_verify bash scripts/tests/sumeragi_model_tiers.sh fast` (ok: model `12 passed`, fairness `7 passed`, `2 ignored`), and `cargo test -p integration_tests --test sumeragi_da sumeragi_rbc_unverified_roster_stash_requests_missing_block -- --nocapture` (ok, `1 passed`, `217.23s`). Remaining open: fallback hardening removed the empty-roster no-target gap but did not prevent the synchronized height-515/516 plateau observed in the latest canonical soak; continue consensus stall root-cause remediation under sustained load.
Latest sync (local reproducible formal setup complete, 2026-02-23): added `scripts/formal/install_apalache.sh` to install pinned Apalache `v0.52.2` with release checksum verification into `target/apalache/toolchains/v0.52.2`, updated `scripts/formal/sumeragi_apalache.sh` to auto-detect that local toolchain when `APALACHE_BIN` is unset, and documented the flow in `docs/formal/sumeragi/README.md` + `ci/README.md`. Local validation now passes without Docker: `bash scripts/formal/install_apalache.sh 0.52.2` (ok), `bash ci/check_sumeragi_formal.sh` (ok, `fast` + `deep`, `EXITCODE: OK`). Remaining formal open item is unchanged: first live PR confirmation of `sumeragi_formal` (and optional digest pin decision).
Latest sync (deep model revalidation + live formal CI status, 2026-02-23): reran `scripts/tests/sumeragi_model_tiers.sh deep` with `CARGO_TARGET_DIR=/tmp/iroha_target_sumeragi_model_verify` to avoid shared cargo-lock contention; all tiers passed (`state_machine_model_tests`: `12 passed` in `112.71s`; non-ignored fairness: `7 passed`, `2 ignored` in `83.18s`; ignored deep fairness: `2 passed` in `174.68s`). At that checkpoint, local formal wiring check (`bash ci/check_sumeragi_formal.sh`) was blocked by missing `apalache-mc` + unavailable Docker daemon. GitHub auth was valid, but there was no open PR/run for branch `i23` (`gh pr list --head i23`, `gh run list --workflow pr.yml --branch i23` empty), so live `sumeragi_formal` verification remained open.
Latest sync (Nexus autoscale repeated-cycle scale-in gating follow-up, 2026-02-24): stabilized `integration_tests/tests/nexus/autoscale_localnet.rs` by making cycle-level strict scale-in enforcement conditional on expansion status quorum visibility. Repeated-cycle flow now keeps deterministic scale-out transition checks, but only requires deterministic scale-in transition quorum when expansion reached quorum via status signals in the same cycle; otherwise it validates contraction profile without failing on missing scale-in transition logs. Added `should_require_scale_in_transition(...)` helper and focused unit coverage (`scale_in_transition_requirement_respects_request_flag`, `scale_in_transition_requirement_is_disabled_without_expansion_status_quorum`). Validation: `cargo fmt --all` (ok), `cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests::scale_in_transition_requirement -- --nocapture` (ok; `2 passed`), `cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture` (ok; `1 passed`, cycle1 relaxed path exercised).
Latest sync (Confidential expanded localnet test coverage, 2026-02-24): extended `integration_tests/tests/zk_confidential_localnet.rs` with two additional end-to-end cases to increase shielding/unshielding confidence: `confidential_public_two_three_hop_sequences_allow_multiple_unshields_localnet` (two 3-hop sequences with two successful unshields before transfer) and `confidential_shielded_asset_three_hop_then_unshield_and_transfer_localnet` (dedicated shielded-asset full cycle including unshield + transfer). Existing two-pass public flow remains in place. Validation: targeted reruns `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet confidential_public_two_three_hop_sequences_allow_multiple_unshields_localnet -- --nocapture --test-threads=1` (ok, `1 passed`, `29.30s`) and `... confidential_shielded_asset_three_hop_then_unshield_and_transfer_localnet -- --nocapture --test-threads=1` (ok, `1 passed`, `24.49s`), plus full-file rerun `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `25 passed`, `0 failed`, `284.49s`). This supersedes the earlier note about reruns being blocked by stalled external cargo processes.
Latest sync (Testus public localnet 3600s soak stabilization closure, 2026-02-23): finalized the remaining Testus soak gap in `integration_tests/tests/testus_public_localnet.rs` by keeping lag-aware process churn selection, adding timeout/connect-aware load submit retries (`LOAD_SUBMIT_RETRY_TIMEOUT_SECS=3`), and splitting churn scheduling so lagged process churn retries quickly (30s) while membership churn retains interval+backoff. Validation: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_testus_soak cargo test -p integration_tests --test testus_public_localnet next_process_churn_deadline_ -- --nocapture` (`2 passed`), `... next_membership_churn_deadline_ -- --nocapture` (`2 passed`), `... churn_index_ -- --nocapture` (`6 passed`), and canonical soak `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TESTUS_KEEP_LOCALNET=1 CARGO_TARGET_DIR=/tmp/iroha_target_testus_soak cargo test -p integration_tests --test testus_public_localnet -- --ignored --nocapture --test-threads=1 testus_public_localnet_5tps_churn_stability` (pass, `3739.36s`; artifact `target/testus-localnet/testus-simulation81TcCN/localnet/testus_simulation_summary.json`: `scheduled/submitted/committed_tps=5.171/5.171/5.170`, `tx_submit_errors=0`, `process_churn_lagged_cycles=0`, `membership_churn_lagged_cycles=3`, `max_height_skew_observed=1`, `view_changes_end-start=0`).
Latest sync (Confidential + Testus localnet validation sweep, 2026-02-23): completed cargo-level rerun of `integration_tests/tests/zk_confidential_localnet.rs` with stable release peer binary and extended bounded `integration_tests/tests/testus_public_localnet.rs` coverage in the same warmed target dir. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `23 passed`, `0 failed`, `225.88s`), `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test testus_public_localnet --no-run` (ok), direct-binary helper subsets `warning_state_` (`2 passed`), `skew_breach_` (`3 passed`), `select_process_churn_index_` (`4 passed`), ignored localnet scenarios `--exact testus_localnet_joiner_register_unregister_behavior --ignored` (ok, `6.37s`) and `--exact testus_localnet_restart_catchup_behavior --ignored` (ok, `6.85s`), bounded churn run `--exact testus_public_localnet_5tps_churn_stability --ignored` with `IROHA_TESTUS_SIM_DURATION_SECS=120 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5` (ok, `121.25s`), and full non-ignored file run `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/iroha-test-network/release/iroha3d KAGAMI_BIN=/Users/takemiyamakoto/dev/iroha/target/debug/kagami cargo test -p integration_tests --test testus_public_localnet -- --nocapture --test-threads=1` (ok, `43 passed`, `3 ignored`, `4.46s`).
Latest sync (Nexus matrix runner JSON success/failure bucketing, 2026-02-23): extended `scripts/nexus/run_cross_runtime_matrix.sh` JSON aggregates with `success` and `exit_code_counts` for both cross/runtime sections, so CI can bucket timeout (`124`) vs functional failures directly from `matrix_summary.json`. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), timeout smoke `scripts/nexus/run_cross_runtime_matrix.sh --cross-runs 1 --runtime-runs 1 --cross-soak-iterations 1 --runtime-bench-iterations 1 --cross-timeout-s 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --summary-json-rows --target-dir /Users/takemiyamakoto/dev/iroha/target --output-dir /tmp/iroha_nexus_matrix_exitcodes_smoke --test-threads 1` (expected timeout rows), and JSON inspection `cat /tmp/iroha_nexus_matrix_exitcodes_smoke/matrix_summary.json` (shows `exit_code_counts` and `success` fields on both sections).
Latest sync (Nexus autoscale localnet strict expansion gating + load-submit resilience, 2026-02-23): tightened `integration_tests/tests/nexus/autoscale_localnet.rs` to reduce false-positive expansion detection and improve localnet reliability under partial peer instability. `wait_for_expanded_lanes_with_heartbeat(...)` now supports strict mode that requires quorum deterministic scale-out transitions and suppresses storage-only fallback in that mode; repeated-cycle flow now keeps strict scale-out checks in both cycles while enforcing deterministic scale-in transition quorum in cycle 1. Also hardened `submit_load_round_robin(...)` to continue on per-transaction submission failures and only fail when all submissions fail, so one unhealthy peer does not starve expansion load. Validation: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_transition cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (`14 passed`), `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture` (pass, `1 passed`, `228.88s`, cycle1 deterministic scale-in quorum `4/4`, cycle2 strict scale-out quorum observed), and `... nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture` (pass, `1 passed`, `202.32s`).
Latest sync (Nexus matrix runner preflight checks + JSON config metadata, 2026-02-23): hardened `scripts/nexus/run_cross_runtime_matrix.sh` with fail-fast dependency checks (`python3`, `rg`) and expanded `matrix_summary.json` with a full `config` block (runs/iterations/timeouts/flags and target/output paths) so artifacts remain reproducible and self-describing across CI hosts. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), runtime smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --runtime-bench-iterations 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --summary-json-rows --target-dir /Users/takemiyamakoto/dev/iroha/target --output-dir /tmp/iroha_nexus_matrix_config_smoke --test-threads 1` (expected timeout row), and JSON inspection `cat /tmp/iroha_nexus_matrix_config_smoke/matrix_summary.json` (verified `config` + typed rows payload).
Latest sync (Nexus matrix runner cross-platform mtime probing, 2026-02-23): updated `scripts/nexus/run_cross_runtime_matrix.sh` with `file_mtime_epoch(...)` so prebuilt `mod-*` binary ordering works on both BSD/macOS (`stat -f`) and GNU/Linux (`stat -c`) hosts. This removes a platform-specific discovery risk in `--prefer-test-binary` mode while preserving deterministic candidate sorting. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok) and runtime smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --runtime-bench-iterations 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --target-dir /Users/takemiyamakoto/dev/iroha/target --output-dir /tmp/iroha_nexus_matrix_cross_platform_smoke --test-threads 1` (expected timeout row with successful resolved-binary selection).
Latest sync (Nexus matrix runner resolved-binary cache optimization, 2026-02-23): optimized `scripts/nexus/run_cross_runtime_matrix.sh` prefer-binary execution by caching validated test-binary paths per filter (`CACHED_CROSS_TEST_BIN`, `CACHED_RUNTIME_TEST_BIN`) so repeated matrix runs avoid redundant candidate probing. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok) and runtime cache smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 2 --runtime-bench-iterations 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --target-dir /Users/takemiyamakoto/dev/iroha/target --output-dir /tmp/iroha_nexus_matrix_binary_cache_smoke --test-threads 1` (expected timeout rows with stable resolved-binary reuse).
Latest sync (Nexus matrix runner test-binary resolution hardening, 2026-02-23): updated `scripts/nexus/run_cross_runtime_matrix.sh` so `--prefer-test-binary` resolves `mod-*` executables by test-filter presence instead of mtime-only selection. Candidate binaries are now probed with `--list` and must contain the requested filter (`cross_dataspace_atomic_swap_is_all_or_nothing` / `runtime_nexus_registration_reports_lane_lifecycle_costs`) before use, and the selected binary path is logged per run for traceability. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), fake-target guard `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --prefer-test-binary --target-dir /tmp/iroha_fake_target --output-dir /tmp/iroha_nexus_matrix_fake_target_check` (expected filter-specific failure), and real-target timeout smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --runtime-bench-iterations 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --target-dir /Users/takemiyamakoto/dev/iroha/target --output-dir /tmp/iroha_nexus_matrix_binary_resolution_smoke2 --test-threads 1` (expected timeout row with resolved-binary log).
Latest sync (Testus public localnet 3600s soak rerun with preserved summary, 2026-02-23): reran `integration_tests/tests/testus_public_localnet.rs::testus_public_localnet_5tps_churn_stability` with `IROHA_TESTUS_KEEP_LOCALNET=1` after process-churn rotation/skew-guard updates. The run passed (`1 passed`, `4079.24s`) and the preserved summary (`target/testus-localnet/testus-simulation31YKUB/localnet/testus_simulation_summary.json`) reported sustained throughput above target (`scheduled_tps=5.1449`, `submitted_tps=5.1449`, `committed_tps=5.1488`) with zero submission errors (`tx_attempted=13246`, `tx_sent=13246`, `tx_submit_errors=0`). Remaining open: reduce prolonged all-validator convergence lag windows (single lagging validator persisted while quorum remained healthy) so final convergence is consistently full-cluster, not only quorum-safe.
Latest sync (Nexus autoscale localnet latency-threshold stabilization + latency-sample guard validation, 2026-02-23): completed the outstanding autoscale localnet determinism follow-up by validating the new core latency-sample guard tests and re-tuning localnet autoscale latency thresholds for stable expansion on fresh startup while keeping config invariants valid. `integration_tests/tests/nexus/autoscale_localnet.rs` now sets `scale_out_latency_ratio=0.1` and `scale_in_latency_ratio=0.05` (`scale_in < scale_out`), and `crates/iroha_core/src/state.rs` helper tests for `autoscale_sample_latency_ms` are covered (`zero timestamp -> None`, monotonic delta, non-monotonic clamp). Validation: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_postguard cargo test -p iroha_core --lib autoscale_sample_latency_ms -- --nocapture` (`3 passed`), `... cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture` (pass, `225.70s`, cycle1 `scale-out transitions 4/4`), and `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture` (pass, `235.77s`, cycle1/2 `scale-out transitions 4/4`, strict cycle `scale-in peers since cycle start 4/4`).
Latest sync (Nexus matrix runner JSON row-embedding option, 2026-02-22): extended `scripts/nexus/run_cross_runtime_matrix.sh` with `--summary-json-rows` so `matrix_summary.json` can optionally include typed per-run arrays (`rows.cross`, `rows.runtime`) for dashboard ingestion while keeping default JSON compact. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), `scripts/nexus/run_cross_runtime_matrix.sh --help` (shows `--summary-json-rows`), and runtime-only smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --runtime-bench-iterations 1 --runtime-timeout-s 120 --prefer-test-binary --summary-json-rows --target-dir /tmp/iroha_target_cross_opt_rolling --output-dir /tmp/iroha_nexus_matrix_json_rows_smoke --test-threads 1` (ok; JSON rows present at `/tmp/iroha_nexus_matrix_json_rows_smoke/matrix_summary.json`).
Latest sync (Nexus matrix runner machine-readable summaries, 2026-02-22): closed the remaining matrix-ingestion gap by extending `scripts/nexus/run_cross_runtime_matrix.sh` to emit `<output-dir>/matrix_summary.json` in addition to CSV and text summaries. JSON now includes deterministic aggregate sections for `cross` and `runtime` (`runs`, `fail`, `timed_out`, duration `min/avg/max`, plus soak/runtime domain counters) and `generated_at_utc` for traceability. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), `scripts/nexus/run_cross_runtime_matrix.sh --help` (shows `matrix_summary.json` output), timeout-path smoke `scripts/nexus/run_cross_runtime_matrix.sh --cross-runs 1 --runtime-runs 1 --cross-soak-iterations 1 --runtime-bench-iterations 1 --cross-timeout-s 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --target-dir /tmp/iroha_target_cross_opt_rolling --output-dir /tmp/iroha_nexus_matrix_json_smoke --test-threads 1` (expected timed-out rows with JSON written), and runtime-only success smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --runtime-bench-iterations 1 --runtime-timeout-s 120 --prefer-test-binary --target-dir /tmp/iroha_target_cross_opt_rolling --output-dir /tmp/iroha_nexus_matrix_json_smoke_runtime --test-threads 1` (runtime success with JSON `pass_rate_avg_percent=100.0`).
Latest sync (Confidential localnet ZkNative behavior re-alignment + duplicate-nullifier stabilization, 2026-02-22): reverted the temporary rejection-only ZkNative assumptions in `integration_tests/tests/zk_confidential_localnet.rs` after direct runtime verification, restoring observed behavior coverage for transparent mint/transfer (`confidential_zknative_transparent_mint_creates_public_balance_localnet`, `confidential_zknative_transparent_transfer_after_mint_localnet`). Also fixed the remaining flaky path in `confidential_unshield_duplicate_nullifier_rejected` by switching immediate post-commit balance reads to retry-based `wait_for_numeric_balance(...)` checks. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test zk_confidential_localnet --no-run` (ok), targeted runtime checks via test binary with `IROHA_TEST_SKIP_BUILD=1` + local binaries (`--exact confidential_zknative_transparent_mint_creates_public_balance_localnet` ok, `16.08s`; `--exact confidential_zknative_transparent_transfer_after_mint_localnet` ok, `24.13s`; `--exact confidential_unshield_duplicate_nullifier_rejected` ok, `25.82s`), and complete post-fix full-file rerun with release peer binary (`TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/iroha-test-network/release/iroha3d`) now passing (`23 passed`, `0 failed`, `258.86s`).
Latest sync (Confidential localnet per-transaction real Halo2 proof generation + quorum poll hardening, 2026-02-22): updated `integration_tests/tests/zk_confidential_localnet.rs` to eliminate shared cached proof attachments and generate fresh real Halo2 backend attachments per `ZkTransfer`/`Unshield` operation via `iroha_core::zk::test_utils::halo2_ivm_execution_envelope(...)` (`live_halo2_attachment`). Also hardened non-empty quorum polling with temporary mute-on-error-streak behavior (`STATUS_ERROR_STREAK_FOR_MUTE=3`, `MUTED_PEER_COOLDOWN_ATTEMPTS=5`) guarded by quorum-safe muting (`should_temporarily_mute_peer`) so unhealthy peer polls are de-prioritized without lowering required quorum checks. Increased fallback polling horizon (`ATTEMPTS=200`, `DELAY=300ms`) after observing one non-empty progression timeout under load. Added helper tests for muting/quorum logic. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/debug/iroha3d IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (pre-horizon-adjustment run: `22 passed`, `1 failed` at `confidential_zknative_transparent_transfer_after_mint_localnet` with `expected non-empty block 3 on quorum 3`; runtime `368.25s`). Post-adjustment targeted reruns are currently blocked by pre-existing long-running/stalled cargo processes (no CPU progress) and were left untouched to comply with the workspace rule against killing `cargo`/`rustc` without explicit approval. Remaining open: rerun the full confidential file and workspace-level validations after those external stuck cargo processes are cleared.
Latest sync (Nexus matrix runner auto-aggregate summaries, 2026-02-22): extended `scripts/nexus/run_cross_runtime_matrix.sh` to publish built-in aggregate results for every invocation, eliminating manual post-processing. The runner now writes `<output-dir>/matrix_summary.txt` and prints `cross_summary`/`runtime_summary` lines derived from CSV data (run count, fail/timeout count, duration min/avg/max, plus soak/pass-rate and delta/fallback metrics). Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), `scripts/nexus/run_cross_runtime_matrix.sh --help` (shows `matrix_summary.txt` output), timeout-path smoke `scripts/nexus/run_cross_runtime_matrix.sh --cross-runs 1 --runtime-runs 1 --cross-soak-iterations 1 --runtime-bench-iterations 1 --cross-timeout-s 1 --runtime-timeout-s 1 --continue-on-failure --prefer-test-binary --target-dir /tmp/iroha_target_cross_opt_rolling --output-dir /tmp/iroha_nexus_matrix_summary_smoke --test-threads 1` (expected timeout rows plus non-empty summaries), and runtime-only success smoke `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --runtime-bench-iterations 1 --runtime-timeout-s 120 --prefer-test-binary --target-dir /tmp/iroha_target_cross_opt_rolling --output-dir /tmp/iroha_nexus_matrix_summary_smoke_runtime --test-threads 1` (`cross_summary runs=0`, runtime row success with `pass_rate_avg=100.00`). This follow-up is now superseded by JSON output and optional row embedding (`matrix_summary.json`, `--summary-json-rows`).
Latest sync (Nexus matrix runner 5x5 direct-binary aggregate sample, 2026-02-22): completed a full mixed cross/runtime matrix with `scripts/nexus/run_cross_runtime_matrix.sh --cross-runs 5 --runtime-runs 5 --prefer-test-binary --cross-timeout-s 240 --runtime-timeout-s 180 --cross-soak-iterations 1 --runtime-bench-iterations 1 --test-threads 1 --target-dir /tmp/iroha_target_cross_opt_rolling --output-dir /tmp/iroha_nexus_matrix_smoke6`. Aggregates from `/tmp/iroha_nexus_matrix_smoke6/cross_dataspace_matrix.csv`: `rows=5`, `fail=0`, `timed_out=0`, duration `min/avg/max=77/83.60/93s`, `soak_fallbacks_total=4`, `soak_retries_total=0`, `swap_nonconverged_total=0`. Aggregates from `/tmp/iroha_nexus_matrix_smoke6/runtime_registration_matrix.csv`: `rows=5`, `fail=0`, `timed_out=0`, duration `min/avg/max=31/34.80/37s`, `pass_rate_avg=100.00`, `submitter_delta_max=0`, `cluster_delta_max=0`, `note_zero_delta_rows=5`. Remaining open for this area: broaden samples with multi-iteration soak/bench settings and capture cross-host variance (cold build vs warm target) before converting these aggregates into hard CI SLO thresholds.
Latest sync (Nexus autoscale localnet repeated-cycle closure, 2026-02-22): closed the strict localnet autoscale test gap by updating cycle assertions to require `scale-in` transitions on a full cycle baseline window (instead of post-expansion-only deltas), wiring strict contraction waits to baseline transition counters, and avoiding contraction heartbeat writes in strict mode. Also tuned localnet autoscale utilization normalization (`per_lane_target_tps=40`) so synthetic burst traffic still triggers deterministic `scale-out` while permitting deterministic `scale-in` emission under the same run profile. Core autoscale scale-in evaluation was split into `autoscale_scale_in_triggered(...)` and no longer depends on latency ratio. Validation: `cargo test -p iroha_core autoscale_scale_in_triggered -- --nocapture` (`2 passed`), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (`13 passed`), strict repeated-cycle localnet (`... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture`) now passes with quorum `scale-in` in both cycles (`since cycle start 4/4`), and single-cycle localnet (`... nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture`) remains passing with matching transition evidence. Remaining open for this area: track pathological early `out_latency_ratio_permille` values observed in peer logs (very large startup-time ratios) and decide whether to clamp/guard latency sampling in core autoscale metrics.
Latest sync (Nexus matrix runner resilience follow-up, 2026-02-22): hardened `scripts/nexus/run_cross_runtime_matrix.sh` to support partial and bounded matrix execution so a stalled test leg does not block all data collection. Added `--skip-cross`/`--skip-runtime`, optional per-run timeout knobs (`--cross-timeout-s`, `--runtime-timeout-s`, default disabled), `--cargo-jobs` (default `1`) for deterministic compile parallelism in busy environments, `--prefer-test-binary` for direct execution of prebuilt `mod-*` test binaries from `<target-dir>/debug/deps` (avoids cargo rebuild overhead on warm targets), and default `NORITO_SKIP_BINDINGS_SYNC=1` injection (opt-out: `--no-skip-bindings-sync`) to avoid fresh-target JDK parity-check failures. CSV outputs include `timed_out` fields for both cross/runtime rows. Validation: `bash -n scripts/nexus/run_cross_runtime_matrix.sh` (ok), `scripts/nexus/run_cross_runtime_matrix.sh --help` (ok), guard-path check `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --skip-runtime` (expected fast failure with no-run selection), and missing-binary guard `scripts/nexus/run_cross_runtime_matrix.sh --skip-cross --runtime-runs 1 --prefer-test-binary --target-dir /tmp/iroha_no_such_target_dir` (expected fast failure: `Unable to locate executable mod-* test binary ...`). Additional smoke evidence: `/tmp/iroha_nexus_matrix_cross_smoke3/cross_dataspace_matrix.csv` contains completed new-schema cargo-path cross row (`run=1`, `exit=0`, `timed_out=0`, `soak_passed=1/1`, `duration_s=832`), `/tmp/iroha_nexus_matrix_cross_smoke4/cross_dataspace_matrix.csv` contains completed direct-binary cross row (`run=1`, `exit=0`, `timed_out=0`, `soak_passed=1/1`, `duration_s=78`), `/tmp/iroha_nexus_matrix_runtime_smoke/runtime_registration_matrix.csv` captured non-timeout runtime failure row (`exit=101`) on missing-JDK Norito parity path, `/tmp/iroha_nexus_matrix_runtime_smoke2/runtime_registration_matrix.csv` captured bounded-timeout row (`exit=124`, `timed_out=1`, `duration_s=600`), `/tmp/iroha_nexus_matrix_runtime_smoke4/runtime_registration_matrix.csv` captured successful direct-binary runtime row (`run=1`, `exit=0`, `timed_out=0`, `duration_s=33`, `pass_rate=100.0`, delta max `0/0`), and combined smoke `/tmp/iroha_nexus_matrix_smoke5/{cross_dataspace_matrix.csv,runtime_registration_matrix.csv}` captured successful cross+runtime rows in one invocation (`cross: exit=0 timed_out=0 duration_s=79`; `runtime: exit=0 timed_out=0 duration_s=21 pass_rate=100.0`).
Latest sync (Confidential localnet bounded all-peer wait hardening, 2026-02-22): updated `integration_tests/tests/zk_confidential_localnet.rs` to cap all-peer non-empty block waits at `15s` (`ALL_PEER_WAIT_TIMEOUT`) before falling back to quorum progression checks, preventing prolonged stalls when a minority peer times out during real Halo2-proof verification. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/debug/iroha3d IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1 confidential_public_and_shielded_three_hop_localnet` (ok, `34.01s`), and full suite rerun `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `20 passed`, `258.45s`). For comparison, a pre-hardening in-flight run completed at `20 passed` in `891.39s`. Remaining open for this area: keep observing long-run peer timeout frequency, but no blocking correctness failures remain in the confidential localnet suite.
Latest sync (formal model liveness closure + local Apalache verification, 2026-02-23): fixed the remaining Apalache liveness counterexample in `docs/formal/sumeragi/Sumeragi.tla` by encoding post-GST scheduler fairness operationally in `Next` (explicit enabled predicates + timeout/fault preemption guards), then revalidated `fast` and `deep` with a local Apalache 0.52.2 binary (`NoError` to bounded length 10 in both modes). Updated `docs/formal/sumeragi/README.md` to document the operational fairness approach because Apalache 0.52.x does not support `WF_` fairness operators inside checked temporal properties.
Remaining open from this formal tranche: first live PR confirmation of the `sumeragi_formal` GitHub Actions job (and optional image digest pinning policy decision) is still pending.
Latest sync (formal CI access restored, 2026-02-23): GitHub auth and GHCR resolution now work from this environment (`gh auth status` valid for `takemiyamakoto`; `ghcr.io/apalache-mc/apalache:latest` digest `sha256:14bde0b944fcdd9dbf0556dbe1cb3dcb04380ebf758e777c69be4f45c562548b`). Docker daemon is still unavailable locally, so local container fallback remains blocked until `/var/run/docker.sock` is available.
Latest sync (fair-scheduler regression coverage, 2026-02-23): added targeted regressions in `state_machine_fairness_model_tests.rs` for the timeout-preemption guard (`fair_scheduler_allows_timeout_before_gst_but_blocks_it_after_gst_when_progress_is_enabled`) and debt preservation on timeout preemption (`fair_scheduler_timeout_step_keeps_honest_progress_debt_when_it_preempts_enabled_event`). Validation: `cargo test -p iroha_core --lib state_machine_fairness_model_tests:: -- --nocapture` (ok: `7 passed`, `2 ignored`, `83.39s`) and `cargo test -p iroha_core --lib state_machine_fairness_model_tests:: -- --ignored --nocapture` (ok: `2 passed`, `175.02s`).
Latest sync (formal CI live-verification blocker details, 2026-02-23): attempted to verify the `sumeragi_formal` GitHub job and pin image digest from this environment, but external access is blocked (`gh auth status` reports invalid token for `takemiyamakoto`; `curl` to `ghcr.io` token endpoint fails DNS resolution). Local wiring is still validated in `.github/workflows/pr.yml` (`sumeragi_formal`, image `ghcr.io/apalache-mc/apalache:latest`, run step `bash ci/check_sumeragi_formal.sh`). Remaining open item is unchanged: first live PR run + optional digest pinning once GitHub/registry access is available.
Latest sync (deep fair-liveness closure + Apalache tag alignment, 2026-02-23): re-enabled fair-liveness checks in ignored deep topologies (`fair_model_permissioned_n10_equivalence_deep_safety_liveness_fairness`, `fair_model_npos_n13_equivalence_deep_safety_liveness_fairness`) by tightening fair-scheduler behavior in `state_machine_fairness_model_tests.rs` so post-GST `TimeoutTick` cannot preempt enabled non-timeout fair-progress events. Also aligned `scripts/formal/sumeragi_apalache.sh` default Docker image tag with workflow/docs (`ghcr.io/apalache-mc/apalache:latest`). Validation: `cargo test -p iroha_core --lib state_machine_fairness_model_tests:: -- --nocapture` (ok: `5 passed`, `2 ignored`, `110.64s`), `cargo test -p iroha_core --lib state_machine_fairness_model_tests:: -- --ignored --nocapture` (ok: `2 passed`, `182.10s`), `bash scripts/tests/sumeragi_model_tiers.sh deep` (ok: model `12 passed` in `91.76s`, non-ignored fairness `5 passed`, `2 ignored` in `83.22s`, ignored deep fairness `2 passed` in `184.59s`), and formal script checks (`bash -n scripts/formal/sumeragi_apalache.sh`, `APALACHE_ALLOW_DOCKER=0 ... fast` expected `127` without local Apalache binary).
Latest sync (Apalache CI wiring + runner fallback hardening, 2026-02-23): added PR CI job `sumeragi_formal` in `.github/workflows/pr.yml` running in `ghcr.io/apalache-mc/apalache:latest` and executing `ci/check_sumeragi_formal.sh` (`fast` + `deep`). Hardened `scripts/formal/sumeragi_apalache.sh` with deterministic fallback order (local `APALACHE_BIN` first, then Docker fallback via `APALACHE_DOCKER_IMAGE` when daemon is available) and explicit diagnostics when Docker exists but daemon is down. Documented usage in `docs/formal/sumeragi/README.md` and `ci/README.md`. Local validation: syntax checks pass, fallback messages behave as expected, and `scripts/formal/sumeragi_apalache.sh fast` currently exits `127` in this environment due missing local binary and unavailable Docker daemon (`/var/run/docker.sock` absent). Remaining open: verify the new CI job on a live PR run and pin image/version if policy requires digest pinning.
Latest sync (Sumeragi DA restart-recovery rerun closure, 2026-02-23): executed the previously-open clean rerun `cargo test -p integration_tests --test sumeragi_da sumeragi_rbc_recovers_after_peer_restart -- --nocapture` and the scenario now passes end-to-end (`1 passed`, `0 failed`, `finished in 259.60s`). Observed shutdown/log-flush warnings from `iroha_test_network` during teardown (`forcing peer shutdown after fatal signal`, `timed out waiting for log flush`) remained non-fatal; peers exited `status(0)` and the test assertion passed.
Latest sync (Sumeragi model runtime closure + deep-tier runner fix, 2026-02-23): reduced bounded model runtime in `crates/iroha_core/src/sumeragi/main_loop/tests/state_machine_model_tests.rs` and `crates/iroha_core/src/sumeragi/main_loop/tests/state_machine_fairness_model_tests.rs` by treating post-commit states as terminal in bounded BFS, collapsing byzantine counters under equivalence reduction, and caching fairness-enabled masks in adversarial fairness recursion. Fixed `scripts/tests/sumeragi_model_tiers.sh` deep ignored invocation (`-- --ignored --nocapture`). Deep ignored topology tests now run and pass with safety+liveness+fair-liveness under fair-scheduler timeout-preemption guards. Validation: `scripts/tests/sumeragi_model_tiers.sh fast` (model `12 passed` in `85.18s`, fairness `5 passed`, `2 ignored` in `82.62s`) and `scripts/tests/sumeragi_model_tiers.sh deep` (model `12 passed` in `92.46s`, non-ignored fairness `5 passed`, `2 ignored` in `87.74s`, ignored deep fairness topologies `2 passed` in `174.98s`).
Latest sync (Sumeragi fairness model + formal scaffolding, 2026-02-23): added `crates/iroha_core/src/sumeragi/main_loop/tests/state_machine_fairness_model_tests.rs` and wired it from `crates/iroha_core/src/sumeragi/main_loop/tests.rs` to model stake-weighted permissioned/NPoS safety+liveness under Byzantine faults with RBC causality constraints, weak-fairness starvation checks, and runtime stake-quorum parity assertions. Added tiered runners (`scripts/tests/sumeragi_model_tiers.sh` with `fast`/`deep`, `scripts/formal/sumeragi_apalache.sh` with `fast`/`deep`) and TLA+/Apalache assets (`docs/formal/sumeragi/Sumeragi.tla`, `Sumeragi_fast.cfg`, `Sumeragi_deep.cfg`, `README.md`). Validation: `scripts/tests/sumeragi_model_tiers.sh fast` passed (`state_machine_model_tests`: `12 passed` in `426.73s`; fairness suite: `5 passed`, `2 ignored` in `605.91s`). Remaining open in this tranche: Apalache execution is blocked in this environment until `apalache-mc` is installed (`scripts/formal/sumeragi_apalache.sh fast` currently reports tool missing).
Latest sync (missing-block in-flight progress accounting + canonical soak rerun, 2026-02-23): stabilized the remaining parallel `iroha_core` failures by isolating shared status globals in `commit_pipeline_uses_epoch_for_height_when_emitting_votes` and `da_gate_context_does_not_block_quorum_reschedule` (`crates/iroha_core/src/sumeragi/main_loop/tests.rs`), then reran `cargo test -p iroha_core --lib -q` (ok: `3480 passed; 0 failed; 2 ignored`). Reran the unchanged canonical Izanami command (`RUST_LOG=izanami::summary=info,izanami::workload=warn ... --duration 3600s --target-blocks 2000 --tps 5 --max-inflight 8 --workload-profile stable`) and captured artifacts at `/tmp/izanami_soak_20260223T043425_codex_3600s.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_lIA9iV`. The run failed with `no block height progress for 300s (quorum min height 138, strict min 65, target 2000, tolerated_failures 1)` at `crates/izanami/src/chaos.rs:1540`; terminal summary: `successes=398 failures=15 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=70 izanami_ingress_endpoint_unhealthy_total=59`. Peer spread at failure was `139/139/138/65`; ingress warnings showed repeated `/transaction` and `/v1/node/capabilities` timeouts on lagging endpoints. Remaining open: eliminate the single-peer stall/divergence path (stuck around height 65) so strict-min tracks quorum-min through the full 3600s window.
Latest sync (canonical 3600s soak rerun after stack-overflow recursion guard, 2026-02-22): reran the unchanged canonical Izanami command and captured artifacts at `/tmp/izanami_soak_20260222T131236_codex_3600s.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_yJqKuh`. The run failed with `no block height progress for 300s (quorum min height 127, strict min 127, target 2000, tolerated_failures 1)` at `crates/izanami/src/chaos.rs:1502`. Terminal summary: `successes=551 failures=17 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=80 izanami_ingress_endpoint_unhealthy_total=52`. Signal shift versus the earlier failing baseline: no peer stack-overflow aborts (all peer stderr logs empty), but throughput still converged-then-stalled at height `127/127/127/127` with repeated workload `transaction queued for too long` / `status_timeout_ms=20s` errors and ongoing `missing_qc`/`no proposal observed` warnings in peer stdout. Remaining open: clear the converged stall at fixed height under queue-timeout pressure and rerun the canonical 3600s soak.
Latest sync (canonical 3600s soak rerun after no-roster hardening, 2026-02-22): reran the unchanged canonical Izanami command and captured artifacts at `/tmp/izanami_soak_20260222T043549_codex_3600s.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_EDoxPo`. The run failed early with `no block height progress for 300s (quorum min height 30, strict min 30, target 2000, tolerated_failures 1)` at `crates/izanami/src/chaos.rs:1502` (exit code 1). Terminal summary: `successes=126 failures=18 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=55 izanami_ingress_endpoint_unhealthy_total=48`. New dominant failure signal: two peers aborted with `sumeragi-tick` stack overflow (`aware_elver`, `prevalent_mammoth`), then ingress degraded with repeated endpoint `Connection refused`/timeout + `transaction queued for too long` warnings. Remaining open: fix the `sumeragi-tick` stack-overflow path under this profile, then rerun the canonical 3600s soak and compare divergence/progress/no-roster counters against the 2026-02-22 baseline.
Latest sync (no-roster starvation root-cause hardening pass, 2026-02-22): landed deterministic no-roster bootstrap liveness hardening in `crates/iroha_core/src/sumeragi/main_loop{.rs,/votes.rs,/commit.rs,/reschedule.rs,/tests.rs}` by introducing explicit bootstrap state (`RosterReady`/`RosterBootstrapPending`/`RosterBootstrapExhausted`), ordered bootstrap reacquire before fail-closed (highest-QC fetch, parent-gap fetch, anchor range-pull, commit-topology refresh), and one deterministic refresh-then-single-fallback allowance per view before strict fail-closed. No-roster fallback budgeting now derives from active timing (`effective_no_roster_fallback_views = clamp(1..cap, ceil(quorum_timeout/propose_timeout))`, capped by config) while preserving deterministic once-per-view/height limits. Torii status now exposes no-roster diagnosis counters (`consensus_no_roster_fallback_total`, `consensus_no_roster_fallback_allowed_total`, `consensus_no_roster_fail_closed_total`) in `crates/iroha_torii/src/routing/consensus.rs`. Izanami fast profile now explicitly sets deterministic no-roster recovery knobs in `crates/izanami/src/chaos.rs` (`missing_qc_reacquire_window_ms=450`, `no_roster_fallback_views=2`, `no_roster_refresh_retry_per_view=2`). Validation passed: `cargo fmt --all`; `cargo test -p iroha_core --lib proposal_liveness_state_machine_tests:: -- --nocapture`; `cargo test -p iroha_core --lib no_roster_bootstrap_prevents_premature_fallback_suppression -- --nocapture`; `cargo test -p iroha_core --lib force_view_change_if_idle_reacquires_missing_qc_once_before_rotating -- --nocapture`; `cargo test -p iroha_core --lib unknown_prev_fallback_anchor_selects_deterministic_start_height -- --nocapture`; `cargo test -p iroha_torii status_snapshot_json_includes_recovery_reacquire_fields -- --nocapture`; `cargo test -p izanami derive_npos_timing_ -- --nocapture`; `cargo test -p izanami make_network_builder_applies_pipeline_time -- --nocapture`; `cargo test -p izanami make_network_builder_injects_npos_parameters -- --nocapture`. Remaining open: rerun the unchanged canonical 3600s Izanami soak and compare divergence/proposal/no-roster warning profiles against the 2026-02-22 baseline logs.
Latest sync (canonical 3600s soak rerun after sub-1s tuning tranche, 2026-02-22): reran the unchanged canonical Izanami command (`RUST_LOG=izanami::summary=info,izanami::workload=warn`, 4 peers, `--duration 3600s --target-blocks 2000 --tps 5 --max-inflight 8`) and captured artifacts at `/tmp/izanami_soak_20260222T001931_impl_3600s.log` and `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_I5J2ZU`. The run did not reach the 3600s horizon: strict divergence guard tripped early at `crates/izanami/src/chaos.rs:1459` with `divergence=9` (`quorum min=13`, `strict min=4`, threshold `2` over `30s`). Terminal summary: `successes=26 failures=0 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=7 izanami_ingress_endpoint_unhealthy_total=4`; peer stderr remained empty. Warning sample counts remained proposal/recovery-skew oriented (`no proposal observed for view before changing view=3`, `no-roster fallback budget exhausted=6`, signature mismatch indicators `0`). Remaining open: reduce early view/roster skew under low height so strict-min tracks quorum-min before throughput tuning can be meaningfully measured against the 3600s target.
Latest sync (sub-1s localnet tuning tranche implemented, 2026-02-21): updated Izanami fast-path consensus defaults in `crates/izanami/src/chaos.rs` for the under-1s objective by setting NPoS timing to `180ms/180ms` (`block_time_ms`/`commit_time_ms`), lowering DA quorum multiplier to `1`, explicitly writing DA availability knobs (`availability_timeout_multiplier=1`, `availability_timeout_floor_ms=250`), tightening recovery windows (`height_window_ms=600`, `missing_qc_reacquire_window_ms=250`, `deferred_qc_ttl_ms=600`, `missing_block_height_ttl_ms=600`), and adding deterministic timeout floors (`propose>=40ms`, `prevote>=60ms`, `precommit>=80ms`) in `derive_npos_timing`. Validation passed: `cargo test -p izanami derive_npos_timing_ -- --nocapture`, `cargo test -p izanami make_network_builder_applies_pipeline_time -- --nocapture`, `cargo test -p izanami make_network_builder_injects_npos_parameters -- --nocapture`. Remaining open: rerun canonical 3600s soak and confirm p95 commit latency <1s with reduced missing-qc/proposal-starvation warnings.

Latest sync (Nexus autoscale localnet repeated-cycle scale-in assertion follow-up, 2026-02-22): updated `integration_tests/tests/nexus/autoscale_localnet.rs` so cycle execution can explicitly require deterministic `scale-in` transition deltas after contraction. `run_expand_contract_cycle` now takes a strictness flag; the single-cycle localnet test keeps strictness disabled for baseline regression coverage, while the repeated-cycle localnet test enables strictness and fails immediately when contraction appears in status but `scale-in` transitions remain absent on quorum peers. Validation: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok, `13 passed`), `TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/iroha-test-network/release/iroha3d IROHA_TEST_SKIP_BUILD=1 NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture` (ok; `scale-in transitions 0/4`), and `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture` (fails in cycle 1 with explicit `scale-in transitions 0/4` quorum assertion). Remaining open: deterministic scale-in transition emission is still missing in localnet, blocking true multi-cycle expansion/contraction validation.
Latest sync (Nexus autoscale localnet transition-counter diagnostics follow-up, 2026-02-22): extended `integration_tests/tests/nexus/autoscale_localnet.rs` with per-peer autoscale transition snapshots derived from peer `run-*-stdout.log` markers (`applied deterministic lane autoscale scale-out/scale-in transition`), surfaced those counters in expansion fallback/timeout diagnostics, and logged cycle-phase transition deltas after expansion/contraction. Also stabilized heartbeat waits by removing premature abort on transient status-snapshot streaks (timeout-scoped retries) and skipping cooldown-clearance gating for cycle 1. Added local unit coverage for transition-marker parsing and baseline delta semantics. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok, `13 passed`), `TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/iroha-test-network/release/iroha3d IROHA_TEST_SKIP_BUILD=1 NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture` (ok; expansion via storage+progress fallback, `scale-out transitions 4/4`, `scale-in transitions 0/4`), and `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --nocapture` (fails in cycle 2 expansion; transition snapshot remains `scale_out=1, scale_in=0` across peers; no lane-1 status activity; occasional Torii status timeout on one peer under sustained probe load). Remaining open: functional autoscale lifecycle gap where deterministic scale-in transitions are not observed, preventing true contraction/re-expansion cycling.
Latest sync (Confidential localnet real backend proof coverage, 2026-02-22): updated `integration_tests/tests/zk_confidential_localnet.rs` to replace debug proof placeholders with real `halo2/ipa` fixture attachments from `iroha_core::zk::test_utils::halo2_fixture_envelope("halo2/ipa:tiny-add-v1", ...)` for `ZkTransfer`/`Unshield` flows. Localnet bootstrap now explicitly enables Halo2 verification (`zk.halo2.enabled=true`) and sets `confidential.verify_timeout_ms=600000`. Also added a quorum fallback in non-empty block progression waits (`wait_for_non_empty_quorum`) to handle minority lagging peers under heavier real-proof verification without relaxing correctness checks. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_zk_conf_localnet_fast NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/Users/takemiyamakoto/dev/iroha/target/debug/iroha3d IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1 confidential_public_and_shielded_three_hop_localnet` (ok, `40.64s`), and full confidential suite `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `17 passed`, `345.19s`).
Latest sync (Nexus cross-dataspace/runtime-registration matrix follow-up, 2026-02-22): expanded stability validation with a 5x5 rerun matrix after soak timeout/retry hardening (`SOAK_PHASE_WAIT_TIMEOUT=32s`, `SOAK_ITERATION_ATTEMPTS=3`). Cross-dataspace matrix passed all `5/5` runs with strict soak `10/10` each run (`soak_fallbacks` range `3..8`, no soak retries, no swap non-convergence fallbacks). Runtime registration matrix passed all `5/5` runs with `pass_rate=100.0%`; submitter/cluster height-delta max remained `0` in every run and is now explicitly treated as expected behavior for control-plane `/v1/nexus/lifecycle` state mutation (`crates/iroha_torii/src/routing.rs`), not a missing-commit regression. Remaining non-functional gap: repeated `cargo test` loops can include intermittent in-run rebuild/startup overhead that inflates wall-clock timing outliers.
Latest sync (Testus remaining-gap closure for public localnet churn/load harness, 2026-02-21): completed the pending hardening in `integration_tests/tests/testus_public_localnet.rs` by adding enforced lag-cycle thresholds (`IROHA_TESTUS_MAX_LAGGED_CYCLE_RATIO`, default `0.35`), adaptive settle/churn timing so short runs still execute churn (`effective_final_settle_window`, `initial_churn_delay`), committed/finalized TPS assertions from validator-min `txs_approved` deltas (`IROHA_TESTUS_MIN_COMMITTED_TPS_RATIO`, default `0.6`), strict all-validator final convergence after quorum convergence, and an absolute transient skew cap (`IROHA_TESTUS_MAX_TRANSIENT_HEIGHT_SKEW`, default `10`). Summary output now includes `committed_tps` and `committed_txs_min_delta`, and helper coverage was added for settle-window/churn-delay/lag-threshold/approved-tx helpers. Validation: `rustfmt --edition 2024 integration_tests/tests/testus_public_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_patch cargo test -p integration_tests --test testus_public_localnet --no-run` (ok), focused helper tests (`simulation_config_defaults_are_valid`, `next_churn_deadline_`, `effective_final_settle_window_scales_with_duration`, `initial_churn_delay_stays_inside_churn_window`, `max_allowed_lagged_cycles_uses_ceiling_ratio`, `min_txs_approved_returns_lowest_counter`) all passed, `... IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=240 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 ... testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (ok, `261.89s`; summary `target/testus-localnet/testus-simulationETSkKD/localnet/testus_simulation_summary.json`: join/leave `2/2`, process churn `2`, lagged cycles `membership=1`, `process=0`, `submitted_tps=6.144`, `committed_tps=6.068`, `max_height_skew_observed=3`), and short-duration verification `... IROHA_TESTUS_SIM_DURATION_SECS=90 ...` (ok, `92.78s`; summary `target/testus-localnet/testus-simulationfAe0kf/localnet/testus_simulation_summary.json`: join/leave `1/1`, process churn `1`, lagged cycles `0/0`, `submitted_tps=7.243`, `committed_tps=5.950`).
Latest sync (Nexus autoscale localnet status-first expansion signaling follow-up, 2026-02-21): updated `integration_tests/tests/nexus/autoscale_localnet.rs` to add `SumeragiStatus` lane-commitment snapshots (`/v1/sumeragi/status`) into expansion quorum detection and enforce status-first expansion observation with delayed storage fallback (`EXPANSION_STATUS_SIGNAL_GRACE=8s`). Expansion now accepts either lane TEU status (`capacity || committed`) or lane commitment activity (`tx_count || teu_total`) on quorum peers, with explicit fallback diagnostics logging status-signal coverage before storage acceptance. Added unit coverage `expansion_accepts_sumeragi_lane_commitment_activity_on_quorum_peers`. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok, `5 passed`), `IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=$PWD/target/iroha-test-network/release/iroha3d KAGAMI_BIN=$PWD/target/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) /tmp/iroha_target_autoscale_gapfill/debug/deps/mod-8c7afc1a90b9898d nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet --nocapture` (ok, `51.96s`), and `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet --nocapture` (ok, `66.81s`). Remaining observed gap from live runs: status signal stayed `0/3` so expansion still converged via delayed storage fallback in both tests.
Latest sync (Nexus autoscale repeated-cycle status-transition follow-up, 2026-02-22): extended `integration_tests/tests/nexus/autoscale_localnet.rs` with baseline-aware expansion signals so status checks can detect lane transitions even when absolute counters are sparse: lane declaration transitions (`lane_governance`/lane presence) plus lane-progress transitions (lane status deltas, commitment `block_height`/counter deltas, and relay-envelope `block_height` deltas). Added unit coverage for both declaration-transition and progress-transition paths. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok, `7 passed`), `IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad KAGAMI_BIN=$PWD/target/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) /tmp/iroha_target_autoscale_gapfill/debug/deps/mod-8c7afc1a90b9898d nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet --nocapture` (ok, expansion via status lane activity, `expansion wait 7.480s`), and `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet --nocapture` (ok; cycle1 expansion via status `5.327s`, cycle2 still fallback after `8.996s` with status signal `0/3`).
Latest sync (Nexus autoscale repeated-cycle lifecycle-signal + probe-window load follow-up, 2026-02-22): extended `integration_tests/tests/nexus/autoscale_localnet.rs` with lane-validator lifecycle snapshots from `GET /v1/nexus/public_lanes/{lane}/validators` (including activation metadata), wired them into baseline transition detection, added fallback reason diagnostics (`ExpansionSignalBreakdown`), and added probe-window load intensification after storage grace (`EXPANSION_POST_STORAGE_TOP_UP_TX_COUNT=16`) to force lane-activity status before fallback. Added unit coverage `expansion_accepts_lane_validator_transition_on_quorum_peers` and `expansion_probe_top_up_intensifies_after_storage_grace`. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/tmp/iroha_target_autoscale_gapfill cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok, `9 passed`), `IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad KAGAMI_BIN=$PWD/target/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) /tmp/iroha_target_autoscale_gapfill/debug/deps/mod-8c7afc1a90b9898d nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet --nocapture` (ok; expansion via status/lifecycle transitions, `expansion wait 8.124s`), and `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet --nocapture` (ok; cycle1 via status/lifecycle transitions, cycle2 fallback after `16.894s`, status signal `0/3`, breakdown `active_capacity=0`, `commitment_activity=0`, `lane_declaration=4`, `lane_declaration_transition=0`, `lane_progress_transition=0`, `lane_validator_snapshot=4`, `lane_validator_activity=0`).
Remaining observed follow-up (still open): repeated-cycle autoscale still reaches storage expansion without lane-activity/status-progress deltas on quorum peers in cycle2; status surfaces lane declaration on all peers but no dynamic transitions. Treat this as a telemetry/status semantics gap in Nexus/Sumeragi status plumbing for re-expansion (not a functional autoscale failure), since expand+contract behavior remains correct in localnet.
Latest sync (Confidential localnet ZkNative shielded-asset 3-hop coverage, 2026-02-21): extended `integration_tests/tests/zk_confidential_localnet.rs` with `confidential_zknative_asset_three_hop_localnet`, validating a shielded-only asset mode (`ZkAssetMode::ZkNative`) by executing three `ZkTransfer` hops on localnet and asserting no transparent balance exposure for the source account. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `TEST_NETWORK_BIN_IROHAD=target/debug/iroha3d IROHA_TEST_SKIP_BUILD=1 IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1 confidential_zknative_asset_three_hop_localnet` (ok, `34.54s`), `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `15 passed`, `296.51s`), and compile guard `cargo test -p integration_tests --test mod --no-run` (ok).
Latest sync (Confidential localnet dedicated shielded-asset 3-hop coverage, 2026-02-21): extended `integration_tests/tests/zk_confidential_localnet.rs` with `confidential_shielded_asset_three_hop_localnet`, isolating the second requested flow as its own localnet scenario: register/mint a confidential-capable asset, shield funds, run three `ZkTransfer` hops, and assert post-hop public balance invariants. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `TEST_NETWORK_BIN_IROHAD=target/debug/iroha3d IROHA_TEST_SKIP_BUILD=1 IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1 confidential_shielded_asset_three_hop_localnet` (ok, `36.26s`), `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `14 passed`, `216.43s`), and compile guard `cargo test -p integration_tests --test mod --no-run` (ok).
Latest sync (Nexus autoscale localnet strict transition validation, 2026-02-21): reworked `integration_tests/tests/nexus/autoscale_localnet.rs` to validate multilane expansion/contraction against dynamic commit quorum and observed runtime activity, not fixed thresholds. Added resilient status snapshot retries, lane-active checks using `capacity || committed`, storage-provisioning expansion signal, unique load payload sequencing (`LOAD_TX_SEQUENCE`), and per-cycle chain-progress gating to prevent false-positive repeated cycles. Added unit test coverage for storage expansion quorum semantics. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (ok, 4 passed), direct single-cycle localnet run `... target/debug/deps/mod-8c7afc1a90b9898d nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet --nocapture` with `IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=$PWD/target/iroha-test-network/release/iroha3d KAGAMI_BIN=$PWD/target/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d)` (ok, ~59.33s), and direct repeated-cycle run `... nexus::autoscale_localnet::nexus_autoscale_repeats_expand_contract_cycles_in_localnet --nocapture` (ok, ~60.43s).
Latest sync (Nexus cross-dataspace strict-soak gap closure, 2026-02-21): finalized hardening in `integration_tests/tests/nexus/cross_dataspace_localnet.rs` after repeated-sweep failures by increasing setup retry tolerance (`SETUP_REGISTER_MINT_ATTEMPTS=3`) with barrier-timeout query fallback, adding bounded per-iteration soak retries (`SOAK_ITERATION_ATTEMPTS=2`) and higher soak wait budgets, classifying rollback `"transaction queued for too long"` as inconclusive for committed-history fallback, and deferring successful/reverse swap fallback barrier-timeout failures to strict balance assertions instead of immediate abort. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/cross_dataspace_localnet.rs` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (ok, `287.01s`; soak `10/10`; health `setup_retries=0 swap_fallbacks=1 soak_fallbacks=10 soak_retries=0`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (ok, `15.80s`; pass_rate `100.0%`). Pre-fix 3-run sweep captured and closed two concrete gaps: rollback misclassification on `"transaction queued for too long"` and premature failure on successful-swap fallback height-barrier timeout.
Latest sync (Confidential localnet transparent-only transfer policy coverage, 2026-02-21): extended `integration_tests/tests/zk_confidential_localnet.rs` with `confidential_transfer_rejected_in_transparent_only_mode`, validating that `ZkTransfer` is rejected when `asset_mode=Hybrid` but transparent-only policy toggles disable shield/unshield operations (`allow_shield=false`, `allow_unshield=false`), with post-attempt public-balance invariants and chain-progress barriers. Validation: `rustfmt --edition 2024 integration_tests/tests/zk_confidential_localnet.rs` (ok), `TEST_NETWORK_BIN_IROHAD=target/debug/iroha3d IROHA_TEST_SKIP_BUILD=1 IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1 confidential_transfer_rejected_in_transparent_only_mode` (ok, `18.36s`), `... cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (ok, `13 passed`, `217.14s`), and compile guard `cargo test -p integration_tests --test mod --no-run` (ok; existing dead-code warning in `integration_tests/tests/nexus/autoscale_localnet.rs`).
Latest sync (Testus churn backoff full-duration revalidation, 2026-02-21): reran the 3600s localnet simulation after adding lag-aware churn pacing/backoff in `integration_tests/tests/testus_public_localnet.rs`. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_patch KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=3600 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (`test ... ok`, `finished in 3602.06s`; summary `target/testus-localnet/testus-simulationxJjAuL/localnet/testus_simulation_summary.json`: `duration_secs=3600`, join/leave `21/21`, process churn `48`, lagged cycles `membership=11`, `process=0`, `submitted_tps=6.547`, zero submit errors, `max_height_skew_observed=8`, view-change metrics `0/0/0`).
Latest sync (Nexus cross-dataspace retryable setup-barrier + swap fallback follow-up, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to make setup `register+mint` barrier timeouts retryable inside the bounded attempt loop, add post-height-barrier committed-outcome rechecks for successful/reverse swap fallback confirmation, and split swap blocking timeout from rollback timeout (`SWAP_BLOCKING_CONFIRMATION_TIMEOUT=30s`; rollback remains `20s`). Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, `460.76s`; soak `iterations=10 pass_rate=100.0%`; phase total `460.600s`; setup retry path exercised) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass, `14.95s`; submit `89.366/93.003/95.260ms`, commit/apply `1.670/2.303/4.520ms`, visibility `2.544/22.701/100.731ms`, total `93.752/118.007/194.067ms`, pass_rate `100.0%`).
Latest sync (Testus churn backoff + lag telemetry hardening, 2026-02-21): updated `integration_tests/tests/testus_public_localnet.rs` so churn pacing adapts when interim convergence lags. Churn cycle helpers now return lag state, the simulation schedules next churn via `next_churn_deadline(...)`, and lagging cycles apply deterministic delay (`INTERIM_LAG_CHURN_BACKOFF_SECS=30`) instead of immediately re-entering churn. Summary output now includes `process_churn_lagged_cycles` and `membership_churn_lagged_cycles`. Added helper coverage: `next_churn_deadline_uses_interval_without_lag`, `next_churn_deadline_adds_backoff_when_lagged`. Validation passed: `rustfmt --edition 2024 integration_tests/tests/testus_public_localnet.rs`, `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_patch cargo test -p integration_tests --test testus_public_localnet next_churn_deadline_ -- --nocapture` (2 passed), `... process_churn_index_ -- --nocapture` (2 passed), `... testus_localnet_bootstrap_7_validators -- --nocapture --test-threads=1` (`27.67s`), `... testus_localnet_joiner_register_unregister_behavior -- --ignored --nocapture --test-threads=1` (`13.71s`), `... testus_localnet_restart_catchup_behavior -- --ignored --nocapture --test-threads=1` (`8.01s`), `... IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=240 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 ... testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (ok, `256.53s`; summary `target/testus-localnet/testus-simulationuXn0Tt/localnet/testus_simulation_summary.json`: join/leave `2/2`, process churn `3`, lagged cycles `membership=0`, `process=0`, zero submit errors), `... IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=900 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 ... testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (ok, `910.91s`; summary `target/testus-localnet/testus-simulationkkaBA2/localnet/testus_simulation_summary.json`: join/leave `8/8`, process churn `15`, lagged cycles `membership=2`, `process=0`, `submitted_tps=6.450`, zero submit errors; runtime logs confirm backoff emission), and compile guard `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_patch cargo test -p integration_tests --test testus_public_localnet --no-run` (ok).
Latest sync (Nexus cross-dataspace setup/swap stabilization + soak fallback recovery, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to keep setup phases pipelined (non-blocking submit + single barrier) while hardening convergence with bounded `register+mint` retries, switched forward/reverse correctness swaps to blocking submit with bounded-timeout inconclusive fallback handling, moved tick-assist traffic to stable observer clients, and fixed soak fallback targeting to use pre-submit height with a secondary committed-outcome check before counting fallback failures. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, `260.70s`; soak `iterations=10 pass_rate=100.0%`; phase total `260.630s`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass, `13.46s`; submit `92.540/99.826/104.853ms`, commit/apply `1.746/1.799/1.848ms`, visibility `2.708/2.746/2.792ms`, total `97.165/104.372/109.414ms`, pass_rate `100.0%`).
Latest sync (Testus public localnet 3600s churn/load soak revalidation, 2026-02-21): reran `integration_tests/tests/testus_public_localnet.rs` with process churn + membership join/leave churn and fixed `5 TPS` load on localnet using skip-build binaries (`KAGAMI_BIN=$PWD/target/debug/kagami`, `TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad`, `IROHA_TEST_SKIP_BUILD=1`) and `IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=3600 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5`. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_patch cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (`test ... ok`, `finished in 3642.66s`; summary `target/testus-localnet/testus-simulationaiuWN3/localnet/testus_simulation_summary.json`: `duration_secs=3635`, join/leave `19/19`, process churn `36`, `tx_sent=2889`, `tx_submit_errors=0`, `submitted_tps=7.042`, `max_height_skew_observed=7`, `view_changes_start=0`, `view_changes_end=0`, `view_change_rate_per_sec=0`). Follow-up focused checks also passed with the same target/binaries: `... testus_localnet_bootstrap_7_validators -- --nocapture --test-threads=1` (`27.67s`), `... testus_localnet_joiner_register_unregister_behavior -- --ignored --nocapture --test-threads=1` (`13.44s`), and `... testus_localnet_restart_catchup_behavior -- --ignored --nocapture --test-threads=1` (`8.43s`).
Latest sync (Nexus autoscale repeated-cycle localnet coverage, 2026-02-21): extended `integration_tests/tests/nexus/autoscale_localnet.rs` with reusable cycle logic plus ignored localnet regression `nexus_autoscale_repeats_expand_contract_cycles_in_localnet`, validating two consecutive expand→contract cycles in one 4-peer network session. Added unit coverage for contraction-profile semantics (`contraction_profile_accepts_absent_or_zero_capacity_elastic_lane`). Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=target/codex-test KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (3 passed), `... cargo test -p integration_tests --test mod nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture --test-threads=1` (pass; startup `18.867s`, cycle1 expansion `5.106s`, cycle1 contraction `0.014s`, total `24.209s`, log `/tmp/autoscale_single_cycle_post_multicycle_20260221T162928.log`), and `... cargo test -p integration_tests --test mod nexus_autoscale_repeats_expand_contract_cycles_in_localnet -- --ignored --nocapture --test-threads=1` (pass; startup `36.012s`, cycle1 expansion `4.132s`, cycle1 contraction `0.010s`, cycle2 expansion `0.009s`, cycle2 contraction `0.008s`, total `40.598s`, log `/tmp/autoscale_multi_cycle_ignored_20260221T163143.log`).
Latest sync (Nexus cross-dataspace tx-outcome confirmation + hybrid soak fallback hardening, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to use tx-entrypoint committed-outcome confirmation for setup grants and successful/reverse swaps (submit prebuilt tx -> wait committed outcome -> assert state), reducing reliance on pure height-proxy barriers. Soak confirmation is now hybrid: committed-outcome first with automatic fallback to height-tick barrier when outcome lookup is inconclusive, improving report-only soak stability under load. Rollback recovery keeps bounded-first behavior and committed-history reason lookup, with a final uncapped blocking submit only as a last resort when history lookup is also inconclusive, preserving strict underfunded reason assertions (`requires 10000`) and rollback balance invariants. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, `394.26s`; soak pass_rate `100.0%`; rollback phase `19.881s`; total phase runtime `394.180s`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass, `14.62s`; submit `142.547/167.024/198.926ms`; commit/apply `3.593/11.941/21.762ms`; visibility `3.955/12.103/26.960ms`; total `173.100/191.068/211.954ms`).
Latest sync (Nexus autoscale localnet stability recheck, 2026-02-21): reran the named multilane autoscale regression `nexus_autoscale_expands_and_contracts_lanes_in_localnet` in `integration_tests/tests/nexus/autoscale_localnet.rs` with skip-build binaries and confirmed deterministic expand/contract behavior on a 4-peer localnet. Validation: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=target/codex-test KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 cargo test -p integration_tests --test mod nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture --test-threads=1` (pass, `1 passed`, runtime `27.73s`; startup `17.838s`, expansion `9.456s`, contraction `0.069s`, total `27.583s`, log `/tmp/autoscale_localnet_recheck_named_20260221T153447.log`) plus a 10-run stress loop of the same command (log `/tmp/autoscale_localnet_named_stress_10run_20260221T153742.log`) with `10/10` pass and timing stats: startup min/avg/max `16.467/26.915/40.033s`, expansion `5.188/15.431/31.618s`, contraction `0.011/0.074/0.261s`, total runtime `25.156/43.212/65.267s`.
Latest sync (Testus public localnet 1800s churn/load soak revalidation, 2026-02-21): reran `integration_tests/tests/testus_public_localnet.rs` with process churn + membership join/leave churn and fixed `5 TPS` load on localnet using skip-build binaries (`KAGAMI_BIN=$PWD/target/debug/kagami`, `TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad`, `IROHA_TEST_SKIP_BUILD=1`) and `IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=1800 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5`. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_patch cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (`test ... ok`, `finished in 1838.23s`; summary `target/testus-localnet/testus-simulationukCJiG/localnet/testus_simulation_summary.json`: `duration_secs=1835`, join/leave `11/11`, process churn `21`, `tx_sent=2841`, `tx_submit_errors=0`, `submitted_tps=6.480`, `max_height_skew_observed=5`, `view_changes_start=0`, `view_changes_end=0`, `view_change_rate_per_sec=0`).
Latest sync (Nexus cross-dataspace barrierized swaps + bounded rollback-history fallback, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` so setup grants and successful/reverse swaps use enqueue + explicit barrier + strict state assertions (replacing capped blocking confirm waits on those paths), and changed rollback rejection recovery from a final uncapped blocking submit to bounded committed-history lookup (`ROLLBACK_HISTORY_RETRY_TIMEOUT=4s`, `ROLLBACK_HISTORY_FALLBACK_TIMEOUT=25s`) after bounded blocking retries. Added committed-rejection detail rendering (`Display + Debug`) so the strict `requires 10000` assertion remains deterministic. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, `353.29s`; rollback phase `60.591s`, soak pass_rate `60.0%`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass, `21.90s`; submit `219.201/284.929/333.192ms`, commit/apply `3.185/23.903/70.189ms`, visibility `5.447/15.731/33.587ms`, total `263.635/324.563/409.735ms`).
Latest sync (Nexus autoscale localnet deterministic scale-out tuning, 2026-02-21): finalized `integration_tests/tests/nexus/autoscale_localnet.rs` reliability hardening for multilane expansion/contraction by replacing aggressive adaptive top-up with deterministic burst cadence (`16 tx` every 10 heartbeats + `32 tx` reinforcement every 30 heartbeats) and tuning test-only autoscale thresholds to avoid latency-gate misses under slow startup (`scale_out_latency_ratio=0.2`, `scale_in_latency_ratio=0.15`, `scale_out_window_blocks=2`). Kept minimum-peer expansion convergence detection (3/4 peers) and contraction semantics checks unchanged. Validation: `rustfmt --edition 2024 integration_tests/tests/nexus/autoscale_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=target/codex-test KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 cargo test -p integration_tests --test mod nexus::autoscale_localnet::tests:: -- --nocapture` (2 passed), and clean skip-build 10-run stress sweep `... cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture` looped 10x (log `/tmp/autoscale_finalcfg_10run_20260221T145529.log`) with `10/10` pass, including slow-start cases up to `39.666s` startup.
Latest sync (Nexus cross-dataspace blocking-confirmation cap + rollback reason fallback, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to cap confirmation waits (`BLOCKING_CONFIRMATION_TIMEOUT=20s`) for setup grants and correctness-path swaps, treating timeout-class blocking outcomes as inconclusive where strict permission/balance assertions remain authoritative. For failing underfunded rollback, capped retries are explicitly limited to `ROLLBACK_CAPPED_ATTEMPTS=2` on fresh leader-targeted submitters before escalating to one final uncapped blocking fallback when both capped attempts are inconclusive, preserving strict rejection-reason verification (`requires 10000`) and rollback balance invariants. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, latest `377.68s`; setup-grants confirm capped to `20.839s` on inconclusive confirmation; rollback reason recovered via final uncapped fallback after two capped attempts; soak report-only pass_rate `50.0%`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`; submit `199.929ms/1.027s/3.574s`; commit/apply `3.696/14.165/33.995ms`; visibility `2.861/24.075/68.907ms`; total `272.532ms/1.066s/3.630s`).
Latest sync (canonical 3600s soak rerun after low-load liveness fixes, 2026-02-21): reran canonical command with unchanged Izanami parameters and long-run target dir (`/tmp/izanami_soak_20260221T225206_impl_rerun.log`, network `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_Gmn4g9`) and observed a materially improved failure mode: no strict-divergence trip, peers stayed converged through the run (last live sample `1426/1426/1426/1426`), and all peer stderr logs stayed empty. Run still ended with `izanami run stopped before target blocks reached` (`crates/izanami/src/chaos.rs:1386`, exit code 1) with summary counters `successes=6839 failures=1 expected_failures=0 unexpected_successes=0 izanami_ingress_failover_total=27 izanami_ingress_endpoint_unhealthy_total=8`. One workload warning surfaced for duplicate `register_domain` (`chaos_child_342`) in retry path. Remaining open: close the throughput gap to reach `target-blocks=2000` within the fixed 3600s window while preserving the new no-divergence behavior.
Latest sync (low-load consensus liveness fix implementation, 2026-02-21): implemented the missing-QC liveness plan across config/core/status/torii/block-sync: new recovery knobs in `iroha_config` (`missing_qc_reacquire_window_ms`, `max_forced_proposal_attempts_per_view`, `no_roster_refresh_retry_per_view`, `rotate_after_reacquire_exhausted`), proposal-liveness state machine and bounded leader forced-proposal/watchdog paths in `sumeragi/main_loop`, bounded missing-QC reacquire defer/rotate flow, no-roster refresh-retry budgeting, deterministic unknown-prev block-sync fallback anchors, and status/torii exposure of recovery counters (including `consensus_forced_proposal_*`, `consensus_missing_qc_reacquire_exhausted_total`, `consensus_missing_qc_rotation_deferred_total`, `consensus_no_roster_refresh_retry_total`). Validation passed: `cargo fmt --all`; `cargo test -p iroha_core --lib proposal_liveness_state_machine_tests:: -- --nocapture`; `cargo test -p iroha_core --lib unknown_prev_fallback_anchor_selects_deterministic_start_height -- --nocapture`; `cargo test -p iroha_torii --lib routing::status_tests::status_snapshot_json_includes_recovery_reacquire_fields -- --nocapture`. Remaining open: rerun canonical Izanami 3600s soak unchanged and compare divergence/proposal-path warnings against `/tmp/izanami_soak_20260221T212454_impl.log`.
Latest sync (canonical 3600s soak rerun after recovery hardening, 2026-02-21): reran the canonical long soak `RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` (`/tmp/izanami_soak_20260221T212454_impl.log`, network `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_1ehEeV`) and it failed fast on strict divergence: `height divergence exceeded safety window (divergence 9, threshold 2, window 30s, quorum min 80, strict min 71, target 2000, tolerated_failures 1)` at `crates/izanami/src/chaos.rs:1418` (exit code 1). Terminal counters: `successes=328`, `failures=0`, `izanami_ingress_failover_total=6`, `izanami_ingress_endpoint_unhealthy_total=3`; peer stderr remained empty and stdout stayed small (`4.0K..9.7K`). Persisted heights show one lagger (`79/79/79/69`), and warning clusters still point to proposal/recovery bottlenecks under skew (`no proposal observed before cutoff`=12 at heights 75/80, `missing block dwell exceeded view-change window`=4, `missing-block hard-cap`=1, `no-roster fail-closed`=1). Remaining open: tighten lagging-peer missing-block/proposal reacquire so strict-min tracks quorum-min without triggering divergence fail-fast, then rerun canonical 3600s soak for sustained convergence/throughput.
Latest sync (canonical soak rerun with strict divergence gate, 2026-02-21): ran the canonical 3600s Izanami command after the new tuning pass (`/tmp/izanami_soak_20260221T194957_postplan_impl.log`, network `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_yDePar`) and it exited early with `height divergence exceeded safety window (divergence 11, threshold 2, window 30s, quorum min 56, strict min 45, target 2000, tolerated_failures 1)` from `crates/izanami/src/chaos.rs:1418` (exit code 1). This confirms the prior long silent-stall mode is now detected quickly, but consensus skew remains unresolved under load. Summary counters at failure: `successes=207`, `failures=0`, `izanami_ingress_failover_total=8`, `izanami_ingress_endpoint_unhealthy_total=5`; peer stderr remained empty and peer stdout volume stayed small (`4.2K..7.8K`). Peer warning evidence points to a lagging missing-block/proposal pipeline (`missing-block hard-cap` + `range-pull ttl expired` + repeated `no proposal observed`/`missing_qc` view changes), with one peer capped at logged height `45` while others reached `55`. Remaining open: fix lagging-peer recovery/proposal acquisition path so strict-min tracks quorum-min without tripping the divergence guard, then rerun canonical 3600s soak to validate sustained convergence and throughput.
Latest sync (Izanami throughput/recovery plan implementation, 2026-02-21): landed the next consensus recovery and soak-safety tuning across `crates/izanami/src/chaos.rs` and `crates/iroha_core/src/sumeragi/main_loop{.rs,/qc.rs,/tests.rs}`. Izanami now writes stricter recovery knobs for canonical soaks (attempt caps/windows, hash-miss and range-pull escalation, retry backoff multiplier/cap, signer-fallback attempt bound), lowers NPoS timing to `400ms/600ms`, reduces RBC per-tick traffic while widening pacemaker backlog soft limits, and enforces a strict divergence guard (`quorum min - strict min > 2` for 30s => fail run) to surface unsafe lag before prolonged stalls. Core missing-block handling now applies exponential retry backoff per-attempt (capped) and escalates attempt streaks to range-pull before hitting hard-cap view-change thresholds. Validation passed: `cargo fmt --all`, `cargo test -p izanami make_network_builder_applies_pipeline_time -- --nocapture`, `cargo test -p izanami divergence_state_ -- --nocapture`, `cargo test -p izanami make_network_builder_forwards_rust_log_and_sets_peer_base_level -- --nocapture`, `cargo test -p izanami run_with_queue_timeout_retry -- --nocapture`, `cargo test -p iroha_core retry_missing_block_requests_backoff_scales_with_attempts_and_caps -- --nocapture`, `cargo test -p iroha_core missing_block_attempt_streak_escalates_to_range_pull_before_hard_cap -- --nocapture`. Remaining open: rerun canonical 3600s Izanami soak with these knobs and capture updated throughput, divergence behavior, and peer log-volume deltas.
Latest sync (Izanami canonical soak post-recovery rerun + peer log-level mitigation, 2026-02-21): reran canonical soak `RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` (`/tmp/izanami_soak_20260221T173428_postplan.log`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_exjbWe`). The previous height-279/300s no-progress failure mode is resolved: peers stayed converged through max height `1452` with `successes=6685`, `failures=0`, `izanami_ingress_failover_total=4`, `izanami_ingress_endpoint_unhealthy_total=0`. Remaining gap: run still exits with `izanami run stopped before target blocks reached` (`crates/izanami/src/chaos.rs:1297`) because `target-blocks=2000` is not reached within the 3600s window. Log volume remained high in that run (peer stdout `37.4M..45.7M`; dominant INFO lines: `block sync sampling peers for latest blocks`, `accepted RBC READY`, `sending RBC chunks to missing-block requester`). Landed mitigation in `crates/izanami/src/chaos.rs`: when forwarding `RUST_LOG` to peers, also force `logger.level=WARN` so targeted directives remain available but global INFO flood is suppressed; validated with `cargo test -p izanami make_network_builder_forwards_rust_log_and_sets_peer_base_level -- --nocapture` and `cargo test -p izanami run_with_queue_timeout_retry -- --nocapture`. Quick confirmation run with the same `RUST_LOG` pattern (`--duration 120s --target-blocks 100`, `/tmp/izanami_soak_20260221T184438_peerlogcheck.log`, network `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_OytBBJ`) reduced peer stdout logs to `1221`, `1217`, `1217`, and `1806` bytes (stderr all `0B`).
Latest sync (Izanami canonical 3600s soak rerun, 2026-02-21): reran the canonical long soak `RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` and it failed again with `no block height progress for 300s (quorum min height 279, strict min height 279, target 2000, tolerated_failures 1)` (`crates/izanami/src/chaos.rs:1334`, exit code 1). Run artifacts: `/tmp/izanami_soak_20260221T162808.log`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_A3GUVp`. Peer commits converged then froze at height 279 on all peers (last commit ~`2026-02-21T12:46:12Z..12:46:13Z`) while workload warnings repeatedly reported `transaction queued for too long` and `transaction.status_timeout_ms=20s` confirmation failures. Izanami failure summary now surfaced as expected (`izanami run finished with errors`, `successes=1228`, `failures=16`, `izanami_ingress_failover_total=95`, `izanami_ingress_endpoint_unhealthy_total=65`), and peers exited cleanly with empty stderr. Remaining open: resolve the height-279 consensus/ingress stall mode so canonical soak can progress beyond the 300s no-progress gate.
Latest sync (offline petal realtime + artifact validation, 2026-02-21): finalized the katakana-base94 distance-safe follow-up in `crates/iroha_cli/src/offline/petal.rs` with adaptive low-light marker thresholding and decode-path hardening so katakana symbol-state extraction no longer hard-fails on binary sampler misses. Realtime distance-safe tests now validate at the quality target dimension (`1024`) and pass. Validation: `cargo fmt --all -- crates/iroha_cli/src/offline/petal.rs`, `CARGO_TARGET_DIR=/tmp/iroha-petal-safe cargo test -p iroha_cli --bin iroha offline::petal::tests:: -- --nocapture` (29 passed). Produced real-data GIF artifacts from `fixtures/offline_bundle/aggregate_proof_fixture.json`: `artifacts/petal_real_katakana_distance_safe_gif/stream.gif`, `artifacts/petal_real_katakana_balanced_gif/stream.gif`, `artifacts/petal_real_binary_baseline_gif/stream.gif`; manifests confirm katakana balanced frames `<` binary baseline (18 vs 23 at 1024, style `sora-temple-command`). Realtime simulation reports on distance-safe PNG frames: pristine `artifacts/petal_real_katakana_distance_safe_v3_png/realtime_pristine_report_v2.json` (`41/41` decoded, stream complete), perturbation-on `artifacts/petal_real_katakana_distance_safe_v3_png/realtime_default_report_v2.json` (`40/41` decoded, stream complete). SHA256 roundtrip remains exact for decode outputs.
Latest sync (consensus recovery + Izanami soak follow-up, 2026-02-21): completed the next deterministic recovery pass across `iroha_core`, `iroha_config`, `izanami`, and torii test config coverage. Added missing-block retry backoff controls (`sumeragi.recovery.missing_block_retry_backoff_multiplier`, `sumeragi.recovery.missing_block_retry_backoff_cap_ms`) and wired effective-window gating in QC retry scheduling; added Izanami queue-timeout retry handling for submission failures classified as `transaction queued for too long`; ensured Izanami failure paths always emit terminal summary/error metrics; and unblocked torii test compilation by completing `connect_gating.rs` config literals for `network.deferred_send_*`, `sumeragi.fanout`, and recovery fields. Validation passed: `cargo fmt --all`, `cargo test -p iroha_config`, `cargo test -p iroha_core --lib retry_missing_block_requests_applies_configured_backoff_before_retrying -- --nocapture`, `cargo test -p izanami run_with_queue_timeout_retry -- --nocapture`, `cargo test -p izanami ingress_failover_marks_queue_timeout_retryable -- --nocapture`, `cargo test -p iroha_torii --tests --no-run`. Remaining open: rerun canonical 3600s Izanami soak + induced-failure >=4-peer integration scenarios to verify the new backoff/retry behavior closes lagging-peer stall windows end-to-end.
Latest sync (root-cause stabilization implementation, 2026-02-21): completed the deterministic recovery + ingress resilience sweep across `iroha_config`, `iroha_core`, and `izanami`. Landed simplified consensus knobs (`sumeragi.recovery.{height_attempt_cap,height_window_ms,hash_miss_cap_before_range_pull,no_roster_fallback_views}` + `sumeragi.fanout.{large_set_threshold,activity_lookback_blocks}` with legacy mapping), bounded no-roster fallback with fail-closed escalation and per-height budget reset semantics, deterministic large-set fanout committee selection wired into transport fanout/metrics, Izanami deterministic endpoint-pool failover for submit + query confirmation with cooldown/reprobe health tracking, and Sumeragi tick re-entrancy protection. Added focused tests for no-roster fail-closed budgeting, deterministic committee sizing/tie-break behavior, tick re-entrancy guard, and Izanami endpoint failover/cooldown paths. Validation passed: `cargo fmt --all`, `CARGO_TARGET_DIR=target-codex cargo check -p iroha_config -p iroha_core -p izanami`, `CARGO_TARGET_DIR=target-codex cargo test -p iroha_core actor_tick_skips_reentrant_invocation -- --nocapture`, `CARGO_TARGET_DIR=target-codex cargo test -p iroha_core --lib no_roster_fallback_is_bounded_and_fail_closed_once_per_view -- --nocapture`, `CARGO_TARGET_DIR=target-codex cargo test -p iroha_core --lib deterministic_fanout_committee_uses_formula_and_key_tiebreak -- --nocapture`, `CARGO_TARGET_DIR=target-codex cargo test -p izanami endpoint_pool_ -- --nocapture`, `CARGO_TARGET_DIR=target-codex cargo test -p iroha_config --test fixtures -- --nocapture`. Remaining open: rerun canonical 3600s Izanami soak + induced-failure >=4-peer integration scenarios to validate end-to-end convergence/progress gates under lagging-peer and single-endpoint degradation.
Latest sync (offline petal distance-safe stress-gate follow-up, 2026-02-21): added fail-fast capture-gate handling in `crates/iroha_cli/src/offline/petal.rs` so `offline petal eval-capture` exits as soon as the configured minimum success ratio becomes impossible while still scoring against the full planned attempt budget (`planned_attempts` denominator + `aborted_early` report flag). Added regression `capture_eval_success_ratio_uses_planned_attempt_budget`. Real-data capture checks for `artifacts/petal_real_katakana_distance_safe_v3_png/png` with fixture payload `fixtures/offline_bundle/aggregate_proof_fixture.json`: default profile (`--trials-per-frame 1 --min-success-ratio 0.95`) passes (`success_ratio=0.9512`, stream complete), while aggressive profile fails quickly (`attempts=6/41`, `aborted_early=true`, `success_ratio=0.0732`). Validation passed: `CARGO_TARGET_DIR=/tmp/iroha-petal-safe cargo test -p iroha_cli --bin iroha offline::petal::tests:: -- --nocapture`.
Latest sync (Nexus cross-dataspace blocking-confirmation cap + rollback reason fallback, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to cap confirmation waits on correctness-path swap submissions (`BLOCKING_CONFIRMATION_TIMEOUT=20s`) and treat timeout-class blocking outcomes as inconclusive for successful/reverse swap submission, with strict balance convergence checks remaining authoritative. For failing underfunded rollback, bounded retries on fresh leader-targeted submitters now escalate to a final uncapped blocking fallback only when all bounded attempts are inconclusive, preserving strict rejection-reason verification (`requires 10000`) and rollback balance invariants. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, latest `463.08s`; successful/reverse swap submit+confirm phases capped around `~21s` when inconclusive; rollback reason recovered via final fallback; soak report-only pass_rate `40.0%`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`; submit `199.929ms/1.027s/3.574s`; commit/apply `3.696/14.165/33.995ms`; visibility `2.861/24.075/68.907ms`; total `272.532ms/1.066s/3.630s`).
Latest sync (Nexus cross-dataspace resilience follow-up, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to tolerate transient Torii/status query failures during polling (height/balance/permission waits now retry until timeout budget, with last-query-error diagnostics), keep setup grants + correctness-critical forward/reverse swaps on blocking confirmation, and retry rollback underfunded submission on fresh leader-targeted clients when transient submitter-thread failures occur, while still enforcing the underfunded rejection reason and unchanged balances. Soak remains report-only with per-iteration leader retargeting and bounded wait windows; route probes retain strict queued-route checks with short approval cap and fallback attribution. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass, latest `333.41s`; route-probe `0.499s`; soak pass_rate `60.0%`; rollback underfunded rejection + rollback invariants asserted) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`; submit `109.550/180.774/237.117ms`; commit/apply `2.529/3.062/3.577ms`; visibility `4.509/8.408/20.920ms`; total `117.925/192.244/244.155ms`).
Latest sync (Testus membership/convergence hardening, 2026-02-21): updated `integration_tests/tests/testus_public_localnet.rs` so churn propagation and terminal settle checks follow resilient BFT quorum semantics under localnet churn. Membership join/leave checks now use validator-wide quorum visibility (`wait_for_peer_presence_across_clients` + `min_presence_matches`), final settle uses quorum convergence (`wait_for_cluster_convergence_quorum`), peer queries retry timeout-class errors (`PEER_QUERY_REQUEST_RETRIES=3`, `PEER_QUERY_REQUEST_RETRY_BACKOFF=200ms`), non-fatal interim convergence waits are bounded (`INTERIM_CONVERGENCE_TIMEOUT_SECS=45`), and leave-propagation lag is warning-only rather than aborting the run. Validation: `rustfmt --edition 2024 integration_tests/tests/testus_public_localnet.rs` (ok), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/Users/takemiyamakoto/dev/iroha/target/testus_patch cargo test -p integration_tests --test testus_public_localnet --no-run` (ok), `... min_presence_matches_uses_bft_commit_quorum -- --nocapture` (ok), `... testus_localnet_bootstrap_7_validators -- --nocapture --test-threads=1` (ok), `... testus_localnet_joiner_register_unregister_behavior -- --ignored --nocapture --test-threads=1` (ok), `... testus_localnet_restart_catchup_behavior -- --ignored --nocapture --test-threads=1` (ok), and `... IROHA_TESTUS_KEEP_LOCALNET=1 IROHA_TESTUS_SIM_DURATION_SECS=420 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture --test-threads=1` (ok, `488.37s`; summary `target/testus-localnet/testus-simulationTYF55F/localnet/testus_simulation_summary.json`: join/leave `3/3`, process churn `4`, submitted TPS `6.024`, zero submit errors, max skew observed `5`).
Latest sync (Confidential localnet shielding/unshielding coverage, 2026-02-21): added `integration_tests/tests/zk_confidential_localnet.rs` with `confidential_public_and_shielded_three_hop_localnet`, covering a 4-peer confidential-enabled localnet flow for (1) public asset shield -> three shielded hops -> unshield -> public transfer to `BOB_ID`, and (2) a second asset registered as confidential with three shielded hops. Added `confidential_unshield_rejected_when_disabled`, `confidential_shield_rejected_when_disabled`, `confidential_shield_rejected_without_zk_registration`, and `confidential_unshield_rejected_with_stale_root_hint` to cover policy denials (`allow_unshield=false`, `allow_shield=false`), missing zk registration, and stale root-hint rejection, all with post-attempt balance invariants after chain progress. The scenarios use all-peer transaction submission plus non-empty block-height progression waits to avoid single-peer pipeline-status confirmation stalls before balance assertions. Added helper detector tests for transient-network and duplicate-transaction classification and revalidated the full file run. Validation: `TEST_NETWORK_BIN_IROHAD=target/debug/iroha3d IROHA_TEST_SKIP_BUILD=1 IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1 confidential_public_and_shielded_three_hop_localnet` (pass; initial run `113.89s`; repeat stability loop 3/3 passes in `157.99s`, `128.70s`, and `49.50s`), `... confidential_unshield_rejected_when_disabled` (pass; `35.30s`), `... confidential_shield_rejected_when_disabled` (pass; `69.88s`), `... confidential_shield_rejected_without_zk_registration` (pass; `35.66s`), `... confidential_unshield_rejected_with_stale_root_hint` (pass; `44.32s`), and `TEST_NETWORK_BIN_IROHAD=target/debug/iroha3d IROHA_TEST_SKIP_BUILD=1 IROHA_TEST_NETWORK_PARALLELISM=16 cargo test -p integration_tests --test zk_confidential_localnet -- --test-threads=1` (pass; full suite now `9 passed` in `224.98s`; prior full-suite runs passed in `216.80s`, `106.42s`, `167.71s`, and `175.38s`). Compile guard: `cargo test -p integration_tests --test mod --no-run` (pass; latest `2m 42s`, prior `1m 03s`/`4m 21s`).
Latest sync (Nexus cross-dataspace fallback/confirmation hardening, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to keep strict queued-route assertions for both explicit route probes while bounding approval waits (`ROUTE_PROBE_APPROVAL_WAIT_TIMEOUT=100ms`) and falling back to `submit_height+1` when approval events are delayed, with probe source attribution (`approved` vs `fallback+1`). Setup grants now use confirmed submission (`submit_all_blocking`) to avoid silent non-blocking misses, and the separate route-probe Bob propagation barrier was removed (later permission/balance checks and final soak sync still cover cross-peer visibility). Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass across repeat reruns; totals `292.009s`, `286.070s`, `252.694s`, `303.995s`, and `316.798s`; route-probe phase `0.388s` to `0.715s`; soak pass_rate `100.0%`), and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`; submit `114.395/164.248/296.024ms`; commit/apply `3.934/11.899/39.868ms`; visibility `7.241/23.898/74.896ms`; total `133.715/200.046/374.854ms`).
Latest sync (Nexus cross-dataspace route-probe cap + setup stall hardening, 2026-02-21): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to bound route-probe approval waiting (`ROUTE_PROBE_APPROVAL_WAIT_TIMEOUT=5s`) after strict queued-route assertion, emit probe source attribution (`approved` vs `fallback+1`), and remove the extra route-probe Bob propagation barrier. Added tick-assisted height waiting for setup register/mint confirmation (`wait_for_height_with_tick`, `HEIGHT_WAIT_TICK_EVERY_POLLS=10`) to avoid intermittent setup barrier timeouts under slow block advancement. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass; route-probe phase `5.282s`; total `382.894s`; soak report-only pass_rate `90.0%` with one iteration timeout), and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`, total min/avg/max `101.104/159.698/239.824ms`).
Latest sync (Testus joiner warning rate-limit follow-up, 2026-02-21): updated `integration_tests/tests/testus_public_localnet.rs` to rate-limit repeated joiner catch-up warnings with per-run state (`JoinerCatchupWarningState`) and first-and-interval logging (`JOINER_STALL_LOG_EVERY=5`, `JOINER_PROGRESS_LOG_EVERY=5`). Membership join churn now surfaces the first warning and every 5th consecutive progress/stall cycle instead of every cycle, reducing hour-long log noise while preserving persistent-signal visibility. Added focused unit coverage (`warning_state_logs_first_and_every_interval_for_stalls`, `warning_state_resets_stall_counter_after_progress`). Validation passed: `rustfmt --edition 2024 integration_tests/tests/testus_public_localnet.rs`, `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_soak_fast2 cargo test -p integration_tests --test testus_public_localnet warning_state_ -- --nocapture`, and `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_soak_fast2 KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=120 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (`test ... ok`, `finished in 122.56s`).
Latest sync (Testus churn catch-up tolerance follow-up, 2026-02-21): extended `integration_tests/tests/testus_public_localnet.rs` hardening by (1) keeping joiner progress-aware catch-up gating (`wait_for_height_or_progress`, `JoinerCatchupAssessment`, `JOINER_CATCHUP_TIMEOUT_SECS=60`, target=`baseline - INTERIM_CONVERGENCE_MAX_SKEW`) and (2) relaxing process-restart catch-up to a skew-tolerant target via `validator_restart_catchup_target(baseline)` with non-fatal lag logging, avoiding false failures when a restarted validator trails by 1-2 blocks under aggressive churn. Added unit coverage for both paths (`joiner_catchup_assessment_*`, `validator_restart_catchup_target_*`). Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_soak_fast2 cargo test -p integration_tests --test testus_public_localnet joiner_catchup_assessment_ -- --nocapture`, `... validator_restart_catchup_target_ -- --nocapture`, `... process_churn_index_skips_primary_when_multiple_validators -- --nocapture`, `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_soak_fast2 KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=120 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (`test ... ok`, `finished in 122.15s`), `... IROHA_TESTUS_SIM_DURATION_SECS=420 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 ... testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (`test ... ok`, `finished in 422.11s`), and `... IROHA_TESTUS_SIM_DURATION_SECS=3600 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 ... testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (`test ... ok`, `finished in 3612.94s`; recurring joiner catch-up progress/stall warnings stayed non-fatal).
Latest sync (Testus public localnet 3600s soak revalidation, 2026-02-21): reran `integration_tests/tests/testus_public_localnet.rs` with full-duration churn/load settings and latest stability hardening active (`INTERIM_CONVERGENCE_MAX_SKEW`, process-churn primary skip, Torii request timeout tuning, timeout-aware tx-burst short-circuit, and final settle window). Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_soak_fast2 cargo test -p integration_tests --test testus_public_localnet process_churn_index_skips_primary_when_multiple_validators -- --nocapture` and `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=$PWD/target/testus_soak_fast2 KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=3600 IROHA_TESTUS_LOAD_TPS=5 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (`test ... ok`, `finished in 3616.29s`; non-fatal joiner catch-up lag warnings persisted during membership join cycles).
Latest sync (Testus status-timeout retry follow-up, 2026-02-21): updated `integration_tests/tests/testus_public_localnet.rs` so validator status sampling retries timeout-class `/status` errors per peer (`collect_statuses` now uses `get_status_with_retry`, with `STATUS_REQUEST_RETRIES=3` and `STATUS_REQUEST_RETRY_BACKOFF=200ms`) instead of failing immediately on a single transient timeout. Added focused unit coverage for timeout detection (`http_timeout_error_detector_matches_timeout_messages`, `http_timeout_error_detector_ignores_non_timeout_messages`). Validation passed: `rustfmt --edition 2024 integration_tests/tests/testus_public_localnet.rs`, `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=/Users/takemiyamakoto/dev/iroha/target/testus_patch cargo test -p integration_tests --test testus_public_localnet http_timeout_error_detector_ -- --nocapture` (2 passed), and definitive patched soak revalidation `NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TESTUS_SIM_DURATION_SECS=420 /Users/takemiyamakoto/dev/iroha/target/testus_patch/debug/deps/testus_public_localnet-01ee6dcaa4b1bac3 testus_public_localnet_5tps_churn_stability --ignored --nocapture` captured via `/tmp/testus_patch_420_20260221T115526.log` (`test ... ok`, `finished in 664.71s`, `exit_code=0`; single non-fatal joiner stall warning observed).
Latest sync (Nexus cross-dataspace barrier follow-up + benchmark revalidation, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to keep concurrent `ds1`/`ds2` route probes while removing redundant Bob-height waiting from setup grants (permission visibility query remains the propagation gate), tightened balance/permission tick cadence (`10 -> 5` polls), and moved successful/reverse swap assertions to tick-assisted balance convergence. Validation: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (latest pass: total `306.578s`, setup grants barrier `9.540s`, setup/swap query phases sub-second, soak pass_rate `100.0%`, soak min/avg/max `10.124/19.048/30.081s`, soak barrier `9.934/18.493/28.232s`) and `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`, submit `159.512/195.297/236.202ms`, commit/apply `1.712/3.138/4.444ms`, visibility `4.354/6.636/8.287ms`, total `172.077/205.072/245.852ms`).
Latest sync (Nexus cross-dataspace route-probe stabilization, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to replace flaky `sumeragi_status.dataspace_commitments` probe checks with pipeline-event routing assertions at `TransactionStatus::Queued` (lane/dataspace), while still waiting for `Approved` to enforce commit progression. This keeps both explicit `ds1`/`ds2` probes and removes observer-dependent commitment races; a short SSE handshake delay was added before probe submit for queued-event reliability. Revalidated full swap semantics and soak path with `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate `100.0%`, total `285.317s`, soak min/avg/max `9.421/13.040/20.123s`) and reran registration isolation with `... cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`, total min/avg/max `149.723/203.065/320.872ms`).
Latest sync (Nexus autoscale localnet load-tuning + startup attribution, 2026-02-21): kept `LOAD_TX_COUNT=48` in `integration_tests/tests/nexus/autoscale_localnet.rs`, added per-phase timing instrumentation, and revalidated repeatedly. Long-tail reruns still pass but show high variance in build-enabled mode (`225.25s`, `166.35s`, `208.13s`, `376.59s`, `605.31s`, `665.16s`, `532.62s`), with phase logs attributing most variance to startup (`network startup: 609.021s`, expansion/contraction single-digit seconds). After rebuilding local binaries (`cargo build -p irohad -p iroha_kagami`) and running with `IROHA_TEST_SKIP_BUILD=1` plus explicit binary paths (`KAGAMI_BIN`/`TEST_NETWORK_BIN_IROHAD`), autoscale localnet stayed green with much lower startup/runtime (`network startup 16.415s`, total `22.726s`; steady-state rerun `network startup 14.491s`, total `25.268s`, wall `56s`; additional consecutive reruns `network startup 12.663s`, total `25.644s`, and `network startup 12.816s`, total `26.434s`; latest rerun `network startup 12.201s`, expansion `6.234s`, total `18.577s`). Additional skip-build stress evidence: one 5-run batch showed 4 passes + 1 expansion-timeout flake (`/tmp/autoscale_repeat_20260221T114131.log`, failed run at `135.19s` waiting for lane-1 provisioning), immediate rerun passed (`startup 12.572s`, `expansion 8.331s`, total `21.191s`), and a 3-run follow-up batch passed 3/3 (`/tmp/autoscale_followup_20260221T115600.log`, total min/avg/max `19.359/21.373/22.926s`). Post-flake hardening: `integration_tests/tests/nexus/autoscale_localnet.rs` now injects deterministic top-up load while awaiting expansion (every 10 heartbeat ticks, 16 tx burst), includes unit coverage (`nexus::autoscale_localnet::tests::expansion_top_up_cadence_is_deterministic`), and revalidated cleanly over 3/3 post-change reruns (`/tmp/autoscale_post_topup_20260221T120319.log`, startup min/avg/max `12.164/12.822/13.215s`, expansion `6.079/6.100/6.112s`, total `18.390/19.030/19.425s`) plus a post-change 5-run sweep with 5/5 pass (`/tmp/autoscale_post_topup_5run_20260221T120932.log`, startup min/avg/max `12.276/12.400/12.636s`, expansion `7.154/8.836/10.532s`, total `19.542/21.429/23.195s`). A larger post-topup 10-run sweep showed `9/10` pass (`/tmp/autoscale_post_topup_10run_20260221T122538.log`) with one expansion-timeout where 3/4 peers had already converged on elastic-lane storage/status; follow-up hardening now treats that deterministic minimum-peer convergence as expansion success (3/4) and adds helper coverage (`nexus::autoscale_localnet::tests::expansion_observed_when_minimum_peers_converge`). Post-threshold rerun is `5/5` pass (`/tmp/autoscale_post_threshold_5run_20260221T125344.log`, total min/avg/max `18.567/62.845/112.852s`), and a final post-threshold stress sweep is `10/10` pass (`/tmp/autoscale_post_threshold_10run_20260221T130806.log`, startup min/avg/max `12.252/12.530/13.616s`, expansion `5.084/6.014/7.120s`, total `17.457/18.646/20.824s`). Also fixed a compile unblock in `integration_tests/tests/nexus/cross_dataspace_localnet.rs` by using the existing runtime binding (`let (network, rt) = ...`) for route-probe waits.
Latest sync (Nexus cross-dataspace setup/swap stabilization, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to add explicit Bob permission-visibility gating after grants (`FindPermissionsByAccountId` with required-token checks) and to use blocking-confirmed submission for setup grants plus successful/reverse swap paths, removing false-positive barrier races before balance assertions. Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate `100.0%`, total `307.356s`, soak min/avg/max `8.301/14.251/26.649s`), `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate `100.0%`, total min/avg/max `92.079/100.388/111.731ms`), and compile sanity `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod --no-run` (ok, warning-free).
Latest sync (Nexus autoscale localnet hardening + pacemaker clamp, 2026-02-20): updated `integration_tests/tests/nexus/autoscale_localnet.rs` so expansion is asserted by provisioned Kura lanes (`1 -> 2`) while contraction is asserted by runtime lane deactivation (`lane 0` active, `lane 1` removed or `capacity=0`), and added short Torii request deadlines for autoscale probes to bound stalled `/status`/heartbeat calls. On core consensus path, `crates/iroha_core/src/state.rs` now applies autoscale lane lifecycle transitions without AXT refresh in block-apply, and pacemaker active-round tracking is clamped to local commit horizon in `crates/iroha_core/src/sumeragi/main_loop/propose.rs` with regression coverage in `crates/iroha_core/src/sumeragi/main_loop/tests.rs` (`pacemaker_clamps_live_height_to_local_commit_horizon`). Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=target/codex-test IROHA_TEST_NETWORK_KEEP_DIRS=1 cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet` (1 passed, 0 failed, 308.61s latest confirmation), repeat soak loops (`for i in 1 2; ... cargo test ...`) passed on both earlier and latest reruns (`705.28s` and `780.60s`; `382s` and `574s` wall clock with test runtimes `357.53s` and `428.83s`), `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=target/codex-test cargo test -p integration_tests --test mod --no-run`, and `NORITO_SKIP_BINDINGS_SYNC=1 CARGO_TARGET_DIR=target/codex-test cargo test -p iroha_core pacemaker_clamps_live_height_to_local_commit_horizon`.
Latest sync (Nexus reverse-swap barrier alignment + soak-target handoff, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` so reverse-swap barrier confirmation is anchored on Alice (assertion peer) and the resulting rolling `next_expected_height` seeds soak start directly (removing an extra status read and reducing Bob-lag sensitivity in swap-to-soak transition). Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate 100.0%, soak min/avg/max `0.032/10.642/14.520s`, barrier min/avg/max `0.005/10.455/14.368s`, total `289.798s`) and `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_SKIP_BUILD=1 TEST_NETWORK_BIN_IROHAD=/tmp/iroha_target_cross_opt_rolling/debug/irohad KAGAMI_BIN=/tmp/iroha_target_cross_opt_rolling/debug/kagami IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate 100.0%, total min/avg/max `91.905/106.478/138.984ms`).
Latest sync (Nexus barrier consolidation + soak loop trim, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to (1) keep submitter reuse with refresh every 3 soak iterations (`SOAK_SUBMITTER_REFRESH_EVERY`) and `target-refresh` telemetry, (2) replace per-iteration soak status polling with rolling `next_soak_target_height`, and (3) remove redundant cross-peer waits by using a single route-probe propagation wait on Bob and single-peer setup/swap confirmation barriers where assertions already enforce correctness. Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing --no-run`, `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate 100.0%, soak min/avg/max `2.873/6.041/9.741s`, barrier `2.868/5.994/9.629s`, target-refresh `0.000/0.042/0.173s`), and `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` (pass_rate 100.0%, total min/avg/max `53.193/67.010/98.552ms`). Context: some runs still show inflated startup when the test-network triggers one-time in-run `irohad --release` compilation.
Latest sync (consensus recovery continuation, 2026-02-20 late): closed remaining `iroha_core` compile/test blockers in the deterministic recovery sweep (`main_loop.rs`, `votes.rs`, `tests.rs`) by fixing borrow-ordering around missing-block fetch planning, stabilizing sidecar mismatch prune retention, and hardening idle view-change recovery regression setup while preserving missing-block pending behavior. Validation passed: `cargo fmt --all`, `CARGO_TARGET_DIR=/tmp/iroha_target_consensusfix cargo check -p iroha_core -p iroha_config -p iroha_p2p -p iroha_telemetry`, `CARGO_TARGET_DIR=/tmp/iroha_target_consensusfix cargo test -p iroha_core --lib missing_block_height_ -- --nocapture`, `... --lib missing_block_hash_miss_streak_escalates_to_range_pull ...`, `... --lib sidecar_mismatch_final_drop_trips_after_retry_cap ...`, `... --lib force_view_change_if_idle_triggers_even_when_missing_blocks_pending ...`, `... --lib quarantined_block_sync_qc_expiry_is_bounded ...`, `CARGO_TARGET_DIR=/tmp/iroha_target_consensusfix cargo test -p iroha_p2p --lib deferred_queue_ -- --nocapture`, and `... --lib missing_session_ -- --nocapture` (all green). Follow-up: canonical 3600s Izanami soak was relaunched with the fixed command and reached peer-directory bootstrap output; final pass/fail artifact is still pending completion.
Latest sync (Sumeragi stack-overflow hardening, 2026-02-20): removed default-stack risk on consensus worker threads by introducing a shared internal `sumeragi_thread_builder` in `crates/iroha_core/src/sumeragi/mod.rs` and routing all Sumeragi thread spawns through it (`sumeragi`, `sumeragi-tick`, queue workers, commit/validation/QC verify/vote verify, RBC persist/seed). Added regression unit coverage for the new helper. Validation passed: `cargo fmt --all`, `CARGO_TARGET_DIR=/tmp/iroha_target_stackfix cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_stackfix cargo test -p iroha_core --lib sumeragi_thread_builder_applies_requested_thread_name -- --nocapture`.
Latest sync (Izanami canonical 3600s rerun, 2026-02-20T20:23 local): reran the fixed soak command with unchanged parameters and captured logs at `/tmp/izanami_soak_20260220T202356_rerun.log` (network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_cljHEm`). The run bootstrapped and peers progressed to `~485`, then entered a persistent lagging split `485/485/485/473` where `advanced_onager` stopped committing (`last commit height=473 @ 2026-02-20T16:42:45Z`) while other peers held at `485` (`~16:43:29Z`). `advanced_onager` then spammed `telemetry sync failed; returning last metrics snapshot` (34 sampled) with no new commits. Recovery activity remained present (`missing_block_retry`, range-pull requests, occasional height hard-cap logs), but Izanami emitted neither progress summaries nor the expected `progress-timeout` failure line; the attempt had to be manually terminated after stall duration exceeded the configured 300s window. Remaining open: fix this harness-visible stuck mode (timeout not surfacing to Izanami output) and the lagging-peer stall itself, then rerun the canonical soak for a definitive pass/fail artifact.
Latest sync (deterministic round topology follow-up, 2026-02-20): removed remaining hash-coupled roster selection for active/future rounds by adding `canonical_round_roster_with_mode` in `crates/iroha_core/src/sumeragi/main_loop/votes.rs` and routing `roster_for_vote_with_mode` / `roster_for_new_view_with_mode` through canonical height-based derivation (commit topology, activation roster, commit-QC roll-forward without target hash). Updated proposal leader-index derivation in `crates/iroha_core/src/sumeragi/main_loop/proposal_handlers.rs` to use the same canonical round roster and restricted hash-coupled sidecar roster replay in `BlockCreated` processing to historical committed heights only. Added deterministic assertions in `crates/iroha_core/src/sumeragi/main_loop/tests.rs::conflicting_vote_does_not_override_first` so competing hashes at the same `(height, view)` resolve identical commit/new-view rosters. Validation passed: `cargo fmt --all`, `cargo test -p iroha_core --lib conflicting_vote_does_not_override_first -- --nocapture`, `cargo test -p iroha_core --lib proposal_updates_leader_index_status -- --nocapture`, `cargo test -p iroha_core --lib proposal_missing_highest_qc_triggers_missing_block_fetch -- --nocapture`. Remaining open: rerun the canonical 3600s soak and >=4-peer induced-failure integration matrix to confirm convergence bounds under lagging-peer churn.
Latest sync (consensus recovery stabilization broad sweep, 2026-02-20): implemented the aggressive deterministic recovery pass across `iroha_core`/`iroha_config`/`iroha_telemetry`/`iroha_p2p`: height-scoped missing-block budgets with hard-cap/range-pull escalation, fail-closed sidecar mismatch quarantine/final-drop state, dependency-gated empty-topology recovery, and proposal gating while missing dependencies are unresolved. Added/validated focused regressions for hash churn, view churn, recovery-budget clear-on-success, hash-miss range-pull escalation, sidecar quarantine gating/final-drop, and deferred-send reconnect burst stability (`missing_session_deferred_burst_schedules_single_reconnect`). Validation passed: `cargo fmt --all`, `cargo test -p iroha_core --no-run`, targeted `iroha_core` recovery tests (7 new/updated cases), `cargo test -p iroha_p2p --no-run`, `cargo test -p iroha_p2p deferred_queue_ -- --nocapture`, `cargo test -p iroha_p2p missing_session_defers_frame_and_schedules_reconnect -- --nocapture`, `cargo test -p iroha_p2p missing_session_deferred_burst_schedules_single_reconnect -- --nocapture`, `cargo test -p iroha_config`. Remaining open: rerun canonical 3600s soak and targeted >=4-peer integration scenarios to confirm lagging-peer convergence bounds (`no progress for 300s` and `>2 blocks behind for >60s` gates).

Latest sync (Nexus soak submission-loop overhead trim, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` soak path to (1) reuse a leader-targeted submitter with refresh every 3 iterations (`SOAK_SUBMITTER_REFRESH_EVERY`) and per-iteration `target-refresh` telemetry, and (2) replace per-iteration status polling with a rolling `next_soak_target_height` barrier target derived from the previous iteration’s synced height. This removes one status RPC per soak iteration while preserving barrier correctness and balance assertions. Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing --no-run` and post-change full run `CARGO_TARGET_DIR=/tmp/iroha_target_cross_opt_rolling NORITO_SKIP_BINDINGS_SYNC=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate 100.0%, soak min/avg/max `5.301/8.305/14.249s`, barrier `3.487/7.689/14.237s`, target-refresh `0.000/0.531/1.987s`; startup `848.900s` due one-time in-run `irohad --release` build); prior passing benchmark run for refresh telemetry remains the low-contention reference (`IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1`: pass_rate 100.0%, soak min/avg/max `3.254/5.895/10.921s`, barrier `3.156/5.690/10.596s`, target-refresh `0.000/0.087/0.395s`) and runtime benchmark stayed green (`IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1`: pass_rate 100.0%, total min/avg/max `57.898/60.369/62.530ms`).
Latest sync (Testus stability hardening follow-up III, 2026-02-20): updated `integration_tests/tests/testus_public_localnet.rs` to (1) keep unrecovering-skew detection + queue-timeout retry submits, (2) reduce membership churn cadence to one event per `churn_interval` (offset start), and (3) raise default stall timeout via `IROHA_TESTUS_STALL_TIMEOUT_SECS=300` while retaining configurable overrides. Updated `integrated_test_framework.md` optional knobs for the new stall-timeout default. Validation passed: `cargo check -p integration_tests --test testus_public_localnet`, `cargo test -p integration_tests --test testus_public_localnet simulation_config_defaults_are_valid -- --nocapture`, `KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=120 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture`, and `KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=900 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (simulation validations passed with non-fatal joiner catch-up lag warnings).
Latest sync (Nexus leader-target freshness fallback, 2026-02-20): updated `leader_or_highest_height_peer_index(...)` in both `integration_tests/tests/nexus/cross_dataspace_localnet.rs` and `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` so the reported leader is only selected when its observed height is within one block of the status probe height; otherwise submissions fall back to highest-height peer targeting. Validation passed: `cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture` and `cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture`.
Latest sync (Nexus soak barrier consolidation, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to remove per-iteration Bob propagation waits inside the 10-iteration soak and keep one final `soak final bob sync barrier` after the soak loop. This preserves cross-peer propagation coverage while removing repeated barrier round-trips from the throughput sample path. Validation passed: `IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate 100%, soak min/avg/max `3.443/5.853/9.383s`).
Latest sync (Nexus route-probe + soak attribution telemetry, 2026-02-20): expanded `integration_tests/tests/nexus/cross_dataspace_localnet.rs` diagnostics so shared route-probe waits now emit per-dataspace first-seen commitment timing/height (`[route-probe] ds1/ds2 first_seen ...`) and soak output now includes per-iteration `submit`/`barrier`/`query` min/avg/max buckets for direct bottleneck attribution. Validation passed: `IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate 100%, route-probe `ds1 first_seen=25.271s`, `ds2 first_seen=7.065s`; soak total min/avg/max `5.563/9.969/14.117s`, submit `0.014/0.061/0.215s`, barrier `2.626/6.762/10.560s`, query `0.006/0.062/0.201s`).
Latest sync (Nexus soak bucket telemetry, 2026-02-20): extended `integration_tests/tests/nexus/cross_dataspace_localnet.rs` soak reporting to include per-iteration `submit`/`barrier`/`query` min/avg/max buckets in addition to end-to-end soak latency, improving bottleneck attribution during throughput regressions. Validation passed: `cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` (pass_rate 100%, total min/avg/max `4.756/7.711/10.969s`, submit `0.003/0.033/0.091s`, barrier `2.604/6.267/9.535s`, query `0.005/0.031/0.071s`).
Latest sync (Nexus cross-dataspace query-overhead trim, 2026-02-20): removed redundant post-wait balance re-queries in `integration_tests/tests/nexus/cross_dataspace_localnet.rs` (successful/reverse/failing swap phases) so correctness checks rely on the existing `wait_for_expected_balances(...)` barriers without duplicate asset fetches. Revalidated both nexus tracks. Validation passed: `cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture` and `cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture`. Follow-up experiment: tighter tick cadence (5 polls) increased soak latency and was reverted to 10 polls.
Latest sync (Nexus route-probe shared barrier completion, 2026-02-20): finalized `integration_tests/tests/nexus/cross_dataspace_localnet.rs` probe refactor so both explicit `ds1`/`ds2` route probes submit in one enqueue phase and synchronize via one `wait_for_dataspace_commitments(...)` barrier, while keeping separate probe assertions/timing and unchanged swap rollback semantics. Revalidated both nexus tracks with current metrics. Validation passed: `cargo test -p integration_tests --test mod cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture` and `cargo test -p integration_tests --test mod runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture`.
Latest sync (Nexus autoscale localnet regression coverage, 2026-02-20): added `nexus.autoscale` config defaults/parsing and deterministic block-apply autoscale transitions in core, plus a new localnet regression `integration_tests/tests/nexus/autoscale_localnet.rs` that asserts lane expansion (burst load) and contraction (heartbeat under low utilization) by polling active Kura lane segments on all peers. Scoped validation passed: `cargo check -p iroha_core -p iroha_config -p integration_tests`, `cargo test -p integration_tests --test mod nexus::autoscale_localnet::nexus_autoscale_expands_and_contracts_lanes_in_localnet -- --nocapture` (sandbox loopback bind restriction auto-skipped startup and test passed); full `cargo test -p integration_tests ...` remains blocked by unrelated `offline_allowance_security` compile errors.
Latest sync (Testus localnet stability hardening follow-up III, 2026-02-20): updated `integration_tests/tests/testus_public_localnet.rs` to combine max-height stall liveness checks, unrecovering-skew detection (skew breach + lagging-min-height no-progress age), queue-timeout retryable membership submits, and reduced membership churn cadence (one event per `churn_interval`, offset-started). Defaults now use `IROHA_TESTUS_MAX_HEIGHT_SKEW_GRACE_SECS=30` and `IROHA_TESTUS_STALL_TIMEOUT_SECS=300`; runbook defaults in `integrated_test_framework.md` were updated accordingly. Added focused helper unit coverage (`skew_breach_is_not_unrecovering_when_min_height_progresses_recently`, `skew_breach_is_unrecovering_when_duration_and_min_age_exceed_grace`, `queue_timeout_error_classifier_matches_expected_message`). Validation passed: `cargo check -p integration_tests --test testus_public_localnet`, `cargo test -p integration_tests --test testus_public_localnet skew_breach_ -- --nocapture`, `cargo test -p integration_tests --test testus_public_localnet queue_timeout_error_classifier_matches_expected_message -- --nocapture`, `KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=120 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture`, and `KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 IROHA_TESTUS_SIM_DURATION_SECS=900 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture` (both simulation validations ok with non-fatal joiner catch-up lag warnings).
Latest sync (Nexus cross-swap enqueue/barrier refinement, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` so forward/reverse success swaps use explicit enqueue+barrier+query phases (instead of blocking submit), setup seed-balance convergence uses tick-assisted polling after `+1` barrier, and stall-tick helpers now have focused unit coverage. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::tests::next_stall_polls_resets_on_height_progress -- --nocapture --test-threads=1`, `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::tests::should_emit_tick_triggers_on_cadence_boundaries -- --nocapture --test-threads=1`, and `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1`.
Latest sync (Nexus runtime revalidation after reliability follow-up, 2026-02-20): reran `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` benchmark after the latest cross-dataspace reliability adjustments and helper-test additions; timing collection remained stable with pass_rate 100% and consistent low-ms windows for submit/commit-apply/visibility/total.
Latest sync (Nexus cross-dataspace setup reliability follow-up, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to make dataspace-probe tick txs stall-aware (sent only on sustained height stalls) and strengthened setup register/mint barrier target to `start_height + 2` before seed-balance assertions. Added focused helper tests in `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` for duration stats/formatting. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::runtime_dataspace_registration_perf::tests::duration_min_avg_max_reports_expected_values -- --nocapture --test-threads=1`, `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::runtime_dataspace_registration_perf::tests::format_duration_selects_unit_by_scale -- --nocapture --test-threads=1`, and `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1`.
Latest sync (Testus public-localnet simulation harness, 2026-02-20): added `integration_tests/tests/testus_public_localnet.rs` to simulate a 7-validator Testus-style localnet bootstrap with join/leave membership churn, validator restart churn, consensus stability checks (height skew/stall/view-change/commit-QC bounds), and fixed-rate load generation (`IROHA_TESTUS_LOAD_TPS`, default 5 TPS). Updated `xtask/src/kagami_profiles.rs` and regenerated `defaults/kagami/iroha3-testus/*` with `min_peers = 7`; documented run commands in `integrated_test_framework.md`. Validation passed: `cargo check -p integration_tests --test testus_public_localnet`, `KAGAMI_BIN=$PWD/target/debug/kagami TEST_NETWORK_BIN_IROHAD=$PWD/target/debug/irohad IROHA_TEST_SKIP_BUILD=1 cargo test -p integration_tests --test testus_public_localnet testus_localnet_bootstrap_7_validators -- --nocapture`, `... testus_localnet_joiner_register_unregister_behavior -- --ignored --nocapture`, `... testus_localnet_restart_catchup_behavior -- --ignored --nocapture`, and `... IROHA_TESTUS_SIM_DURATION_SECS=120 IROHA_TESTUS_CHURN_INTERVAL_SECS=30 IROHA_TESTUS_LOAD_TPS=5 cargo test -p integration_tests --test testus_public_localnet testus_public_localnet_5tps_churn_stability -- --ignored --nocapture`.
Latest sync (Nexus runtime peer-visibility polling refinement, 2026-02-20): updated `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` to measure all-peer visibility via round-robin polling across pending peers (excluding submitter), removing sequential per-peer wait bias from propagation metrics. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::runtime_dataspace_registration_perf::runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1`.
Latest sync (Nexus runtime metric-window refinement, 2026-02-20): updated `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` so `all-peer visibility` measures post-apply propagation on non-submitter peers, leaving `submit` and `commit/apply` windows independently attributable. Added explicit output note when submitter commit height does not advance during lifecycle visibility. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::runtime_dataspace_registration_perf::runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1`.
Latest sync (Nexus cross-dataspace follow-up tuning, 2026-02-20): updated `integration_tests/tests/nexus/cross_dataspace_localnet.rs` and `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` to improve performance attribution and reduce avoidable runtime overhead. Cross-dataspace soak now pipelines forward/reverse swaps with `submit_all` + one barrier per iteration, balance assertions use targeted `FindAssetById`, per-poll validator activation tick txs were removed, and dataspace probe tick txs are throttled (every 10 polls) to keep probe reliability with lower noise. Runtime benchmark now submits one lifecycle plan to a leader-targeted peer per iteration and reports distinct `submit`, `commit/apply`, `all-peer visibility`, and `total` metrics with ms/us formatting plus submitter height-delta stats. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::runtime_dataspace_registration_perf::runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1` and `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1`.
Latest sync (IrohaSwift settlement execution status API, 2026-02-20): added `ToriiSettlementExecutionStatus` plus `ToriiClient.getSettlementStatus(bundleIdHex:)` / `getOfflineSettlementStatus(bundleIdHex:)` (async + completion) in `IrohaSwift/Sources/IrohaSwift/ToriiClient.swift` to classify offline settlement bundles as settled/pending/rejected/unknown. The SDK now caches submit outcomes by bundle id so accepted submits surface as `pending` until a settlement row appears, and reject-code submit failures surface as `rejected(reason:)`; rejected settlement rows also resolve reason from payload or revocation-query fallback. Added endpoint tests in `IrohaSwift/Tests/IrohaSwiftTests/ToriiOfflineEndpointsTests.swift` for settled/pending/rejected/unknown outcomes and reject/revocation reason resolution. Validation passed: `swift test --filter ToriiOfflineEndpointsTests`. Full-suite `swift test` still reports pre-existing `TransactionParityFixturesTests` mismatches outside this change.
Latest sync (Izanami 3600s soak rerun, 2026-02-20, second run): reran the canonical long soak (`RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable`) and still failed with `no block height progress for 300s (min height 670, target 2000)` (log: `/tmp/izanami_soak_20260220T162157.log`, network dir: `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_7CWNyy`). Failure remained lagging-peer concentrated: `sisterly_owlet` retained tiered-state manifests at heights `668/669` while peers held `678/679`, with bounded but persistent recovery churn (`empty commit topology`=135, `roster sidecar mismatch`=133, `missing block fetch retry`=428) and no deterministic hard-cap escalation log (`missing-block retry attempts exceeded deterministic cap`=0). Remaining open: make missing-block hard-cap escalation and sidecar-mismatch final-drop paths trigger reliably under this soak profile so lagging peers converge instead of sustained bounded retries.
Latest sync (Izanami 3600s soak rerun, 2026-02-20): reran the canonical long soak (`RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) IROHA_TEST_NETWORK_PARALLELISM=2 CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable`) and still failed with `no block height progress for 300s (min height 360, target 2000)` (log: `/tmp/izanami_soak_20260220T143914.log`, network dir: `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_RW1dQX`). Three peers committed through height `364` by `2026-02-20T10:58:59Z`, while lagging peer `learning_chinook` stopped committing at height `360` (`10:58:30Z`) and entered sustained bounded-retry churn: `deferring consensus path: empty commit topology` (3686), `ignoring roster sidecar with mismatched hash` (7993), and `retrying missing block fetch` (9438), alongside workload backpressure warnings (`transaction queued for too long`, tx confirmation timeout). Remaining open: tighten roster-sidecar mismatch quarantine/final-drop path and missing-block retry escalation so the lagging peer deterministically converges instead of cycling in high-volume bounded retries under this soak profile.
Latest sync (consensus empty-topology + deferred-send hardening, 2026-02-20): completed bounded empty-commit-topology recovery in Sumeragi (`crates/iroha_core/src/sumeragi/main_loop.rs` + companions) with deterministic staged escalation (topology refresh, targeted block-sync fetch, bounded wait, forced view change), per-round retry/age bounds, and defer-log cooldown suppression; wired new status/telemetry counters (`consensus_empty_commit_topology_defer_total`, `consensus_empty_commit_topology_escalation_total`, `consensus_recovery_state_transitions_total{state=*}`); and bounded deferred roster-QC replay with TTL/attempt-driven fetch + expiry escalation. Completed P2P deferred-send bundle in `crates/iroha_p2p/src/network.rs` with unit coverage for queue ordering/generation markers, TTL/cap drops, and reconnect scheduling; updated p2p integration config fixtures for new `deferred_send_ttl`/`deferred_send_max_per_peer` fields. Validation passed: `cargo test -p iroha_core --lib deferred_roster_qc_expiry_is_bounded -- --nocapture`, `cargo test -p iroha_core --lib empty_commit_topology_recovery_escalates_after_bounded_retries -- --nocapture`, `cargo test -p iroha_core --lib missing_block_fetch_counters_surface_in_snapshot -- --nocapture`, `cargo test -p iroha_p2p deferred_queue_ -- --nocapture`, `cargo test -p iroha_p2p missing_session_defers_frame_and_schedules_reconnect -- --nocapture`. Remaining open: rerun the canonical 3600s soak to verify no 300s progress stalls and confirm lagging-peer recovery bounds end-to-end.
Latest sync (consensus latency recovery hardening, 2026-02-20): implemented bounded deferred-QC missing-payload recovery and bounded block-sync QC quarantine/revalidation without changing quorum/lock semantics. Added `DeferredQcEntry` and `QuarantinedQcCandidate` paths in Sumeragi (`qc.rs`, `block_sync.rs`) with TTL/cap/attempt bounds, targeted missing-block fetch escalation, and deterministic view-change fallback. Added new status/Prometheus counters (`blocksync_qc_quarantine_total`, `blocksync_qc_revalidated_total`, `blocksync_qc_final_drop_total{reason=*}`, `qc_deferred_missing_payload_total`, `qc_deferred_resolved_total`, `qc_deferred_expired_total`) with focused regression tests for bounded expiry paths and snapshot surfacing. Validation passed: `cargo check -p iroha_core -p iroha_telemetry`; targeted tests `deferred_missing_payload_qc_expiry_is_bounded`, `quarantined_block_sync_qc_expiry_is_bounded`, `block_sync_counters_surface_in_snapshot`, `missing_block_fetch_counters_surface_in_snapshot`.
Latest sync (offline petal katakana distance-safe + realtime simulation, 2026-02-20): updated `crates/iroha_cli/src/offline/petal.rs` and `docs/source/petal_stream.md` so katakana-base94 keeps explicit `--channel` routing with binary fallback while adding `--katakana-preset {balanced|distance-safe}` auto-defaults when grid/chunk are not forced (`balanced`: `chunk_size=176`, min grid `41`; `distance-safe`: `chunk_size=96`, min grid `33`). Added `offline petal simulate-realtime` to emulate frame-by-frame live camera decoding (capture perturbation, channel-aware decode, assembler progression, timeline/report metrics, optional payload write). Added regression coverage for distance-safe preset resolution and deterministic realtime katakana reconstruction.
Latest sync (Nexus cross-dataspace execution + registration benchmark split, 2026-02-19): refactored `integration_tests/tests/nexus/cross_dataspace_localnet.rs` to use leader-targeted submissions with pipelined setup barriers, preserved both `ds1`/`ds2` route probes, and added a 10-iteration soak report (pass-rate + min/avg/max) while keeping rollback semantics checks; added isolated runtime lifecycle benchmark coverage in `integration_tests/tests/nexus/runtime_dataspace_registration_perf.rs` with per-step timing (`submit`, `commit/apply`, all-peer visibility, total) over 5 iterations. Validation passed: `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::cross_dataspace_localnet::cross_dataspace_atomic_swap_is_all_or_nothing -- --nocapture --test-threads=1` and `NORITO_SKIP_BINDINGS_SYNC=1 cargo test -p integration_tests --test mod nexus::runtime_dataspace_registration_perf::runtime_nexus_registration_reports_lane_lifecycle_costs -- --nocapture --test-threads=1`.
Latest sync (ZK audit remediation workspace lint closure, 2026-02-19): completed the previously deferred environment follow-up by running a full workspace lint pass and fixing the remaining clippy findings in `crates/norito/src/lib.rs`, `crates/fastpq_prover/src/backend.rs`, and `xtask/src/norito_rpc.rs`. Validation passed: `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/tmp/iroha_target_gapfix4 cargo clippy --workspace --all-targets -- -D warnings`, `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix4 cargo test -p norito --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix4 cargo test -p fastpq_prover execution_mode_observer_receives_resolution -- --nocapture`, and `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix4 cargo test -p xtask --no-run`.
Latest sync (ZK audit remediation gap closure follow-up, 2026-02-19): resolved the previously listed unrelated blockers by importing `StateReadOnly` for `crates/iroha_core/src/telemetry.rs` tests, updating Nexus multilane integration tests to the current `LaneRouter::route(&tx)` signature (`integration_tests/tests/nexus/multilane_pipeline.rs`, `integration_tests/tests/nexus/multilane_router.rs`), removing an unused `gpuzstd_metal` import (`crates/gpuzstd_metal/src/lib.rs`), and fixing `iroha_data_model` clippy findings (`crates/iroha_data_model/src/account/address.rs`, `crates/iroha_data_model/src/parameter/system.rs`). Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix3 cargo test -p iroha_core --lib --no-run`, `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix3 cargo test -p integration_tests --no-run`, `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix3 cargo test --workspace --no-run`, and `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix3 cargo clippy -p gpuzstd_metal --all-targets -- -D warnings`. Historical note: the wider clippy sweep was initially blocked by disk capacity (`No space left on device`, os error 28) and is now closed by the workspace lint closure entry above.
Latest sync (ZK audit remediation gap closure, 2026-02-19): closed the remaining Torii/Halo2 hardening items by persisting and querying a report-summary index in `crates/iroha_torii/src/zk_prover.rs` (list/count/delete/GC no longer require full report scans), adding bounded report-file reads (`REPORT_FILE_MAX_BYTES`), enforcing attachment metadata scan caps with explicit `429` on over-budget scans and tri-state legacy tag fallback in `crates/iroha_torii/src/zk_attachments.rs`, and keeping limited Rayon pool cache eviction bounded in `crates/iroha_zkp_halo2/src/lib.rs` (clippy-clean). Targeted checks passed before unrelated workspace blockers: `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix cargo test -p iroha_torii --lib report_index_tracks_save_and_delete -- --nocapture`, `... load_report_rejects_oversized_report_file ...`, `... count_reports_filters_using_report_summaries ...`, `... attachment_meta_tag_match_backfills_legacy_tags ...`, `... attachment_meta_tag_match_marks_inconclusive_after_legacy_cap ...`, `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix cargo clippy -p iroha_zkp_halo2 --features parallel --all-targets -- -D warnings`, and `CARGO_TARGET_DIR=/tmp/iroha_target_gapfix cargo test -p iroha_zkp_halo2 --features parallel limited_pool_cache_is_bounded -- --nocapture`. Follow-up status: unrelated compile blockers from that snapshot were closed in the 2026-02-19 follow-up entry above; remaining work is environment-only (free disk for broader clippy sweep).
Latest sync (offline escrow reclaim + authority hardening, 2026-02-19): implemented controller-driven post-expiry recovery and escrow-source transfer hardening across data model/core/executor, added `OfflineAllowanceReclaimed` event/filter handling, rejected direct transfers from offline escrow source accounts, added reclaim and escrow-source rejection regressions, and updated `docs/source/offline_allowance.md`.
Latest sync (Torii MCP transaction/connect alias hardening, 2026-02-19): tightened agent ergonomics for transaction polling and Connect session flows in `crates/iroha_torii/src/mcp.rs`. `iroha.transactions.status` now accepts `transaction_hash` aliases consistently (including `query.transaction_hash`) and canonicalizes query keys before dispatch so agents can poll `/v1/pipeline/transactions/status` with either hash field. Explorer detail aliases now also accept nested `path.transaction_hash` for `iroha.transactions.get` and `iroha.instructions.get` (in addition to `path.hash` and top-level shortcuts). Connect flows now accept `session_id` aliases alongside `sid` for `connect.ws.ticket`, `connect.session.create`, `connect.session.create_and_ticket`, and `connect.session.delete` (including `path.sid` and `path.session_id` for delete), and Connect schemas now enforce required alias groups (`connect.ws.ticket` requires `role` plus `sid`/`session_id`; `connect.session.delete` requires `sid`/`session_id`/`path`, with nested `path.sid`/`path.session_id` validation). Added focused unit coverage (`ws_ticket_accepts_session_id_alias`, `build_connect_session_create_body_accepts_session_id_shortcut`, `extract_optional_transaction_hash_argument_accepts_query_alias_shortcut`, `extract_connect_sid_argument_accepts_path_session_id_alias`, `extract_transaction_hash_argument_accepts_path_alias_shortcut`), endpoint coverage in `crates/iroha_torii/tests/mcp_endpoints.rs` (`mcp_jsonrpc_tools_call_agent_alias_transaction_status_accepts_transaction_hash_alias`, `mcp_jsonrpc_tools_call_agent_alias_transaction_wait_accepts_query_transaction_hash_alias`, `mcp_jsonrpc_tools_call_agent_alias_transactions_get_accepts_path_transaction_hash_alias`, `mcp_jsonrpc_tools_call_agent_alias_instructions_get_accepts_path_transaction_hash_alias`, lifecycle/create-ticket tests updated to use `session_id` aliases including nested delete-path alias), and docs updates in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::ws_ticket_accepts_session_id_alias -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::build_connect_session_create_body_accepts_session_id_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_optional_transaction_hash_argument_accepts_query_alias_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_connect_sid_argument_accepts_path_session_id_alias -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_transaction_hash_argument_accepts_path_alias_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_transaction_status_accepts_transaction_hash_alias -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_transaction_wait_accepts_query_transaction_hash_alias -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_transactions_get_accepts_path_transaction_hash_alias -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_instructions_get_accepts_path_transaction_hash_alias -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_session_create_and_ticket_dispatches_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_session_lifecycle_dispatches_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_alias_lifecycle_dispatches_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (130 passed).
Latest sync (Kura/WSV hardening implementation, 2026-02-19): implemented the first remediation tranche from `docs/source/kura_wsv_security_performance_audit_20260219.md`. `crates/iroha_core/src/kura.rs` now reports writer IO failures without panic (writer fault state + `Error::BlockWriterFaulted`) and added incremental pending-budget accounting on enqueue/persist paths; `crates/iroha_core/src/state.rs` now handles DA cursor advancement failures in `apply_without_execution` without panicking; `crates/ivm/src/mock_wsv.rs` now enforces envelope-size limits and caller permission checks for JSON admin aliases (`ManageRoles`, `ManagePermissions`, `ManageTriggers`, `ManagePeers`) with new unit/integration regressions. Validation passed via targeted `iroha_core` and `ivm` tests under `CARGO_TARGET_DIR=target_tmp_kura_wsv_harden` (`store_block_reports_writer_fault`, `replace_top_block_reports_writer_fault`, `apply_without_execution_persists_da_shard_cursor_journal_in_background`, `wsv_host_roles_triggers_envelope`, `wsv_host_execute_query_envelope`, `input_publish_tlv_rejects_oversized_envelope`, `execute_instruction_rejects_oversized_json_envelope`, `parse_manage_permissions_variants_ok`). Remaining Kura/WSV open work from the audit: reduce eviction lock hold time/full-file rewrite latency and shorten coarse `view_lock` commit critical sections.
Latest sync (ZK audit remediation implementation, 2026-02-19): implemented the planned ZK security/performance hardening across Torii/IVM/data-model/Halo2 surfaces. `crates/iroha_torii/src/zk_prover.rs` now sanitizes report IDs end-to-end, rejects invalid report IDs on app API routes, bounds report scans (`REPORT_SCAN_MAX_FILES`), and tightens background scan scheduling (bounded in-flight joins + pre-spawn permit acquisition). `crates/iroha_torii/src/zk_attachments.rs` now stores ingest-time `zk1_tags` metadata and uses bounded legacy body scanning (`TAG_FILTER_LEGACY_SCAN_CAP`) for tag filters. `crates/ivm/src/zk.rs` now rejects cross-chunk memory writes during `verify_trace` replay; `crates/iroha_data_model/src/proof.rs` now applies explicit caps to len-prefixed decode fields; `crates/iroha_zkp_halo2/src/lib.rs` now caches limited Rayon pools for batch verification. Validation passed via targeted tests: `cargo fmt --all -- --check`, `cargo test -p ivm verify_trace_rejects_cross_chunk_memory_writes -- --nocapture`, `cargo test -p iroha_torii get_report_rejects_invalid_id -- --nocapture`, `cargo test -p iroha_torii --lib delete_report_rejects_invalid_id -- --nocapture`, `cargo test -p iroha_torii --lib zk1_extract_tags_collects_tlv_tags -- --nocapture`, `cargo test -p iroha_data_model take_len_prefixed_slice_rejects_fields_beyond_cap -- --nocapture`, and `cargo test -p iroha_data_model proofbox_decode_rejects_oversized_len_prefixed_payloads -- --nocapture` (all ok). Remaining open follow-up: run full `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` for end-to-end regression sweep when CI budget allows.
Latest sync (offline settlement atomicity hardening, 2026-02-19): tightened `crates/iroha_core/src/smartcontracts/isi/offline.rs` so `SubmitOfflineToOnlineTransfer` applies allowance/commitment/counter mutation and writes transfer audit records only after escrow-backed deposit credit succeeds, eliminating the partial-failure path where escrow rejection could still decrement `remaining_amount`. Hardened `credit_deposit_account` with destination-account preflight and refund-on-deposit-failure behavior so failed credits cannot silently debit escrow. Added regressions `submit_transfer_rejects_insufficient_escrow_without_mutating_allowance_or_credit` and `credit_deposit_account_rejects_missing_deposit_without_escrow_debit`; revalidated related replay/escrow regressions and integration coverage. Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/smartcontracts/isi/offline.rs`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_insufficient_escrow_without_mutating_allowance_or_credit -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_duplicate_bundle_without_mutating_allowance -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_replayed_counter_without_credit -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib credit_deposit_account_rejects_insufficient_escrow_without_credit -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib credit_deposit_account_rejects_missing_deposit_without_escrow_debit -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p integration_tests --test offline_allowance_security -- --nocapture --test-threads=1`.
Latest sync (Kura/WSV security-performance audit, 2026-02-19): completed exhaustive audit coverage for `crates/iroha_core/src/kura.rs`, production WSV paths in `crates/iroha_core/src/state.rs`, and IVM test/dev WSV host paths in `crates/ivm/src/mock_wsv.rs`; report captured in `docs/source/kura_wsv_security_performance_audit_20260219.md`. Priority follow-ups to schedule: replace Kura writer panic-on-IO with recoverable handling, reduce full-file eviction lock hold/latency, add incremental Kura budget accounting on enqueue, bound mock-host TLV/JSON envelope sizes, and add explicit permission checks (or strict feature-gating) for mock WSV JSON admin aliases.
Latest sync (offline settlement spend-safety hardening, 2026-02-18): added an early duplicate-bundle rejection in `crates/iroha_core/src/smartcontracts/isi/offline.rs` so replayed `bundle_id`s are rejected before proof/sig processing and before any settlement-path allowance mutations. Added `credit_deposit_account_rejects_insufficient_escrow_without_credit`, `submit_transfer_rejects_duplicate_bundle_without_mutating_allowance`, and `submit_transfer_rejects_replayed_counter_without_credit` to lock in no-credit/no-debit behavior for rejected settlement paths (escrow underfunding, bundle replay, and counter replay). Validation passed: `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib credit_deposit_account_rejects_insufficient_escrow_without_credit -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_duplicate_bundle_without_mutating_allowance -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_replayed_counter_without_credit -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_revoked_verdict -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_delta_mismatch -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p integration_tests --test offline_allowance_security -- --nocapture --test-threads=1`.
Latest sync (offline allowance revocation + test harness hardening, 2026-02-18): tightened `crates/iroha_core/src/smartcontracts/isi/offline.rs` so settlement rejects allowances whose `verdict_id` is present in `offline_verdict_revocations`, preserving no-side-effect rejection semantics; added/updated regression coverage in `integration_tests/tests/offline_allowance_security.rs` (non-controller + missing-escrow rejection) and fixed offline allowance genesis setup in `integration_tests/tests/address_canonicalisation.rs` by pre-minting the controller asset and enabling offline metadata so stricter register-time checks pass. Validation passed: `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib refresh_after_expiry_is_rejected -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib register_allowance_rejects_future_certificate -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib register_allowance_rejects_expired_certificate -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib submit_transfer_rejects_revoked_verdict -- --nocapture`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p integration_tests --test offline_allowance_security -- --nocapture --test-threads=1`, `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p integration_tests --test address_canonicalisation offline_allowances_ -- --nocapture --test-threads=1`.
Latest sync (Izanami 3600s soak + block-sync fallback anchor, 2026-02-18): full long-run validation `cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` (with `IROHA_TEST_NETWORK_KEEP_DIRS=1`, `IROHA_TEST_NETWORK_PARALLELISM=2`, `CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun`) failed at `min height 1217` due `no block height progress for 300s`. Peer heights diverged (`1239/1239/1239/1217`) and late-stage logs showed renewed `no proposal observed before cutoff` plus `commit quorum missing past timeout` on the straggler, alongside sustained `qc_missing_payload` and block-sync lock conflicts. Implemented a follow-up in `crates/iroha_core/src/block_sync.rs`: unknown-`prev_hash` responses now anchor from requester `latest_hash` when known (`unknown_prev_fallback_start_height`) instead of always sharing from genesis, reducing repeated `Block hash not found; sharing from genesis` churn on lagging peers. Added unit coverage (`unknown_prev_fallback_uses_known_latest_hash_height`, `unknown_prev_fallback_none_when_latest_hash_unknown`) and validated with `cargo test -p iroha_core --lib unknown_prev_fallback_ -- --nocapture`, `cargo test -p iroha_core --lib should_share_unknown_prev_hash_tracks_peer_and_height -- --nocapture`, `cargo check -p iroha_core --lib`. Remaining open: re-run the 3600s soak to confirm whether late-stage straggler proposal starvation around heights `~1220-1230` is reduced and whether min-height skew closes.
Latest sync (Izanami 3600s soak + DA consensus backlog tuning, 2026-02-19): implemented three consensus-path mitigations in `crates/iroha_core/src/sumeragi/main_loop.rs` + `crates/iroha_core/src/sumeragi/main_loop/reschedule.rs`: (1) proposal-gap idle view-change now adds availability-timeout grace when DA backlog is active (`consensus_queue_backlog`), (2) near-quorum reschedule deferral now considers block/payload/consensus queue backlog (not only `rbc_chunk_rx`), and (3) near-quorum rebroadcast widens to all non-local topology peers when inferred missing-vote targets collapse to <=1 peer. Added regressions `force_view_change_if_idle_defers_proposal_gap_with_backlog_until_availability_timeout`, `reschedule_defers_near_commit_quorum_while_block_queue_backlogged`, and `quorum_reschedule_near_quorum_fans_out_payload_rebroadcast_targets` (plus rechecked `reschedule_defers_near_commit_quorum_while_rbc_chunks_arrive`), all passing under `cargo test -p iroha_core --lib ...`. Full 3600s rerun (`/tmp/izanami_soak_20260219T060658.log`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_5Z9C0f`) finished at `min_height=1480` (`pace=24.711 blocks/min`, `shortfall=25.867%` vs target 33.333). Bottlenecks shifted but remain: `qc_payload_missing=47` (down), `commit_quorum_missing=1` (down), `no_proposal_cutoff=5` (up), `block_sync_locked_conflict=293` (up), with largest synchronized stall windows around heights `737->739` and `1173->1174` (~16–18s gaps). Remaining open: reduce lock-conflict churn and proposal-gap view-change bursts during late-window backlog convergence to close the remaining ~26% throughput gap.
Latest sync (tx validation perf, 2026-02-18): improved both stateless and stateful transaction validation hot paths in `crates/iroha_core/src/block.rs`. Stateless cache reuse now covers all static-validation entry paths (`validate`, `validate_with_events`, `validate_keep_voting_block_inner`, `validate_keep_voting_block_with_events`): static validation reads/writes `State::stateless_validation_cache`, passes cache-hit snapshots into `validate_static_with_snapshot`, and skips cached transactions in static scheme preverification/per-tx checks. Stateful block validation precomputes `routing_decisions` once and reuses them across fraud-policy stateless checks, telemetry, and apply flow. Added cache warmup coverage for non-voting + voting variants (`validate_populates_stateless_cache`, `validate_with_events_populates_stateless_cache`, `validate_keep_voting_block_populates_stateless_cache`, `validate_keep_voting_block_with_events_populates_stateless_cache`). Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_txval_perf cargo test -p iroha_core --lib populates_stateless_cache -- --nocapture` (4 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_txval_perf cargo test -p iroha_core --lib validate_and_record_transactions_skip_stateless_matches_full -- --nocapture`, `CARGO_TARGET_DIR=/tmp/iroha_target_txval_perf cargo test -p iroha_core --lib validate_static_snapshot_rejects_invalid_signature -- --nocapture`. Benchmark comparison (`cargo bench -p iroha_core --bench validation -- validate --noplot --sample-size 20 --measurement-time 3`): previous run `11.104 µs .. 11.142 µs`, current run `11.291 µs .. 11.393 µs` (Criterion: `+1.53% .. +2.57%`, regression).
Latest sync (Torii MCP governance aliases): added `iroha.gov.instances.list`, `iroha.gov.proposals.get`, `iroha.gov.referenda.get`, `iroha.gov.tally.get`, `iroha.gov.council.current`, `iroha.gov.enact`, and `iroha.gov.finalize` in `crates/iroha_torii/src/mcp.rs` so agents can browse governance instances, fetch proposal/referendum/tally details, read the live council set, and invoke governance enact/finalize mutations through stable MCP aliases for `/v1/gov/*`. Added `tools/call` dispatch wiring, `extract_governance_entity_id_argument` helper coverage, tool-registry/list assertions, endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs alias inventory/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_governance_entity_id_argument_accepts_alias_shortcuts -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_gov_ -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (100 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (126 passed).
Latest sync (Torii MCP proof-record aliases): added `iroha.proofs.get`, `iroha.proofs.query`, and `iroha.proofs.retention` in `crates/iroha_torii/src/mcp.rs` so agents can fetch proof records by id, run proof-record queries, and inspect proof retention status via stable MCP aliases for `/v1/proofs/{id}`, `/v1/proofs/query`, and `/v1/proofs/retention`. Added `tools/call` dispatch wiring, `extract_proof_record_id_argument` helper coverage, tools/list assertions, endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs alias inventory/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_proof_record_id_argument_accepts_proof_id_alias_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_proofs_ -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (99 passed).
Latest sync (Torii MCP contract metadata/list aliases): added `iroha.contracts.code.bytes.get` and `iroha.contracts.instances.list` in `crates/iroha_torii/src/mcp.rs` so agents can fetch contract code bytes (`GET /v1/contracts/code-bytes/{code_hash}` with `code_hash`/`hash` shortcuts) and list contract instances by namespace (`GET /v1/contracts/instances/{ns}` with `ns`/`namespace` shortcuts + flat query filters). Added `tools/call` dispatch wiring, `extract_contract_namespace_argument` helper coverage, tools/list assertions, endpoint alias tests in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs alias inventory/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_contract_namespace_argument_accepts_namespace_alias_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_contracts_ -- --nocapture` (4 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (98 passed).
Latest sync (Torii MCP evidence-submit + chain NFT list aliases): added `iroha.sumeragi.evidence.submit` and `iroha.nfts.chain.list` in `crates/iroha_torii/src/mcp.rs` so agents can submit consensus evidence (`POST /v1/sumeragi/evidence/submit`, flat `evidence_hex` shortcut supported) and list chain-state NFTs directly (`GET /v1/nfts`) alongside explorer/query NFT aliases. Expanded `tools/call` dispatch wiring, tool-registry/list assertions, endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and alias inventory/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_sumeragi_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_nfts_ -- --nocapture` (4 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (96 passed).
Latest sync (Torii MCP Sumeragi diagnostics aliases): added `iroha.sumeragi.evidence.count`, `iroha.sumeragi.evidence.list`, `iroha.sumeragi.new_view`, `iroha.sumeragi.rbc.delivered`, `iroha.sumeragi.vrf.penalties`, `iroha.sumeragi.vrf.epoch`, `iroha.sumeragi.vrf.commit`, `iroha.sumeragi.vrf.reveal`, and `iroha.sumeragi.rbc.sample` in `crates/iroha_torii/src/mcp.rs` so agents can inspect evidence/new-view counters, RBC delivery/sample snapshots, and VRF diagnostics via MCP aliases. Added `tools/call` dispatch wiring to `/v1/sumeragi/evidence/count`, `/v1/sumeragi/evidence`, `/v1/sumeragi/new_view/json`, `/v1/sumeragi/rbc/delivered/{height}/{view}`, `/v1/sumeragi/vrf/penalties/{epoch}`, `/v1/sumeragi/vrf/epoch/{epoch}`, `/v1/sumeragi/vrf/commit`, `/v1/sumeragi/vrf/reveal`, and `/v1/sumeragi/rbc/sample`; added `epoch`/`view` extraction helpers; expanded tool-registry/list assertions; extended endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`; and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_epoch_argument_accepts_top_level_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_view_argument_accepts_top_level_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_sumeragi_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (95 passed).
Latest sync (Torii MCP extended Sumeragi inspection aliases): added `iroha.sumeragi.key_lifecycle`, `iroha.sumeragi.telemetry`, `iroha.sumeragi.rbc.sessions`, `iroha.sumeragi.commit_qc.get`, and `iroha.sumeragi.collectors` in `crates/iroha_torii/src/mcp.rs` so agents can inspect key-lifecycle state, Sumeragi telemetry, RBC session inventory, commit-QC details by hash, and collector planning via MCP aliases. Added `tools/call` dispatch wiring to `/v1/sumeragi/key-lifecycle`, `/v1/sumeragi/telemetry`, `/v1/sumeragi/rbc/sessions`, `/v1/sumeragi/commit_qc/{hash}`, and `/v1/sumeragi/collectors`; expanded tool-registry/list assertions; extended endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`; and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_sumeragi_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (95 passed).
Latest sync (Torii MCP Sumeragi status/QC/key aliases): added `iroha.sumeragi.status`, `iroha.sumeragi.leader`, `iroha.sumeragi.qc`, `iroha.sumeragi.checkpoints`, `iroha.sumeragi.consensus_keys`, and `iroha.sumeragi.bls_keys` in `crates/iroha_torii/src/mcp.rs` so agents can inspect live Sumeragi status, leader/QC snapshots, checkpoint summaries, and key sets via MCP aliases. Added `tools/call` dispatch wiring to `/v1/sumeragi/status`, `/v1/sumeragi/leader`, `/v1/sumeragi/qc`, `/v1/sumeragi/checkpoints`, `/v1/sumeragi/consensus-keys`, and `/v1/sumeragi/bls_keys`; expanded tool-registry/list assertions; extended endpoint alias coverage in `mcp_jsonrpc_tools_call_agent_alias_sumeragi_endpoints_dispatch`; and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_sumeragi_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (95 passed).
Latest sync (Torii MCP DA ingest alias): added `iroha.da.ingest` in `crates/iroha_torii/src/mcp.rs` so agents can submit DA ingest payloads via a stable MCP alias for `/v1/da/ingest`. Added `tools/call` dispatch wiring with flat-body forwarding support, expanded tool-registry/list assertions, added endpoint coverage (`mcp_jsonrpc_tools_call_agent_alias_da_ingest_accepts_body`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_da_ -- --nocapture` (4 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_runtime_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (95 passed).
Latest sync (Torii MCP DA pin-intent aliases): added `iroha.da.pin_intents.list`, `iroha.da.pin_intents.prove`, and `iroha.da.pin_intents.verify` in `crates/iroha_torii/src/mcp.rs` so agents can list/prove/verify pin-intent payloads through stable MCP aliases. Added `tools/call` dispatch wiring to `/v1/da/pin_intents`, `/v1/da/pin_intents/prove`, and `/v1/da/pin_intents/verify` with flat-body forwarding support, expanded tool-registry/list assertions, added endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_da_pin_intents_endpoints_accept_body`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_da_ -- --nocapture` (3 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_runtime_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (94 passed).
Latest sync (Torii MCP runtime upgrade mutation aliases): added `iroha.runtime.upgrades.propose`, `iroha.runtime.upgrades.activate`, and `iroha.runtime.upgrades.cancel` in `crates/iroha_torii/src/mcp.rs` so agents can submit and manage runtime upgrade lifecycle calls through stable MCP aliases. Added `tools/call` dispatch wiring to `/v1/runtime/upgrades/propose`, `/v1/runtime/upgrades/activate/{id}`, and `/v1/runtime/upgrades/cancel/{id}` with `id`/`upgrade_id` shortcuts plus flat-body forwarding for mutation payloads. Added helper coverage (`extract_runtime_upgrade_id_argument_accepts_upgrade_id_alias_shortcut`), expanded tool-registry/list assertions, added endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_runtime_upgrade_mutation_endpoints_dispatch`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::extract_runtime_upgrade_id_argument_accepts_upgrade_id_alias_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_runtime_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (93 passed).
Latest sync (Torii MCP runtime aliases): added `iroha.runtime.abi.active`, `iroha.runtime.abi.hash`, `iroha.runtime.metrics`, and `iroha.runtime.upgrades.list` in `crates/iroha_torii/src/mcp.rs` so agents can inspect active runtime ABI metadata/hash, runtime metrics, and runtime upgrade history through stable aliases. Added `tools/call` dispatch wiring to `/v1/runtime/abi/active`, `/v1/runtime/abi/hash`, `/v1/runtime/metrics`, and `/v1/runtime/upgrades`, expanded tool-registry and tools/list assertions, added endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_runtime_endpoints_dispatch`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_runtime_endpoints_dispatch -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_runtime_alias_run2 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ -- --nocapture` (92 passed).
Latest sync (Torii MCP DA aliases): added `iroha.da.proof_policies`, `iroha.da.proof_policy_snapshot`, `iroha.da.manifests.get`, `iroha.da.commitments.list`, `iroha.da.commitments.prove`, and `iroha.da.commitments.verify` in `crates/iroha_torii/src/mcp.rs` so agents can inspect DA proof policy state, resolve manifest payloads by ticket, and call DA commitment listing/prove/verify flows through stable aliases. Added `tools/call` dispatch wiring to `/v1/da/proof_policies`, `/v1/da/proof_policy_snapshot`, `/v1/da/manifests/{ticket}`, `/v1/da/commitments`, `/v1/da/commitments/prove`, and `/v1/da/commitments/verify` with ticket shortcuts (`ticket`/`manifest_ticket`/`id`) for manifest lookup and flat-body fallback support for commitment POST routes. Added helper coverage (`extract_ticket_argument_accepts_id_alias_shortcut`), expanded tools/list + registry assertions, added endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_da_...`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --lib mcp::tests::extract_ticket_argument_accepts_id_alias_shortcut -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_da_ -- --nocapture` (2 passed).
Latest sync (Torii MCP Sumeragi aliases): added `iroha.sumeragi.commit_certificates`, `iroha.sumeragi.validator_sets.list`, `iroha.sumeragi.validator_sets.get`, `iroha.sumeragi.rbc`, `iroha.sumeragi.pacemaker`, `iroha.sumeragi.phases`, and `iroha.sumeragi.params` in `crates/iroha_torii/src/mcp.rs` so agents can inspect commit-certificate history, validator-set snapshots, and consensus status through stable aliases. Added `tools/call` dispatch wiring to `/v1/sumeragi/commit-certificates`, `/v1/sumeragi/validator-sets`, `/v1/sumeragi/validator-sets/{height}`, `/v1/sumeragi/rbc`, `/v1/sumeragi/pacemaker`, `/v1/sumeragi/phases`, and `/v1/sumeragi/params` with flat `from`/`limit` shortcuts for commit-certificate windows and `height`/`block_height` shortcuts for validator-set detail lookups. Expanded tool-registry/list assertions and endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_sumeragi_...`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_mcp_sumeragi_alias cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_sumeragi_ -- --nocapture` (2 passed).
Latest sync (Torii MCP ledger/finality aliases): added `iroha.ledger.headers`, `iroha.ledger.state_root`, `iroha.ledger.state_proof`, `iroha.ledger.block_proof`, `iroha.bridge.finality.proof`, and `iroha.bridge.finality.bundle` in `crates/iroha_torii/src/mcp.rs` so agents can request block headers, state roots/proofs, block-entry proofs, and bridge finality artifacts through stable aliases. Added `tools/call` dispatch wiring to `/v1/ledger/headers`, `/v1/ledger/state/{height}`, `/v1/ledger/state-proof/{height}`, `/v1/ledger/block/{height}/proof/{entry_hash}`, `/v1/bridge/finality/{height}`, and `/v1/bridge/finality/bundle/{height}` with height shortcuts (`height`/`block_height`) and block-proof entry hash shortcuts (`entry_hash`/`tx_hash`/`hash`). Added helper coverage (`extract_height_argument_accepts_block_height_alias_shortcut`, `extract_entry_hash_argument_accepts_alias_shortcut`), expanded tools/list + registry assertions, added endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_ledger_...`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo check -p iroha_torii`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --lib mcp::tests::extract_height_argument_accepts_block_height_alias_shortcut -- --nocapture` (1 passed), `cargo test -p iroha_torii --lib mcp::tests::extract_entry_hash_argument_accepts_alias_shortcut -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_ledger_ -- --nocapture` (3 passed).
Latest sync (Torii MCP ISO 20022 aliases): added `iroha.iso20022.pacs008.submit`, `iroha.iso20022.pacs009.submit`, and `iroha.iso20022.status.get` in `crates/iroha_torii/src/mcp.rs` so agents can submit ISO bridge messages and read ISO message status through stable aliases. Added `tools/call` dispatch wiring to `/v1/iso20022/pacs008`, `/v1/iso20022/pacs009`, and `/v1/iso20022/status/{msg_id}`, XML payload shortcuts for submit aliases (`message_xml`/`xml` -> request bytes with `application/xml` default), and message-id shortcuts for status (`msg_id`/`message_id`/`id`). Added helper unit coverage (`extract_iso20022_message_id_argument_accepts_alias_shortcut`, `build_iso20022_payload_body_accepts_xml_shortcut`), expanded tools/list assertions, added endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_iso20022_...`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --lib mcp::tests::extract_iso20022_message_id_argument_accepts_alias_shortcut -- --nocapture` (1 passed), `cargo test -p iroha_torii --lib mcp::tests::build_iso20022_payload_body_accepts_xml_shortcut -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_iso20022_ -- --nocapture` (3 passed).
Latest sync (offline escrow invariant hardening, 2026-02-18): tightened `crates/iroha_core/src/smartcontracts/isi/offline.rs` so both allowance reservation and settlement credit reject missing escrow bindings with `escrow_missing` even when `settlement.offline.escrow_required=false`, closing the unbacked-offline-flow mint path. Added regression tests `register_allowance_fails_without_escrow_when_not_required_and_metadata_disabled_or_missing` and `credit_deposit_account_rejects_missing_escrow_when_not_required`, and refreshed escrow requirement wording in `docs/source/offline_allowance.md` and `scripts/offline_topup/README.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/smartcontracts/isi/offline.rs`; `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib register_allowance_fails_without_escrow_when_not_required_and_metadata_disabled_or_missing -- --nocapture`; `CARGO_TARGET_DIR=target_tmp_security_fix cargo test -p iroha_core --lib credit_deposit_account_rejects_missing_escrow_when_not_required -- --nocapture`.
Latest sync (queue rejection decoding + NPoS performance harness stabilization, 2026-02-18): shared Torii queue-rejection Norito payload types in `crates/iroha_torii_shared/src/lib.rs` and switched both Torii (`crates/iroha_torii/src/lib.rs`) and client (`crates/iroha/src/client.rs`) to those shared types so `ResponseReport` decodes `429 PRTRY:QUEUE_FULL` payloads (instead of "body isn't a valid utf-8 string"). Updated `integration_tests/tests/sumeragi_npos_performance.rs` queue stress to use non-blocking submission with explicit queue-backpressure reject accounting and relaxed RBC-deferral coupling, and made RBC-store timeout handling non-fatal while preserving summary telemetry (`timed_out`). Validation passed: `cargo test -p iroha_torii_shared queue_error_envelope_roundtrip_preserves_fields -- --nocapture`, `cargo test -p iroha with_msg_decodes_shared_queue_error_envelope -- --nocapture`, `cargo test -p integration_tests --test sumeragi_npos_performance npos_queue_backpressure_triggers_metrics -- --nocapture`, `cargo test -p integration_tests --test sumeragi_npos_performance npos_rbc_store_backpressure_records_metrics -- --nocapture`.
Latest sync (NPoS performance harness stability, 2026-02-18): hardened `integration_tests/tests/sumeragi_npos_performance.rs` against telemetry timing variance by tolerating delayed collector gauges in baseline sampling, validating pacemaker jitter against `sumeragi_pacemaker_max_backoff_ms`, reducing redundant-send/chunk-loss timeout flakiness, and switching large scenario submissions to non-blocking `submit_transaction` paths. Targeted reruns with prebuilt binaries passed for the four previously failing scenarios: `npos_baseline_1s_k3_captures_metrics`, `npos_pacemaker_jitter_within_band`, `npos_redundant_send_retries_update_metrics`, and `npos_rbc_chunk_loss_fault_reports_backlog`.
Latest sync (JS offline allowance/certificate fixtures): updated `javascript/iroha_js/test/toriiClient.test.js` offline allowance/certificate fixtures to include required `certificate.operator` account ids so tests match current SDK request normalization. Validation passed: `node --test --test-name-pattern "issueOfflineCertificate|registerOfflineAllowance|renewOfflineAllowance|topUpOfflineAllowance" javascript/iroha_js/test/toriiClient.test.js` (7 passed) and `node --test javascript/iroha_js/test/toriiClient.test.js` (596 passed, 0 failed, 7 skipped).
Latest sync (Iroha Connect relay-traffic counter integration): extended `crates/iroha_torii/tests/connect_gating.rs` with websocket relay-counter coverage across all relay runtime modes: `connect_ws_broadcast_relay_updates_p2p_rebroadcast_counter`, `connect_ws_broadcast_without_p2p_increments_skipped_rebroadcast_counter`, `connect_ws_local_only_with_p2p_does_not_rebroadcast`, and `connect_ws_relay_disabled_with_p2p_does_not_rebroadcast`, plus status-only integration coverage for relay-disabled and unknown-strategy effective behavior (`connect_status_reports_local_only_when_relay_disabled_with_p2p_attached`, `connect_status_reports_unknown_strategy_as_local_only_with_p2p_attached`). These tests send real `AppToWallet` frames and confirm `/v1/connect/status` tracks decentralized behavior correctly: broadcast+P2P rebroadcasts over node transport, broadcast-without-P2P reports skipped rebroadcasts with effective `local_only`, unknown strategies stay local-only even with P2P attached, and both `local_only`/`relay_enabled=false` paths never rebroadcast even when P2P is attached. JS SDK coverage now includes relay-disabled effective-local-only normalization (`getConnectStatus preserves relay-disabled effective local-only fields`), and Connect docs now clarify status JSON fields vs telemetry metric names (`docs/connect_config.md`) and the relay-disabled effective-local-only interpretation in the JS guide (`docs/source/sdk/js/connect.md`). Validation passed: `rustfmt --edition 2024 crates/iroha_torii/tests/connect_gating.rs`, `node --test --test-name-pattern "getConnectStatus" javascript/iroha_js/test/toriiClient.test.js` (4 passed), `CARGO_HOME=/tmp/cargo_home_connect CARGO_TARGET_DIR=target_connect_harden3 cargo test -p iroha_torii --test connect_gating connect_status_ -- --nocapture` (6 passed), `CARGO_HOME=/tmp/cargo_home_connect CARGO_TARGET_DIR=target_connect_harden3 cargo test -p iroha_torii --test connect_gating --features ws_integration_tests -- --nocapture` (19 passed).
Latest sync (Iroha Connect relay-attachment status integration): extended `crates/iroha_torii/tests/connect_gating.rs` with `connect_status_reports_broadcast_effective_when_p2p_attached`, which wires a test P2P handle through `Torii::with_p2p(...)` and polls `/v1/connect/status` until the async attachment completes, then asserts `policy.relay_strategy="broadcast"`, `policy.relay_effective_strategy="broadcast"`, and `policy.relay_p2p_attached=true` (with rebroadcast counters still zero before traffic). Updated JS README examples to include the new Connect status relay fields (`relayStrategy`, `relayEffectiveStrategy`, `relayP2pAttached`, `p2pRebroadcastsTotal`, `p2pRebroadcastSkippedTotal`) and to show required `certificate.operator` in offline top-up drafts. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/tests/connect_gating.rs`, `CARGO_HOME=/tmp/cargo_home_connect CARGO_TARGET_DIR=target_connect_harden3 cargo test -p iroha_torii --test connect_gating connect_status_ -- --nocapture` (4 passed), `node --test --test-name-pattern "issueOfflineCertificate|registerOfflineAllowance|renewOfflineAllowance|topUpOfflineAllowance" javascript/iroha_js/test/toriiClient.test.js` (7 passed), `node --test javascript/iroha_js/test/toriiClient.test.js` (596 passed, 0 failed, 7 skipped).
Latest sync (Iroha Connect hardening): Torii Connect now enforces strict contiguous per-direction sequencing with session-close on violations, validates role-to-direction ingress at WS attach, and exposes new status counters for sequence/role violations. Connect relay strategy parsing now supports `broadcast`/`local_only` with unknown values forced to `local_only` to keep relay behavior decentralized over peer-to-peer node transport. SDK hardening landed in parallel: Swift `ConnectSession` now synchronizes mutable state and only consumes flow-control tokens after successful decrypt/key checks (new concurrency + token-consumption regressions in `ConnectSessionTests`), Python Connect codec loading now fails closed unless `IROHA_PYTHON_CONNECT_CODEC=stub` is explicitly set and the stub codec dropped `pickle` in favor of deterministic JSON+base64url encoding, and JS Connect queue journal/error handling now tolerates corrupted journal entries and classifies `HTTP 408/429` as timeout/retryable transport conditions. Latest validation (2026-02-18): `cargo check -p iroha_torii --lib` and `cargo test -p iroha_torii connect::tests:: -- --nocapture` passed in an isolated target dir, including dedicated role/direction mismatch ingress coverage and duplicate-frame dedupe-before-sequence regression coverage; WS integration coverage in `connect_gating` with `--features ws_integration_tests` also passed (`connect_ws_*`, `connect_ws_closes_on_role_direction_mismatch`, `connect_ws_duplicate_frame_does_not_close_session`, `connect_session_delete_rejects_ws_attach`, and full `connect_gating` suite).
Latest sync (Iroha Connect decentralization docs/guardrails): clarified Connect runtime/docs to explicitly state that relay forwarding is either local-only or Iroha node-to-node P2P broadcast (no centralized relay gateway mode), documented role→direction mismatch closure and strict contiguous sequence semantics (including dedupe-before-sequence behavior and status counters), updated the unsupported-strategy regression sentinel to `bogus_strategy` to avoid implying a supported centralized strategy name, added `connect_status_*` coverage in `connect_gating` so `/v1/connect/status` confirms unknown strategies are coerced to `local_only` and compatibility aliases are normalized, added `p2p_rebroadcasts_total` status telemetry plus rebroadcast-behavior tests (`broadcast_strategy_records_p2p_rebroadcast_when_network_attached`, `broadcast_strategy_without_network_does_not_increment_rebroadcast_counter`, `local_only_strategy_skips_p2p_rebroadcast_when_network_attached`, `relay_disabled_skips_p2p_rebroadcast_when_network_attached`), then added relay runtime-effectiveness status (`policy.relay_effective_strategy`, `policy.relay_p2p_attached`) and a skip counter (`p2p_rebroadcast_skipped_total`) so operators can confirm when broadcast is effectively local-only due to missing P2P attachment. JS SDK `getConnectStatus()` normalization now maps the full relay/decentralization fields (`relayStrategy`, `relayEffectiveStrategy`, `relayP2pAttached`, `p2pRebroadcastsTotal`, `p2pRebroadcastSkippedTotal`, plus sequence/role counters), with updated unit coverage and docs/examples. Latest validation (2026-02-18): `rustfmt --edition 2024 crates/iroha_torii/src/connect.rs crates/iroha_torii/tests/connect_gating.rs` (ok), `CARGO_HOME=/tmp/cargo_home_connect CARGO_TARGET_DIR=target_connect_harden3 cargo test -p iroha_torii --lib connect::tests:: -- --nocapture` (ok, 33 passed), `CARGO_HOME=/tmp/cargo_home_connect CARGO_TARGET_DIR=target_connect_harden3 cargo test -p iroha_torii --test connect_gating connect_status_ -- --nocapture` (ok, 3 passed), `node --test --test-name-pattern "getConnectStatus" javascript/iroha_js/test/toriiClient.test.js` (ok, 3 passed).
Latest sync (Torii MCP signed-query alias): added `iroha.queries.submit` in `crates/iroha_torii/src/mcp.rs` so agents can submit signed query payloads through `/query` without relying on raw `torii.post_query` names. The alias accepts binary shortcut fields mirroring transaction submit ergonomics (`signed_query_base64`/`query_base64` and `signed_query_hex`/`query_hex`/`body_hex`) plus optional `content_type`/`headers`/`accept`, with hex payloads converted to bytes before dispatch. Added `tools/call` match-arm wiring, schema/tool registration, tool-registry assertions, list-surface coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, endpoint shortcut coverage (`mcp_jsonrpc_tools_call_agent_alias_query_submit_...`), and docs inventory/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_query_submit_ -- --nocapture` (2 passed).
Latest sync (Torii MCP Connect SID ergonomics): updated `crates/iroha_torii/src/mcp.rs` so `connect.session.create` and `connect.session.create_and_ticket` auto-generate a random base64url 32-byte SID when callers omit `sid`, while preserving explicit `body.sid`/`body.node` values. Added helper coverage in `crates/iroha_torii/src/mcp.rs` (`build_connect_session_create_body_generates_sid_when_missing`, `build_connect_session_create_body_preserves_sid_in_body`) and endpoint coverage in `crates/iroha_torii/tests/mcp_endpoints.rs` for both helper and namespaced aliases (`mcp_jsonrpc_connect_session_create_generates_sid_when_omitted`, `mcp_jsonrpc_connect_session_create_and_ticket_generates_sid_when_omitted`). Updated `crates/iroha_torii/docs/mcp_api.md` Connect lifecycle docs to note SID is optional and auto-generated. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::build_connect_session_create_body_ -- --nocapture` (2 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_session_create_generates_sid_when_omitted -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_session_create_and_ticket_generates_sid_when_omitted -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_session_ -- --nocapture` (5 passed).
Latest sync (Torii MCP alias-runtime tools): added `iroha.aliases.resolve` and `iroha.aliases.resolve_index` in `crates/iroha_torii/src/mcp.rs` so agents can resolve alias names and alias indices through `/v1/aliases/resolve` and `/v1/aliases/resolve_index` without raw OpenAPI tool names. Added dispatch wiring, schema definitions, flat-body shortcuts (`alias`, `index`) when `body` is omitted, tool-registry assertions, `tools/list` coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and endpoint coverage (`mcp_jsonrpc_tools_call_agent_aliases_...`). Updated `crates/iroha_torii/docs/mcp_api.md` alias inventory and examples to include alias-runtime calls. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_aliases_ -- --nocapture` (2 passed).
Latest sync (Torii MCP node preflight aliases): added `iroha.time.now`, `iroha.time.status`, and `iroha.api.versions` in `crates/iroha_torii/src/mcp.rs` so agents can fetch time snapshots and supported API versions via `/v1/time/now`, `/v1/time/status`, and `/v1/api/versions` using stable agent-facing tool names. Added dispatch wiring, tool schemas, tool-registry assertions, `tools/list` coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and node-ops dispatch coverage (`mcp_jsonrpc_tools_call_agent_alias_node_operational_endpoints_dispatch`). Updated `crates/iroha_torii/docs/mcp_api.md` alias inventory and Node Ops examples to include these preflight aliases. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_node_operational_endpoints_dispatch -- --nocapture` (1 passed).
Latest sync (Torii MCP offline parity aliases): completed the remaining offline alias surface in `crates/iroha_torii/src/mcp.rs` by adding `iroha.offline.revocations.list`, `iroha.offline.revocations.query`, `iroha.offline.transfers.proof`, `iroha.offline.spend_receipts.submit`, `iroha.offline.state`, `iroha.offline.bundle.proof_status`, `iroha.offline.rejections.list`, `iroha.offline.summaries.list`, and `iroha.offline.summaries.query`, with dispatch wiring, schema definitions, and registry assertions. Expanded `tools/list` and endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs` (including flat query/body shortcut coverage and error-surface checks), and updated `crates/iroha_torii/docs/mcp_api.md` alias inventory/examples (including the existing `iroha.offline.certificates.*` family). Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_ -- --nocapture` (30 passed).
Latest sync (iroha_core test module-path conflict fix): removed the duplicate `crates/iroha_core/tests/test_world.rs` integration-test file and consolidated shared test-world helpers in `crates/iroha_core/tests/test_world/mod.rs` to eliminate the `mod test_world` path conflict that blocked workspace formatting. Validation passed: `rustfmt --edition 2024 crates/iroha_core/tests/test_world/mod.rs`, `cargo fmt --all`, `cargo test -p iroha_core --test zk_verify --no-run`.
Latest sync (Torii MCP offline allowance lifecycle aliases): added `iroha.offline.allowances.get`, `iroha.offline.allowances.issue`, and `iroha.offline.allowances.renew` in `crates/iroha_torii/src/mcp.rs` so agents can inspect and mutate allowance certificates without raw OpenAPI tool names. Added `certificate_id_hex` path extraction shortcuts (`certificate_id`/`id` aliases), dispatch wiring for `/v1/offline/allowances/{certificate_id_hex}`, `/v1/offline/allowances`, and `/v1/offline/allowances/{certificate_id_hex}/renew`, plus flat-body fallback support for issue/renew when `body` is omitted. Expanded list-surface assertions and endpoint alias tests in `crates/iroha_torii/tests/mcp_endpoints.rs`, added helper unit coverage for certificate-id extraction in `crates/iroha_torii/src/mcp.rs`, and documented the new aliases/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `cargo test -p iroha_torii --lib mcp::tests::extract_certificate_id_hex_argument_accepts_id_alias_shortcut -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_allowances -- --nocapture` (5 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_ -- --nocapture` (14 passed).
Latest sync (Sumeragi DA/RBC bottleneck follow-up): implemented consensus hot-path mitigations in `crates/iroha_core/src/sumeragi/main_loop/{reschedule.rs,qc.rs,block_sync.rs,validation.rs,main_loop.rs}` with new/updated coverage in `crates/iroha_core/src/sumeragi/main_loop/tests.rs`. Reschedule now defers near-quorum churn while RBC chunks are still arriving (using progress stall age and availability-timeout gating), idle view-change now defers within a post-timeout RBC convergence grace window, missing-block QC fetch retries widen when RBC sessions show chunk/READY/pending progress, block-sync locked-chain conflict logs are throttled and prefiltered, and ahead-of-tip pending blocks now request missing parents before static validation dispatch. Targeted validation passed: `cargo test -p iroha_core --lib defer_qc_if_block_missing_widens_retry_window_when_rbc_progresses -- --nocapture`, `cargo test -p iroha_core --lib reschedule_defers_near_commit_quorum_while_rbc_chunks_arrive -- --nocapture`, `cargo test -p iroha_core --lib force_view_change_if_idle_defers_after_timeout_while_rbc_backlog_progresses -- --nocapture`, `cargo test -p iroha_core --lib validation_defers_block_ahead_of_local_height -- --nocapture`, plus `defer_qc_if_block_missing_uses_rebroadcast_cooldown` and `reschedule_defers_quorum_timeout_while_rbc_incomplete`.
Latest sync (Torii MCP offline allowance/receipt alias coverage): added missing integration + docs coverage for existing offline MCP aliases in `crates/iroha_torii/tests/mcp_endpoints.rs` and `crates/iroha_torii/docs/mcp_api.md`. `tools/list` assertions now include `iroha.offline.allowances.list`, `iroha.offline.allowances.query`, `iroha.offline.receipts.list`, and `iroha.offline.receipts.query`; endpoint tests now cover flat-query/flat-query-envelope dispatch for those aliases; docs now list the tools and include allowance/receipt call examples. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_allowances -- --nocapture` (2 passed), `cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_receipts -- --nocapture` (2 passed), `cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed).
Latest sync (Izanami long-run follow-up, 2026-02-18): ran `cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` with `CARGO_TARGET_DIR=/tmp/iroha_target_izanami_longrun`, `IROHA_TEST_NETWORK_KEEP_DIRS=1`, and isolated permit settings. Run stopped before target at `core_blocks=1420` (all peers converged at 1420; `blocks.hashes` size `45440` on each peer) and exited with `izanami run stopped before target blocks reached`. Peer exits were clean (`status 0`, empty `run-1-stderr.log`). Remaining bottleneck evidence in peer logs points to intermittent DA/RBC backlog-driven view changes (`no proposal observed before cutoff` with `rbc_backlog=true` and `consensus_queue_backlog=true`), so additional consensus hot-path throughput work remains open.
Latest sync (Sumeragi DA/RBC rebroadcast selector follow-up, 2026-02-18): replaced per-tick full-map urgent-session sorting in `crates/iroha_core/src/sumeragi/main_loop.rs` with a bounded near-tip candidate selector sourced from inflight/pending keys (cursor-fair under session budget), and added `rebroadcast_stalled_rbc_payloads_prioritizes_urgent_near_tip_session` in `crates/iroha_core/src/sumeragi/main_loop/tests.rs`. Targeted suite `cargo test -p iroha_core --lib rebroadcast_stalled_rbc_payloads_ -- --nocapture` passed (11/11). Full Izanami reruns remain below target: `irohad_test_network_qX5cOf` finished at `min_height=981`; post-selector `irohad_test_network_3AgY53` improved to `min_height=1151` but still short of prior `~1420` baseline. Current dominant signals are unresolved payload-missing QCs (`68`), block-sync locked-chain conflicts (`261`), periodic no-proposal/commit-quorum stalls, and 20-30s progress gaps.
Latest sync (Torii MCP offline settlement aliases): added `iroha.offline.settlements.list`, `iroha.offline.settlements.get`, `iroha.offline.settlements.query`, and `iroha.offline.settlements.submit` in `crates/iroha_torii/src/mcp.rs` so agents can inspect settlement bundles and submit settlement transactions via flat MCP names. Added dispatch wiring and schemas with `bundle_id_hex`/`bundle_id` path shortcuts, flat query/query-envelope support for list/query routes, and flat body shortcuts for submit (`authority`, `private_key`, `transfer`) when `body` is omitted. Added list-surface assertions and endpoint alias tests in `crates/iroha_torii/tests/mcp_endpoints.rs` plus docs/tool examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_settlements -- --nocapture` (4 passed), `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --test mcp_endpoints mcp_tools_list_exposes_account_and_transaction_interfaces -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed).
Latest sync (Torii MCP offline transfer aliases): added `iroha.offline.transfers.list`, `iroha.offline.transfers.get`, and `iroha.offline.transfers.query` in `crates/iroha_torii/src/mcp.rs` so agents can inspect offline transfer bundles via flat MCP tool names (including `bundle_id_hex`/`bundle_id` shortcuts and flat query/query-envelope fields). Added `tools/call` dispatch wiring, tool schemas, bundle-id extraction helper coverage, list-surface assertions, and endpoint alias tests in `crates/iroha_torii/tests/mcp_endpoints.rs`, plus docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_tools_call_agent_alias_offline_transfers -- --nocapture` (3 passed), `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --lib mcp::tests::tool_registry_skips_ws_and_sse_routes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --lib mcp::tests::extract_bundle_id_hex_argument_accepts_alias_shortcut -- --nocapture` (1 passed).
Latest sync (Torii MCP Connect create+ticket error-path follow-up): added endpoint regression coverage in `crates/iroha_torii/tests/mcp_endpoints.rs` to ensure `connect.session.create_and_ticket` and `iroha.connect.session.create_and_ticket` return raw non-`2xx` session-create responses unchanged (without ticket payload) when creation fails. Updated `crates/iroha_torii/docs/mcp_api.md` to document success vs non-`2xx` behavior explicitly. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/tests/mcp_endpoints.rs`, `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo test -p iroha_torii --test mcp_endpoints mcp_jsonrpc_connect_session_create_and_ticket -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo check -p iroha_torii`.
Latest sync (Torii MCP Connect create+ticket alias): added one-call Connect bootstrap tools `connect.session.create_and_ticket` and `iroha.connect.session.create_and_ticket` in `crates/iroha_torii/src/mcp.rs` so agents can create a Connect session and receive a role-matched WebSocket ticket in one MCP call. The new dispatcher enforces `role` (`app`/`wallet`), reuses existing `connect.session.create` + ticket builder paths, forwards non-2xx create responses unchanged, accepts `ticket_node_url` override (`node_url`/`node` fallbacks), and returns structured output with `status`, `sid`, `role`, `create`, and `ticket`. Added tool-list + endpoint alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs` and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (70 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (32 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/iroha_target_connect_mcp_check2 CARGO_INCREMENTAL=0 cargo check -p iroha_torii`.
Latest sync (state-view contention follow-up XXXIV): reduced gossip-admission and queue-drain contention in `crates/iroha_core/src/{gossiper.rs,queue.rs}`. Gossiper now acquires one committed-transactions snapshot per incoming gossip batch and reuses it for known-hash checks (including cached duplicate checks), and it reuses the first local routing decision during enqueue via `push_with_gossip_payload_with_state_and_routing` instead of routing each accepted gossip transaction twice. Queue state-pop now reuses one committed-transactions snapshot per drain pass in `pop_from_queue_with_state` (instead of calling `state.has_committed_transaction` per hash), and reroute/gossip-state paths continue to reuse per-pass snapshots with fallback view caching (`reroute_pending_transactions_with_state` + `gossip_batch_with_state`). Added regression coverage for the precomputed-routing enqueue path (`push_with_gossip_payload_with_state_and_routing_skips_router_lookup`). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/gossiper.rs crates/iroha_core/src/queue.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib push_with_gossip_payload_with_state_and_routing_skips_router_lookup -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib route_for_gossip_with_state_ -- --nocapture` (3 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib queue_accepts_gossip_payload_cache_with_state -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib gossip_skips_already_known_transaction_hashes -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib gossip_drops_route_mismatch -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib gossip_accepts_restricted_route_match -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib reroute_pending_transactions_with_state_uses_view_router_fallback -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib gossip_batch_with_state_skips_committed_entries -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing_b cargo test -p iroha_core --lib queue_throttling -- --nocapture` (1 passed).
Latest sync (Torii MCP transaction wait alias): added `iroha.transactions.wait` in `crates/iroha_torii/src/mcp.rs` so agents can wait for terminal pipeline outcomes using an existing transaction hash (`hash`/`transaction_hash`/`query.hash`) without re-submitting transaction bytes. The alias reuses the shared wait controls (`timeout_ms`, `poll_interval_ms`, `terminal_statuses`, `status_accept`) and now shares the unified terminal-status polling helper with submit-and-wait/contract-call-and-wait flows (helper updated to support optional submit metadata). Added list-surface + endpoint coverage (`mcp_jsonrpc_tools_call_agent_alias_transaction_wait_accepts_flat_hash`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, helper/unit coverage in `crates/iroha_torii/src/mcp.rs`, and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (69 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (32 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXXIII): removed runtime full-view snapshots from startup/runtime flows in `crates/irohad/src/main.rs` by switching to narrow accessors (`committed_height`, `world_view`, `query_view`, `commit_topology_snapshot`, `pipeline_snapshot`) and by refactoring local consensus-cap computation helper wiring to use world+height (`compute_consensus_handshake_caps` now delegates to `compute_consensus_handshake_caps_from_world` instead of requiring `StateView`). This removed all direct `state.view()` usage from `irohad` main startup paths (snapshot load logging, replay catch-up height, handshake-cap/fingerprint derivation, genesis topology/bootstrap parameter checks, NPoS roster adapter seed peers, and DAG recovery prepass strategy lookup) while preserving deterministic behavior. Updated related unit tests in `irohad` manifest/consensus metadata checks to use world+height helper inputs. Validation passed: `rustfmt --edition 2024 crates/irohad/src/main.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p irohad` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p irohad consensus_caps_follow_activation_height -- --nocapture` (2 test binaries passed; 0 failures).
Latest sync (state-view contention follow-up XXXII): reduced block-validation lock contention in `crates/iroha_core/src/block.rs` by removing full `state.view()` snapshots from `ValidBlock::validate_keep_voting_block_inner` and `ValidBlock::validate_keep_voting_block_with_events`. State-dependent static validation now runs on `state.query_view()` (`validate_static_state_dependent` narrowed to `StateReadOnly`), and committed-transaction heights are prefetched via `committed_heights_for_block` from `state.transactions.view()` then passed into `validate_static_with_snapshot`, so transaction-index locks are not held across signature batching/stateless validation work. Updated static-snapshot tests to use the new committed-height snapshot path. Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/block.rs crates/iroha_core/src/query/snapshot/mod.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib validate_static_snapshot_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib snapshot_iterable_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib handle_queries_iterable_assets_non_empty -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXXI): removed full-view acquisition from the shared query snapshot lane by switching `run_on_snapshot_with_mode` in `crates/iroha_core/src/query/snapshot/mod.rs` from `state.view()` to the lightweight `state.query_view()` path. This keeps client query validation/execution semantics unchanged (`StateReadOnly` API surface) while avoiding transaction-index/coarse view-lock acquisition for Torii query endpoint execution (`handle_queries_with_opts` route path). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/query/snapshot/mod.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib snapshot_iterable_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib handle_queries_iterable_assets_non_empty -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXX): removed Torii ZK IVM derive/prove runtime full-view snapshots by introducing a lightweight `State::query_view()` path (`StateQueryView`) in `crates/iroha_core/src/state.rs` and wiring IVM query-state plumbing to support it (`QueryStateRef::QueryView` + `QueryStateSource for StateQueryView` in `crates/iroha_core/src/smartcontracts/ivm/host.rs`). Switched Torii ZK derive/prove handlers in `crates/iroha_torii/src/lib.rs` to call `state.query_view()` instead of `state.view()` inside spawn-blocking prove/derive jobs, preserving deterministic query/IVM semantics while avoiding transaction-index/view-lock acquisition in this hot path. Added core accessor coverage (`query_view_matches_basic_read_only_snapshot_fields`). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/state.rs crates/iroha_core/src/smartcontracts/ivm/host.rs crates/iroha_torii/src/lib.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib query_view_matches_basic_read_only_snapshot_fields -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib zk_ivm_derive_returns_proved_payload_without_gas_used -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib zk_ivm_prove_job_completes_and_does_not_expose_gas_used -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXIX): removed additional Torii runtime full-view usage on startup/test harness configuration by adding `State::content_snapshot` in `crates/iroha_core/src/state.rs` and switching `crates/iroha_torii/src/lib.rs` content-config reads from `state.view().content.clone()` to `state.content_snapshot()`. Added accessor coverage (`content_snapshot_reflects_latest_content_config`) in core state tests. Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/state.rs crates/iroha_torii/src/lib.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib content_snapshot_reflects_latest_content_config -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXVIII): removed the remaining Torii app-auth runtime `state.view()` query-validation snapshot in `crates/iroha_torii/src/app_auth.rs` by switching `validate_iter_query_for_authority` to `world_view` + `latest_block_header_fast` and the new world-based query-validation path. Added core narrow accessors/APIs to support this: `State::latest_block_header_fast` in `crates/iroha_core/src/state.rs`, `Executor::validate_query_with_world_parts` in `crates/iroha_core/src/executor.rs`, and `ValidQueryRequest::validate_for_client_world_parts` in `crates/iroha_core/src/smartcontracts/isi/query.rs` (with `validate_for_client_parts` delegating through it). Added coverage for both new fast paths (`latest_block_header_fast_reads_latest_committed_header`, `validate_for_client_world_parts_matches_state_view_path`). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/executor.rs crates/iroha_core/src/smartcontracts/isi/query.rs crates/iroha_core/src/state.rs crates/iroha_torii/src/app_auth.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib latest_block_header_fast_reads_latest_committed_header -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib validate_for_client_world_parts_matches_state_view_path -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib verify_accepts_valid_signature -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXVII): removed additional Torii runtime full-view reads in proof-processing and SoraFS handlers by replacing `state.view()` with narrow accessors. In `crates/iroha_torii/src/zk_prover.rs`, switched VK lookup/verification paths (`find_vk_record_by_commitment`, `process_proof_attachment`) to `world_view` + `zk_snapshot` so backend guardrails and VK registry reads no longer take a full `StateView`. In `crates/iroha_torii/src/sorafs/api.rs`, switched capacity/pin snapshot paths (`handle_get_sorafs_capacity_state`, `pin_snapshot_with_attestation`) to `world_view`, `committed_height`, `latest_block_hash_fast`, and `chain_id_ref`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/zk_prover.rs crates/iroha_torii/src/sorafs/api.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib scan_and_report_single_attachment -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib capacity_state_returns_empty_snapshot -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib pin_registry_respects_norito_accept_header -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXVI): removed additional Torii consensus-route full-view reads by switching `crates/iroha_torii/src/routing/consensus.rs` handlers/helpers (`/v1/sumeragi/bls_keys`, `/v1/sumeragi/collectors`, `/v1/sumeragi/params`, `/v1/sumeragi/evidence/count`, `/v1/sumeragi/evidence`, `/v1/sumeragi/telemetry`, `/v1/sumeragi/vrf/epoch/{epoch}`, `/v1/sumeragi/commit_qc/{hash}`, and evidence decode/validation) from `state.view()` snapshots to narrow accessors (`world_view`, `committed_height`, `commit_topology_snapshot`, `chain_id_ref`, world-based Sumeragi helpers). Added world-based query helpers in `crates/iroha_core/src/query/mod.rs` (`evidence_count_from_world`, `evidence_list_snapshot_from_world`) so evidence endpoints no longer require `StateView`. Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/query/mod.rs crates/iroha_torii/src/routing/consensus.rs crates/iroha_core/src/state.rs crates/iroha_torii/src/lib.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib evidence_world_helpers_match_state_snapshot_helpers_on_empty_state -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib decode_and_validate_evidence_ -- --nocapture` (4 passed). Endpoint integration targets in this workspace configuration compile/run with zero discovered tests (`sumeragi_collectors_endpoint`, `sumeragi_params_endpoint`, `sumeragi_evidence_count_endpoint`, `sumeragi_evidence_list_endpoint`, `sumeragi_commit_qc_endpoints`).
Latest sync (Torii MCP transaction submit-and-wait alias): added `iroha.transactions.submit_and_wait` in `crates/iroha_torii/src/mcp.rs` so agents can submit signed transactions and poll pipeline status to terminal outcomes in one MCP call (defaults: `Committed`, `Applied`, `Rejected`, `Expired`). The alias reuses transaction payload shortcuts (`signed_tx_base64`/`tx_base64`/hex aliases), adds polling controls (`timeout_ms`, `poll_interval_ms`, `terminal_statuses`, `status_accept`), auto-extracts `tx_hash` from Norito submission receipts (with optional `hash`/`transaction_hash` override), and retries transient polling statuses (`404`, `429`, `5xx`) until timeout. Added helper-unit coverage (`extract_transaction_hash_from_submit_result_decodes_submission_receipt`, `extract_pipeline_status_kind_reads_pipeline_envelope`, `resolve_submit_wait_terminal_statuses_*`), endpoint alias coverage (`mcp_jsonrpc_tools_call_agent_alias_submit_and_wait_surfaces_submit_error`) in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (67 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (30 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXV): removed additional Torii ledger proof `StateView` contention by adding a Kura-backed `State::block_proofs_for_entry(...)` accessor in core (shared proof construction helper used by both `StateReadOnly` and `State`) and switching `handler_block_proof` to that state-based path instead of `state.view()`. Updated core block-proof coverage to exercise the new state accessor directly (`block_proofs_for_entry_produce_valid_merkle_paths`). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/state.rs crates/iroha_torii/src/lib.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib block_proof_handler_emits_norito -- --nocapture` (1 passed).
Latest sync (Torii MCP contract aliases): added `iroha.contracts.code.register`, `iroha.contracts.code.get`, `iroha.contracts.deploy`, `iroha.contracts.instance.create`, `iroha.contracts.instance.activate`, `iroha.contracts.call`, and `iroha.contracts.state.get` in `crates/iroha_torii/src/mcp.rs` so agents can run contract register/deploy/call/state flows through stable MCP names with flat shortcuts (`code_hash`/`hash`) and body fallback support. Expanded alias coverage in `crates/iroha_torii/tests/mcp_endpoints.rs` (tool list + endpoint dispatch), updated MCP docs/examples in `crates/iroha_torii/docs/mcp_api.md`, and confirmed current Connect coverage remains complete (`connect.*` + `iroha.connect.*` for `/v1/connect/session`, `/v1/connect/session/{sid}`, `/v1/connect/ws`, `/v1/connect/status`). Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`, `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (66 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (26 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXIV): removed additional gossip admission `StateView` contention in core by switching gossiper route/queue admission to state-based paths (`Queue::route_for_gossip_with_state`, new `Queue::push_with_gossip_payload_with_state`) and removing the legacy view-based gossip routing helper. This keeps gossip batch processing on narrow state/world accessors instead of holding a full `state.view()` snapshot during route verification + enqueue. Added queue coverage for the new API (`queue_accepts_gossip_payload_cache_with_state`) and updated routing helper coverage (`route_for_gossip_with_state_uses_router_decision`, plus existing fallback/no-state route tests), with gossip duplicate-skip behavior revalidated (`gossip_skips_already_known_transaction_hashes`). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/queue.rs crates/iroha_core/src/gossiper.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib queue_accepts_gossip_payload_cache_with_state -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib route_for_gossip_with_state_ -- --nocapture` (3 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib gossip_skips_already_known_transaction_hashes -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXIII): removed additional Torii/Core full-view hot-path contention by routing queue admission/reconfigure through narrow state accessors (`Queue::push_with_lane_with_state`, `Queue::reconfigure_nexus_with_state`, shared `push_checked_with_lane_context`, and state-based reroute) and switching Torii transaction enqueue (`handle_transaction_inner`) plus lane-lifecycle queue refresh (`handle_post_nexus_lane_lifecycle`) to these state-based paths instead of `state.view()` snapshots. Added queue regression coverage for the new enqueue API (`push_with_lane_with_state_accepts_multiple_transactions`, `push_with_lane_with_state_rejects_committed_transaction`) and reran lifecycle reconfigure coverage (`apply_lane_lifecycle_reconfigures_router_and_limits`). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/queue.rs crates/iroha_torii/src/routing.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib push_with_lane_with_state_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_core --lib apply_lane_lifecycle_reconfigures_router_and_limits -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XXII): removed Torii App API full-view transaction scans from `/v1/accounts/{account_id}/transactions` and `/v1/accounts/{account_id}/transactions/query` by introducing `committed_transactions_snapshot` (built from `committed_height` + `block_by_height` with `FindTransactions`-equivalent newest-first ordering) and executing filter/select/sort pagination against that snapshot instead of `state.view()`-backed `FindTransactions` queries. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/routing.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_perf_keepgoing cargo test -p iroha_torii --lib account_transactions_ -- --nocapture` (8 passed).
Latest sync (Torii MCP node-operations aliases): added `iroha.health`, `iroha.status`, `iroha.parameters.get`, and `iroha.node.capabilities` mapped to `/health`, `/status`, `/v1/parameters`, and `/v1/node/capabilities` so agents can run node preflight checks through stable MCP alias names. Added dispatcher/schema wiring in `crates/iroha_torii/src/mcp.rs`, list-surface + endpoint-dispatch coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Also resolved pre-existing non-`Send` handler compile failures in `crates/iroha_torii/src/routing.rs` by moving offline allowance lookups into synchronous helpers so `world_view` state does not cross `.await`. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs crates/iroha_torii/src/routing.rs`, `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (63 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (23 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed).
Latest sync (Torii MCP NFT query alias): added `iroha.nfts.query` mapped to `/v1/nfts/query` so agents can run NFT QueryEnvelope calls through flat MCP shortcuts (for example `filter`, `sort`, `limit`, `offset`) without raw OpenAPI tool names. Added dispatcher/schema wiring in `crates/iroha_torii/src/mcp.rs`, list-surface + endpoint alias tests in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (62 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (state-view contention follow-up XXI): removed additional Torii App API full-view reads in list/query scans by switching accounts and asset-definition handlers (`/v1/accounts`, `/v1/accounts/query`, `/v1/assets/definitions`, `/v1/assets/definitions/query`) plus account-assets/asset-holders handlers (`/v1/accounts/{account_id}/assets/query`, `/v1/assets/{definition_id}/holders`, `/v1/assets/{definition_id}/holders/query`) from `state.view()` + broad `Find*` scans to `world_view`-backed iterators with short-lived snapshot materialization (`accounts_iter`, `asset_definitions_iter`, `assets_in_account_iter`, `assets_by_definition_iter`). Also relaxed asset-holder `asset_id` filter validation/handling to accept string literals (including compressed account-address forms) instead of strict `AssetId` parse-only checks. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/routing.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --lib accounts_query_ -- --nocapture` (2 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --lib assets_definitions_query_ -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --test asset_definitions_endpoints -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --lib account_assets_ -- --nocapture` (7 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --lib asset_holders_ -- --nocapture` (9 passed).
Latest sync (state-view contention follow-up XX): removed additional Torii App API full-view reads by adding narrow hash-based block accessors in core state (`State::block_height_by_hash`, `State::block_by_hash`) and switching `/v1/explorer/blocks/{identifier}` detail lookup to `block_by_height`/`block_by_hash`; also replaced full `state.view()` reads with `world_view` snapshots in Nexus public-lane read endpoints (`/v1/nexus/public-lane/{lane_id}/validators|stake|rewards`) and subscription read/precheck handlers (`/v1/subscription-plans`, `/v1/subscriptions`, `/v1/subscriptions/{id}`, and subscription pause/resume/cancel/keep/charge-now/create prechecks). Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/state.rs crates/iroha_torii/src/routing.rs`, `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo check -p iroha_core --lib`, `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo check -p iroha_torii`, `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_core --lib block_by_hash_reads_kura_without_state_view -- --nocapture` (1 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --lib subscription_api_tests:: -- --nocapture` (18 passed), `CARGO_TARGET_DIR=/tmp/iroha_target_bridge_followup3 cargo test -p iroha_torii --lib pending_rewards_filter_by_asset_id -- --nocapture` (1 passed).
Latest sync (state-view contention follow-up XIX): removed Torii bridge-finality full-view reads by introducing a narrow bridge read surface (`BridgeStateReadOnly`) in `crates/iroha_core/src/bridge.rs` and switching finality proof/bundle builders plus `/v1/bridge/finality/{height}` and `/v1/bridge/finality/bundle/{height}` handlers to use narrow accessors (`chain_id_ref`, `block_by_height`, `world_view` validator PoP lookup) instead of `state.view()`. Validation passed: `rustfmt --edition 2024 crates/iroha_core/src/bridge.rs crates/iroha_torii/src/routing.rs`, `cargo check -p iroha_core --lib`, `cargo check -p iroha_torii`, `cargo test -p iroha_core --test bridge_finality_proof -- --nocapture` (9 passed), `cargo test -p iroha_torii --test bridge_finality_endpoint -- --nocapture` (1 passed).
Latest sync (Torii MCP subscription write aliases): completed subscription action coverage with `iroha.subscriptions.plans.create`, `iroha.subscriptions.create`, `iroha.subscriptions.cancel`, `iroha.subscriptions.pause`, `iroha.subscriptions.resume`, `iroha.subscriptions.keep`, `iroha.subscriptions.usage`, and `iroha.subscriptions.charge_now` so agents can operate subscription lifecycle/billing flows via MCP without raw OpenAPI names. Added shared action dispatch/tool-generation wiring in `crates/iroha_torii/src/mcp.rs` (flat `subscription_id`/`id` shortcuts + object-body normalization), list-surface assertions + action endpoint alias tests in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs updates listing the full action family in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (61 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (23 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP subscription read aliases): added `iroha.subscriptions.plans.list`, `iroha.subscriptions.list`, and `iroha.subscriptions.get` so agents can inspect subscription plans/subscriptions and fetch subscription detail via MCP without nested payload envelopes. Added dedicated dispatch handlers and schemas in `crates/iroha_torii/src/mcp.rs`, new `subscription_id` extraction coverage, list-surface assertions + endpoint alias-call tests in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (57 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (21 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP account QR alias): added `iroha.accounts.qr` mapped to `/v1/explorer/accounts/{account_id}/qr`, with flat `account_id` shortcut handling so agents can fetch account QR payloads via MCP without nested `path` payloads. Added dedicated dispatch/schema wiring in `crates/iroha_torii/src/mcp.rs`, list-surface assertion + endpoint alias-call coverage in `crates/iroha_torii/tests/mcp_endpoints.rs`, and docs/examples in `crates/iroha_torii/docs/mcp_api.md`. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (54 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (20 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (state-view contention follow-up XVIII): removed more Torii non-consensus full-view reads by switching Sumeragi consensus-key listing, SoraDNS directory-latest lookup, Sumeragi telemetry VRF summary, VRF epoch lookup, and commit-QC lookup handlers to `world_view` snapshots, and switched `/version` to `committed_height` + `block_by_height` instead of a full `state.view()` snapshot. Validation passed: `rustfmt --edition 2024 crates/iroha_torii/src/routing.rs`, `cargo check -p iroha_torii`, `cargo test -p iroha_torii --lib version_tests:: -- --nocapture` (1 passed), `cargo test -p iroha_torii --lib soradns_tests:: -- --nocapture` (3 passed), `cargo test -p iroha_torii --lib status_tests:: -- --nocapture` (15 passed).
Latest sync (Torii MCP bridge): Torii now exposes native `/v1/mcp` GET/POST with JSON-RPC handling (`initialize`, `tools/list`, `tools/call`) backed by OpenAPI-derived tool descriptors plus Connect helper tools (`connect.ws.ticket`, `connect.session.create/delete`, `connect.status`). New `torii.mcp` config fields are wired through user/actual/default layers (enable switch, request/tool limits, operator route exposure, rate/burst), the MCP registry now includes namespaced Connect aliases (`iroha.connect.ws.ticket`, `iroha.connect.session.create/delete`, `iroha.connect.status`) and stable agent-friendly aliases for account/transaction operations (`iroha.accounts.list`, `iroha.accounts.resolve`, `iroha.accounts.transactions`, `iroha.transactions.submit`, `iroha.transactions.status`), Connect MCP ergonomics now accept top-level `sid`/`node` shortcuts for session creation plus role-matched `token_app`/`token_wallet` aliases for WS ticket generation, OpenAPI now documents the MCP endpoint/tag, MCP usage/docs now include concrete Connect session lifecycle, account, and transaction call examples (`crates/iroha_torii/docs/mcp_api.md`), and router-level MCP endpoint coverage now validates capabilities/list/call (including OpenAPI-derived `torii.healthCheck` route dispatch), token-gated access behavior (forbidden without inbound `x-api-token`, accepted with valid MCP request header), forwarded-header dispatch behavior (API-version rejection via `tools/call.arguments.headers`), JSON-RPC protocol validation (non-object payload + unsupported `jsonrpc` version), JSON-RPC batch execution/empty-batch rejection, unknown-tool invalid-params handling, invalid JSON, rate limiting, oversized payload handling, disabled-route gating, paginated account/transaction/connect tool discovery through `tools/list`, account transaction dispatch with `arguments.path` + `arguments.query`, agent-alias account/tx/connect dispatch checks, binary transaction submission dispatch via both `torii.post_transaction` and `iroha.transactions.submit`, and MCP-driven Connect session lifecycle dispatch (`connect.session.create`/`connect.ws.ticket`/`connect.status`/`connect.session.delete`) plus namespaced alias dispatch (`iroha.connect.session.create`/`iroha.connect.ws.ticket`/`iroha.connect.status`/`iroha.connect.session.delete`). Targeted validation passed (`cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture`, `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture`, `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture`, `cargo test -p iroha_config --test fixtures -- --nocapture`).
Latest sync (Torii MCP alias ergonomics): `iroha.accounts.*`/`iroha.transactions.*` aliases now dispatch through dedicated MCP handlers that accept flat AI-agent arguments without requiring nested `path`/`query`/`body` envelopes (`account_id`, `hash`, `literal`, top-level query keys, transaction payload aliases `signed_tx_base64`/`tx_base64`, and hex aliases `signed_tx_hex`/`tx_hex`/`body_hex`). The MCP alias surface now also includes account-inspection helpers (`iroha.accounts.assets`, `iroha.accounts.permissions`), query-oriented account aliases (`iroha.accounts.query`, `iroha.accounts.transactions.query`, `iroha.accounts.assets.query`) with QueryEnvelope shortcuts (`filter`, `sort`, `limit`, `offset`, `fetch_size`, `address_format`) when `body` is omitted, and account onboarding via `iroha.accounts.onboard` (`alias`/`account_id` shortcuts). Alias schemas/docs were updated to describe these shortcuts and `tools/call.result.isError` semantics for HTTP `>= 400`, and endpoint coverage was expanded with flat-argument alias tests (status hash shortcut, account query shortcut, account-transactions/assets query shortcuts, account-assets shortcut, account-permissions shortcut, account-onboard shortcut, account-resolve literal shortcut, hex transaction-submit shortcut). Targeted validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture`, `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture`, `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture`.
Latest sync (Torii MCP alias expansion): extended agent-friendly aliases beyond account/tx basics with portfolio/domain/asset discovery coverage: `iroha.accounts.portfolio` (flat `uaid`), `iroha.domains.list`, `iroha.domains.query`, `iroha.assets.definitions`, `iroha.assets.definitions.query`, `iroha.assets.holders`, and `iroha.assets.holders.query` (flat `definition_id`). Added dedicated dispatch handlers and tool schemas for these aliases, added helper extraction coverage for `uaid`/`definition_id`, expanded MCP endpoint tests with list-surface assertions and flat-argument alias calls for each new family, and updated MCP docs with the new aliases/examples. Validation reruns are recorded in the latest Torii MCP entries above.
Latest sync (Torii MCP transaction explorer aliases): added `iroha.transactions.list` (flat explorer filters) and `iroha.transactions.get` (flat `hash`/`transaction_hash` shortcut) so agents can inspect committed/rejected history and fetch transaction details through MCP without nested payloads. Added dedicated dispatch handlers, tool schemas, helper extraction coverage, docs examples, and endpoint tests for both aliases. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (41 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (14 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP instruction explorer aliases): added `iroha.instructions.list` (flat explorer filters) and `iroha.instructions.get` (flat `hash`/`transaction_hash` + `index`/`instruction_index` shortcuts) so agents can inspect instruction history and fetch instruction detail through MCP without nested payload envelopes. Added dedicated dispatch handlers, tool schemas, helper extraction coverage, list-surface assertions, endpoint alias tests, and MCP docs examples for instruction explorer usage. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (44 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (16 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP block explorer aliases): added `iroha.blocks.list` (flat explorer pagination filters) and `iroha.blocks.get` (flat `identifier`/`block_identifier`/`block_height`/`block_hash` shortcuts) so agents can inspect block history and fetch block detail through MCP without nested payload envelopes. Added dedicated dispatch handlers, tool schemas, block-identifier extraction coverage, list-surface assertions, endpoint alias tests, and MCP docs examples for block explorer usage. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (46 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (17 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP asset explorer aliases): added `iroha.assets.list` (flat explorer pagination/filter fields) and `iroha.assets.get` (flat `asset_id`/`id` shortcuts) so agents can inspect holdings and fetch asset detail through MCP without nested payload envelopes. Added dedicated dispatch handlers, tool schemas, asset-id extraction coverage, list-surface assertions, endpoint alias tests, and MCP docs examples for explorer asset usage. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (48 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (18 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP NFT explorer aliases): added `iroha.nfts.list` (flat explorer pagination/filter fields) and `iroha.nfts.get` (flat `nft_id`/`id` shortcuts) so agents can inspect NFT holdings and fetch NFT detail through MCP without nested payload envelopes. Added dedicated dispatch handlers, tool schemas, NFT-id extraction coverage, list-surface assertions, endpoint alias tests, and MCP docs examples for explorer NFT usage. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (50 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (19 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (Torii MCP explorer detail aliases): added `iroha.accounts.get`, `iroha.domains.get`, and `iroha.assets.definitions.get` so agents can fetch explorer account/domain/asset-definition details with flat shortcut arguments (`account_id`, `domain_id`/`domain`, `definition_id`) without nested `path` payloads. Added dedicated dispatch handlers, tool schemas, domain-id extraction coverage, list-surface assertions, endpoint alias tests, and MCP docs examples for these detail lookups. Validation passed: `cargo check -p iroha_torii`, `cargo test -p iroha_torii --test mcp_endpoints -- --nocapture` (53 passed), `cargo test -p iroha_torii --lib mcp::tests:: -- --nocapture` (20 passed), `cargo test -p iroha_torii --lib openapi::tests::generated_spec_includes_documented_paths -- --nocapture` (1 passed), `rustfmt --edition 2024 crates/iroha_torii/src/mcp.rs crates/iroha_torii/tests/mcp_endpoints.rs`.
Latest sync (offline petal transport): `offline petal` now defaults to a new `sora-temple` renderer (katakana tile field + `天` logo silhouette + data-derived 3-ring banding), exposes `render_style` + `estimated_payload_bytes_per_second` in encode manifests, adds `offline petal eval-capture` for deterministic distant/moving-capture robustness gates (default `--min-success-ratio 0.95`), and now includes `offline petal score-styles` to emit repeatable ranked JSON style reports (`aesthetic_score`, `decode_completion_score`, `effective_payload_bytes_per_second`, `overall_score`, `recommended_style`). 2026-02-16 style iteration increased tile visual mass (larger tile fill, softer logo-cell framing), boosted `sora-temple-ghost` logo legibility, and revalidated `cargo test -p iroha_cli offline::petal::tests:: -- --nocapture` (pass). Latest default-profile capture eval (`chunk_size=140`, `grid_size=41`, `~3310 B/s`): `sora-temple` 86/86 complete, `sora-temple-ghost` 86/86 complete, `sora-temple-aegis` 85/86, `sora-temple-radiant` 81/86, `sora-temple-bold` 77/86, `sora-temple-command` 69/86.
Latest sync (subscriptions billing): `subscription_bill` now sanitizes trigger metadata before rescheduling by removing reserved lifecycle keys (`__registered_block_height`, `__registered_at_ms`), preventing trigger-registration rejection and restoring invoice persistence in `integration_tests/tests/subscriptions.rs`. Targeted validation passes: `cargo test -p integration_tests --test subscriptions subscription_scenarios -- --nocapture`, `cargo test -p iroha_core --lib subscription_bill_ -- --nocapture`.
Latest sync (test-network permits): stale network-permit reclaim now uses errno-based PID liveness checks (`nix::sys::signal::kill`) instead of locale-sensitive shell stderr parsing, eliminating an indefinite wait class in `NetworkBuilder::build()` when dead permit-holder PIDs were misdetected as live.
Latest sync (test-network permit wait diagnostics): `iroha_test_network` now prints lock-owner details while waiting for a network permit and bounds waits by default (5 minutes) so blocked integration runs fail clearly instead of hanging indefinitely. `IROHA_TEST_NETWORK_PERMIT_WAIT_TIMEOUT` overrides the bound (`0` disables timeout), and panic output now includes the owning PID/lock file for quick triage.
Latest sync (test-network permit namespace isolation): default permit paths now resolve to `$TMPDIR/iroha_test_network_permits/ppid-<parent_pid>` when `IROHA_TEST_NETWORK_PERMIT_DIR` is unset, so unrelated concurrent Cargo invocations do not contend on the same global lock while still allowing explicit shared permit dirs when requested.
Latest sync (time-trigger scenarios): `integration_tests/tests/triggers/time_trigger.rs` now treats leader hints as advisory and falls back to rotating submissions across the highest-height peers when the hinting peer is weakly connected, reducing `time_trigger_scenarios` block-height timeout flakes. Targeted validations passed: `cargo test -p integration_tests --test mod triggers::time_trigger::time_trigger_scenarios -- --nocapture`, `cargo test -p integration_tests --test mod triggers::time_trigger::timeout_tests -- --nocapture`.
Latest sync (IVM ZK verification): `SYSCALL_PROVE_EXECUTION`/`SYSCALL_VERIFY_PROOF` now return `NotImplemented` in default hosts (reserved for a future end-to-end execution proving stack), and `CoreHost` ZK verify syscalls now verify first-release `iroha_data_model::zk::OpenVerifyEnvelope` payloads through the full backend verifier path with VK-registry binding checks (circuit/schema/namespace/manifest + config caps).
Latest sync (IvmProved execution verification): `Executable::IvmProved` admission now verifies `halo2/ipa:ivm-execution-v1` envelopes end-to-end: (1) public-input binding to `(code_hash, overlay_hash, events_commitment, gas_policy_commitment)`, (2) backend proof verification, and (3) deterministic IVM replay that must reproduce the submitted overlay and commitments. The proved bytecode must have the IVM ZK mode bit set (`mode & ZK != 0`). Gas usage is committed (not exposed) via a trace-hash-bound `gas_policy_commitment` so it cannot be brute-forced without reproducing the execution trace; `ivm-execution-v1` now uses a canonical public-input schema descriptor/hash for VK binding (instances are checked directly from proof bytes). The legacy `halo2/ipa:ivm-overlay-bind-v1` binding-only circuit remains explicitly rejected. Torii `/v1/zk/ivm/derive` derives an `IvmProved` payload by executing bytecode (commitments only, no plaintext `gas_used`), and Torii `/v1/zk/ivm/prove` jobs generate real Halo2 IPA proof attachments by loading proving key bytes from `torii.zk_prover_keys_dir/<backend>__<name>.pk` (and VK bytes from `.vk` when not stored inline), are bounded by `torii.zk_ivm_prove_max_inflight` + `torii.zk_ivm_prove_max_queue`, and use configurable job retention via `torii.zk_ivm_prove_job_ttl_secs` + `torii.zk_ivm_prove_job_max_entries` (pending/running jobs are cancelled best-effort on `DELETE` or TTL eviction; status values are `pending|running|done|error`; telemetry exports `torii_zk_ivm_prove_inflight`/`torii_zk_ivm_prove_queued`; CLI helpers live under `iroha app zk ivm ...`).
Latest sync (Torii STARK prove parity): `/v1/zk/ivm/prove` now dispatches by backend (`halo2/ipa` and `stark/fri-v1/*`) and emits backend-matching proof attachments for `ivm-execution-v1`, using native STARK envelope synthesis for STARK VKs. `iroha_test_network` now forwards `TEST_NETWORK_IROHAD_FEATURES` into spawned `irohad` builds so multi-peer tests can enable `zk-stark`, `integration_tests/tests/zk_stark_network.rs` now covers a 4-peer STARK governance + shielded IVM flow with mismatch rejection checks for both ballot and `IvmProved` paths (network startup is skipped automatically in loopback-restricted sandboxes), `crates/iroha_core/tests/zk_stark.rs` now includes mixed-governance backend coverage (`governance_accepts_valid_halo2_and_stark_ballots_in_same_state`) plus negative `CreateElection` role-circuit checks for both ballot and tally VKs, STARK vote helpers enforce ballot/tally circuit roles (`vote-ballot-v1`/`vote-tally-v1`), and shared backend/circuit policy constants now keep Torii/core allowlist checks aligned (`ZK_BACKEND_HALO2_IPA`, `ZK_BACKEND_STARK_FRI_V1`, `IVM_EXECUTION_V1_CIRCUIT_ID`).
Latest sync (STARK verifier negative paths): core STARK tests now explicitly cover malformed envelope rejection, domain-tag tamper rejection, STARK `OpenVerifyEnvelope` circuit-id mismatch rejection, and malformed STARK wrapper handling via timing-report verification (`verify_backend_with_timing`) to ensure malformed payload paths fail deterministically without panics.
Latest sync (STARK guardrails/matchers): overlay + voting extraction paths now use shared backend constants (`ZK_BACKEND_HALO2_IPA`) instead of repeated string literals, and guardrails include explicit STARK max-payload-size regression coverage (`guardrails_enforce_stark_max_proof_bytes`).
Latest sync (backend normalizer constants): Halo2 alias strip/normalization helpers in core + Torii now key off `ZK_BACKEND_HALO2_IPA` (instead of repeated `halo2/ipa::*` literals), keeping circuit-id canonicalization aligned with backend dispatch and allowlist policy across STARK/Halo2 voting, shielded admission, and Torii derive/prove checks. Targeted reruns (`verify_stark_open_verify_envelope_rejects_`, `extract_vote_public_inputs_handles_stark_envelope`, `resolve_ballot_and_tally_vk_accept_valid_stark_attachments`, `overlay_rejects_stark_ivm_proved_when_circuit_id_mismatch`, `zk_ivm_prove_job_completes_for_stark_backend`) pass.
Latest sync (P2P SCION): switched to automatic capability-driven SCION preference: peers advertise `scion_supported` in handshake metadata, gossip transport capabilities through peer gossip, and the dialer prefers SCION only when both sides support it with automatic fallback to legacy transports; legacy `network.scion_*` config knobs are now ignored at runtime and retained only for compatibility parsing. SCION transport counters (`p2p_scion_inbound_total`, `p2p_scion_outbound_total`) remain exported through telemetry/docs.
Latest sync (offline receipts, first release): receipt/challenge payloads now carry `sender_certificate_id` instead of embedded certificates across Rust core/torii/cli plus Swift/Android/connect bridges, and settlement verification resolves sender certificates by id from ledger state. Targeted Rust compile checks pass, Swift receipt-challenge tests pass, and Android `./gradlew test` passes when `JAVA_HOME=/opt/homebrew/opt/openjdk@21` is set.
Latest sync: Pipeline events now batch via `EventBox::PipelineBatch` (Torii WS/SSE consumers expand batches); DA shard cursor journal persistence now runs in a background worker during block apply; consensus payload frames now use the block-sync cap (defaults raised to 16 MiB) with the RBC chunk default at 256 KiB, and BlockSyncUpdate trimming preserves NPoS stake snapshots to avoid QC stalls; NPoS on-chain parameters now drive mode-aware timing (block/commit timeouts), collector fan-out, election/reconfig policy, and activation lag, with the genesis consensus fingerprint capturing the full NPoS election payload plus epoch seed and docs/unit coverage updated; Governance ZK ballot hint formats are standardized to `root_hint`/`nullifier` across public inputs and BallotProof hex strings (optional `0x`/`blake2b32:` prefixes) with deprecated alias keys rejected, SDK/CLI/bridge updates, and refreshed docs; Torii now rejects deprecated ZK V1 alias keys directly from JSON payloads and enforces canonical owner hints for ZK public inputs with regression coverage; canonical IH58 owner hints are now required across CLI, JS SDK (torii client + builders), Rust/JS bridges, Swift SDK, Python SDKs, and mock client paths with new tests; governance portal + JS SDK ZK examples updated to canonical owners; RBC INIT/READY/DELIVER now bind roster hashes and INIT rebroadcasts go out even without cached chunks; RBC persistence now derives missing roster snapshots and READY rebroadcasts enforce roster-hash matching; RBC READY/DELIVER handling now accepts derived rosters when roster hashes match (refreshing derived snapshots on mismatches when available; otherwise stashing until INIT) and local emission uses computed chunk roots when INIT is missing, READY/DELIVER now seed missing expected chunk roots and persisted sessions adopt computed roots even when INIT metadata is missing, manifests without commit hashes can still reload sessions, the RBC store retains delivered chunk bytes for sampling with zero-sized chunk configs rejected, payload rebroadcasts continue while chunks are missing even after READY quorum, and pending RBC session caps no longer evict active stashes; exec-witness fallback added for missing pre-vote roots; DA gate now treats locally available payload matches as availability evidence (missing_local_data clears without waiting on RBC delivery); asset integration tests re-run (all six) passed; `cargo test -p iroha_core` still timing out after 3h; integration-test revalidation still pending (see `status.md` for details); NFT register/unregister now reject missing domains without panicking and include regression coverage; offline balance proof builders now reject invalid blinding/scalar lengths without panicking; tx admission compares signature/instruction/bytecode limits using u64 counts to avoid usize overflow panics; block sync now avoids decode panics on invalid payloads and validates block sequences at handling time, plus NPoS share batches drop roster hints without stake snapshots to avoid invalid roster evidence; NPoS QC validation now enforces stake-quorum snapshots for commit and block-sync QCs with unit coverage; DA/RBC defaults now keep `sumeragi.da.enabled=true` to match the v3 requirement; commit QC persistence now records committed/known-block certificates into world/journal/sidecar with regression coverage; block sync now processes commit votes for known blocks without roster hints, with regression coverage; Kagami localnet now supports `--sora-profile` for Nexus/dataspace presets with enforced peer minimums and Sora startup flags; permissioned block sync now requires roster evidence, rejects commit QCs whose view mismatches the block header, enforces validated commit-quorum gating, and block-sync share filtering validates commit QCs before letting them bypass signature checks; permissioned view rotation uses u64 indices to stay deterministic across architectures, and validator checkpoints embed the view index plus parent/post state roots so receivers can validate them without commit-QC history (invalid QC hints are ignored) and satisfy block-sync quorum gating even when block signatures are trimmed; P2P tx gossip now budgets for encrypted frame overhead to avoid frame-too-large disconnects (Izanami validation pending); block sync ShareBlocks batches now trim to P2P frame caps to avoid oversize disconnects. Torii `/v1/node/capabilities` now includes `data_model_version`; Swift `ToriiClient` enforces it for submissions; JS + Python SDKs enforce it for submissions; Rust `Client` enforces it for submissions. Nexus fee sponsorship now requires `CanUseFeeSponsor` grants and contract calls require `gas_limit` metadata; JS SDK + CLI contract simulation now enforce the required gas limit with updated docs/tests. Sumeragi replay/QC tests now use valid non-genesis Ed25519 signers and mode-aware timing with targeted unit passes. Sumeragi block-sync and BlockCreated unit tests now use valid signed blocks and pre-seeded pending payloads; RBC payload bundles now emit INIT when expected chunk digests are available without cached bytes; BLS blstrs verify now rejects non-canonical/identity encodings with parse-error tests; Sumeragi/localnet idle worker-loop ticks now throttle to a block/commit-derived minimum gap to reduce idle CPU churn; the worker loop now refreshes drain budgets and tick gaps each iteration to follow on-chain timing updates; the worker loop now wakes on queue enqueues, suppresses ticks when fully idle, and blocks until new work or the next tick deadline to avoid fixed polling; tick deadlines now honor missing-block retry/view-change windows and commit-inflight timeouts to avoid idle polling between scheduled work; tick deadlines now cover pending-block timeouts/retention, RBC rebroadcast/TTL, and idle view-change windows, with commit/RBC persist workers waking the loop, RBC stale scheduling covered by unit tests, and new coverage for commit/RBC persist wake signals; `cargo test --workspace` timed out after 60m while running `integration_tests/tests/mod.rs` (tests completed before the timeout passed); test-network log drains now exit on shutdown notify to avoid peer-task timeout warnings with unit coverage (`cargo test -p iroha_test_network log_drain_exits_on_shutdown_notify -- --nocapture` passed); Android Norito fixture manifest tests now decode signature fields and re-encode signed payloads to guard against serialization drift, with fixture authorities normalized to canonical payloads and signed fixtures updated to the 4-field layout with refreshed hashes. Pacemaker bootstrap now bypasses online-peer deferral before the first successful proposal (unit coverage added) to avoid localnet stalls while peer connectivity is still initializing. BlockCreated/FetchPendingBlock/RBC INIT now route through the block queue with block draining ahead of block-payload work to avoid missing-block stalls, while BlockSyncUpdate stays on the block-payload queue. Empty-child fallback configuration is removed so pacemaker stays idle without queued work. Proposal assembly now defers when all queued transactions exceed the payload budget, avoiding heartbeat-only blocks. Kagami localnet NPoS bootstrap stake now raises to the configured min_self_bond to keep validator registration valid. Stale-view RBC handling now accepts messages when the payload is only present via an aborted pending block so READY quorum can still form after view changes. NEW_VIEW missing-block recovery now falls back to commit-QC history rosters when the commit topology is empty, and proposal assembly defers when the highest QC block is missing with unit coverage added. Android SDK instruction payloads are now wire-only; legacy InstructionBuilders removed and InstructionBox.fromNorito now requires wire payloads. Offline allowances now re-derive escrow bindings from `offline.enabled` metadata when the escrow map is empty so escrow-required registrations succeed after metadata updates.
Latest sync (100 TPS): NPoS commit quorum timing now floors to canonical `effective_commit_time()` in core Sumeragi timing resolution, removing unstable short quorum windows during 7-peer 100 TPS runs; `scripts/tx_load.py` now falls back across peer `/status` endpoints and disables queue gating once per shard on prolonged status loss, preventing shard abort loops and single-endpoint throughput under-reporting.
Latest sync (100 TPS rerun): reduced Sumeragi requeue churn (`InBlockchain`/`IsInQueue` treated as benign duplicates), tightened quorum reschedule stall tracking (`quorum_stall_age`/progress-age) and cached-slot pacemaker timeout handling, plus gossip fast-path skip for already-known tx hashes. Post-fix 7-peer 100 TPS runs completed in both modes: permissioned `artifacts/localnet-100tps-profile/20260208T202436-permissioned` (18000/18003/18004 submitted/admitted/committed), NPoS `artifacts/localnet-100tps-profile/20260208T204917-npos` (18000/18000/18000), both with queue baseline/final 0->0.
Latest sync (100 TPS follow-up, 2026-02-08): additional both-mode reruns completed with fresh artifacts `artifacts/localnet-100tps-profile/20260208T235026-permissioned` and `artifacts/localnet-100tps-profile/20260208T235403-npos` (6000/6000/6000 submitted/admitted/committed each). Remaining bottlenecks are NPoS view-change churn (`view=1/2` spikes), repeated RBC READY quorum deferrals, and gossip decode/parse CPU hotspots (`decode_field_canonical`, `decode_gossip_transaction_payload`, `MessageReader::parse_next_encrypted_frame`). A longer 180s both-mode rerun still needed manual tx-load termination and saw NPoS pprof timeout under load; the profiling harness now enforces bounded tx-load wait/termination and adds `--load-drain-timeout` to prevent indefinite hangs.
Latest sync (100 TPS follow-up, 2026-02-09): perf harness now applies logger overrides directly in generated peer TOML (`deploy_localnet.sh --logger-level/--logger-filter`, `run_100tps_profile_localnet.sh --logger-level` default `warn`), eliminating per-tx `log_isi` spam during load tests. Fresh both-mode 7-peer 100 TPS rerun artifacts `artifacts/localnet-100tps-profile/20260209T091257-permissioned` and `artifacts/localnet-100tps-profile/20260209T091745-npos` reached ~93.9 TPS admitted (permissioned) and ~94.8 TPS admitted (NPoS) with queue `0 -> 0`; remaining hotspots are runtime I/O scheduling and Norito decode, not logging churn.
Latest sync (integration tests): seven-peer consistency now waits for submitter mint visibility and requires RBC delivery from commit quorum.
Latest sync (integration tests): `sumeragi_rbc_recovers_after_peer_restart` now enforces an in-flight RBC restart window (recovery-sized payload/chunks, persisted in-flight snapshot requirement, and recovered-flag check before post-restart commit wait); clean end-to-end rerun remains pending due concurrent build/session instability in the current environment.
Latest sync (integration tests): `sumeragi_rbc_recovers_after_restart_with_roster_change` now passes in isolated reruns (3/3): two runs with `IROHA_TEST_ALLOW_REENTRANT_BUILD=1 cargo test -p integration_tests --test mod sumeragi_da::sumeragi_rbc_recovers_after_restart_with_roster_change -- --nocapture --exact --test-threads=1`, plus one run with the default invocation (no env override).
Latest sync (Sumeragi test stability): `phase_ema_seeded_on_startup` / `phase_ema_seeded_on_mode_flip` now reseed+retry status snapshot checks, and `rebroadcast_precommit_votes_keep_collectors_when_below_quorum` disables relay backpressure in-fixture, removing parallel-suite flakes tied to shared status/relay counters.
Latest sync (proposal perf): cached confidential feature digests + queued tx size/gas to reduce proposal assembly overhead, and proposal gating now reuses the tip snapshot for stale-pending checks.
Latest sync (NFT metadata): detached parallel apply now performs delta-aware permission checks for NFT metadata edits so they stay in the detached path without bypassing authz; unit coverage added and the integration lifecycle scenario revalidated.
Latest sync (detached permissions): detached permission deltas now apply last-write-wins semantics for account/role permission changes; account/role grant→revoke ordering validated in integration tests (detached metrics asserted only when Nexus is enabled because permissioned networks suppress lane metrics).
Latest sync (roster/activation): pacemaker proposals and live votes now use the active validator roster only (no commit-QC fallback), online-peer gating counts only validators, validator activation now defers to the next epoch boundary, and genesis bootstrap falls back to the genesis roster if commit/world rosters are empty.
Latest sync (worker loop): tick scheduling now uses a derived busy gap (clamped below the idle gap) when queues are backlogged so consensus progress is not throttled by pending drains; unit coverage added. The worker loop now supports a parallel ingress mode (per-queue threads + ticketed gate) controlled by `sumeragi.advanced.worker.parallel_ingress` (default true), with docs/config updates and new unit coverage.
Latest sync (fixtures): SDK transaction payload fixtures now store wire-only instruction entries (no legacy kind/arguments), exporter strips legacy instruction fields on regen, legacy instruction-schema/double-payment JSON fixtures and Norito blobs were removed across Android/Swift/Python (and the root Norito manifest), and fixture regen scripts no longer sync legacy schema files.
Latest sync (IVM fixtures): regenerated `crates/ivm/tests/fixtures/predecoder/mixed/index.json` to drop the stale `v1.7` artifact entry so `predecoder_fixture_verify` matches ABI v1 metadata parsing (`version_minor == 0`).
Latest sync (consensus timing): on-chain `pacing_factor_bps` now scales the effective block/commit targets and is surfaced via status/params outputs alongside the derived timeouts.
Latest sync (NPoS phase scaling): NPoS per-phase timeouts are normalized to the default pipeline budget so the sum of propose/DA/prevote/precommit/commit matches the target block time (without changing overrides).
Latest sync (consensus liveness): quorum reschedules and idle view changes now proceed after the availability timeout even if RBC backlog persists, avoiding DA deadlocks under partial payload loss.
Latest sync (genesis): genesis transaction validation now uses the block timestamp during static checks to avoid clock-skew rejections.
Latest sync (config): NPoS pacemaker timeouts now derive from on-chain `SumeragiParameters.block_time_ms` with optional `sumeragi.advanced.npos.timeouts` overrides, `sumeragi_npos_parameters` no longer carries timeout fields, and the `sumeragi.npos.block_time_ms` fallback is removed; docs/templates refreshed and minimal config fixtures now include PoP coverage, with the snapshot refreshed to drop the removed NPoS block-time/timeouts fields and match current crypto defaults.
Latest sync (CLI tests): updated `integration_tests/tests/iroha_cli.rs` to use `iroha ops executor` and `iroha ledger domain` command groups.
Latest sync (tests): `cargo test -p iroha_core --tests --features "zk-tests halo2-dev-tests" zk` timed out after 1800s while running `gov_auto_close_zk_requires_tally`; ZK unit tests passed, including `kaigi_usage_backend_accepts_valid_proof`.
Latest sync (STARK governance e2e): added `stark_governance_submit_and_finalize_accept_valid_proofs` in `crates/iroha_core/tests/zk_stark.rs`, covering production `CreateElection` → `SubmitBallot` → `FinalizeElection` with real `stark/fri-v1/sha256-goldilocks-v1` verification, backend-specific envelope/schema checks, and VK-reference resolution; targeted and full `zk_stark` test target reruns pass.
Latest sync (ZK guardrails test follow-up): `zk_asset_vk_enforcement` and `zk_confidential_events` fixtures now explicitly enable `state.zk.halo2.enabled` and set `state.zk.verify_timeout = Duration::ZERO` so positive transfer/unshield paths are validated under the current guardrail-enforced verifier flow (`verify_backend_with_timing_checked`); targeted reruns for those tests and `gov_finalize_real_vk` now pass with `--features "zk-tests halo2-dev-tests zk-halo2-ipa zk-stark"`.
Latest sync (zk voting/tally integration): the remaining feature-gated Torii tally-handler regression is closed; `cargo test -p iroha_torii --features "halo2-dev-tests" --test zk_vote_tally_handler` now passes with proof/tally shape aligned to `FinalizeElection` invariants (one public input per tally option), alongside passing STARK/Halo2 governance/shielded-path targeted suites (`zk_stark`, `zk_asset_stark_envelope`, `zk_verify_vendor_e2e`, `zk_prover_integration`).
Latest sync (tests): Sumeragi main-loop harness now times out P2P startup and falls back to a closed handle to avoid hanging `cargo test -p iroha_core`.
Latest sync (tests): `cargo test -p iroha_core rbc_backlog_ -- --nocapture` and `cargo test -p iroha_core bypasses_background_queue -- --nocapture` (ok).
Latest sync (tests): `cargo test -p iroha_core commit_pipeline_defers_reschedule_until_availability_timeout -- --nocapture` and `cargo test -p iroha_core rbc_chunk_commit_pipeline_runs_on_completion -- --nocapture` (ok).
Latest sync (tests): `cargo test -p iroha_core commit_pipeline_ -- --nocapture` (ok).
Latest sync (telemetry): Sumeragi phase EMA gauges are seeded on startup and `collect_da` is recorded once `BlockCreated` payloads arrive, with unit coverage added.
Latest sync (state commit): Sumeragi state commit drops the tiered-backend lock before taking `view_lock` to avoid lock-order inversion with lane lifecycle updates; unit coverage added.
Latest sync (tests): `cargo test -p iroha_core force_view_change_if_idle_skips_when_consensus_queue_backpressure -- --nocapture` (ok).
Latest sync (consensus relay): RBC chunk traffic now uses a dedicated `ConsensusChunk` queue promoted to high priority to avoid low-priority stalls while keeping chunk flow isolated from other control-plane traffic.
Latest sync (consensus ingress/P2P): critical caps now apply to votes/VRF/proposals/fetch-pending + BlockCreated + RBC INIT/READY/DELIVER/CHUNK + control-flow evidence (penalty cooldowns skipped, RBC-session tracked), bulk payloads (BlockSyncUpdate/ExecWitness/BlockSync) use a separate bulk bucket seeded from the standard caps (penalty cooldowns skipped), critical caps fall back to standard caps when unset with penalty cooldowns enabled, and defaults are 300 msg/s.
Latest sync (consensus telemetry): consensus_message_handling now tracks proposals, votes, QCs, VRF, RBC init/ready/chunk, fetch-pending requests, and evidence drops/deferrals with unit coverage.
Latest sync (storage budgets): Kura now persists oversized blocks directly into `da_blocks/` (indexed as evicted) while budget accounting treats oversized pending blocks at the evicted footprint; unit tests and docs updated.
Latest sync (roster/PoP): PoP filtering now skips incomplete PoP maps and falls back to the BLS baseline roster to avoid divergent validator topologies.
Latest sync (NPoS roster): canonicalize active-validator roster ordering for NPoS to keep signer indices stable across peers even when commit topology order diverges; roster unit coverage updated.
NPoS block sync now accepts explicitly requested missing-block updates using uncertified rosters with stake snapshots; unit coverage added.
Latest sync (queue): queue telemetry now splits active/queued/inflight, queued length is derived from the hash queue minus stale markers, and Torii high-load gating aligns to active queue depth.
Latest sync (tests): checkpoint view regression tests passed in `iroha_data_model` and `iroha_core`; permissioned QC view mismatch regression passed in `iroha_core`; activation-height NPoS vote tests now pass (`handle_vote_uses_activation_height_mode_tag`, `emit_precommit_vote_uses_activation_height_mode_tag`); `cargo test --workspace` failed due to merge conflict markers in `crates/iroha_core/src/sumeragi/main_loop.rs`; `cargo check -p iroha_executor` (ok; invoice_id accessor avoids private-field access); `cargo test -p iroha_core sanitize_block_sync_qc_drops_view_mismatch -- --nocapture` timed out after 120s during compilation; `cargo test -p iroha_core --lib handle_qc_ -- --nocapture` (pass; network bind unavailable warnings); `cargo test -p iroha_core --lib block_created_updates_locked_status_when_lock_missing -- --nocapture` (pass); `cargo test -p iroha_core --lib pacemaker_updates_highest_qc_status_from_new_view -- --nocapture` (pass); `cargo test -p iroha_core --lib qc_extends_locked_if_present_allows_missing_lock_locally -- --nocapture` (pass); `cargo test -p iroha_core --lib locked_qc_subject_updates_for_same_height_view -- --nocapture` (pass); `cargo test -p iroha_core --lib pacemaker_bootstraps_with_commit_qc_when_active_roster_empty -- --nocapture` (pass); `cargo test -p iroha_core --lib prune_descendants_keeps_view_seen_after_divergent_proposal_drop -- --nocapture` (pass); `cargo test -p iroha_core --lib commit_inflight_timeout_triggers_view_change_and_retains_aborted_pending -- --nocapture` (pass); `cargo test --workspace` timed out after 1200s while running tests (warnings: unused `MissingQc` variant in `mochi/mochi-ui-egui/src/main.rs`, unused `base64::Engine` import in `crates/iroha_cli/src/contracts.rs`, unused assignment in `integration_tests/tests/asset.rs`); `cargo test -p iroha_core --lib idle_wait_duration_tracks_min_gap -- --nocapture` (pass); `cargo test -p iroha_core --lib try_incoming_block_message_wakes_worker_on_accept -- --nocapture` (pass); `cargo test -p iroha_core --lib run_worker_iteration_skips_tick_when_actor_disables_tick -- --nocapture` (pass); `cargo test -p iroha_core --lib actor_should_tick_tracks_missing_block_requests -- --nocapture` (pass); `cargo test -p iroha_core --lib push_wakes_sumeragi_when_configured -- --nocapture` (pass); `cargo test -p iroha_core --lib run_worker_iteration_respects_tick_deadline -- --nocapture` (pass); `cargo test -p iroha_core --lib actor_next_tick_deadline_tracks_missing_block_windows -- --nocapture` (pass); `cargo test --workspace` timed out after 1200s while running tests (warning: unused trigger metadata constants in `crates/iroha_core/src/smartcontracts/isi/triggers/mod.rs`); `cargo test -p iroha_core --lib commit_worker_wakes_on_result -- --nocapture` (pass); `cargo test -p iroha_core --lib rbc_persist_worker_wakes_on_result -- --nocapture` (pass); `cargo test --workspace` aborted by user after ~15m (no failures observed before abort); `IROHA_TEST_NETWORK_PARALLELISM=5 IROHA_SOAK_TARGET_BLOCKS=100 cargo test -p integration_tests --test sumeragi_localnet_smoke permissioned_localnet_soak_thousands -- --ignored --nocapture` skipped network startup due to loopback bind denial, so the soak did not run; `IROHA_TEST_NETWORK_PARALLELISM=5 IROHA_SOAK_TARGET_BLOCKS=100 cargo test -p integration_tests --test sumeragi_localnet_smoke permissioned_localnet_soak_thousands -- --ignored --nocapture` failed: localnet soak stalled for 40s at min_non_empty=59/target 101 with status timeout for `aspirant_perch`.

The repository now serves two release lines:
- **Iroha 2** — the self-hosted deployment track for organisations running
  independent permissioned or consortium networks.
- **Iroha 3 (SORA Nexus)** — the single global network backed by multi-lane
  Nexus infrastructure where operators join by registering data spaces.

Both release lines share the same Iroha Virtual Machine and Kotodama toolchain, so smart contracts and bytecode
artifacts are portable between self-hosted deployments and the SORA Nexus ledger.

Unless stated otherwise, roadmap items call out which release line they affect.

> **Status Legend:** 🈴 Completed · 🈺 In Progress · 🈯 Planned · 🈸 Drafting · 🈳 Not Started · 🈲 At Risk

## Current Open Work
- [ ] Verify the new PR CI `sumeragi_formal` job succeeds on a live GitHub runner and, if required by policy, pin `ghcr.io/apalache-mc/apalache` to an explicit version/digest after first green run.
- Norito streaming math upgrades: motion-compensated prediction, rANS rate-model RDO, adaptive quantization + deterministic deblock, deterministic audio LPC/MDCT path, and SIMD/GPU acceleration for fixed-point DCT.
- GPU zstd (Metal): full GPU entropy coding + decode (bit-for-bit parity with CPU zstd).
 - [x] Architecture: document the full GPU zstd pipeline (frame/block layout, buffer lifetimes, deterministic bitstream packing, and kernel responsibilities) with explicit parity constraints.
 - [x] Bitstream core: implement Metal bitstream writer/reader utilities (little-endian bit packing, byte-aligned flush, overflow checks) with CPU golden vectors for validation.
 - [x] Huffman literals: implement Metal Huffman encoder (code length generation + canonical table build + literal encoding) and decoder with parity tests.
 - [x] FSE sequences: implement Metal FSE encoder/decoder for LL/ML/OF (table build + state machine) with parity tests.
 - [x] Block/frame encoder: emit standard zstd frames (headers, raw/RLE/compressed blocks, optional checksum) using GPU entropy output.
- [x] Full helper-side decode: parse frames/blocks on the host and decode literals + sequences into output buffers (CPU zstd fallback for unsupported frames; GPU block decode still pending).
 - [x] Verification: add corpus parity + fuzz tests across CPU/GPU, including determinism checks and error handling edge cases.
 - [x] Performance + integration: benchmark vs CPU zstd, set thresholds for GPU offload, and update gpuzstd_metal runtime gating/docs.

- Localnet 1 Hz: re-run the NPoS 1 Hz soak with the fast-finality gas cap enabled (`sumeragi.block.fast_gas_limit_per_block`) and confirm block spacing falls under 1s on localhost. 2026-01-31 run (4 peers, NPoS, block/commit 1000ms, fast_gas_limit_per_block=1_000_000) used `/private/tmp/iroha-localnet-npos-1hz-fastgas-20260131b` with status sampling in `sumeragi_status_1hz_fastgas_20260131d.jsonl`: commit_qc height 183->250 (+67) over 181.8s (~0.368 blocks/s, ~2.7s/block), view_change_install_total +0, pending_rbc max 0 bytes, missing_block_fetch +1; effective block/commit time 2000/1500 with pacing_factor_bps=20000 (still above 1s target). 2026-01-31 rerun after localnet pacing clamp (block/commit 1000ms, fast_gas_limit_per_block=1_000_000) used `/private/tmp/iroha-localnet-npos-1hz-fastgas-clamped-20260131T185723Z` with status sampling in `sumeragi_status_1hz_fastgas_clamped_20260131T185723Z.jsonl`: commit_qc height 1->48 (+47) over 179s (~0.263 blocks/s, ~3.81s/block), view_change_install_total +2, pending_rbc max 5563 bytes, missing_block_fetch +17; effective block/commit time 1000/750 with pacing_factor_bps=10000 (still above 1s target).

1. **NEXUS-LANE-RELAY-RECOVERY — Emergency validator restore for dataspaces** (Consensus/Governance, Line: Iroha 3, Owner: Nexus Core WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Define the emergency admin multisig message to add validators when a dataspace pool falls below `3f+1`.
 - [x] Implement validation/admission plumbing, telemetry, and regression tests; update operator docs/runbooks.
 - [x] Enforce lane relay QC committee membership + aggregate signature validation and expose dataspace `fault_tolerance` in telemetry/status.

2. **OFFLINE-SECURITY-HARDENING — Close remaining offline receipt threat gaps** (Core/SDK, Line: Shared, Owner: Offline WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Bind receipt challenges to `chain_id` across core, SDKs, and FFI helpers.
 - [x] Enforce `policy.max_tx_value` during settlement and surface `max_tx_value_exceeded` rejections.
 - [x] Require allowance-scale consistency across receipts, policy limits, and balance proofs in core + SDK validators.
 - [x] Add range-proof verification for `resulting_commitment` (non-negative/bounded balance) and update docs/tests.
 - [x] Document operational guardrails for front-running/rollback risk and rooted-device privacy exposure.
 - [x] Add receipt timestamps + `max_receipt_age_ms` guardrails to reduce front-running and replay risk; update SDK UX and docs.
 - [x] Mitigate memory exposure for commitment witness material (zeroization in the Norito bridge).

3. **INTEGRATION-TEST-REVALIDATION — Re-run integration tests after Sumeragi drain-order fix** (QA/Consensus, Line: Shared, Owner: QA WG, Priority: High, Status: 🈺 In Progress, target TBD)
- [x] Re-run the previously failing suites (connected_peers, unstable_network, sumeragi_da, time_trigger, npos_pacemaker) with the now more conservative default test-network parallelism (peers-per-network divisor 64) to confirm the resource-contention fix holds (connected_peers ok; unstable_network 5/8/9-2/9-3 ok; time_trigger ok; sumeragi_da payload-loss/eviction/kura eviction + commit-certificate reruns ok; npos_pacemaker ok).
- [x] Re-run remaining `tmp_failures.txt` cases (proof events/queries, extra_functional suites, sumeragi_da) after seeding the genesis commit roster on commit to confirm consensus recovery. Re-ran `proofs` + `events::proof::proof_event_scenarios` + `queries::proof::proof_query_scenarios`, full `extra_functional::` suite with `--test-threads=1` (32/33 pass; restart_peer fixed and re-ran), and `sumeragi_da_*` with `--test-threads=1`.
- [x] Re-run the `sumeragi_da_*` failures after aligning proposal block signatures with the local validator index (RBC INIT leader signature should now match non-zero leaders).
- [x] Stabilize `sumeragi_commit_qc_with_tight_block_queue_four_peers` against RBC session snapshot timing by allowing `run_sumeragi_da_scenario_with` to disable strict session observation per-scenario and using commit-timing fallback only for this tight-queue case; `cargo test -p integration_tests --test sumeragi_da sumeragi_commit_qc_with_tight_block_queue_four_peers -- --nocapture` now passes.
- [x] Add 4-fault coverage to the unstable-network integration suite (raised 12-peer case to `n_rounds=3` to match the restored baselines).
 - [x] Re-run `cargo test -p integration_tests sumeragi_rbc_da_large_payload_four_peers -- --nocapture` to confirm DA large-payload RBC flow completes after READY/DELIVER queue routing.
 - [x] Re-run `cargo test -p integration_tests sumeragi_rbc_da_large_payload_six_peers -- --nocapture` after wiring `sumeragi.debug.rbc.force_deliver_quorum_one` to confirm the 6-peer large-payload scenario stays within delivery budgets.

4. **IZANAMI-NPOS-VALIDATION-LATENCY — Reduce pre-vote validation latency to meet 1 TPS targets** (Consensus/Perf, Line: Iroha 3, Owner: Core WG, Priority: Medium, Status: 🈺 In Progress, target TBD)
- [x] Instrument `validate_block_for_voting` with stage timings (stateless checks vs execution) and surface per-block validation latency in Izanami logs (debug log + testing guide note).
- [x] Optimize validation hot path (signature batching, trigger execution cost, or cached stateless results) so `pending_age_ms` stays below `fast_timeout_ms` at 1 TPS. (2026-02-16: switched pending fast-path timeout to quorum-derived runtime timing and trimmed trigger-heavy recipes from the stable Izanami profile.)
- [ ] Re-run Izanami 1 TPS (300s/200 blocks) and confirm `commit pipeline defers validation` no longer appears and block height reaches target (2026-01-31 run with 300s/120 blocks reached min height 121 in 261.7s; no defers-validation logs; 200-block target still pending). 2026-01-31 rerun (existing binaries: `target/debug/izanami` + `target/release/iroha3d`) stopped before target at committed height 137; network dir `/tmp/iroha-test-network-izanami-1tps-20260131-run1/irohad_test_network_al0y15`. 2026-01-31 rerun (`/tmp/iroha-codex-izanami-1tps-run2/release/izanami` + `target/release/iroha3d`) stopped before target at committed height 117 (nationwide_zorilla; others at 116); network dir `/tmp/iroha-test-network-izanami-1tps-20260131-run2/irohad_test_network_vQxKSK`. 2026-01-31 rerun (`CARGO_TARGET_DIR=/Users/mtakemiya/dev/iroha/target-izanami-1tps-200`) stopped before target with min_height 99 at 300s; no `commit pipeline defers validation` in the Izanami log; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_wTeoI6`, log `/tmp/izanami_1tps_200_20260131_run2.log`. 2026-02-01 rerun (`/tmp/iroha-codex-izanami-1tps-run2/release/izanami` + `target/release/iroha3d`) stopped before target at committed height 62 (calm_accentor/diplomatic_smew/advisable_planarian; affluent_reindeer at 61); no `commit pipeline defers validation` in the Izanami log; network dir `/tmp/iroha-test-network-izanami-1tps-20260201-run1/irohad_test_network_X9ruRf`, log `/tmp/izanami_1tps_20260201_run1.log`. 2026-02-01: enabled `sumeragi.advanced.pacemaker.da_fast_reschedule` in Izanami to shorten DA reschedule stalls; 2026-02-01 rerun (release build, 4 peers, 300s, target 200) stopped before target at min_height 136; no `RegisterBox::Trigger invoke` errors or plan submission failures observed; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_O6hWla`, log `/tmp/izanami_1tps_200_20260201T092010Z.log`. 2026-02-01 rerun with `kura.fsync_mode=off` in Izanami config layers (release build, 4 peers, 300s, target 200) stopped before target at committed height 164-165 (min 164); network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_Pa4Vny` (peer logs `run-1-stdout.log`), no mint-trigger missing errors observed in peer logs.
- [ ] 2026-02-16 isolated-binary reruns (`/tmp/iroha-izanami-finish/release/{izanami,irohad,iroha,kagami}`) still miss the 300s/200 gate: run6 min_height 168 (`/tmp/izanami_1tps_200_20260216_run6.log`, net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_SsUoCM`), run9 min_height 165 after stable-profile trigger trimming (`/tmp/izanami_1tps_200_20260216_run9.log`, net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_JJdm0b`). Canary runs (`run7`/`run8`/`run10`/`run11`) ranged 51-76 blocks over 120s; no `commit pipeline defers validation` or `validation inflight exceeded fast timeout` matches were observed.
 - [x] Re-run `cargo test -p integration_tests --test address_canonicalisation -- --nocapture` to confirm the suite completes without timeouts (latest attempt with `IROHA_TEST_NETWORK_PARALLELISM=5` timed out after ~12m with tests still running and permit waits; sandbox run with `IROHA_TEST_NETWORK_PARALLELISM=4` skipped network startup due to loopback bind denial; rerun timed out after ~16m with network guard stuck at 4/4 permits; serialized rerun failed to compile `iroha_genesis` due to missing Norito JSON traits for `RegisterPublicLaneValidator`/`ActivatePublicLaneValidator`; fixes landed, but the subsequent rerun terminated by SIGKILL).
 - [x] Re-run `cargo test -p integration_tests --test asset client_add_asset_quantities_should_increase_asset_amounts -- --nocapture` to confirm DA delivery gating no longer stalls decimal mint flows.
 - [x] Re-run `cargo test -p integration_tests --test asset client_add_asset_quantities_should_increase_asset_amounts -- --nocapture` and `cargo test -p integration_tests --test asset find_rate_and_make_exchange_isi_should_succeed -- --nocapture` after the sync retry budget + asset test sync updates to confirm asset flows no longer time out on tx confirmation.
 - [x] Re-run asset integration tests after the exec-witness fallback for missing pre-vote roots to confirm commit votes and confirmations no longer stall.
 - [x] Re-run `cargo test -p integration_tests --test asset -- --nocapture` after the asset client-rotation fallback to confirm Torii timeouts no longer stall exchange/spec flows (sandbox run with `IROHA_TEST_NETWORK_PARALLELISM=4` skipped network startup due to loopback bind denial).
 - [x] Re-run `cargo test -p integration_tests --test asset -- --nocapture` after broadcasting asset submissions to all peers and widening asset-test consensus timing (6s pipeline, DA timeout multipliers) to confirm integer transfer no longer stalls (pass: `CARGO_TARGET_DIR=/tmp/iroha-codex-roadmap-target IROHA_TEST_NETWORK_PARALLELISM=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo test -p integration_tests --test asset -- --nocapture --test-threads=1`).
 - [x] Re-run `cargo test -p integration_tests --test sumeragi_da sumeragi_rbc_recovers_after_peer_restart -- --nocapture --test-threads=1` after replay-roster fixes to confirm restart recovery completes.
 - [x] Re-run `cargo test -p integration_tests --test sumeragi_da sumeragi_idle_view_change_recovers_after_leader_shutdown -- --nocapture` to validate idle view-change recovery after leader shutdown (sandbox run with `IROHA_TEST_NETWORK_PARALLELISM=4` skipped network startup due to loopback bind denial; rerun with `CARGO_TARGET_DIR=/tmp/iroha-codex-target-tests` + `IROHA_TEST_NETWORK_PARALLELISM=1` + `IROHA_TEST_SKIP_BUILD=1` + `TEST_NETWORK_BIN_IROHAD=target/debug/iroha3d` passed with `trusted_peers_pop` warnings and signal 6 on shutdown; updated the test to submit to all running peers and extend the recovery window to 90s, then reran with `CARGO_TARGET_DIR=/tmp/iroha-codex-roadmap-target` + `IROHA_TEST_NETWORK_PARALLELISM=1` + `IROHA_TEST_NETWORK_KEEP_DIRS=1` + `IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d)` (pass; `trusted_peers_pop` warnings during profile scan)).
 - [x] Re-run `IROHA_TEST_NETWORK_KEEP_DIRS=1 cargo test -p integration_tests --test sumeragi_da sumeragi_da_eviction_rehydrates_block_bodies -- --nocapture` after NPoS roster canonicalization to confirm DA eviction rehydration (pass).
 - [x] Re-run `cargo test -p integration_tests --test sumeragi_localnet_smoke permissioned_localnet_soak_thousands -- --ignored --nocapture` to confirm the thousands-block soak stays stable (2026-01-31 run with `IROHA_SOAK_TARGET_BLOCKS=200` + serialized network finished with min_non_empty 201; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_3cjuPe`).
 - [x] Logger: avoid `TestWriter` panics on broken pipes by using a lossy stdout writer in `iroha_logger` (unit coverage added) so spawned peers don't abort during shutdown.
 - [x] Limit integration-test network concurrency in the sandbox harness (CPU-scaled default + `IROHA_TEST_NETWORK_PARALLELISM`) so `--test-threads=1` is no longer required.
 - [x] Add a restart regression that simulates a crash after persisting a finalized VRF epoch record but before writing the seed-only next-epoch snapshot.

4. **NPOS-LOCALNET-1HZ — Restore 1 Hz localnet block cadence** (Consensus/Localnet, Line: Iroha 3, Owner: Consensus WG, Priority: Medium, Status: 🈺 In Progress, target TBD)
 - [x] Identify pacemaker backpressure sources holding proposal cadence (~7.5s/block) under 1 Hz ping load; capture per-stage timings (added per-reason deferral counters/ages/durations plus pacemaker eval/propose timing histograms; docs updated).
 - [x] Tune NPoS timeouts and commit pipeline thresholds for localnet to reach 1 block/sec without triggering view changes (propose/prevote/precommit/commit/DA/agg = 300/400/500/650/600/100 ms; DA timeout multipliers 1/1; commit inflight 4–10s with 6x multiplier).
 - [x] Re-run the 1 Hz / 100-block soak and confirm view changes remain zero and RBC queues stay under cap (rerun 2026-01-21 on `/tmp/iroha-localnet-npos-1hz-run2`; 100x1 Hz `transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `status-1hz-20260121T192416Z.jsonl`: `commit_qc.height` +12 over 118s (~0.10 blocks/s), `view_change_install_total` +3 (`stake_quorum_timeout_total` +2), pending RBC max 1040 bytes / 1 session (cap 8 MiB / 256); 2026-01-23 debug rerun on `/tmp/iroha-localnet-npos-1hz-20260123T132039Z`: `commit_qc.height` 1->14 over 363 samples, `view_change_install_total` +3 (stake_quorum_timeout/missing_qc), `pending_rbc.bytes` max 1260 / 2 sessions; 2026-01-23 release rerun on `/tmp/iroha-localnet-npos-1hz-20260123T133537Z`: `commit_qc.height` 1->17 over 136 samples, `view_change_install_total` +71 (missing_qc), `pending_rbc.bytes` max 2220 / 1 session; 2026-01-23 tuned rerun on `/tmp/iroha-localnet-npos-1hz-20260123T140944Z` with `redundant_send_r=3` + `availability_timeout_multiplier=2`: `commit_qc.height` 0->7 over 146 samples, `view_change_install_total` +3 (missing_qc), `pending_rbc.bytes` max 0; 2026-01-23 rerun with RBC INIT routed via block-payload queue on `/tmp/iroha-localnet-npos-1hz-20260123T181930Z`: 120x1 Hz `transaction ping --no-wait` with `ops sumeragi status` sampling in `sumeragi_status_1hz_fix_status_20260123T184602Z.jsonl` (ping log `ping_1hz_fix_nowait_20260123T184602Z.log`), `commit_qc.height` 83->95 (+12 over 120 samples), `view_change_install_total` +4 (`missing_qc_total` +1, `stake_quorum_timeout_total` +3), `missing_payload_total` +0, `pending_rbc.bytes` 0, `stash_ready_init_missing_total` +8; 2026-01-23 tuned rerun on `/private/tmp/iroha-localnet-npos-1hz-20260123T190128Z` with `redundant_send_r=3` + `availability_timeout_multiplier=2` + `quorum_timeout_multiplier=2`: 120x1 Hz `transaction ping --no-wait` with `ops sumeragi status` sampling in `sumeragi_status_1hz_tuned_20260123T190128Z.jsonl` (ping log `ping_1hz_tuned_20260123T190128Z.log`), `commit_qc.height` 1->14 (+13 over 120 samples), `view_change_install_total` +1 (`stake_quorum_timeout_total` +1), `missing_qc_total` +0, `missing_payload_total` +0, `missing_block_fetch.total` +6, `pending_rbc.bytes` max 220 (sessions max 1); 2026-01-23 tuned rerun on `/private/tmp/iroha-localnet-npos-1hz-20260123T195213Z` with `redundant_send_r=4`, `availability_timeout_multiplier=3`, `quorum_timeout_multiplier=3`, `rbc.payload_chunks_per_tick=256`, and `worker.tick_work_budget_cap_ms=1000`: 120x1 Hz `transaction ping --no-wait` with `ops sumeragi status` sampling in `sumeragi_status_1hz_tuned2_20260123T195213Z.jsonl` (ping log `ping_1hz_tuned2_20260123T195213Z.log`), `commit_qc.height` 1->28 (+27 over 120 samples), `view_change_install_total` +30 (`missing_qc_total` +22), `stake_quorum_timeout_total` +0, `missing_payload_total` +0, `missing_block_fetch.total` +27, `pending_rbc.bytes` max 4189 (sessions max 1); cadence improved but still below 1 Hz; 2026-01-24 rerun on `/private/tmp/iroha-localnet-npos-1hz-20260124T065327Z` with NPoS timeouts propose/prevote/precommit/commit/da/agg=300/500/700/900/800/100, collectors_k=4, redundant_send_r=4, and `sumeragi.advanced.da.availability_timeout_multiplier=2`/`quorum_timeout_multiplier=2`: 100x1 Hz `transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_timeouts_collectors_20260124T065327Z.jsonl` (ping log `ping_1hz_timeouts_collectors_20260124T065327Z.log`), `commit_qc.height` 1->42 (+41 over 120 samples), `view_change_install_total` +7 (`missing_qc_total` +6), `stake_quorum_timeout_total` +0, `missing_payload_total` +0, `missing_block_fetch.total` +14, `pending_rbc.bytes` max 220 (sessions max 1); cadence still below 1 Hz; 2026-01-24 rerun on `/private/tmp/iroha-localnet-npos-1hz-20260124T113118Z` with `block_time_ms=1000`, `commit_time_ms=1500`, NPoS timeouts propose/prevote/precommit/commit/da/agg=500/800/1100/1500/1400/200, `sumeragi.advanced.da.availability_timeout_multiplier=3`, `sumeragi.advanced.da.quorum_timeout_multiplier=3`: 100x1 Hz `transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_20260124T113118Z_retry2.jsonl` (ping log `ping_1hz_20260124T113118Z_retry2.log`), `commit_qc.height` 53->53 (+0 over 120 samples), `view_change_install_total` +0 (start had `missing_payload_total=13`, `missing_qc_total=3`, `stake_quorum_timeout_total=1`), `missing_block_fetch.total` +1433, `pending_rbc.bytes` max 0 (sessions max 0); stalled on missing payload at height 54; 2026-01-24 rerun on `/private/tmp/iroha-localnet-npos-1hz-20260124T121035Z` with `block_time_ms=1000`, `commit_time_ms=1500`, NPoS timeouts propose/prevote/precommit/commit/da/agg=500/800/1100/1500/1400/200, `sumeragi.advanced.da.availability_timeout_multiplier=3`, `sumeragi.advanced.da.quorum_timeout_multiplier=3`, `sumeragi.advanced.rbc.pending_ttl_ms=120000`, `sumeragi.advanced.rbc.session_ttl_ms=240000`, `sumeragi.recovery.missing_block_signer_fallback_attempts=0`: 100x1 Hz `transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_20260124T121035Z.jsonl` (ping log `ping_1hz_20260124T121035Z.log`), `commit_qc.height` 1->64 (+63 over 120 samples), `view_change_install_total` +0, `missing_qc_total` +0, `missing_payload_total` +0, `stake_quorum_timeout_total` +0, `missing_block_fetch.total` +15, `pending_rbc.bytes` max 2708 (sessions max 1); cadence improved but still below 1 Hz; 2026-01-24 rerun on `/private/tmp/iroha-localnet-npos-1hz-20260124T130140Z` with `block_time_ms=1000`, `commit_time_ms=1500`, NPoS timeouts propose/prevote/precommit/commit/da/agg=500/800/1100/1500/1400/200, `sumeragi.advanced.da.availability_timeout_multiplier=3`, `sumeragi.advanced.da.quorum_timeout_multiplier=3`, `sumeragi.advanced.rbc.pending_ttl_ms=120000`, `sumeragi.advanced.rbc.session_ttl_ms=240000`, `sumeragi.recovery.missing_block_signer_fallback_attempts=0`: 100x1 Hz `transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_20260124T130140Z.jsonl` (ping log `ping_1hz_20260124T130140Z.log`), `commit_qc.height` 1->60 (+59 over 120 samples), `view_change_install_total` +3 (`missing_qc_total` +1), `missing_payload_total` +0, `stake_quorum_timeout_total` +0, `missing_block_fetch.total` +7, `pending_rbc.bytes` max 0 (sessions max 0); 2026-01-24 rerun after validation inflight fast-timeout fix on `/private/tmp/iroha-localnet-npos-1hz-20260124T130140Z`: 100x1 Hz `transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_fix_20260124T200000Z_20260124T194233Z.jsonl` (ping log `ping_1hz_fix_20260124T200000Z_20260124T194233Z.log`), `commit_qc.height` 1->58 (+57 over 120 samples), `view_change_install_total` +1 (no view_change_causes deltas), `missing_qc_total` +0, `missing_payload_total` +0, `stake_quorum_timeout_total` +0, `missing_block_fetch.total` +11, `pending_rbc.bytes` max 0 (sessions max 0); 2026-01-24 rerun after DA fast-path disable on `/tmp/iroha-localnet-npos-1hz-20260124T172906Z`: 100x1 Hz `ledger transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_fastpath_fix_20260124T173233Z.jsonl` (ping log `ping_1hz_fastpath_fix_20260124T173233Z.log`), `commit_qc.height` 50->104 (+54 over 120 samples), `view_change_install_total` +0, `missing_qc_total` +0, `missing_payload_total` +0, `stake_quorum_timeout_total` +0, `missing_block_fetch.total` +15, `pending_rbc.bytes` max 440 (sessions max 1); cadence still below 1 Hz; 2026-01-24 rerun on `/private/tmp/iroha-localnet-npos-1hz-20260124T190127Z`: 100x1 Hz `ledger transaction ping --no-wait` with `/v1/sumeragi/status` sampling in `sumeragi_status_1hz_missing_init_debug_20260124T190127Z.jsonl` (ping log `ping_1hz_missing_init_debug_20260124T190127Z.log`), `commit_qc.height` 2->58 (+56 over 100 samples), `view_change_install_total` +0, `missing_qc_total` +0, `missing_payload_total` +0, `stake_quorum_timeout_total` +0, `missing_block_fetch.total` +24, `pending_rbc.bytes` max 220 (sessions max 1), `stash_ready_init_missing_total` 18.
 - [ ] Re-run `cargo test -p integration_tests -- --nocapture` after the targeted suite completes cleanly (ran 60m; timed out while running `integration_tests/tests/mod.rs` with network guard stuck at 4/4 permits and unstable-network relay connection-refused warnings).
 - [x] Re-run `cargo test -p integration_tests --test sumeragi_localnet_smoke -- --nocapture` to confirm localnet tx-status fallbacks no longer emit WARN noise.
- [x] Capture a localnet stall run with the new `sumeragi_localnet_smoke` status snapshots to pinpoint any remaining consensus races (saw repeated `dropping vote: empty commit topology` and RBC READY quorum stalls).
- [x] Add P2P queue depth telemetry and RBC store pressure logging to diagnose localnet soak stalls.
- [x] Reproduce 4-peer DEBUG 1s localnet subscriber-queue drops with bulk ping load after relay worker-queue/backpressure gating, expanded relay backpressure drop counters (post/broadcast drops, per-peer post overflows, topic-cap violations), unresolved-RBC backlog deferrals plus relay backpressure/pending-block proposal gating, stale-view RBC acceptance limited to active pending/inflight blocks and inactive-session rebroadcast/backlog gating suppression, queue hash compaction + queued-len gating, localnet NPoS bootstrap stake/gas defaults, NPoS roster bootstrap from active validators, Kagami genesis sign auto-bootstrap of NPoS stake/topology, consensus-ingress RBC session limit scaling, raised consensus-ingress rate/burst defaults + `QcVote` exemption, NEW_VIEW votes fanning out to collectors/topology fallback (leader included), transaction gossip resend deferral (`network.transaction_gossip_resend_ticks`), per-tick RBC chunk broadcast pacing (`sumeragi.advanced.rbc_payload_chunks_per_tick`), ConsensusChunk topic + low-priority RBC chunk routing, MissingLocalData quorum-reschedule deferral, and NPoS aggregator timeout aligned to base; capture per-topic drop counters and Torii tx-rate-limit behavior to isolate any remaining flood sources (run2 with `--count 3000 --parallel 16` showed no queue-full logs; run20 2h soak with 2 lanes/peer `--count 15000 --parallel 48` saw 0 queue drops/view-changes/429s; rerun after preauth cap clamp + localnet RLIMIT uplift: RLIMIT 16384, `--count 50000 --parallel 128`, Torii accepted ~11.6k tx on peer0, no subscriber drops, blocks stuck at height 1 due to `not enough stake collected for QC`; localnet now mints stake in a prior genesis tx, registers/activates NPoS validators in a follow-up tx, enables Nexus for staged NPoS cutovers, and start scripts pass `--sora` when Nexus is enabled; 4-peer DEBUG load rerun (ports 46180/50337, `--count 20000 --parallel 128`) shows `commit_qc.validator_set_len=4` but still no tx admission/commit, with stake quorum timeouts and missing payload/QC view changes; rerun with peer1 submission target (ports 46380/50537, `--count 20000 --parallel 128`, backgrounded) shows commit heights 3-5 and `commit_qc.validator_set_len=4` on all peers, no subscriber-queue drops, and `vote_validation_drops` limited to `duplicate`/`stale_height` while peer1 still leads in stake-quorum/missing-QC counts; latest debug run (ports 18180/13400, peer1 `--count 5000 --parallel 32 --no-wait`) reached `commit_qc.height=2` with `validator_set_len=4`, `stake_quorum_timeout_total=5`, `missing_payload_total=2`, `missing_qc_total=1`, and `vote_validation_drops` still `stale_height` only, while peer0 logged early `not enough stake collected for QC` with `voting_signers=1`; 2026-01-18 rerun with `kagami localnet --consensus-mode npos --peers 4 --out-dir /private/tmp/iroha-localnet-npos-debug-1s-run25 --base-api-port 48180 --base-p2p-port 53337`, `iroha --config /private/tmp/iroha-localnet-npos-debug-1s-run25/client-peer1.toml transaction ping --count 20000 --parallel 128 --no-wait`: `/v1/sumeragi/status` shows `commit_qc.height=0` on all peers with peer1 `view_change_install_total=72`/`missing_qc_total=59`, worker-loop drops 0, and peer1 log still reports `not enough stake collected for QC`).
- [ ] Re-run a 7-peer localnet load after the BlockSyncUpdate + block-payload blocking enqueue change, background post fallback, queue-pop lock reduction, bounded proposal queue scan (`sumeragi.block.proposal_queue_scan_multiplier`), higher localnet TEU caps (`nexus.fusion.exit_teu`), and epoch-roster widening when commit topology omits active validators to confirm block height advances under block-queue pressure and updates are no longer dropped. Latest run (release, 7 peers, 100k ping txs, `--parallel 128 --with-index`) reached height 32 (blocks_non_empty +22) with ~20 tps admitted and ~31 tps committed; queue depth climbed and missing-payload view-change warnings persisted. 2026-01-18: `scripts/run_localnet_throughput.sh --release --artifact-dir ./artifacts/localnet-throughput --keep-dirs --env IROHA_TEST_NETWORK_PARALLELISM=1 --env IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d)` timed out after 20m during `permissioned_localnet_throughput_10k_tps` (release build completed; run started with 7 peers, block/commit 1s, total_blocks=40); logs in `/var/folders/7l/w31n0ppj4zg874c4szhllss00000gn/T/irohad_test_network_hQttXn`, artifacts dir empty.
 - [x] Re-run integration tests that previously stalled on tx confirmation streams to validate the poll-priority + queued/approved tracking, base64 rejection decoding, and listener-connect gating for early events (pipeline/notification/sse_smoke cases passed).
- [x] Re-run `cargo test -p integration_tests --test mod multiple_blocks_created -- --nocapture` (and unregister/soft-fork cases) after the merge QC view wiring (lane-tip view) and commit-certificate/roster sidecar persistence fix to confirm block-sync roster validation no longer drops updates (multiple_blocks_created, network_stable_after_add_and_after_remove_peer, soft_fork passed).
 - [x] Re-run `cargo test -p integration_tests --test sumeragi_kagami_localnet -- --nocapture` after the BlockSyncUpdate backpressure, missing-parent fetch, RBC chunk background drop, periodic RBC payload rebroadcast, and proposal/RBC broadcast-order fixes; ran outside the sandbox with `IROHA_KAGAMI_LOCALNET_KEEP=1` (passed).
 - [x] Re-run `cargo test -p integration_tests --test mod network_stable_after_add_and_after_remove_peer -- --nocapture` after the RBC roster fallback (pass).
- [x] Re-run `cargo test -p integration_tests --test mod -- --nocapture` with `API_ADDRESS`/`PUBLIC_KEY` env overrides set to confirm test-network peers ignore host env config overrides (2026-02-01: `API_ADDRESS=not_a_socket PUBLIC_KEY=deadbeef IROHA_TEST_BUILD_PROFILE=release IROHA_TEST_SKIP_BUILD=1 IROHA_TEST_NETWORK_PARALLELISM=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) TEST_NETWORK_TMP_DIR=/Users/mtakemiya/dev/iroha/tmp/test_network TEST_NETWORK_BIN_IROHAD=/Users/mtakemiya/dev/iroha/target/release/iroha3d TEST_NETWORK_BIN_IROHA=/Users/mtakemiya/dev/iroha/target/release/iroha cargo test -p integration_tests --test mod -- --nocapture --test-threads=1` passed).
- [x] Re-run `cargo test -p integration_tests --test mod` after the payload-hash stabilization fix (strip results/extra signatures from DA/RBC payload bytes) and the block-sync seen-block filtering; attempt 2025-12-31 timed out after 5m with pipeline event failures, peers waiting for block 1, and status endpoint connection refused—confirm the gating condition is resolved (2026-02-01 run above passed).
- [x] Re-run `cargo test -p integration_tests --test mod` after stabilizing NPoS PRF seed handling (seed fixed within epoch + next-epoch record persisted at rollover + replay PRF rotation) to confirm event/connected-peers suites no longer hang (2026-02-01 run above passed).
- [x] Re-run `cargo test -p integration_tests --test mod` after preserving `proposals_seen` across membership changes (prevents re-proposing the same view during roster updates) to confirm peer membership tests no longer stall consensus (2026-02-01 run above passed).
- [x] Run the Sumeragi targeted matrix (no full-workspace run): `cargo test -p iroha_core --lib sumeragi::main_loop::tests -- --nocapture`, `cargo test -p integration_tests --test sumeragi_da -- --nocapture`, and `cargo test -p integration_tests --test sumeragi_localnet_smoke -- --ignored --nocapture`; update `status.md` with command results and artifact/log paths. 2026-02-15 evidence: `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p iroha_core --lib sumeragi::main_loop::tests -- --nocapture` (1048 passed / 1 failed: `handle_qc_records_commit_qc_history`), isolated rerun `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p iroha_core --lib sumeragi::main_loop::tests::handle_qc_records_commit_qc_history -- --nocapture` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p integration_tests --test sumeragi_da -- --nocapture` (ok; sandbox loopback guard skips network startup), `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p integration_tests --test sumeragi_localnet_smoke -- --ignored --nocapture` (ok; sandbox loopback guard skips network startup).
- [x] Fix block-sync seen-block tracking to avoid marking uncommitted payloads (prevents catch-up stalls), add unit coverage, and re-run `extra_functional::connected_peers::register_new_peer` (pass; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_y8a4wO`).

4. **ADAPTIVE-PACING-FLOOR — Genesis minimum finality + adaptive timing governor** (Consensus, Line: Shared, Owner: Core WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Data model + genesis: add `min_finality_ms` to `SumeragiParameters` (Norito + JSON) and consensus genesis fingerprint; update on-chain defaults to start at the floor (100ms) and refresh config templates/docs.
- [x] Validation + clamping: enforce `commit_time_ms >= block_time_ms >= min_finality_ms` in parameter parsing and clamp runtime timing resolution (permissioned + NPoS) so commit/quorum/availability/pacemaker derive from the floored values.
- [x] Deterministic pacing governor: define window size + thresholds (view-change pressure + commit spacing), implement hysteresis (fast up/slow down) with a bounded factor, and emit `SetParameter` updates only at height boundaries.
- [x] Telemetry + tests: add pacing-factor fields to `/v1/sumeragi/status` and cover governor scale up/down decisions (effective timing fields + Norito/JSON round-trip coverage landed).

5. **SUMERAGI-PARAM-STREAMLINE — Reduce operator-facing consensus knobs** (Consensus, Line: Shared, Owner: Core WG, Priority: Medium, Status: 🈴 Completed, target TBD)
- [x] Spec: `docs/source/sumeragi_parameter_streamlining.md`.
- [x] Parameter surface: keep only the minimal on-chain/operator knobs (block/commit/min_finality/clock_drift, collectors, DA enable) and treat all other timing knobs as derived-by-default.
- [x] Config shape: introduce `sumeragi.advanced` (or equivalent) for override-only knobs (pacemaker backoff/jitter caps, DA multipliers/floors, RBC TTL/storage, worker budgets); ensure deterministic fallbacks.
- [x] NPoS overrides: move timeout overrides to the advanced section (or remove them) and derive from `block_time_ms` when unset; update validation to allow 0/None as "derive".
- [x] Observability: extend `/v1/sumeragi/status` with the remaining effective derived fields not covered by the pacing task and document the new surface.
- [x] Docs/templates: refresh config templates and docs to match the streamlined surface (no backward-compat required for first release).
- [x] Fix snapshot deserialization on restart so `extra_functional::restart_peer::restarted_peer_should_restore_its_state` can rely on persisted snapshots (2026-01-24: install domain-selector resolver from snapshot domains before parsing, remove cleanup; `cargo test -p iroha_core snapshot_read_installs_domain_selector_resolver -- --nocapture` and `IROHA_TEST_NETWORK_PARALLELISM=1 cargo test -p integration_tests --test mod restarted_peer_should_restore_its_state -- --nocapture` passed).
- [x] Resolve build-directory lock timeouts so full-workspace test runs complete reliably (standardize `CARGO_TARGET_DIR` or serialize builds).

6. **SUMERAGI-LIVENESS-GAPS — Close remaining idle view-change + pacing-governor gaps** (Consensus/Perf, Line: Shared, Owner: Core WG, Priority: High, Status: 🈺 In Progress, target TBD)
- [x] Idle view-change: allow leader rotation after the idle timeout even when consensus queues stay saturated; add logging/telemetry for the override path. (touch `crates/iroha_core/src/sumeragi/main_loop.rs`)
- [x] Idle view-change tests: add relay-backpressure branch coverage (pre-timeout blocked, post-timeout allowed) and update the consensus-queue backpressure test to assert the post-timeout override. (touch `crates/iroha_core/src/sumeragi/main_loop/tests.rs`)
- [x] Pacing governor always-on: remove/ignore the `sumeragi.advanced.pacing_governor.enabled` toggle across config defaults/user/actual parsing and runtime application; update fixtures/tests and docs/config templates to match. (touch `crates/iroha_config/src/parameters/{defaults,actual,user}.rs`, `crates/iroha_core/src/state.rs`, config templates)
- [x] Liveness bottlenecks: cap block-queue drain per iteration under backlog (adaptive drain cap) and add coverage to keep worker-loop drain latency bounded. (touch `crates/iroha_core/src/sumeragi/mod.rs`)
- [x] Re-run Izanami 1 TPS + NPoS localnet 1 Hz after the drain-cap change and capture results in `status.md`. (2026-02-01: Izanami 1 TPS run `/tmp/iroha-test-network-izanami-1tps-20260201T054440/irohad_test_network_56VhGX` + log `/tmp/izanami_1tps_20260201T054440.log`; NPoS localnet 1 Hz run `/private/tmp/iroha-localnet-npos-1hz-20260201T101516` with `sumeragi_status_1hz_20260201T130626.jsonl` + `ping_1hz_20260201T130626.log`.) (touch `status.md`)

7. **PERF-100TPS-PIPELINE — Tx pipeline & consensus hot-spot cleanup** (Consensus/Perf, Line: Shared, Owner: Core WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Norito encode: direct encode-into-writer for owned payloads when exact length is known; use seq size hints to reserve packed payload buffers.
- [x] P2P/gossip: reuse outbound frames, avoid cloning large tx payloads by arcing gossip transactions, and keep cached encoded bytes for retransmit.
- [x] BLS verification: reuse verified vote/QC caches and prepared-key cache; skip per-vote checks on aggregate-pass for NPoS.
- [x] Consensus payload churn: cache pre-encoded BlockMessage/RBC chunk bytes and compact RBC chunk headers for repeated fields.
- [x] Backpressure tuning: raise RBC store caps/backlog soft limits and NPoS timeout defaults; update docs/templates.

4. **LOCALNET-10K-TPS — 7-peer localnet throughput + stall resilience** (Consensus/Performance/Tooling, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈺 In Progress, target TBD)
- [ ] Use the new pending-block/commit-inflight metrics to isolate pacemaker backpressure during 100 TPS localnet runs (run151 NPoS: avg slot 5.68s, view changes 11, proposal_gap 1, pending/inflight <=4/1; run152 permissioned + commit_time_ms=300: avg slot 10.69s, view changes 18, pending/inflight <=4/1; run153 NPoS 7-peer 753ms soft limits: height 5, view changes 42, tx_queue depth 4796, pending max 5/inflight 0, missing_qc/stake_quorum timeouts; run154 permissioned 7-peer 753ms: height 5, view changes 9, tx_queue depth 11522, pending/inflight 0, missing_qc/quorum timeouts). Identify the proposal-gap root cause and re-test.
- [ ] Validate worker-iteration drain-cap impact with a load generator that sustains 50 TPS (current CLI batch loop undershoots per-second pacing) and correlate view-change spikes with proposal gaps vs queue backpressure.
- [x] Re-run the Izanami 1 TPS (300s) profile after RBC seed-worker offload to confirm BlockCreated handling latency drops and capture the updated metrics in `status.md` (2026-01-25 run recorded).
- [x] Re-run the Izanami 1 TPS (300s) profile after increasing RBC seed worker concurrency and queue caps to confirm additional BlockCreated seeding latency reduction; update `status.md` (2026-01-29 attempt: commit height 53 in ~308s, avg interval 6.05s; missed target 200; logs `/private/tmp/iroha-izanami-logs/irohad_test_network_W2m842`).
- [x] Re-run the Izanami 1 TPS profile after the pipeline shared-pool reuse + stateless cache + stateless signature batching to confirm vote/block drain improvements and update `status.md`.
- [x] Re-run the Izanami 1 TPS profile with auto-scaled `pipeline.workers` and `sumeragi.advanced.worker.validation_*` (2026-01-28 run stalled with committed height 26; vote_drain_ms mean 266ms p95 1156 max 3378; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_Wk33iK`).
- [x] Investigate per-peer commit persistence stalls (Izanami `irohad_test_network_o6jN6V`: swaying_deer never logged `stored committed block to kura` for height 40 while other peers did); added commit->Kura stage breakdown metrics (kura_store/state_apply/state_commit) plus state commit view_lock wait/hold histograms to correlate persistence stalls with lock contention.
- [x] Re-run Izanami 1 TPS with expanded block-validation sub-stage timings (stateless vs execution breakdown); execution is dominated by `execution_tx_ms` (mean ~740ms, p95 ~4011ms, max ~5313ms) while other sub-stages are ~0ms (run 2026-01-28, logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_mwreSi`).
- [x] Re-run Izanami 1 TPS with tx sub-stage timings; `execution_tx_apply_ms` dominates (mean ~508ms, p95 ~3875ms, max ~5862ms) with occasional DAG spikes (max 1803ms) in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_FE6T7a`.
- [x] Split `execution_tx_apply_ms` into apply sub-stages (detached exec vs merge vs sequential fallback vs trigger execution vs finalization) to pinpoint the dominant apply hot spot before optimizing.
- [x] Re-run Izanami 1 TPS after forcing missing highest-QC fetch when the only local payload is an aborted pending block (proposal/proposal-hint paths) to confirm height progress and clear missing-highest stalls. 2026-01-30 run (4 peers, 300s, target 200, tps 1, `--max-inflight 8`) reached committed height 104–105 and stopped before the target; no missing-highest logs observed; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_LaHP2G`. 2026-01-31 debug run (4 peers, 120s, target 80, tps 1, vote/qc debug) reached committed height 55–56; votes were recorded for all signers and QCs aggregated, but tick-loop lagging showed commit_pipeline_ms spikes (~3.9s); no missing-highest logs observed; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_OnNAOB`. 2026-01-31 main_loop debug run (4 peers, 120s, target 80, tps 1, validation timing debug) reached committed height 23; `execution_tx_dag_ms` spiked to 2052ms at height 10 with tx_count=4 and DAG key_count=10 (decoded sidecar), suggesting pipeline sidecar fsync I/O in the DAG stage; `execution_tx_time_triggers_ms` spiked to 817ms at height 23; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_Fv0sDg`. 2026-01-31 main_loop debug run (4 peers, 300s, target 200, tps 1) reached committed height 69; validation timings mean total_ms 8.6 p95 20; tick-loop lagging p95 tick_cost_ms ~2476 and commit_pipeline_ms max 3965; worker-iteration slow lines dominated by block drains (post_tick_blocks_drain_ms p95 ~4071); network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_xL0j16`. 2026-01-31 main_loop debug run with commit-pipeline substage timings (4 peers, 300s, target 200, tps 1) reached committed height 56; commit interval avg 5.52s p95 11.82s; tick-loop lagging p95 tick_cost_ms ~1488, commit_pipeline_ms max 4488 with drain dominating (commit_pipeline_drain_ms mean 163 p95 904) and validation mean 27 p95 110; worker iteration slow p95 elapsed ~6074 with pre/post block drains p95 3917/2915 and block_payload_drain p95 1064; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_5uSRtP`. 2026-01-31 rerun after lowering Izanami block/commit to 500/750ms with pacing_factor_bps=10000, collectors_k=3, redundant_send_r=3, and pacing governor clamped to 1x: min height 122 in 300s (target 200), no missing-highest logs observed; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_pSftTI`, log `/tmp/izanami_1tps_20260131_192200_run6.log`.
- [x] Gate pipeline/roster sidecar fsync on `kura.fsync_mode=on` (batched/off skips fsync) and rerun the 300s 1 TPS profile to confirm height progress. 2026-01-31 rerun after sidecar-fsync gating (4 peers, 300s, target 200, tps 1) stopped before the target with lane_000_core `blocks.hashes` count 130 across peers; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_99DZ8m`.
- [x] Profile/trim time-trigger execution by short-circuiting data-trigger DFS when no data triggers exist, then rerun the 300s 1 TPS profile. 2026-01-31 rerun (4 peers, 300s, target 200, tps 1) stopped before the target with lane_000_core `blocks.hashes` size 4480 bytes (~140 entries); network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_Zkkr3A`.
- [x] Move pipeline recovery sidecar writes to a background worker (no consensus-path I/O) and rerun the 300s 1 TPS profile. 2026-01-31 rerun (4 peers, 300s, target 200, tps 1) stopped before the target with lane_000_core `blocks.hashes` size 4000 bytes (~125 entries); network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_3nsRop`.
- [x] Memoize commit-QC/checkpoint roster validation results (hash + stake snapshot keyed) to skip repeated BLS verification; add unit coverage.
- [x] Re-run the Izanami 1 TPS profile to confirm queue-latency reduction after roster-validation memoization (2026-01-31 run; 4 peers, 300s, target 200, tps 1) stopped before the target with lane_000_core `blocks.hashes` size 3104 bytes (~97 entries); network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_oVDDBU`.
- [x] Reduce vote-drain hotspots now that vote-verify offload + batching still leave `vote_drain_ms` dominant (latest 1 TPS run: mean 432–490ms, p95 2182–2344ms, 49–50 blocks in 300s; 2026-01-27 run after SumeragiParameters timing injection: p50 0, p90 734, max 3242, height 41/300s, logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_wWvhSZ`; 2026-01-27 run after vote-inbound deferral: p50 0, p90 778, max 3485, height 44/300s, logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_p4poFN`); vote/QC verify workers now auto-size threads/queue caps when config values are 0, buffering more work before synchronous fallback.
- [x] Investigate Izanami 1 TPS QC fast-path run `irohad_test_network_bLPG1H`: peer exit status 3 and Torii connection refusals; stabilize startup and re-run the profile. 2026-01-31 rerun: no Torii connection refusals or exit status 3 observed; stopped before target at min height 122; network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_pSftTI`, log `/tmp/izanami_1tps_20260131_192200_run6.log`.
- [x] Enable expensive-metrics capture for throughput runs (telemetry profile/config) or surface pending-block + commit-inflight gauges in `/v1/sumeragi/status`; current `/metrics` scrapes are empty under the test-network defaults.
- [x] Investigate skewed NEW_VIEW participation during throughput stalls (recent NPoS/permissioned runs show `new_view_slots` max < quorum with per-peer NEW_VIEW vote traffic heavily skewed; 2026-02-16: NEW_VIEW vote emission now reuses collector-plan contacted peers with redundant collector expansion, parallel topology fanout, remote target floor top-up, and leader inclusion to reduce skew; updated unit coverage in `new_view_votes_target_collectors_even_when_below_quorum` and `new_view_votes_target_collectors_when_local_leads_in_npos`).
 - [ ] Use the new retransmit/gossip status counters in `/v1/sumeragi/status` during 7-peer 100 TPS runs (`retransmit_target_set_*`, `retransmit_skip_*`, `gossip_duplicate_known_skipped_total`, `quorum_stall_age_escalation_total`) and tune backlog pacing/hysteresis until the 180s both-mode script exits without manual tx-load termination.
 - [x] NPoS roster selection now appends active validators missing from the commit topology so the full validator set participates; unit coverage added.
 - [x] Seed commit topology from the checkpoint topology when world peers are incomplete (prevents genesis roster shrink/QC stalls); unit coverage added.
 - [x] Add a Kagami/localnet regression that asserts validator count + commit QC validator-set length match the peer count on startup.
 - [x] Scale bulk consensus-ingress caps for 1s block times, emit per-topic drop/penalty metrics for payload traffic, and update localnet defaults (no legacy compatibility toggles).
 - [x] Refactor `iroha transaction ping` to cap parallel workers (`--parallel-cap`, default 1024; 0 disables) and reuse a single `Client` per batch; add unit coverage for the cap logic.
 - [x] Extend `scripts/tx_load.py` to shard load across Torii peers (`--peer-urls`/`--peer-count`), report rate-limit hits, and keep per-shard stats (tests added).
 - [x] Add an ignored 7-peer localnet throughput regression (target 10k tps, 1s block time) that asserts block height advances, commit time stays near target, and no consensus stalls occur.
 - [x] Define 1s-finality SLO thresholds for permissioned and NPoS (p95/p99 commit-QC latency, view-change rate, queue backpressure).
 - [x] Specify pass/fail criteria and measurement windows (warm-up, steady-state duration, no-height-advance timeout).
 - [x] Document the SLO acceptance table for both modes in the performance docs (see `docs/source/sumeragi_localnet_throughput.md`).
 - [x] Enumerate required status/telemetry fields for throughput runs (status endpoints, metrics names) and list any gaps to add.
 - [x] Add a throughput report template (run metadata, config fingerprint, hardware, commands, logs/metrics paths).
 - [x] Add a script or harness output to emit the report artifacts (status snapshots, metrics export, log paths).
 - [x] Add explicit 10k TPS / 1s finality config profile for permissioned mode (block/commit times, collectors_k/r, payload/queue caps) in Kagami templates and docs.
 - [x] Add explicit 10k TPS / 1s finality config profile for NPoS mode (NPoS timeouts, collectors_k/r, payload/queue caps, stake bootstrap knobs) in Kagami templates and docs.
 - [x] Wire a profile selector into the localnet runner so permissioned/NPoS perf profiles are easy to switch.
 - [x] Extend the load harness with a deterministic 10k TPS recipe (tx type/size, batch, parallelism, peer count, fixed RNG seed).
 - [x] Add warm-up + steady-state phases and stall detection (no-height-advance timeout) to the harness run flow.
 - [x] Emit standardized throughput/finality metrics from the harness (commit-QC latency p95/p99, view-change rate, queue backpressure, submitted/committed TPS) for comparison runs.
 - [x] Investigate pending-block backpressure gating (blocking proposals until quorum reschedule) that keeps localnet cadence near commit-quorum timeout; design a safe fast-path to reach sub-1s finality without speculative execution (added fast-path unblock when no votes/QCs arrive by `min(block_time, commit_time)`, applied in proposal backpressure + quorum reschedule; unit coverage added and pacemaker docs updated).
- [ ] Run the ignored 7-peer localnet throughput regression in permissioned mode and capture throughput/commit-time metrics in `status.md` after the oversized BlockSyncUpdate fetch fallback fix (attempts failed: submit queue stalled at ~22k/42k with min_non_empty=1-2; release runs timed out after 20m; latest debug run timed out after 20m with height stuck at 2 and repeated view-change/missing-QC logs; logs in `/var/folders/7l/w31n0ppj4zg874c4szhllss00000gn/T/irohad_test_network_uDjmli`, `/var/folders/7l/w31n0ppj4zg874c4szhllss00000gn/T/irohad_test_network_cO9vgq`, `/var/folders/7l/w31n0ppj4zg874c4szhllss00000gn/T/irohad_test_network_TY25OZ`, `/var/folders/7l/w31n0ppj4zg874c4szhllss00000gn/T/irohad_test_network_hMZDGM`; 2026-01-22 release run failed with submit queue not draining below 20000 within 180s, artifacts in `/private/tmp/iroha-throughput-permissioned-20260122b/throughput-1769106279251`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_gIn3el`; 2026-02-02 release run failed with submit queue stuck ~22.5k (min_non_empty=1), not draining below 20000 within 180s; artifacts `/tmp/iroha-throughput-permissioned-20260202b`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_q2bRUM`).
- [ ] Run the ignored 7-peer localnet throughput regression in NPoS mode and capture throughput/commit-time metrics in `status.md` after the above gates and harness outputs are in place (2026-01-22 release run failed with submit queue not draining below 20000 within 180s; artifacts in `/private/tmp/iroha-throughput-npos-20260122/throughput-1769107672300`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_Xq0l0o`; 2026-02-02 release run failed with submit queue stuck ~22.5k (min_non_empty=1), not draining below 20000 within 180s; artifacts `/tmp/iroha-throughput-npos-20260202b`, network dir `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_c6Gw2W`).
 - [x] Re-run the NPoS localnet 1 Hz / 100-block check with `/v1/sumeragi/status` sampling after the progress-age quorum timeout change; capture any stalls in `status.md` (2026-01-21 run on `/tmp/iroha-localnet-npos-1hz-run2`: `commit_qc.height` +12 over 118s, `view_change_install_total` +3, pending RBC max 1040 bytes / 1 session).
 - [x] Re-run the NPoS localnet 1 Hz / 100-block check with `/v1/sumeragi/status` sampling after the progress-age quorum timeout change and commit-pipeline backlog-bypass tweaks; capture any stalls in `status.md` (2026-01-23 run at `/private/tmp/iroha-localnet-npos-1hz-20260123153657`: `commit_qc.height` 0->6 over ~140s, `view_change_install_total` +3, `pending_rbc.bytes` max 2960 with sessions max 4; cadence still below 1 Hz).
 - [x] Re-run the NPoS localnet 1 Hz soak after the FetchPendingBlock missing-INIT response fix to confirm INIT recovery without waiting on full blocks. (ran 2026-01-24 on `/private/tmp/iroha-localnet-npos-1hz-20260124T190127Z`, see `sumeragi_status_1hz_missing_init_debug_20260124T190127Z.jsonl`).

4. **DA-TORII-REFACTOR — Decompose Torii DA ingestion + erasure coding** (Torii/DA, Line: Shared, Owner: Torii WG, Priority: High, Status: 🈺 In Progress, target TBD)
 - [x] Split Torii DA into `crates/iroha_torii/src/da/` with `mod.rs`, `ingest.rs`, `persistence.rs`, `taikai.rs`, `rs16.rs`, and `tests.rs`; keep the public API in `mod.rs` and allow internal breakage for the first release.
 - [x] Extract rs16 erasure coding into a shared module (prefer `iroha_primitives`); update Torii ingestion + `crates/sorafs_car/src/bin/da_reconstruct.rs` to use the shared implementation, delete duplicated fixture logic, and add shared unit tests/fixtures.
 - [x] Add SIMD backends (NEON, AVX2) for rs16 matrix/field ops behind feature flags with deterministic fallback; add parity tests across backends and document the accel knobs.
 - [x] Add a `torii_da_chunking_seconds` histogram around erasure coding/segment assembly, wire it into telemetry exports, and document it in telemetry docs with unit coverage for metric registration.
 - [x] Finish module boundary cleanup (visibility tweaks, test relocation, and lint fixes).
 - [x] Verify Torii DA unit tests pass.
 - [x] Run the DA/Torii targeted matrix (no full-workspace run): `cargo test -p iroha_torii -- --nocapture`, `cargo test -p iroha_core --lib overlay_ -- --nocapture`, and `cargo test -p integration_tests --test mod -- --nocapture` (or narrowed DA/Torii subsets if runtime limits hit); update `status.md` with command results and artifact/log paths. 2026-02-15 evidence: `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p iroha_torii -- --nocapture` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p iroha_core --lib overlay_ -- --nocapture` (ok), narrowed integration subset reruns `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p integration_tests --test mod sumeragi_da::torii_max_content_len_adds_headroom -- --nocapture --exact` (ok) and `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p integration_tests --test mod sumeragi_da::sumeragi_da_payload_loss_does_not_block_commit -- --nocapture --exact` (ok; network startup skipped by sandbox loopback guard).

5. **LOCALNET-DEMO-FLOW — Verify training-script localnet bootstrap** (Consensus/Tooling, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Add an ignored localnet soak integration test that drives thousands of blocks/transactions and document how to run it.
 - [x] Align consensus rebroadcast cooldowns (RBC/pending/precommit) to on-chain `block_time` with a 200ms floor and 2x base multiplier, keeping payload replays at an additional 2x to avoid queue saturation on 1s targets.
 - [x] Widen Sumeragi worker loop drain budgets and block `BlockSyncUpdate` enqueue under backpressure to prevent DA-localnet asset test hangs.【crates/iroha_core/src/sumeragi/mod.rs】
 - [x] Switch localnet + defaults to per-queue Sumeragi channel caps (votes/payload/RBC/block) tuned for 20k TPS and 1s block times.【crates/iroha_kagami/src/localnet.rs】【crates/iroha_config/src/parameters/defaults.rs】
 - [x] Run the updated `training_script_2` workflow against a fresh 4-peer localnet and confirm block 2+ finalize quickly with DA on and no dev bypasses (full script completed; peer0 shows height 2 committed at 10:18:12.75 and height 10 at 10:18:20.84 in ~1s steps; localnet shut down).
 - [x] If Torii readiness or CLI timeouts still stall the run, adjust the script with bounded retries/timeouts and capture the needed Torii readiness checks.
 - [x] Remove empty-child fallback proposals so pacemaker stays idle without queued work; unit tests + docs updated.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【docs/source/sumeragi.md】
 - [x] Validate the block-sync lock realignment path (commit-certificate override) against the localnet stall scenario; 20-run sweep shows no height-49 view-change loop and ~1s block deltas at height 49.
 - [x] Let validated block-sync precommit QCs realign locks even without commit certificates to cover QC-only commits; regression ensures non-extending QC caches and advances the lock.【crates/iroha_core/src/sumeragi/main_loop/block_sync.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】
 - [x] Repeat `training_script_2` runs (>=3) on a fresh 4-peer localnet to confirm 1s cadence stability and capture any hangs/timeouts after pacemaker proposal gating changes (17/20 completed; 3 tx confirmation timeouts captured).
 - [x] Catch up on higher NEW_VIEW frames and fan out NEW_VIEW votes to deterministic collectors with topology fallback (leader included) so quorum converges without broadcast storms; tests/docs updated.【crates/iroha_core/src/sumeragi/main_loop/commit.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【docs/source/sumeragi.md】【docs/source/p2p.md】【docs/source/references/peer.template.toml】
 - [x] Drop stale-view consensus traffic (proposal/block, votes/Prevote QCs, block sync, RBC) to prevent old-view payloads from forming conflicting commits during localnet retries, while allowing availability/precommit QCs to clear DA/commit gates; tests added.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/main_loop/proposal_handlers.rs】【crates/iroha_core/src/sumeragi/main_loop/votes.rs】【crates/iroha_core/src/sumeragi/main_loop/qc.rs】【crates/iroha_core/src/sumeragi/main_loop/rbc.rs】【crates/iroha_core/src/sumeragi/main_loop/block_sync.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【sumeragi.md】
 - [x] Emit availability votes even when validation is deferred (block ahead of local height) so DA/QC formation stays live while lagging peers catch up; tests added.【crates/iroha_core/src/sumeragi/main_loop/commit.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】
 - [x] Include commit-inflight + processing blocks in locked-chain ancestry lookups so precommit/QC aggregation and proposals don't stall while a parent is mid-commit; tests added.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【crates/iroha_core/src/sumeragi/main_loop/commit.rs】
 - [x] Drop precommit votes that are below or conflicting with the lock on receipt and prune stale non-extending precommit votes as the lock advances to avoid non-extending QC churn in localnet; tests added.【crates/iroha_core/src/sumeragi/main_loop/votes.rs】【crates/iroha_core/src/sumeragi/main_loop/qc.rs】【crates/iroha_core/src/sumeragi/main_loop/commit.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】
 - [x] Increase DA commit quorum timeout slack, align availability gate timeout with the shared helper, and allow stale-view RBC payload messages for known pending blocks to clear availability after view changes; tests/docs updated.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/main_loop/commit.rs】【crates/iroha_core/src/sumeragi/main_loop/reschedule.rs】【crates/iroha_core/src/sumeragi/main_loop/rbc.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【docs/source/sumeragi.md】
 - [x] Accept stale precommit votes for known pending blocks after view changes so late votes can still form precommit QCs; tests/docs updated.【crates/iroha_core/src/sumeragi/main_loop/votes.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【docs/source/sumeragi.md】
- [x] Anchor pipeline recovery sidecars to the block hash (v1) so DAG fingerprint checks only apply to the exact block and stale sidecars from forks/restarts are ignored; tests/docs updated.【crates/iroha_core/src/kura.rs】【crates/iroha_core/src/block.rs】【docs/source/pipeline.md】
 - [x] Realign divergent locked QCs to the committed tip before proposing so leaders don't stall on forked locks; unit test + docs updated.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/main_loop/propose.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【docs/source/sumeragi.md】
 - [x] Re-run `training_script_2` (20x) after the precommit vote lock + proposal deferral to confirm no forked commits, no long stalls, and ~1s block cadence across runs (20/20 succeeded; height10 8-9s; no long stalls).
 - [x] Re-run `training_script_2` with the fast localnet pipeline defaults, DA availability timeout tuning, and redundant-send fanout to confirm tx status timeouts and rare >40s gaps are gone (20/20 succeeded; no tx status timeouts or >40s gaps; ~0.95s cadence).【crates/iroha_kagami/src/localnet.rs】【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Re-run localnet after the availability-vote emission fixes (including deferred-validation votes) to confirm missing-availability QC warnings and DAG fingerprint mismatch warnings are gone (0 missing-availability warnings; 0 DAG fingerprint mismatches).
 - [x] Re-run the seven-peer DA consistency test after restoring the DA quorum timeout multiplier to confirm view-change stalls no longer stretch commits (pass: `cargo test -p integration_tests --test mod seven_peer_cross_peer_consistency_basic`).【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Eliminate remaining tx status timeouts (approve/propose/register) and rare >40s block gaps observed during the 20-run localnet sweep.【crates/iroha/src/client.rs】【crates/iroha_kagami/src/localnet.rs】【crates/iroha_kagami/README.md】
 - [x] Seed peer telemetry with explicit Torii URLs in localnet configs so peer monitors stop hitting P2P ports during demos.【crates/iroha_torii/src/lib.rs】【crates/iroha_kagami/src/localnet.rs】【docs/source/telemetry.md】

6. **INTEGRATION-TEST-STABILITY — Replace transient skips with deterministic readiness** (QA/Consensus, Line: Shared, Owner: QA WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Audit integration tests that now skip on timeouts (events pipeline, sumeragi_da, unstable_network, genesis/offline peers, triggers/time triggers) and replace with deterministic readiness probes or tuned deadlines.
 - [x] Investigate serial-guard port contention and long startup waits; tighten network teardown or add cleanup hooks to reduce startup stalls.
 - [x] Re-run `cargo test -p integration_tests --test mod` without skip paths and confirm stability across at least three consecutive runs.

7. **SUM-ACTOR-SPLIT-OBSERVABILITY — Decompose Sumeragi actor and harden inflight commit safety** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Split DA/VRF/commit state into sub-actors/state modules with explicit interfaces (DA state isolated, VRF actor extracted, commit inflight tracking isolated).
 - [x] Added backpressure/queue diagnostics plus inflight duration metrics and `/v1/sumeragi/status` hooks for queue depths/diagnostics + commit inflight snapshots.
 - [x] Added inflight timeout/abort logic (requeue stalled commits) with unit coverage to prevent permanent stalls.

8. **OFFLINE-FLOW-BLOCKERS — Complete issuer APIs, FASTPQ proofs, and platform proof plumbing** (Core/Torii/SDK, Line: Shared, Owner: Offline WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Issuer API spec: define OfflineWalletCertificate issuance/renewal endpoints (operator-signed), request/response payloads, attestation nonce flow, and spend-key custody rules (wallet-generated keypair, public-only submission; define any import/rotation path); update `docs/source/offline_allowance.md` + Torii OpenAPI.
 - [x] Issuer service implementation: add Torii app_api routes or a dedicated issuer service, add `iroha_config` knobs for operator keys/allowed controllers, and enforce controller/operator authorization; include issue/renew/revoke integration tests.
 - [x] SDK issuance helpers: add Swift/Android/JS clients for issue/renew endpoints; model `OfflineWalletCertificateDraft`/responses; document spend-key custody (wallet-generated keypair, public-only submission) and any import/rotation path.
 - [x] FASTPQ bridge (Poseidon): expose receipts-root hashing via `connect_norito_bridge`, wire `NoritoNativeBridge`/Swift helpers, and keep deterministic CPU fallback while documenting Metal/CUDA acceleration hooks.
 - [x] Aggregate proof envelope wiring: build `AggregateProofEnvelope` from witness payloads, define metadata keys for circuit/parameter set selection, and add regression fixtures (Rust + Swift) for proof bytes + receipts_root parity.
 - [x] Proof-mode enforcement: add `settlement.offline.proof_mode` in `iroha_config` (optional vs required), enforce missing/invalid proofs in admission, and update docs/tests.
 - [x] Platform counters storage: add SDK-side `OfflineCounterState` persistence with summary-hash parity to `OfflineCounterSummary`, sync from `/v1/offline/summaries`, and validate monotonic increments before bundle submission.
 - [x] Play Integrity / HMS wrappers: implement Android SDK helpers for token acquisition/caching, nonce + TTL handling, and metadata validation; add Swift/JS models for token snapshot ingest/validation; align required metadata keys with Torii validation.
 - [x] App Attest/KeyMint counter flow: add SDK APIs to read last counter checkpoints (App Attest/KeyMint + provisioned), validate monotonic increments, surface mismatch errors, and add tests against fixtures/Torii summary payloads.

8. **SUM-RBC-ROSTER-PERSISTENCE — Persist RBC session rosters across restarts** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Persist per-session commit-topology snapshots in the RBC store so READY/DELIVER validation survives restarts and roster flips; include versioned migration handling and restart coverage.
 - [x] Add a 4-peer DA integration test that restarts a validator during an in-flight RBC while unregistering another peer, asserting recovery succeeds with persisted rosters.【integration_tests/tests/sumeragi_da.rs】

9. **GOV-API-WIRING — Implement governance API + execution pipeline** (Core/Torii/Governance, Line: Shared, Owner: Governance WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define signature schemes for `GovernanceEnactment` and `ParliamentEnactment`; update data model + docs.
 - [x] Replace Torii governance endpoint stubs with real validation, transaction construction, and execution wiring.
 - [x] Persist governance proposals/ballots/enactments in WSV and model full state transitions.
 - [x] Add end-to-end tests for proposal/ballot/enactment flows, authorization gates, and rejection paths.
 - [x] Update governance docs and Torii OpenAPI to reflect the real API surface.

10. **OFFLINE-QR-STREAM — Animated QR transport for offline payloads** (Core/SDK/CLI, Line: Shared, Owner: Offline WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Spec: define `QrStreamEnvelope` + `QrStreamFrame` wire format (Norito), payload kinds, CRC32 rules, chunking, replay guards, and size caps; include deterministic hashing + schema binding in `docs/source/qr_stream.md`.
 - [x] Spec: define frame scheduling (header cadence, data/parity frames), binary QR requirements, and capacity profile (ECC level + chunk size bounds) with a compatibility matrix for iOS/Android/JS scanners.
 - [x] Parity frames: select a deterministic erasure scheme (reuse `iroha_primitives::rs16` if possible) and define parameters/seed derivation + target drop tolerance.
 - [x] Parity frames: implement encode/decode + recovery tests (out-of-order, dropped frames, duplicates) in Rust helpers.
 - [x] Fixtures: generate deterministic fixtures under `fixtures/qr_stream/` (receipt + transfer payloads, envelope bytes, frame bytes, parity frames) and document regeneration flow.
 - [x] Data model: add `qr_stream` types + helpers to `iroha_data_model` (encode/decode, CRC32, assembler state machine) with unit coverage.
 - [x] Data model: add assembler caps (max payload bytes/frames, timeout policy) and tests for rejection paths.
 - [x] CLI tooling: add `iroha offline qr encode` (binary QR mode, chunk sizing, ECC/fps controls, SVG/PNG + animated GIF/APNG export) with tests.
 - [x] CLI tooling: add `iroha offline qr decode` (frames/dir input, hash verification, Norito/JSON output) with tests.
 - [x] Swift SDK: add QrStream encoder/decoder + ScanSession core with unit tests.
 - [x] Swift SDK: integrate Vision/AVFoundation scan pipeline to feed raw bytes into ScanSession + fixture tests.
 - [x] Android SDK: add QrStream encoder/decoder + ScanSession core with unit tests.
 - [x] Android SDK: integrate CameraX/ZXing scan pipeline to feed raw bytes into ScanSession + fixture tests.
 - [x] JS SDK: add QrStream encoder/decoder + ScanSession core with unit tests.
 - [x] JS SDK: add scan-loop integration (browser/Node) + fixture tests.
 - [x] SDK theme palette + frame-style helpers for sakura animation (colors, pulse, petal phase).
 - [x] Streaming animation: ship on-brand playback skins (petal drift, warm gradient, progress overlay), plus reduced-motion and low-power variants for accessibility/perf.
 - [x] UX guidance: add scan timing + progress UI guidelines and fallback flows to `docs/source/offline_allowance.md` + SDK guides.
 - [x] Optional Petal Stream transport: add petal-grid framing + CLI encode/decode + JS/Swift/Android scan helpers + docs for custom scanners.

10.2. **OFFLINE-PETAL-BRAND-TUNING — Keep branded styles decode-safe at 3KB/s+** (Core/CLI, Line: Shared, Owner: Offline WG, Priority: High, Status: 🈺 In Progress, target TBD)
 - [x] Increase perceived tile size while preserving bit/channel decoding (`blend_sora_data_tile` margin + bracket tuning) and keep deterministic rendering.
 - [x] Run default-profile simulated capture eval over the style set and record completion ratios for `sora-temple*` variants.
 - [x] Add deterministic katakana distance-safe realtime loop simulation and decode hardening (`--realtime-loops`, stream-id locking/filtering, duplicate-header ignore, candidate frame validation), and validate real fixture roundtrip + looped perturbation reconstruction (`artifacts/petal_real_katakana_distance_safe_v3_realtime_l3_report.json`, SHA match against `fixtures/offline_bundle/aggregate_proof_fixture.json`).
 - [ ] Land a decode-safe stronger-logo profile for baseline `sora-temple` (target: preserve complete stream reconstruction in default profile while improving `天` visibility over v11).
 - [x] Add a repeatable style score script/report (aesthetic + decode completion + effective B/s) to keep visual iterations measurable (`iroha offline petal score-styles` in `crates/iroha_cli/src/offline/petal.rs`, documented in `docs/source/petal_stream.md`).
 - [ ] Re-run aggressive capture profile once per candidate style and publish pass/fail gates in `status.md` for operator-facing defaults.

10. **ZK-VERIFY-COMPLETE — Full ZK verification + prover** (ZK/Core/Torii, Line: Shared, Owner: ZK WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Route `halo2/ipa` OpenVerifyEnvelope decoding into the IPA verifier, remove placeholder acceptance, and refresh tiny-add fixtures/tests to use real proofs + VK bytes.
 - [x] Resolve `vk_ref` to key bytes for `VerifyProof` before verification.
 - [x] Replace Torii ZK prover stub with a real proving pipeline (or remove the feature if unsupported).
 - [x] Define prover scope + plumbing: supported backends/circuits, proving-key storage, attachment-to-circuit mapping, and Torii report schema fields (backend, vk_ref, proof hash).
 - [x] Decide Groth16/STARK support: implement verifiers or drop stub tags; document the supported backend matrix.
 - [x] Expand deterministic verification fixtures/tests beyond tiny-add (public-instance and multi-row circuits).
 - [x] Resolve merge-conflict markers in `crates/iroha_core/src/zk.rs` that currently break builds/tests.
 - [x] Update ZK docs/configs to reflect the expanded fixture set and supported test circuits.

10.1. **IVM-ZK-EXECUTION-PROOF — Real IVM execution semantics proof (no replay)** (ZK/IVM/Core/Torii, Line: Shared, Owner: ZK WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Define the end-to-end statement and public inputs for a real IVM execution proof (bind at least `code_hash`, `overlay_hash`, `events_commitment`, `gas_policy_commitment`) and the witness requirements (execution trace + host I/O).
- [x] Implement a Halo2 circuit that proves IVM step semantics (start with a restricted opcode/syscall subset) and add the required deterministic 64-bit arithmetic gadgets.
- [x] Extend Torii proving to execute bytecode and build the circuit witness (either via a new endpoint or by versioning `/v1/zk/ivm/prove`), preserving privacy guarantees (no plaintext `gas_used`).
- [x] Update `Executable::IvmProved` admission to optionally skip deterministic VM replay only when the proof circuit guarantees full semantics; keep replay as the fallback and add regression tests for both modes.
- [x] Add `pipeline.ivm_proved.skip_replay` and wire admission skipping for full-semantics circuits with regression coverage.

11. **SUMERAGI-ACTOR-REFACTOR — Replace ad-hoc worker loop with a deterministic actor model** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Document the current worker loop/queue topology, ordering rules, backpressure behavior, and determinism constraints; capture known failure modes and desired invariants.
 - [x] Define a new message envelope with explicit priority classes and deterministic tie-breakers; map `BlockMessage`, `ControlFlow`, RBC, lane relay, and background requests to priority tiers.
 - [x] Implement a single scheduling layer (priority mailbox + bounded budgets) that replaces multi-queue draining while preventing starvation and preserving deterministic processing.
 - [x] Move background network posting into a dedicated worker with backpressure + metrics; remove inline-send fallback paths from the actor loop.
 - [x] Upgrade the current FIFO dedup caches to bounded LRU/TTL structures partitioned by message kind; add eviction metrics and duplicate-suppression tests.
 - [x] Split `Actor` state into explicit subcomponents with minimal shared mutable state (commit, propose/pacemaker, DA/RBC, VRF, merge/lane relay) and unit-testable interfaces.
 - [x] Add deterministic replay tests: given a fixed message trace, the scheduler produces identical state transitions and outputs across runs; include saturation and fairness scenarios.
 - [x] Update `docs/source/sumeragi.md`, `/v1/sumeragi/status` telemetry notes, and `status.md`/`roadmap.md` migration guidance to reflect the new actor model.

12. **SUMERAGI-BCERT-REFLOW — Replace QC commit gating with B-Chain commit certificates** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Update API/telemetry surfaces (`/v1/sumeragi/status`, `/v1/status`, Prometheus labels) to reflect commit certificates and quorum tracking.
 - [x] Add integration coverage (commit-vote counters via `/v1/sumeragi/status` + metrics, stake quorum, commit-cert block sync, Kagami 4+ peer localnet bootstrap).
 - [x] Capture commit-certificate migration notes in `status.md` once integration coverage is in place.

13. **QUERY-DSL-IMPLEMENTATION — Replace lightweight query DSL stubs** (Data Model/Core/SDK, Line: Shared, Owner: Data Model WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement real predicate/projection semantics for `query::dsl` and `query::dsl_fast`.
 - [x] Ensure Norito serialization captures predicate payloads and remains deterministic.
 - [x] Wire Torii config limits into query post-processing and require config in request limit validation.
 - [x] Add unit/integration tests; update builder APIs and examples.

15. **IZANAMI-SUMERAGI-FRAME-CAP — Validate consensus frame cap + RBC chunk clamp** (Consensus/QA, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈺 In Progress, target TBD)
- [x] Re-run Izanami 4-peer DA runs to confirm no `FrameTooLarge` disconnects and consensus reaches target blocks (2026-01-23 run: `RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo run -p izanami -- --allow-net --nexus --peers 4 --faulty 0 --duration 300s --target-blocks 20 --progress-interval 10s --progress-timeout 180s --tps 5 --max-inflight 8 --workload-profile stable` completed with 112 successes/0 failures; no `FrameTooLarge` observed; duplicate metric registration warnings persisted).
 - [x] Run the frame-cap targeted matrix (no full-workspace run): `cargo test -p iroha_core --lib sumeragi::main_loop::tests -- --nocapture`, `cargo test -p integration_tests --test sumeragi_da -- --nocapture`, and `cargo test -p integration_tests --test sumeragi_localnet_smoke -- --ignored --nocapture`; update `status.md` with command results and artifact/log paths. 2026-02-15 evidence: `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p iroha_core --lib sumeragi::main_loop::tests -- --nocapture` (1048 passed / 1 failed: `handle_qc_records_commit_qc_history`), isolated rerun `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p iroha_core --lib sumeragi::main_loop::tests::handle_qc_records_commit_qc_history -- --nocapture` (ok), `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p integration_tests --test sumeragi_da -- --nocapture` (ok; network startup skipped by sandbox loopback guard), `CARGO_TARGET_DIR=/tmp/iroha_target_roadmap_noworkspace cargo test -p integration_tests --test sumeragi_localnet_smoke -- --ignored --nocapture` (ok; network startup skipped by sandbox loopback guard).
 - [x] Size consensus frames using NetworkMessage wire lengths, trim BlockSyncUpdate payloads to fit caps, and guard background consensus sends against oversize frames.
 - [x] Trim proposal tx batches in bulk when BlockCreated frames exceed consensus caps to avoid O(n^2) assembly stalls; add unit coverage.
 - [x] Harden Norito length decoding to reject u64->usize overflows across core and AoS columnar views; add regression coverage.
 - [x] Tighten Norito StreamMapIter packed-seq offset validation and payload length accounting; add regression coverage.
 - [x] Validate NCB name/opt-str offsets for monotonicity and use canonical DecodeFromSlice in Norito streaming types; add regression coverage.
 - [x] Harden NCB columnar views with checked length multipliers, enum dict code validation, and u32-delta bounds; add regression coverage.
 - [x] Validate NCB dict codes, harden opt-column length parsing, and guard SIMD bundle length handling; add regression coverage.
 - [x] Reject id-delta underflow in NCB views and add regression coverage for delta-coded ids.
 - [x] Guard opt-column presence-bitset masking against 32-bit overflow in optstr/optu32 NCB views.
 - [x] Enforce ABI v1 only (remove Experimental policy paths) and update related tests/docs.
- [x] Remove Norito toggles and length/offset decode paths (update `norito.md`).
 - [x] Standardize Norito length-prefix flag scoping (COMPACT_LEN/COMPACT_SEQ_LEN/VARINT_OFFSETS) and update the spec/docs.
- [x] Sweep docs/translations for remaining references and clean up tooling flags.

15. **SUMERAGI-UNIFIED-QC — Merge commit + execution certificates into a single QC** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define unified `QC`/`QcVote` types (rename Commit*), add `parent_state_root` + `post_state_root`, update Norito schema, and bump `PROTO_VERSION`.
 - [x] Update vote preimage/signing rules to include roots for all phases (zero roots for non-Commit) and refresh signature tests/fixtures.
 - [x] Remove ExecVote/ExecutionQC wire messages and handlers; decide ExecWitness scope (pipeline-only) and update `BlockMessage`/event plumbing.
 - [x] Capture execution roots during pre-vote validation (witness capture) and store them with pending blocks.
 - [x] Require commit votes to use stored roots and remove commit-phase recomputation.
 - [x] Refactor QC aggregation/validation to verify roots + signers, and remove exec_vote_log/execution_qc_cache tracking and telemetry.
 - [x] Evidence updates: drop conflicting-root evidence variants, treat conflicting roots as double-commit evidence, and update evidence validation/tests.
 - [x] Storage updates: remove execution-QC/root storage, persist QC roots for audit/bridge, update tiered storage segments/snapshots.
 - [x] Block sync + bridge: adapt QC derivation/validation and finality proof paths to the unified QC layout (roots included).
 - [x] Config cleanup: remove execution-QC gating toggles, collapse ProofPolicy to QC (+ optional ZK), update defaults.
 - [x] Docs/telemetry/tests: pipeline/sumeragi/telemetry docs, status/metrics fields, SDK clients/tests, and perf fixtures updated for unified QC; evidence language refreshed.

16. **SUMERAGI-RBC-MISMATCH-COVERAGE — Fill remaining JSON coverage gaps** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: Low, Status: 🈴 Completed, target TBD)
- [x] Add a JSON roundtrip assertion for `rbc_mismatch` in `sumeragi_status_wire_roundtrip_to_json_preserves_fields` (client JSON payload).
- [x] Add a consensus routing JSON test for `rbc_mismatch` in `crates/iroha_torii/src/routing/consensus.rs`.
- [x] Run targeted tests for the new assertions (client + torii routing).
- [x] Add unit coverage that stashes a mismatched RBC chunk with a sender before INIT, flushes pending RBC, and asserts per-peer mismatch counters increment.
- [x] Include sender context in hydrated payload mismatch logs when available (payload hash/digest/root mismatch paths).
- [x] Run targeted unit tests for the mismatch attribution gap fixes and record results in `status.md`.

17. **SUMERAGI-STATE-MACHINE-FIXES — Resolve aborted-pending recovery and mode-flip edge cases** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Define and implement the recovery path for aborted pending blocks (allow BlockCreated/BlockSync to re-admit payloads, refresh tracking state, and preserve validation status for safe finalize).
- [x] Ensure missing-block requests are cleared when BlockCreated/BlockSync is accepted via duplicate/processing/inflight fast paths to avoid stale view-change triggers.
- [x] Gate runtime mode flips while commit pipeline work is active (pending_processing/commit inflight) and surface the blocked reason via status/telemetry.
- [x] Add unit coverage for aborted-pending recovery, mode-flip with inflight/pending_processing, and missing-block request clearing on duplicate/processing/inflight BlockCreated paths.
- [x] Suppress idle view-change timeouts while a commit is inflight so local execution does not trigger spurious view rotations.

17. **BRIDGE-RECEIPTS-TYPED — Replace bridge log stub with typed events** (Core/Executor/CLI, Line: Shared, Owner: Core Protocol WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Emit typed Data/event payloads for bridge receipts (remove log stub).
 - [x] Update CLI to submit real bridge receipts; add tests + docs.

18. **SORAFS-GATEWAY-LIVE — Replace fixture gateway stub with live integration** (SoraFS/Core, Line: Iroha 3, Owner: SoraFS WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Wire gateway to the real storage/pinning pipeline and remove fixture-only logic.
 - [x] Replace `proof-stub` header acceptance with real proof verification and update SoraFS gateway tests/fixtures.
 - [x] Retire `sorafs_manifest_stub`/`sorafs_provider_advert_stub` usage once real manifest/proof pipelines land; update CLI helpers and docs.
 - [x] Add integration tests and update runbooks.

19. **SORANET-RELAY-LIVE — Implement SoraNet relay** (Networking, Line: Iroha 3, Owner: Networking WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement relay protocol, persistence, and telemetry; remove stub designation.
 - [x] Add tests and docs for relay operations.

21. **P2P-NOISE-HANDSHAKE — Implement noise handshake path** (P2P/Core, Line: Shared, Owner: Networking WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement `noise_handshake` feature behavior (not just the default handshake).
 - [x] Add alignment tests and update docs.

23. **TESTS-FLAKY-UNBLOCK — Re-enable ignored/failing tests** (QA/Consensus, Line: Shared, Owner: QA WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Re-run `integration_tests/tests/sumeragi_da.rs::sumeragi_rbc_recovers_after_restart_with_roster_change` after the ExecutionQC stale-view handling update and confirm strict ExecQC gating clears.
 - [x] Verify relay-based unstable network tests after serializing startup (`start_network_under_relay` now holds the global serial guard); investigate remaining timeouts if any.
 - [x] Stabilize and re-enable `connected_peers_with_f_2_1_2`.
 - [x] Fix trigger executor path in `integration_tests/tests/upgrade.rs` and re-enable the test.
 - [x] Rewrite trigger integration tests to async with shorter timings.
 - [x] Re-run `integration_tests/tests/sumeragi_*` after restoring full-roster QC quorum counting (Set B signatures count) to confirm quorum acceptance behavior.

24. **DOCS-I18N-STUBS — Replace auto-generated translation stubs** (Docs, Line: Shared, Owner: Docs WG, Priority: Low, Status: 🈺 In Progress, target TBD)
 - [x] Inventory stubbed translations by locale + directory (current scan: ~17.5k stubs across ar/es/fr/he/ja/pt/ru/ur + unknown); publish a tracker under `docs/i18n/` with counts and ownership.
 - [x] Replace repo-root `MAINTAINERS.*` stubs with translations (ar/es/fr/pt/ru/ur).
 - [x] Replace repo-root `integrated_test_framework.es.md` stub with a Spanish translation.
 - [x] Replace repo-root `integrated_test_framework.fr.md`, `integrated_test_framework.pt.md`, and `integrated_test_framework.ru.md` stubs with translations.
 - [x] Replace repo-root `integrated_test_framework.ar.md` stub with an Arabic translation.
 - [x] Replace `docs/source/soranet_gateway_hardening.*` stubs (ar/es/fr/he/ja/pt/ru/ur) with translations.
 - [x] Replace `docs/automation/README.*` stubs (ar/es/fr/he/ja/pt/ru/ur) with translations.
 - [x] Replace `docs/automation/android/README.*` stubs (ar/es/fr/he/ja/pt/ru/ur) with translations.
 - [x] Replace `docs/automation/da/README.*` stubs (ar/es/fr/he/ja/pt/ru/ur) with translations.
 - [x] Replace `docs/bindings/README.*` stubs (ar/es/fr/he/ja/pt/ru/ur) with translations.
 - [x] Replace `docs/devportal/try-it.*` stubs (he/ja) with translations.
 - [x] Replace `docs/settlement-router.*` stubs (he/ja) with translations.
 - [x] Replace `docs/space-directory.*` stubs (he/ja) with translations.
 - [x] Replace `docs/amx.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/governance_playbook.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/taikai_cache_hierarchy.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet_vpn.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soracles_evidence_retention.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/p2p_trust_gossip.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/compute_lane.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soracles.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soradns_ir_playbook.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet_gateway_billing.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet_gateway_billing_m0.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/mochi/packaging.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/mochi/index.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/mochi/quickstart.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/mochi/troubleshooting.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/benchmarks/history.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/agents.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/agents/missing_docs_inventory.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/agents/env_var_migration.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/android_support_playbook.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/compliance/android/checklists/and8_ga_2027-10_rehearsal.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/android/and7_governance_hotlist.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/android/android_support_playbook.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/android/partner_sla_discovery.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/compliance/android/eu/README.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/compliance/android/jp/README.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/android/norito_fixture_alignment.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/index.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/project_tracker/norito_streaming_post_mvp.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/compliance/android/device_lab_contingency.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/swift/connect_risk_tracker.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/crypto/attachments/sm_openssl_provenance.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/governance_pipeline.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/kagami_profiles.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/android/telemetry_override_log.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/release_dual_track_schedule.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/swift/readiness/screenshots/2026-03-05/README.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/swift/readiness/screenshots/2026-02-28/README.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/nsc-55-legal.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/nsc-42-legal.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/reports/pow_resilience.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/snnet15_m3_runbook.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/reports/circuit_stability.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/snnet15_m2_runbook.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/swift_xcframework_procurement_request.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/project_tracker/sorafs_pin_registry_tracker.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/torii/norito_rpc_tracker.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/connect_architecture_followups.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/gar_cdn_policy_bus.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/crypto/sm_wg_sync_template.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/templates/downgrade_communication_template.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/swift/ios5_dx_completion.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/norito_stage1_cutover.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sns/reports/steward_scorecard_2026q1.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/status/soranet_testnet_weekly_digest.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sdk/swift/readiness/reports/202603_and7_quiz.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/torii/api_versioning.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/zk/proof_retention.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/sorafs/migration_ledger.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet/lane_profiles.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet_billing_m0.*` stubs (he/ja) with translations.
 - [x] Replace `docs/source/soranet_gateway_bug_bounty.*` stubs (he/ja) with translations.
 - [x] Replace stub translation files across repo root, `docs/source`, and `docs/portal` (preserve front matter, keep `source_hash`/`translation_last_reviewed` aligned).
   - [x] Portal OpenAPI README stubs translated across ar/es/fr/he/ja/pt/ru/ur (`docs/portal/static/openapi/README.*`).
   - [x] Portal versioned reference stubs translated for README, publishing checklist, and Norito codec (`docs/portal/versioned_docs/version-2025-q2/reference/*.{ar,es,fr,he,ja,pt,ru,ur}.md`).
   - [x] Portal versioned Norito streaming roadmap stubs translated (`docs/portal/versioned_docs/version-2025-q2/norito-streaming-roadmap.{ar,es,fr,he,ja,pt,ru,ur}.md`).
   - [x] Portal versioned Norito overview + getting-started stubs translated (`docs/portal/versioned_docs/version-2025-q2/norito/{overview,getting-started}.{ar,es,fr,he,ja,pt,ru,ur}.md`).
   - [x] Portal versioned intro + devportal try-it stubs translated (`docs/portal/versioned_docs/version-2025-q2/{intro,devportal/try-it}.{ar,es,fr,he,ja,pt,ru,ur}.md`).
   - [x] Portal root README translated for Hebrew + Japanese (`docs/portal/README.he.md`, `docs/portal/README.ja.md`).
   - [x] Portal i18n mirrors synced for all completed translations across reference/intro/norito/devportal/quickstart sets (`docs/portal/i18n/*/docusaurus-plugin-content-docs/**` sources updated from completed docs).
   - [x] Portal SDK recipe JavaScript ledger flow translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/recipes/javascript-ledger-flow.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/javascript-ledger-flow.md`).
   - [x] Portal SDK recipe Python ledger flow translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/recipes/python-ledger-flow.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/python-ledger-flow.md`).
   - [x] Portal SDK recipe Swift ledger flow translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/recipes/swift-ledger-flow.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/swift-ledger-flow.md`).
   - [x] Portal SDK recipe Rust ledger flow translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/recipes/rust-ledger-flow.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/rust-ledger-flow.md`).
   - [x] Portal SDK recipe Java ledger flow translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/recipes/java-ledger-flow.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/java-ledger-flow.md`).
   - [x] Portal SDK recipe JavaScript Connect preview translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/recipes/javascript-connect-preview.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/javascript-connect-preview.md`).
   - [x] Portal SNS training collateral translated across locales and synced to i18n mirrors (`docs/portal/docs/sns/training-collateral.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sns/training-collateral.md`).
   - [x] Portal SNS regulatory memo EU DSA 2026-03 translated across locales and synced to i18n mirrors (`docs/portal/docs/sns/regulatory/eu-dsa-2026-03.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sns/regulatory/eu-dsa-2026-03.md`).
   - [x] Portal SNS regulatory memo EU DSA 2027-01 translated across locales and synced to i18n mirrors (`docs/portal/docs/sns/regulatory/eu-dsa-2027-01.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sns/regulatory/eu-dsa-2027-01.md`).
   - [x] Docs/source Org stubs for LTS selection + release procedure replaced by source-mirrored content with updated translation metadata (`docs/source/lts_selection.*.org`, `docs/source/release_procedure.*.org`).
   - [x] Portal SDK Rust quickstart translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/rust.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/rust.md`).
   - [x] Portal SDK Python quickstart translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/python.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/python.md`).
   - [x] Portal AI Moderation Runner specification translated across locales and synced to i18n mirrors (`docs/portal/docs/ministry/ai-moderation-runner.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/ministry/ai-moderation-runner.md`).
   - [x] Portal SDK Android telemetry redaction translated across locales and synced to i18n mirrors (`docs/portal/docs/sdks/android-telemetry-redaction.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/android-telemetry-redaction.md`).
   - [x] Portal SDK JavaScript quickstart stubs replaced across locales and synced to i18n mirrors (mirrors the canonical source content; `docs/portal/docs/sdks/javascript.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/javascript.md`).
   - [x] Portal SDK JavaScript governance & ISO examples stubs replaced across locales and synced to i18n mirrors (mirrors the canonical source content; `docs/portal/docs/sdks/javascript-governance-iso.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/javascript-governance-iso.md`).
   - [x] Portal SDK recipe JavaScript governance & ISO stubs replaced across locales and synced to i18n mirrors (mirrors the canonical source content; `docs/portal/docs/sdks/recipes/javascript-governance-iso.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sdks/recipes/javascript-governance-iso.md`).
   - [x] Portal finance settlement ISO mapping stubs replaced across locales and synced to i18n mirrors (mirrors the canonical source content; `docs/portal/docs/finance/settlement-iso-mapping.*`, `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/finance/settlement-iso-mapping.md`).
   - [x] SoraFS gateway Spanish stubs translated (`docs/source/sorafs_gateway_dns_design_agenda.es.md`, `docs/source/sorafs_gateway_operator_playbook.es.md`, `docs/source/sorafs_gateway_profile.es.md`).
   - [x] SoraFS Spanish stubs translated for chunker registry/profile authoring, portal publish plan, node plan/storage, and storage capacity marketplace (`docs/source/sorafs/chunker_registry.es.md`, `docs/source/sorafs/chunker_profile_authoring.es.md`, `docs/source/sorafs/portal_publish_plan.es.md`, `docs/source/sorafs/sorafs_node_plan.es.md`, `docs/source/sorafs/sorafs_node_storage.es.md`, `docs/source/sorafs/storage_capacity_marketplace.es.md`).
   - [x] Remaining portal/reference/versioned stubs now mirror canonical sources (all `translation_last_reviewed: null` stubs cleared in versioned portal docs).
   - [x] Portal i18n stub copies under `docs/portal/i18n/*/docusaurus-plugin-content-docs` now mirror their source content with `translation_last_reviewed` set (stubs removed).
   - [x] Repo root + `docs/source` stub translations now mirror canonical sources with `translation_last_reviewed` set (stubs cleared).
 - [x] Complete translations for core policy/docs (CODE_OF_CONDUCT, PATENTS, README, AGENTS, status/roadmap, configuration/runbooks).
 - [x] Prioritize portal docs: `docs/portal/i18n/*/docusaurus-plugin-content-docs` (especially SoraFS/SoraNet) and mark completion per locale (stubs mirrored and marked complete across locales).
 - [x] Portal SoraFS quickstart translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS capacity simulation runbook translations completed across all locales in `docs/portal/docs`, `docs/portal/i18n`, and `docs/source/sorafs/runbooks`.
 - [x] Portal SoraFS runbooks index translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS manifest pipeline translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS orchestrator ops runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS orchestrator tuning guide translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS multi-source rollout runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS orchestrator configuration guide translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.

25. **SUBSCRIPTION-TRIGGER-API — Trigger-based subscriptions for arrears + fixed billing** (Core/DataModel/Contracts/Torii, Line: Shared, Owner: App Platform WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - Design: Plans are `AssetDefinition` entries with `billing`/`pricing` metadata; subscriptions are `Nft`s owned by the subscriber; billing uses time triggers with deterministic UTC calendar-month scheduling and `bill_for` set to `previous_period` (arrears) or `next_period` (advance); usage plans accumulate counters in subscription metadata; fixed plans charge a constant amount; optional invoice metadata is recorded on the subscription NFT for the latest attempt (apps can mint invoice NFTs for full history).
 - [x] Specify the canonical metadata schema for plan/subscription/invoice objects (Norito JSON), including usage accumulator keys and deterministic UTC calendar helpers; document in `docs/source/subscriptions_api.md`, update `docs/source/data_model_and_isi_spec.md`, and add typed schema in `crates/iroha_data_model/src/subscription.rs`.
 - [x] Implement deterministic calendar-period helper (UTC month boundaries, anchor day/time, arrears/advance) shared by contracts and tests.【crates/iroha_primitives/src/calendar.rs:1】
 - [x] Add an IVM billing syscall/contract wrapper that computes the bill, transfers assets, updates subscription state, writes the latest `subscription_invoice`, and reschedules or retries on failure while enforcing `max_failures`/`grace_ms`.
 - [x] Add a usage recorder syscall path (by-call trigger or contract) to increment subscription usage counters; gate with `CanExecuteTrigger` permissions.
- [x] Add Torii endpoints for plans/subscriptions (create/list/get/pause/resume/cancel/charge-now) and expose subscription listing queries.
 - [x] Add CLI/SDK helpers for plan publishing, subscribe/cancel, usage reporting, and subscription management.
 - [x] Tests: unit coverage for calendar math + subscription billing/usage syscalls.
 - [x] Tests: integration coverage with 4 peers for arrears usage billing, fixed-price advance billing, and retry/grace failure paths (`integration_tests/tests/subscriptions.rs`).
 - [x] Telemetry/events: counters for billing attempts/outcomes and TriggerCompleted filters for subscription billing; update docs/runbooks.

26. **IVM-QUERY-SYSCALLS — Norito query syscall with gas metering** (Core/IVM/Query, Line: Shared, Owner: VM WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - Design: `SMARTCONTRACT_EXECUTE_QUERY` accepts `&NoritoBytes(QueryRequest)` and returns `&NoritoBytes(QueryResponse)` executed under the contract authority; iterable queries run in ephemeral cursor mode so results do not outlive the VM run.
- [x] Implement CoreHost query syscall, deterministic gas model (base + per-item + per-byte, sort multiplier), and unit coverage; update syscall spec/docs for Norito query requests.
- [x] Abort query execution early when the remaining gas budget is exhausted (meter during materialization) to avoid long scans.
- [x] Add `pipeline.query_max_fetch_size` for IVM query validation and document the knob alongside syscall semantics.
- [x] Reject responses that exceed per-byte gas budgets before encoding when exact Norito sizing is available, and add targeted coverage.
 - [x] Add a Kotodama `execute_query` builtin that bridges SMARTCONTRACT_EXECUTE_QUERY for Norito-encoded queries.

25. **NEXUS-STORAGE-BUDGETS — Enforce global storage budgets + DA offload** (Storage/Core, Line: Iroha 3, Owner: Nexus Core WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Add Nexus storage budget configuration with per-component weight splits, clamp Kura/WSV cold tier/SoraFS/SoraNet spool caps, and wire defaults into the Nexus profile config.
- [x] Account for merge-ledger + sidecar + roster journal bytes (including queued blocks + retired lanes) in Kura budget enforcement.
- [x] Surface operator-facing warnings/telemetry when caps trigger.
- [x] Implement live hot-tier eviction using measured WSV memory usage (not snapshot size estimates).
- [x] Wire dedicated SoraVPN spool caps for Iroha 3 (budgets now map to `streaming.soravpn`).
- [x] Wire DA-backed cold/WSV retrieval for Iroha 3 while keeping Iroha 2 full-replica behavior.
 - [x] Add operator guidance + metrics for sizing `nexus.storage` budgets and monitoring cap pressure.
 - [x] Portal SoraFS node operations runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS node storage design translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS node implementation plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS provider admission policy translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS multi-source provider advert translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS reserve ledger digest translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS docs stubs cleared across remaining locales in `docs/portal/docs/sorafs/**` and `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/sorafs/**`.
 - [x] Portal SoraFS migration ledger translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS migration roadmap translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS pin registry implementation plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS pin registry operations runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS pin registry validation plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.

25. **RUST-1-92-ROLLOUT — Adopt Rust 1.92 lints and APIs** (Tooling/Core, Line: Shared, Owner: Release Eng, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Run `ci/check_rust_1_92_lints.sh` and fix any new deny-by-default findings (never-type fallback, macro-export arguments).
 - [x] Sweep std `RwLock` write-then-read handoffs and use `RwLockWriteGuard::downgrade` where it preserves semantics.
 - [x] Audit const helpers for rotate usage now that `slice::rotate_left`/`rotate_right` are const-stable.
 - [x] Track additional Rust 1.92 API adoption in `docs/source/rust_1_92_adoption.md`.
 - [x] Portal SoraFS portal publish plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS signing ceremony translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS storage capacity marketplace translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS provider advert rollout translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS priority snapshot (2025-03) translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Taikai monitoring dashboards translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS SF-6 security review translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS SF1 determinism dry-run translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS Orchestrator GA parity report translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS SF-2c capacity accrual soak report translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal AI moderation calibration report (2026-02) translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS capacity marketplace validation report translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal GAR jurisdictional review (SNNet-9) translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet PQ primitives overview translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet constant-rate profiles translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet transport overview translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet testnet rollout (SNNet-10) translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet PQ ratchet runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet PQ rollout plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet puzzle service operations guide translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraNet docs stubs cleared across all locales in `docs/portal/docs/soranet/**` and synced to `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/soranet/**`.
 - [x] Portal SoraNet privacy metrics pipeline translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal intro page translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito Streaming roadmap translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal reference index translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito codec reference translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito overview translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito getting started translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito quickstart translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito streaming translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito ledger walkthrough translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito examples index translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito Hajimari entrypoint example translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito call-transfer-asset example translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito register-and-mint example translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito transfer-asset example translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito NFT flow example translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito Try-It console translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Norito examples stubs cleared across all locales in `docs/portal/docs/norito/examples/**` and synced to `docs/portal/i18n/*/docusaurus-plugin-content-docs/current/norito/examples/**`.
 - [x] Portal Sora Nexus overview translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus operations runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus settlement FAQ translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus default lane quickstart translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus elastic lane provisioning translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus technical specification translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus fee model updates translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus telemetry remediation plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal confidential gas calibration ledger translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus routed-trace audit report (2026 Q1) translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus data-space operator onboarding translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus bootstrap & observability plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus ledger refactor plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus transition notes translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus lane model translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Sora Nexus confidential assets & ZK transfers translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal account address compliance reference translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal publishing checklist translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal address safety & accessibility reference translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS address checksum incident runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS registry schema translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS local-to-global address toolkit translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS metrics & onboarding kit translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Connect session preview runbook translations completed across all locales in `docs/runbooks`.
 - [x] Connect chaos & fault rehearsal plan translations completed across all locales in `docs/runbooks`.
 - [x] SNS training templates (slides, workbook, evaluation, invite email) translations completed across all locales in `docs/examples`.
 - [x] Docs preview templates (invite, request, feedback digest/form) translations completed across all locales in `docs/examples`.
 - [x] DA manifest review template translations completed across all locales in `docs/examples`.
 - [x] Taikai anchor lineage packet translations completed across all locales in `docs/examples`.
 - [x] Pen-test remediation report template translations completed across all locales in `docs/examples`.
 - [x] SoraFS release notes translations completed across all locales in `docs/examples`.
 - [x] SoraFS CI cookbook translations completed across all locales in `docs/examples`.
 - [x] Android device lab reservation request template translations completed across all locales in `docs/examples`.
 - [x] Android partner SLA discovery notes translations completed across all locales in `docs/examples`.
 - [x] SNS arbitration transparency report template translations completed across all locales in `docs/examples`.
 - [x] Nexus steering priority snapshot (2025-03 wave) translations completed across all locales in `docs/examples`.
 - [x] SoraNet GAR intake template translations completed across all locales in `docs/examples`.
 - [x] SoraNet testnet operator kit templates (readme, checklist, telemetry, incident playbook, verification report) translations completed across all locales in `docs/examples`.
 - [x] SoraNet testnet stage-gate report template and sample report translations completed across all locales in `docs/examples`.
 - [x] SoraFS CI sample fixtures README and SoraFS capacity simulation toolkit README translations completed across all locales in `docs/examples`.
 - [x] SoraGlobal gateway billing reconciliation report template translations completed across all locales in `docs/examples`.
 - [x] SoraNet relay incentive parliament packet translations (README, economic analysis, rollback plan) completed across all locales in `docs/examples`.
 - [x] Finance repo custodian ack and governance packet templates translations completed across all locales in `docs/examples`.
 - [x] Ministry volunteer brief example moderation stub cleared in `docs/examples`.
 - [x] Nexus overview translations completed across all locales in `docs/source`.
 - [x] Nexus transition notes translations completed across all locales in `docs/source`.
 - [x] Nexus lane model translations completed across all locales in `docs/source`.
 - [x] Nexus technical design spec translations completed across all locales in `docs/source`.
 - [x] Nexus lane compliance policy engine translations completed across all locales in `docs/source`.
 - [x] Nexus fee model translations completed across all locales in `docs/source`.
 - [x] Nexus operations runbook translations completed across all locales in `docs/source`.
 - [x] Nexus public lane staking translations completed across all locales in `docs/source`.
 - [x] Nexus cross-lane commitments translations completed across all locales in `docs/source`.
 - [x] Nexus telemetry remediation plan translations completed across all locales in `docs/source`.
 - [x] Nexus routed-trace audit report (2026 Q1) translations completed across all locales in `docs/source`.
 - [x] Nexus ledger refactor plan translations completed across all locales in `docs/source`.
 - [x] Nexus SDK quickstarts translations completed across all locales in `docs/source`.
 - [x] Nexus settlement FAQ translations completed across all locales in `docs/source`.
 - [x] Nexus elastic lane provisioning translations completed across all locales in `docs/source`.
 - [x] Nexus privacy commitments translations completed across all locales in `docs/source`.
 - [x] Testing and troubleshooting guide translations completed across all locales in `docs/source`.
 - [x] Account address compliance status translations completed across all locales in `docs/source`.
 - [x] Release artifact selection translations completed across all locales in `docs/source`.
 - [x] Docker builder image translations completed across all locales in `docs/source`.
 - [x] Bridge proofs translations completed across all locales in `docs/source`.
 - [x] Bridge finality proofs translations completed across all locales in `docs/source`.
 - [x] Runtime upgrades translations completed across all locales in `docs/source`.
 - [x] Sumeragi NPoS task breakdown translations completed across all locales in `docs/source`.
 - [x] Sumeragi evidence audit API translations completed across all locales in `docs/source`.
 - [x] Sumeragi randomness evidence runbook translations completed across all locales in `docs/source`.
 - [x] Sumeragi aggregators translations completed across all locales in `docs/source`.
 - [x] Sumeragi pacemaker translations completed across all locales in `docs/source`.
 - [x] Portal Torii app API parity audit translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal GAR operator onboarding brief translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS dispute & revocation runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS capacity reconciliation runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS gateway & DNS kickoff runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Taikai anchor observability runbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS operations playbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS observability plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS node-client protocol translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS staging manifest playbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS chunker registry rollout checklist translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS chunker registry translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS chunker registry charter translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS chunker conformance translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS chunker profile authoring translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS deal engine translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS developer index translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS developer CI recipes translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS developer CLI cookbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS deployment notes translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS deployment guide translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS KPI dashboard translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS address display guidelines translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS governance playbook translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS suffix catalog translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS registrar API translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS payment settlement plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SNS bulk onboarding toolkit translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal DA replication policy translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal DA ingest plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal DA commitments plan translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal DA threat model translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal Nexus SDK quickstarts translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal governance API endpoint translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS release process translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS direct-mode fallback pack translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS SDK index translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] Portal SoraFS Rust SDK snippets translations completed across all locales in `docs/portal/docs` and `docs/portal/i18n`.
 - [x] TODO: Continue SoraFS/SoraNet portal translations beyond the completed SoraFS docs set (quickstart, runbooks, pipeline/orchestrator guides, node plan/storage, node operations, provider admission policy, multi-source provider advert, provider advert rollout, priority snapshot 2025-03, taikai monitoring dashboards, sf6 security review, sf1 determinism dry-run, orchestrator GA parity report, sf2c capacity soak report, ai moderation calibration report 2026-02, capacity marketplace validation report, GAR jurisdictional review, GAR operator onboarding, pq primitives, pq ratchet runbook, pq rollout plan, puzzle service operations guide, privacy metrics pipeline, constant-rate profiles, transport overview, testnet rollout, reserve ledger digest, migration ledger, migration roadmap, pin registry plan, pin registry ops, pin registry validation plan, portal publish plan, signing ceremony, storage capacity marketplace, chunker docs, developer docs, SDK docs), then fill remaining portal stubs per locale (mirrored canonical sources for first release coverage).
 - [x] Ensure Akkadian translations are semantic and written in cuneiform (no transliteration).
 - [x] Extend CLI i18n coverage (remaining messages and clap help output).
 - [x] Fill governance schedule placeholders in scripts/templates that currently say TBD.
 - [x] Add checks to prevent stub-only translations from shipping (CI guard + `scripts/sync_docs_i18n.py --dry-run` gate).

35. **SEC-TELEMETRY-REDACTION — Enforce redaction + tamper-evident telemetry** (Observability/Security, Line: Shared, Owner: Observability WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define sensitive-field taxonomy and expected redaction behavior (keywords + explicit annotations).
 - [x] Make redaction mandatory for operator/release telemetry profiles (config default + build guard).
 - [x] Add CI/static checks to prevent new unredacted sensitive fields and update allowlist policy.
 - [x] Implement tamper-evident telemetry export (hash chaining + optional signing key rotation).
 - [x] Emit redaction/audit metrics for skipped or truncated fields.
 - [x] Add unit/integration tests + update `docs/source/telemetry.md` and threat model notes.

36. **SEC-TIME-HARDENING — Enforce NTS/time bounds** (Runtime/Ops, Line: Shared, Owner: Runtime WG + Ops, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Add config for min samples, max offset, max confidence, and enforcement mode (warn/reject).
 - [x] Extend network time status with sample counts and health flags.
 - [x] Gate time-sensitive validations on NTS health (offline receipts, attestations, governance windows).
 - [x] Emit telemetry/status for drift, unhealthy states, and fallback usage.
 - [x] Add alerting guidance + runbook updates for NTS drift/unsynced states.
 - [x] Add tests for insufficient samples, out-of-bounds offsets, and enforcement behavior.

37. **SEC-UPGRADE-PROVENANCE — Require SBOM/SLSA attestations for runtime upgrades** (Core/Governance/Security, Line: Shared, Owner: Security WG + Governance WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Extend runtime-upgrade manifests with provenance payloads (SBOM digests, SLSA attestation bytes, signer metadata).
 - [x] Add config-driven enforcement for provenance policy and trusted signers/thresholds.
 - [x] Enforce provenance in runtime-upgrade ISIs; reject missing or invalid attestations.
 - [x] Surface telemetry + error codes for provenance failures.
 - [x] Wire Torii/governance APIs to accept provenance and emit rejection telemetry.
 - [x] Add tests + update `docs/source/runtime_upgrades.md` and threat model tracking.

38. **SEC-TORII-OPERATOR-AUTH — WebAuthn/mTLS hardening for operator endpoints** (Torii/Security, Line: Shared, Owner: Torii WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define operator auth policy in `iroha_config` (WebAuthn required, mTLS gating, token fallback rules).
 - [x] Implement WebAuthn credential enrollment + storage (WSV or config-backed).
 - [x] Add challenge/verify flow and gate operator endpoints behind auth policy.
 - [x] Add rate limits and audit telemetry for auth failures/lockouts.
 - [x] Add tests for enrollment, rollover, and fallback paths; update OpenAPI + runbooks.

39. **SEC-ATTACHMENT-SANITISATION — Safe ingest pipeline for Torii attachments** (Torii/Runtime, Line: Shared, Owner: Runtime WG + Torii WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Add config for allowed MIME types, max expanded size, and archive depth.
 - [x] Implement magic-byte sniffing + strict allowlist enforcement before persistence.
 - [x] Enforce deterministic decompression/expansion limits and reject unknown formats.
 - [x] Sandbox risky parsers with strict CPU/memory limits (bounded worker).
 - [x] Add subprocess sanitizer mode (`torii.attachments_sanitizer_mode`), OS-level rlimits, and re‑sanitize exports on download.
 - [x] Store provenance metadata (sniffed type, hashes, sanitizer verdict) alongside attachment records.
 - [x] Add telemetry counters + malicious fixture/subprocess tests; update `docs/source/security_hardening_requirements.md`.

40. **SEC-MEMBERSHIP-MISMATCH — Detect and alert on roster drift** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Propagate membership view-hash in consensus gossip/status payloads.
 - [x] Compare peer hash vs local roster and record mismatch counters + active gauges.
 - [x] Add config for alert thresholds and optional fail-closed behavior.
 - [x] Clear mismatch on alignment and expose last-mismatch context via `/v1/sumeragi/status`.
 - [x] Add tests for mismatch detection + update Sumeragi runbook with remediation steps.

26. **JDG-SIGNATURE-SCHEMES — Expand jurisdiction signature support** (Core/Governance, Line: Shared, Owner: Governance WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Define policy surface for additional JDG signature schemes and encode it in config/data model.
 - [x] Implement verification for new schemes and add alignment tests.
 - [x] Update governance/jurisdiction docs with scheme support and limits.

29. **KOTODAMA-TERNARY-LOWERING — Implement ternary IR lowering** (IVM, Line: Shared, Owner: IVM WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement IR lowering for ternary expressions (beyond parse/typecheck).
 - [x] Add compiler regression tests and update Kotodama docs/examples.

30. **IVM-CONFORMANCE-SUITE — Fill TBD conformance tests** (IVM, Line: Shared, Owner: IVM WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement the conformance test files listed as TBD in `crates/ivm/docs/conformance.md` (decoder roundtrip, op semantics, gas golden, crypto vectors, zk trace).
 - [x] Wire CI coverage for the new test suite and update docs.

31. **MERGE-COMMITTEE-SIGNATURES — Wire merge-committee signatures** (Consensus/Core, Line: Shared, Owner: Consensus WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Define merge quorum pipeline requirements and signature payloads.
 - [x] Implement merge-committee signature handling in state/consensus.
 - [x] Add tests and update consensus/governance docs.

32. **CODE-HEALTH-REFACTOR — Remove remaining refactor TODOs** (Core/Tooling, Line: Shared, Owner: Core Protocol WG, Priority: Low, Status: 🈴 Completed, target TBD)
 - [x] Split large helpers flagged in `irohad/src/main.rs` and `iroha/src/client.rs`.
 - [x] Decompose Sumeragi loops (`main_loop.rs`, `mode.rs`, `validation.rs`, `pending_block.rs`, `qc.rs`, `reschedule.rs`, `votes.rs`) and `block_sync.rs` validation flow.
 - [x] Refactor `account_admission.rs`, `settlement.rs` error payloads, and multisig ownership/borrowing to remove lint suppressions.【crates/iroha_core/src/smartcontracts/isi/account_admission.rs:1】【crates/iroha_core/src/smartcontracts/isi/settlement.rs:1】【crates/iroha_core/src/smartcontracts/isi/multisig.rs:1】
 - [x] Consolidate boolean policy knobs in `state.rs` into enums/bitflags and split large validation helpers.

34. **EVENTS-PIPELINE-HANG — Unblock mod.rs integration event tests** (QA/Consensus, Line: Shared, Owner: QA WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Reproduce `cargo test -p integration_tests --test mod` hangs and confirm DA-enabled single-peer networks stall on post-genesis commits.
 - [x] Enforce a 4-peer minimum in the integration-test harness and update direct peer-start paths to avoid single-peer stalls.
 - [x] Allow pending-block quorum reschedules to drop/requeue after a retry even with partial precommit votes; add unit test coverage.
 - [x] Re-run the full `cargo test -p integration_tests --test mod` suite to confirm stability.

41. **NEXUS-STORAGE-DA-RETENTION — DA-backed storage eviction under disk caps** (Core/DA/WSV, Line: Iroha 3, Owner: Nexus Core WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define deterministic eviction order across Kura block bodies, tiered-state cold snapshots, and storage spools once `nexus.storage.max_disk_usage_bytes` is reached (no hardware-dependent heuristics).
 - [x] Implement DA-backed rehydration for evicted blocks/WSV cold shards with cache hit/miss + churn telemetry.
 - [x] Add 4-peer DA integration tests covering eviction + rehydrate with strict disk caps.

43. **IZANAMI-LONGRUN-STABILITY — Validate long-run multi-peer execution-root QC flow** (Consensus/Tooling, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈺 In Progress, target TBD)
- [ ] Run Izanami stable profile on 4+ peers for 2000+ blocks (no faults) and confirm commit QCs aggregate with matching execution roots (2026-01-23 run: `RUST_LOG=izanami::summary=info,izanami::workload=warn IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` failed with no block height progress for 300s at min height 98; workload submissions hit `transaction.status_timeout_ms=600s`, peer logs show repeated `missing_qc` view changes and "no proposal observed" errors; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_eMeaCB`; no `execution roots`/QC split or deadlock warnings in peer logs. 2026-01-24 run: same command failed with no block height progress for 300s at min height 12; plan submissions timed out (`transaction.status_timeout_ms=600s`) plus `transaction queued for too long`, duplicate metric registration warnings persisted; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_XT3mKF`. 2026-01-24 run: `RUST_LOG=iroha_p2p=debug,iroha_core=debug IROHA_TEST_NETWORK_KEEP_DIRS=1 IROHA_TEST_NETWORK_PERMIT_DIR=$(mktemp -d) cargo run -p izanami --release --locked -- --allow-net --nexus --peers 4 --faulty 0 --duration 3600s --target-blocks 2000 --progress-interval 10s --progress-timeout 300s --tps 5 --max-inflight 8 --workload-profile stable` failed with no block height progress for 300s at min height 36; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_KrCc5b`. 2026-01-24 run: same command ran full 3600s but stopped before target blocks (concise_cardinal committed height 307); logs show block sync frames + RBC chunk ingress with missing BlockCreated requests while awaiting INIT and later INIT rebroadcasts; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_kDdEkr`. 2026-01-24 run: same command (tool timed out after ~65m; no Izanami summary) shows committed height 319 across peers and 1888 txs in lane_000_core; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_IElRk7`. 2026-01-25 ramp runs (900s, info logs) at tps 2/3/5 reached heights 94/86/79 with 505/483/467 txs in lane_000_core; view-change/missing_qc low and consensus backlog cleared, but throughput stayed ~0.5 tps; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_ydcz68`, `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_NGrVys`, `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_RvgeXq`. 2026-01-25 run: tps 5 with `--max-inflight 64` (900s, info logs) timed out in harness and reached height 28-33 with 739 txs in lane_000_core; view-change/missing_qc/no-proposal/stake-quorum timeouts resurfaced; logs in `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_lkyzd2`).
- [ ] 2026-02-16 isolated-binary long-run rerun (`/tmp/iroha-izanami-finish/release/izanami`, 4 peers, tps 5, duration 900s, target 2000) stayed stable but finished at height 464 on all peers (net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_GMgnhL`, log `/tmp/izanami_longrun_20260216_run12.log`); no execution-root mismatch/split logs were found, but the 2000-block gate remains unmet.
- [ ] 2026-02-25 shared-host stabilization follow-up: reran fixed-5-TPS probes with explicit progress telemetry. Control (`RUST_LOG=izanami::summary=info,izanami::workload=warn,izanami::progress=info ... --duration 300s --target-blocks 180`) hit `quorum/strict=180/180` in `281.188953375s` (`successes=783 failures=1`; logs `/tmp/izanami_probe_control_progress_20260225.log`, net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_XTwiJp`). Profile-active (`... --duration 3600s --target-blocks 180`) hit `185/185` in `300.866788916s` (`successes=807 failures=3`; logs `/tmp/izanami_probe_profile_progress_20260225.log`, net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_52iuYT`) and summary-only spot probe remained reproducible at `successes=811 failures=0` (`/tmp/izanami_probe_profile_20260225.log`, net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_367Pqb`). Extended profile-active run (`... --duration 3600s --target-blocks 600`) crossed the prior `~513` stall zone with transient slowdown then recovered, reaching `600/600` in `1082.708463625s` (`successes=2659 failures=7`, `izanami_ingress_failover_total=2`; logs `/tmp/izanami_probe_profile_600_20260225.log`, net `/var/folders/n2/xxntlr312qbfdnp0j1xp52hw0000gn/T/irohad_test_network_TIKt7q`). Liveness regression is reduced (no 300s no-progress timeout through height 600), but pace remains `~0.55-0.64 blocks/s`; 3600/3600 acceptance remains open.
- [x] Hydrate INIT-created RBC sessions from pending payloads so missing-chunk stalls clear after view changes; unit coverage added.
- [x] Izanami: raise pipeline parallelism, enable signature batching, and increase validation worker throughput defaults for local validation/execution; add config coverage.
- [x] Reduce state-view lock contention in hot consensus/telemetry paths by switching cheap reads to narrow state accessors (`committed_height`, `latest_block_hash_fast`, `prev_block_hash_fast`, `world_view`, `commit_topology_snapshot`, `has_committed_transaction`, `committed_transaction_height`), reusing one snapshot inside block-sync QC filtering, adding world-only QC derivation (`BlockSynchronizer::block_sync_qc_for_world`), moving gossiper route/parameter snapshots plus NPoS timeout/block-time lookups onto `nexus_snapshot`/world-snapshot helpers, reusing a shared `StateView` per inbound gossip batch via `Queue::push_with_gossip_payload_in_view`, switching outbound gossip batching to `Queue::gossip_batch_with_state` + `commit_topology_snapshot`, switching Sumeragi QC tally/collector/heartbeat reads to `world_view` + `load_npos_collector_config_from_world` + `committed_height`, and replacing block-sync share/QC and Torii pipeline-status queue/tx-index reads with `world_view`/`contains_pending_hash` + narrow accessors so hot loops avoid full `state.view()` acquisition; keep legacy fast-timeout call signatures for compatibility and cover new helpers with unit tests.
- [x] Contention follow-up: reuse a shared `StateView` for commit-failure requeue admission (`Queue::push_in_view`), move remaining Sumeragi hot-loop tip/epoch/mode reads from `state.view()` to `committed_height` + `latest_block_hash_fast` + `world_view` snapshots (including `latest_committed_qc`, pending/RBC scheduling, idle/backpressure view-change gates, and worker-loop config refresh), switch uncertified roster fallback paths to `derive_active_topology_from_views`, and replace Torii account-resolver/permission/AXT/ledger-header state reads with narrow accessors (`world_view`, `axt_policy_snapshot`, `block_by_height`) with added queue/state unit coverage.
- [x] Contention follow-up III: remove remaining proposal/vote hot-path `state.view()` reacquisition by reusing shared proposal requeue snapshots (`Queue::push_in_view`) for deferred/overflow/failure reinsertion, switching proposal trigger/pacemaker tip reads to narrow state accessors (`world_view`, `committed_height`, `latest_block_hash_fast`, `block_by_height`), and replacing vote roster/block-time fallbacks with world/topology snapshots plus `State::prev_commit_topology_snapshot()` (unit coverage added).
	- [x] Contention follow-up IV: remove additional QC/commit hot-path `state.view()` usage by switching commit/pending tip checks to `committed_height` + `latest_block_hash_fast`, adding world-based stake quorum validation (`stake_quorum_reached_for_world`), deriving active rosters from world/topology snapshots (`derive_active_topology_for_mode_from_world`) for QC missing-block fallback + aggregation, and replacing commit timing/VRF existence probes with `world_view` snapshots; unit coverage added for the new world-derivation helper and stake-quorum path parity.
	- [x] Contention follow-up V: add `State::time_triggers_due_for_block_fast()` and switch empty-block handling in `handle_block_created` + QC empty-block filtering to this narrow accessor so time-trigger readiness checks use `committed_height` + Kura + `world_view` snapshots instead of full `state.view()`; unit coverage now asserts parity with `StateView::time_triggers_due_for_block`.
	- [x] Contention follow-up VI: add world-only NPoS parameter helpers (`load_npos_epoch_params_from_world`, `resolve_npos_election_params_from_world`), switch `refresh_npos_seed` collector/epoch/seed reads to `world_view` + world helpers, and use a world/topology snapshot local-validator-index path (`derive_local_validator_index_for_mode_from_world` via `local_validator_index_current`) in commit refresh so this branch no longer acquires `state.view()` for signer lookup.
	- [x] Contention follow-up VII: remove remaining mode/VRF/election `state.view()` acquisitions by switching mode-flip epoch/timing/status rebuilds to `world_view` + `committed_height` + world helpers, adding `compute_consensus_handshake_caps_from_world(...)` so mode-flip capability refresh no longer requires a full `StateView`, moving VRF commit/reveal local-signer lookup onto `local_validator_index_current()`, and extracting epoch stake-roster derivation into a world/topology helper (`epoch_validator_peer_ids_from_world`) exposed through `State::epoch_validator_peer_ids_fast(...)` so validator-election params + roster + candidate profile reads all run on narrow world/topology snapshots. Added world/view parity assertions for handshake caps and epoch roster lookup in tests.
	- [x] Contention follow-up VIII: remove penalty/evidence/proposal `state.view()` contention by adding world-only NPoS delay helpers (`resolve_npos_activation_lag_blocks_from_world`, `resolve_npos_slashing_delay_blocks_from_world`), switching penalty delay/epoch/mode/roster lookups to `world_view` + `commit_topology_snapshot`, removing per-offender `StateView` reacquisition during validator mapping, moving evidence replay horizon reads to `committed_height` + `world_view`, and reusing the existing proposal assembly snapshot for deferred requeue admission instead of reacquiring a second full `StateView`. Added world/view parity tests for the new delay helpers and reran penalties/evidence/proposal regressions.
	- [x] Contention follow-up IX: remove additional startup/status snapshot reads by switching Sumeragi actor bootstrap paths (`determine_genesis_account`, staged mode-tag capture, initial leader/timing status publish) from full `state.view()` to `world_view` + `local_validator_index_current()` + `update_effective_timing_status_from_world(...)`; move actor-init mode/topology/pacemaker/epoch-seed derivation to world/topology snapshots (`effective_consensus_mode_for_height_from_world`, `derive_active_topology_for_mode_from_world`, `resolve_npos_block_time_from_world`, `resolve_npos_timeouts_from_world`, `load_npos_collector_config_from_world`, `load_npos_epoch_params_from_world`, `prf_seed_for_height_from_world`); and narrow roster-index derivation helper `compute_roster_indices_from_state` to `committed_height` + `commit_topology_snapshot`.
	- [x] Contention follow-up X: remove requeue hot-loop `state.view()` contention by adding a consensus requeue queue-admission path (`Queue::push_requeued_with_routing`) that uses narrow committed-hash checks (`State::has_committed_transaction`) and precomputed routing decisions, then switching `requeue_block_transactions` to this path so commit/reschedule requeue loops no longer hold a full `StateView` across batch reinsertion.
	- [x] Contention follow-up XI: remove fallback requeue-route `state.view()` usage for normal runtime routers by extending `LaneRouter` with a no-state route fast path (`route_without_state`) implemented by `SingleLaneRouter` and `ConfigLaneRouter`, adding `Queue::route_for_gossip_without_state(...)`, and updating `requeue_block_transactions` to prefer cached routing then no-state routing before taking a short-lived snapshot fallback.
		- [x] Contention follow-up XII: remove proposal-path requeue `state.view()` snapshots by reusing the consensus requeue API in proposal assembly (`assemble_and_broadcast_proposal`) for deferred/overflow/chunk-cap/frame-cap reinsertion, adding proposal-local helper requeue routing (`requeue_accepted_transaction`) and threading overflow routing hints so proposal requeue loops avoid batch-wide `StateView` acquisition.
		- [x] Contention follow-up XIII: remove proposal queue-pop/commit-filter `state.view()` dependency by adding a narrow-state queue selection path (`Queue::get_transactions_for_block_with_state` + `pop_from_queue_with_state`) and switching proposal scan/filter helpers (`pull_transactions_for_proposal`, `filter_committed_transactions_for_proposal`) to `&State` + committed-hash checks; added queue hash-resync parity coverage for the new state path.
		- [x] Contention follow-up XIV: remove residual proposal requeue routing fallbacks by preserving routing hints end-to-end for deferred/overflow/trimmed transactions (`enforce_lane_teu_limits_with_consumption_and_routing`, routed deferred gas requeue, routed chunk/frame-cap trims, and routed assembly-failure replay), so proposal requeue no longer reacquires `state.view()` for route recomputation.
		- [x] Contention follow-up XV: remove residual commit-failure requeue `state.view()` fallback by adding `LaneRouter::route_with_state` + `Queue::route_for_gossip_with_state` (prefer `route_without_state`, only snapshot when router requires world data) and switching `requeue_block_transactions` to the new routing path; added queue coverage for both no-state and snapshot-required routers.
				- [x] Contention follow-up XVI: remove non-consensus full-view reads in periodic/query helpers by switching snapshot-maker change detection (`SnapshotMaker::create_snapshot` / `from_config`) to `latest_block_hash_fast` + `committed_height`, replacing snapshot-read block-hash capture with `State::committed_block_hashes_snapshot()`, switching Torii ZK proof list/count filters to `world_view` snapshots, and moving query test-height helpers (`next_height_for_state`) to `committed_height()` instead of `state.view()`.
				- [x] Contention follow-up XVII: remove additional Torii non-consensus full-view reads by switching runtime metrics/upgrades, Kaigi relay collection, confidential asset transition reads, proof/vk lookup handlers, Sumeragi status/evidence read endpoints, ZK roots/vote-tally read paths, and query cursor/gas config reads to `world_view`/`committed_height`/`commit_topology_snapshot`/`pipeline_snapshot` accessors; move consensus-evidence validation off `StateView` by using world-based mode/seed helpers (`effective_consensus_mode_for_height_from_world`, `npos_seed_for_height_from_world`) and expose `npos_seed_for_height_from_world` for cross-crate read-only callers.
				- [x] Contention follow-up XVIII: remove additional Torii non-consensus full-view reads by switching Sumeragi consensus-key listing, SoraDNS directory-latest lookup, Sumeragi telemetry VRF summary reads, VRF epoch lookup, and commit-QC lookup handlers to `world_view` snapshots, and switching `/version` to `committed_height` + `block_by_height`.
				- [x] Contention follow-up XIX: remove Torii bridge-finality full-view reads by introducing `BridgeStateReadOnly` and switching bridge finality proof/bundle builders + `/v1/bridge/finality/{height}` and `/v1/bridge/finality/bundle/{height}` handlers to narrow accessors (`chain_id_ref`, `block_by_height`, `world_view` validator PoP lookup) instead of `state.view()`.
				- [x] Contention follow-up XX: remove additional Torii App API full-view reads by adding hash-based state block accessors (`block_height_by_hash`, `block_by_hash`) for explorer block detail and switching Nexus public-lane + subscription read/precheck handlers to `world_view` snapshots.
				- [x] Contention follow-up XXI: remove additional Torii App API `state.view()` scans by switching account/asset-definition list-query handlers and account-assets/asset-holders handlers to `world_view` iterators (`accounts_iter`, `asset_definitions_iter`, `assets_in_account_iter`, `assets_by_definition_iter`) and accept string-form asset-holder `asset_id` filters (including compressed forms).
				 - [ ] Investigate and resolve any execution-root divergence or consensus stalls surfaced during the run.

## Archived

33. **TRYBUILD-REL-PATHS — Normalize trybuild manifest paths** (Tooling/Tests, Line: Shared, Owner: Tooling WG, Priority: Low, Status: 🈴 Completed, target TBD)
 - [x] Rewrite trybuild-generated `Cargo.toml` path entries in `norito_derive` UI tests to be relative so target-codex artifacts avoid developer-specific absolute paths; status.md updated.

15. **EMPTY-BLOCK-HEARTBEAT — Restore empty-block rejection with heartbeat tx** (Consensus/Core, Line: Shared, Owner: Consensus WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement a lightweight heartbeat transaction or alternative liveness mechanism.
 - [x] Reinstate empty-block rejection and add regressions for liveness.

16. **TORII-OPENAPI-FULL — Generate complete OpenAPI spec** (Torii/Docs, Line: Shared, Owner: Docs WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Replace the stub spec with full endpoint coverage and schema sections.
 - [x] Add tests validating expected paths/schemas; update docs.

22. **DEP-STUBS-REMOVAL — Remove stubbed dependency patches** (Build/Tooling, Line: Shared, Owner: Tooling WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Replace `rust_decimal_stub` and `vergen_git2_stub` patches with real crates or full local implementations.
 - [x] Validate builds/tests and update CI/tooling docs as needed.

5. **NORITO-AUDIT-FIXES — Lock v1 layout, header framing, and canonical JSON** (Serialization/Norito, Line: Shared, Owner: Data Model WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] V1 layout policy: set `V1_LAYOUT_FLAGS`/`default_encode_flags()` to `0x00`, require `VERSION_MINOR` match, and accept explicit header flags within the supported mask (no heuristic flag guessing); document the v1 table in `norito.md`.
 - [x] Header-only decode: remove heuristic flag guessing and headerless block decoding; require framed `SignedBlockWire` and add negative regressions.
 - [x] Feature defaults: keep layout-affecting feature toggles from changing on-wire v1 semantics.
 - [x] Packed-struct derive semantics: align self-delimiting classification and add coverage for packed struct headers/sizes.
 - [x] Streaming length caps + guard precedence: enforce `max_archive_len` in streaming readers, ensure header flags override any `DecodeFlagsGuard`, and add regressions.
 - [x] Canonical JSON floats + strict-safe policy: non-finite -> `null`, stable float formatting, `guarded_try_deserialize` honors `strict-safe` with tests.
 - [x] NSC streaming scope + docs: clarify `norito::streaming` as data-only and update portal/spec docs; translations flagged for update.

4. **DOCS-ALIGNMENT-GUARDRAILS — Keep documentation synced with code and portal paths** (Docs/DevRel, Line: Shared, Owner: Docs WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Confirmed Torii address guidance (`torii.address` in config) across translations and portal docs so env shims stay retired.
 - [x] Verified SoraFS storage docs align with config + `SORAFS_STORAGE_*` dev/test notes across portal/localized copies.
 - [x] Replaced lingering `serde_json` usage in docs with Norito JSON helpers.
 - [x] Extended `scripts/check_md_links.py` for Docusaurus heading IDs, relative doc-id resolution, README/index directory links, and docs-bundle allowlisting; added unit coverage.
 - [x] Re-ran link scans across docs/portal bundles (including artifacts previews) to confirm local links resolve.

2. **MOCHI-BINARY-COMPAT-CHECKS — Guard profile/binary mismatches and bootstrap builds** (Tooling/Supervisor, Line: Shared, Owner: Tooling WG, Priority: Medium, Status: 🈴 Completed)
 - [x] Binary discovery: record paths from env/CLI/config and check `irohad`/`kagami`/`iroha_cli` availability + version/build-line (iroha2 vs iroha3, DA/RBC support) before launch; block on mismatches with actionable errors.
 - [x] Genesis verification: run `kagami verify --profile …` in prepare, parse fingerprint/chain id/VRF status, and surface results in UI logs; refuse launch when verify fails.
 - [x] Auto-build: optional `--build-binaries` path that runs `cargo build -p irohad -p iroha_cli -p kagami`, caches success, and short-circuits future runs; surface failures in UI.
 - [x] Telemetry/UX: emit an alignment summary in the UI (detected profile, binary versions, build-line, verify status, hash); add help text for overrides.
 - [x] Tests/docs: regressions for missing/old binaries, profile mismatch, auto-build success/failure; document alignment checks and escape hatches.

3. **MOCHI-READINESS-SMOKE — Gate readiness on real transaction/block flow** (Tooling/Supervisor, Line: Shared, Owner: Tooling WG, Priority: High, Status: 🈴 Completed)
 - [x] Implement a post-boot smoke: submit a signed tx, wait for commit via block/event streams, and gate readiness on success with retries/backoff and bounded timeouts.
 - [x] Snapshot/restore validation: check Kura/genesis hash before restore, refuse mismatches, and re-run the smoke after restore (with status surfaced).
 - [x] Status/UX: expose per-peer readiness, last smoke result/timestamp/error in UI and status API; add CLI flag to disable smoke for debugging.
 - [x] Tests: MockTorii-driven happy/timeout/fail cases plus snapshot/restore smoke rerun coverage.

1. **SUM-KURA-ATOMIC-COMMIT — Keep WSV/Kura updates atomic on commit** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Commit staging design: define a staging buffer for WSV mutations (tx index, DA shard/receipt cursors, pin intents, AXT policy install, merge metadata) that applies after Kura success; keep exec witness handling aligned and avoid double-emitting events.
 - [x] Pipeline refactor: split `finalize_pending_block` into Kura-first persistence and a post-Kura stage apply; ensure staged data is discarded on failure and not inserted into live WSV until persistence succeeds.
 - [x] Retry/abort hygiene: retries rebuild or reuse staging without replaying side effects; abort path clears staging, pending entries, QC caches, RBC sessions, and requeue lists without advancing cursors/indexes and without losing the ability to fetch/rehydrate.
 - [x] Tests: unit + integration for Kura failure (retry + abort) proving no WSV drift, no cursor/index advancement before Kura success, requeued txs don’t double-apply, and recovered state matches Kura; happy-path regression to ensure telemetry/events still fire after the refactor.
 - [x] Telemetry/docs: add “Kura staged/rollback” counters + last-error/height fields; document sequencing and operator remediation in Sumeragi runbook/config notes.

2. **SUM-KURA-ABORT-LIVENESS — Release locks and gates after persistence aborts** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Lock reset policy: on Kura abort that drops pending/RBC state, choose a deterministic rollback target for `locked_qc`/`highest_qc` (committed ancestor or highest cached QC) and/or force a view change with evidence so proposals can resume.
 - [x] Cache/pending cleanup: when QC/RBC caches are purged, also clear or reschedule pending entries and emit a fetch/rebroadcast plan for a fresh tip; ensure DA/exec gates don’t reopen on stale pending blocks.
 - [x] Liveness/tests: simulate Kura abort with live lock/QC caches; assert locks reset or view change triggers, no commits happen without payload, and the node accepts/rebuilds a replacement block (DA on/off, with/without QC caches).
 - [x] Telemetry/status: add “lock reset after Kura abort” counters and last {height, view, reason}; document operator steps for repeated abort-driven resets and expected status snapshots after a reset.

1. **IMPLICIT-ACCOUNTS — Domain-scoped implicit account receive** (Core/Executor/Data Model/SDK, Line: Shared, Owner: Core Protocol WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Finalize on-chain `AccountAdmissionPolicy` struct + deterministic wire format (domain metadata key `iroha:account_admission_policy`, chain-parameter fallback `iroha:default_account_admission_policy`).
 - [x] Enforce optional per-tx/per-block implicit-creation caps (domain policy).
 - [x] Extend executor permission model to support self-scope baseline rights for newly implicit accounts (or domain-configurable `default_role_on_create`), with deterministic deny rules.
 - [x] Core ISI semantics: update `Mint<Asset>` / `Transfer<Asset>` / `Transfer<Nft>` to `ensure_receiving_account` and auto-create accounts in `ImplicitReceive` domains; preserve explicit-only failure modes.
 - [x] Emit typed admission errors (`ImplicitAccountCreationDisabled`, `QuotaExceeded`, `AlgorithmNotAllowed`) and guarantee event ordering (`AccountCreated` before receipt events).
 - [x] Tests: add integration tests for open vs restricted domains, multi-receipt in one tx, cap enforcement, and determinism/event ordering (unit tests exist).
 - [x] SDK/CLI UX: remove destination-prevalidation requiring account existence; add helper to “register-if-missing”/atomic send for open domains; surface policy errors distinctly.
 - [x] Docs/migration: update `docs/source/data_model_and_isi_spec.md`, `rbac.md`, and CLI/SDK quickstarts; add upgrade checklist and sample policy blobs.

1. **DA-SHARD-CURSOR-MAPPING — Map lanes to WSV/Kura shards with durable cursors** (Nexus/Core, Line: Iroha 3, Owner: Core Protocol WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - Mapping: DONE — lane metadata `da_shard_id` (defaulting to `lane_id`) now drives `(lane, epoch, sequence) -> shard ID` mapping, resharded/unknown lanes are dropped on load, and Sumeragi persists per-lane cursors into `da-shard-cursors.norito` beside the DA spool.
 - Admission: DONE — block-and-revert paths rewind DA commitment/receipt/shard indexes to the pre-tip height before validation, rebuild the cursor journal from Kura when checkpoints regress, and keep touched-lane gating bound to the rewound shard cursors so soft forks require fresh DA commitments.
 - Recovery: enforce DA cursors during catch-up; telemetry for lagging/missing shard commitments; operator runbook for remediation (including Byzantine reshard attempts). Telemetry/runbook: DONE — `da_shard_cursor_lag_blocks{lane,shard}` now reports cursor gaps (missing/stale/unknown lanes set lag to the required height/delta; healthy advances reset to zero) and the DA ingest plan documents operator remediation for non-zero lags.【crates/iroha_core/src/state.rs】【crates/iroha_telemetry/src/metrics.rs】【docs/source/da/ingest_plan.md】
 - Tests: DONE — regressions cover resharding, DA journal persistence, Kura replay truncation/rewind, and soft-fork admission rejecting touched lanes without a matching shard cursor at the rewritten height.【crates/iroha_core/src/state.rs】

2. **SUM-BLOCK-SYNC-ROSTER-PERSISTENCE — Certified roster persistence and fetch** (Consensus/Sumeragi/Core, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - Kura persistence: DONE — commit certificates and validator checkpoints are journaled to `commit-rosters.norito` beside the block store and preloaded into Sumeragi status caches on startup so block sync can reuse persisted rosters. Retention/GC is configurable (`kura.roster_sidecar_retention`) and Kura now prunes roster sidecars to the configured window.
 - Roster fetch path: block sync now attaches commit certificates/checkpoints from persisted sidecars or the commit-roster journal for `(height, hash)` and validates them before caching, keeping uncertified hints out of the pipeline.
 - Control-flow hardening: uncertified rosters are dropped, `handle_block_sync_update` skips QC handling on payload failure, and roster selection prefers persisted snapshots over hints/history.
 - Integration/tests: coverage exercises journal fallbacks and missing-sidecar cases so long-range catch-up reuses persisted rosters without relying on raw roster hints.

3. **SUM-PENDING-BLOCK-FLOW — Stop view changes from marooning payloads** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Leader replay: view changes and retry exits rebroadcast the highest-QC `BlockCreated` payload to the current topology with per-(height,view) dedupe so pending blocks hydrate without roster hints.【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Pending-block metadata: `FetchPendingBlock` replies return `BlockCreated` for pending blocks and `BlockSyncUpdate` for committed blocks; block sync ignores raw roster hints in favour of certified commit certificates/checkpoints from durable snapshots.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/commit_roster_journal.rs】
 - [x] View-change visibility: view-change causes (commit failure/quorum timeout/DA availability/missing payload or QC/validation reject) export labeled counters plus last-cause/timestamp through status/telemetry for operator alerts (DA availability is advisory).【crates/iroha_core/src/sumeragi/status.rs】【crates/iroha_core/src/telemetry.rs】
 - [x] Localnet tuning: pacemaker backoff is capped at 10s and 1s block-time defaults keep DA-off localnets responsive; the NPoS pacemaker latency smoke holds 4-node nets under ~1.5s spacing even with 250 ms RTT.【crates/iroha_config/src/parameters/defaults.rs】【integration_tests/tests/sumeragi_npos_pacemaker_latency.rs】
 - [x] Tests: roster selection/fetch regressions cover commit-roster journal and sidecar recovery, and pacemaker/view-change telemetry tests pin the counters and rebroadcast path so missing payloads are repaired deterministically.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/status.rs】

4. **SUM-VOTE-PREVALIDATION — Gate votes on stateless+stateful checks** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Pre-vote guard (code): votes are gated until stateless+stateful validation succeeds; pending blocks track validation status, mark invalid payloads aborted, requeue transactions, and clean proposal/RBC caches before proceeding.【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Telemetry/docs: validation rejects record labeled counters (`sumeragi_validation_reject_total{reason=*}`) with last height/view/block/timestamp surfaced via `/v1/sumeragi/status`; view-change cause telemetry includes a `validation_reject` bucket and the Sumeragi runbook documents alert guidance.【crates/iroha_core/src/sumeragi/status.rs】【crates/iroha_core/src/telemetry.rs】【crates/iroha_data_model/src/block/consensus.rs】【docs/source/sumeragi.md】
 - [x] Evidence/view-change: invalid proposals emit evidence tied to the payload hash/parent QC and trigger view-change realignment without advancing Highest/Locked QCs; reasons are logged for operators.【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/sumeragi/evidence.rs】
 - [x] Coverage: status/telemetry/unit tests lock reason buckets and last-reject snapshots, and invalid-proposal evidence validation covers height/view/parent mismatches to keep negative paths deterministic.【crates/iroha_core/src/sumeragi/status.rs】【crates/iroha_core/src/sumeragi/evidence.rs】

5. **SUM-PACEMAKER-LIVENESS-GAP — Keep quorum/DA timers running without inbound traffic** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
   - [x] Wire pacemaker tick -> commit path (both lines): `tick` now drives `process_commit_candidates` with an early return on empty pending sets and records status/telemetry counters (`commit_pipeline_tick_total`, `sumeragi_commit_pipeline_tick_total{mode,outcome}`) so timer-driven commits stay visible; DA-off/permissioned and DA-off NPoS smoke runs keep quorum-timeout liveness.
   - [x] DA availability rescheduler (deprecated; DA is advisory): `reschedule_stale_pending_blocks` reused the availability timeout plus DA abort hooks to reschedule `MissingLocalData` blocks, requeueing payloads, purging RBC state, bumping view, and tagging mode-labeled counters (`sumeragi_rbc_da_reschedule_by_mode_total{mode}`); consensus no longer reschedules on missing availability.
   - [x] Prevote-only recovery policy (both lines, mode-aware): prevote-quorum timeouts requeue payloads, rebroadcast `BlockCreated` + `PrevoteQC`, clear stale HighestQC, and trigger view change to avoid precommit starvation across permissioned and NPoS (DA on/off) runs.
   - [x] Observability/docs/tests: added unit coverage for prevote timeout detection and commit-pipeline tick status, telemetry label assertions for DA reschedules, and refreshed the Sumeragi runbook with the liveness path and operator signals (Iroha 2 vs Iroha 3 / DA-on vs DA-off).【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/telemetry.rs】【crates/iroha_telemetry/src/metrics.rs】【docs/source/sumeragi.md】

6. **MULTISIG-AUTH-HARDENING — Enforce controller semantics and eliminate derived-key bypass** (Core/Executor/SDK/Torii, Line: Shared, Owner: Security WG, Priority: High, Status: 🈴 Completed, target TBD)
 - Admission crash guard: (A) patch `tx::validate`/`SignedTransaction::verify_signature` to handle `AccountController::Multisig` without panic and emit stable reject codes **DONE**; (B) add unit/integration coverage for multisig authorities (accept/reject matrix) and Torii error/header plumbing **DONE**.
 - Policy verification path: (A) **DONE** — signature bundles carry multi-alg member signatures with signer ids; (B) **DONE** — deduped weight/quorum verification against `MultisigPolicy`; (C) **DONE** — signature-limit rejects now increment Torii telemetry with authority labels; (D) **DONE** — regressions for single-key alignment and signature-count overflow cases.【crates/iroha_data_model/src/transaction/signed.rs】【crates/iroha_core/src/tx.rs】
 - Controller id enforcement: (A) **DONE** — multisig registration requires an explicit controller id (JSON decoding rejects missing `account`, builder helpers randomise the controller in the signatory domain); (B) **DONE** — direct signing with multisig accounts is rejected in admission/execution with regression coverage.【crates/iroha_executor_data_model/src/isi.rs】【crates/iroha_core/src/smartcontracts/isi/multisig.rs】【crates/iroha_core/src/tx.rs】【docs/source/references/multisig_policy_schema.md】
 - TTL enforcement:
 - [x] Remove the “downward proposal” TTL bypass created by multisig-role propagation; relayer proposals now cap expiry per multisig policy to prevent longer-lived nested approvals.【crates/iroha_core/src/smartcontracts/isi/multisig.rs】【crates/iroha_executor/src/default/isi/multisig/transaction.rs】
 - [x] Enforce `transaction_ttl_ms` caps for proposers/relayers including nested multisig with regression coverage for nested relayer expiry.【crates/iroha_core/src/smartcontracts/isi/multisig.rs】
 - [x] Refresh CLI UX to surface the capped TTL behaviour and expiry expectations.
 - [x] Refresh SDK builders/fixtures (JS/Swift/Android) to preview and enforce the capped TTL before submission; JS now ships `buildProposeMultisigInstruction` with TTL-cap enforcement plus README/tests, and Swift/Android builders retain the preview/enforce scaffolding with refreshed fixtures.
 - SDK/CLI/docs alignment: **DONE** — Android/OkHttp transports surface Torii reject headers with regression coverage, SDK docs now call out `rejectCode()` handling alongside derived-key/TTL migration guidance, and status.md records the shipped guards.【java/iroha_android/android/src/test/java/org/hyperledger/iroha/android/client/HttpClientRejectCodeOkHttpTests.java】【java/iroha_android/README.md】【status.md】

7. **RBC-LIVENESS-RECOVERY — Diagnose and unblock reliable-broadcast stalls** (Consensus/Sumeragi/Core, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
   - [x] Instrumentation: RBC backlog and status snapshots now export READY/chunk deferral counters, pending-stash age/size/drops, retry/abort totals, and store-pressure gauges across `/v1/status`, `/v1/sumeragi/status`, and Prometheus so stalled deliveries can be triaged quickly.【crates/iroha_core/src/sumeragi/status.rs】【crates/iroha_telemetry/src/metrics.rs】
   - [x] Reschedule paths: pending blocks stuck on `MissingLocalData` are rescheduled off the availability timeout (requeue payloads, purge RBC state, bump view), and RBC handlers keep sessions bounded with unit coverage for deferral/eviction counters and stash limits.【crates/iroha_core/src/sumeragi/main_loop.rs】
   - [x] Adversarial coverage: chunk-drop/shuffle scenarios, cold-start recovery, and restart liveness suites hold blocks pending without committing until RBC delivery resumes and assert telemetry reflects the stalled sessions under packet loss/reorder/view changes.【integration_tests/tests/sumeragi_adversarial.rs】【integration_tests/tests/sumeragi_da.rs】【integration_tests/tests/sumeragi_npos_liveness.rs】
   - [x] Operational guidance: the Sumeragi runbook documents the RBC backlog/deferral/store-pressure signals, availability vote health, and DA reschedule counters used to remediate stalls.【docs/source/sumeragi.md】

8. **SUM-MODE-CUTOVER-LIVE — Runtime flip between permissioned and NPoS without restart** (Consensus/Sumeragi/Core, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - Flip trigger + safety: tick polls `effective_consensus_mode` vs staged `next_mode`/activation height, debounces repeat flips, and enforces the `mode_flip_enabled` kill switch with activation-lag surfacing in status/telemetry.
 - Runtime swap: live flips now reset pacemaker/view-change trackers (including the phase tracker and VRF local state) to the base pacemaker interval, clear mode-sensitive caches (pending blocks/RBC sessions/proposals/QCs), rebuild PRF/epoch managers + collectors, and refresh mode tags/PRF context in status + telemetry.
 - Pending state handling: in-flight proposals, votes, RBC sessions, and cached blocks are purged during the flip so DA/Exec gating can resume cleanly on the new mode without reusing pre-flip artefacts.
 - alignment surface: consensus fingerprints/handshake caps are recomputed and pushed to the network on flip; success/failure/blocked counters and last-error surfaces are wired for operators.
 - Tests: runtime-reset regression locks pacemaker/view/RBC-cache clearing on flip and the Sumeragi doc calls out the deterministic reset policy.【crates/iroha_core/src/sumeragi/main_loop.rs】【docs/source/sumeragi.md】

9. **P2P-GOSSIP-SCALING — Fanout strategy for large NPoS overlays** (Consensus/Networking, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Route availability/precommit votes through deterministic collector targets with commit-topology fallback to reduce broadcast fanout in the steady state.
 - [x] Send precommit-triggered block-sync updates to the commit topology instead of broadcast.
 - [x] Scope proposals, QCs, RBC, VRF, and NEW_VIEW/view-change control frames to the commit topology (validators) instead of network-wide broadcast.
 - [x] Gossip `BlockSyncUpdate` payloads (commit + vote/backfill paths) to a capped fanout (`block_gossip_size`) with per-update random sampling, removing full broadcast and documenting the fanout behavior.
 - [x] Add config knobs + telemetry for per-plane gossip fanout and target reshuffle cadence.【crates/iroha_config/src/parameters/user.rs】【crates/iroha_core/src/gossiper.rs】【crates/iroha_core/src/telemetry.rs】【crates/iroha_telemetry/src/metrics.rs】【docs/source/references/configuration.md】
 - [x] Run synthetic or localnet-scale load tests (>=22 peers) to validate bandwidth/CPU headroom. 22-peer localnet run captured with `scripts/deploy_localnet.sh --peers 22 --out-dir./localnet-22 --release --skip-asset-register --telemetry-profile extended`, storing `/status`, `/metrics`, and CPU snapshots for review.【scripts/deploy_localnet.sh】

9. **SUM-MODE-CUTOVER-OPS — Observability and docs for mixed-mode cutover** (Consensus/Sumeragi/Torii/Docs, Line: Shared, Owner: Consensus WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - Status/telemetry: `/v1/sumeragi/status` exports staged vs active tags, activation height, flip lag, kill-switch state, `{success,failure,blocked}_total`, and last flip timestamp/error; Prometheus mirrors the gauges/counters with mode labels for alerting.
 - Governance/API/SDK: `/v1/configuration` now includes consensus mode + `mode_flip_enabled`, status mirrors staged/active modes, and runtime flips recompute + re-announce consensus handshake caps to peers (dropping stale connections) so SDKs can refresh capabilities on tag changes.
 - Runbook/docs: live cutover notes document the kill switch, runtime cache/pacemaker reset, and handshake-cap refresh, plus the operator surfaces to watch for blocked flips or lag.【docs/source/sumeragi.md】【docs/source/references/configuration.md】
 - Tests: configuration endpoint regression covers consensus mode/kill-switch fields and status snapshot coverage pins flip counter/error updates.【crates/iroha_torii/tests/configuration_endpoint.rs】【crates/iroha_core/src/sumeragi/status.rs】

10. **IVM-STACK-POLICY-ALIGNMENT — Enforce configured stack limits for guest code and Rayon workers** (IVM/Compute, Line: Shared, Owner: Runtime WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - Config surface: introduce distinct knobs for guest stack bytes, scheduler worker stack bytes, and prover worker stack bytes derived from `ComputeResourceBudget.max_stack_bytes`; validate bounds at parse-time and emit config warnings when defaults are overridden or budgets are mismatched.
 - Guest stack/ABI: remove or justify the current slop window, enforce stack size at program load/admission (reject over-limit bytecode), and add tests that SP init/top-of-stack and Kotodama lowering frames stay within the configured region.
 - Pool enforcement: ensure scheduler/prover Rayon pools always use the configured stack size even when a global pool already exists; add a deterministic fallback (per-VM pool) when the global pool conflicts and cover deep-recursion paths.
 - Telemetry and failure surfacing: log/metric when stack overrides are ignored, when pool initialization falls back, and when guest stack limits are hit at runtime; expose current stack settings via status endpoints.
 - Docs/runbook/tests: update IVM docs/status.md/operator notes to spell out the stack limits and knobs; add integration/unit tests for admission rejections, SP boundaries, and pool fallback behaviour.
 - Per-route budget enforcement: (a) plumb `ComputeResourceBudget.max_stack_bytes` into admission, route execution, and the IVM builder; (b) reject/clamp bytecode exceeding `min(route budget, config cap, gas cap)` with explicit error codes; (c) add unit/integration tests for over-budget programs, SP init under per-route caps, and admission-time rejections.
 - Gas->stack policy surface: (a) expose a configurable gas->stack multiplier/strategy (with validation) instead of the fixed heuristic; (b) surface the derived stack cap and which constraint applied (route/config/gas) via status/telemetry; (c) document the policy, defaults, and operator tuning guidance.
 - Global Rayon fallback: (a) detect when a pre-existing global Rayon pool conflicts with requested stack size; (b) choose a deterministic fallback (per-VM pool or fail-fast) and emit logs/metrics for degraded mode; (c) add deep-recursion coverage under fallback and operator signals for the degraded path.

11. **NEXUS-LANE-BOUNDARY-ENFORCEMENT — Keep Iroha 2 lane-free, gate lanes to Nexus only** (Config/Core/Torii, Line: Shared, Owner: Nexus Core WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Config guards: single-lane configs now reject lane/dataspace/routing overrides when `nexus.enabled=false`, with fixtures/tests pinning the failure and Nexus profile templates unchanged.【crates/iroha_config/src/parameters/user.rs】【crates/iroha_config/tests/fixtures.rs】
 - [x] Admission/runtime: startup refuses Nexus lane overrides without `nexus.enabled` and treats any Nexus lane config as a Sora-profile feature requiring `--sora`.【crates/irohad/src/main.rs】
 - [x] API/telemetry split: gate `/status`, `/v1/sumeragi/status`, and Prometheus lane labels behind `nexus.enabled`; lane/dataspace caches reset when Nexus is disabled (dual-profile fixtures still to come).【crates/iroha_torii/src/routing.rs】
 - [x] Torii/SDK surfacing: lane-scoped endpoints (public lanes, DA commitments) return `nexus_disabled` in Iroha 2 mode with regression coverage.【crates/iroha_torii/tests/nexus_public_lanes.rs】【crates/iroha_torii/src/da/commitments.rs】
 - [x] Docs/runbook: config/transition notes call out the lane-free Iroha 2 boundary and the need for `nexus.enabled=true`/`--sora` to expose lanes.【docs/source/references/configuration.md】【docs/source/nexus_transition_notes.md】
 - [x] CI guard: lane-disabled regressions in routing/telemetry and DA/public-lane tests keep `nexus.enabled=false` builds from exposing lane metrics or routes.【crates/iroha_torii/src/routing.rs】【crates/iroha_torii/tests/nexus_public_lanes.rs】

12. **NEXUS-LANE-LIFECYCLE — Dynamic lane creation/destruction per dataspace** (Nexus/Core/Storage, Line: Iroha 3, Owner: Nexus Core WG, Priority: High, Status: 🈴 Completed, target TBD)
 - Design/API: derive lane instantiation from dataspace manifests (validator set, governance, DA/storage profile) with deterministic slug/segment geometry and shard cursors; define retirement semantics, catalog drift, and rollback on failure.
 - Storage/runtime guards: DONE — lifecycle plans now validate dataspace bindings, abort on lane storage/tiered reconciliation failures instead of mutating the catalog, and prune cached lane relay envelopes when lanes retire; ops note lives in `docs/source/nexus_transition_notes.md` and tests cover the rollback path.【crates/iroha_core/src/state.rs】
 - Provision/teardown: implement per-lane Kura/WSV segment creation/removal, routing-table rebuilds, shard cursor init, and telemetry/status diffs on lane add/remove without restart.
 - Routing/telemetry: queue/runtime helpers now reload Nexus lane/dataspace catalogs and rebuild routing decisions + TEU/backpressure snapshots at runtime; `Queue::apply_lane_lifecycle` reconfigures routing/manifests after catalog changes, guards reject lifecycle when `nexus.enabled=false`, and regression coverage pins rerouting + per-lane TEU caps.
 - Scheduling/fairness: move to per-lane queues, TEU/DA quotas, and RBC/payload budgets that rebalance when lanes are added/removed; guard head-of-line blocking during churn.
 - Governance/handoff: wire manifest activation/expiry to lane lifecycle; ensure validator-set changes propagate to per-lane consensus membership and DA policy.
 - Validator reuse: allow the same validator identities to serve multiple lanes concurrently; document/support multi-lane membership in manifest validation, scheduler membership maps, and telemetry.
 - Tooling/tests: helpers to create/retire lanes from manifests; soak/integration tests that add/remove lanes under load and assert deterministic storage paths, routing, and telemetry snapshots.
 - Ops/runbook: document lifecycle hooks, cleanup of retired segments, and alerting for stuck create/destroy flows.

13. **NEXUS-LANE-PROOFS-MERGE — Per-lane proof ingestion into the global merge ledger** (Nexus/Core/Consensus, Line: Iroha 3, Owner: Nexus Core WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Relay receiver: validate lane relays against the lane catalog/dataspace mapping, enforce commit-topology quorum for execution QCs, reject stale/conflicting or Nexus-disabled submissions, and dedupe per lane/dataspace before surfacing to status.
 - [x] Merge integration: synthesise merge-ledger entries from the latest relays across all lanes, reduce settlement hashes into `global_state_root`, persist via Kura, and suppress duplicate commits when inputs are unchanged.
 - [x] Lane finality/telemetry: accepted relays update per-lane block height/finality lag and RBC byte totals so `/status` and Prometheus expose merge progress by lane.
 - [x] Evidence/persistence/tests: relays are retained in-memory keyed per lane/dataspace with stale/conflict guards, merge-ledger commits are durable, and unit tests cover happy paths plus dataspace/quorum/stale rejection and merge synthesis.

1. **DA-QUEUED-FEED-BRIDGE — Deterministic DA receipt fanout (Iroha 3 only)** (Nexus/Core/Torii, Line: Iroha 3, Owner: DA WG + Core Protocol WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Durable receipt spool loader + cursor index track highest `(lane, epoch, sequence)` values from committed bundles; Kura hydration/commit advance cursors with telemetry updates and prune stale receipt files to bound disk usage.【crates/iroha_core/src/da/receipts.rs】【crates/iroha_core/src/state.rs】
 - [x] Block assembly enforces contiguous DA receipts per lane/epoch using cursor snapshots and sealed sets, errors when reachable receipts lack commitments, and filters bundles to receipt-aligned slices to avoid omissions or replays.【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Receipt cursor gauges surface alongside shard cursors and the DA commitments plan documents the spool-based queue, retention, and inclusion guard for operators.【crates/iroha_core/src/telemetry.rs】【docs/source/da/commitments_plan.md】【status.md】

1. **DA-CONFIDENTIAL-COMPUTE-LANES — Privacy path for SMPC/computation DA** (Nexus/Core/Storage, Line: Iroha 3, Owner: DA WG + Compute Lane WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Lane metadata flags (`confidential_compute`, `confidential_key_version`, optional mechanism/audience labels) derive deterministic confidential-compute policies for each lane.【crates/iroha_data_model/src/da/confidential_compute.rs】【crates/iroha_config/src/parameters/actual.rs】
 - [x] Validation enforces non-zero payload/manifest digests and storage tickets, rejects full-replica storage for confidential lanes, and indexes confidential-compute receipts with policy versions while hydrating them from Kura replay.【crates/iroha_core/src/da/confidential.rs】【crates/iroha_core/src/da/confidential_store.rs】【crates/iroha_core/src/sumeragi/main_loop.rs】【crates/iroha_core/src/state.rs】
 - [x] Regression coverage exercises policy parsing, validation failures, and restart hydration; the ingest plan documents the operator knobs for confidential compute lanes.【crates/iroha_core/src/da/confidential.rs】【crates/iroha_core/src/state.rs】【docs/source/da/ingest_plan.md】

1. **DA-PIN-REGISTRY-REPLAY — On-chain PinIntent for SoraFS/web assets** (Nexus/SoraFS/Core, Line: Iroha 3, Owner: Storage Team + Core Protocol WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Schema and block wiring: versioned `DaPinIntent`/bundle with deterministic ordering + Merkle root now hashes into `da_pin_intents_hash`, and block assembly loads spool intents, dedupes sealed entries, and keeps pin processing non-blocking for proposal build.【crates/iroha_data_model/src/da/pin_intent.rs】【crates/iroha_data_model/src/block/header.rs】【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Registry/replay/query surface: WSV stores intents keyed by ticket/alias/manifest/lane-epoch, sanitizes unknown lanes/owners/zero manifests while tagging telemetry reasons, rebuilds indexes from Kura replay, and exposes find-by-{ticket,manifest,alias,lane/epoch/seq} via Torii.【crates/iroha_core/src/da/mod.rs】【crates/iroha_core/src/state.rs】【crates/iroha_telemetry/src/metrics.rs】【crates/iroha_torii/src/da/pin_intents.rs】
 - [x] Tests/docs: spool canonicalization covers alias supersession/duplicate drops, replay/owner-drop regressions lock registry behaviour, and the DA ingest plan documents PinIntent artefacts alongside DA commitments.【crates/iroha_core/src/da/pin_intents.rs】【crates/iroha_core/src/state.rs】【docs/source/da/ingest_plan.md】

1. **DA-MANIFEST-ADMISSION-GUARD — Enforce manifest availability before commitment** (Nexus/Core/Torii, Line: Iroha 3, Owner: DA WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Availability policy + telemetry: manifest guard reasons (missing/hash mismatch/read/spool scan) feed `ManifestGuard` counters in status/telemetry alongside DA availability snapshots, honoring per-lane audit vs strict policies.【crates/iroha_data_model/src/block/consensus.rs】【crates/iroha_core/src/sumeragi/status.rs】【crates/iroha_telemetry/src/metrics.rs】
 - [x] Core wiring: block assembly scans DA spool for manifests, drops mismatched commitments, gates precommit on manifest availability, and re-evaluates guards when manifests arrive or spool errors clear while leaving audit-only lanes as warnings.【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Tests/docs: regressions cover missing/mismatched/late manifests and spool read/scan failures plus gate release after manifest arrival; the Sumeragi runbook documents manifest guard fields for operators.【crates/iroha_core/src/sumeragi/main_loop.rs】【docs/source/sumeragi.md】

1. **DA-PROOF-POLICY-LANES — Fix commitment/proof scheme per lane** (Nexus/Core/SDK, Line: Iroha 3, Owner: DA WG + SDK WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Added a versioned `DaProofPolicyBundle` with a Norito-derived `policy_hash`, threaded it into block headers via `da_proof_policies_hash`, and sealed it during block assembly so blocks pin the active per-lane proof policy set.【crates/iroha_data_model/src/da/commitment.rs】【crates/iroha_data_model/src/block/header.rs】【crates/iroha_core/src/block.rs】【crates/iroha_core/src/sumeragi/main_loop.rs】
 - [x] Torii now serves the bundle from `/v1/da/proof_policies` (version + hash + policies) and docs call out the new response shape for clients to validate lane schemes before building proofs.【crates/iroha_torii/src/da/commitments.rs】【docs/source/da/ingest_plan.md】

1. **SUM-BLOCK-FINALITY-REMEDIATION — Track DA availability and safe commit rollback paths** (Consensus/Sumeragi/Core, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
   - [x] DA availability correctness (logic):
     - [x] Remove implicit `availability_qc_view` mutation on RBC deliver; only `availability evidence` marks availability.【crates/iroha_core/src/sumeragi/main_loop.rs:20324】
     - [x] Record `MissingLocalData` until an `availability evidence` arrives when `da_enabled=true`; `finalize_pending_block` proceeds regardless and RBC deliver remains a transport/hydration path only.【crates/iroha_core/src/sumeragi/main_loop.rs:17954】【crates/iroha_core/src/sumeragi/main_loop.rs:18049】【crates/iroha_core/src/sumeragi/main_loop.rs:18160】
     - [x] Add structured telemetry/log reasons for `MissingLocalData` and record which availability condition was satisfied last.【crates/iroha_core/src/sumeragi/status.rs:2019】【crates/iroha_core/src/sumeragi/status.rs:3075】
   - [x] DA availability regressions (tests):
     - [x] Unit: DA availability reports `MissingLocalData` until a real QC arrives even if RBC delivered; DA-disabled variant still commits with QC+payload.【crates/iroha_core/src/sumeragi/main_loop.rs:17942】【crates/iroha_core/src/sumeragi/main_loop.rs:18160】
     - [x] Integration: RBC-only (no availability evidence) still records missing availability; availability evidence-only (no payload/manifests) records availability while fetch runs; QC-first with late payload + DA artifacts commits once payload is available, independent of availability status.【crates/iroha_core/src/sumeragi/main_loop.rs:18029】【crates/iroha_core/src/sumeragi/main_loop.rs:18077】【crates/iroha_core/src/sumeragi/main_loop.rs:18111】
     - [x] Regression: availability tracking keeps Highest/Locked QC stable while waiting on payload/manifest arrival; availability evidence is recorded independently.【crates/iroha_core/src/sumeragi/main_loop.rs:18862】
   - [x] Storage/WSV atomicity:
     - [x] Reorder commit path to persist to Kura before/state in lockstep, or keep block pending with retry/backoff; never advance WSV on failed store.【crates/iroha_core/src/sumeragi/main_loop.rs:6823】
     - [x] Add telemetry/alerts on Kura failure (per-block) and a retry budget.【crates/iroha_core/src/telemetry.rs:5742】【crates/iroha_telemetry/src/metrics.rs:5556】【docs/source/sumeragi.md:255】
     - [x] Regression: simulate `kura.store_block` error; assert WSV/locks/highest_qc unchanged and pending entry retained for retry.【crates/iroha_core/src/sumeragi/main_loop.rs:20186】【crates/iroha_core/src/sumeragi/main_loop.rs:20244】【crates/iroha_core/src/sumeragi/main_loop.rs:21330】
   - [x] QC-backed commit failure recovery:
     - [x] When commit fails with precommit QC quorum, keep pending block and requeue txs; trigger view-change/evidence if payload invalid.【crates/iroha_core/src/sumeragi/main_loop.rs:7062】【crates/iroha_core/src/sumeragi/main_loop.rs:7080】
     - [x] Ensure `locked_qc`/`highest_qc` realign to a committable chain (or stay unchanged) and avoid deadlock on uncommittable block.【crates/iroha_core/src/sumeragi/main_loop.rs:206】【crates/iroha_core/src/sumeragi/main_loop.rs:15870】
     - [x] Regression: invalid-payload-with-QC does not drop txs or stall liveness; view change or retry proceeds.【crates/iroha_core/src/sumeragi/main_loop.rs:7062】【crates/iroha_core/src/sumeragi/main_loop.rs:21330】
   - [x] Docs/runbook:
     - [x] Update DA/commit/finality docs with the availability matrix (DA on/off), required artifacts, and retry/backoff semantics.【docs/source/sumeragi.md:673】
     - [x] Document telemetry/log labels for DA availability warnings, Kura errors, QC-backed commit failures, and operator actions/alerts.【docs/source/sumeragi.md:673】
     - [x] Export DA availability, missing-block fetch, and Kura persistence snapshots via `/v1/sumeragi/status` (JSON + Norito) with Torii/data-model roundtrip tests and operator notes.【crates/iroha_torii/src/routing.rs:19642】【crates/iroha_torii/src/routing/consensus.rs:2169】【crates/iroha_data_model/src/block/consensus.rs:618】【docs/source/sumeragi.md:255】
1. **RBC-HYDRATE-AVAILABILITY — Require READY/DELIVER evidence before availability is marked** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Hydration now validates payload hash, chunk root, and chunk count against INIT, marking sessions invalid, updating status/backlog snapshots, and clearing pending stashes on mismatches.【crates/iroha_core/src/sumeragi/main_loop.rs:2692】【crates/iroha_core/src/sumeragi/main_loop.rs:5695】
 - [x] Hydrated sessions emit READY and only attempt DELIVER once RBC quorum and a local validator index exist; hydration never sets delivery on its own so availability remains missing until RBC evidence lands.【crates/iroha_core/src/sumeragi/main_loop.rs:5651】【crates/iroha_core/src/sumeragi/main_loop.rs:5726】
 - [x] Regression coverage holds availability missing on hydration-only paths, derives missing chunk roots, and rejects hash/root/layout mismatches.【crates/iroha_core/src/sumeragi/main_loop.rs:22172】【crates/iroha_core/src/sumeragi/main_loop.rs:22224】【crates/iroha_core/src/sumeragi/main_loop.rs:22252】【crates/iroha_core/src/sumeragi/main_loop.rs:22269】

1. **RBC-PENDING-STASH-BOUNDS — Bound pre-INIT chunk buffering** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Enforced per-session chunk/byte caps, TTL eviction, and stash-size housekeeping before INIT, dropping/evicting pending frames deterministically and tracking per-session drop counters in backlog snapshots.【crates/iroha_core/src/sumeragi/main_loop.rs:2606】【crates/iroha_core/src/sumeragi/main_loop.rs:3517】【crates/iroha_core/src/sumeragi/main_loop.rs:8467】
 - [x] Surfaced pending stash gauges and drop/eviction counters (with per-entry breakdown) via `/v1/sumeragi/status` and telemetry payloads and added regression coverage for cap eviction/TTL and drop accounting.【crates/iroha_core/src/sumeragi/status.rs:741】【crates/iroha_torii/src/routing.rs:19555】【crates/iroha_torii/src/routing/consensus.rs:2348】【crates/iroha_core/src/telemetry.rs:5796】【crates/iroha_core/src/sumeragi/main_loop.rs:22733】

1. **BLOCK-SYNC-QC-HARDENING — Reject forged block-sync aggregates** (Core/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Block-sync batches now drop when commit-signature validation fails even if a QC is present; incoming QCs are validated against BLS aggregate signatures and verified block signers before processing.【crates/iroha_core/src/block_sync.rs:904】【crates/iroha_core/src/sumeragi/main_loop.rs:595】
 - [x] QC handling reuses verified signer tallies to reject forged aggregates, mismatched bitmaps, and BLS aggregate mismatches before caching or commit, aligning block-sync updates with the commit-signature set.【crates/iroha_core/src/block_sync.rs:904】【crates/iroha_core/src/sumeragi/main_loop.rs:595】
 - [x] Regression coverage exercises forged QCs paired with single-signature or missing-proxy-tail blocks and stale signer records to prove proxy-tail/quorum enforcement in block sync.【crates/iroha_core/src/block_sync.rs:1569】【crates/iroha_core/src/block_sync.rs:1588】【crates/iroha_core/src/sumeragi/main_loop.rs:17460】

1. **SUM-VIEW-CHANGE-ROSTER-HARDENING — Avoid stalls on roster/source drift** (Consensus/Sumeragi, Line: Shared, Owner: Consensus WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Roster lookup order: block sync prefers persisted commit-roster snapshots (journal -> sidecar) before caches, logs the chosen source, and drops uncertified rosters; pending-block repair uses `BlockCreated` replies instead of block-sync updates.
 - [x] Hint retirement: raw roster hints are decode-only/ignored; roster selection relies on certified commit certificates/checkpoints and durable snapshots.
 - [x] Persistence symmetry: certified rosters arriving via block sync are persisted to the journal and sidecar, with a restart regression that clears status caches and still selects the journal entry.
 - [x] PoP/topology safety: PoP filtering warns when maps are incomplete, preserves commit quorum, and reintroduces the local peer when allowed.
 - [x] Telemetry/docs: added source/drop counters (status + Prometheus) and refreshed the Sumeragi runbook with the roster source order and recovery notes.

1. **P2P-TRUST-GOSSIP-GATING — Honor trust-gossip capability without starving peer gossip** (P2P/Core, Line: Shared, Owner: Network WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Capability note details the dual handshake flags (`network.trust_gossip` and `soranet_handshake.trust_gossip`), send/recv gating, relay semantics, and operator guidance so permissioned overlays can opt out while peer-address gossip stays untouched.【docs/source/p2p.md:206】【docs/source/p2p_trust_gossip.md:1】【docs/source/references/configuration.md:15】【docs/source/references/peer.template.toml:53】
 - [x] Wiring keeps `PeerTrustGossip` on the `TrustGossip` topic, enforces capability checks on post/broadcast/recv, and counts/logs skipped trust frames without changing peer gossip caps/backoffs; skip telemetry lives at `p2p_trust_gossip_skipped_total{direction,reason}`.【crates/iroha_core/src/lib.rs:246】【crates/iroha_p2p/src/network.rs:676】【crates/iroha_p2p/src/network.rs:4011】【crates/iroha_p2p/src/network.rs:4528】【crates/iroha_telemetry/src/metrics.rs:7915】
 - [x] Unit coverage asserts the gating helper blocks trust frames when disabled while leaving other topics unaffected, exercising both send and recv paths.【crates/iroha_p2p/src/network.rs:4943】
 - [x] Integration: a `trust_gossip=false` peer neither sends nor receives trust frames but still exchanges peer-address gossip, and trust-enabled peers deliver trust frames end-to-end under the existing Low queue/caps.【crates/iroha_p2p/tests/integration/p2p_trust_gossip.rs:1】

1. **P2P-TRUST-PENALTY-SURFACE — Align unknown-peer penalty with behaviour** (P2P/Docs, Line: Shared, Owner: Network WG, Priority: Low, Status: 🈴 Completed, target TBD)
 - [x] Decision: keep unknown-peer penalties enforced for permissioned overlays (and penalty-free for public/NPoS) with operator impact/alerting captured in the trust-gossip note.【docs/source/p2p_trust_gossip.md:8】
 - [x] Enforcement/telemetry: off-topology trust gossip logs and labels `p2p_trust_penalties_total{reason="unknown_peer"}` without touching peer-address gossip, keeping penalty reasons centralised for telemetry.【crates/iroha_core/src/peers_gossiper.rs:613】
 - [x] Tests: regressions cover penalty decay -> eviction -> recovery plus the public-mode allowance so dial sets stay open when penalties are suppressed.【crates/iroha_core/src/peers_gossiper.rs:824】【crates/iroha_core/src/peers_gossiper.rs:894】【crates/iroha_core/src/peers_gossiper.rs:966】
 - [x] Templates/docs refreshed with the reason label and reinstatement flow so operators can alarm on `unknown_peer` penalties without misreading skips.【docs/source/references/peer.template.toml:51】【docs/source/references/configuration.md:16】【docs/source/p2p_trust_gossip.md:8】

1. **AXT-COMPOSABILITY — Cross-dataspace atomic transactions** (Nexus/IVM/Core, Line: Shared, Owner: mtakemiya, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define AXT policy snapshot builder (Space Directory -> lane map) with Norito goldens and error vectors; keep `current_slot`/lane gating deterministic and sorted.【crates/iroha_data_model/tests/axt_policy_vectors.rs:1】
 - [x] Cache AXT policies in WSV on startup and during Space Directory events; host-side enforcement now falls back to cached snapshots and records telemetry tags on reject paths.【crates/iroha_core/src/state.rs:933】【crates/iroha_core/src/smartcontracts/ivm/host.rs:1342】
 - [x] Persist AXT envelopes/handles into block artifacts and admission: export handle/proof fragments from CoreHost, thread through block assembly, and add WSV/admission validation for duplicates/expiry/lane mismatches.
 - [x] Dataspace manifest lifecycle: refresh bindings on manifest expire/revoke/rotate, gossip manifest roots per lane, and add negative tests for stale/missing manifests and zeroed roots.
 - [x] Remote spend flow: validate proofs per dataspace, enforce handle budgets/sub-nonces/expiry across lanes, and add replay protection + slot-length config knob in `iroha_config`.
 - [x] Telemetry/metrics: expose policy snapshot version, reject labels (lane/manifest/era/sub_nonce/expiry), and cache-hit/miss counters for host state hydration.
 - [x] Integration tests: multi-dataspace happy path (touch+proof+handle), lane-mismatch/expired-handle/budget-exceeded rejects, manifest rotation mid-envelope, and restart resilience (cached policies restored).
 - [x] Docs/runbooks: describe AXT lane catalog mapping, manifest requirements, operator checklist for cross-dataspace composability, and SDK samples for remote spend without token egress.


1. **AXT-USABILITY — Developer and operator ergonomics for AXT** (IVM/Core/SDK, Line: Shared, Owner: mtakemiya, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Error taxonomy: structured `AxtRejectReason` codes now flow from CoreHost into overlay/block `ValidationFail::AxtReject` and Torii headers; SDKs expose enums/helpers (TS/Swift/Android) with tests for manifest/expiry/replay contexts and telemetry labels match reason codes.
 - [x] Proof cache visibility: expose per-dataspace cache status (hit/miss/invalid/slot/manifest_root) via telemetry metrics and a `/v1/debug/axt/cache` Torii endpoint; add host/unit tests that invalidate cache on manifest rotation/slot change and verify metrics labels.
 - [x] Expiry/slot ergonomics: validate `nexus.axt.slot_length_ms` against sane ranges, add `nexus.axt.max_clock_skew_ms` to host expiry checks, and document recommended values per block cadence with unit tests for zero/too-large/negative cases.
 - [x] Handle refresh hints: include `next_min_handle_era`/`next_min_sub_nonce` in host/Torii rejects; add SDK helpers to parse and request refreshed handles; add tests that the hinted minima advance after replay/era failures.
 - [x] Descriptor/build helpers: add SDK builder utilities + Norito fixtures for deterministic `AxtDescriptor`/`TouchManifest`/binding computation, ship multi-DS JSON samples, and wire a lint/test guarding descriptor/touch schema drift.【crates/iroha_data_model/src/nexus/axt.rs:1】【crates/iroha_data_model/tests/axt_descriptor_fixture.rs:1】【crates/iroha_data_model/tests/fixtures/axt_descriptor_multi_ds.json:1】
 - [x] Replay ledger: persist handle usage/nonces in WSV (with bounded retention) and add cross-node/restart integration tests proving replay rejection after peer switch; document retention knobs.【crates/iroha_core/src/state.rs:11498】【crates/iroha_core/tests/ivm_corehost_axt.rs:1497】【docs/amx.md:21】
 - [x] Unified documentation: consolidate host + policy enforcement rules, cache semantics, and troubleshooting into one doc/runbook with per-reject remediation guidance and links to telemetry metrics.
 - [x] Golden fixtures: publish happy/reject multi-DS fixtures (handles/proofs/touches) plus reusable test helpers; add a “golden refresh” script with schema pinning and CI guard.【crates/iroha_data_model/tests/fixtures/axt_descriptor_multi_ds.json:1】【crates/iroha_data_model/tests/fixtures/axt_envelope_multi_ds.json:1】【crates/iroha_data_model/tests/fixtures/axt_poseidon_constants.json:1】【crates/iroha_data_model/src/bin/axt_fixtures.rs:1】【ci/check_axt_fixtures.sh:1】【crates/iroha_data_model/tests/axt_envelope_fixture.rs:1】
 - [x] Observability: attach reject reason + snapshot version to Torii/block AXT responses; ensure telemetry/logs include per-envelope failure causes and propose alert templates for common rejects. Torii now stamps `X-Iroha-Axt-*` headers and ISO bridge rejection codes use `PRTRY:AXT_*` so operators can scrape reasons without decoding payloads.
 - [x] Config discoverability: surface AXT knobs (slot length, skew, replay retention, cache TTLs) in `iroha_config` defaults; add docs/examples and SDK config readers that consume the same fields. `/v1/configuration` now exports the `nexus.axt` block and JS/Swift readers normalise the values for clients.【crates/iroha_config/src/client_api.rs:1】【javascript/iroha_js/src/toriiClient.js:6436】【IrohaSwift/Sources/IrohaSwift/ToriiClient.swift:1】【docs/source/references/configuration.md:53】


1. **AGENTS-CONFIG-SURFACE — Prefer iroha_config over env toggles** (Config/Runtime, Line: Shared, Owner: Config, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Refresh the env-toggle inventory/ownership map and stage migrations per subsystem (Torii/P2P/pipeline/IVM/FastPQ/SDKs) with target config sections and deprecation warnings where shims remain test-only. Inventory now tags `build.rs` + `#[cfg(test)]` + debug-guarded scopes (including `dev_env_flag` checks), reclassifying CUDA build flags and harness envs, and was regenerated Dec 07, 2025.
 - [x] Migrate remaining production env knobs into `iroha_config` defaults (user->actual) and host constructors; env shims (`IROHA_P2P_TOPOLOGY_UPDATE_MS`, `IVM_COMPILER_DEBUG`, `NORITO_CRC64_GPU_LIB`/`NORITO_GPU_CRC64_MIN_BYTES`, `NORITO_DISABLE_PACKED_STRUCT`, `TORII_DEBUG_SORT`) are removed entirely so both release and debug/test builds rely solely on configuration/defaults.
 - [x] Extend CI/pre-commit guardrails to fail on new env toggles in production paths and add regression tests proving each migrated knob is sourced from config (host/unit/integration as appropriate). Guard now diffs the env inventory against `AGENTS_BASE_REF` and fails on new production shims unless `ENV_CONFIG_GUARD_ALLOW=1` is explicitly set.
 - [x] Update config/operator docs plus the env inventory/migration tracker with timelines, and leave TODO breadcrumbs for any still-pending shims with assigned owners/ETA. Tracker refreshed with the new scope classification and the release guards above; next sweeps run alongside config-surface updates.


1. **P2P-DATASPACE-GOSSIP — Isolate public vs permissioned data spaces** (P2P/Core, Line: Shared, Owner: Network WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Target selection: derive per-dataspace peer sets from lane manifests/Space Directory (validators, relay policy, visibility) with cache/expiry semantics; define deterministic fallback (e.g., commit topology) and a drop policy when metadata is stale.【crates/iroha_core/src/gossiper.rs:120】
 - [x] Overlay separation: add distinct overlays/topics for public vs restricted dataspaces (tx gossip, trust gossip, lane relay, DA/RBC); enforce accept-side gating that rejects mixed-plane deliveries with stable error codes.【crates/iroha_core/src/lib.rs:226】【crates/iroha_p2p/src/network.rs:5298】
 - [x] Payload handling: refuse restricted-dataspace payloads when only public overlays are available (until encrypted fallback ships) and surface per-attempt telemetry/error labels for blocked fallbacks.【crates/iroha_core/src/gossiper.rs:386】【crates/iroha_telemetry/src/metrics.rs:7898】
 - [x] Config surface: expose dataspace-aware gossip knobs (`p2p.overlay.{public,restricted}`, per-topic caps, routing strategy, fallback policy) in `iroha_config`; wire through P2P network, gossipers, and lane relay.【crates/iroha_config/src/parameters/actual.rs:3121】【crates/iroha_config/src/parameters/user.rs:6328】
 - [x] Telemetry/ops: per-topic caps plus per-dataspace gossip/skip counters (public vs restricted), `/status` views showing chosen targets per lane/dataspace, and alerts for fallback-to-public/drop events.【crates/iroha_telemetry/src/metrics.rs:7898】【crates/iroha_core/src/telemetry.rs:811】
 - [x] Tests: integration suites mixing public/restricted lanes covering routing/targets/drop paths, lane-relay scoping, trust/peer gossip gating, DA/RBC overlays, and stale/missing metadata fallback; add unit tests for target selection and gating.【crates/iroha_core/src/gossiper.rs:860】
 - [x] Docs/runbooks: update P2P/Nexus docs with the dataspace-aware model, config examples, failure/alert taxonomy, and operator checklist for co-hosting public+restricted lanes; include troubleshooting for fallback/denial cases.【docs/source/references/configuration.md:10】


1. **NPOS-VALIDATOR-LIFECYCLE — Admission/retirement for public vs permissioned networks** (Consensus/Staking/Core, Line: Shared, Owner: Sumeragi/Stake WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Activation path (public NPoS):
 - [x] Add a governance/election instruction or scheduled hook to flip `PendingActivation` -> `Active` at epoch boundaries.【crates/iroha_core/src/state.rs:3914】【crates/iroha_core/src/smartcontracts/isi/staking.rs:87】
 - [x] Track activation epoch/height monotonically; reject out-of-order activations; tests for epoch-boundary activation and roster refresh.【crates/iroha_core/src/smartcontracts/isi/staking.rs:150】【crates/iroha_core/src/smartcontracts/isi/staking.rs:1772】
 - [x] Telemetry/metrics for activation events; Norito payloads documented.【crates/iroha_core/src/telemetry.rs:1281】【docs/source/nexus_public_lanes.md:44】
 - [x] Genesis/permissioned handling:
 - [x] Keep genesis peers `Active` without staking admission and document the invariant.
 - [x] In permissioned mode, ensure admin multisig `RegisterPeer`/`Unregister` bypasses staking guards; add regression covering admin path.【crates/iroha_core/src/smartcontracts/isi/staking.rs:62】【crates/iroha_core/src/smartcontracts/isi/staking.rs:1957】
 - [x] Exit/unregister:
 - [x] Add an exit instruction (`ExitPublicLaneValidator`) that sets `Exiting(releases_at_ms)` -> `Exited`, unblocks capacity, and gates rewards.
 - [x] Allow re-registration after `Exited`; add invariants/tests for capacity release, reward stop, slash/unbond interplay, and duplicate prevention while Exiting.
 - [x] Roster gating on peers:
 - [x] Require a live `Peer` entry (with address) before accepting `RegisterPublicLaneValidator`; fail fast with clear error/telemetry.【crates/iroha_core/src/smartcontracts/isi/staking.rs:956】【crates/iroha_core/src/telemetry.rs:1169】【crates/iroha_core/src/smartcontracts/isi/staking.rs:1567】
 - [x] Make `StakeSnapshot` drop validators whose peer is missing/disabled; tests for missing-peer gating and address changes.【crates/iroha_core/src/state.rs:4337】【crates/iroha_core/src/state.rs:4718】
 - [x] Peer unregister coupling:
 - [x] On `Unregister<Peer>`, jail/eject linked validators and prune them from rosters; ensure bonded stake/slash paths stay consistent.【crates/iroha_core/src/smartcontracts/isi/world.rs:7321】【crates/iroha_core/src/smartcontracts/isi/staking.rs:1701】
 - [x] Regression: peer removal while staked frees capacity and prevents dangling signers in consensus.【crates/iroha_core/src/smartcontracts/isi/staking.rs:1701】
 - [x] Mixed-mode branching:
 - [x] Add lane/network config selecting stake-elected activation for public lanes vs peer-admin for permissioned lanes; default sane values.【crates/iroha_config/src/parameters/actual.rs:1472】【crates/iroha_core/src/state.rs:4945】
 - [x] Tests for mixed deployments (public + restricted lanes) proving the correct path per lane and preventing cross-mode leakage.【crates/iroha_core/src/smartcontracts/isi/staking.rs:1957】【crates/iroha_core/src/state.rs:4938】【crates/iroha_core/src/state.rs:5018】
 - [x] CLI/Torii/docs/tests:
 - [x] Update staking/NPoS docs and CLI/Torii help with activation/exit lifecycle, peer prerequisites, mode selection, and genesis behavior.
 - [x] Add API/SDK/Torii docs for new instructions/endpoints; add regression tests for pending->active, capacity leak prevention, peer removal effects, re-registration after exit/slash, and mixed-mode behavior.


1. **AGENTS-DETERMINISM-ACCEL — Hardware acceleration with deterministic fallback** (IVM/Crypto/Performance, Line: Shared, Owner: IVM/Crypto, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Choose the next accel targets (ZK/FastPQ hotspots) with profiling evidence and baseline budgets so CPU-only/simd-off runs stay canonical. Metal trace `bench_trace.json` (16×2 048 columns) shows `poseidon_hash_columns` CPU 13.94 ms vs GPU 13.97 ms and `lde` CPU 5.53 ms vs GPU 3.91 ms; budgets and owners are captured in the accel runbook.【docs/source/config/acceleration.md:117】
 - [x] Implement shared accel toggle/config plumbing (METAL/NEON/SIMD/CUDA) with deterministic CPU fallbacks, keeping syscalls/opcodes shipped regardless of hardware. Host accel shims now thread the SIMD switch alongside GPU knobs in `apply_ivm_acceleration_config`, and `acceleration-state` exposes SIMD status for operators.【crates/irohad/src/main.rs:2830】【crates/ivm/src/lib.rs:178】【xtask/src/main.rs:567】
 - [x] Expand tests/benches to run accel-on/off across architectures (Metal vs CPU, optional CUDA) and assert identical outputs plus telemetry budget labels. SIMD parity now covers poseidon instructions, vector ops, and Merkle roots with accel toggled off/on, alongside existing Poseidon parity and CUDA/Metal guards.【crates/ivm/tests/crypto.rs:57】【crates/ivm/tests/crypto.rs:139】【crates/ivm/tests/acceleration_simd.rs:17】
 - [x] Update docs/comments with accel/fallback behaviour, operator knobs, and parity test recipes; leave TODO breadcrumbs for hardware-specific paths still pending.【docs/source/config/acceleration.md:1】【docs/source/metal_neon_acceleration_plan.md:24】
 - [x] SIMD observability: `acceleration_runtime_errors` now distinguishes config-disabled, forced-scalar, and hardware-missing cases; regression covers clearing overrides, and the acceleration guide documents the error strings for operators.【crates/ivm/src/lib.rs:401】【crates/ivm/tests/acceleration_simd.rs:74】【docs/source/config/acceleration.md:79】


1. **AGENTS-DOC-TEST-POLICY — Docs, tests, and PR hygiene** (Docs/QA, Line: Shared, Owner: QA/Docs, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Regenerate the missing-docs inventory for crates touched by recent changes, then queue follow-ups to add inner `//!` and item docs instead of `allow(missing_docs)`. Inventory now lives at `docs/source/agents/missing_docs_inventory.{json,md}` via `scripts/inventory_missing_docs.py` with guardrail freshness checks.【scripts/inventory_missing_docs.py:1】【docs/source/agents/missing_docs_inventory.md:1】【ci/check_missing_docs_guard.sh:1】
 - [x] Wire a guard that fails when changed functions lack at least one unit test (inline or crate `tests/`) and keep `cargo test --workspace` the default merge gate; encourage optional `cargo clippy -- -D warnings`. Guard maps changed lines to owning functions and requires a matching test reference (existing or new) instead of auto-passing on touched tests, so unrelated test edits no longer bypass coverage checks.【ci/check_tests_guard.sh:1】【ci/check_tests_guard.py:1】【ci/tests/test_check_tests_guard.py:1】
 - [x] Fill proc-macro trybuild coverage gaps for macros still missing UI diagnostics and stabilise `.stderr` fixtures; pair UI suites with unit tests for helpers.
 - [x] Add a lightweight lint/pre-commit/CI hook that blocks dropping TODO markers or skipping docs/tests on touched functions, and document the policy in CONTRIBUTING/dev workflow. TODO guard wired into Makefile/pre-commit/agents workflow (`ci/check_todo_guard.sh`) and documented alongside the new skip knob.【ci/check_todo_guard.sh:1】【Makefile:1】【hooks/pre-commit.sample:1】【docs/source/dev_workflow.md:27】【CONTRIBUTING.md:34】


1. **JS-SDK-DX — Developer experience for the JS SDK** (SDK, Line: Shared, Owner: mtakemiya (SDK WG), Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Norito fallback: wire the pure-JS Norito path when native bindings are missing/disabled; add JS-only unit tests + CI job; surface a clear error message with a “JS mode” hint; document the native-free flow in README/examples.
 - [x] Native build ergonomics: stop `getNativeBinding` from auto-building; gate builds behind an explicit `npm run build:native` (or postinstall) with a fast failure message when natives are absent; keep the loader async/non-blocking and log the exact env/steps to enable native mode.
 - [x] Packaging/exports: publish built ESM artifacts with subpath exports (`@iroha/iroha-js/torii`, `/norito`, `/crypto`, `/offline`); trim the export map to hide internals; add a bundle-size/tree-shake check (skips when esbuild is unavailable) to keep subpaths minimal.
 - [x] Surface shaping: regroup public API into namespaces (Torii client, crypto, Norito helpers, offline recipes), deprecate the monolithic barrel by re-exporting shims with warnings, and update `index.d.ts`/README to show the new import paths plus a migration guide.
 - [x] JS-only CI coverage: run the `npm run test:js` suite with the native binding disabled in CI and gate integration runs on both native and JS-only jobs to keep the fallback path green.【.github/workflows/javascript-sdk.yml:1】


1. **SDK-SWIFT-DEVEX — Clarify bridge dependency and make signing deterministic** (SDK/Swift, Line: Shared, Owner: Swift SDK WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] NoritoBridge policy: default builds require `dist/NoritoBridge.xcframework` with `IROHASWIFT_USE_BRIDGE=optional/0` enabling Swift-only/disabled modes; Connect/crypto/TxBuilder errors surface the bridge-mode hint.
 - [x] Bridge coverage: Connect codec + transfer encoder now have regressions for the bridge-disabled path (using the runtime overrides) that assert `bridgeUnavailable` errors carry the env hint.
 - [x] SwiftPM guardrails: Package.swift fails fast when the xcframework is missing unless `IROHASWIFT_USE_BRIDGE=optional/0` is set, emits a fallback warning, and threads an explicit `useBridge` boolean into the target defines/docs.
 - [x] SPM validation: add an SPM resolution/build job (with and without the bridge) to CI to lock the manifest behaviour; fail the job when the bridge is missing and `useBridge` is on.【scripts/check_swift_spm_validation.sh:1】【ci/check_swift_spm_validation.sh:1】【.github/workflows/swift-packaging.yml:1】
 - [x] Deterministic signing: add an injectable clock (creationTimeMs provider) to `TxBuilder`/`SwiftTransactionEncoder` and queue replay; wire default to `Date()` and allow overrides; add unit tests proving hash stability under a fixed clock and advancement under real time.
 - [x] Input validation: introduce ID validators or small wrapper types for chain/account/asset IDs in Transfer/Mint/Burn/etc.; fail fast with clear errors before bridge/Torii; add negative fixtures and assertion tests in `IrohaSwiftTests`.
 - [x] Swift parity fixtures: add a regen helper to rebuild `swift_parity_*` fixtures from payload JSON and refresh the Swift parity manifest/fixtures to match current encoder output.【crates/connect_norito_bridge/src/bin/swift_parity_regen.rs】【IrohaSwift/Fixtures/swift_parity_manifest.json】
 - [x] CocoaPods packaging: refresh `IrohaSwift.podspec` metadata (author/version/homepage/license matches repo); define how NoritoBridge is sourced (hosted URL vs vendored path) and add a `pod lib lint`/CI step that fails when the framework is missing.【IrohaSwift/IrohaSwift.podspec:1】【scripts/check_swift_pod_bridge.sh:1】【.github/workflows/swift-packaging.yml:1】【ci/check_swift_pod_bridge.sh:1】
 - [x] Install docs: README + Swift quickstart now call out `IROHASWIFT_USE_BRIDGE` values, bridge-present/absent behaviour, and the fallback/disabled troubleshooting steps.


1. **NEXUS-AMX-AUDIT — Close AXT/AMX cross-dataspace gaps** (Nexus/IVM/Core/Telemetry, Line: Nexus, Owner: Nexus Core WG, Priority: High, Status: 🈴 Completed, target TBD) - [x] Data model + WSV surfaces: define Norito structs/schema for per-DS AXT fragments (touch/proof/handle) with lane binding and commit markers; thread executor outputs into WSV/block artifacts with codec roundtrips and gossip/replication hooks. - [x] Admission/scheduler validation: add pre-exec checks rejecting conflicting/partial fragments and keep per-tx state hashes stable, including deterministic reschedule/rollback handling.
 - [x] Proof/PVO verification + cache: implement proof-service client integration binding proofs to DS roots/DA commitments with expiry checks; add a verify-once-per-slot cache with telemetry reasons and retry/backoff policy using `expiry_slot`.
 - [x] UAID manifest/role enforcement: evaluate Space Directory manifests for `AXT_TOUCH`/`USE_ASSET_HANDLE`; enforce lane binding, era/sub-nonce monotonicity, manifest_root match, expiry/clock skew, target_lane, and deterministic budget consumption with replay protection.
 - [x] WSV policy surface (schema + data): add a `DataspaceAxtPolicy` Norito struct and extend WSV/Space Directory snapshots with `HashMap<DataSpaceId, policy>` plus helper `current_slot = current_time_ms / slot_length_ms` (slot length configurable or defaulted).
 - [x] WSV policy persistence: add an `axt_policies` map to State/WSV populated from Space Directory manifest activation/expiry/revocation (manifest hash -> manifest_root, lane/era/sub_nonce/slot), and emit `AxtPolicySnapshot` for hosts/gossip.
 - [x] Host/admission wiring: default CoreHost/DefaultHost construction pulls `AxtPolicySnapshot` from State; admission rejects handles on lane/manifest_root/era/sub_nonce/expiry mismatch with telemetry counters for each reason.
 - [x] Block/gossip persistence: include `AxtPolicySnapshot` (or hash) in block metadata/relay payloads and hydrate it deterministically during sync.
 - [x] Policy implementation: replace `WsvAxtPolicy` with `SpaceDirectoryAxtPolicy::from_snapshot(map, current_slot)` enforcing expiry_slot > current_slot, target_lane, manifest_root, handle_era >= min_handle_era, sub_nonce >= min_sub_nonce (unknown DS -> PermissionDenied); leave binding/scope/budget/proof checks untouched.
 - [x] Host wiring: CoreHost now derives AXT policy from WSV Space Directory snapshots (lane catalog + manifest roots/activation era/current slot) instead of allow-all defaults; WsvHost refreshes policy at `AXT_BEGIN`/state restore/current_time updates.
 - [x] Tests/fixtures/docs: add accept/deny unit tests for lane/root/expiry/era/sub_nonce + missing DS for CoreHost via injected snapshots; add golden policy/handle/descriptor vectors; document policy fields/failure modes in AMX/AXT docs (WsvHost coverage landed; CoreHost now has policy snapshot/WSV tests).【crates/iroha_data_model/tests/fixtures/axt_golden.rs:1】【crates/iroha_data_model/tests/axt_policy_vectors.rs:1】【crates/ivm/tests/core_host_policy.rs:800】【docs/amx.md:71】
 - [x] Budget/telemetry: run `ivm::analysis::enforce_amx_budget` per descriptor, map overruns to `AMX_TIMEOUT`/`AMX_LOCK_CONFLICT` with DS labels, align heavy-instruction allowlists/PVO requirements across hosts, and surface metrics (`iroha_amx_prepare_ms`, `iroha_amx_commit_ms`, abort counters) plus receipt labels.
 - [x] Tests/docs: add cross-DS commit/failure coverage (missing/expired proof, manifest deny, handle replay/budget/expiry/lane mismatch, cache cold/hit, heavy-instruction gating) with golden bindings/proof vectors; update `docs/amx*.md`, `docs/source/nexus.md`, status, SDK runbooks, and operator checklist for cache validation/telemetry alarms.


2. **SUM-QC-COVERAGE — Harden Sumeragi QC/vote validation with tests** (Consensus/Sumeragi, Status: 🈴 Completed, target TBD)
   - Subject binding: QC validation now rejects votes whose block hash differs from the QC subject with a regression guarding the path.
   - NEW_VIEW gating: HighestQC on NEW_VIEW must be a commit QC or a prepare QC at the target height; validation rejects other phases.
   - Lock extension: non-extending precommit QCs are rejected by the lock-extension helper.
   - Lock/phase safety: unit tests for rejecting precommit QCs that do not extend `locked_qc`; prevent lock regression on same-height hash mismatch; drop NEW_VIEW with wrong-phase highest QC; assert lock remains after back-to-back view changes.
   - Roster/view/epoch binding: enforce bitmap length == roster and signer IDs in range; reject QCs replayed across roster/epoch change; NEW_VIEW highest QC must match current view/epoch; add roster-change regression covering bitmap length increase/decrease.
   - Bitmap edge cases: reject empty bitmap; count duplicate bits once; reject high-bit-only sparse maps beyond roster; reject oversized bitmaps early; ensure signer-count math matches min_votes_for_commit across small/large rosters.
   - Payload/DA handling: QC-first arrival without payload must not lock/commit; with `da_enabled=true`, availability evidence is tracked (not local RBC delivery) while commit proceeds once payload is available; DA-disabled path still handles QC-first safely; add DA timeout + DA-disabled regression.
   - Byzantine leader: equivocation with two proposals same height/view emits evidence, accepts only extending QC, ignores conflicting QC; QC for already committed height with divergent hash rejected; rebuild ignores invalid signatures and refuses forged QC.
   - Recovery/sync: node starting from snapshot consumes highest QC but still enforces lock and DA before progressing; late payload arrival triggers QC rebuild and commit if quorum valid; integration test: QC-first then payload-late happy path; integration test: sync via checkpoint + highest QC with missing payload triggers block fetch before lock/commit.
   - Evidence validation: enforce cryptographic vote signature checks (Torii evidence ingest validates against commit topology), rejecting forged payloads before persistence.【crates/iroha_core/src/sumeragi/evidence.rs:383】【crates/iroha_torii/src/routing.rs:4432】
   - Commit QC integrity: validate aggregate signature/bitmap/quorum on receipt before caching/persisting; regressions cover valid/invalid aggregates.【crates/iroha_core/src/sumeragi/main_loop/qc.rs:1118】【crates/iroha_core/src/sumeragi/main_loop/tests.rs:4443】
   - Witness routing: exec witnesses target deterministic collectors per `(height, view)` with commit-topology fallback when collectors are empty/local-only/below quorum; tests/docs updated.【crates/iroha_core/src/sumeragi/main_loop/commit.rs】【crates/iroha_core/src/sumeragi/main_loop/tests.rs】【docs/source/sumeragi.md】
   - NEXT: refine into executable work items:
     1) Double-vote evidence:
        - [x] Added a shared `record_double_vote` helper and prevote/precommit regressions to persist/deduplicate equivocation evidence across the in-memory store and WSV, covering duplicate suppression on repeated votes.
     2) DA sequencing (QC-first/DA-required):
        - [x] Simulate QC arrival before payload with `da_enabled=true`, assert no lock/commit until payload fetch succeeds while availability evidence is tracked (pending block test now exercises QC-first + DA-enabled with late payload and availability QC).【crates/iroha_core/src/sumeragi/main_loop.rs:13940】
        - [x] Simulate DA-disabled path to prove QC-first still progresses (liveness) without gating (pending block regression keeps gates open when DA is off even if RBC is enabled).【crates/iroha_core/src/sumeragi/main_loop.rs:13960】
        - [x] Add late payload fetch path that triggers QC rebuild -> lock/commit; payload fetch proceeds independently of availability status and records `availability evidence` when observed.【crates/iroha_core/src/sumeragi/main_loop.rs:4181】【crates/iroha_core/src/sumeragi/main_loop.rs:15672】
     3) NEW_VIEW validation tightening:
        - [x] Reject NEW_VIEW frames whose highest QC is not commit or same-height prepare, or whose height/view regress relative to the local pacemaker/locked QC via a freshness guard.
        - [x] Regression tests cover stale-view drops and locked-QC height regression alongside the existing wrong-phase coverage.
     4) RBC resync:
        - [x] Clear missing-block fetch state once payloads arrive so retries are allowed immediately; regression confirms `touch_missing_block_request` reissues after `clear_missing_block_request`.【crates/iroha_core/src/sumeragi/main_loop.rs:12136】
        - [x] Pending-block replacement for new payload hashes resets timestamps and vote state so late payload fetches can trigger QC rebuild and precommit emission cleanly.【crates/iroha_core/src/sumeragi/main_loop.rs:15973】
        - [x] Ensure QC-first with missing block triggers block fetch before lock/commit when the payload is absent; fetch planning now falls back to the commit topology when signer hints are empty/out of range with regression coverage.【crates/iroha_core/src/sumeragi/main_loop.rs:1228】【crates/iroha_core/src/sumeragi/main_loop.rs:12208】【crates/iroha_core/src/sumeragi/main_loop.rs:12272】
        - [x] Cover missing-block resync path: after fetch, lock/commit proceeds while availability status records `availability evidence` when it arrives (or immediately when DA is off).【crates/iroha_core/src/sumeragi/main_loop.rs:14230】【crates/iroha_core/src/sumeragi/main_loop.rs:14258】
     5) Bitmap/roster replay:
        - [x] Integration-style test where a QC from an old roster (bitmap length/signers mismatch) is replayed after roster change and is rejected; include signer-index mismatch coverage.【crates/iroha_core/src/sumeragi/main_loop.rs:14634】
     6) Evidence persistence assertions:
        - [x] Integration test that feeds two conflicting proposals at same height/view, confirms evidence is recorded in WSV, and subsequent duplicates are ignored.【crates/iroha_core/src/sumeragi/evidence.rs:1558】
7) QC+block pairing + fetch enforcement:
 - [x] Add invariants/tests that paired block+QC gossip is the common case; drop/park orphaned QCs until payload fetch succeeds and assert the fetch retry/backoff path.【crates/iroha_core/src/sumeragi/main_loop.rs:1248】
 - [x] Add integration coverage for block-only and QC-only delivery (with/without DA), proving we resync deterministically and never advance locks/commit on orphaned artifacts.【crates/iroha_core/src/sumeragi/main_loop.rs:12664】【crates/iroha_core/src/sumeragi/main_loop.rs:12719】【crates/iroha_core/src/sumeragi/main_loop.rs:14952】
 - [x] Add a property/telemetry check that missing-block fetch is always issued to QC signers first (fallback to commit topology) and that lock/highest_qc are untouched until payload arrival.【crates/iroha_core/src/sumeragi/main_loop.rs:12957】
 - [x] Add a deterministic harness that replays QC->payload and payload->QC arrival orders across views/epochs to pin expected logs/metrics.【crates/iroha_core/src/sumeragi/main_loop.rs:13121】
 - [x] Add a smoke test that gossips paired block+QC together and asserts no redundant fetch requests/logs are emitted in the happy path.【crates/iroha_core/src/sumeragi/main_loop.rs:13353】
 - [x] Add a regression for empty/out-of-range signer targets to ensure missing-block fetch falls back to the full commit topology (with a warning when no peers are available).【crates/iroha_core/src/sumeragi/main_loop.rs:9033】【crates/iroha_core/src/sumeragi/main_loop.rs:14131】
 - [x] Add metrics assertions for “requested missing block payload” log/telemetry fields (targets, dwell_ms) to aid ops alerts.【crates/iroha_core/src/sumeragi/main_loop.rs:9345】【crates/iroha_core/src/sumeragi/main_loop.rs:12664】【crates/iroha_core/src/sumeragi/main_loop.rs:12719】【crates/iroha_core/src/sumeragi/status.rs:159】【crates/iroha_core/src/sumeragi/status.rs:2742】【crates/iroha_core/src/telemetry.rs:6867】【crates/iroha_core/src/telemetry.rs:6875】【crates/iroha_core/src/telemetry.rs:6886】【crates/iroha_torii/src/routing/consensus.rs:147】【crates/iroha_torii/src/routing/consensus.rs:1715】
 8) Finality proof surface hygiene:
 - [x] Clarify the canonical finality proof tuple (header + block hash + commit certificate), document the choice, and add a regression that rejects forged certificates whose hash disagrees with the stored block.【crates/iroha_core/tests/bridge_finality_proof.rs:90】【docs/source/bridge_finality.md:18】
 - [x] Add regressions for replayed finality proofs across epochs/rosters; the light-client verifier now anchors validator-set hashes and epochs, rejecting mismatched rosters/epochs with deterministic errors.【crates/iroha_data_model/src/bridge.rs:560】【crates/iroha_data_model/src/bridge.rs:588】【crates/iroha_data_model/src/bridge.rs:910】
 - [x] Add light-client proof verifier tests for wrong-chain_id and stale/advanced height to ensure rejects with stable errors.【crates/iroha_data_model/src/bridge.rs:519】【crates/iroha_data_model/src/bridge.rs:827】
 - [x] Add a bounded retention test ensuring finality proofs beyond the retention window are pruned and that fetch errors are surfaced cleanly.【crates/iroha_core/tests/bridge_finality_proof.rs:149】
 - [x] Add a replay test where a valid proof from a prior epoch/roster is rejected after topology change with a precise error label.【crates/iroha_data_model/src/bridge.rs:920】【docs/source/bridge_finality.md:72】
 9) Byzantine/DoS edge cases:
 - [x] Fuzz signer bitmaps vs. vote content (e.g., signer index points to a different key than the signature) and ensure verification catches the mismatch before aggregation.【crates/iroha_core/src/sumeragi/main_loop.rs:16693】
 - [x] Exercise adversarial leaders withholding blocks while spamming highest-QC NEW_VIEW frames; assert pacemaker/view-change throttles progress without violating locks and that evidence is emitted once a valid block is seen.【crates/iroha_core/src/sumeragi/main_loop.rs:247】【crates/iroha_core/src/sumeragi/main_loop.rs:14069】
 - [x] Add a test that drops duplicate/high-bit-only bitmaps with mismatched signatures and records evidence without progressing locks.【crates/iroha_core/src/sumeragi/main_loop.rs:16790】
 - [x] Add a pacing guard regression for repeated NEW_VIEW without payload that ensures pacemaker backoff and evidence emission once a valid block/QC arrives.【crates/iroha_core/src/sumeragi/main_loop.rs:14069】
 - [x] Add a stress test where equivocation is detected across prevote and precommit phases in the same view, ensuring deduped evidence and no lock regression.【crates/iroha_core/src/sumeragi/evidence.rs:1721】
 - [x] Add telemetry assertions for bitmap/signature mismatch paths so alerts can key off consensus errors rather than generic invalid-QC; QC validation now tags bitmap length/out-of-bounds/insufficient-or-missing-vote/subject-mismatch/invalid-signature failures into labeled counters across handle_qc/NEW_VIEW/block-sync, with mapping + disabled-telemetry regressions for alerting.【crates/iroha_core/src/sumeragi/main_loop.rs:9296】【crates/iroha_core/src/sumeragi/main_loop.rs:9676】【crates/iroha_core/src/telemetry.rs:4086】【crates/iroha_core/src/sumeragi/main_loop.rs:15668】
 10) RBC/DA fetch gating:
 - [x] Integration test where QC arrives first, payload is missing, and block fetch must complete before lock/commit (DA on/off).【crates/iroha_core/src/sumeragi/main_loop.rs:12208】【crates/iroha_core/src/sumeragi/main_loop.rs:14230】【crates/iroha_core/src/sumeragi/main_loop.rs:14292】
 - [x] Add retry/backoff assertion for `touch_missing_block_request` so liveness is maintained without premature lock updates.
 - [x] Add a DA-disabled variant proving QC-first still progresses after payload fetch without waiting on availability QC.【crates/iroha_core/src/sumeragi/main_loop.rs:14292】
 - [x] Add metrics/log invariants for retry windows and first-seen dwell times so operational alerts can key off stalled fetches.【crates/iroha_core/src/sumeragi/main_loop.rs:9085】【crates/iroha_core/src/telemetry.rs:4035】【crates/iroha_telemetry/src/metrics.rs:5051】
 - [x] Add a timed-out transport scenario that triggers a fetch and later payload arrival, asserting we commit only after both payload and (when DA-on) availability QC arrive.【crates/iroha_core/src/sumeragi/main_loop.rs:14699】
 - [x] Add a regression ensuring availability-QC arrival without payload still keeps the gate closed until payload fetch completes.【crates/iroha_core/src/sumeragi/main_loop.rs:14187】
 - Remaining gaps to close SUM-QC-COVERAGE:
 - [x] Fuzz signer bitmap/signature mismatches (index swap/different key) so verification rejects before aggregation and telemetry captures the reason.
 - [x] Simulate adversarial leaders spamming NEW_VIEW with highest-QC while withholding payloads; assert pacemaker backoff, evidence emission, and lock safety until a valid block arrives.【crates/iroha_core/src/sumeragi/main_loop.rs:247】【crates/iroha_core/src/sumeragi/main_loop.rs:18428】
 - [x] Add a regression for duplicate/high-bit-only bitmaps with mismatched signatures that records evidence without advancing locks and tags telemetry for alerting.
 - [x] Stress-test equivocation across prevote and precommit in the same view with deduped evidence and stable locks/highest_qc.【crates/iroha_core/src/sumeragi/main_loop.rs:8650】【crates/iroha_core/src/sumeragi/evidence.rs:1744】
 - [x] Add a pacing guard test for repeated NEW_VIEW without payload that proves liveness once payload+QC eventually arrive and enforces backoff in the meantime.【crates/iroha_core/src/sumeragi/main_loop.rs:18428】


1. **IROHA-CORE-TEST-STABILIZATION — Repair failing iroha_core test suite** (Core/Testing, Status: 🈴 Completed, target TBD)
 - Kura persistence/cache:
 - [x] `fast_init` tamper/truncation: reproduce hash-file truncation and tamper paths, ensure hash manifests are regenerated and truncated block data pruned (`fast_init_prunes_truncated_block_data`, `fast_init_rewrites_tampered_hash_file`).【crates/iroha_core/src/kura.rs:4652】【crates/iroha_core/src/kura.rs:4690】
 - [x] Block cache rebuild: make cached-bytes path return `None` on missing data and repopulate cache on read (`get_block_returns_none_when_data_missing`, `get_block_caches_loaded_block`, `deep_history_get_block_uses_cached_bytes`).【crates/iroha_core/src/kura.rs:4019】【crates/iroha_core/src/kura.rs:4053】【crates/iroha_core/src/kura.rs:4084】
 - [x] Merge ledger/log truncation: enforce log truncation on prune/rollback and preserve ledger entries across restart (`merge_ledger_entries_persist_across_restart`, `merge_log_truncated_when_block_store_pruned`, `kura_not_miss_replace_block`).【crates/iroha_core/src/kura.rs:3532】【crates/iroha_core/src/kura.rs:3615】
 - [x] Strict init corruption: drop corrupted index segments on startup and verify reindex (`strict_init_prunes_corrupted_index_end_to_end`).【crates/iroha_core/src/kura.rs:4569】
 - Consensus key policy/HSM:
 - [x] Allow-listed algorithms: align `register_consensus_key_*` expectations with policy config and error surfaces; add regression covering allowed/denied algorithms with precise errors.【crates/iroha_core/src/state.rs:9797】【crates/iroha_core/src/smartcontracts/isi/world.rs:9171】
 - [x] HSM gating: thread HSM-required flag through admission and lifecycle history; add coverage for HSM-required vs optional paths and rotation status (`register_consensus_key_requires_hsm_when_configured`, `rotate_consensus_key_requires_hsm_when_configured`, `rotate_consensus_key_allows_missing_hsm_when_optional`).【crates/iroha_core/src/smartcontracts/isi/world.rs:9311】【crates/iroha_core/src/smartcontracts/isi/world.rs:9395】
 - IVM host/syscalls:
 - [x] Wire syscalls 36/37/224 into the CoreHost dispatcher: test programs now encode ABI v1 so pointer-ABI transfers/NFT sentinels/INPUT_PUBLISH_TLV reach the host instead of returning `UnknownSyscall`, with regressions covering the three surfaces.【crates/iroha_core/src/smartcontracts/ivm/host.rs:1468】【crates/iroha_core/src/smartcontracts/ivm/host.rs:2218】
 - [x] Pointer-ABI TLV: wrong type IDs now surface `NoritoInvalid` with a regression covering POINTER_FROM_NORITO mismatches.【crates/ivm/tests/pointer_abi_tests.rs:19】
 - Streaming config:
 - [x] Bundle width guard now caps `bundle_width` at the SignedRansTablesV1 width during parsing, with a failing fixture/regression for oversized widths.【crates/iroha_config/tests/fixtures/bad.streaming_bundle_width.toml:1】【crates/iroha_config/tests/fixtures.rs:1720】
 - Soranet incentives numerics:
 - [x] `numeric_to_nanos` now guards nanos overflow and returns `None` when conversion would exceed `u128`, with boundary/overflow coverage so reward fixtures keep realistic conversions.【crates/iroha_core/src/soranet_incentives.rs:515】【crates/iroha_core/src/soranet_incentives.rs:808】
 - Repo/settlement/sorafs/staking retention/telemetry:
 - [x] Repo proofs: refresh lifecycle proof fixtures and deterministic ordering (`repo_deterministic_lifecycle_proof_matches_fixture`) with a helper to regenerate goldens under pinned toolchains.
 - [x] Settlement DvP: ensure commit-first/second roll back on payment spec errors with stable telemetry (`dvp_commit_first_keeps_delivery_on_payment_spec_error`, `dvp_commit_second_rolls_back_on_payment_spec_error`) and add asserts on ledger state and alerts.
 - [x] Sorafs capacity/telemetry: de-flake retention/penalty/cooldown expectations and capacity fee ledger soak tests; document expected ranges/tolerances for ops.
 - [x] Staking retention: verify prune window on retention entries (`retention_prunes_entries_older_than_grace_window`) and add fixture to lock the grace-window math.【crates/iroha_core/src/smartcontracts/isi/world.rs:5370】
 - Data triggers/fixtures:
 - [x] Refresh `aborts_on_execution_error` expect JSONs and guard transfers against negative balances so chained triggers roll back cleanly (`atomically_chains_from_{time,transaction}`).【crates/iroha_core/src/smartcontracts/isi/asset.rs:139】【crates/iroha_core/tests/fixtures/data_trigger/aborts_on_execution_error-txn.json:1】【crates/iroha_core/tests/fixtures/data_trigger/aborts_on_execution_error-time.json:1】
 - Block sync candidate rejection:
 - [x] `candidate_prev_block_hash_mismatch` now feeds a mismatched hash so ShareBlocks validation rejects bad chains deterministically with the correct error surface.【crates/iroha_core/src/block_sync.rs:739】
 - Election determinism:
 - [x] Make seat bands/correlation caps deterministic across runs and align expected seat counts (`election_is_deterministic_and_capped`, `seat_band_allows_extra_validators_and_correlation_limits_entities`) with fixtures that pin random seeds and roster variations.【crates/iroha_core/src/sumeragi/election.rs:164】【crates/iroha_core/src/sumeragi/election.rs:889】



1. **NEXUS-MULTILANE-GAPS — Enablement + CI/docs cleanup** (Nexus/Core/QA, Line: Nexus, Owner: @nexus-core, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Turn on Nexus when multi-lane catalogs are present or `--sora` is used: set the nexus profile/defaults to `enabled = true`, fail fast when `lane_count > 1` but `nexus.enabled` is false, and add unit tests for `apply_sora_profile`/`should_use_config_router` plus the sample config in `defaults/nexus/config.toml` to lock the behaviour.【crates/iroha_config/src/parameters/{defaults,actual}.rs】【crates/irohad/src/main.rs】【defaults/nexus/config.toml】
 - [x] Wire `uses_sora_features`/`enforce_build_line` to emit a clear error when multi-lane catalogs appear without `nexus.enabled`.
 - [x] Add regression that `--sora` flips `nexus.enabled` and produces the Sora catalog hashes, and that `lane_count == 1` preserves single-lane defaults (two tests: one for CLI flag, one for explicit config parse).
 - [x] Snapshot updated `defaults/nexus/config.toml` hashes in the tracker/status files used by the rehearsal/runbook so enablement is auditable.
 - [x] Restore multi-lane regression coverage + CI: add an integration/py test that drives ConfigLaneRouter with a 3-lane catalog and asserts lane-local Kura/merge-log provisioning and routing, and wire a CI target for it to replace the missing `pytests/nexus/test_multilane_pipeline.py` / `ci/integration_tests_multilane.yml` references.【crates/iroha_core/src/queue.rs】【crates/iroha_core/src/kura.rs】【integration_tests/tests/nexus/multilane_router.rs:1】【integration_tests/tests/nexus/multilane_pipeline.rs:1】【ci/check_nexus_multilane.sh:1】【ci/check_nexus_multilane_pipeline.sh:1】
 - [x] Exercise lane-specific storage layout (`lane_{id:03}_{slug}` / merge logs) and routing paths (governance/zk rules) with assertions on produced block dirs and telemetry labels.【integration_tests/tests/nexus/multilane_router.rs:1】【integration_tests/tests/nexus/multilane_pipeline.rs:1】
 - [x] Add a CI entry (agents guardrail or workflow) that runs the multilane test suite and publishes artefact hashes to avoid future drift (pin job name + path, e.g., `.github/workflows/integration_tests_multilane.yml`).【ci/check_nexus_multilane.sh:1】【ci/check_nexus_multilane_pipeline.sh:1】【.github/workflows/integration_tests_multilane.yml:1】
 - [x] Include a negative test ensuring Iroha2 build-line still rejects multi-lane configs to keep split release lines safe.【crates/irohad/src/main.rs:2802】
 - [x] Fix docs/runbooks pointing at absent artefacts once coverage exists: update Nexus transition notes/runbook and status references to cite the new test/CI job and document the required `nexus.enabled = true`/config hash for the Nexus profile.【docs/source/nexus_transition_notes.md:1】【docs/source/runbooks/nexus_multilane_rehearsal.md】【status.md】
 - [x] Replace the dead `pytests/nexus/test_multilane_pipeline.py` reference with the new test path and CI job name.【docs/source/nexus_transition_notes.md:1】
 - [x] Refresh `defaults/nexus/config.toml` hash pointers in the tracker/runbook once the enablement flag ships, and ensure `status.md` links to the validated artefacts plus any new telemetry pack manifests from the multilane CI run.
 - [x] Add a short operator checklist in the runbook noting the `nexus.enabled` requirement, the expected lane catalog digest, and where to find the multilane CI artefacts.【docs/source/runbooks/nexus_multilane_rehearsal.md】【status.md】


1. **SORAFS-SORANET-AUTHZ — Gate SoraFS/SoraNet surfaces with RBAC and ownership** (Core/Torii/Config/Docs, Line: Shared, Owner: Storage/Gov WG, Priority: High, Status: 🈴 Completed, target TBD)
 - **RBAC tokens + enforcement**
 - [x] Define data-model + executor tokens (names/payloads) for: pin register/approve/retire/alias; capacity declare/telemetry/dispute; replication order issue/complete; pricing set; provider credit upsert; SoraNet privacy ingest.
 - [x] Wire tokens into `declare_permissions!` and instruction registry; add grant/revoke validation rules and negative tests for unknown/unauthorised callers.
 - [x] Add allow/deny unit tests for replication-order issue/complete paths in `isi/sorafs.rs` to prove permissions are enforced and deterministic; expand to the remaining instructions.
 - **Ownership binding**
 - [x] Introduce provider->account binding storage (WSV table + query) populated from config/genesis/CLI; add invariants/tests for uniqueness and deletion.
 - [x] Enforce ownership or delegated role on provider-scoped actions (declare/telemetry/dispute/orders/pricing/credits) with specific error codes; add regression tests for spoofed provider IDs and missing bindings.
 - [x] Scope telemetry submitter allow-lists per provider, default `require_submitter/require_nonce=true`, expose config knobs, and test bypass attempts/fallbacks.
 - **Torii API authn/z**
 - [x] Add authn (signed token or mTLS) + rate limits to `/v1/sorafs/storage/pin`; integrate with config surface; add tests for auth success/failure, quota exhaustion, and abuse throttling.【crates/iroha_torii/src/sorafs/api.rs:2695】【crates/iroha_torii/src/sorafs/api.rs:8742】
 - [x] Add authn/z + rate limits to `/v1/soranet/privacy/{event,share}`; gate ingestion on config flag + token; add poisoning/DoS regressions and telemetry counters.
 - [x] Add optional namespace restriction (trusted subnets) for both endpoints with deny-by-default tests when not configured.
 - **Docs + tooling**
 - [x] Update docs/runbooks/CLI help to show the governance workflow (who can register/approve/retire pins, issue orders, submit telemetry), new tokens, provider binding, and endpoint auth requirements.
 - [x] Add operator checklist for provisioning tokens, binding providers, rotating submitter allow-lists, and rolling endpoint credentials; include sample configs and CLI snippets.
 - [x] Add CI guard or lint to reject new unauthenticated SoraFS/SoraNet endpoints and ensure docs stay aligned with the enforced model.【ci/check_soranet_privacy_guard.sh:1】【ci/check_agents_guardrails.sh:1】

### AGENTS Task Breakdown
 - [x] Owners/priorities recorded for all AGENTS items (Line: Shared across Iroha 2/Nexus unless noted). - [x] DevEx: contributor runbook + helper target wrapping `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo build/test --workspace`, and `swift test` with runtime caveats; pre-commit/CI guards that block `Cargo.lock` edits and unapproved new workspace members, enforce fmt/clippy/test by default, and nudge large changes into TODO-backed follow-ups. Delivered via `docs/source/dev_workflow.md`, `scripts/dev_workflow.sh` (`make dev-workflow`), `.github/workflows/agents-guardrails.yml`, `ci/check_agents_guardrails.sh`, and the guard hook in `hooks/pre-commit.sample`. - [x] DevEx: dependency discipline—guardrails fail on new dependencies/workspace members and lockfile edits via `ci/check_agents_guardrails.sh` (Makefile, workflow, pre-commit) with `ci/check_dependency_discipline.sh` available as an explicit lint; docs/dev workflow/contributing guides cover the policy, and `make agents-preflight` runs all guards (including missing-docs/std-only/status-sync). - [x] Config: inventory env toggles and replace production knobs with `iroha_config` user->actual->defaults parameters threaded through constructors/hosts; keep env overrides only in dev/test harnesses, document defaults, and add regressions proving production paths source config only. Progress: block scheduler/overlay traces now live under `pipeline.debug_trace_scheduler_inputs`/`pipeline.debug_trace_tx_eval`, Torii filter debug tracing is `torii.debug_match_filters`, P2P topology updates now warn on `IROHA_P2P_TOPOLOGY_UPDATE_MS` and use config cadence, Torii attachments/webhooks/DA spools use `torii.data_dir` (env ignored outside tests), governance pipeline tracing is `governance.debug_trace_pipeline`, and the duplicate-metric panic shim is limited to debug builds with docs updated. Env toggle inventory/guard stays current via `scripts/inventory_env_toggles.py` + `docs/source/agents/env_var_inventory.{json,md}` and `ci/check_env_config_surface.sh` (`make check-env-config-surface`, `.github/workflows/env-config-guard.yml`).
 - [x] Removed production env overrides for peer gossip/topology cadence in favour of config-driven intervals with regression coverage and warnings for retired envs.
 - [x] Added `governance.debug_trace_pipeline` to replace the `IROHA_TRACE_PIPELINE` env shim and documented the knob; `torii.data_dir` now seeds the Torii persistence root with OverrideGuard reserved for tests/dev.
 - [x] Scoped the `IROHA_METRICS_PANIC_ON_DUPLICATE` env shim to debug/test builds with the config knob as the production source.
 - [x] Removed IVM env shim for non-v1 ABI opt-out (`IVM_ALLOW_NON_V1_ABI`) and banner suppression (`IVM_SUPPRESS_BANNER`); compiler now rejects non-v1 unconditionally and banner suppression stays programmatic only.
 - [x] Retired IVM cache/GPU/prover env shims (`IVM_CACHE_CAPACITY`, `IVM_CACHE_MAX_BYTES`, `IVM_MAX_DECODED_OPS`, `IVM_MAX_GPUS`, `IVM_PROVER_THREADS`) in favour of config knobs (`pipeline.{cache_size,ivm_cache_max_bytes,ivm_cache_max_decoded_ops,ivm_prover_threads}`, `accel.max_gpus`) and runtime setters (`ivm::ivm_cache::configure_limits`, `ivm::zk::set_prover_threads`); tests now use `CacheLimitsGuard` instead of env overrides and the env inventory was regenerated. - [x] Publish an env->config migration tracker (owners, priority, target config section) so any remaining toggles are sequenced and reviewed. - [x] Classified remaining production env toggles with owner/target-config proposals in `docs/source/agents/env_var_migration.md` to stage the next migration sweep. - [x] Connect diagnostics resolve `connect.queue.root` (default `~/.iroha/connect`) with the `IROHA_CONNECT_QUEUE_ROOT` shim gated to dev/test via an explicit `allowEnvOverride` opt-in; JS helpers accept config/rootDir, config templates add the knob, and docs/inventory were refreshed. - [x] Add config-driven `ivm.banner.{show,beep}` defaults (replacing the `IROHA_BEEP` shim) and honour them when rendering the startup banner/jingle; dev/test-only env overrides remain available for diagnostics. - [x] Fence the DA spool override behind `cfg(test)` helpers (`IROHA_DA_SPOOL_DIR`) and mark it test-only in the env inventory; production paths rely solely on configured spool directories. - [x] Migrate remaining production env toggles from the inventory into `iroha_config` defaults and constructors with follow-up tests/docs. FastPQ Metal tuning now flows through `fastpq.metal_{max_in_flight,threadgroup_width,metal_trace,metal_debug_enum,metal_debug_fused}`, applied at lane startup via `fastpq_prover::apply_metal_overrides`, which also freezes the `FASTPQ_METAL_*`/`FASTPQ_DEBUG_*` env shims as dev/test-only fallbacks once configuration loads. Docs, the env inventory, and the migration tracker were refreshed, and unit coverage maps the config -> override struct.【crates/iroha_core/src/fastpq/lane.rs:109】【crates/fastpq_prover/src/overrides.rs:11】【docs/source/agents/env_var_migration.md:37】【docs/source/agents/env_var_inventory.md:1】
 - [x] Fence IVM debug env toggles (`IVM_DECODE_TRACE`, `IVM_DEBUG_WSV`, `IVM_DEBUG_COMPACT`, `IVM_DEBUG_INVALID`, `IVM_DEBUG_REGALLOC`, Metal/CUDA debug/force/self-test shims) to debug/test builds via a shared helper so production ignores them; regenerate the env inventory/migration tracker.【crates/ivm/src/dev_env.rs:1】【crates/ivm/src/vector.rs:235】【crates/ivm/src/cuda.rs:314】【docs/source/agents/env_var_inventory.md:1】【docs/source/agents/env_var_migration.md:1】
 - [x] Move SM intrinsic overrides to the `crypto.sm_intrinsics` config policy (`auto`/`force-enable`/`force-disable`), drop the `IROHA_{DISABLE,ENABLE}_SM_INTRINSICS` and `IROHA_SM_OPENSSL_PREVIEW` shims, and keep the bench/test-only override on `CRYPTO_SM_INTRINSICS`.
 - [x] Remove the `IROHA_ALLOW_NET` shim; Izanami requires the `allow_net` CLI/config flag and persists the setting via its TUI/config snapshot.
 - [x] Izanami: persist pipeline/target/progress settings in config snapshots with Norito defaults for legacy payloads; add roundtrip/legacy decode tests.【crates/izanami/src/persistence.rs:1】
 - [x] Izanami: validate TPS is finite and yields a non-zero schedule interval; add config + TUI regression tests.【crates/izanami/src/config.rs:1】【crates/izanami/src/tui.rs:1】
 - [x] Izanami: reject ultra-low TPS values that would overflow timer intervals; add regression coverage.【crates/izanami/src/config.rs:1】
 - [x] Izanami: fix TUI CLI override detection for fault toggles (arg-id mismatch) and add coverage.【crates/izanami/src/main.rs:1】
 - [x] Izanami: wake load/fault loops promptly on stop signals to avoid waiting for long intervals; add stop-notify coverage.【crates/izanami/src/chaos.rs:1】【crates/izanami/src/faults.rs:1】
 - [x] Izanami: drain completed submission tasks to prevent unbounded handle growth; add drain helper coverage.【crates/izanami/src/chaos.rs:1】
 - [x] Izanami: clamp fault-loop sleep to the remaining deadline to avoid overshooting run duration; add bounded-delay coverage.【crates/izanami/src/faults.rs:1】
 - [x] Confined `FASTPQ_UPDATE_FIXTURES` to FASTPQ integration tests so production sources no longer read it; regenerated the env inventory/migration tracker after moving the Stage 2 proof fixture check out of `src` tests.【crates/fastpq_prover/src/digest.rs:1】【crates/fastpq_prover/src/proof.rs:1】【crates/fastpq_prover/tests/proof_fixture.rs:1】【docs/source/agents/env_var_inventory.md:441】【docs/source/agents/env_var_migration.md:1】 - [x] Codec/IVM: Norito-only serialization sweep replacing direct `serde`/`serde_json` with `norito` helpers; run `scripts/check_no_scale.sh`; add CI guardrails against new serde deps; add regressions for Norito headers (including genesis) plus canonical `SignedBlockWire` encoding/decoding. *Delivered: guardrails + inventories stay green, builder JSON roundtrips cover account/asset/NFT registration paths, and Norito docs/migration guide are refreshed.*
 - [x] Inventory direct `serde`/`serde_json` usage and deps across crates (e.g., `scripts/check_no_direct_serde.sh` + targeted `rg`) with hotspots tagged; refreshed `docs/source/norito_json_inventory.{json,md}` via `scripts/inventory_serde_usage.py` (flagged production hits now 0 with scope summary).
 - [x] Draft CI/pre-commit denylist lint to block new serde deps/usages and wire it into the AGENTS guardrail set (serde/serde_json/AoS guards now run in `make agents-preflight` and the AGENTS workflow; denylist script glob handling fixed).
 - [x] Replace highest-impact serde call sites with `norito` helpers and add Norito roundtrip coverage for the touched types (including `iroha_data_model` builders). NewAccount/NewAssetDefinition builders now carry Norito `default` + deny-unknown JSON attrs, FastJsonWrite implementations, and roundtrip/unknown-field tests so registration payloads stay canonical.【crates/iroha_data_model_derive/src/registrable_builder.rs:136】【crates/iroha_data_model/src/account.rs:1396】【crates/iroha_data_model/src/account.rs:1439】【crates/iroha_data_model/src/account.rs:1455】【crates/iroha_data_model/src/asset/definition.rs:706】【crates/iroha_data_model/src/asset/definition.rs:745】
 - [x] Add Norito header/genesis roundtrip + canonical `SignedBlockWire` regressions to enforce advertised layouts (no decode heuristics); new unit coverage locks canonical vs framed bytes and genesis deframe parity.【crates/iroha_data_model/src/block/mod.rs:1773】【crates/iroha_data_model/src/block/mod.rs:1802】
 - [x] Wire `scripts/check_no_scale.sh` into `make agents-preflight`/CI so SCALE stays isolated to the Norito benchmark harness.
 - [x] Finish the Norito JSON migration checklist (enum derives/visitor helpers, config loader swap, snapshot codec/manifest cleanup per `docs/source/norito_json_migration.md`), removing TODO markers and guarding via the serde inventory.【docs/source/norito_json_migration.md:1】
 - [x] Refresh `norito.md`/codec docs and fixtures to reflect any layout/flag updates made during the sweep.【norito.md:1】 - [x] IVM: ABI v1 enforcement—keep `abi_syscall_list` ordered, map unknown syscalls to `VMError::UnknownSyscall`, maintain pointer-type IDs/policy mapping, refresh goldens/tests (`abi_syscall_list_golden.rs`, `abi_hash_versions.rs`, `pointer_type_ids_golden.rs`), update `crates/ivm/docs/syscalls.md`, and ensure admission/manifests tests cover `abi_hash` match/mismatch. *Host/admission coverage now includes a static syscall scan that rejects unknown numbers before execution plus manifest `abi_hash` enforcement across metadata and WSV manifests with docs synced to the guardrails.*
 - [x] Re-run ABI goldens (`abi_syscall_list`/pointer-type IDs/hash versions) to capture the current baseline and note any drift; `abi_syscall_list` is now deduped/sorted with goldens asserting ordering, and Experimental(1|2) map to the v1 surface while hashing separately.【crates/ivm/src/syscalls.rs:276】【crates/ivm/tests/abi_syscall_list_golden.rs:6】【crates/ivm/tests/pointer_type_ids_golden.rs:15】
 - [x] Fix ordering/unknown-syscall handling and refresh `abi_syscall_list_golden.rs`, `abi_hash_versions.rs`, and `pointer_type_ids_golden.rs` alongside host wiring as needed.【crates/ivm/src/pointer_abi.rs:97】【crates/ivm/docs/syscalls.md:17】
 - [x] Add host/admission regressions for unknown syscalls and `abi_hash` match/mismatch (including manifest enforcement) across IVM/host boundaries. Admission now scans decoded bytecode for `SCALL` instructions and rejects syscall numbers outside the active ABI policy before runtime, and manifest `abi_hash` mismatches raise structured errors for both inline and WSV-stored manifests—including metadata vs. state conflicts—with regression coverage.【crates/iroha_core/src/tx.rs:1110】【crates/iroha_core/src/tx.rs:2714】【crates/iroha_core/tests/ivm_manifest_abi_reject.rs:136】【crates/iroha_core/tests/ivm_syscall_policy.rs:70】
 - [x] Update `crates/ivm/docs/syscalls.md`, roadmap/status notes, and any ABI tables to reflect the refreshed surface.【crates/ivm/docs/syscalls.md:17】【status.md:3】 - [x] IVM/Crypto: deterministic acceleration—Poseidon2/6 opcodes are the first target; accel-on/off parity locks Metal/CUDA config toggles with CPU fallbacks and optional CUDA checks, and docs/tests capture the workflow.
 - [x] Select the first acceleration target syscall/opcode (Poseidon2/6) based on current profiling and IVM hotspots.
 - [x] Design the deterministic accel/fallback shape (feature detection + config wiring) that keeps outputs identical and ships all syscalls/opcodes; `AccelerationConfig` now drives CPU-only vs accel-on modes with runtime status assertions to mirror host policy.
 - [x] Add benches/tests that toggle accel on/off and compare outputs across architectures, capturing expected budgets/telemetry. Regression `poseidon_instructions_match_across_acceleration_configs` runs Poseidon opcodes twice (accel disabled/enabled) and cross-checks CUDA outputs when present.【crates/ivm/tests/crypto.rs:100】
 - [x] Document acceleration toggles/fallback expectations in code comments and docs, including operator-facing knobs and the parity test recipe.【docs/source/config/acceleration.md:75】 - [x] QA/Docs: docs/testing hygiene—sweep crates to remove `allow(missing_docs)`, require crate-level docs, ensure each new/modified function gains at least one unit test (inline or `tests/`), add trybuild UI coverage for proc macros, tag partial impls with `TODO:`, and keep PR templates capturing change summary + `Testing` commands. *Status: proc-macro UI coverage + docs/testing guardrails in place; keep diagnostics stable as surfaces evolve.*
 - [x] Extend the missing-docs guard into a lint/pre-commit hook that fails on changed crates lacking crate-level docs or newly introduced undocumented items; the guard now scans crate roots and new public definitions in diffs.
 - [x] Sweep existing `#[allow(missing_docs)]` allowances crate-by-crate, replacing them with real docs or `TODO:` markers and adding at least one unit test per touched function.
 - [x] Inventory proc-macro crates lacking trybuild UI coverage and add harnesses/tests for them (e.g., `norito_derive` and other derive/proc crates). - [x] Capture stable, non-panicking diagnostics in trybuild `.stderr` fixtures and document the proc-macro testing policy/guard in the dev workflow/CONTRIBUTING. - [x] Update PR template/contributor docs with explicit doc/test evidence expectations and the new hook invocation.
 Progress: CI/pre-commit guard `ci/check_missing_docs_guard.sh` blocks new `#[allow(missing_docs)]` additions, enforces crate-level docs for touched crates (including bin-only crates), and fails on new public items without `///` docs; PR template/contributor guide call out `make check-missing-docs` and doc/test evidence expectations. The sweep now covers the data-model visitor/ISI layers and derive builders: `#[model]`/`model_single!` inject docs for items/fields/variants, visitor helpers and instruction registries are documented, and instruction wire IDs plus Sumeragi NPoS getters carry docs so no `allow(missing_docs)` shims remain. trybuild UI suites now exercise the entrypoint/parameter/permission macros across `iroha_primitives_derive`, `iroha_executor_derive`, `iroha_executor_data_model_derive`, `iroha_smart_contract_derive`, and `iroha_trigger_derive` with pass/fail fixtures and golden `.stderr` outputs for the negative paths, anchoring proc-macro diagnostics to stable fixtures; executor custom parameters implement `Identifiable` so the derives’ conversions now compile cleanly. *Next: keep diagnostics stable as surfaces evolve.*【crates/iroha_data_model/src/visit/mod.rs】【crates/iroha_data_model_derive/src/model.rs】【crates/iroha_data_model_derive/src/registrable_builder.rs】【crates/iroha_data_model/src/isi/mod.rs】【crates/iroha_data_model/src/parameter/{system,custom}.rs】【crates/iroha_primitives_derive/tests/ui.rs】【crates/iroha_executor_data_model_derive/tests/ui.rs】【crates/iroha_smart_contract_derive/tests/ui.rs】【crates/iroha_trigger_derive/tests/ui.rs】【crates/iroha_executor_derive/tests/ui.rs】 - [x] Build: std-only posture—`ci/check_std_only.sh` blocks `no_std`/`wasm32` cfgs in code/CI (Makefile, agents-preflight, pre-commit, AGENTS workflow), wasm `.cargo` configs were removed, and compute/crypto spike docs now note the std-only stance. - [x] PM/Docs: status hygiene—`ci/check_status_sync.sh` (`make check-status-sync`) now fails when `roadmap.md` or `status.md` change without the other (override via `STATUS_SYNC_ALLOW_UNPAIRED=1` with `AGENTS_BASE_REF` pinned), docs/runbooks call out the stricter guard, and the PR template requires paired roadmap/status edits.【ci/check_status_sync.sh:1】【docs/source/dev_workflow.md:35】【CONTRIBUTING.md:30】【.github/pull_request_template.md:1】


1. **KAGAMI-NPOS-NETWORKS — Generate NPoS-ready networks with Kagami** (Tooling/Genesis, Status: 🈴 Completed, target TBD)
 - [x] Extend `kagami localnet`/`swarm` to accept `--consensus-mode {permissioned,npos}` and propagate the choice into generated genesis/configs/Compose manifests with NPoS cutover wiring through the swarm signing path.【crates/iroha_kagami/src/swarm.rs:137】【crates/iroha_swarm/src/schema.rs:50】【crates/iroha_kagami/src/localnet.rs:37】【crates/iroha_kagami/src/genesis/sign.rs:50】
 - [x] Allow localnet/swarm generation to set `--mode-activation-height` (default none) so staged NPoS activation is reproducible; fail fast when NPoS is requested without `sumeragi_npos_parameters` via the shared helper.【crates/iroha_kagami/src/genesis/npos.rs:16】【crates/iroha_kagami/src/swarm.rs:145】【crates/iroha_kagami/src/localnet.rs:77】
 - [x] Add doc snippets/workflows for spinning up NPoS devnets via Kagami (bare-metal and Docker), including how to supply VRF seeds/rosters and how to pass PoPs/topology at sign time.【crates/iroha_kagami/README.md:45】【crates/iroha_kagami/docs/swarm.md:97】
 - [x] Tests: regenerate localnet/swarm fixtures under both modes; add sanity checks that generated configs parse and that NPoS manifests carry `next_mode` + `mode_activation_height` when requested.【crates/iroha_kagami/src/localnet.rs:848】【crates/iroha_swarm/src/schema.rs:177】
 - [x] Enforce Iroha3 NPoS-only consensus with no staged cutovers across Kagami generate/sign/localnet/swarm, propagate BLS PoPs into swarm signing, and update Kagami swarm/NPoS docs to use the `--next-consensus-mode` + activation-height pair.【crates/iroha_kagami/src/genesis/generate.rs:410】【crates/iroha_kagami/src/genesis/sign.rs:86】【crates/iroha_kagami/src/swarm.rs:145】【crates/iroha_swarm/src/schema.rs:74】【crates/iroha_kagami/docs/swarm.md:67】


1. **SUM-MODE-CUTOVER — Make consensus mode transitions operable and explicit** (Consensus/Genesis/Config, Status: 🈴 Completed, target TBD)
 - [x] Add an explicit `--next-consensus-mode` + `--mode-activation-height` pair to `kagami genesis generate`/`sign`, stamping `next_mode` and `mode_activation_height` together while keeping `consensus_mode` as the pre-activation fingerprint; fail fast when only one is provided.
 - [x] Tighten staging guard: reject manifests that set `mode_activation_height` without `next_mode` (and vice versa, unless `next_mode` simply restates the configured mode) and make handshake fingerprints track the effective runtime mode until activation with regressions.
 - [x] Docs/runbooks: show permissioned-only vs staged permissioned->NPoS cutover, expected `mode_tag`/fingerprint before and after activation, and the governance `SetParameter` pair required for staging.
 - [x] Integration tests: staged manifests keep the permissioned fingerprint until activation height, peers compute matching pre/post-activation fingerprints across nodes, and manifest `consensus_fingerprint` mismatches fail startup; runtime cutover flips to NPoS at the activation height with status/params parity and rejects mismatched manifest modes.【integration_tests/tests/sumeragi_mode_cutover.rs:244】【integration_tests/tests/sumeragi_mode_cutover.rs:311】【integration_tests/tests/sumeragi_mode_cutover.rs:419】【crates/irohad/src/main.rs:4256】


1. **SUM-PEER-SIG-HARDENING — Harden peer/block signature validation** (Consensus/Core/Sumeragi, Status: 🈴 Completed, target TBD)
 - Vote/RBC signature enforcement
 - [x] Remove the warn-and-accept fallback for invalid BLS votes/READY/DELIVER frames; reject on signature failure across all algorithms and emit a consistent error tag.
 - [x] Add unit tests (BLS/Ed25519/secp) for bad signatures on Vote/AvailableVote/READY/DELIVER paths; assert rejection and telemetry counters/labels for `invalid_signature`.
 - [x] Wire log/metric throttling so repeated bad frames do not spam logs; add a regression ensuring per-peer rate limits trigger.【crates/iroha_core/src/sumeragi/main_loop.rs:607】【crates/iroha_core/src/sumeragi/main_loop.rs:2727】【crates/iroha_core/src/telemetry.rs:4608】【crates/iroha_telemetry/src/metrics.rs:5397】【crates/iroha_core/src/sumeragi/main_loop.rs:17985】
 - [x] Ensure gossip/block-sync handlers drop invalid signatures before queueing; add harness coverage for mixed-valid/invalid batches.【crates/iroha_core/src/block_sync.rs:629】【crates/iroha_core/src/block_sync.rs:873】
 - QC validation hardening
 - [x] Require votes for every bit set in `signers_bitmap`; remove the “missing votes with consistent aggregate” acceptance path and keep telemetry for missing-vote rejects.
 - [x] Validate BLS aggregate signatures against signer bitmaps/topology and vote preimages; add tests for forged bitmap, subject hash, height/view/epoch, and signer-order changes.
 - [x] Add a guard that signers_bitmap length matches roster and bits beyond roster cause rejection; cover with unit and integration tests.
 - [x] Reject QCs whose BLS aggregate signature fails verification for the declared bitmap/signers; add negative tests for signature mismatch.
 - [x] Update block-sync/new-view consumers to enforce the stricter QC validation before lock/commit.
 - Block commit quorum correctness
 - [x] Count validated leader + all validators (Set A/B, incl. proxy tail) toward quorum; track Set B signatures separately for visibility.
 - [x] Reject duplicate signer indices and mismatched signature keys; add regressions for leader+proxy-tail+set-B spoof attempts and duplicate-index signatures.
 - [x] Align telemetry to report both “present signatures” and “counted quorum signatures” so ops can see Set B participation alongside quorum health.【crates/iroha_telemetry/src/metrics.rs:5413】【crates/iroha_core/src/telemetry.rs:6727】【crates/iroha_core/src/sumeragi/main_loop.rs:6270】
 - [x] Ensure commit path re-runs signature validation on replacement signatures (block sync/admission) with tests for rollback on failure.
 - Consensus-key lifecycle coverage
 - [x] Enforce consensus-key liveness/expiry for leader and proxy-tail roles (not just validators) with tests for expired/missing keys across all roles.【crates/iroha_core/src/block.rs:2599】【crates/iroha_core/src/block.rs:6603】
 - [x] Add config/WSV-driven grace window tests for overlap/expiry to prove enforcement matches parameters.【crates/iroha_core/src/block.rs:6634】
 - [x] Guard consensus-key lookup against missing roster entries during view changes and block sync; block-sync filters now reapply lifecycle checks and drop expired rotations with regression coverage for leader/validator/proxy signatures.【crates/iroha_core/src/block.rs:2629】【crates/iroha_core/src/block_sync.rs:354】
 - Quorum rules for small topologies
 - [x] Restore 3-of-4 commit threshold and update collector/proxy-tail math, telemetry, and tests for 4-node deployments; add a regression for 2-of-4 being rejected.【crates/iroha_core/src/block.rs:6396】
 - [x] Recompute any dependent thresholds (collector selection, proxy-tail index) and update docs/status to match the new quorum rule.【crates/iroha_core/src/sumeragi/network_topology.rs:940】
 - [x] Add integration tests covering 1/2/3/4-node topologies to confirm quorum math and proxy-tail selection remain consistent across rotations.【crates/iroha_core/src/sumeragi/network_topology.rs:940】【crates/iroha_core/src/block.rs:6425】


1. **GENESIS-BOOTSTRAP-GOSSIP — Fetch genesis from a trusted peer** (Bootstrap/P2P/Core, Line: Shared, Owner: mtakemiya, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Protocol surface:
 - Define `GenesisRequest` (chain id + request id with optional expected hash/pubkey) and `GenesisResponse` (hash, pubkey, size hint, optional error) frames under P2P control with `Preflight`/`Fetch` kinds; cap payload size and disable responses from non-whitelisted peers (trusted set or explicit allowlist in config).
 - Include a metadata-only preflight before sending the blob so mismatches can abort early and dedupe responses by `request_id`.
 - [x] Requester flow:
 - On empty storage with no `genesis.file`, send preflight to trusted peers with configurable retry/backoff; accept only matching chain id and configured genesis pubkey.
 - If preflight hash matches (or none provided), fetch blob, run `validate_genesis_block`, reject/abort on multiple distinct hashes or signature failures, and persist the signed blob before applying it.
 - Persist the signed blob in the existing format, apply via the current commit path, and cache the hash/pubkey locally for future boots.
 - [x] Responder flow:
 - Serve only when local genesis is present and matches the configured chain id/pubkey; refuse if storage is empty or hash differs from config.
 - Honour payload cap and rate-limit responses with allowlist/trusted-peers gating and duplicate-request guards.
 - [x] Tests:
 - Unit: responder whitelist/caps, preflight pubkey checks, size-cap rejection.
 - Integration: happy-path fetch plus negative paths were added; compilation of the wider suite is currently blocked by an existing `iroha_config` Option/`config(default)` conflict unrelated to the bootstrapper.
 - [x] Docs/runbook:
 - Operator steps to enable bootstrap-from-peer (configure trusted peer + expected genesis pubkey/hash, optional allowlist flag).
 - Failure modes and recovery (no responder, hash mismatch, invalid signature, size cap, multiple hashes); note local caching after first fetch and how to pin the fetched file for subsequent boots.


2. **KAGAMI/MOCHI IROHA3 PROFILES — One-switch genesis for dev/testus/nexus** (Tooling/Genesis, Line: Iroha3, Owner: Tooling WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Kagami profiles: add `--profile {iroha3-dev, iroha3-testus, iroha3-nexus}` that pins build-line=iroha3, DA/RBC on, pre-canned chain IDs, collectors_k/r, gas limit, and VRF seed rules (dev derives from chain id; testus/nexus require explicit seed).
 - [x] Profile validation UX: enforce roster/PoP completeness per profile (dev allows 1 peer; testus/nexus require ≥4), reject conflicting overrides, and render a post-run summary (chain, DA/RBC, collectors_k/r, VRF seed presence, consensus fingerprint, kagami version).
 - [x] Verification command: add `kagami verify --profile... --genesis...` to replay profile expectations, VRF seed rules, roster/PoPs, and consensus fingerprint before shipping bundles.
 - [x] Mochi integration: surface the same profiles in `mochi-genesis`, forward to Kagami, and emit the Kagami summary in logs/CLI output.
 - [x] Docs: add quick recipes for iroha3-dev/testus/nexus, including required inputs (seed, PoPs), expected outputs (fingerprint, summary), and failure modes for profile validation.【docs/source/kagami_profiles.md】
 - [x] Tests: add profile-specific fixtures covering DA/RBC flags, VRF seed handling, roster/PoP completeness, and `kagami verify` negative/positive paths; mirror through Mochi profile wiring.
 - [x] Developer QoL: ship sample config/genesis bundles for each profile (`defaults/kagami/iroha3-{dev,testus,nexus}/`) with a `kagami verify` transcript and a `docker-compose` snippet for smoke runs; add `cargo xtask kagami-profiles --profile <...>` to regenerate them.【xtask/src/kagami_profiles.rs】【defaults/kagami/iroha3-dev】【defaults/kagami/iroha3-testus】【defaults/kagami/iroha3-nexus】

27. **ACCOUNT-IDENTITY-REFACTOR — Canonical account IDs + alias/UAID unification** (Core/Data Model/Torii/SDK, Line: Shared, Owner: Core Protocol WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Canonicalize AccountId to IH58-only wire/display format (no `@domain`), and define a resolver spec for accepted inputs (`alias@domain`, `public_key@domain`, `uaid:<hex>`, katakana address, etc.); treat this as a first-release breaking change.
 - [x] Add WSV index: `uaid -> account` (1:1 enforced).
- [x] Add WSV indices: `alias@domain -> account`, `domain_selector -> domain`, and `opaque_id -> uaid` for PII-safe tokens; block raw email/phone values from on-chain storage.
 - [x] Enforce UAID uniqueness at admission/onboarding, and replace UAID scans/portfolio aggregation with direct lookup; errors on duplicates are invariant violations.
- [x] Refactor Torii routing/address parsing to canonicalize all account inputs without config gating (remove `strict_addresses` and ISO-only alias resolver assumptions).
- [x] Update SDKs/CLI/bridges to accept the new input forms and always render IH58; refresh docs/examples for Sora Nexus account addressing.
- [x] Tests: unit coverage for UAID uniqueness and index behavior.
- [x] Tests: unit + integration coverage for alias collisions, domain-selector reverse index determinism, and input canonicalization.

28. **CLI-OUTPUT-NORMALIZATION — Unify CLI outputs/flags for first release** (Tooling/CLI, Line: Shared, Owner: DX WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Normalize output formats and error handling across `iroha_cli` subcommands (JSON vs text summaries).
 - [x] Align flag naming to avoid collisions with `--output-format` (handshake token `--token-encoding`).
 - [x] Capture CLI output normalization status in `status.md`.

33. **CI-TODO-CLEANUP — Resolve open TODOs in GitHub workflows/labeler** (Tooling/CI, Line: Shared, Owner: Release Eng, Priority: Low, Status: 🈴 Completed, target TBD)
- [x] Update `.github/labeler.yml` so config label templates reflect the actual configuration surfaces (TODO #4288).
- [x] Upload clippy artifacts in the PR workflow and wire the clippy report path into Sonar (`.github/workflows/pr.yml`).

34. **OFFLINE-QR-STORM-FOLLOWUP — Validate sakura-storm transport reliability across scanners** (CLI/SDK/Docs, Line: Shared, Owner: Offline WG, Priority: High, Status: 🈴 Completed, target TBD)
- [x] Add scanner-loop validation for `--style sakura-storm` at the 3 KB/s preset (`chunk_size=336`, `parity_group=4`, `fps=12`) across iOS/Android/browser capture paths (`IrohaSwift/Tests/IrohaSwiftTests/OfflineQrStreamTests.swift`, `java/iroha_android/src/test/java/org/hyperledger/iroha/android/offline/OfflineQrStreamTest.java`, `javascript/iroha_js/test/offlineQrStream.test.js`). 2026-02-15 validation: JS + Swift pass; Android harness run is blocked in this environment by Gradle/JDK parsing (`java.lang.IllegalArgumentException: 25`).
- [x] Mirror sakura-storm playback skin parameters into Swift/Android/JS offline QR helpers so preview appearance stays consistent across SDKs (`IrohaSwift/Sources/IrohaSwift/OfflineQrStream.swift`, `java/iroha_android/src/main/java/org/hyperledger/iroha/android/offline/OfflineQrStream.java`, `javascript/iroha_js/src/offlineQrStream.js`, `javascript/iroha_js/src/index.js`).
- [x] Publish a short operator runbook with recommended `ecc`/dimension/fps combinations for noisy camera environments (`docs/source/offline_qr_operator_runbook.md`, linked from `docs/source/petal_stream.md`).

35. **SORACLOUD-PLATFORM-MVP — IVM + custom container cloud runtime baseline** (Platform/Runtime, Line: Iroha 3, Owner: Core + Networking WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define and freeze `SoraContainerManifestV1`, `SoraServiceManifestV1`, and `SoraStateBindingV1` Norito schemas (versioned, deterministic, with docs + fixtures). (`crates/iroha_data_model/src/soracloud.rs`, `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs`, `fixtures/soracloud/*.json`, `docs/source/soracloud/manifest_schemas.md`)
 - [x] Add deterministic deployment-bundle admission validation (`SoraDeploymentBundleV1`) to bind container hash/schema, enforce mutable-state capability policy, and require public-route health probes (`crates/iroha_data_model/src/soracloud.rs`, `fixtures/soracloud/sora_deployment_bundle_v1.json`).
 - [x] Implement Sora Container Runtime (SCR) host integration (no Docker): process sandbox, capability policy, deterministic admission, resource limits, lifecycle hooks. (Torii deploy/upgrade/rollback admission now enforces deterministic SCR host policy in `crates/iroha_torii/src/soracloud.rs`: bounded resource caps (`cpu_millis`, `memory_bytes`, `ephemeral_storage_bytes`, `max_open_files`, `max_tasks`), lifecycle grace caps (`start_grace_secs`, `stop_grace_secs`), mutable-binding vs `allow_state_writes` capability compatibility, and validated egress allowlist policy. Each admitted revision stores host-runtime snapshot fields (`runtime`, capability flags, network policy, resource/lifecycle limits, deterministic `sandbox_profile_hash`) plus monotonic simulated process lifecycle markers (`process_generation`, `process_started_sequence`) across deploy/upgrade/rollback paths.)
 - [x] Implement service registry + routing control-plane (deploy/status/upgrade/rollback) with signed manifests and audit events (`crates/iroha_torii/src/soracloud.rs`, routes wired in `crates/iroha_torii/src/lib.rs`, CLI network mutations in `crates/iroha_cli/src/soracloud.rs`).
 - [x] Implement deterministic state-binding guardrails so SCR services can only mutate canonical state through IVM-governed bindings (signed `POST /v1/soracloud/state/mutate` in `crates/iroha_torii/src/soracloud.rs` enforces binding prefix, mutability class, encryption policy, and per-binding byte quotas with append-only/read-only protection and auditable governance transaction hash linkage).
 - [x] Add `iroha app soracloud` CLI baseline (`init`, `deploy`, `status`, `upgrade`, `rollback`) with machine-readable output and deterministic local registry/audit simulation (`crates/iroha_cli/src/soracloud.rs`, wired in `crates/iroha_cli/src/main_shared.rs`, with unit coverage).
 - [x] Extend `iroha app soracloud` with network-backed integration tests against live control-plane endpoints (`integration_tests/tests/iroha_cli.rs`: `soracloud_status_uses_live_torii_control_plane`, `soracloud_mutations_use_live_torii_control_plane`; validates live Torii-backed status + deploy/upgrade/rollback flows through CLI `--torii-url` mode).
 - [x] Add telemetry and `/v1/soracloud/*` status surfaces (service health, routing, resource pressure, failed admissions, control-plane registry snapshot) via `GET /v1/soracloud/status` in Torii with telemetry-profile gating, token/rate-limit enforcement, and machine-readable JSON/Norito negotiation (`crates/iroha_torii/src/lib.rs` + `crates/iroha_cli/src/soracloud.rs` network mode).

36. **SORACLOUD-WEB-FOUNDATION — Static + dynamic web frameworks** (DevEx/Web, Line: Iroha 3, Owner: SDK + SoraFS WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Ship `sora init site` template (Vue3/Vite static SPA) with SoraFS publish + SoraDNS binding workflow. (`iroha app soracloud init --template site` now scaffolds `site/` Vue3 starter files, SoraFS packaging flow, and SoraDNS alias-binding command templates in `README.md` via `crates/iroha_cli/src/soracloud.rs`, documented in `docs/source/soracloud/cli_local_control_plane.md`)
 - [x] Ship `sora init webapp` template (Vue3 SPA + API service on SCR) with auth/session + chain identity integration. (`iroha app soracloud init --template webapp` now scaffolds `webapp/frontend` (Vue3/Vite), `webapp/api/server.mjs` (session/auth + chain-ID verification hook), and Soracloud API deploy flow docs via `crates/iroha_cli/src/soracloud.rs`)
 - [x] Implement zero-downtime rollout primitives (canary %, health gates, rollback handles) for site + API bundles. (Torii now exposes signed `POST /v1/soracloud/rollout` with canary promotion, health-failure tracking, and auto-rollback to baseline; `iroha app soracloud` now includes `rollout` (local + `--torii-url` mode), local registry tracks `active_rollout`/`last_rollout`, and live CLI integration covers deploy/upgrade/rollout/rollback flow in `integration_tests/tests/iroha_cli.rs`.)
 - [x] Add framework docs/runbooks for Vue3 SPA + API deployment and production operations. (Added `docs/source/soracloud/vue3_spa_api_runbook.md` and linked it from `docs/source/soracloud/cli_local_control_plane.md` with generation/build/package/deploy/rollout/rollback operational guidance.)
 - [x] Add end-to-end integration tests: build Vue3 app, deploy static site, deploy API, bind route, assert upgrade/rollback correctness. (`integration_tests/tests/iroha_cli.rs`: `soracloud_templates_deploy_site_and_webapp_with_rollout_and_rollback` now scaffolds `site` + `webapp`, validates template `vite build` script presence, deploys both services against live Torii, verifies route bindings, and exercises upgrade -> rollout -> rollback correctness for the site service.)

37. **SORACLOUD-AI-APARTMENTS — Persistent agent runtime with wallet/key control** (AI/Runtime, Line: Iroha 3, Owner: AI Platform WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define `AgentApartmentManifestV1` (tool caps, policy caps, spend limits, state quota, network egress policy, upgrade policy). (Added versioned schema + validation + tests/fixtures in `crates/iroha_data_model/src/soracloud.rs`, `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs`, `fixtures/soracloud/agent_apartment_manifest_v1.json`, docs in `docs/source/soracloud/manifest_schemas.md`)
 - [x] Add deterministic local apartment scheduler lifecycle in CLI (`agent-deploy`, `agent-lease-renew`, `agent-restart`, `agent-status`) with append-only apartment events, lease expiry semantics, and restart metadata (`crates/iroha_cli/src/soracloud.rs`, docs in `docs/source/soracloud/cli_local_control_plane.md`).
 - [x] Add CLI integration coverage for local lease-expiry recovery (`agent-deploy --lease-ticks 1` -> lease-expired rejection -> `agent-lease-renew` -> successful policy action) in `integration_tests/tests/iroha_cli.rs` (`soracloud_agent_lease_expiry_and_renew_recovery_local_flow`).
 - [x] Add deterministic local wallet/key policy engine simulation in CLI (`agent-wallet-spend`, `agent-wallet-approve`, `agent-policy-revoke`) with capability guardrails (`wallet.sign`/`wallet.auto_approve`), spend-limit enforcement, approval workflow, and runtime capability revocation (`crates/iroha_cli/src/soracloud.rs`, docs in `docs/source/soracloud/cli_local_control_plane.md`).
 - [x] Add deterministic local multi-agent mailbox simulation in CLI (`agent-message-send`, `agent-message-ack`, `agent-mailbox-status`) with policy checks (`agent.mailbox.send`/`agent.mailbox.receive`) and append-only mailbox audit traces in `apartment_events` (`crates/iroha_cli/src/soracloud.rs`, docs in `docs/source/soracloud/cli_local_control_plane.md`).
 - [x] Add CLI integration coverage for local wallet + mailbox policy workflows (`agent-wallet-spend`, `agent-wallet-approve`, `agent-policy-revoke`, `agent-message-send`, `agent-mailbox-status`, `agent-message-ack`) including rejection after `wallet.sign` revocation (`integration_tests/tests/iroha_cli.rs`: `soracloud_agent_wallet_and_mailbox_local_policy_flow`).
 - [x] Extend Torii SCR control-plane runtime with signed scheduler lifecycle endpoints (`agent/deploy`, `agent/lease/renew`, `agent/restart`, `agent/status`) and deterministic runtime status/audit state (lease expiry, restart metadata, pending queue counters) in `crates/iroha_torii/src/soracloud.rs` + routes in `crates/iroha_torii/src/lib.rs`.
 - [x] Implement wallet/key policy engine in Torii SCR control-plane runtime (`agent/wallet/spend`, `agent/wallet/approve`) with deterministic capability checks, per-asset spend limits, daily-budget accounting, approval queue semantics, and revocation enforcement.
 - [x] Implement multi-agent mailbox runtime in Torii SCR control-plane (`agent/message/send`, `agent/message/ack`, `agent/mailbox/status`) with deterministic policy checks and audit trails.
 - [x] Implement long-lived in-node SCR execution persistence for agent apartments (state bindings + process lifecycle continuity across node restarts/churn), beyond control-plane runtime metadata. (Torii apartment runtime now persists deterministic process lifecycle fields (`process_generation`, process start/last-active/last-checkpoint sequences) and checkpointed autonomy state usage (`persistent_state_total_bytes`/`persistent_state_key_count`) under `manifest.state_quota_bytes` enforcement; quota overflow rejects autonomy run admission. Coverage: `crates/iroha_torii/src/soracloud.rs` unit tests `agent_autonomy_checkpoint_rejects_when_state_quota_is_exceeded` + `agent_runtime_state_persists_across_registry_reload`, and live CLI integration `integration_tests/tests/iroha_cli.rs::soracloud_agent_runtime_state_recovers_after_peer_restart_live_torii_control_plane` now verifies persisted checkpoint/process-generation continuity across peer restart + apartment restart.)
 - [x] Add deterministic local safety controls for autonomous software build/run loops in CLI (`agent-artifact-allow`, `agent-autonomy-run`, `agent-autonomy-status`) with artifact allowlists, optional provenance pinning, and execution budget ceilings (`crates/iroha_cli/src/soracloud.rs`, `docs/source/soracloud/cli_local_control_plane.md`).
 - [x] Add CLI integration coverage for local autonomy workflow (`agent-deploy` + `agent-artifact-allow` + `agent-autonomy-run` + `agent-autonomy-status`) including provenance-mismatch and budget-overflow rejection (`integration_tests/tests/iroha_cli.rs`: `soracloud_agent_autonomy_local_policy_flow`).
 - [x] Integrate autonomous build/run safety controls into SCR runtime admission/execution paths (artifact allowlists, provenance checks, and budget enforcement beyond local CLI simulation). (Torii Soracloud runtime now exposes signed `POST /v1/soracloud/agent/deploy`, `POST /v1/soracloud/agent/policy/revoke`, `POST /v1/soracloud/agent/autonomy/allow`, `POST /v1/soracloud/agent/autonomy/run`, and `GET /v1/soracloud/agent/autonomy/status`; runtime enforces lease expiry, capability revocation, artifact allowlist+provenance pinning, and budget ceilings in `crates/iroha_torii/src/soracloud.rs`, routes wired in `crates/iroha_torii/src/lib.rs`.)
 - [x] Add live Torii CLI integration coverage for runtime lifecycle + policy flows (`agent-deploy`, `agent-lease-renew`, `agent-restart`, `agent-status`, `agent-wallet-spend`, `agent-wallet-approve`, `agent-message-send`, `agent-message-ack`, `agent-mailbox-status`) in `integration_tests/tests/iroha_cli.rs` (`soracloud_agent_wallet_mailbox_and_lease_recovery_use_live_torii_control_plane`).
 - [x] Add integration coverage for autonomous-agent/node-churn recovery scenarios (peer restarts, queue/state recovery, and lease continuity under churn) in `integration_tests/tests/iroha_cli.rs` (`soracloud_agent_runtime_state_recovers_after_peer_restart_live_torii_control_plane`).

38. **SORACLOUD-FHE-PRIVATE-DATA — FHE-first private state and compute** (Privacy/Crypto, Line: Iroha 3, Owner: Crypto + Core WG, Priority: High, Status: 🈴 Completed, target TBD)
 - [x] Define FHE parameter governance model (`fhe_param_set`, versioning, activation/deprecation/withdraw) and deterministic execution policy. (Added `FheParamSetV1`, `FheExecutionPolicyV1`, and `FheGovernanceBundleV1` schemas + validation in `crates/iroha_data_model/src/soracloud.rs`; added canonical fixtures/tests in `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs` + `fixtures/soracloud/fhe_*.json`; documented lifecycle/execution constraints in `docs/source/soracloud/manifest_schemas.md`.)
 - [x] Implement ciphertext-native state model (public metadata + encrypted payloads + commitments) and `SecretEnvelopeV1`. (Added `SecretEnvelopeV1`, `CiphertextStateMetadataV1`, and `CiphertextStateRecordV1` with deterministic consistency checks in `crates/iroha_data_model/src/soracloud.rs`; added canonical fixtures/tests in `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs` + `fixtures/soracloud/{secret_envelope_v1,ciphertext_state_record_v1}.json`; documented validation rules in `docs/source/soracloud/manifest_schemas.md`.)
 - [x] Implement `FheJobSpecV1` admission/execution path and deterministic validator-side ciphertext operations where applicable. (Added `FheJobSpecV1` + deterministic output sizing/commitment logic and admission checks in `crates/iroha_data_model/src/soracloud.rs`; added canonical fixture/tests in `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs` + `fixtures/soracloud/fhe_job_spec_v1.json`; wired signed runtime execution endpoint `POST /v1/soracloud/fhe/job/run` with binding/policy/quota enforcement and audit events in `crates/iroha_torii/src/soracloud.rs` + route in `crates/iroha_torii/src/lib.rs`.)
 - [x] Implement decryption authority model (client-held keys and/or threshold decryption service) with strict policy gating and auditability. (Added `DecryptionAuthorityModeV1`, `DecryptionAuthorityPolicyV1`, and `DecryptionRequestV1` with deterministic policy/request validation in `crates/iroha_data_model/src/soracloud.rs`; added canonical fixtures/tests in `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs` + `fixtures/soracloud/{decryption_authority_policy_v1,decryption_request_v1}.json`; wired signed admission endpoint `POST /v1/soracloud/decrypt/request` with binding-encryption/prefix gating and immutable audit logging in `crates/iroha_torii/src/soracloud.rs` + route in `crates/iroha_torii/src/lib.rs`.)
 - [x] Add privacy-preserving query interfaces (ciphertext queries, proof-carrying results, disclosure-minimized metadata). (Added `CiphertextQuerySpecV1`/`CiphertextQueryResponseV1`/`CiphertextInclusionProofV1` schemas + validation in `crates/iroha_data_model/src/soracloud.rs`, canonical fixtures in `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs` + `fixtures/soracloud/{ciphertext_query_spec_v1,ciphertext_query_response_v1}.json`, and signed runtime endpoint `POST /v1/soracloud/ciphertext/query` in `crates/iroha_torii/src/soracloud.rs` + route in `crates/iroha_torii/src/lib.rs`.)
 - [x] Add conformance suite: plaintext non-disclosure guarantees, deterministic FHE execution parity, and policy enforcement regressions. (Added Soracloud query conformance regressions in `crates/iroha_torii/src/soracloud.rs` for plaintext-binding rejection, binding-prefix scope enforcement, minimal-metadata non-disclosure, and deterministic cross-registry query parity; added schema-level validation regressions in `crates/iroha_data_model/src/soracloud.rs` for metadata projection and bounded query limits.)

39. **SORACLOUD-PII-DATA-PACK — Regulated private workload framework** (Vertical/Compliance, Line: Iroha 3, Owner: Compliance + SDK WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Ship `sora init pii-app` framework with private schema presets, consent policy templates, and retention/deletion workflows. (`iroha app soracloud init --template pii-app` now scaffolds `pii-app/frontend` (Vue3), `pii-app/api/server.mjs` (consent + retention/deletion workflow endpoints), and governance preset files under `pii-app/policy/{consent_policy_template.json,retention_policy_template.json,deletion_workflow_template.json}` with PII-generic manifest defaults in `crates/iroha_cli/src/soracloud.rs`; the legacy `health-app` template selector is removed in first-release strict mode.)
 - [x] Close scaffold auth/security gaps for webapp + pii-app: replace placeholder auth with deterministic challenge-signature login, add single-use replay protection and shared session/challenge state prefixes (`/state/auth/challenges`, `/state/auth/sessions`), enforce capability-gated private routes (`pii.*`), document strict runtime env requirements (`SESSION_HMAC_KEY`, `AUTH_*`, `PUBLIC_BASE_URL`) in scaffold docs/readmes/tests (`crates/iroha_cli/src/soracloud.rs`, `docs/source/soracloud/cli_local_control_plane.md`), and add executable scaffold-auth smoke harness coverage for replay/expiry/signature/session-sharing/capability behavior plus strict startup fail-closed checks for weak session key, invalid `AUTH_MODE`, and missing/invalid capability map. Also hardened challenge-expiry determinism via cleanup markers under `/state/auth/challenges/_meta/expired` so expired login attempts return `AUTH_CHALLENGE_EXPIRED` consistently while unknown IDs still return `AUTH_CHALLENGE_NOT_FOUND`, made external shared-state adapters default-required in strict/production mode (`AUTH_REQUIRE_EXTERNAL_SHARED_STATE`, with explicit `=0` only for local single-replica dev), required adapter `putIfAbsent` semantics for deterministic replay guards, added challenge-consume lock markers under `/state/auth/challenges/_meta/consume_locks` to serialize challenge consumption, validated positive adapter paths for both webapp and pii-app (session minting and no local fallback file writes), enforced session-origin checks on authenticated requests (including missing-origin fail-closed behavior when an origin is bound), and added static marker fallback assertions for node-unavailable environments to keep coverage fail-closed. Webapp smoke coverage additionally validates canonical challenge message construction, principal mismatch rejection (`AUTH_CHALLENGE_PRINCIPAL_MISMATCH`), origin mismatch rejection (`AUTH_ORIGIN_MISMATCH`), private-route empty-capability-map rejection (`AUTH_CAPABILITY_MAP_REQUIRED`), per-request session-origin enforcement, hardened cookie flags, tampered-cookie rejection, and cross-replica logout invalidation; pii-app smoke coverage additionally validates canonical challenge messages, principal-mismatch/malformed/unknown/expired/replay auth rejection paths, full protected-route capability gating, and cross-replica session continuation/logout invalidation. Validation: `cargo fmt --all -- crates/iroha_cli/src/soracloud.rs` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_auth_startup_fails_on_invalid_auth_mode -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_auth_startup_fails_when_external_state_is_defaulted_without_adapter -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_startup_fails_when_external_state_is_defaulted_without_adapter -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_auth_startup_fails_with_invalid_external_state_adapter_shape -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_startup_fails_with_invalid_external_state_adapter_shape -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_startup_fails_on_weak_session_key_in_strict_mode -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_startup_fails_on_invalid_auth_mode -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_auth_smoke_rejects_origin_mismatch -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_smoke_rejects_origin_mismatch -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_private_route_requires_non_empty_capability_map -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_startup_fails_on_empty_capability_map_object -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_auth_smoke_rejects_replay_and_supports_shared_sessions -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_webapp_auth_replay_lock_contention_is_fail_closed -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_module_contains_replay_and_signature_guards -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_startup_fails_when_production_disables_external_state_requirement -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_replay_lock_contention_is_fail_closed -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha soracloud::tests::generated_pii_app_auth_smoke_enforces_capability_authorization -- --nocapture --test-threads=1` (ok), `cargo test -p iroha_cli --bin iroha generated_pii_app_auth_smoke_ -- --nocapture` (ok; 3 passed), `cargo test -p iroha_cli --bin iroha soracloud::tests:: -- --nocapture` (ok; 97 passed).
 - [x] Implement access consent + break-glass policy primitives with immutable audit trails and jurisdiction tags. (Extended `DecryptionAuthorityPolicyV1`/`DecryptionRequestV1` with jurisdiction tags, consent-evidence gating, and mandatory break-glass reason semantics in `crates/iroha_data_model/src/soracloud.rs`; updated canonical fixtures/tests in `crates/iroha_data_model/tests/soracloud_manifest_fixtures.rs` + `fixtures/soracloud/{decryption_authority_policy_v1,decryption_request_v1}.json`; extended Torii access runtime/audit metadata for policy/jurisdiction/consent/break-glass fields in `crates/iroha_torii/src/soracloud.rs` and added signed health-alias endpoint `POST /v1/soracloud/health/access/request` in `crates/iroha_torii/src/lib.rs`.)
 - [x] Add compliance telemetry/report pack (access logs, consent evidence, data-flow attestations, policy diff history). (Torii now exposes signed `GET /v1/soracloud/health/compliance/report` with deterministic filtering (`service_name`, `jurisdiction_tag`, `limit`) and report-pack sections for access-log summaries, consent-evidence coverage metrics, per-jurisdiction stats, encrypted binding data-flow attestations, and policy snapshot diff history in `crates/iroha_torii/src/soracloud.rs` + route wiring in `crates/iroha_torii/src/lib.rs`; decryption audit/response payloads now include `policy_snapshot_hash`, and Soracloud tests include `health_compliance_report_summarizes_access_logs_policy_history_and_attestations`.)
 - [x] Add privacy/security test matrix for health workloads (least-privilege, unauthorized-access rejection, evidence completeness). (Added Torii Soracloud regressions `health_privacy_matrix_enforces_declared_binding_scope`, `health_privacy_matrix_rejects_unauthorized_decryption_signatures`, and `health_privacy_matrix_reports_evidence_completeness_gaps` in `crates/iroha_torii/src/soracloud.rs`, and documented the matrix in `docs/source/soracloud/cli_local_control_plane.md`.)

40. **SORACLOUD-MODEL-TRAINING — Distributed training + weight lifecycle** (AI/ML Infra, Line: Iroha 3, Owner: AI Platform + Storage WG, Priority: Medium, Status: 🈴 Completed, target TBD)
 - [x] Implement distributed training job orchestration on SCR (worker groups, checkpointing, retry semantics, cost metering). (Torii Soracloud runtime now exposes signed `POST /v1/soracloud/training/job/start`, `POST /v1/soracloud/training/job/checkpoint`, `POST /v1/soracloud/training/job/retry`, and `GET /v1/soracloud/training/job/status` in `crates/iroha_torii/src/{soracloud.rs,lib.rs}` with deterministic worker-group admission, checkpoint cadence + budget metering, retry-policy enforcement, and persisted runtime/audit state; covered by `soracloud::tests::training_job_lifecycle_tracks_checkpoints_retries_and_completion` and `soracloud::tests::training_job_start_rejects_services_without_training_capability`.)
 - [x] Implement model/weight registry (version lineage, signatures, promotion gates, rollback). (Torii Soracloud runtime now exposes signed `POST /v1/soracloud/model/weight/register`, `POST /v1/soracloud/model/weight/promote`, `POST /v1/soracloud/model/weight/rollback`, and `GET /v1/soracloud/model/weight/status` in `crates/iroha_torii/src/{soracloud.rs,lib.rs}` with deterministic lineage validation (`parent_version` policy), completed-training-job gating, signature-verified mutation payloads, promotion gate approval checks, rollback target validation, and persisted version/audit state; covered by `soracloud::tests::model_weight_lifecycle_supports_lineage_promotion_and_rollback` and `soracloud::tests::model_weight_register_rejects_non_completed_training_jobs`.)
 - [x] Implement secure artifact pipeline (dataset refs, training config hash, reproducibility metadata, provenance attestations). (Torii Soracloud runtime now exposes signed `POST /v1/soracloud/model/artifact/register` and `GET /v1/soracloud/model/artifact/status` in `crates/iroha_torii/src/{soracloud.rs,lib.rs}`. Model-weight registration now requires prior artifact attestation for the same completed training job and rejects metadata/hash mismatches (`dataset_ref`, `weight_artifact_hash`, `training_config_hash`, `reproducibility_hash`, `provenance_attestation_hash`) before consuming the artifact record into a version lineage. Coverage: `soracloud::tests::secure_artifact_pipeline_requires_artifact_registration_before_weight_register` and `soracloud::tests::secure_artifact_pipeline_enforces_metadata_match_and_consumption`.)
 - [x] Add integration benchmarks for training throughput, checkpoint recovery, and deterministic promotion policy enforcement. (Added deterministic benchmark regressions in `crates/iroha_torii/src/soracloud.rs`: `soracloud::tests::training_benchmark_throughput_and_checkpoint_recovery_are_deterministic` (throughput ratio + checkpoint recovery after persisted reload) and `soracloud::tests::promotion_policy_benchmark_gate_enforcement_is_deterministic` (repeatable gate-reject/gate-approve promotion behavior).)

## TODO Inventory (Repo Scan 2026-01-30)

This appendix tracks open TODO markers discovered in the repository. Items are grouped by area. Reference-only TODO mentions are called out separately so they do not get mistaken for active work items.

### Code / Runtime TODOs
- None in current scan.

### SDK TODOs
- None in current scan.

### Test TODOs
- None in current scan.

### Tooling / CI TODOs
- None in current scan.

### Docs TODOs (feature gaps)
- None in current scan.

### TODO References (non-actionable / informational)
- Documentation guidance that uses “TODO” as an instruction or historical note, not an open task:
  - `docs/source/da/ingest_plan.md` and `docs/portal/docs/da/ingest-plan.md` (TODO Resolution Summary).
  - `docs/source/dev_workflow*.md` (guardrail guidance).
  - `AGENTS*.md`, `CONTRIBUTING*.md`, `CHANGELOG*.md`, `ci/check_todo_guard.sh`, `hooks/pre-commit.sample` (policy/history references).
  - `docs/source/agents/env_var_migration*.md` (policy note for temporary shims).
  - `docs/source/sorafs_gateway_dns_design_runbook*.md` (session notes instruction).
  - `docs/source/soranet/pq_rollout_plan*.md` plus portal/i18n copies (instruction to log TODOs in rollout tracker).
  - `docs/source/sdk/android/security*.md`, `docs/source/sdk/android/samples/operator_console*.md`, `docs/source/sdk/android/manifest_codegen_parity*.md`, `docs/source/sdk/swift/issues/IOS3-CONNECT-003*.md` (references to already-closed TODOs).
  - `docs/source/rust_1_91_adoption*.md`, `docs/source/rust_1_92_adoption*.md` (TODO labels in completed checklists).
  - `docs/source/sns/steward_replacement_playbook.md` (post-mortem instruction to add TODOs if needed).
  - `docs/source/ivm_isi_kotodama_alignment.ja.md` (TODO tracking table; items now marked complete).
  - `crates/ivm/README.md` (section header “Status and TODOs”).
  - `docs/source/fastpq_plan.md` (definition note explaining TODO markers).
  - `vendor/icrate/**`, `vendor/halo2-axiom/**`, `vendor/halo2curves-axiom/**` (upstream TODO markers in vendored dependencies).
