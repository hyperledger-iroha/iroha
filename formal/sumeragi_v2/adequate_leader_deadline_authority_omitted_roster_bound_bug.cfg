\* Mutation: record shape alone lets the fabricated receipt outlive the real
\* frozen-roster window, so NoPrematureExit fails at RosterDeadline.
CONSTANT EnforceRosterDeadlineAuthority = FALSE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT NoPrematureExit

CHECK_DEADLOCK FALSE
