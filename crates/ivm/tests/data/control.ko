// Example illustrating control flow with a loop and branches.
// Loops from `a` up to `b` and
// uses different builtins depending on the condition.
seiyaku Example {
    kotoage fn control(a: i64, b: i64) authorize("Entry") {
        for offset in range(64) {
            let i = a + offset;
            if i > b {
                break;
            }
            if i == b {
                let poseidon_commitment = crypto::poseidon2(i, b);
            } else {
                let value_commitment = crypto::valcom(i, b);
            }
        }
    }
}
