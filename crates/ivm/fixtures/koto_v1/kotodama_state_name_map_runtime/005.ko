
        seiyaku C {
            state StateMap<Name, int> Foo;

            fn read_value(Name key) -> int {
                return Foo.ensure(key: key, default: 0);
            }

            kotoage fn main() -> int authorize("WriteState") {
                Foo[Name::parse("alice")] = 1;
                return read_value(key: Name::parse("alice"));
            }
        }
    
