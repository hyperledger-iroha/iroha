// Ternary conditional example (choose the smaller value).
seiyaku TernaryDemo {
    kotoage fn choose_min(int a, int b) -> int authorize("Entry") {
        let value = (a < b) ? a : b;
        return value;
    }
}
