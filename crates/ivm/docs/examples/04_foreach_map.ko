// Presence-aware StateMap reads inside a contract.
seiyaku MapIndexing {
    state values: StateMap<i64, i64>;
    kotoage fn sum_two() -> i64 authorize("Entry") {
        let a = values.get(1).unwrap_or(0);
        let b = values.get(2).unwrap_or(0);
        return a + b;
    }
}
