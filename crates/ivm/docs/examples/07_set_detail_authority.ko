// Using context::authority() in a metadata call inside a contract.
seiyaku DetailAuthority {
    kotoage fn write_cursor() authorize("Admin") {
        ledger::account::set_detail(context::authority(), Name::parse("cursor"), Json::parse("{\"cursor\":1,\"query\":\"sc_dummy\"}"));
    }
}
