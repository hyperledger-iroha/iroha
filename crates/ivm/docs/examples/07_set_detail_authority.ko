// Using authority() in a metadata call inside a contract.
seiyaku DetailAuthority {
  kotoage fn write_cursor() permission(Admin) {
    set_account_detail(
      authority(),
      name!("cursor"),
      json!{ query: "sc_dummy", cursor: 1 }
    );
  }
}
