
            seiyaku Demo {
                kotoage fn run(int count) -> int authorize("Run") { return count; }

                #[test]
                fn drive_run() {
                    let target = "run";
                    let _next = test::invoke_kotoage(kotoage: target, arguments: Json::parse("{\"count\":7}"));
                }
            }
            