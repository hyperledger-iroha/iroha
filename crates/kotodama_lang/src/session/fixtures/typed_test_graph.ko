seiyaku Demo {
            fn current() -> int { return 1; }
            view fn value() -> int { return current(); }
            #[test]
            fn smoke() { test::assert(current() == 1); }
        }
