
seiyaku Test {
  state StateMap<int, int> Foo;

  kotoage fn sum(int n) -> int  authorize("Entry") {
    var int acc = 0;
    for (k, v) in Foo.take(n) {
      acc = acc + v;
    }
    return acc;
  }
}
