
            fn first() -> int { 1 }
            fn second() -> int { 2 }
            fn initial() -> int { 3 }
            fn combine(int _ base, int left, int right) -> int { base + left * 10 + right }
            fn run() -> int { combine(initial(), right: second(), left: first()) }
        
