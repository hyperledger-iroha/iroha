// Minimal sample ensuring a contract can execute a benign metadata write.
seiyaku FilterQueriesSample {
    kotoage fn run() authorize("Admin") {
        ledger::account::set_detail(context::authority(), Name::parse("noop"), Json::parse("1"));
    }
}
