# Test Provider Admission Envelopes

Integration fixtures reference this directory when validating the `torii.sorafs.admission_envelopes_dir`
configuration parameter. It remains empty because the tests only assert that the path is threaded
through the configuration loader.
