extends = "base.toml"

[fraud_monitoring]
enabled = true
service_endpoints = [
  "https://fraud.local/assess",
  "https://fraud.local/assess",
  "https://fraud.secondary/verify"
]
connect_timeout_ms = 0
request_timeout_ms = 1_800
missing_assessment_grace_secs = 5
required_minimum_band = "medium"
