// Auto-generated wrapper for contract 'TairaXorSccpBridge'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/messages"
import "@contracts/storage"

/// Storage `BridgeStorage` is defined in `@contracts/storage`
struct TairaXorSccpBridge {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun TairaXorSccpBridge.fromStorage(
    storage: BridgeStorage,
    toShard: AddressShardingOptions? = null,
): TairaXorSccpBridge {
    val stateInit = ContractState {
        code: build("TairaXorSccpBridge"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return TairaXorSccpBridge { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun TairaXorSccpBridge.fromAddress(address: address): TairaXorSccpBridge {
    return TairaXorSccpBridge { address };
}

/// Deploys the contract to the blockchain
fun TairaXorSccpBridge.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendSccpFinalizeFromTaira(
    self,
    from: address,
    queryId: uint64,
    version: uint16,
    messageId: uint256,
    statementHash: uint256,
    publicSignals: cell,
    proof: cell,
    canonicalPayload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SccpFinalizeFromTaira {
            queryId,
            version,
            messageId,
            statementHash,
            publicSignals,
            proof,
            canonicalPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendSccpMasterMintAck(
    self,
    from: address,
    queryId: uint64,
    messageId: uint256,
    recipient: address,
    jettonAmount: coins,
    payloadHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SccpMasterMintAck { queryId, messageId, recipient, jettonAmount, payloadHash },
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendSccpMasterBurnAck(
    self,
    from: address,
    queryId: uint64,
    messageId: uint256,
    jettonAmount: coins,
    payloadHash: uint256,
    sourceEventDigest: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SccpMasterBurnAck {
            queryId,
            messageId,
            jettonAmount,
            payloadHash,
            sourceEventDigest,
        },
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendSccpWalletRefundAck(
    self,
    from: address,
    queryId: uint64,
    refundId: uint256,
    recipientOwner: address,
    jettonAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SccpWalletRefundAck { queryId, refundId, recipientOwner, jettonAmount },
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendSccpRetry(
    self,
    from: address,
    queryId: uint64,
    messageId: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SccpRetry { queryId, messageId },
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TairaXorSccpBridge.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TairaXorSccpBridge.getSccpRouteConfigurationHash(self): uint256 {
    return net.runGetMethod(self.address, "get_sccp_route_configuration_hash");
}

fun TairaXorSccpBridge.getSccpNonce(self, owner: address): uint64 {
    return net.runGetMethod(self.address, "get_sccp_nonce", [owner]);
}

fun TairaXorSccpBridge.getSccpReplayStatus(self, messageId: uint256, inbound: bool): uint8? {
    return net.runGetMethod(self.address, "get_sccp_replay_status", [messageId, inbound]);
}
