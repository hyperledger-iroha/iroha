"""Bind membership admission and consuming publication to their actual owners.

Used by the multilane release checker; no environment configuration is required.
These structural obligations do not qualify the unfinished State/Apply publisher.
"""

from __future__ import annotations

import re
from pathlib import Path
from typing import Any, Callable

from sumeragi_v2_multilane_geometry_evidence_contract import _code


STATE = "crates/iroha_core/src/state.rs"
STORAGE = "crates/iroha_core/src/state/storage_transactions.rs"
CAPTURE = "crates/iroha_core/src/state/storage_transactions/block/capture.rs"
HISTORY = "crates/iroha_core/src/state/storage_transactions/history.rs"
HISTORY_SLOT = "crates/iroha_core/src/state/storage_transactions/history_slot.rs"
CAPTURE_TESTS = "crates/iroha_core/src/state/storage_transactions/block/capture_tests.rs"
MODELS = (
    "SumeragiV2NativeApplicationEvidence",
    "SumeragiV2AutonomousReservationCarrier",
)
MEMBERSHIP_BINDINGS = (
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'TransactionsStorage', ('write_lock: Mutex<Identity>', 'released: concread::release::ReleaseNotification', 'latest_block: TipStore', 'blocks: history::Map', 'budget: mv::allocation::AllocationBudget', 'pending: Mutex<Option<history::Pending>>', 'publication_sequence: AtomicU64')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'TransactionsStorage::block_impl', ('    fn block_impl(&self, replacement: bool) -> TransactionsBlock<\'_> {\n        let mut pending = Some(\n            self.prepare_next_block(replacement)\n                .expect("fixture history admission"),\n        );\n        self.attach_prepared(&mut pending)\n            .expect("fixture original predecessor")\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'TransactionsBlock', ("_guard: MembershipWriter<'storage>", "latest_block_ref: &'storage TipStore", "blocks_ref: &'storage history::Map", "baseline: HistoryReader<'storage>", "budget_ref: &'storage AllocationBudget", "publication_sequence: &'storage AtomicU64")),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'PreparedTransactionsBlock', ("    pub(crate) struct PreparedTransactionsBlock<'storage> {\n        block: TransactionsBlock<'storage>,\n        publication: MembershipPublication,\n        next_identity: Identity,\n        // Fixed original-owner metadata. During publication the admitted action\n        // stays in place, and every displaced allocation stays in this caller.\n        retired_tip: Option<Tip>,\n        publication_started: bool,\n        published: bool,\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'enum', 'MembershipPublication', ('Repeated', 'Replace', 'Advance', '_previous: Option<Tip>', 'current: Tip')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'TransactionsBlock::prepare_commit', ('let mut capture = self.capture_slot();', 'capture.try_prepare()?;', 'Ok(capture.into_prepared())')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'TransactionsBlock::admit_publication', ('self._guard.identity();', 'self.latest_block_ref.load_full()', 'TransactionsBlockError::MissingInsertBlock', 'previous_block.height == current_block.height', 'previous_block.transactions == current_block.transactions', 'MembershipPublication::Repeated', 'usize::from(!self.revert)', '.checked_add(addition)', 'TransactionsBlockError::HeightOverflow', 'expected_current_height != current_height', 'TransactionsBlockError::HeightMismatch', 'MembershipPublication::Replace', 'MembershipPublication::Advance', '_previous: previous_block')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::as_block', ("&TransactionsBlock<'storage>", '&self.block')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::publish', ('        pub(crate) fn publish(mut self) -> TransactionsPublicationRetirement {\n            self.try_prepare_physical()\n                .expect("fixture physical membership admission");\n            self.publish_prepared()\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'TransactionsPublicationRetirement', ('    pub(crate) struct TransactionsPublicationRetirement {\n        _tip: Option<Tip>,\n        _staged: Option<Tip>,\n        _identity: Identity,\n        _publication: MembershipPublication,\n        _release: MembershipRelease,\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'PublishedTransactions', ('    pub(crate) struct PublishedTransactions<Installation> {\n        _retirement: TransactionsPublicationRetirement,\n        _installation: Installation,\n        _preflight_release: Option<concread::release::DeferredRelease>,\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedDetachedTransactionsBlock::publish', ('        pub(crate) fn publish(self) -> PublishedTransactions<Installation> {\n            let Self {\n                prepared,\n                installation,\n                preflight_release,\n            } = self;\n            PublishedTransactions {\n                _retirement: prepared.publish_prepared(),\n                _installation: installation,\n                _preflight_release: preflight_release,\n            }\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'DetachedTransactionsBlock', ('predecessor_identity: Identity', 'history: history::Pending', 'predecessor: Option<Tip>', 'current: Tip', 'revert: bool', 'publication: MembershipPublication', 'next_identity: Identity')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::detach', ('fn detach(self)', 'self.detach_retaining().0')),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::detach_retaining', ('        fn detach_retaining(self) -> (DetachedTransactionsBlock, MembershipRelease) {\n            self.assert_unpublished();\n            let Self {\n                block,\n                publication,\n                next_identity,\n                retired_tip: _,\n                publication_started: _,\n                published: _,\n            } = self;\n            let mut block = block;\n            let history = block\n                ._guard\n                .history\n                .as_mut()\n                .expect("original history")\n                .recover_detached();\n            let detached = DetachedTransactionsBlock {\n                history,\n                predecessor_identity: block._guard.identity().clone(),\n                predecessor: block.latest_block_ref.load_full(),\n                current: block\n                    .current_block\n                    .as_ref()\n                    .expect("admitted membership")\n                    .clone(),\n                revert: block.revert,\n                publication,\n                next_identity,\n            };\n            let TransactionsBlock {\n                _guard,\n                current_block,\n                ..\n            } = block;\n            let release = _guard.into_release();\n            // The detached journal owns the original staged allocation.\n            drop(current_block);\n            (detached, release)\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'DetachedTransactionsBlock::observe_predecessor', ('        pub(crate) fn observe_predecessor(\n            &self,\n            storage: &TransactionsStorage,\n        ) -> MembershipPredecessorStatus {\n            let Some(guard) = storage.write_lock.try_lock() else {\n                return MembershipPredecessorStatus::Busy;\n            };\n            let guard = storage.released.guard(guard);\n            if Identity::ptr_eq(&guard, &self.predecessor_identity) {\n                MembershipPredecessorStatus::Current\n            } else {\n                MembershipPredecessorStatus::Changed\n            }\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'DetachedTransactionsBlock::try_prepare_publication', ("        pub(crate) fn try_prepare_publication<'storage, Installation, E>(\n            self,\n            storage: &'storage TransactionsStorage,\n            admit: impl FnOnce(&Self, &TransactionsStorage) -> Result<Installation, E>,\n        ) -> Result<\n            PreparedDetachedTransactionsBlock<'storage, Installation>,\n            (\n                Self,\n                mv::PublicationPreparationError<E>,\n                AbortedTransactions<Installation>,\n            ),\n        > {\n            let mut slot = self.publication_slot(storage);\n            match slot.try_prepare(admit) {\n                Ok(()) => Ok(slot.into_prepared()),\n                Err(error) => {\n                    let original = slot.recover_original();\n                    Err((original, error, slot.into_cleanup()))\n                }\n            }\n        }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'enum', 'MembershipWriterPhase', ("enum MembershipWriterPhase<'storage> {\n    Attached(OriginalMembershipGuard<'storage>),\n    Released(concread::release::DeferredRelease),\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'struct', 'MembershipWriter', ("pub(in crate::state::storage_transactions) struct MembershipWriter<'storage> {\n    phase: Option<MembershipWriterPhase<'storage>>,\n    pub(super) history: Option<history_slot::Slot<'storage>>,\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'enum', 'MembershipCapturePhase', ("pub(super) enum MembershipCapturePhase<'storage> {\n    Empty,\n    Attached(TransactionsBlock<'storage>),\n    Prepared(PreparedTransactionsBlock<'storage>),\n    Captured(DetachedTransactionsBlock),\n    Published(PreparedTransactionsBlock<'storage>),\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'struct', 'TransactionsCaptureSlot', ("pub(crate) struct TransactionsCaptureSlot<'storage> {\n    pub(super) phase: MembershipCapturePhase<'storage>,\n    attempted: bool,\n    released: bool,\n    // Last: the original staged payloads precede the successful capture's\n    // deferred notification. All physical siblings must already be free.\n    cleanup: Option<MembershipRelease>,\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::new', ("    pub(in crate::state::storage_transactions) fn new(\n        guard: OriginalMembershipGuard<'storage>,\n        history: Option<history_slot::Slot<'storage>>,\n    ) -> Self {\n        Self {\n            phase: Some(MembershipWriterPhase::Attached(guard)),\n            history,\n        }\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::identity', ('    pub(in crate::state::storage_transactions) fn identity(&self) -> &Identity {\n        match self.phase.as_ref() {\n            Some(MembershipWriterPhase::Attached(guard)) => guard,\n            _ => panic!("original membership writer was terminally released"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::identity_mut', ('    pub(super) fn identity_mut(&mut self) -> &mut Identity {\n        match self.phase.as_mut() {\n            Some(MembershipWriterPhase::Attached(guard)) => guard,\n            _ => panic!("original membership writer was terminally released"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::release', ('    fn release(&mut self) {\n        if let Some(history) = self.history.as_mut() {\n            history.release();\n        }\n        match self.phase.take() {\n            Some(MembershipWriterPhase::Attached(guard)) => {\n                // Native unlock does not invoke the original callback.\n                let ((), release) = guard.release_deferred(drop);\n                self.phase = Some(MembershipWriterPhase::Released(release));\n            }\n            other => self.phase = other,\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::into_release', ('    pub(super) fn into_release(mut self) -> MembershipRelease {\n        self.take_release()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::drop', ('    fn drop(&mut self) {\n        self.release();\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::try_prepare', ('    pub(crate) fn try_prepare(&mut self) -> Result<(), TransactionsBlockError> {\n        assert!(\n            !self.attempted && !self.released,\n            "membership capture is one-shot"\n        );\n        self.attempted = true;\n        let MembershipCapturePhase::Attached(block) = &mut self.phase else {\n            panic!("original attached membership capture");\n        };\n        let publication = block.admit_publication()?;\n        let next_identity = block\n            ._guard\n            .history\n            .as_mut()\n            .expect("original history")\n            .next_identity();\n        // All fallible work precedes extraction. Only original-owner moves\n        // occur until the prepared owner is stored back in the caller slot.\n        let MembershipCapturePhase::Attached(block) =\n            std::mem::replace(&mut self.phase, MembershipCapturePhase::Empty)\n        else {\n            unreachable!("original checked membership block");\n        };\n        self.phase = MembershipCapturePhase::Prepared(PreparedTransactionsBlock::new(\n            block,\n            publication,\n            next_identity,\n        ));\n        Ok(())\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::try_capture', ('    pub(crate) fn try_capture(&mut self) -> Result<(), TransactionsBlockError> {\n        assert!(!self.released, "membership capture was terminally released");\n        if matches!(&self.phase, MembershipCapturePhase::Attached(_)) {\n            self.try_prepare()?;\n        }\n        // This assertion also excludes an explicitly released prepared block\n        // before taking it out of caller custody.\n        match &self.phase {\n            MembershipCapturePhase::Prepared(prepared) => {\n                prepared.assert_unpublished();\n                prepared.block._guard.identity();\n            }\n            _ => panic!("original prepared membership capture"),\n        }\n        let MembershipCapturePhase::Prepared(prepared) =\n            std::mem::replace(&mut self.phase, MembershipCapturePhase::Empty)\n        else {\n            unreachable!("original checked membership preparation");\n        };\n        // Existing detach kernel has no callback, user Drop, semantic\n        // refusal or allocation after the prepared owner is extracted.\n        let (journal, release) = prepared.detach_retaining();\n        self.phase = MembershipCapturePhase::Captured(journal);\n        self.cleanup = Some(release);\n        Ok(())\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::release', ('    pub(crate) fn release(&mut self) {\n        self.released = true;\n        let block = match &mut self.phase {\n            MembershipCapturePhase::Attached(block) => Some(block),\n            MembershipCapturePhase::Prepared(prepared)\n            | MembershipCapturePhase::Published(prepared) => Some(&mut prepared.block),\n            MembershipCapturePhase::Captured(_) | MembershipCapturePhase::Empty => None,\n        };\n        if let Some(block) = block {\n            block.release_writers();\n            if self.cleanup.is_none() {\n                self.cleanup = Some(block._guard.take_release());\n            }\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::into_prepared', ('    pub(super) fn into_prepared(mut self) -> PreparedTransactionsBlock<\'storage> {\n        assert!(!self.released, "membership capture was terminally released");\n        match &self.phase {\n            MembershipCapturePhase::Prepared(prepared) => {\n                prepared.assert_unpublished();\n                prepared.block._guard.identity();\n            }\n            _ => panic!("original membership preparation did not complete"),\n        }\n        match std::mem::replace(&mut self.phase, MembershipCapturePhase::Empty) {\n            MembershipCapturePhase::Prepared(prepared) => prepared,\n            _ => unreachable!("original checked membership preparation"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::into_detached', ('    pub(crate) fn into_detached(mut self) -> (DetachedTransactionsBlock, MembershipRelease) {\n        assert!(!self.released, "membership capture was terminally released");\n        match std::mem::replace(&mut self.phase, MembershipCapturePhase::Empty) {\n            MembershipCapturePhase::Captured(journal) => (\n                journal,\n                self.cleanup\n                    .take()\n                    .expect("original membership capture release"),\n            ),\n            original => {\n                self.phase = original;\n                panic!("original membership capture did not complete");\n            }\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::drop', ('    fn drop(&mut self) {\n        self.release();\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlock::capture_slot', ("    pub(crate) fn capture_slot(self) -> TransactionsCaptureSlot<'storage> {\n        TransactionsCaptureSlot {\n            phase: MembershipCapturePhase::Attached(self),\n            attempted: false,\n            released: false,\n            cleanup: None,\n        }\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlock::release_writers', ('    pub(crate) fn release_writers(&mut self) {\n        self._guard.release();\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::new', ("        fn new(\n            block: TransactionsBlock<'storage>,\n            publication: MembershipPublication,\n            next_identity: Identity,\n        ) -> Self {\n            Self {\n                block,\n                publication,\n                next_identity,\n                retired_tip: None,\n                publication_started: false,\n                published: false,\n            }\n        }",)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::assert_unpublished', ('        fn assert_unpublished(&self) {\n            assert!(\n                !self.publication_started && !self.published,\n                "membership publication was already attempted"\n            );\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::publish_in_place', ('        fn publish_in_place(&mut self) {\n            self.assert_unpublished();\n            assert!(\n                self.is_physically_prepared(),\n                "original physical membership preparation must precede publication"\n            );\n            self.block._guard.identity();\n            self.publication_started = true;\n            if !matches!(&self.publication, MembershipPublication::Repeated) {\n                let next = self\n                    .block\n                    ._guard\n                    .history\n                    .as_ref()\n                    .expect("original history")\n                    .next_sequence();\n                self.block\n                    .publication_sequence\n                    .store(next - 1, Ordering::Release);\n                self.block\n                    ._guard\n                    .history\n                    .as_mut()\n                    .expect("original history")\n                    .publish();\n                let current = match &self.publication {\n                    MembershipPublication::Replace { current }\n                    | MembershipPublication::Advance { current, .. } => current,\n                    MembershipPublication::Repeated => unreachable!(),\n                };\n                self.retired_tip = self.block.latest_block_ref.swap(Some(current.clone()));\n                self.block\n                    .publication_sequence\n                    .store(next, Ordering::Release);\n            }\n            if !matches!(&self.publication, MembershipPublication::Repeated) {\n                // Install the exact pre-admitted identity and retain its exact\n                // predecessor in the same field. No identity is reconstructed.\n                std::mem::swap(self.block._guard.identity_mut(), &mut self.next_identity);\n            }\n            self.block.release_writers();\n            self.published = true;\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::into_retirement', ('        fn into_retirement(self) -> TransactionsPublicationRetirement {\n            assert!(\n                self.published,\n                "original membership publication must complete"\n            );\n            let Self {\n                block,\n                publication,\n                next_identity,\n                retired_tip,\n                publication_started: _,\n                published: _,\n            } = self;\n            let TransactionsBlock {\n                current_block,\n                _guard,\n                ..\n            } = block;\n            let release = _guard.into_release();\n            TransactionsPublicationRetirement {\n                _tip: retired_tip,\n                _staged: current_block,\n                _identity: next_identity,\n                _publication: publication,\n                _release: release,\n            }\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::take_release', ('    fn take_release(&mut self) -> MembershipRelease {\n        self.release();\n        MembershipRelease {\n            _history: self.history.as_mut().map(|history| history.cleanup()),\n            _writer: self.take_writer_release(),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::publish_prepared', ('    pub(crate) fn publish_prepared(&mut self) {\n        assert!(!self.released, "membership capture was terminally released");\n        let MembershipCapturePhase::Prepared(prepared) = &mut self.phase else {\n            panic!("original prepared membership publication");\n        };\n        prepared.publish_in_place();\n        self.cleanup = Some(prepared.block._guard.take_release());\n        // No fallible work occurs during this inert successful phase transfer.\n        let MembershipCapturePhase::Prepared(prepared) =\n            std::mem::replace(&mut self.phase, MembershipCapturePhase::Empty)\n        else {\n            unreachable!("original completed membership publication");\n        };\n        self.phase = MembershipCapturePhase::Published(prepared);\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::executing', ('    fn executing(&self) -> &TransactionsBlock<\'storage> {\n        assert!(\n            !self.attempted && !self.released,\n            "membership execution authority ended"\n        );\n        match &self.phase {\n            MembershipCapturePhase::Attached(block) => block,\n            _ => panic!("original executing membership"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::executing_mut', ('    fn executing_mut(&mut self) -> &mut TransactionsBlock<\'storage> {\n        assert!(\n            !self.attempted && !self.released,\n            "membership execution authority ended"\n        );\n        match &mut self.phase {\n            MembershipCapturePhase::Attached(block) => block,\n            _ => panic!("original executing membership"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::into_executing', ('    fn into_executing(mut self) -> TransactionsBlock<\'storage> {\n        self.executing();\n        match std::mem::replace(&mut self.phase, MembershipCapturePhase::Empty) {\n            MembershipCapturePhase::Attached(block) => block,\n            _ => unreachable!("checked original executing membership"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'struct', 'TransactionsBlockField', ("pub struct TransactionsBlockField<'storage> {\n    pub(super) slot: TransactionsCaptureSlot<'storage>,\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::new', ("    pub(crate) fn new(block: TransactionsBlock<'storage>) -> Self {\n        Self {\n            slot: block.capture_slot(),\n        }\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::try_prepare_publication', ('    pub(crate) fn try_prepare_publication(&mut self) -> Result<(), TransactionsBlockError> {\n        self.slot.try_prepare()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::publish_prepared', ('    pub(crate) fn publish_prepared(&mut self) {\n        self.slot.publish_prepared();\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::into_executing', ("    pub(crate) fn into_executing(self) -> TransactionsBlock<'storage> {\n        self.slot.into_executing()\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::into_capture', ("    pub(crate) fn into_capture(self) -> TransactionsCaptureSlot<'storage> {\n        self.slot.executing();\n        self.slot\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::deref', ('    fn deref(&self) -> &Self::Target {\n        self.slot.executing()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::deref_mut', ('    fn deref_mut(&mut self) -> &mut Self::Target {\n        self.slot.executing_mut()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::get', ('    fn get<Q>(&self, key: &Q) -> Option<Value>\n    where\n        Key: Borrow<Q>,\n        Q: Hash + Eq + Ord + ?Sized,\n    {\n        self.slot.executing().get(key)\n    }',)),
)

DETACHED = 'crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs'
DETACHED_TESTS = 'crates/iroha_core/src/state/storage_transactions/block/detached_publication_tests.rs'
MEMBERSHIP_BINDINGS += (
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'enum', 'Phase', ("enum Phase<'storage> {\n    Original(DetachedTransactionsBlock),\n    Prepared(PreparedTransactionsBlock<'storage>),\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'struct', 'DetachedTransactionsPublicationSlot', ("pub(crate) struct DetachedTransactionsPublicationSlot<'storage, Installation> {\n    target: &'storage TransactionsStorage,\n    phase: Option<Phase<'storage>>,\n    writer: Option<MembershipWriter<'storage>>,\n    attempted: bool,\n    retryable: bool,\n    complete: bool,\n    released: bool,\n    installation: Option<Installation>,\n    preflight_release: Option<DeferredRelease>,\n    writer_release: Option<MembershipRelease>,\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsBlock::publication_slot', ("    pub(crate) fn publication_slot<Installation>(\n        self,\n        target: &TransactionsStorage,\n    ) -> DetachedTransactionsPublicationSlot<'_, Installation> {\n        DetachedTransactionsPublicationSlot {\n            target,\n            phase: Some(Phase::Original(self)),\n            writer: None,\n            attempted: false,\n            retryable: true,\n            complete: false,\n            released: false,\n            installation: None,\n            preflight_release: None,\n            writer_release: None,\n        }\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::original', ('    fn original(&self) -> &DetachedTransactionsBlock {\n        match self.phase.as_ref() {\n            Some(Phase::Original(original)) => original,\n            _ => panic!("original detached membership phase"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::refuse', ('    fn refuse<E>(\n        &mut self,\n        error: PublicationPreparationError<E>,\n    ) -> Result<(), PublicationPreparationError<E>> {\n        self.retryable = true;\n        Err(error)\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::try_prepare', ('    pub(crate) fn try_prepare<E>(\n        &mut self,\n        admit: impl FnOnce(&DetachedTransactionsBlock, &TransactionsStorage) -> Result<Installation, E>,\n    ) -> Result<(), PublicationPreparationError<E>> {\n        assert!(\n            !self.attempted && !self.released,\n            "detached membership preparation is one-shot"\n        );\n        self.attempted = true;\n        // A caught admission panic never grants retry authority.\n        self.retryable = false;\n        let target = self.target;\n        let wait = target.released.observe();\n        let Some(guard) = target.write_lock.try_lock() else {\n            return self.refuse(PublicationPreparationError::after_failed_acquisition(wait));\n        };\n        self.writer = Some(MembershipWriter::new(target.released.guard(guard), None));\n        let current = Identity::ptr_eq(\n            self.writer\n                .as_ref()\n                .expect("original observation writer")\n                .identity(),\n            &self.original().predecessor_identity,\n        );\n        // Unlock without notifying. Keep the actual event before admission can\n        // run arbitrary code, refuse, or unwind through the enclosing owner.\n        self.preflight_release = Some(\n            self.writer\n                .take()\n                .expect("original observation writer")\n                .into_writer_release(),\n        );\n        if !current {\n            return self.refuse(PublicationPreparationError::Changed);\n        }\n        let installation = match admit(self.original(), target) {\n            Ok(installation) => installation,\n            Err(error) => return self.refuse(PublicationPreparationError::Admission(error)),\n        };\n        self.installation = Some(installation);\n        let wait = target.released.observe();\n        let Some(guard) = target.write_lock.try_lock() else {\n            return self.refuse(PublicationPreparationError::after_failed_acquisition(wait));\n        };\n        // Install the actual final acquisition before checking its predecessor.\n        self.writer = Some(MembershipWriter::new(target.released.guard(guard), None));\n        if !Identity::ptr_eq(\n            self.writer\n                .as_ref()\n                .expect("original publication writer")\n                .identity(),\n            &self.original().predecessor_identity,\n        ) {\n            return self.refuse(PublicationPreparationError::Changed);\n        }\n        // All checks precede extraction. These moves reuse the admitted action,\n        // immutable payload and next identity; no new admission or allocation.\n        let Some(Phase::Original(original)) = self.phase.take() else {\n            unreachable!("checked original membership phase");\n        };\n        let DetachedTransactionsBlock {\n            predecessor_identity: _,\n            predecessor: _,\n            history,\n            current,\n            revert,\n            publication,\n            next_identity,\n        } = original;\n        let baseline = target\n            .blocks\n            .read_predecessor(history.baseline.as_ref().expect("original history cut"))\n            .expect("checked original history family");\n        self.writer\n            .as_mut()\n            .expect("original membership writer")\n            .history = Some(history_slot::Slot::new(target, history));\n        self.phase = Some(Phase::Prepared(PreparedTransactionsBlock::new(\n            TransactionsBlock {\n                latest_block_ref: &target.latest_block,\n                budget_ref: &target.budget,\n                blocks_ref: &target.blocks,\n                baseline,\n                publication_sequence: &target.publication_sequence,\n                _guard: self\n                    .writer\n                    .take()\n                    .expect("checked original publication writer"),\n                revert,\n                current_block: Some(current),\n            },\n            publication,\n            next_identity,\n        )));\n        // The complete original block is back in the caller-owned phase before\n        // the native tree acquires any physical publication lock.\n        let Some(Phase::Prepared(prepared)) = self.phase.as_mut() else {\n            unreachable!()\n        };\n        if !matches!(&prepared.publication, MembershipPublication::Repeated) {\n            if let Err(error) = prepared\n                .block\n                ._guard\n                .history\n                .as_mut()\n                .expect("original history")\n                .prepare()\n            {\n                let refusal = match error {\n                    MembershipAdmissionError::Busy(wait) => PublicationPreparationError::Busy(wait),\n                    MembershipAdmissionError::Changed(_) => PublicationPreparationError::Changed,\n                    MembershipAdmissionError::Poisoned => PublicationPreparationError::Poisoned,\n                    _ => unreachable!("native reattachment performs no allocation admission"),\n                };\n                return self.refuse(refusal);\n            }\n        }\n        self.complete = true;\n        self.retryable = true;\n        Ok(())\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::recover_original', ('    pub(crate) fn recover_original(&mut self) -> DetachedTransactionsBlock {\n        assert!(\n            self.retryable && !self.released,\n            "unwound or released membership is not retry authority"\n        );\n        if let Some(Phase::Prepared(prepared)) = self.phase.as_ref() {\n            prepared.assert_unpublished();\n        }\n        self.released = true;\n        self.complete = false;\n        if let Some(writer) = self.writer.take() {\n            self.writer_release = Some(writer.into_release());\n        }\n        match self.phase.take().expect("original membership journal") {\n            Phase::Original(original) => original,\n            Phase::Prepared(prepared) => {\n                let (original, release) = prepared.detach_retaining();\n                self.writer_release = Some(release);\n                original\n            }\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::release_writers', ('    pub(crate) fn release_writers(&mut self) {\n        self.released = true;\n        self.retryable = false;\n        self.complete = false;\n        if let Some(writer) = self.writer.take() {\n            self.writer_release = Some(writer.into_release());\n        }\n        if let Some(Phase::Prepared(prepared)) = self.phase.as_mut() {\n            prepared.block.release_writers();\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::into_prepared', ('    pub(crate) fn into_prepared(\n        mut self,\n    ) -> PreparedDetachedTransactionsBlock<\'storage, Installation> {\n        assert!(\n            self.complete && !self.released,\n            "complete original membership preparation"\n        );\n        let Some(Phase::Prepared(prepared)) = self.phase.take() else {\n            unreachable!("checked prepared membership phase");\n        };\n        self.released = true;\n        PreparedDetachedTransactionsBlock {\n            prepared,\n            installation: self.installation.take().expect("original installation"),\n            preflight_release: self.preflight_release.take(),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::into_cleanup', ('    pub(super) fn into_cleanup(mut self) -> AbortedTransactions<Installation> {\n        assert!(\n            self.released && self.phase.is_none(),\n            "original membership was recovered"\n        );\n        AbortedTransactions {\n            _installation: self.installation.take(),\n            _preflight_release: self.preflight_release.take(),\n            _release: self.writer_release.take(),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/detached_publication.rs', 'method', 'DetachedTransactionsPublicationSlot::drop', ('    fn drop(&mut self) {\n        self.release_writers();\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'PreparedDetachedTransactionsBlock', ("    pub(crate) struct PreparedDetachedTransactionsBlock<'storage, Installation> {\n        prepared: PreparedTransactionsBlock<'storage>,\n        installation: Installation,\n        preflight_release: Option<concread::release::DeferredRelease>,\n    }",)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'AbortedTransactions', ('    pub(crate) struct AbortedTransactions<Installation> {\n        _installation: Option<Installation>,\n        _preflight_release: Option<concread::release::DeferredRelease>,\n        _release: Option<MembershipRelease>,\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedDetachedTransactionsBlock::abort', ('        pub(crate) fn abort(\n            self,\n        ) -> (DetachedTransactionsBlock, AbortedTransactions<Installation>) {\n            let Self {\n                prepared,\n                installation,\n                preflight_release,\n            } = self;\n            let (journal, release) = prepared.detach_retaining();\n            (\n                journal,\n                AbortedTransactions {\n                    _release: Some(release),\n                    _installation: Some(installation),\n                    _preflight_release: preflight_release,\n                },\n            )\n        }',)),
)

# Bind original physical history preparation and cleanup beneath membership.
MEMBERSHIP_HISTORY_BINDINGS = (
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::publish_prepared', ('        fn publish_prepared(mut self) -> TransactionsPublicationRetirement {\n            self.publish_in_place();\n            self.into_retirement()\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::try_prepare_physical', ('        pub(in crate::state) fn try_prepare_physical(\n            &mut self,\n        ) -> Result<(), TransactionsBlockError> {\n            self.assert_unpublished();\n            if !matches!(&self.publication, MembershipPublication::Repeated) {\n                self.block\n                    ._guard\n                    .history\n                    .as_mut()\n                    .expect("original history")\n                    .prepare()\n                    .map_err(TransactionsBlockError::MembershipAdmission)?;\n            }\n            Ok(())\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'PreparedTransactionsBlock::is_physically_prepared', ('        fn is_physically_prepared(&self) -> bool {\n            matches!(&self.publication, MembershipPublication::Repeated)\n                || self\n                    .block\n                    ._guard\n                    .history\n                    .as_ref()\n                    .expect("original history")\n                    .is_prepared()\n        }',)),
    ('crates/iroha_core/src/state/storage_transactions.rs', 'struct', 'MembershipRelease', ('    pub(crate) struct MembershipRelease {\n        _history: Option<history_slot::Cleanup>,\n        _writer: concread::release::DeferredRelease,\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::into_writer_release', ('    pub(super) fn into_writer_release(mut self) -> concread::release::DeferredRelease {\n        assert!(self.history.is_none(), "observation-only writer");\n        self.take_writer_release()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'MembershipWriter::take_writer_release', ('    fn take_writer_release(&mut self) -> concread::release::DeferredRelease {\n        self.release();\n        match self.phase.take() {\n            Some(MembershipWriterPhase::Released(release)) => release,\n            _ => unreachable!("original membership release custody"),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsCaptureSlot::try_prepare_physical', ('    pub(crate) fn try_prepare_physical(&mut self) -> Result<(), TransactionsBlockError> {\n        assert!(!self.released, "original membership capture is live");\n        let MembershipCapturePhase::Prepared(prepared) = &mut self.phase else {\n            panic!("logical original membership preparation precedes physical admission");\n        };\n        prepared.try_prepare_physical()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::try_prepare_physical', ('    pub(crate) fn try_prepare_physical(&mut self) -> Result<(), TransactionsBlockError> {\n        self.slot.try_prepare_physical()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/block/capture.rs', 'method', 'TransactionsBlockField::recover_preparation', ('    pub(crate) fn recover_preparation(&mut self) -> history::Pending {\n        let MembershipCapturePhase::Prepared(prepared) = &mut self.slot.phase else {\n            panic!("original logically prepared membership");\n        };\n        prepared.assert_unpublished();\n        let mut original = prepared\n            .block\n            ._guard\n            .history\n            .as_mut()\n            .expect("original history")\n            .recover();\n        original.next_identity = Some(std::mem::replace(\n            &mut prepared.next_identity,\n            original.predecessor.clone(),\n        ));\n        original\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history.rs', 'struct', 'Pending', ('pub(crate) struct Pending {\n    pub(super) predecessor: Identity,\n    pub(super) latest: Option<Tip>,\n    pub(super) replacement: bool,\n    pub(super) next_sequence: u64,\n    batch: Option<ChargedBuffer<Key>>,\n    next: usize,\n    pub(super) work: Option<Work>,\n    pub(super) next_identity: Option<Identity>,\n    pub(super) baseline: Option<concread::bptree::BptreeMapRetainedPredecessor<Key, Value, Mode>>,\n    leased: bool,\n    // Last: clear the original loan and retire payloads before its actual wake.\n    lease_release: Option<concread::release::DeferredRelease>,\n    pub(super) attachment_releases: concread::release::DeferredReleaseBatch,\n}',)),
    ('crates/iroha_core/src/state/storage_transactions/history.rs', 'method', 'Pending::new', ('    fn new(\n        predecessor: Identity,\n        latest: Option<Tip>,\n        replacement: bool,\n        next_sequence: u64,\n        attachment_releases: concread::release::DeferredReleaseBatch,\n    ) -> Self {\n        Self {\n            predecessor,\n            latest,\n            replacement,\n            next_sequence,\n            batch: None,\n            next: 0,\n            work: None,\n            next_identity: None,\n            baseline: None,\n            leased: false,\n            lease_release: None,\n            attachment_releases,\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history.rs', 'method', 'Pending::advance', ('    fn advance(&mut self, storage: &TransactionsStorage) -> Result<(), MembershipAdmissionError> {\n        if self.work.is_none() {\n            let len = if self.replacement {\n                0\n            } else {\n                self.latest.as_ref().map_or(0, |b| b.transactions.len())\n            };\n            let batch_layout = std::alloc::Layout::array::<Key>(len).map_err(|_| {\n                MembershipAdmissionError::Capacity(AllocationRefusal::DemandOverflow)\n            })?;\n            let identity_layout = Identity::layout();\n            let wait = storage.released.observe();\n            let writer = storage\n                .blocks\n                .try_write_admitted_with_footprint(|existing, additional| {\n                    let required = existing\n                        .bytes()\n                        .checked_add(additional.bytes())\n                        .and_then(|n| n.checked_add(batch_layout.size()))\n                        .and_then(|n| n.checked_add(identity_layout.size().checked_mul(2)?))\n                        .ok_or(MembershipAdmissionError::Capacity(\n                            AllocationRefusal::DemandOverflow,\n                        ))?;\n                    if required > storage.budget.limit_bytes() {\n                        return Err(MembershipAdmissionError::Capacity(\n                            AllocationRefusal::ExceedsLimit {\n                                requested_bytes: required,\n                                limit_bytes: storage.budget.limit_bytes(),\n                            },\n                        ));\n                    }\n                    // The callback precedes every cursor shell allocation. Fund\n                    // its exact demand with the missing original batch and next\n                    // identity in one pool acquisition before sorting or building\n                    // any of them. Already retained siblings remain owned by this\n                    // Pending across a later private-tree insertion refusal.\n                    let initial_bytes = additional\n                        .bytes()\n                        .checked_add(if self.batch.is_none() {\n                            batch_layout.size()\n                        } else {\n                            0\n                        })\n                        .and_then(|n| {\n                            n.checked_add(if self.next_identity.is_none() {\n                                identity_layout.size()\n                            } else {\n                                0\n                            })\n                        })\n                        .ok_or(MembershipAdmissionError::Capacity(\n                            AllocationRefusal::DemandOverflow,\n                        ))?;\n                    let mut reservation = storage\n                        .budget\n                        .try_reserve_bytes(initial_bytes)\n                        .map_err(MembershipAdmissionError::Capacity)?;\n                    if self.next_identity.is_none() {\n                        let charge = reservation\n                            .try_split(identity_layout)\n                            .expect("complete original identity layout was admitted");\n                        let shell = Reserved::<IdentityState, AllocationCharge>::try_new(charge)\n                            .map_err(|(_charge, error)| MembershipAdmissionError::Allocator {\n                                requested_bytes: error.layout().size(),\n                            })?;\n                        self.next_identity = Some(shell.initialize(IdentityState::default()));\n                    }\n                    if self.batch.is_none() {\n                        let charge = reservation\n                            .try_split(batch_layout)\n                            .expect("complete original batch layout was admitted");\n                        let mut batch = ChargedBuffer::try_from_charge(len, charge).map_err(\n                            |(_charge, error)| match error {\n                                ChargedBufferFromChargeError::Allocator { layout } => {\n                                    MembershipAdmissionError::Allocator {\n                                        requested_bytes: layout.size(),\n                                    }\n                                }\n                                ChargedBufferFromChargeError::DemandOverflow\n                                | ChargedBufferFromChargeError::LayoutMismatch { .. } => {\n                                    unreachable!("checked original batch layout and charge")\n                                }\n                            },\n                        )?;\n                        if !self.replacement\n                            && let Some(latest) = &self.latest\n                        {\n                            for key in &latest.transactions {\n                                batch\n                                    .append(std::slice::from_ref(key))\n                                    .expect("exact preceding tip count");\n                            }\n                        }\n                        batch.as_mut_slice().sort_unstable();\n                        self.batch = Some(batch);\n                    }\n                    debug_assert_eq!(reservation.remaining_bytes(), additional.bytes());\n                    Ok(Policy(reservation))\n                })\n                .map_err(|error| match error {\n                    MapAdmissionError::Busy => MembershipAdmissionError::Busy(wait.clone()),\n                    MapAdmissionError::Poisoned => MembershipAdmissionError::Poisoned,\n                    MapAdmissionError::Changed => MembershipAdmissionError::Changed(wait.clone()),\n                    MapAdmissionError::Planning(e) => MembershipAdmissionError::Planning(e),\n                    MapAdmissionError::Refused(e) => e,\n                })?;\n            self.baseline = Some(writer.predecessor().retain());\n            self.work = Some(writer.detach());\n        }\n        let batch = self.batch.as_ref().expect("original funded batch");\n        let work = self.work.as_mut().expect("original successor");\n        while self.next < batch.as_slice().len() {\n            let key = batch.as_slice()[self.next];\n            let height = self\n                .latest\n                .as_ref()\n                .expect("nonempty promotion has a tip")\n                .height;\n            let wait = storage.released.observe();\n            let result = work.try_insert_admitted(key, height, |d| admit(&storage.budget, d));\n            if let Err((_, error)) = result {\n                // Distinguish a permanently impossible original successor from\n                // credits retained by other readers. This floor is diagnostic,\n                // not an authorization and is computed only after refusal.\n                let error = edit_error(error, wait);\n                if let MembershipAdmissionError::Capacity(AllocationRefusal::Capacity {\n                    requested_bytes,\n                    ..\n                }) = error\n                {\n                    let floor = work\n                        .required_allocation_floor()\n                        .map_err(MembershipAdmissionError::Planning)?\n                        .bytes();\n                    let batch_bytes = std::alloc::Layout::array::<Key>(batch.capacity())\n                        .map_err(|_| {\n                            MembershipAdmissionError::Capacity(AllocationRefusal::DemandOverflow)\n                        })?\n                        .size();\n                    let required = floor\n                        .checked_add(batch_bytes)\n                        .and_then(|n| n.checked_add(Identity::layout().size() * 2))\n                        .and_then(|n| n.checked_add(requested_bytes))\n                        .ok_or(MembershipAdmissionError::Capacity(\n                            AllocationRefusal::DemandOverflow,\n                        ))?;\n                    if required > storage.budget.limit_bytes() {\n                        return Err(MembershipAdmissionError::Capacity(\n                            AllocationRefusal::ExceedsLimit {\n                                requested_bytes: required,\n                                limit_bytes: storage.budget.limit_bytes(),\n                            },\n                        ));\n                    }\n                }\n                return Err(error);\n            }\n            self.next += 1;\n        }\n        Ok(())\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history.rs', 'method', 'Pending::release_loan', ('    pub(super) fn release_loan(&mut self) -> Option<concread::release::DeferredRelease> {\n        if self.leased {\n            self.predecessor.loaned.store(false, Ordering::Release);\n            self.leased = false;\n        }\n        self.lease_release.take()\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history.rs', 'method', 'Pending::drop', ('    fn drop(&mut self) {\n        // The actual release remains a last field, after the flag and all payloads.\n        if self.leased {\n            self.predecessor.loaned.store(false, Ordering::Release);\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'enum', 'Phase', ("enum Phase<'a> {\n    Original(Work),\n    Acquired(Acquired<'a>),\n    Preparing(Preparing<'a>),\n    Abandoned(Abandoned),\n    Published(Retired),\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'struct', 'Cleanup', ('pub(super) struct Cleanup {\n    _pending: Option<Pending>,\n    _abandoned: Option<Abandoned>,\n    _retired: Option<Retired>,\n    _reader: Option<DeferredRelease>,\n    _writer: Option<DeferredRelease>,\n    _loan: Option<DeferredRelease>,\n}',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'struct', 'Slot', ("pub(super) struct Slot<'a> {\n    target: &'a TransactionsStorage,\n    pending: Option<Pending>,\n    phase: Option<Phase<'a>>,\n    prepared: bool,\n    reader_release: Option<DeferredRelease>,\n    writer_release: Option<DeferredRelease>,\n    loan_release: Option<DeferredRelease>,\n}",)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::new', ('    pub(super) fn new(target: &\'a TransactionsStorage, mut pending: Pending) -> Self {\n        let work = pending\n            .work\n            .take()\n            .expect("original complete private successor");\n        Self {\n            target,\n            pending: Some(pending),\n            phase: Some(Phase::Original(work)),\n            prepared: false,\n            reader_release: None,\n            writer_release: None,\n            loan_release: None,\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::next_sequence', ('    pub(super) fn next_sequence(&self) -> u64 {\n        self.pending\n            .as_ref()\n            .expect("original pending metadata")\n            .next_sequence\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::next_identity', ('    pub(super) fn next_identity(&mut self) -> Identity {\n        self.pending\n            .as_mut()\n            .expect("original pending metadata")\n            .next_identity\n            .take()\n            .expect("prepaid next identity")\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::prepare', ('    pub(super) fn prepare(&mut self) -> Result<(), MembershipAdmissionError> {\n        assert!(!self.prepared, "original history preparation is one-shot");\n        if matches!(self.phase, Some(Phase::Original(_))) {\n            let Some(Phase::Original(work)) = self.phase.take() else {\n                unreachable!()\n            };\n            let wait = self.target.released.observe();\n            let acquired = match self.target.blocks.try_acquire_owned_retained(work) {\n                Ok(acquired) => self.target.released.guard(acquired),\n                Err((work, error)) => {\n                    self.phase = Some(Phase::Original(work));\n                    return Err(physical_error(error, wait));\n                }\n            };\n            self.phase = Some(Phase::Acquired(acquired));\n        }\n        if matches!(self.phase, Some(Phase::Acquired(_))) {\n            let Some(Phase::Acquired(acquired)) = self.phase.take() else {\n                unreachable!()\n            };\n            let wait = self.target.released.observe();\n            let writer = match acquired.try_map_preserving_release(|a| a.validate()) {\n                Ok(writer) => writer,\n                Err((acquired, error)) => {\n                    self.phase = Some(Phase::Acquired(acquired));\n                    return Err(physical_error(error, wait));\n                }\n            };\n            self.phase = Some(Phase::Preparing(\n                writer.map_preserving_release(|w| w.commit_slot()),\n            ));\n        }\n        let wait = self.target.blocks.observe_reader_release();\n        let Some(Phase::Preparing(slot)) = self.phase.as_mut() else {\n            unreachable!()\n        };\n        slot.try_prepare()\n            .map_err(|error| physical_error(error, wait))?;\n        self.prepared = true;\n        Ok(())\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::is_prepared', ('    pub(super) fn is_prepared(&self) -> bool {\n        self.prepared\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::publish', ('    pub(super) fn publish(&mut self) {\n        assert!(self.prepared, "original prepared membership history");\n        let Some(Phase::Preparing(slot)) = self.phase.take() else {\n            unreachable!()\n        };\n        let (retired, writer) =\n            slot.release_deferred(|slot| slot.into_prepared().publish().release());\n        self.writer_release = Some(writer);\n        self.phase = Some(Phase::Published(retired));\n        self.prepared = false;\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::release', ('    pub(super) fn release(&mut self) {\n        self.prepared = false;\n        self.phase = match self.phase.take() {\n            Some(Phase::Acquired(acquired)) => {\n                let (work, writer) = acquired.release_deferred(|a| a.abort());\n                self.writer_release = Some(writer);\n                Some(Phase::Original(work))\n            }\n            Some(Phase::Preparing(slot)) => {\n                let ((work, reader), writer) = slot.release_deferred(|slot| {\n                    let (writer, reader) = slot.abort_retaining();\n                    (writer.abort_retaining(), reader)\n                });\n                self.reader_release = reader;\n                self.writer_release = Some(writer);\n                Some(Phase::Abandoned(work))\n            }\n            phase => phase,\n        };\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::recover', ('    pub(super) fn recover(&mut self) -> Pending {\n        self.prepared = false;\n        let work = match self.phase.take().expect("original tree custody") {\n            Phase::Original(work) => work,\n            Phase::Acquired(acquired) => {\n                let (work, writer) = acquired.release_deferred(|a| a.abort());\n                self.writer_release = Some(writer);\n                work\n            }\n            Phase::Preparing(slot) => {\n                let ((work, reader), writer) = slot.release_deferred(|slot| {\n                    let (writer, reader) = slot.abort_retaining();\n                    (writer.detach(), reader)\n                });\n                self.reader_release = reader;\n                self.writer_release = Some(writer);\n                work\n            }\n            _ => panic!("terminal history is not retry authority"),\n        };\n        let mut pending = self.pending.take().expect("original pending metadata");\n        pending.work = Some(work);\n        pending\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::recover_detached', ('    pub(super) fn recover_detached(&mut self) -> Pending {\n        let mut pending = self.recover();\n        self.loan_release = pending.release_loan();\n        pending\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::cleanup', ('    pub(super) fn cleanup(&mut self) -> Cleanup {\n        self.release();\n        let mut pending = self.pending.take();\n        let mut abandoned = None;\n        let mut retired = None;\n        match self.phase.take() {\n            Some(Phase::Original(work)) => {\n                pending.as_mut().expect("original metadata").work = Some(work)\n            }\n            Some(Phase::Abandoned(work)) => abandoned = Some(work),\n            Some(Phase::Published(work)) => retired = Some(work),\n            None => {}\n            _ => unreachable!("all physical history owners released"),\n        }\n        Cleanup {\n            _pending: pending,\n            _abandoned: abandoned,\n            _retired: retired,\n            _reader: self.reader_release.take(),\n            _writer: self.writer_release.take(),\n            _loan: self.loan_release.take(),\n        }\n    }',)),
    ('crates/iroha_core/src/state/storage_transactions/history_slot.rs', 'method', 'Slot::drop', ('    fn drop(&mut self) {\n        self.release();\n    }',)),
)
MEMBERSHIP_HISTORY_BINDINGS += (('crates/iroha_core/src/state/storage_transactions/history.rs', 'method', 'TransactionsStorage::prepare_next_block', ('    pub(crate) fn prepare_next_block(\n        &self,\n        replacement: bool,\n    ) -> Result<Pending, MembershipAdmissionError> {\n        self.budget.with_deferred_refund_notifications(|_| {\n            // This custody is declared before either lock so actual retained\n            // notices also retire after both locks on unwind, not only success.\n            let mut retired = None;\n            let wait = self.released.observe();\n            let guard = self\n                .write_lock\n                .try_lock()\n                .ok_or_else(|| MembershipAdmissionError::Busy(wait.clone()))?;\n            let guard = self.released.guard(guard);\n            if guard.loaned.load(Ordering::Acquire) {\n                let identity = guard.clone();\n                drop(guard);\n                // Observe after this read-only acquisition releases; its own\n                // release must not turn an outstanding loan into a busy loop.\n                let after_release = self.released.observe();\n                return Err(MembershipAdmissionError::Busy(\n                    if identity.loaned.load(Ordering::Acquire) {\n                        after_release\n                    } else {\n                        wait\n                    },\n                ));\n            }\n            let next_sequence = self\n                .publication_sequence\n                .load(Ordering::Relaxed)\n                .checked_add(2)\n                .ok_or(MembershipAdmissionError::Planning(PlanningError::Overflow))?;\n            let mut pending = self.pending.lock();\n            if pending.as_ref().is_some_and(|p| {\n                !Identity::ptr_eq(&p.predecessor, &guard) || p.replacement != replacement\n            }) {\n                retired = pending.take();\n            }\n            let original = pending.get_or_insert_with(|| {\n                Pending::new(\n                    guard.clone(),\n                    self.latest_block.load_full(),\n                    replacement,\n                    next_sequence,\n                    self.released.deferred_batch(),\n                )\n            });\n            #[cfg(test)]\n            tests::panic_before_advance();\n            let result = original.advance(self);\n            let mut ready = result\n                .is_ok()\n                .then(|| pending.take().expect("completed original preparation"));\n            if let Some(ready) = ready.as_mut() {\n                ready.predecessor.loaned.store(true, Ordering::Release);\n                ready.leased = true;\n            }\n            drop(pending);\n            if let Some(ready) = ready.as_mut() {\n                ready.lease_release = Some(guard.release_deferred(drop).1);\n            } else {\n                drop(guard);\n            }\n            drop(retired);\n            result?;\n            Ok(ready.expect("successful original preparation"))\n        })\n    }',)), ('crates/iroha_core/src/state/storage_transactions/history.rs', 'method', 'TransactionsStorage::retain_preparation', ('    pub(crate) fn retain_preparation(&self, mut original: Pending) {\n        self.budget.with_deferred_refund_notifications(|_| {\n            // This method is called only after the enclosing aggregate releases\n            // all physical fences. No other accepted preparation may be displaced.\n            let guard = self.released.guard(self.write_lock.lock());\n            let mut pending = self.pending.lock();\n            let current = Identity::ptr_eq(&original.predecessor, &guard);\n            if current {\n                assert!(\n                    original.leased && pending.is_none(),\n                    "exclusive original preparation loan"\n                );\n            }\n            let notice = original.release_loan();\n            let attachments = std::mem::replace(\n                &mut original.attachment_releases,\n                self.released.deferred_batch(),\n            );\n            let stale = if current {\n                *pending = Some(original);\n                None\n            } else {\n                Some(original)\n            };\n            drop(pending);\n            drop(guard);\n            drop(stale);\n            drop(attachments);\n            drop(notice);\n        });\n    }',)), ('crates/iroha_core/src/state/storage_transactions.rs', 'method', 'TransactionsStorage::attach_prepared', ('    pub(crate) fn attach_prepared<\'a>(\n        &\'a self,\n        pending: &mut Option<history::Pending>,\n    ) -> Result<TransactionsBlock<\'a>, MembershipAdmissionError> {\n        let original = pending\n            .as_mut()\n            .expect("original pre-World membership preparation");\n        let wait = self.released.observe();\n        // A foreign family cannot acquire this target\'s notification authority.\n        let baseline = self\n            .blocks\n            .read_predecessor(original.baseline.as_ref().expect("original history cut"))\n            .ok_or_else(|| MembershipAdmissionError::Changed(wait.clone()))?;\n        let guard = self\n            .write_lock\n            .try_lock()\n            .ok_or_else(|| MembershipAdmissionError::Busy(wait.clone()))?;\n        if !Identity::ptr_eq(&guard, &original.predecessor) {\n            let released = self\n                .released\n                .guard(guard)\n                .try_release_into(&mut original.attachment_releases, drop);\n            assert!(released.is_ok(), "original preparation release family");\n            return Err(MembershipAdmissionError::Changed(wait));\n        }\n        let original = pending.take().expect("checked original preparation");\n        let revert = original.replacement;\n        Ok(TransactionsBlock {\n            latest_block_ref: &self.latest_block,\n            budget_ref: &self.budget,\n            blocks_ref: &self.blocks,\n            baseline,\n            publication_sequence: &self.publication_sequence,\n            _guard: block::MembershipWriter::new(\n                self.released.guard(guard),\n                Some(history_slot::Slot::new(self, original)),\n            ),\n            revert,\n            current_block: None,\n        })\n    }',)))
MEMBERSHIP_BINDINGS += MEMBERSHIP_HISTORY_BINDINGS

MEMBERSHIP_SOURCE_RELATIVES = (
    Path("scripts/formal/sumeragi_v2_multilane_membership_contract.py"),
    Path("pytests/scripts/sumeragi_v2_multilane_membership_contract_test.py"),
    Path(STATE), Path(STORAGE), Path(CAPTURE), Path(CAPTURE_TESTS),
    Path(DETACHED), Path(DETACHED_TESTS), Path(HISTORY), Path(HISTORY_SLOT),
)


def validate_membership_contract(
    root: Path, models: Any, errors: list[str], rust_binding_item: Callable,
) -> None:
    """Require original writer ownership, exact admission and infallible consumption."""

    for name in MODELS:
        owners = [m for m in models if isinstance(m, dict) and m.get("module") == name]
        bindings = owners[0].get("production_symbols", ()) if len(owners) == 1 else ()
        for path, kind, symbol, tokens in MEMBERSHIP_BINDINGS:
            matches = [b for b in bindings if isinstance(b, dict)
                       and (b.get("path"), b.get("kind"), b.get("symbol")) == (path, kind, symbol)]
            if len(matches) != 1:
                errors.append(f"membership ledger {name} owner {symbol} must occur exactly once")
            elif tuple(matches[0].get("required_tokens", ())) != tokens:
                errors.append(f"membership reviewed tokens changed for {name}::{symbol}")

    items: dict[str, str] = {}
    raw_items: dict[str, str] = {}
    for path, kind, symbol, tokens in MEMBERSHIP_BINDINGS + (
        (STATE, "fn", "commit_inner", ()),
        (STATE, "struct", "StateBlockFields", ()),
    ):
        item = rust_binding_item(root, path, kind, symbol, "membership ownership", errors)
        if item is not None:
            raw_items[symbol] = item
            items[symbol] = _code(item)
            for token in tokens:
                if _code(token) not in items[symbol]:
                    errors.append(f"membership {symbol} is missing executable relation {token!r}")

    def require(symbol: str, relation: str) -> None:
        if symbol in items and _code(relation) not in items[symbol]:
            errors.append(f"membership {symbol} is missing executable relation {relation!r}")

    require("TransactionsBlock::prepare_commit", "fn prepare_commit(self,)")
    require("TransactionsBlock::prepare_commit",
            "let mut capture = self.capture_slot(); capture.try_prepare()?; Ok(capture.into_prepared())")
    require("TransactionsBlock::admit_publication",
            "if !self.revert && previous_block.as_ref().is_some_and(|previous_block| { previous_block.height == current_block.height && previous_block.transactions == current_block.transactions }) { return Ok(MembershipPublication::Repeated); }")
    require("TransactionsBlock::admit_publication",
            "if expected_current_height != current_height { return Err(TransactionsBlockError::HeightMismatch {")
    require("TransactionsBlock::admit_publication",
            "if self.revert { Ok(MembershipPublication::Replace { current: current_block.clone(), }) } else { Ok(MembershipPublication::Advance { _previous: previous_block, current: current_block.clone(), }) }")
    require("PreparedTransactionsBlock::publish",
            'fn publish(mut self) -> TransactionsPublicationRetirement { self.try_prepare_physical().expect("fixture physical membership admission"); self.publish_prepared() }')
    require("TransactionsBlock::admit_publication",
            "self._guard.identity(); let previous_block = self.latest_block_ref.load_full();")
    require("PreparedTransactionsBlock::publish_in_place", "if !matches!(&self.publication, MembershipPublication::Repeated) {")
    require("PreparedTransactionsBlock::publish_in_place",
            'self.assert_unpublished(); assert!(self.is_physically_prepared(), "original physical membership preparation must precede publication"); self.block._guard.identity(); self.publication_started = true; if !matches!(&self.publication, MembershipPublication::Repeated)')
    require("PreparedTransactionsBlock::publish_in_place",
            "if !matches!(&self.publication, MembershipPublication::Repeated) { std::mem::swap(self.block._guard.identity_mut(), &mut self.next_identity); }")
    require("PreparedTransactionsBlock::publish_in_place",
            "self.block.release_writers(); self.published = true;")
    require("PreparedTransactionsBlock::detach", "self.detach_retaining().0")
    require("PreparedTransactionsBlock::detach_retaining", "let release = _guard.into_release();")
    require("DetachedTransactionsBlock::observe_predecessor",
            "let Some(guard) = storage.write_lock.try_lock() else { return MembershipPredecessorStatus::Busy; }; let guard = storage.released.guard(guard); if Identity::ptr_eq(&guard, &self.predecessor_identity)")
    require("DetachedTransactionsBlock::try_prepare_publication",
            "let mut slot = self.publication_slot(storage); match slot.try_prepare(admit)")
    require("DetachedTransactionsBlock::try_prepare_publication",
            "let original = slot.recover_original(); Err((original, error, slot.into_cleanup()))")
    require("PreparedTransactionsBlock::as_block",
            "fn as_block(&self) -> &TransactionsBlock<'storage> { &self.block }")

    prepared = raw_items.get("PreparedTransactionsBlock", "")
    if re.search(r"(?m)^\s*pub(?:\([^)]*\))?\s+\w+\s*:", prepared) or "&mut" in prepared:
        errors.append("membership prepared owner exposes mutable authority")
    detached = raw_items.get("DetachedTransactionsBlock", "")
    if re.search(r"(?m)^\s*pub(?:\([^)]*\))?\s+\w+\s*:", detached) or "&mut" in detached:
        errors.append("membership detached owner exposes mutable authority")
    if any(owner in detached for owner in ("MutexGuard", "ReleaseGuard", ": TransactionsBlock<")):
        errors.append("membership detached owner retains a physical writer")
    for forbidden in ("admit_publication(", "validate_commit(", "prepare_commit(",
                      "load_full(", "Err(", "Result<", "?", ".lock("):
        if any(forbidden in items.get(symbol, "") for symbol in (
            "PreparedTransactionsBlock::publish_prepared", "PreparedTransactionsBlock::publish_in_place",
        )):
            errors.append(f"membership publication repeats admission or can refuse: {forbidden}")

    # The physical preparation seam is production; standalone fixture helpers
    # cannot become an alternate publication path.
    storage_source = (root / STORAGE).read_text(encoding="utf-8")
    for method, attribute in (
        ("TransactionsStorage::block_impl", '#[cfg(any(test, feature = "iroha-core-tests", feature = "bench"))]'),
        ("PreparedTransactionsBlock::publish", "#[cfg(test)]"),
    ):
        item = raw_items.get(method)
        if item is not None:
            # The attribute belongs to this exact definition; a commented gate
            # or a gate on the previous helper cannot qualify a production body.
            prefix = storage_source[:storage_source.index(item)]
            if not _code(prefix).endswith(_code(attribute)):
                errors.append(f"membership fixture-only owner became production: {method}")

    # Both the inherent aggregate delegate and trait delegate must preserve the
    # same terminal release engine. The exact parser deliberately rejects an
    # ambiguous Type::method, so verify the two defining bodies explicitly.
    from check_sumeragi_v2_multilane_models import _extract_rust_binding_items
    capture_source = (root / CAPTURE).read_text(encoding="utf-8")
    delegates = _extract_rust_binding_items(
        capture_source, "method", "TransactionsBlockField::release_writers",
    )
    expected_delegates = (
        "pub(crate) fn release_writers(&mut self) { self.slot.release(); }",
        "fn release_writers(&mut self) { self.slot.release(); }",
    )
    if tuple(map(_code, delegates)) != tuple(map(_code, expected_delegates)):
        errors.append("membership terminal release delegates differ from original slot")
    for owner in ("TransactionsBlock", "TransactionsBlockField"):
        for method in ("publish_in_place", "publish_prepared"):
            definitions = _extract_rust_binding_items(
                capture_source + "\n" + (root / STORAGE).read_text(encoding="utf-8"),
                "method", f"{owner}::{method}",
            )
            if owner == "TransactionsBlock" and definitions:
                errors.append("membership executing block exposes borrowed publication")
            if owner == "TransactionsBlockField" and method == "publish_prepared":
                if len(definitions) != 1 or _code(definitions[0]) != _code(
                    "pub(crate) fn publish_prepared(&mut self) { self.slot.publish_prepared(); }"
                ):
                    errors.append("membership publication wrapper exposes external authority")

    # The actual attached State owner retains membership and every sibling
    # throughout refusal/unwind. No consumed membership temporary is recreated.
    require("StateBlockFields",
            "pub transactions: storage_transactions::TransactionsBlockField<'state>")
    commit = items.get("commit_inner", "")
    cursor = 0
    for relation in (
        "let mut publication_notice = self.state_ref.state_view_publication();",
        "let state_commit_authorization = state_commit_authorization;",
        "let mut world_effects = None;",
        "let mut commit_fence = self.state_ref.state_commit_lock.defer_notifications();",
        "let mut write_fence = self.state_write_lock.defer_notifications();",
        "let mut lifecycle_fence = self.state_ref.lane_lifecycle_lock.defer_notifications();",
        "hash_budget.with_deferred_refund_notifications(|_| {",
        "let mut this = self;",
        "} = this.fields.as_mut().expect(\"original executing State\");",
        "let _state_commit_lock = commit_fence.lock();",
        "let tx_validate_result = transactions.try_prepare_publication();",
        "tx_validate_result?;",
        "let autoscale_lifecycle_guard",
        "autoscale_retirement_queue_veto.as_mut()",
        "world_effects = Some(world_commit::PreparedWorldCommit::prepare_overlay(",
        "state_ref.apply_committed_autoscale_lane_geometry(",
        "if let Err(error) = transactions.try_prepare_physical()",
        "block_hashes.try_prepare_publication().map_err(",
        "world.prepare_publication();",
        "canonical_runtime.prepare_publication();",
        "let _view_generation = publication_notice.begin();",
        "transactions.publish_prepared();",
        "canonical_runtime.publish_prepared();",
        "world.publish_prepared();",
        "block_hashes.publish_prepared();",
        "drop(_state_commit_lock);",
    ):
        normalized = _code(relation)
        position = commit.find(normalized, cursor)
        if position < 0 or commit.count(normalized) != 1:
            errors.append(f"membership State publication misses or reorders {relation!r}")
            break
        cursor = position + len(normalized)
    # A temporary physical refusal keeps the original history loan until all
    # State sibling writers have retired, then returns it to the same storage.
    for relation in (
        "let membership_target = &self.state_ref.transactions; let membership_budget = membership_target.budget.clone(); let mut membership_retry = None;",
        "let result = membership_budget.with_deferred_refund_notifications(|_| hash_budget.with_deferred_refund_notifications(|_| {",
        "if let Err(error) = transactions.try_prepare_physical() { if matches!(&error, TransactionsBlockError::MembershipAdmission(storage_transactions::MembershipAdmissionError::Busy(_))) { membership_retry = Some(transactions.recover_preparation()); } return Err(error); }",
        "Ok(()) })); if let Some(original) = membership_retry { membership_target.retain_preparation(original); } result",
    ):
        require("commit_inner", relation)
    if "into_fields(" in commit or "transactions.prepare_commit(" in commit:
        errors.append("membership State publication consumes its original attached owner")
