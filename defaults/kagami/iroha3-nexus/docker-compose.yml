# Intentionally non-deployable. Public Nexus has no repository-owned canonical
# XOR asset-definition identity, so this checked-in template cannot be signed.
# Regenerate the complete four-validator bundle with:
#
# cargo xtask kagami-profiles --profile iroha3-nexus \
#   --nexus-xor-asset-definition-id <BASE58>
services: {}
