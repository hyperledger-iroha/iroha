# Intentionally non-deployable. Materialize `genesis.template.json` with an
# explicitly owned disposable-dev KAGEMUSHA authority, then regenerate the
# complete profile bundle before enabling services.
services: {}
