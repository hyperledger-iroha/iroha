// Durable StateMap set and get wrapped in a seiyaku.
seiyaku MapOps {
    state StateMap<int, int> values;
    kotoage fn map_example() -> int authorize("Entry") {
        values[1] = 42;
        let v = values.get(1).unwrap_or(0);
        return v;
    }
}
