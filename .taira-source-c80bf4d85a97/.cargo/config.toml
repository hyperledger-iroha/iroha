# Keep plain `cargo test` stable on WSL and memory-constrained VMs. Cargo and
# libtest otherwise default to the logical CPU count, which can fan out into
# many rustc processes or hundreds of runtime test threads before failures
# degrade into recoverable per-test errors.
[build]
jobs = 1

[env]
RUST_TEST_THREADS = { value = "1", force = false }
