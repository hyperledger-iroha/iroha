---
lang: ru
direction: ltr
source: docs/portal/docs/sorafs/orchestrator-config.ur.md
status: complete
generator: docs/portal/scripts/sync-i18n.mjs
translator: machine-google-reviewed
translation_last_reviewed: 2026-02-07
---

---
идентификатор: конфигурация оркестратора
title: SoraFS آرکسٹریٹر کنفیگریشن
Sidebar_label: آرکسٹریٹر کنفیگریشن
описание: ملٹی سورس fetch آرکسٹریٹر کو کنفیگر کریں، ناکامیوں کی تشریح کریں، اور ٹیلیمیٹری آؤٹ پٹ ڈی بگ کریں۔
---

:::примечание
یہ صفحہ `docs/source/sorafs/developer/orchestrator.md` کی عکاسی کرتا ہے۔ جب تک پرانی ڈاکیومنٹیشن ریٹائر نہ ہو جائے, دونوں نقول کو ہم آہنگ رکھیں۔
:::

# ملٹی سورس fetch آرکسٹریٹر گائیڈ

SoraFS Чтобы получить доступ к рекламным объявлениям
پروائیڈرز سیٹ سے ڈٹرمنسٹک اور متوازی ڈاؤن لوڈز چلاتا ہے۔ یہ گائیڈ وضاحت کرتی
ہے کہ آرکسٹریٹر کیسے کنفیگر کیا جائے, رول آؤٹس کے کوران کون سی ناکامی سگنلز
Если вы хотите, чтобы вы знали, как это сделать, вы можете сделать это самостоятельно.

## 1. کنفیگریشن اوور ویو

В качестве примера можно привести следующие:

| Сэнсэй | مقصد | نوٹس |
|------|------|------|
| `OrchestratorConfig.scoreboard` | Весы и весы, которые можно использовать для тренировки ہے، اور آڈٹس کے لیے Табло JSON для просмотра | `crates/sorafs_car::scoreboard::ScoreboardConfig` مبنی۔ |
| `OrchestratorConfig.fetch` | رن ٹائم حدود لگاتا ہے (бюджеты повторных попыток, границы параллелизма, переключатели проверки)۔ | `crates/sorafs_car::multi_fetch` میں `FetchOptions` سے میپ ہوتا ہے۔ |
| Интерфейс CLI/SDK | Одноранговые узлы Доступ к регионам телеметрии и отказу/повышению уровня доступа | `sorafs_cli fetch` یہ flags براہ راست دیتا ہے؛ SDKs `OrchestratorConfig` کے ذریعے پاس کرتے ہیں۔ |

`crates/sorafs_orchestrator::bindings` Использование помощников JSON для Norito
Сериализация JSON и другие привязки SDK.
بنایا جا سکے۔

### 1.1 Поддержка JSON

```json
{
  "scoreboard": {
    "latency_cap_ms": 6000,
    "weight_scale": 12000,
    "telemetry_grace_secs": 900,
    "persist_path": "/var/lib/sorafs/scoreboards/latest.json"
  },
  "fetch": {
    "verify_lengths": true,
    "verify_digests": true,
    "retry_budget": 4,
    "provider_failure_threshold": 3,
    "global_parallel_limit": 8
  },
  "telemetry_region": "iad-prod",
  "max_providers": 6,
  "transport_policy": "soranet_first"
}
```

Для создания многоуровневого слоя `iroha_config` (`defaults/`, пользователь, фактический)
Возможности развертывания и ограничения ограничений
Развертывание SNNet-5a и резервный вариант только для прямого подключения.
`docs/examples/sorafs_direct_mode_policy.json`
`docs/source/sorafs/direct_mode_pack.md` دیکھیں۔

### 1.2 Переопределение соответствия

SNNet-9 обеспечивает возможность подключения к сети SNNet-9. Norito JSON
Если вы хотите использовать `compliance`, вырежьте вырезы, чтобы получить доступ к ним.
Для прямых трансляций доступны следующие варианты:

```json
"compliance": {
  "operator_jurisdictions": ["US", "JP"],
  "jurisdiction_opt_outs": ["US"],
  "blinded_cid_opt_outs": [
    "C6B434E5F23ABD318F01FEDB834B34BD16B46E0CC44CD70536233A632DFA3828"
  ],
  "audit_contacts": ["mailto:compliance@example.org"]
}
```- `operator_jurisdictions` ISO‑3166 альфа‑2 کوڈز ڈکلیئر کرتا ہے جہاں یہ
  آرکسٹریٹر انسٹینس آپریٹ کرتا ہے۔ синтаксический анализ کے دوران کوڈز прописные буквы میں нормализовать
  ہوتے ہیں۔
- `jurisdiction_opt_outs` گورننس رجسٹر کو зеркало کرتا ہے۔ جب کوئی آپریٹر
  юрисдикция فہرست میں ہو، آرکسٹریٹر `transport_policy=direct-only` نافذ
  کرتا ہے اور резервная причина `compliance_jurisdiction_opt_out` испускает کرتا ہے۔
- Дайджесты манифеста `blinded_cid_opt_outs` (скрытые CID, шестнадцатеричные заглавные буквы).
  ہے۔ сопоставление полезных данных только прямое планирование
  `compliance_blinded_cid_opt_out` резервный вариант
- `audit_contacts` в URI, где можно получить доступ к GAR
  playbooks میں شائع کرنے کی توقع کرتی ہے۔
- `attestations` کمپلائنس کی سائنڈ پیکٹس محفوظ کرتا ہے۔ ہر запись ایک необязательно
  `jurisdiction` (ISO-3166 альфа-2), `document_uri`, 64-символьный `digest_hex`,
  временная метка выдачи `issued_at_ms`, или необязательный `expires_at_ms`.
  یہ артефакты آرکسٹریٹر کی контрольный список аудита میں جاتے ہیں تاکہ инструменты управления
  переопределяет کو سائنڈ دستاویزات سے جوڑ سکے۔

Как сделать так, чтобы наложение слоев было как можно лучше
ڈٹرمنسٹک переопределяет ملیں۔ Подсказки по режиму записи и подсказки по режиму записи _بعد_
Для этого используйте SDK `upload-pq-only`, который используется в манифесте юрисдикции.
отказ от участия только прямой транспорт پر لے جاتے ہیں اور جب کوئی соответствие
провайдер

Каталоги отказа от Canonical `governance/compliance/soranet_opt_outs.json` میں
موجود ہیں؛ Совет по управлению отметил релизы کے ذریعے اپڈیٹس شائع کرتی ہے۔
Аттестации سمیت مکمل مثال `docs/examples/sorafs_compliance_policy.json` میں
دستیاب ہے، اور آپریشنل پروسیس
[Пособие по соблюдению требований GAR](../../../source/soranet/gar_compliance_playbook.md)
میں درج ہے۔

### 1.3 CLI и ручки SDK| Флаг / Поле | ثر |
|--------------|-----|
| `--max-peers` / `OrchestratorConfig::with_max_providers` | Здесь можно найти табло с табло, которое можно использовать. `None` پرسیٹ کریں تاکہ تمام, подходящие для участия в программе |
| `--retry-budget` / `FetchOptions::per_chunk_retry_limit` | Повторная попытка фрагмента حد مقرر کرتا ہے۔ حد سے تجاوز `MultiSourceError::ExhaustedRetries` اٹھاتا ہے۔ |
| `--telemetry-json` | снимки задержек/сбоев, построитель табло и внедрение, کرتا ہے۔ `telemetry_grace_secs` سے پرانی ٹیلیمیٹری پرووائیڈرز کو کر دیتی ہے۔ |
| `--scoreboard-out` | حساب شدہ (имеет право + не соответствует требованиям) |
| `--scoreboard-now` | Временная метка табло (в секундах Unix) переопределить کرتا ہے تاکہ прибор захватывает детерминированный رہیں۔ |
| `--deny-provider` / крючок политики оценки | Рекламные объявления کیلیٹ کیے بغیر پرووائیڈرز کو детерминистические طور پر исключают کرتا ہے۔ فوری بلیک لسٹنگ کے لیے مفید۔ |
| `--boost-provider=name:delta` | گورننس весы برقرار رکھتے ہوئے پرووائیڈر کے взвешенные кредиты по круговой системе ایڈجسٹ کرتا ہے۔ |
| `--telemetry-region` / `OrchestratorConfig::with_telemetry_region` | Использование структурированных журналов для создания волнового развертывания и поворота کر سکیں۔ |
| `--transport-policy` / `OrchestratorConfig::with_transport_policy` | اب ڈیفالٹ `soranet-first` ہے کیونکہ ملٹی آرکسٹریٹر baseline ہے۔ Понизить версию директивы соответствия کے لیے `direct-only` استعمال کریں، اور PQ-only Pilots کے لیے `soranet-strict` محفوظ رکھیں؛ соответствие требованиям имеет приоритет |

SoraNet-first с доставкой по умолчанию и откатами, а также блокировщиком SNNet.
حوالہ دینا چاہیے۔ SNNet-4/5/5a/5b/6a/7/8/12/13 может быть подключен к сети
Требуемая поза تب تک صرف
переопределения, управляемые инцидентами, или `direct-only`, чтобы выполнить развертывание
log میں ریکارڈ کرنا لازمی ہے۔

تمام flags اوپر `sorafs_cli fetch` اور `sorafs_fetch` для разработчиков.
Синтаксис `--` SDK и типизированные строители.
ایکسپوز کرتے ہیں۔

### 1.4 Тайник охраны مینجمنٹ

CLI и переключатель защиты SoraNet при проводном подключении или подключении к SNNet-5
транспортное развертывание سے پہلے входные реле کو детерминированные انداز میں pin کر سکیں۔
Ниже приведены флаги, которые можно использовать в следующих случаях:

| Флаг | مقصد |
|------|------|
| `--guard-directory <PATH>` | Используйте JSON для создания консенсуса в режиме ретрансляции (подмножество данных). گیا ہے)۔ Выбор каталога и обновление защитного кэша |
| `--guard-cache <PATH>` | Norito в кодировке `GuardSet`. Создание каталога и возможность повторного использования кэша. |
| `--guard-target <COUNT>` / `--guard-retention-days <DAYS>` | входные охранники есть (3) и окно удержания (30 дней) есть дополнительные переопределения |
| `--guard-cache-key <HEX>` | дополнительный 32-байтовый ключ, Blake3, MAC, защита кеша, тег, повторное использование, проверка, проверка. |

Полезные данные каталога защиты в компактной схеме:

`--guard-directory` флаг اب Norito-закодированная полезная нагрузка `GuardDirectorySnapshotV2`
ожидайте کرتا ہے۔ Бинарный снимок в формате:- `version` — схема (فی الحال `2`).
- И18НИ00000080Х, И18НИ00000081Х, И18НИ00000082Х, И18НИ00000083Х —
  метаданные консенсуса جو ہر встроенный сертификат سے match کرنا ضروری ہے۔
- `validation_phase` — шлюз политики сертификата (`1` = одна подпись Ed25519
  разрешить, `2` = предпочтение двойной подписи, `3` = требование двойной подписи).
- `issuers` — эмитенты управления `fingerprint`, `ed25519_public`, `mldsa65_public`.
  Отпечатки пальцев اس طرح بنائے جاتے ہیں:
  `BLAKE3("soranet.src.v2.issuer" || ed25519 || u32(len(ml-dsa)) || ml-dsa)`.
- `relays` — пакеты SRCv2 доступны (выход `RelayCertificateBundleV2::to_cbor()`).
  ہر дескриптор пакетной ретрансляции, флаги возможностей, политика ML-KEM, Ed25519/ML-DSA-65
  двойные подписи رکھتا ہے۔

Пакет CLI, объявленные ключи эмитента, проверка или проверка каталога.
снимки لازمی ہیں۔

`--guard-directory` позволяет использовать CLI для настройки консенсуса и кэширования
کے ساتھ merge کیا جا سکے۔ селектор закреплен на страже کو برقرار رکھتا ہے جو ابھی
Окно хранения: каталог, соответствующий требованиям Срок действия реле истек
записи کو заменить کرتے ہیں۔ Загрузите обновленный кэш `--guard-cache`
کے путь پر لکھ دی جاتی ہے، جس سے اگلی سیشنز детерминированный رہتی ہیں۔ SDK یہی
поведение `GuardSelector::select(&RelayDirectory, existing_guard_set, now_unix_secs)`
Если вы хотите, чтобы `GuardSet` был `SorafsGatewayFetchOptions`, вы можете использовать `GuardSet`.
воспроизводить کر سکتی ہیں۔

Селектор `ml_kem_public_hex` и защитные устройства с поддержкой PQ могут быть отключены.
Внедрение SNNet-5 в ближайшее время переключатели этапов (`anon-guard-pq`, `anon-majority-pq`,
`anon-strict-pq`) Для классических реле можно использовать понижение уровня: جب PQ
Guard دستیاب ہو تو اضافی Classic Pins ہٹا دیتا ہے تاکہ اگلی سیشنز
гибридное рукопожатие کو ترجیح دیں۔ Краткое описание CLI/SDK и другие варианты сочетания
И18НИ00000104Х/И18НИ00000105Х, И18НИ00000106Х,
И18НИ00000107Х, И18НИ00000108Х, И18НИ00000109Х,
`anonymity_classical_ratio` اور متعلقہ кандидат/дефицит/дельта предложения فیلڈز کے
ذریعے ظاہر کرتے ہیں، جس سے отключения электроэнергии и классические резервные варианты واضح ہو جاتے ہیں۔

Каталоги защиты `certificate_base64` можно использовать для встраивания пакета SRCv2.
سکتی ہیں۔ Пакет для декодирования и подписи Ed25519/ML-DSA.
повторная проверка сертификата, анализируемого сертификата и охранного кэша, проверка кэша
ہے۔ Сертификат, ключи PQ, настройки рукопожатия, взвешивание
Канонический источник بن جاتا ہے؛ сертификаты с истекшим сроком действия отбросить
управление کے ذریعے распространять ہوتے ہیں اور `telemetry::sorafs.guard` اور
`telemetry::sorafs.circuit` کے ذریعے поверхность ہوتے ہیں، جو validity window,
наборы рукопожатий, двойные подписи или двойные подписи.

Помощники CLI, снимки экрана и издатели, а также синхронизация:

```bash
sorafs_cli guard-directory fetch \
  --url https://directory.soranet.dev/mainnet_snapshot.norito \
  --output ./state/guard_directory.norito \
  --expected-directory-hash <directory-hash-hex>

sorafs_cli guard-directory verify \
  --path ./state/guard_directory.norito \
  --expected-directory-hash <directory-hash-hex>
```

`fetch` Моментальный снимок SRCv2 для проверки подлинности или проверки состояния `verify`
Использование артефактов и конвейера проверки.
Вывод селектора защиты CLI/SDK سے match کرنے и сводный вывод JSON

### 1.5 Менеджер жизненного цикла схемыКаталог релейной защиты Кэш-память Защитный кэш-память Жизненный цикл схемы
менеджер, чтобы получить информацию о схемах SoraNet и предварительной сборке.
обновить کیا جائے۔ Код `OrchestratorConfig`
(`crates/sorafs_orchestrator/src/lib.rs:305`) Если вам нужна информация о том, как это сделать:

- `relay_directory`: снимок каталога SNNet-3 для промежуточных/выходных переходов.
  детерминированный طریقے سے منتخب ہوں۔
- `circuit_manager`: дополнительный режим (включен режим TTL) для цепи TTL
  کو کنٹرول کرتی ہے۔

Norito JSON и `circuit_manager` можно просмотреть в формате:

```json
"circuit_manager": {
  "enabled": true,
  "circuit_ttl_secs": 900
}
```

Данные каталога SDK
`SorafsGatewayFetchOptions::relay_directory`
(`crates/iroha/src/client.rs:320`) Перенаправление вперед или назад через CLI
Подключение к проводному устройству `--guard-directory` دیا جائے
(`crates/iroha_cli/src/commands/sorafs.rs:365`).

Цепи менеджера могут обновить метаданные защиты (конечная точка, ключ PQ).
закрепленная временная метка) بدل جائے یا TTL ہو۔ ہر fetch سے پہلے вызвать ہونے والا
помощник `refresh_circuits` (`crates/sorafs_orchestrator/src/lib.rs:1346`) `CircuitEvent`
لاگز کرتا ہے تاکہ آپریٹرز lifecycle فیصلوں کو ٹریس کر سکیں۔ тест на выдержку
`circuit_manager_latency_soak_remains_stable_across_rotations`
(`crates/sorafs_orchestrator/src/soranet.rs:1479`) Ротация охранников.
задержка دکھاتا ہے؛ Код `docs/source/soranet/reports/circuit_stability.md:1`
دیکھیں۔

### 1.6 Доступ к QUIC-прокси

Доступ к прокси-серверу QUIC и использованию расширений браузера и SDK
адаптеры, сертификаты, управление ключами защитного кэша, а также дополнительные возможности. прокси-петля
Используйте привязку и соединения QUIC, чтобы получить доступ к Norito
Наличие манифеста и сертификата, а также дополнительного ключа защитного кэша.
прокси-сервер генерирует транспортные события `sorafs_orchestrator_transport_events_total`
میں شمار کیا جاتا ہے۔

Включите JSON-файл `local_proxy` и включите прокси-сервер:

```json
"local_proxy": {
  "bind_addr": "127.0.0.1:9443",
  "telemetry_label": "dev-proxy",
  "guard_cache_key_hex": "0123456789ABCDEF0123456789ABCDEF0123456789ABCDEF0123456789ABCDEF",
  "emit_browser_manifest": true,
  "proxy_mode": "bridge",
  "prewarm_circuits": true,
  "max_streams_per_circuit": 64,
  "circuit_ttl_hint_secs": 300,
  "norito_bridge": {
    "spool_dir": "./storage/streaming/soranet_routes",
    "extension": "norito"
  },
  "kaigi_bridge": {
    "spool_dir": "./storage/streaming/soranet_routes",
    "extension": "norito",
    "room_policy": "public"
  }
}
```- `bind_addr` کنٹرول کرتا ہے کہ proxy کہاں Listen کرے (پورٹ `0` سے эфемерный
  پورٹ مانگیں)۔
- `telemetry_label` позволяет распространять прокси-серверы панелей мониторинга.
  выбор сеансов
- `guard_cache_key_hex` (اختیاری) прокси-сервер с ключом защиты кеша دکھانے دیتا ہے
  Поддержка CLI/SDK и синхронизация расширений браузера.
- `emit_browser_manifest` اس بات کو переключить манифест рукопожатия
  Используйте расширения ذخیرہ/verify کر سکیں۔
- `proxy_mode` может использовать прокси-сервер для подключения моста (`bridge`) یا صرف
  метаданные выдают SDK и схемы SoraNet (`metadata-only`).
  ڈیفالٹ `bridge` ہے؛ Манифест دینا ہو تو `metadata-only` سیٹ کریں۔
- `prewarm_circuits`, `max_streams_per_circuit`, `circuit_ttl_hint_secs`.
  подсказки Использование параллельных потоков Использование прокси-сервера
  повторное использование
- `car_bridge` (اختیاری) Для кэша архива CAR کی طرف اشارہ کرتا ہے۔ `extension`
  Суффикс اس کو کنٹرول کرتا ہے جو `*.car` غائب ہونے پر لگایا جاتا ہے؛ `allow_zst = true`
  سے `*.car.zst` براہ راست служить ہو سکتا ہے۔
- `kaigi_bridge` (اختیاری) Kaigi маршруты, прокси-сервер и выставление доступа. `room_policy`
  Используйте мост `public` или `authenticated` для клиентов браузера.
  Этикетки GAR چن سکیں۔
- `sorafs_cli fetch` `--local-proxy-mode=bridge|metadata-only`
  `--local-proxy-norito-spool=PATH` переопределяет формат JSON-файла.
  Режим выполнения, режим буферизации, режим работы, режим выполнения.
- `downgrade_remediation` (اختیاری) Возможность перехода на более раннюю версию
  Включено ہو تو آرکسٹریٹر релейная телеметрия и пакетное понижение версии دیکھتا ہے اور
  `window_secs` можно использовать `threshold` для подключения локального прокси `target_mode`
  (ڈیفالٹ `metadata-only`) جب понизил версию رک جائیں تو proxy
  `cooldown_secs` کے بعد `resume_mode` پر واپس آتا ہے۔ `modes` массив для конкретного
  роли реле تک محدود کرنے کے لیے استعمال کریں (ڈیفالٹ входные реле)۔

Режим прокси-моста может быть использован для служб приложений или служб приложений:

- **`norito`** — можно указать целевой поток потока `norito_bridge.spool_dir`, чтобы разрешить разрешение
  ہوتا ہے۔ цели کو санировать کیا جاتا ہے (обход и абсолютные пути نہیں)
  Расширение или расширение, настроенный суффикс, расширение полезной нагрузки.
  براؤزر کو کیا جاتا ہے۔
- **`car`** — целевые объекты потока `car_bridge.cache_dir` для разрешения проблемы.
  наследование расширения по умолчанию и сжатые полезные данные.
  `allow_zst` فعال نہ ہو۔ Мосты `STREAM_ACK_OK` کے ساتھ جواب دیتے ہیں
  Архивные байты или байты, проверка или конвейер, проверка

دونوں صورتوں میں прокси-тег кэша HMAC مہیا کرتا ہے (рукопожатие ключа охранного кэша
Коды причин телеметрии `norito_*` / `car_*`.
Панели мониторинга, отсутствующие файлы, сбои в очистке и т. д.
سمجھ سکیں۔`Orchestrator::local_proxy().await` چلتے ہوئے ручка и экспонирование کرتا ہے تاکہ
Сертификат PEM и манифест браузера.
ختم ہونے پر постепенное завершение работы

Включен прокси-сервер **manifest v2**. Сертификат на получение сертификата
Ключ защитного кэша для версии 2 или версии 2:

- `alpn` (`"sorafs-proxy/1"`) или массив `capabilities` для потока потока
  پروٹوکول کی تصدیق کر سکیں۔
- ہر рукопожатие کے لیے `session_id` или `cache_tagging` блокирование за сеанс
  сходство охраны и теги HMAC مشتق کیے جا سکیں۔
- схема подсказок по выбору защиты (`circuit`, `guard_selection`, `route_hints`)
  Интеграция потоков браузера Расширение расширенного пользовательского интерфейса Расширение возможностей
- `telemetry_v2` Инструменты для выборки проб/ручки конфиденциальности.
- ہر `STREAM_ACK_OK` میں `cache_tag_hex` شامل ہوتا ہے۔ کلائنٹس اسے
  `x-sorafs-cache-tag` заголовок зеркала выбор кэшированной защиты
  остальное میں зашифровано رہیں۔

Подмножество версии v1.

## 2. Семантика сбоя

Вы можете получить доступ к возможностям и проверкам бюджета.
ہے۔ В качестве примера можно привести:

1. **Несоблюдение требований (предполетная подготовка).** Возможности дальности действия истекли.
   реклама, устаревшая телеметрия и провайдеры, артефакт табло и журнал регистрации
   جاتا ہے اور планирование سے نکال دیا جاتا ہے۔ Сводка CLI `ineligible_providers`
   массив میں причины بھر دیتی ہیں تاکہ операторы журналы очистка کیے بغیر управление
   дрифт دیکھ سکیں۔
2. **Исчерпание времени выполнения.** Сбои поставщика при отслеживании ошибок جب
   `provider_failure_threshold` является поставщиком услуг, который может помочь вам в работе.
   `disabled` کر دیا جاتا ہے۔ Поставщики услуг `disabled` и другие поставщики услуг
   `MultiSourceError::NoHealthyProviders { last_error, chunk_index }` واپس کرتا ہے۔
3. **Детерминированные прерывания.** Возможные структурированные ошибки, которые могут возникнуть в результате:
   - `MultiSourceError::NoCompatibleProviders` — манифест фрагмента диапазона یا
     выравнивание مانگتا ہے جسے باقی провайдеры честь نہیں کر سکتے۔
   - `MultiSourceError::ExhaustedRetries` — бюджет повторных попыток на блок.
   - `MultiSourceError::ObserverFailed` — нижестоящие наблюдатели (потоковые перехватчики)
     نے проверенный фрагмент رد کر دیا۔

ہر ошибка, нарушающая индекс чанка اور جہاں ممکن ہو причина сбоя конечного провайдера
کے ساتھ آتا ہے۔ Блокировщики освобождения Блокировщики — Доступ к входным данным и повторным попыткам
Неисправность, отсутствие рекламы, телеметрия, здоровье поставщика, состояние здоровья.

### 2.1 Сохранение табло

جب `persist_path` کنفیگر ہو تو آرکسٹریٹر ہر رن کے بعد финальное табло لکھتا ہے۔
JSON-файл для отображения:

- `eligibility` (`eligible` یا `ineligible::<reason>`).
- `weight` (اس رن کے لیے назначен нормализованный вес).
- Метаданные `provider` (идентификатор, конечные точки, бюджет параллелизма).

Снимки табло, выпуск артефактов, архив, черный список
Развертывание с возможностью аудита

## 3. Телеметрия и отладка

### 3.1 Метрики Prometheus

آرکسٹریٹر `iroha_telemetry` کے ذریعے درج ذیل میٹرکس испускает کرتا ہے:| Метрическая | Этикетки | Описание |
|--------|--------|-------------|
| `sorafs_orchestrator_active_fetches` | `manifest_id`, `region` | оркестрованные выборки данных в полете |
| `sorafs_orchestrator_fetch_duration_ms` | `manifest_id`, `region` | Задержка сквозной выборки и гистограмма. |
| `sorafs_orchestrator_fetch_failures_total` | `manifest_id`, `region`, `reason` | Счетчик сбоев терминала (исчерпаны повторные попытки, нет провайдеров, сбой наблюдателя). |
| `sorafs_orchestrator_retries_total` | `manifest_id`, `provider`, `reason` | Счетчик повторных попыток провайдера |
| `sorafs_orchestrator_provider_failures_total` | `manifest_id`, `provider`, `reason` | сбои провайдера на уровне сеанса, счетчик или отключение |
| `sorafs_orchestrator_policy_events_total` | `region`, `stage`, `outcome`, `reason` | политика анонимности فیصلوں کی подсчет (встреча против отключения электроэнергии) этап اور резервная причина کے مطابق۔ |
| `sorafs_orchestrator_pq_ratio` | `region`, `stage` | Для SoraNet задана доля реле PQ и гистограмма. |
| `sorafs_orchestrator_pq_candidate_ratio` | `region`, `stage` | Снимок табло, коэффициенты питания реле PQ и гистограмма. |
| `sorafs_orchestrator_pq_deficit_ratio` | `region`, `stage` | дефицит политики (целевой показатель или фактическая доля PQ کے فرق) کا histogram۔ |
| `sorafs_orchestrator_classical_ratio` | И18НИ00000235Х, И18НИ00000236Х | ہر سیشن میں доля классического реле и гистограмма۔ |
| `sorafs_orchestrator_classical_selected` | `region`, `stage` | Для создания классических реле можно использовать гистограмму. |

Ручки производства, метрики и промежуточные информационные панели, а также метрики и промежуточные панели управления.
Ниже представлен план наблюдения за самолетом SF-6, следуя приведенному пункту:

1. **Активные выборки** – заполнение манометра в случае необходимости
2. **Коэффициент повторов** — счетчики `retry` отображают базовые показатели и предупреждения.
3. **Сбои поставщика** — 15 минут от поставщика услуг `session_failure > 0`.
   оповещения на пейджере۔

### 3.2 Цели структурированного журнала

Детерминированные цели и структурированные события могут быть такими:

- `telemetry::sorafs.fetch.lifecycle` — `start` и маркеры жизненного цикла `complete`.
  Подсчет количества фрагментов, повторных попыток и общей продолжительности.
- `telemetry::sorafs.fetch.retry` — события повтора (`provider`, `reason`, `attempts`).
- `telemetry::sorafs.fetch.provider_failure` — повторяющиеся ошибки کی وجہ سے отключены
  провайдеры۔
- `telemetry::sorafs.fetch.error` — `reason` — дополнительные метаданные поставщика.
  Неисправности терминалов۔

В потоках используется Norito конвейер журналов, перенаправление и реагирование на инциденты.
کو ایک ہی источник истины ملے۔ события жизненного цикла PQ/классический микс
И18НИ00000252Х, И18НИ00000253Х, И18НИ00000254Х
Счетчики и счетчики, а также информационные панели и очистка метрик
Проволока или провод Внедрение общедоступной версии с возможностью повторных событий жизненного цикла/повторных попыток.
Уровень журнала `info` Проверка ошибок терминала Уровень журнала `warn`

### 3.3 Сводки в формате JSON

`sorafs_cli fetch` В Rust SDK есть структурированное резюме, которое можно использовать:

- `provider_reports` при подсчете успехов/неуспехов при отключении поставщика.
- `chunk_receipts` позволяет получить доступ к чанковому провайдеру
- `retry_stats` или массивы `ineligible_providers`.Поставщики и отладчики Архив сводных файлов — квитанции
دیے گئے метаданные журнала سے براہ راست جڑتے ہیں۔

## 4. Операционный контрольный список

1. **Настройка этапа CI** `sorafs_fetch` — настройка целевой конфигурации.
   Просмотр соответствия требованиям `--scoreboard-out` دیں، اور پچھلے Release سے diff کریں۔
   Продвижение неподходящего поставщика.
2. **Выборка телеметрии из нескольких источников позволяет получать данные из нескольких источников.
   Возможности развертывания метрик `sorafs.fetch.*` и экспорта структурированных журналов.
   ہے۔ metrics کی عدم موجودگی عموماً اس بات کا اشارہ ہے کہ фасад оркестратора
   вызвать نہیں ہوئی۔
3. **Переопределяет установленные параметры** `--deny-provider` или `--boost-provider`.
   Доступ к JSON (вызов CLI) для журнала изменений и фиксации фиксации откаты کو
   переопределить возврат, чтобы сделать снимок экрана табло, чтобы сделать снимок экрана.
4. **Дымовые тесты и ограничения** повторные попытки бюджетов и ограничения провайдера.
   каноническое приспособление (`fixtures/sorafs_manifest/ci_sample/`) для получения дополнительной информации
   Проверьте детерминированность квитанций фрагментов.

Поэтапное развертывание и воспроизводимое развертывание
Реагирование на инциденты и телеметрия

### 4.1 Переопределение политики

Этап транспортировки/анонимности операторов базовая конфигурация PIN-код
کر سکتے ہیں، بس `policy_override.transport_policy` اور
`policy_override.anonymity_policy` کو اپنے `orchestrator` JSON Формат файла (یا
`sorafs_cli fetch` или `--transport-policy-override=` / `--anonymity-policy-override=`
پاس کریں)۔ جب override موجود ہو تو آرکسٹریٹر обычное отключение при отключении питания, а также пропустить
Для этого: выберите уровень PQ и выберите fetch `no providers`, чтобы получить доступ к нему.
ہوتا ہے بجائے خاموشی سے понизить версию ہونے کے۔ поведение по умолчанию پر واپسی اتنی ہی
آسان ہے جتنی переопределение полей, которые можно использовать