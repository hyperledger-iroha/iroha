# Use Cargo's native jobserver and libtest defaults for ordinary development.
# Memory-constrained evidence and packaging lanes set explicit local limits in
# their own scripts instead of serializing every workspace build and test.

[alias]
xtask = "run --package xtask --features dev-tools --bin xtask --"
