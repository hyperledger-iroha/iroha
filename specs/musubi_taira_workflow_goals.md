# Musubi to Taira first-release workflow goals

Status: integrated wallet and public-developer candidate passes focused native qualification; public faucet-policy rollout and end-to-end verification remain incomplete. Started 2026-09-13. Owner: Musubi / native wallet and contract deployment / Taira onboarding.

The user requested all findings in `dist/kotodama-musubi-demo/ux-review.md` be fixed at their roots, with no backward-compatibility aliases, wrappers or duplicate implementation, and a real end-to-end demo. This ledger records acceptance criteria, not release qualification.

| Goal | Outcome | Completion evidence | Status |
| --- | --- | --- | --- |
| G1 | Runnable contract scaffold and contract-only packages | Default contract template checks/tests/builds; no dummy library; exact library dependency semantics; public view boundary tested; source example matches scaffold | Complete locally |
| G2 | Useful output, explicit profiles, stable workspace locks | Named tests and full failures in human/JSON; artifact/interface/code identity and resolved profile; offline read-only source listing; publication evidence separate from workspace lock; no inert release flags | Complete locally |
| G3 | One package-aware deployment/view workflow | Shared native deployment owner; redundant standalone helper retired and consumers migrated; exact target/artifact/fee/permission preflight; immutable signed plan and status recovery; Applied receipt with height/scope and exact code/address readback | Implemented; live verification pending |
| G4 | Executable Taira onboarding and discoverable reads | Current network identity, faucet and fee instructions; paid namespace acquisition with exact alias scope; separate privileged code-management governance; read-only view MCP capability; English and maintained locales verified | Implemented; public faucet-policy route absent |
| G5 | Fresh end-to-end Taira recording | Supported account path, real deployment Applied evidence and deployed quote(3)=30; reproducible source, visible edits/scaffold, readable short video; runtime keys excluded | Local video complete; on-chain recording pending |
| G6 | Integrated developer wallet and transactions | Musubi creates/connects a private wallet outside the project, discovers/pins the exact network, shows account and XOR balance, onboards/funds with supported network policy, sends a quoted fee-paying transaction and recovers its exact saved hash; deployment uses the selected wallet without hand-built client configuration | Implemented and tested locally; live flow pending |
| G7 | Public developer admission and mandatory fees | Audit normal account, transfer, contract upload/deployment and calls against canonical fee enforcement; remove unnecessary frontend friction and repair protocol defects while preserving fee settlement and ownership authorization; prove behavior with focused regressions | Implemented and tested locally; public rollout pending |

## Design constraints

- Musubi.toml is the source/build authority. A public named target selects exact runtime client configuration and contract alias; signing custody remains outside the repository.
- A registry publication is independent of contract deployment. Consumer locks are not overwritten with publication-only graphs.
- Native orchestration lives in a focused service boundary shared by consumers, not copied into frontends or added to the network SDK as a filesystem/journal runtime.
- Registered, funded accounts may publish immutable verified contract code with normal transaction fees. Named alias deployment retains exact namespace ownership and `CanManageAccountAlias` scope, acquired through paid native domain setup. Privileged removal/delegation and its grant/revoke authority remain separately governed. Never reset shared Taira to make a demonstration pass.
- Submission is not completion. Retain exact hashes/signed envelopes through ambiguity; final success requires state-resolved Applied and contract/code readback.
- Read-only contract views are not new finalized transactions or credits to balances.
- Local checks and previously captured footage do not qualify live deployment or the full workspace suite.
- Every ordinary submitted transaction has a canonical fee payer and SDK-quoted signed fee limits. Default authority payment is an explicit product policy; sponsorship is selected with its exact immutable program. Onboarding and funding must also account for their payer rather than silently bypassing fees.
- The integrated wallet is a shared native service with thin CLI adapters, not another signing implementation or a shell-out to a second CLI. Developer setup must not require hand-authoring a client configuration file. Private custody and public project bindings remain separate.

## Current qualification

- The current combined Musubi suite passes **476 tests, zero failures and one existing ignore** in `dist/kotodama-musubi-demo/musubi-wallet-qualified-tests.log`. It includes wallet terminal-state guidance, context-preserving build/deployment continuation and per-transaction fee-payer review. The current Torii faucet filter also passes **14 tests, zero failures**, including unsigned policy/extractor coverage, in `dist/kotodama-musubi-demo/faucet-flow-final-tests.log`. The combined native Musubi/Iroha executable build passes (`wallet-public-recording-build.log`, 6m 56s). The rebuilt twelve-check public doctor passes 11 checks and fails only faucet-policy discovery with HTTP 404 (`dist/kotodama-musubi-demo/public-wallet-preview/doctor-current.json`). The fresh local and public-wallet captures use these exact executable images; local checks do not establish live deployment.
- The integrated `iroha_wallet` custody, transfer, namespace and onboarding candidate passes all 52 shared-wallet tests. The native CLI passes 35 focused doctor, onboarding, deadline and exact-check-surface tests. SDK query validation passes 34 tests, unsigned bootstrap five, and quoted-fee validation seven. Evidence is retained in `dist/kotodama-musubi-demo/wallet-shared-final-tests.log`, `wallet-cli-doctor-final-tests.log`, `wallet-sdk-query-final-tests.log`, `wallet-bootstrap-tests.log` and `wallet-fee-quote-tests.log`. Registry readers/signers select integrated default or explicit wallets; package publication inherits pinned workspace identity while retaining separate explicit storage policy. The pre-wallet counts below do not qualify these additions.
- The fresh executable creates `coffee-demo` custody against public Taira and pins the discovered genesis identity/profile 369. Its funding command exits with the explicit policy-route HTTP 404 before creating an operation journal or submitting a transaction. The accepted capture is `dist/kotodama-musubi-demo/public-wallet-preview/capture-absolute/`; the earlier relative-path recorder recipe failure is retained separately in `capture/`. No funding, namespace purchase or deployment is claimed.
- At 14:31:49 UTC on 2026-09-13, `/v1/accounts/capabilities` returned HTTP 200 with exact NetworkId and profile 369; `/v1/accounts/faucet/policy` returned HTTP 404. The then-current native basic doctor passed all ten route/tool checks. The new candidate requires twelve basic checks, including SDK-validated account capabilities and faucet policy; the observed missing policy must fail current readiness. Automatic wallet funding remains blocked by the missing deployed faucet-policy route. This is a rollout gap, not a wrong signer or a reason to reset the network. Evidence: `dist/kotodama-musubi-demo/taira-live-audit/wallet-bootstrap-probe.json` and `doctor-wallet-flow.json`. No live wallet funding, namespace purchase, deployment or on-chain video is claimed.

- Core authorization and exact committed block replay pass ten focused tests, including both Initial and bundled executors and both parallel apply modes. The current full default-executor suite passes 161 tests (`dist/contract-management-executor-full-tests.log`). The generated four-peer Taira genesis/config seed test passes.
- Kagami passes all five focused NEVO genesis tests, including exact retained bytes and success/error address-profile restoration (`dist/contract-management-nevo-fixture-tests-qualified.log`). The broader release module passes 16 of 19: three failures share a Cargo.lock pin already mismatched at HEAD, recorded in `dist/contract-management-rollout-pin-baseline.json`. Those unrelated rollout pins remain unchanged; this is not complete release-tool qualification.
- The native deployment service passes 32 tests, including durable progress ordering, exact recovery, historical-authority inspection, cancellation, HTTP pagination and incomplete-route rejection. SDK exact finality classification passes five. Root CLI presentation/network checks cover artifact substitution, fee bounds, canonical target names, network-file size, progress redaction and one-document JSON output.
- The new read-only MCP view tool passes three focused tests. Authored onboarding, Musubi and contract guides pass the maintained 20-locale and content-policy checks. All nine exact local Musubi tutorial commands pass with the rebuilt binary and authored source blocks.
- The pre-wallet integrated Musubi checkpoint passes 460 tests with one existing ignore. The full Kotodama library suite passes 1,093 tests, and all 52 focused IVM invocation/return tests pass. Public calls use the canonical argument record and current-caller host dispatch; the retired JSON override is removed. Ordinary Taira onboarding passes 16 tests, the native faucet/reset regression filter passes 17, and the retained native contract CLI suite passes 33. Default-directory `init` and exact onboarding fee review regressions pass.
- The fresh final-binary local recording is `dist/kotodama-musubi-demo/local-workflow-current/coffee-rewards-local.mp4` (60 seconds, 1920×1080 H.264). All five captured command/directory steps and four quote tests pass; the source ZIP matches the captured contract, and all eight delivery hashes plus ZIP integrity verify. The separate `public-wallet-preview/public-wallet-preview.mp4` shows wallet creation and the actual funding-policy HTTP 404; it does not represent completed funding or deployment. Captures retain actual output with edited presentation pacing and no runtime signing material.
- The pre-wallet 60-second local video at `dist/kotodama-musubi-demo/local-workflow/coffee-rewards-local.mp4` uses its recorded rebuilt binary and actual `new`, `cd`, `check`, `test` and `build` output. All four public quote cases pass. Source, lock and compiled artifacts match byte-for-byte across two workspace roots. The neutral capture excludes home paths from visible output; H.264 playback, all eight delivery-file hashes and source-ZIP integrity pass. Captions, transcript and exact provenance accompany the video. Edited pacing is explicit.
- The full source-budget gate remains failing with 244 findings across the candidate and recorded baseline, including pre-existing overages and reduced files requiring baseline ratchets. New local-workflow tests are extracted below the 3,000-line limit; no budget exceptions are expanded. Scoped formatting, workspace target inventory, codec and historical-archive checks pass; full-workspace runtime qualification is not claimed.

## Previous blocked audit and resumed scope

The user subsequently identified the missing built-in wallet and transaction workflow and explicitly requested its completion. The previous conclusion that no useful local action remained was too narrow: account creation/connection, funding, balance, and fee-paying transaction UX are part of the developer journey. G6/G7 reopen that local implementation work. The existing app goal remains unfinished; an attempt to create the newly requested goal was refused because only one unfinished goal may exist. This ledger adds the acceptance criteria without falsely completing or replacing the original end-to-end objective.

The earlier three-turn audit retained missing external client input and HTTP 502 evidence in `dist/kotodama-musubi-demo/taira-live-audit/blocked-audit-inputs.json`, `doctor-recheck.json` and `doctor-recheck-stderr.txt`. That blocker description is historical: integrated wallet creation removes hand-authored client configuration from the newcomer path, and the earlier ten-check basic Taira scope recovered. The rebuilt twelve-check wallet-aware doctor now confirms 11 checks pass and faucet-policy discovery alone fails with HTTP 404. The absent public route remains the current observed rollout blocker.

The focused wallet/frontend qualification is complete. End-to-end completion requires deployment of the supported account/faucet and admission policy, and an authorized live developer flow: create or select runtime custody, fund XOR, acquire a namespace, retain and apply the exact deployment transactions, verify code/alias readback, call deployed `quote(3) = 30`, and record the result. Keep keys and tokens outside repository files and messages. No public reset is implied or requested, and local checks or footage do not satisfy these live acceptance criteria.

`Musubi.networks.toml` pins each named target's exact `network-id` and
`chain-discriminant`, with a public reference to the runtime `config` file and an explicit `fee`
payer selection. Native SDK fee quotes supply the exact signed charge limits. Contract aliases are keyed by `namespace/package::target` to keep workspace
members independent. The bindings and their lock never enter source packages.
Registry publication writes `target/package/Musubi.publish.lock`; local builds
retain `Musubi.lock`. Deployment journals retain exact signed envelopes, terminal
failure or Applied evidence, and code readback under `target/deploy/`. Explicit
cancellation is limited to fully unattempted local plans; attempted ambiguity
always retains exact-hash recovery. Read-only inspection authenticates historical
signed plans independently of the current reader authority, on the same exact
network; executing or resuming still requires the original signing authority.
Human deployment progress shows the authenticated plan before dispatch, exact
stage hashes during submit or recovery, durable Applied evidence and readback.
Progress uses the same redaction boundary as final output; JSON remains one final
document. Progress cannot change replay or completion decisions.
